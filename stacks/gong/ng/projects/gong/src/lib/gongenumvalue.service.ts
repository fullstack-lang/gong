// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GongEnumValueAPI } from './gongenumvalue-api';
import { GongEnumValueDB } from './gongenumvalue-db';

@Injectable({
  providedIn: 'root'
})
export class GongEnumValueService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GongEnumValueServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private gongenumvaluesUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/stacks/gong/go/v1/gongenumvalues';

  constructor(
    private http: HttpClient
  ) { }

  /** GET gongenumvalues from the server */
  getGongEnumValues(): Observable<GongEnumValueDB[]> {
    return this.http.get<GongEnumValueDB[]>(this.gongenumvaluesUrl)
      .pipe(
        tap(_ => this.log('fetched gongenumvalues')),
        catchError(this.handleError<GongEnumValueDB[]>('getGongEnumValues', []))
      );
  }

  /** GET gongenumvalue by id. Will 404 if id not found */
  getGongEnumValue(id: number): Observable<GongEnumValueDB> {
    const url = `${this.gongenumvaluesUrl}/${id}`;
    return this.http.get<GongEnumValueDB>(url).pipe(
      tap(_ => this.log(`fetched gongenumvalue id=${id}`)),
      catchError(this.handleError<GongEnumValueDB>(`getGongEnumValue id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new gongenumvalue to the server */
  postGongEnumValue(gongenumvalueAPI: GongEnumValueAPI): Observable<GongEnumValueDB> {
    return this.http.post<GongEnumValueDB>(this.gongenumvaluesUrl, gongenumvalueAPI, this.httpOptions).pipe(
      tap((newGongEnumValue: GongEnumValueDB) => {})
    );
  }

  /** DELETE: delete the gongenumvaluedb from the server */
  deleteGongEnumValue(gongenumvaluedb: GongEnumValueDB | number): Observable<GongEnumValueDB> {
    const id = typeof gongenumvaluedb === 'number' ? gongenumvaluedb : gongenumvaluedb.ID;
    const url = `${this.gongenumvaluesUrl}/${id}`;

    return this.http.delete<GongEnumValueDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted gongenumvaluedb id=${id}`)),
      catchError(this.handleError<GongEnumValueDB>('deleteGongEnumValue'))
    );
  }

  /** PUT: update the gongenumvaluedb on the server */
  updateGongEnumValue(gongenumvaluedb: GongEnumValueDB): Observable<GongEnumValueDB> {
    const id = typeof gongenumvaluedb === 'number' ? gongenumvaluedb : gongenumvaluedb.ID;
    const url = `${this.gongenumvaluesUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)
    let _GongEnum_GongEnumValues_reverse = gongenumvaluedb.GongEnum_GongEnumValues_reverse
    gongenumvaluedb.GongEnum_GongEnumValues_reverse = {}

    return this.http.put(url, gongenumvaluedb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        gongenumvaluedb.GongEnum_GongEnumValues_reverse = _GongEnum_GongEnumValues_reverse
        this.log(`updated gongenumvaluedb id=${gongenumvaluedb.ID}`)
      }),
      catchError(this.handleError<GongEnumValueDB>('updateGongEnumValue'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
