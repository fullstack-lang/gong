// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { PointerToGongStructFieldAPI } from './pointertogongstructfield-api';
import { PointerToGongStructFieldDB } from './pointertogongstructfield-db';

@Injectable({
  providedIn: 'root'
})
export class PointerToGongStructFieldService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  PointerToGongStructFieldServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private pointertogongstructfieldsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/stacks/gong/go/v1/pointertogongstructfields';

  constructor(
    private http: HttpClient
  ) { }

  /** GET pointertogongstructfields from the server */
  getPointerToGongStructFields(): Observable<PointerToGongStructFieldDB[]> {
    return this.http.get<PointerToGongStructFieldDB[]>(this.pointertogongstructfieldsUrl)
      .pipe(
        tap(_ => this.log('fetched pointertogongstructfields')),
        catchError(this.handleError<PointerToGongStructFieldDB[]>('getPointerToGongStructFields', []))
      );
  }

  /** GET pointertogongstructfield by id. Will 404 if id not found */
  getPointerToGongStructField(id: number): Observable<PointerToGongStructFieldDB> {
    const url = `${this.pointertogongstructfieldsUrl}/${id}`;
    return this.http.get<PointerToGongStructFieldDB>(url).pipe(
      tap(_ => this.log(`fetched pointertogongstructfield id=${id}`)),
      catchError(this.handleError<PointerToGongStructFieldDB>(`getPointerToGongStructField id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new pointertogongstructfield to the server */
  postPointerToGongStructField(pointertogongstructfieldAPI: PointerToGongStructFieldAPI): Observable<PointerToGongStructFieldDB> {
    return this.http.post<PointerToGongStructFieldDB>(this.pointertogongstructfieldsUrl, pointertogongstructfieldAPI, this.httpOptions).pipe(
      tap((newPointerToGongStructField: PointerToGongStructFieldDB) => {})
    );
  }

  /** DELETE: delete the pointertogongstructfielddb from the server */
  deletePointerToGongStructField(pointertogongstructfielddb: PointerToGongStructFieldDB | number): Observable<PointerToGongStructFieldDB> {
    const id = typeof pointertogongstructfielddb === 'number' ? pointertogongstructfielddb : pointertogongstructfielddb.ID;
    const url = `${this.pointertogongstructfieldsUrl}/${id}`;

    return this.http.delete<PointerToGongStructFieldDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted pointertogongstructfielddb id=${id}`)),
      catchError(this.handleError<PointerToGongStructFieldDB>('deletePointerToGongStructField'))
    );
  }

  /** PUT: update the pointertogongstructfielddb on the server */
  updatePointerToGongStructField(pointertogongstructfielddb: PointerToGongStructFieldDB): Observable<PointerToGongStructFieldDB> {
    const id = typeof pointertogongstructfielddb === 'number' ? pointertogongstructfielddb : pointertogongstructfielddb.ID;
    const url = `${this.pointertogongstructfieldsUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)
    pointertogongstructfielddb.GongStruct = {}
    let _GongStruct_PointerToGongStructFields_reverse = pointertogongstructfielddb.GongStruct_PointerToGongStructFields_reverse
    pointertogongstructfielddb.GongStruct_PointerToGongStructFields_reverse = {}

    return this.http.put(url, pointertogongstructfielddb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        pointertogongstructfielddb.GongStruct_PointerToGongStructFields_reverse = _GongStruct_PointerToGongStructFields_reverse
        this.log(`updated pointertogongstructfielddb id=${pointertogongstructfielddb.ID}`)
      }),
      catchError(this.handleError<PointerToGongStructFieldDB>('updatePointerToGongStructField'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
