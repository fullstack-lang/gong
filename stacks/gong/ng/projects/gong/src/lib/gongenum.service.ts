// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GongEnumAPI } from './gongenum-api';
import { GongEnumDB } from './gongenum-db';

@Injectable({
  providedIn: 'root'
})
export class GongEnumService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GongEnumServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private gongenumsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/stacks/gong/go/v1/gongenums';

  constructor(
    private http: HttpClient
  ) { }

  /** GET gongenums from the server */
  getGongEnums(): Observable<GongEnumDB[]> {
    return this.http.get<GongEnumDB[]>(this.gongenumsUrl)
      .pipe(
        tap(_ => this.log('fetched gongenums')),
        catchError(this.handleError<GongEnumDB[]>('getGongEnums', []))
      );
  }

  /** GET gongenum by id. Will 404 if id not found */
  getGongEnum(id: number): Observable<GongEnumDB> {
    const url = `${this.gongenumsUrl}/${id}`;
    return this.http.get<GongEnumDB>(url).pipe(
      tap(_ => this.log(`fetched gongenum id=${id}`)),
      catchError(this.handleError<GongEnumDB>(`getGongEnum id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new gongenum to the server */
  postGongEnum(gongenumAPI: GongEnumAPI): Observable<GongEnumDB> {
    return this.http.post<GongEnumDB>(this.gongenumsUrl, gongenumAPI, this.httpOptions).pipe(
      tap((newGongEnum: GongEnumDB) => {})
    );
  }

  /** DELETE: delete the gongenumdb from the server */
  deleteGongEnum(gongenumdb: GongEnumDB | number): Observable<GongEnumDB> {
    const id = typeof gongenumdb === 'number' ? gongenumdb : gongenumdb.ID;
    const url = `${this.gongenumsUrl}/${id}`;

    return this.http.delete<GongEnumDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted gongenumdb id=${id}`)),
      catchError(this.handleError<GongEnumDB>('deleteGongEnum'))
    );
  }

  /** PUT: update the gongenumdb on the server */
  updateGongEnum(gongenumdb: GongEnumDB): Observable<GongEnumDB> {
    const id = typeof gongenumdb === 'number' ? gongenumdb : gongenumdb.ID;
    const url = `${this.gongenumsUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)
    gongenumdb.GongEnumValues = []

    return this.http.put(url, gongenumdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated gongenumdb id=${gongenumdb.ID}`)
      }),
      catchError(this.handleError<GongEnumDB>('updateGongEnum'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
