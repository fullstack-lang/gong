// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { ModelPkgAPI } from './modelpkg-api';
import { ModelPkgDB } from './modelpkg-db';

@Injectable({
  providedIn: 'root'
})
export class ModelPkgService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ModelPkgServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private modelpkgsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/stacks/gong/go/v1/modelpkgs';

  constructor(
    private http: HttpClient
  ) { }

  /** GET modelpkgs from the server */
  getModelPkgs(): Observable<ModelPkgDB[]> {
    return this.http.get<ModelPkgDB[]>(this.modelpkgsUrl)
      .pipe(
        tap(_ => this.log('fetched modelpkgs')),
        catchError(this.handleError<ModelPkgDB[]>('getModelPkgs', []))
      );
  }

  /** GET modelpkg by id. Will 404 if id not found */
  getModelPkg(id: number): Observable<ModelPkgDB> {
    const url = `${this.modelpkgsUrl}/${id}`;
    return this.http.get<ModelPkgDB>(url).pipe(
      tap(_ => this.log(`fetched modelpkg id=${id}`)),
      catchError(this.handleError<ModelPkgDB>(`getModelPkg id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new modelpkg to the server */
  postModelPkg(modelpkgAPI: ModelPkgAPI): Observable<ModelPkgDB> {
    return this.http.post<ModelPkgDB>(this.modelpkgsUrl, modelpkgAPI, this.httpOptions).pipe(
      tap((newModelPkg: ModelPkgDB) => {})
    );
  }

  /** DELETE: delete the modelpkgdb from the server */
  deleteModelPkg(modelpkgdb: ModelPkgDB | number): Observable<ModelPkgDB> {
    const id = typeof modelpkgdb === 'number' ? modelpkgdb : modelpkgdb.ID;
    const url = `${this.modelpkgsUrl}/${id}`;

    return this.http.delete<ModelPkgDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted modelpkgdb id=${id}`)),
      catchError(this.handleError<ModelPkgDB>('deleteModelPkg'))
    );
  }

  /** PUT: update the modelpkgdb on the server */
  updateModelPkg(modelpkgdb: ModelPkgDB): Observable<ModelPkgDB> {
    const id = typeof modelpkgdb === 'number' ? modelpkgdb : modelpkgdb.ID;
    const url = `${this.modelpkgsUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)

    return this.http.put(url, modelpkgdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated modelpkgdb id=${modelpkgdb.ID}`)
      }),
      catchError(this.handleError<ModelPkgDB>('updateModelPkg'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
