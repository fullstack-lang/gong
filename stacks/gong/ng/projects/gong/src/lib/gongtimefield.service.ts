// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GongTimeFieldAPI } from './gongtimefield-api';
import { GongTimeFieldDB } from './gongtimefield-db';

@Injectable({
  providedIn: 'root'
})
export class GongTimeFieldService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GongTimeFieldServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private gongtimefieldsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/stacks/gong/go/v1/gongtimefields';

  constructor(
    private http: HttpClient
  ) { }

  /** GET gongtimefields from the server */
  getGongTimeFields(): Observable<GongTimeFieldDB[]> {
    return this.http.get<GongTimeFieldDB[]>(this.gongtimefieldsUrl)
      .pipe(
        tap(_ => this.log('fetched gongtimefields')),
        catchError(this.handleError<GongTimeFieldDB[]>('getGongTimeFields', []))
      );
  }

  /** GET gongtimefield by id. Will 404 if id not found */
  getGongTimeField(id: number): Observable<GongTimeFieldDB> {
    const url = `${this.gongtimefieldsUrl}/${id}`;
    return this.http.get<GongTimeFieldDB>(url).pipe(
      tap(_ => this.log(`fetched gongtimefield id=${id}`)),
      catchError(this.handleError<GongTimeFieldDB>(`getGongTimeField id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new gongtimefield to the server */
  postGongTimeField(gongtimefieldAPI: GongTimeFieldAPI): Observable<GongTimeFieldDB> {
    return this.http.post<GongTimeFieldDB>(this.gongtimefieldsUrl, gongtimefieldAPI, this.httpOptions).pipe(
      tap((newGongTimeField: GongTimeFieldDB) => {})
    );
  }

  /** DELETE: delete the gongtimefielddb from the server */
  deleteGongTimeField(gongtimefielddb: GongTimeFieldDB | number): Observable<GongTimeFieldDB> {
    const id = typeof gongtimefielddb === 'number' ? gongtimefielddb : gongtimefielddb.ID;
    const url = `${this.gongtimefieldsUrl}/${id}`;

    return this.http.delete<GongTimeFieldDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted gongtimefielddb id=${id}`)),
      catchError(this.handleError<GongTimeFieldDB>('deleteGongTimeField'))
    );
  }

  /** PUT: update the gongtimefielddb on the server */
  updateGongTimeField(gongtimefielddb: GongTimeFieldDB): Observable<GongTimeFieldDB> {
    const id = typeof gongtimefielddb === 'number' ? gongtimefielddb : gongtimefielddb.ID;
    const url = `${this.gongtimefieldsUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)

    return this.http.put(url, gongtimefielddb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated gongtimefielddb id=${gongtimefielddb.ID}`)
      }),
      catchError(this.handleError<GongTimeFieldDB>('updateGongTimeField'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
