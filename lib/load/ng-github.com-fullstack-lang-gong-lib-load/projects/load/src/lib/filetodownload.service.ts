// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { FileToDownloadAPI } from './filetodownload-api'
import { FileToDownload, CopyFileToDownloadToFileToDownloadAPI } from './filetodownload'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class FileToDownloadService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FileToDownloadServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private filetodownloadsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.filetodownloadsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/load/go/v1/filetodownloads';
  }

  /** GET filetodownloads from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI[]> {
    return this.getFileToDownloads(Name, frontRepo)
  }
  getFileToDownloads(Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<FileToDownloadAPI[]>(this.filetodownloadsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<FileToDownloadAPI[]>('getFileToDownloads', []))
      );
  }

  /** GET filetodownload by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {
    return this.getFileToDownload(id, Name, frontRepo)
  }
  getFileToDownload(id: number, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.filetodownloadsUrl}/${id}`;
    return this.http.get<FileToDownloadAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched filetodownload id=${id}`)),
      catchError(this.handleError<FileToDownloadAPI>(`getFileToDownload id=${id}`))
    );
  }

  // postFront copy filetodownload to a version with encoded pointers and post to the back
  postFront(filetodownload: FileToDownload, Name: string): Observable<FileToDownloadAPI> {
    let filetodownloadAPI = new FileToDownloadAPI
    CopyFileToDownloadToFileToDownloadAPI(filetodownload, filetodownloadAPI)
    const id = typeof filetodownloadAPI === 'number' ? filetodownloadAPI : filetodownloadAPI.ID
    const url = `${this.filetodownloadsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FileToDownloadAPI>(url, filetodownloadAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FileToDownloadAPI>('postFileToDownload'))
    );
  }
  
  /** POST: add a new filetodownload to the server */
  post(filetodownloaddb: FileToDownloadAPI, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {
    return this.postFileToDownload(filetodownloaddb, Name, frontRepo)
  }
  postFileToDownload(filetodownloaddb: FileToDownloadAPI, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FileToDownloadAPI>(this.filetodownloadsUrl, filetodownloaddb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted filetodownloaddb id=${filetodownloaddb.ID}`)
      }),
      catchError(this.handleError<FileToDownloadAPI>('postFileToDownload'))
    );
  }

  /** DELETE: delete the filetodownloaddb from the server */
  delete(filetodownloaddb: FileToDownloadAPI | number, Name: string): Observable<FileToDownloadAPI> {
    return this.deleteFileToDownload(filetodownloaddb, Name)
  }
  deleteFileToDownload(filetodownloaddb: FileToDownloadAPI | number, Name: string): Observable<FileToDownloadAPI> {
    const id = typeof filetodownloaddb === 'number' ? filetodownloaddb : filetodownloaddb.ID;
    const url = `${this.filetodownloadsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FileToDownloadAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted filetodownloaddb id=${id}`)),
      catchError(this.handleError<FileToDownloadAPI>('deleteFileToDownload'))
    );
  }

  // updateFront copy filetodownload to a version with encoded pointers and update to the back
  updateFront(filetodownload: FileToDownload, Name: string): Observable<FileToDownloadAPI> {
    let filetodownloadAPI = new FileToDownloadAPI
    CopyFileToDownloadToFileToDownloadAPI(filetodownload, filetodownloadAPI)
    const id = typeof filetodownloadAPI === 'number' ? filetodownloadAPI : filetodownloadAPI.ID
    const url = `${this.filetodownloadsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<FileToDownloadAPI>(url, filetodownloadAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FileToDownloadAPI>('updateFileToDownload'))
    );
  }

  /** PUT: update the filetodownloaddb on the server */
  update(filetodownloaddb: FileToDownloadAPI, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {
    return this.updateFileToDownload(filetodownloaddb, Name, frontRepo)
  }
  updateFileToDownload(filetodownloaddb: FileToDownloadAPI, Name: string, frontRepo: FrontRepo): Observable<FileToDownloadAPI> {
    const id = typeof filetodownloaddb === 'number' ? filetodownloaddb : filetodownloaddb.ID;
    const url = `${this.filetodownloadsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FileToDownloadAPI>(url, filetodownloaddb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated filetodownloaddb id=${filetodownloaddb.ID}`)
      }),
      catchError(this.handleError<FileToDownloadAPI>('updateFileToDownload'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FileToDownloadService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FileToDownloadService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
