// generated code, do not edit
// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { BufferGeometryAPI } from './buffergeometry-api'
import { BufferGeometry, CopyBufferGeometryToBufferGeometryAPI } from './buffergeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { Vector3API } from './vector3-api'
import { TriangleAPI } from './triangle-api'

@Injectable({
  providedIn: 'root'
})
export class BufferGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  BufferGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private buffergeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.buffergeometrysUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/buffergeometrys';
  }

  // updateFront copy buffergeometry to a version with encoded pointers and update to the back
  updateFront(buffergeometry: BufferGeometry, Name: string): Observable<BufferGeometryAPI> {
    let buffergeometryAPI = new BufferGeometryAPI
    CopyBufferGeometryToBufferGeometryAPI(buffergeometry, buffergeometryAPI)
    const id = typeof buffergeometryAPI === 'number' ? buffergeometryAPI : buffergeometryAPI.ID
    const url = `${this.buffergeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<BufferGeometryAPI>(url, buffergeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BufferGeometryAPI>('updateBufferGeometry'))
    );
  }

  /** PUT: update the buffergeometrydb on the server */
  update(buffergeometrydb: BufferGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BufferGeometryAPI> {
    return this.updateBufferGeometry(buffergeometrydb, Name, frontRepo)
  }
  updateBufferGeometry(buffergeometrydb: BufferGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BufferGeometryAPI> {
    const id = typeof buffergeometrydb === 'number' ? buffergeometrydb : buffergeometrydb.ID;
    const url = `${this.buffergeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<BufferGeometryAPI>(url, buffergeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated buffergeometrydb id=${buffergeometrydb.ID}`)
      }),
      catchError(this.handleError<BufferGeometryAPI>('updateBufferGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in BufferGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("BufferGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
