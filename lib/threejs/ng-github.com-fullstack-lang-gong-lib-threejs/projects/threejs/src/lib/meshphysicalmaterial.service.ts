// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { MeshPhysicalMaterialAPI } from './meshphysicalmaterial-api'
import { MeshPhysicalMaterial, CopyMeshPhysicalMaterialToMeshPhysicalMaterialAPI } from './meshphysicalmaterial'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class MeshPhysicalMaterialService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  MeshPhysicalMaterialServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private meshphysicalmaterialsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.meshphysicalmaterialsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/meshphysicalmaterials';
  }

  /** GET meshphysicalmaterials from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI[]> {
    return this.getMeshPhysicalMaterials(Name, frontRepo)
  }
  getMeshPhysicalMaterials(Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<MeshPhysicalMaterialAPI[]>(this.meshphysicalmaterialsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<MeshPhysicalMaterialAPI[]>('getMeshPhysicalMaterials', []))
      );
  }

  /** GET meshphysicalmaterial by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {
    return this.getMeshPhysicalMaterial(id, Name, frontRepo)
  }
  getMeshPhysicalMaterial(id: number, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.meshphysicalmaterialsUrl}/${id}`;
    return this.http.get<MeshPhysicalMaterialAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched meshphysicalmaterial id=${id}`)),
      catchError(this.handleError<MeshPhysicalMaterialAPI>(`getMeshPhysicalMaterial id=${id}`))
    );
  }

  // postFront copy meshphysicalmaterial to a version with encoded pointers and post to the back
  postFront(meshphysicalmaterial: MeshPhysicalMaterial, Name: string): Observable<MeshPhysicalMaterialAPI> {
    let meshphysicalmaterialAPI = new MeshPhysicalMaterialAPI
    CopyMeshPhysicalMaterialToMeshPhysicalMaterialAPI(meshphysicalmaterial, meshphysicalmaterialAPI)
    const id = typeof meshphysicalmaterialAPI === 'number' ? meshphysicalmaterialAPI : meshphysicalmaterialAPI.ID
    const url = `${this.meshphysicalmaterialsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MeshPhysicalMaterialAPI>(url, meshphysicalmaterialAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MeshPhysicalMaterialAPI>('postMeshPhysicalMaterial'))
    );
  }

  /** POST: add a new meshphysicalmaterial to the server */
  post(meshphysicalmaterialdb: MeshPhysicalMaterialAPI, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {
    return this.postMeshPhysicalMaterial(meshphysicalmaterialdb, Name, frontRepo)
  }
  postMeshPhysicalMaterial(meshphysicalmaterialdb: MeshPhysicalMaterialAPI, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MeshPhysicalMaterialAPI>(this.meshphysicalmaterialsUrl, meshphysicalmaterialdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted meshphysicalmaterialdb id=${meshphysicalmaterialdb.ID}`)
      }),
      catchError(this.handleError<MeshPhysicalMaterialAPI>('postMeshPhysicalMaterial'))
    );
  }

  /** DELETE: delete the meshphysicalmaterialdb from the server */
  delete(meshphysicalmaterialdb: MeshPhysicalMaterialAPI | number, Name: string): Observable<MeshPhysicalMaterialAPI> {
    return this.deleteMeshPhysicalMaterial(meshphysicalmaterialdb, Name)
  }
  deleteMeshPhysicalMaterial(meshphysicalmaterialdb: MeshPhysicalMaterialAPI | number, Name: string): Observable<MeshPhysicalMaterialAPI> {
    const id = typeof meshphysicalmaterialdb === 'number' ? meshphysicalmaterialdb : meshphysicalmaterialdb.ID;
    const url = `${this.meshphysicalmaterialsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<MeshPhysicalMaterialAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted meshphysicalmaterialdb id=${id}`)),
      catchError(this.handleError<MeshPhysicalMaterialAPI>('deleteMeshPhysicalMaterial'))
    );
  }

  // updateFront copy meshphysicalmaterial to a version with encoded pointers and update to the back
  updateFront(meshphysicalmaterial: MeshPhysicalMaterial, Name: string): Observable<MeshPhysicalMaterialAPI> {
    let meshphysicalmaterialAPI = new MeshPhysicalMaterialAPI
    CopyMeshPhysicalMaterialToMeshPhysicalMaterialAPI(meshphysicalmaterial, meshphysicalmaterialAPI)
    const id = typeof meshphysicalmaterialAPI === 'number' ? meshphysicalmaterialAPI : meshphysicalmaterialAPI.ID
    const url = `${this.meshphysicalmaterialsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<MeshPhysicalMaterialAPI>(url, meshphysicalmaterialAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MeshPhysicalMaterialAPI>('updateMeshPhysicalMaterial'))
    );
  }

  /** PUT: update the meshphysicalmaterialdb on the server */
  update(meshphysicalmaterialdb: MeshPhysicalMaterialAPI, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {
    return this.updateMeshPhysicalMaterial(meshphysicalmaterialdb, Name, frontRepo)
  }
  updateMeshPhysicalMaterial(meshphysicalmaterialdb: MeshPhysicalMaterialAPI, Name: string, frontRepo: FrontRepo): Observable<MeshPhysicalMaterialAPI> {
    const id = typeof meshphysicalmaterialdb === 'number' ? meshphysicalmaterialdb : meshphysicalmaterialdb.ID;
    const url = `${this.meshphysicalmaterialsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<MeshPhysicalMaterialAPI>(url, meshphysicalmaterialdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated meshphysicalmaterialdb id=${meshphysicalmaterialdb.ID}`)
      }),
      catchError(this.handleError<MeshPhysicalMaterialAPI>('updateMeshPhysicalMaterial'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in MeshPhysicalMaterialService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("MeshPhysicalMaterialService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
