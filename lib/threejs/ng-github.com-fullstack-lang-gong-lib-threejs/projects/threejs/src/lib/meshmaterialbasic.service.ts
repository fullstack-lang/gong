// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { MeshMaterialBasicAPI } from './meshmaterialbasic-api'
import { MeshMaterialBasic, CopyMeshMaterialBasicToMeshMaterialBasicAPI } from './meshmaterialbasic'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class MeshMaterialBasicService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  MeshMaterialBasicServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private meshmaterialbasicsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.meshmaterialbasicsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/meshmaterialbasics';
  }

  /** GET meshmaterialbasics from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI[]> {
    return this.getMeshMaterialBasics(Name, frontRepo)
  }
  getMeshMaterialBasics(Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<MeshMaterialBasicAPI[]>(this.meshmaterialbasicsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<MeshMaterialBasicAPI[]>('getMeshMaterialBasics', []))
      );
  }

  /** GET meshmaterialbasic by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {
    return this.getMeshMaterialBasic(id, Name, frontRepo)
  }
  getMeshMaterialBasic(id: number, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.meshmaterialbasicsUrl}/${id}`;
    return this.http.get<MeshMaterialBasicAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched meshmaterialbasic id=${id}`)),
      catchError(this.handleError<MeshMaterialBasicAPI>(`getMeshMaterialBasic id=${id}`))
    );
  }

  // postFront copy meshmaterialbasic to a version with encoded pointers and post to the back
  postFront(meshmaterialbasic: MeshMaterialBasic, Name: string): Observable<MeshMaterialBasicAPI> {
    let meshmaterialbasicAPI = new MeshMaterialBasicAPI
    CopyMeshMaterialBasicToMeshMaterialBasicAPI(meshmaterialbasic, meshmaterialbasicAPI)
    const id = typeof meshmaterialbasicAPI === 'number' ? meshmaterialbasicAPI : meshmaterialbasicAPI.ID
    const url = `${this.meshmaterialbasicsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MeshMaterialBasicAPI>(url, meshmaterialbasicAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MeshMaterialBasicAPI>('postMeshMaterialBasic'))
    );
  }

  /** POST: add a new meshmaterialbasic to the server */
  post(meshmaterialbasicdb: MeshMaterialBasicAPI, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {
    return this.postMeshMaterialBasic(meshmaterialbasicdb, Name, frontRepo)
  }
  postMeshMaterialBasic(meshmaterialbasicdb: MeshMaterialBasicAPI, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MeshMaterialBasicAPI>(this.meshmaterialbasicsUrl, meshmaterialbasicdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted meshmaterialbasicdb id=${meshmaterialbasicdb.ID}`)
      }),
      catchError(this.handleError<MeshMaterialBasicAPI>('postMeshMaterialBasic'))
    );
  }

  /** DELETE: delete the meshmaterialbasicdb from the server */
  delete(meshmaterialbasicdb: MeshMaterialBasicAPI | number, Name: string): Observable<MeshMaterialBasicAPI> {
    return this.deleteMeshMaterialBasic(meshmaterialbasicdb, Name)
  }
  deleteMeshMaterialBasic(meshmaterialbasicdb: MeshMaterialBasicAPI | number, Name: string): Observable<MeshMaterialBasicAPI> {
    const id = typeof meshmaterialbasicdb === 'number' ? meshmaterialbasicdb : meshmaterialbasicdb.ID;
    const url = `${this.meshmaterialbasicsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<MeshMaterialBasicAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted meshmaterialbasicdb id=${id}`)),
      catchError(this.handleError<MeshMaterialBasicAPI>('deleteMeshMaterialBasic'))
    );
  }

  // updateFront copy meshmaterialbasic to a version with encoded pointers and update to the back
  updateFront(meshmaterialbasic: MeshMaterialBasic, Name: string): Observable<MeshMaterialBasicAPI> {
    let meshmaterialbasicAPI = new MeshMaterialBasicAPI
    CopyMeshMaterialBasicToMeshMaterialBasicAPI(meshmaterialbasic, meshmaterialbasicAPI)
    const id = typeof meshmaterialbasicAPI === 'number' ? meshmaterialbasicAPI : meshmaterialbasicAPI.ID
    const url = `${this.meshmaterialbasicsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<MeshMaterialBasicAPI>(url, meshmaterialbasicAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MeshMaterialBasicAPI>('updateMeshMaterialBasic'))
    );
  }

  /** PUT: update the meshmaterialbasicdb on the server */
  update(meshmaterialbasicdb: MeshMaterialBasicAPI, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {
    return this.updateMeshMaterialBasic(meshmaterialbasicdb, Name, frontRepo)
  }
  updateMeshMaterialBasic(meshmaterialbasicdb: MeshMaterialBasicAPI, Name: string, frontRepo: FrontRepo): Observable<MeshMaterialBasicAPI> {
    const id = typeof meshmaterialbasicdb === 'number' ? meshmaterialbasicdb : meshmaterialbasicdb.ID;
    const url = `${this.meshmaterialbasicsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<MeshMaterialBasicAPI>(url, meshmaterialbasicdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated meshmaterialbasicdb id=${meshmaterialbasicdb.ID}`)
      }),
      catchError(this.handleError<MeshMaterialBasicAPI>('updateMeshMaterialBasic'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in MeshMaterialBasicService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("MeshMaterialBasicService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
