// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { PlaneGeometryAPI } from './planegeometry-api'
import { PlaneGeometry, CopyPlaneGeometryToPlaneGeometryAPI } from './planegeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class PlaneGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  PlaneGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private planegeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.planegeometrysUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/planegeometrys';
  }

  /** GET planegeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI[]> {
    return this.getPlaneGeometrys(Name, frontRepo)
  }
  getPlaneGeometrys(Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<PlaneGeometryAPI[]>(this.planegeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<PlaneGeometryAPI[]>('getPlaneGeometrys', []))
      );
  }

  /** GET planegeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {
    return this.getPlaneGeometry(id, Name, frontRepo)
  }
  getPlaneGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.planegeometrysUrl}/${id}`;
    return this.http.get<PlaneGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched planegeometry id=${id}`)),
      catchError(this.handleError<PlaneGeometryAPI>(`getPlaneGeometry id=${id}`))
    );
  }

  // postFront copy planegeometry to a version with encoded pointers and post to the back
  postFront(planegeometry: PlaneGeometry, Name: string): Observable<PlaneGeometryAPI> {
    let planegeometryAPI = new PlaneGeometryAPI
    CopyPlaneGeometryToPlaneGeometryAPI(planegeometry, planegeometryAPI)
    const id = typeof planegeometryAPI === 'number' ? planegeometryAPI : planegeometryAPI.ID
    const url = `${this.planegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<PlaneGeometryAPI>(url, planegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<PlaneGeometryAPI>('postPlaneGeometry'))
    );
  }

  /** POST: add a new planegeometry to the server */
  post(planegeometrydb: PlaneGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {
    return this.postPlaneGeometry(planegeometrydb, Name, frontRepo)
  }
  postPlaneGeometry(planegeometrydb: PlaneGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<PlaneGeometryAPI>(this.planegeometrysUrl, planegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted planegeometrydb id=${planegeometrydb.ID}`)
      }),
      catchError(this.handleError<PlaneGeometryAPI>('postPlaneGeometry'))
    );
  }

  /** DELETE: delete the planegeometrydb from the server */
  delete(planegeometrydb: PlaneGeometryAPI | number, Name: string): Observable<PlaneGeometryAPI> {
    return this.deletePlaneGeometry(planegeometrydb, Name)
  }
  deletePlaneGeometry(planegeometrydb: PlaneGeometryAPI | number, Name: string): Observable<PlaneGeometryAPI> {
    const id = typeof planegeometrydb === 'number' ? planegeometrydb : planegeometrydb.ID;
    const url = `${this.planegeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<PlaneGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted planegeometrydb id=${id}`)),
      catchError(this.handleError<PlaneGeometryAPI>('deletePlaneGeometry'))
    );
  }

  // updateFront copy planegeometry to a version with encoded pointers and update to the back
  updateFront(planegeometry: PlaneGeometry, Name: string): Observable<PlaneGeometryAPI> {
    let planegeometryAPI = new PlaneGeometryAPI
    CopyPlaneGeometryToPlaneGeometryAPI(planegeometry, planegeometryAPI)
    const id = typeof planegeometryAPI === 'number' ? planegeometryAPI : planegeometryAPI.ID
    const url = `${this.planegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<PlaneGeometryAPI>(url, planegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<PlaneGeometryAPI>('updatePlaneGeometry'))
    );
  }

  /** PUT: update the planegeometrydb on the server */
  update(planegeometrydb: PlaneGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {
    return this.updatePlaneGeometry(planegeometrydb, Name, frontRepo)
  }
  updatePlaneGeometry(planegeometrydb: PlaneGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<PlaneGeometryAPI> {
    const id = typeof planegeometrydb === 'number' ? planegeometrydb : planegeometrydb.ID;
    const url = `${this.planegeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<PlaneGeometryAPI>(url, planegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated planegeometrydb id=${planegeometrydb.ID}`)
      }),
      catchError(this.handleError<PlaneGeometryAPI>('updatePlaneGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in PlaneGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("PlaneGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
