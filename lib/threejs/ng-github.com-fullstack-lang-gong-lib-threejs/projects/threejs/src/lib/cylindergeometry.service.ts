// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CylinderGeometryAPI } from './cylindergeometry-api'
import { CylinderGeometry, CopyCylinderGeometryToCylinderGeometryAPI } from './cylindergeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class CylinderGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CylinderGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private cylindergeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.cylindergeometrysUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/cylindergeometrys';
  }

  /** GET cylindergeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI[]> {
    return this.getCylinderGeometrys(Name, frontRepo)
  }
  getCylinderGeometrys(Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<CylinderGeometryAPI[]>(this.cylindergeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<CylinderGeometryAPI[]>('getCylinderGeometrys', []))
      );
  }

  /** GET cylindergeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {
    return this.getCylinderGeometry(id, Name, frontRepo)
  }
  getCylinderGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.cylindergeometrysUrl}/${id}`;
    return this.http.get<CylinderGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched cylindergeometry id=${id}`)),
      catchError(this.handleError<CylinderGeometryAPI>(`getCylinderGeometry id=${id}`))
    );
  }

  // postFront copy cylindergeometry to a version with encoded pointers and post to the back
  postFront(cylindergeometry: CylinderGeometry, Name: string): Observable<CylinderGeometryAPI> {
    let cylindergeometryAPI = new CylinderGeometryAPI
    CopyCylinderGeometryToCylinderGeometryAPI(cylindergeometry, cylindergeometryAPI)
    const id = typeof cylindergeometryAPI === 'number' ? cylindergeometryAPI : cylindergeometryAPI.ID
    const url = `${this.cylindergeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CylinderGeometryAPI>(url, cylindergeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CylinderGeometryAPI>('postCylinderGeometry'))
    );
  }

  /** POST: add a new cylindergeometry to the server */
  post(cylindergeometrydb: CylinderGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {
    return this.postCylinderGeometry(cylindergeometrydb, Name, frontRepo)
  }
  postCylinderGeometry(cylindergeometrydb: CylinderGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CylinderGeometryAPI>(this.cylindergeometrysUrl, cylindergeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted cylindergeometrydb id=${cylindergeometrydb.ID}`)
      }),
      catchError(this.handleError<CylinderGeometryAPI>('postCylinderGeometry'))
    );
  }

  /** DELETE: delete the cylindergeometrydb from the server */
  delete(cylindergeometrydb: CylinderGeometryAPI | number, Name: string): Observable<CylinderGeometryAPI> {
    return this.deleteCylinderGeometry(cylindergeometrydb, Name)
  }
  deleteCylinderGeometry(cylindergeometrydb: CylinderGeometryAPI | number, Name: string): Observable<CylinderGeometryAPI> {
    const id = typeof cylindergeometrydb === 'number' ? cylindergeometrydb : cylindergeometrydb.ID;
    const url = `${this.cylindergeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CylinderGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted cylindergeometrydb id=${id}`)),
      catchError(this.handleError<CylinderGeometryAPI>('deleteCylinderGeometry'))
    );
  }

  // updateFront copy cylindergeometry to a version with encoded pointers and update to the back
  updateFront(cylindergeometry: CylinderGeometry, Name: string): Observable<CylinderGeometryAPI> {
    let cylindergeometryAPI = new CylinderGeometryAPI
    CopyCylinderGeometryToCylinderGeometryAPI(cylindergeometry, cylindergeometryAPI)
    const id = typeof cylindergeometryAPI === 'number' ? cylindergeometryAPI : cylindergeometryAPI.ID
    const url = `${this.cylindergeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CylinderGeometryAPI>(url, cylindergeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CylinderGeometryAPI>('updateCylinderGeometry'))
    );
  }

  /** PUT: update the cylindergeometrydb on the server */
  update(cylindergeometrydb: CylinderGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {
    return this.updateCylinderGeometry(cylindergeometrydb, Name, frontRepo)
  }
  updateCylinderGeometry(cylindergeometrydb: CylinderGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<CylinderGeometryAPI> {
    const id = typeof cylindergeometrydb === 'number' ? cylindergeometrydb : cylindergeometrydb.ID;
    const url = `${this.cylindergeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CylinderGeometryAPI>(url, cylindergeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated cylindergeometrydb id=${cylindergeometrydb.ID}`)
      }),
      catchError(this.handleError<CylinderGeometryAPI>('updateCylinderGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CylinderGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("CylinderGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
