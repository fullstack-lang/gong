// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { ExtrudeGeometryAPI } from './extrudegeometry-api'
import { ExtrudeGeometry, CopyExtrudeGeometryToExtrudeGeometryAPI } from './extrudegeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeAPI } from './shape-api'
import { CurveAPI } from './curve-api'

@Injectable({
  providedIn: 'root'
})
export class ExtrudeGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ExtrudeGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private extrudegeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.extrudegeometrysUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/extrudegeometrys';
  }

  /** GET extrudegeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI[]> {
    return this.getExtrudeGeometrys(Name, frontRepo)
  }
  getExtrudeGeometrys(Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<ExtrudeGeometryAPI[]>(this.extrudegeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<ExtrudeGeometryAPI[]>('getExtrudeGeometrys', []))
      );
  }

  /** GET extrudegeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {
    return this.getExtrudeGeometry(id, Name, frontRepo)
  }
  getExtrudeGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.extrudegeometrysUrl}/${id}`;
    return this.http.get<ExtrudeGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched extrudegeometry id=${id}`)),
      catchError(this.handleError<ExtrudeGeometryAPI>(`getExtrudeGeometry id=${id}`))
    );
  }

  // postFront copy extrudegeometry to a version with encoded pointers and post to the back
  postFront(extrudegeometry: ExtrudeGeometry, Name: string): Observable<ExtrudeGeometryAPI> {
    let extrudegeometryAPI = new ExtrudeGeometryAPI
    CopyExtrudeGeometryToExtrudeGeometryAPI(extrudegeometry, extrudegeometryAPI)
    const id = typeof extrudegeometryAPI === 'number' ? extrudegeometryAPI : extrudegeometryAPI.ID
    const url = `${this.extrudegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ExtrudeGeometryAPI>(url, extrudegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ExtrudeGeometryAPI>('postExtrudeGeometry'))
    );
  }

  /** POST: add a new extrudegeometry to the server */
  post(extrudegeometrydb: ExtrudeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {
    return this.postExtrudeGeometry(extrudegeometrydb, Name, frontRepo)
  }
  postExtrudeGeometry(extrudegeometrydb: ExtrudeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ExtrudeGeometryAPI>(this.extrudegeometrysUrl, extrudegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted extrudegeometrydb id=${extrudegeometrydb.ID}`)
      }),
      catchError(this.handleError<ExtrudeGeometryAPI>('postExtrudeGeometry'))
    );
  }

  /** DELETE: delete the extrudegeometrydb from the server */
  delete(extrudegeometrydb: ExtrudeGeometryAPI | number, Name: string): Observable<ExtrudeGeometryAPI> {
    return this.deleteExtrudeGeometry(extrudegeometrydb, Name)
  }
  deleteExtrudeGeometry(extrudegeometrydb: ExtrudeGeometryAPI | number, Name: string): Observable<ExtrudeGeometryAPI> {
    const id = typeof extrudegeometrydb === 'number' ? extrudegeometrydb : extrudegeometrydb.ID;
    const url = `${this.extrudegeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ExtrudeGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted extrudegeometrydb id=${id}`)),
      catchError(this.handleError<ExtrudeGeometryAPI>('deleteExtrudeGeometry'))
    );
  }

  // updateFront copy extrudegeometry to a version with encoded pointers and update to the back
  updateFront(extrudegeometry: ExtrudeGeometry, Name: string): Observable<ExtrudeGeometryAPI> {
    let extrudegeometryAPI = new ExtrudeGeometryAPI
    CopyExtrudeGeometryToExtrudeGeometryAPI(extrudegeometry, extrudegeometryAPI)
    const id = typeof extrudegeometryAPI === 'number' ? extrudegeometryAPI : extrudegeometryAPI.ID
    const url = `${this.extrudegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<ExtrudeGeometryAPI>(url, extrudegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ExtrudeGeometryAPI>('updateExtrudeGeometry'))
    );
  }

  /** PUT: update the extrudegeometrydb on the server */
  update(extrudegeometrydb: ExtrudeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {
    return this.updateExtrudeGeometry(extrudegeometrydb, Name, frontRepo)
  }
  updateExtrudeGeometry(extrudegeometrydb: ExtrudeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<ExtrudeGeometryAPI> {
    const id = typeof extrudegeometrydb === 'number' ? extrudegeometrydb : extrudegeometrydb.ID;
    const url = `${this.extrudegeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ExtrudeGeometryAPI>(url, extrudegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated extrudegeometrydb id=${extrudegeometrydb.ID}`)
      }),
      catchError(this.handleError<ExtrudeGeometryAPI>('updateExtrudeGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ExtrudeGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("ExtrudeGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
