// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CameraAPI } from './camera-api'
import { Camera, CopyCameraToCameraAPI } from './camera'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class CameraService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CameraServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private camerasUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.camerasUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/cameras';
  }

  /** GET cameras from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<CameraAPI[]> {
    return this.getCameras(Name, frontRepo)
  }
  getCameras(Name: string, frontRepo: FrontRepo): Observable<CameraAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<CameraAPI[]>(this.camerasUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<CameraAPI[]>('getCameras', []))
      );
  }

  /** GET camera by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {
    return this.getCamera(id, Name, frontRepo)
  }
  getCamera(id: number, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.camerasUrl}/${id}`;
    return this.http.get<CameraAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched camera id=${id}`)),
      catchError(this.handleError<CameraAPI>(`getCamera id=${id}`))
    );
  }

  // postFront copy camera to a version with encoded pointers and post to the back
  postFront(camera: Camera, Name: string): Observable<CameraAPI> {
    let cameraAPI = new CameraAPI
    CopyCameraToCameraAPI(camera, cameraAPI)
    const id = typeof cameraAPI === 'number' ? cameraAPI : cameraAPI.ID
    const url = `${this.camerasUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CameraAPI>(url, cameraAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CameraAPI>('postCamera'))
    );
  }

  /** POST: add a new camera to the server */
  post(cameradb: CameraAPI, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {
    return this.postCamera(cameradb, Name, frontRepo)
  }
  postCamera(cameradb: CameraAPI, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CameraAPI>(this.camerasUrl, cameradb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted cameradb id=${cameradb.ID}`)
      }),
      catchError(this.handleError<CameraAPI>('postCamera'))
    );
  }

  /** DELETE: delete the cameradb from the server */
  delete(cameradb: CameraAPI | number, Name: string): Observable<CameraAPI> {
    return this.deleteCamera(cameradb, Name)
  }
  deleteCamera(cameradb: CameraAPI | number, Name: string): Observable<CameraAPI> {
    const id = typeof cameradb === 'number' ? cameradb : cameradb.ID;
    const url = `${this.camerasUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CameraAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted cameradb id=${id}`)),
      catchError(this.handleError<CameraAPI>('deleteCamera'))
    );
  }

  // updateFront copy camera to a version with encoded pointers and update to the back
  updateFront(camera: Camera, Name: string): Observable<CameraAPI> {
    let cameraAPI = new CameraAPI
    CopyCameraToCameraAPI(camera, cameraAPI)
    const id = typeof cameraAPI === 'number' ? cameraAPI : cameraAPI.ID
    const url = `${this.camerasUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CameraAPI>(url, cameraAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CameraAPI>('updateCamera'))
    );
  }

  /** PUT: update the cameradb on the server */
  update(cameradb: CameraAPI, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {
    return this.updateCamera(cameradb, Name, frontRepo)
  }
  updateCamera(cameradb: CameraAPI, Name: string, frontRepo: FrontRepo): Observable<CameraAPI> {
    const id = typeof cameradb === 'number' ? cameradb : cameradb.ID;
    const url = `${this.camerasUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CameraAPI>(url, cameradb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated cameradb id=${cameradb.ID}`)
      }),
      catchError(this.handleError<CameraAPI>('updateCamera'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CameraService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("CameraService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
