// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { TriangleAPI } from './triangle-api'
import { Triangle, CopyTriangleToTriangleAPI } from './triangle'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class TriangleService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TriangleServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private trianglesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.trianglesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/triangles';
  }

  /** GET triangles from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<TriangleAPI[]> {
    return this.getTriangles(Name, frontRepo)
  }
  getTriangles(Name: string, frontRepo: FrontRepo): Observable<TriangleAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<TriangleAPI[]>(this.trianglesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<TriangleAPI[]>('getTriangles', []))
      );
  }

  /** GET triangle by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {
    return this.getTriangle(id, Name, frontRepo)
  }
  getTriangle(id: number, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.trianglesUrl}/${id}`;
    return this.http.get<TriangleAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched triangle id=${id}`)),
      catchError(this.handleError<TriangleAPI>(`getTriangle id=${id}`))
    );
  }

  // postFront copy triangle to a version with encoded pointers and post to the back
  postFront(triangle: Triangle, Name: string): Observable<TriangleAPI> {
    let triangleAPI = new TriangleAPI
    CopyTriangleToTriangleAPI(triangle, triangleAPI)
    const id = typeof triangleAPI === 'number' ? triangleAPI : triangleAPI.ID
    const url = `${this.trianglesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TriangleAPI>(url, triangleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TriangleAPI>('postTriangle'))
    );
  }

  /** POST: add a new triangle to the server */
  post(triangledb: TriangleAPI, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {
    return this.postTriangle(triangledb, Name, frontRepo)
  }
  postTriangle(triangledb: TriangleAPI, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TriangleAPI>(this.trianglesUrl, triangledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted triangledb id=${triangledb.ID}`)
      }),
      catchError(this.handleError<TriangleAPI>('postTriangle'))
    );
  }

  /** DELETE: delete the triangledb from the server */
  delete(triangledb: TriangleAPI | number, Name: string): Observable<TriangleAPI> {
    return this.deleteTriangle(triangledb, Name)
  }
  deleteTriangle(triangledb: TriangleAPI | number, Name: string): Observable<TriangleAPI> {
    const id = typeof triangledb === 'number' ? triangledb : triangledb.ID;
    const url = `${this.trianglesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TriangleAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted triangledb id=${id}`)),
      catchError(this.handleError<TriangleAPI>('deleteTriangle'))
    );
  }

  // updateFront copy triangle to a version with encoded pointers and update to the back
  updateFront(triangle: Triangle, Name: string): Observable<TriangleAPI> {
    let triangleAPI = new TriangleAPI
    CopyTriangleToTriangleAPI(triangle, triangleAPI)
    const id = typeof triangleAPI === 'number' ? triangleAPI : triangleAPI.ID
    const url = `${this.trianglesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<TriangleAPI>(url, triangleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TriangleAPI>('updateTriangle'))
    );
  }

  /** PUT: update the triangledb on the server */
  update(triangledb: TriangleAPI, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {
    return this.updateTriangle(triangledb, Name, frontRepo)
  }
  updateTriangle(triangledb: TriangleAPI, Name: string, frontRepo: FrontRepo): Observable<TriangleAPI> {
    const id = typeof triangledb === 'number' ? triangledb : triangledb.ID;
    const url = `${this.trianglesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TriangleAPI>(url, triangledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated triangledb id=${triangledb.ID}`)
      }),
      catchError(this.handleError<TriangleAPI>('updateTriangle'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TriangleService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("TriangleService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
