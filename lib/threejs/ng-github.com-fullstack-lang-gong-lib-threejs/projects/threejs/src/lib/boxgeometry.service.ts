// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { BoxGeometryAPI } from './boxgeometry-api'
import { BoxGeometry, CopyBoxGeometryToBoxGeometryAPI } from './boxgeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class BoxGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  BoxGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private boxgeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.boxgeometrysUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/boxgeometrys';
  }

  /** GET boxgeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI[]> {
    return this.getBoxGeometrys(Name, frontRepo)
  }
  getBoxGeometrys(Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<BoxGeometryAPI[]>(this.boxgeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<BoxGeometryAPI[]>('getBoxGeometrys', []))
      );
  }

  /** GET boxgeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {
    return this.getBoxGeometry(id, Name, frontRepo)
  }
  getBoxGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.boxgeometrysUrl}/${id}`;
    return this.http.get<BoxGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched boxgeometry id=${id}`)),
      catchError(this.handleError<BoxGeometryAPI>(`getBoxGeometry id=${id}`))
    );
  }

  // postFront copy boxgeometry to a version with encoded pointers and post to the back
  postFront(boxgeometry: BoxGeometry, Name: string): Observable<BoxGeometryAPI> {
    let boxgeometryAPI = new BoxGeometryAPI
    CopyBoxGeometryToBoxGeometryAPI(boxgeometry, boxgeometryAPI)
    const id = typeof boxgeometryAPI === 'number' ? boxgeometryAPI : boxgeometryAPI.ID
    const url = `${this.boxgeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BoxGeometryAPI>(url, boxgeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BoxGeometryAPI>('postBoxGeometry'))
    );
  }

  /** POST: add a new boxgeometry to the server */
  post(boxgeometrydb: BoxGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {
    return this.postBoxGeometry(boxgeometrydb, Name, frontRepo)
  }
  postBoxGeometry(boxgeometrydb: BoxGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BoxGeometryAPI>(this.boxgeometrysUrl, boxgeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted boxgeometrydb id=${boxgeometrydb.ID}`)
      }),
      catchError(this.handleError<BoxGeometryAPI>('postBoxGeometry'))
    );
  }

  /** DELETE: delete the boxgeometrydb from the server */
  delete(boxgeometrydb: BoxGeometryAPI | number, Name: string): Observable<BoxGeometryAPI> {
    return this.deleteBoxGeometry(boxgeometrydb, Name)
  }
  deleteBoxGeometry(boxgeometrydb: BoxGeometryAPI | number, Name: string): Observable<BoxGeometryAPI> {
    const id = typeof boxgeometrydb === 'number' ? boxgeometrydb : boxgeometrydb.ID;
    const url = `${this.boxgeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<BoxGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted boxgeometrydb id=${id}`)),
      catchError(this.handleError<BoxGeometryAPI>('deleteBoxGeometry'))
    );
  }

  // updateFront copy boxgeometry to a version with encoded pointers and update to the back
  updateFront(boxgeometry: BoxGeometry, Name: string): Observable<BoxGeometryAPI> {
    let boxgeometryAPI = new BoxGeometryAPI
    CopyBoxGeometryToBoxGeometryAPI(boxgeometry, boxgeometryAPI)
    const id = typeof boxgeometryAPI === 'number' ? boxgeometryAPI : boxgeometryAPI.ID
    const url = `${this.boxgeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<BoxGeometryAPI>(url, boxgeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BoxGeometryAPI>('updateBoxGeometry'))
    );
  }

  /** PUT: update the boxgeometrydb on the server */
  update(boxgeometrydb: BoxGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {
    return this.updateBoxGeometry(boxgeometrydb, Name, frontRepo)
  }
  updateBoxGeometry(boxgeometrydb: BoxGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<BoxGeometryAPI> {
    const id = typeof boxgeometrydb === 'number' ? boxgeometrydb : boxgeometrydb.ID;
    const url = `${this.boxgeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<BoxGeometryAPI>(url, boxgeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated boxgeometrydb id=${boxgeometrydb.ID}`)
      }),
      catchError(this.handleError<BoxGeometryAPI>('updateBoxGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in BoxGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("BoxGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
