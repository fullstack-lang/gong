// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CurveAPI } from './curve-api'
import { Curve, CopyCurveToCurveAPI } from './curve'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { Vector3API } from './vector3-api'

@Injectable({
  providedIn: 'root'
})
export class CurveService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CurveServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private curvesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.curvesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/threejs/go/v1/curves';
  }

  /** GET curves from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<CurveAPI[]> {
    return this.getCurves(Name, frontRepo)
  }
  getCurves(Name: string, frontRepo: FrontRepo): Observable<CurveAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<CurveAPI[]>(this.curvesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<CurveAPI[]>('getCurves', []))
      );
  }

  /** GET curve by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {
    return this.getCurve(id, Name, frontRepo)
  }
  getCurve(id: number, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.curvesUrl}/${id}`;
    return this.http.get<CurveAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched curve id=${id}`)),
      catchError(this.handleError<CurveAPI>(`getCurve id=${id}`))
    );
  }

  // postFront copy curve to a version with encoded pointers and post to the back
  postFront(curve: Curve, Name: string): Observable<CurveAPI> {
    let curveAPI = new CurveAPI
    CopyCurveToCurveAPI(curve, curveAPI)
    const id = typeof curveAPI === 'number' ? curveAPI : curveAPI.ID
    const url = `${this.curvesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CurveAPI>(url, curveAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CurveAPI>('postCurve'))
    );
  }

  /** POST: add a new curve to the server */
  post(curvedb: CurveAPI, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {
    return this.postCurve(curvedb, Name, frontRepo)
  }
  postCurve(curvedb: CurveAPI, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CurveAPI>(this.curvesUrl, curvedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted curvedb id=${curvedb.ID}`)
      }),
      catchError(this.handleError<CurveAPI>('postCurve'))
    );
  }

  /** DELETE: delete the curvedb from the server */
  delete(curvedb: CurveAPI | number, Name: string): Observable<CurveAPI> {
    return this.deleteCurve(curvedb, Name)
  }
  deleteCurve(curvedb: CurveAPI | number, Name: string): Observable<CurveAPI> {
    const id = typeof curvedb === 'number' ? curvedb : curvedb.ID;
    const url = `${this.curvesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CurveAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted curvedb id=${id}`)),
      catchError(this.handleError<CurveAPI>('deleteCurve'))
    );
  }

  // updateFront copy curve to a version with encoded pointers and update to the back
  updateFront(curve: Curve, Name: string): Observable<CurveAPI> {
    let curveAPI = new CurveAPI
    CopyCurveToCurveAPI(curve, curveAPI)
    const id = typeof curveAPI === 'number' ? curveAPI : curveAPI.ID
    const url = `${this.curvesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CurveAPI>(url, curveAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CurveAPI>('updateCurve'))
    );
  }

  /** PUT: update the curvedb on the server */
  update(curvedb: CurveAPI, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {
    return this.updateCurve(curvedb, Name, frontRepo)
  }
  updateCurve(curvedb: CurveAPI, Name: string, frontRepo: FrontRepo): Observable<CurveAPI> {
    const id = typeof curvedb === 'number' ? curvedb : curvedb.ID;
    const url = `${this.curvesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CurveAPI>(url, curvedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated curvedb id=${curvedb.ID}`)
      }),
      catchError(this.handleError<CurveAPI>('updateCurve'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CurveService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("CurveService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
