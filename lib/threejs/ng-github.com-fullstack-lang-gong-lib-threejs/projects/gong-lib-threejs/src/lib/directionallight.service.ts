// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { DirectionalLightAPI } from './directionallight-api'
import { DirectionalLight, CopyDirectionalLightToDirectionalLightAPI } from './directionallight'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class DirectionalLightService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  DirectionalLightServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private directionallightsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.directionallightsUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/directionallights';
  }

  /** GET directionallights from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI[]> {
    return this.getDirectionalLights(Name, frontRepo)
  }
  getDirectionalLights(Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<DirectionalLightAPI[]>(this.directionallightsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<DirectionalLightAPI[]>('getDirectionalLights', []))
      );
  }

  /** GET directionallight by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {
    return this.getDirectionalLight(id, Name, frontRepo)
  }
  getDirectionalLight(id: number, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.directionallightsUrl}/${id}`;
    return this.http.get<DirectionalLightAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched directionallight id=${id}`)),
      catchError(this.handleError<DirectionalLightAPI>(`getDirectionalLight id=${id}`))
    );
  }

  // postFront copy directionallight to a version with encoded pointers and post to the back
  postFront(directionallight: DirectionalLight, Name: string): Observable<DirectionalLightAPI> {
    let directionallightAPI = new DirectionalLightAPI
    CopyDirectionalLightToDirectionalLightAPI(directionallight, directionallightAPI)
    const id = typeof directionallightAPI === 'number' ? directionallightAPI : directionallightAPI.ID
    const url = `${this.directionallightsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<DirectionalLightAPI>(url, directionallightAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<DirectionalLightAPI>('postDirectionalLight'))
    );
  }

  /** POST: add a new directionallight to the server */
  post(directionallightdb: DirectionalLightAPI, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {
    return this.postDirectionalLight(directionallightdb, Name, frontRepo)
  }
  postDirectionalLight(directionallightdb: DirectionalLightAPI, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<DirectionalLightAPI>(this.directionallightsUrl, directionallightdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted directionallightdb id=${directionallightdb.ID}`)
      }),
      catchError(this.handleError<DirectionalLightAPI>('postDirectionalLight'))
    );
  }

  /** DELETE: delete the directionallightdb from the server */
  delete(directionallightdb: DirectionalLightAPI | number, Name: string): Observable<DirectionalLightAPI> {
    return this.deleteDirectionalLight(directionallightdb, Name)
  }
  deleteDirectionalLight(directionallightdb: DirectionalLightAPI | number, Name: string): Observable<DirectionalLightAPI> {
    const id = typeof directionallightdb === 'number' ? directionallightdb : directionallightdb.ID;
    const url = `${this.directionallightsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<DirectionalLightAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted directionallightdb id=${id}`)),
      catchError(this.handleError<DirectionalLightAPI>('deleteDirectionalLight'))
    );
  }

  // updateFront copy directionallight to a version with encoded pointers and update to the back
  updateFront(directionallight: DirectionalLight, Name: string): Observable<DirectionalLightAPI> {
    let directionallightAPI = new DirectionalLightAPI
    CopyDirectionalLightToDirectionalLightAPI(directionallight, directionallightAPI)
    const id = typeof directionallightAPI === 'number' ? directionallightAPI : directionallightAPI.ID
    const url = `${this.directionallightsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<DirectionalLightAPI>(url, directionallightAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<DirectionalLightAPI>('updateDirectionalLight'))
    );
  }

  /** PUT: update the directionallightdb on the server */
  update(directionallightdb: DirectionalLightAPI, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {
    return this.updateDirectionalLight(directionallightdb, Name, frontRepo)
  }
  updateDirectionalLight(directionallightdb: DirectionalLightAPI, Name: string, frontRepo: FrontRepo): Observable<DirectionalLightAPI> {
    const id = typeof directionallightdb === 'number' ? directionallightdb : directionallightdb.ID;
    const url = `${this.directionallightsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<DirectionalLightAPI>(url, directionallightdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated directionallightdb id=${directionallightdb.ID}`)
      }),
      catchError(this.handleError<DirectionalLightAPI>('updateDirectionalLight'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in DirectionalLightService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("DirectionalLightService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
