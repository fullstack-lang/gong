// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CanvasAPI } from './canvas-api'
import { Canvas, CopyCanvasToCanvasAPI } from './canvas'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { DirectionalLightAPI } from './directionallight-api'
import { AmbiantLightAPI } from './ambiantlight-api'
import { MeshAPI } from './mesh-api'

@Injectable({
  providedIn: 'root'
})
export class CanvasService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CanvasServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private canvassUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.canvassUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/canvass';
  }

  /** GET canvass from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<CanvasAPI[]> {
    return this.getCanvass(Name, frontRepo)
  }
  getCanvass(Name: string, frontRepo: FrontRepo): Observable<CanvasAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<CanvasAPI[]>(this.canvassUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<CanvasAPI[]>('getCanvass', []))
      );
  }

  /** GET canvas by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {
    return this.getCanvas(id, Name, frontRepo)
  }
  getCanvas(id: number, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.canvassUrl}/${id}`;
    return this.http.get<CanvasAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched canvas id=${id}`)),
      catchError(this.handleError<CanvasAPI>(`getCanvas id=${id}`))
    );
  }

  // postFront copy canvas to a version with encoded pointers and post to the back
  postFront(canvas: Canvas, Name: string): Observable<CanvasAPI> {
    let canvasAPI = new CanvasAPI
    CopyCanvasToCanvasAPI(canvas, canvasAPI)
    const id = typeof canvasAPI === 'number' ? canvasAPI : canvasAPI.ID
    const url = `${this.canvassUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CanvasAPI>(url, canvasAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CanvasAPI>('postCanvas'))
    );
  }

  /** POST: add a new canvas to the server */
  post(canvasdb: CanvasAPI, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {
    return this.postCanvas(canvasdb, Name, frontRepo)
  }
  postCanvas(canvasdb: CanvasAPI, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CanvasAPI>(this.canvassUrl, canvasdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted canvasdb id=${canvasdb.ID}`)
      }),
      catchError(this.handleError<CanvasAPI>('postCanvas'))
    );
  }

  /** DELETE: delete the canvasdb from the server */
  delete(canvasdb: CanvasAPI | number, Name: string): Observable<CanvasAPI> {
    return this.deleteCanvas(canvasdb, Name)
  }
  deleteCanvas(canvasdb: CanvasAPI | number, Name: string): Observable<CanvasAPI> {
    const id = typeof canvasdb === 'number' ? canvasdb : canvasdb.ID;
    const url = `${this.canvassUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CanvasAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted canvasdb id=${id}`)),
      catchError(this.handleError<CanvasAPI>('deleteCanvas'))
    );
  }

  // updateFront copy canvas to a version with encoded pointers and update to the back
  updateFront(canvas: Canvas, Name: string): Observable<CanvasAPI> {
    let canvasAPI = new CanvasAPI
    CopyCanvasToCanvasAPI(canvas, canvasAPI)
    const id = typeof canvasAPI === 'number' ? canvasAPI : canvasAPI.ID
    const url = `${this.canvassUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CanvasAPI>(url, canvasAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CanvasAPI>('updateCanvas'))
    );
  }

  /** PUT: update the canvasdb on the server */
  update(canvasdb: CanvasAPI, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {
    return this.updateCanvas(canvasdb, Name, frontRepo)
  }
  updateCanvas(canvasdb: CanvasAPI, Name: string, frontRepo: FrontRepo): Observable<CanvasAPI> {
    const id = typeof canvasdb === 'number' ? canvasdb : canvasdb.ID;
    const url = `${this.canvassUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CanvasAPI>(url, canvasdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated canvasdb id=${canvasdb.ID}`)
      }),
      catchError(this.handleError<CanvasAPI>('updateCanvas'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CanvasService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("CanvasService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
