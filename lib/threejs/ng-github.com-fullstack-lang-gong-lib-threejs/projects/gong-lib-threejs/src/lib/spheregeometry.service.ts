// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SphereGeometryAPI } from './spheregeometry-api'
import { SphereGeometry, CopySphereGeometryToSphereGeometryAPI } from './spheregeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class SphereGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SphereGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spheregeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spheregeometrysUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/spheregeometrys';
  }

  /** GET spheregeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI[]> {
    return this.getSphereGeometrys(Name, frontRepo)
  }
  getSphereGeometrys(Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<SphereGeometryAPI[]>(this.spheregeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SphereGeometryAPI[]>('getSphereGeometrys', []))
      );
  }

  /** GET spheregeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {
    return this.getSphereGeometry(id, Name, frontRepo)
  }
  getSphereGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.spheregeometrysUrl}/${id}`;
    return this.http.get<SphereGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spheregeometry id=${id}`)),
      catchError(this.handleError<SphereGeometryAPI>(`getSphereGeometry id=${id}`))
    );
  }

  // postFront copy spheregeometry to a version with encoded pointers and post to the back
  postFront(spheregeometry: SphereGeometry, Name: string): Observable<SphereGeometryAPI> {
    let spheregeometryAPI = new SphereGeometryAPI
    CopySphereGeometryToSphereGeometryAPI(spheregeometry, spheregeometryAPI)
    const id = typeof spheregeometryAPI === 'number' ? spheregeometryAPI : spheregeometryAPI.ID
    const url = `${this.spheregeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SphereGeometryAPI>(url, spheregeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SphereGeometryAPI>('postSphereGeometry'))
    );
  }

  /** POST: add a new spheregeometry to the server */
  post(spheregeometrydb: SphereGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {
    return this.postSphereGeometry(spheregeometrydb, Name, frontRepo)
  }
  postSphereGeometry(spheregeometrydb: SphereGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SphereGeometryAPI>(this.spheregeometrysUrl, spheregeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spheregeometrydb id=${spheregeometrydb.ID}`)
      }),
      catchError(this.handleError<SphereGeometryAPI>('postSphereGeometry'))
    );
  }

  /** DELETE: delete the spheregeometrydb from the server */
  delete(spheregeometrydb: SphereGeometryAPI | number, Name: string): Observable<SphereGeometryAPI> {
    return this.deleteSphereGeometry(spheregeometrydb, Name)
  }
  deleteSphereGeometry(spheregeometrydb: SphereGeometryAPI | number, Name: string): Observable<SphereGeometryAPI> {
    const id = typeof spheregeometrydb === 'number' ? spheregeometrydb : spheregeometrydb.ID;
    const url = `${this.spheregeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SphereGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spheregeometrydb id=${id}`)),
      catchError(this.handleError<SphereGeometryAPI>('deleteSphereGeometry'))
    );
  }

  // updateFront copy spheregeometry to a version with encoded pointers and update to the back
  updateFront(spheregeometry: SphereGeometry, Name: string): Observable<SphereGeometryAPI> {
    let spheregeometryAPI = new SphereGeometryAPI
    CopySphereGeometryToSphereGeometryAPI(spheregeometry, spheregeometryAPI)
    const id = typeof spheregeometryAPI === 'number' ? spheregeometryAPI : spheregeometryAPI.ID
    const url = `${this.spheregeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SphereGeometryAPI>(url, spheregeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SphereGeometryAPI>('updateSphereGeometry'))
    );
  }

  /** PUT: update the spheregeometrydb on the server */
  update(spheregeometrydb: SphereGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {
    return this.updateSphereGeometry(spheregeometrydb, Name, frontRepo)
  }
  updateSphereGeometry(spheregeometrydb: SphereGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<SphereGeometryAPI> {
    const id = typeof spheregeometrydb === 'number' ? spheregeometrydb : spheregeometrydb.ID;
    const url = `${this.spheregeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SphereGeometryAPI>(url, spheregeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spheregeometrydb id=${spheregeometrydb.ID}`)
      }),
      catchError(this.handleError<SphereGeometryAPI>('updateSphereGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SphereGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("SphereGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
