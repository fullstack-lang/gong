// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { TubeGeometryAPI } from './tubegeometry-api'
import { TubeGeometry, CopyTubeGeometryToTubeGeometryAPI } from './tubegeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { CurveAPI } from './curve-api'

@Injectable({
  providedIn: 'root'
})
export class TubeGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TubeGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private tubegeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.tubegeometrysUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/tubegeometrys';
  }

  /** GET tubegeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI[]> {
    return this.getTubeGeometrys(Name, frontRepo)
  }
  getTubeGeometrys(Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<TubeGeometryAPI[]>(this.tubegeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<TubeGeometryAPI[]>('getTubeGeometrys', []))
      );
  }

  /** GET tubegeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {
    return this.getTubeGeometry(id, Name, frontRepo)
  }
  getTubeGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.tubegeometrysUrl}/${id}`;
    return this.http.get<TubeGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched tubegeometry id=${id}`)),
      catchError(this.handleError<TubeGeometryAPI>(`getTubeGeometry id=${id}`))
    );
  }

  // postFront copy tubegeometry to a version with encoded pointers and post to the back
  postFront(tubegeometry: TubeGeometry, Name: string): Observable<TubeGeometryAPI> {
    let tubegeometryAPI = new TubeGeometryAPI
    CopyTubeGeometryToTubeGeometryAPI(tubegeometry, tubegeometryAPI)
    const id = typeof tubegeometryAPI === 'number' ? tubegeometryAPI : tubegeometryAPI.ID
    const url = `${this.tubegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TubeGeometryAPI>(url, tubegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TubeGeometryAPI>('postTubeGeometry'))
    );
  }

  /** POST: add a new tubegeometry to the server */
  post(tubegeometrydb: TubeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {
    return this.postTubeGeometry(tubegeometrydb, Name, frontRepo)
  }
  postTubeGeometry(tubegeometrydb: TubeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TubeGeometryAPI>(this.tubegeometrysUrl, tubegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted tubegeometrydb id=${tubegeometrydb.ID}`)
      }),
      catchError(this.handleError<TubeGeometryAPI>('postTubeGeometry'))
    );
  }

  /** DELETE: delete the tubegeometrydb from the server */
  delete(tubegeometrydb: TubeGeometryAPI | number, Name: string): Observable<TubeGeometryAPI> {
    return this.deleteTubeGeometry(tubegeometrydb, Name)
  }
  deleteTubeGeometry(tubegeometrydb: TubeGeometryAPI | number, Name: string): Observable<TubeGeometryAPI> {
    const id = typeof tubegeometrydb === 'number' ? tubegeometrydb : tubegeometrydb.ID;
    const url = `${this.tubegeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TubeGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted tubegeometrydb id=${id}`)),
      catchError(this.handleError<TubeGeometryAPI>('deleteTubeGeometry'))
    );
  }

  // updateFront copy tubegeometry to a version with encoded pointers and update to the back
  updateFront(tubegeometry: TubeGeometry, Name: string): Observable<TubeGeometryAPI> {
    let tubegeometryAPI = new TubeGeometryAPI
    CopyTubeGeometryToTubeGeometryAPI(tubegeometry, tubegeometryAPI)
    const id = typeof tubegeometryAPI === 'number' ? tubegeometryAPI : tubegeometryAPI.ID
    const url = `${this.tubegeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<TubeGeometryAPI>(url, tubegeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TubeGeometryAPI>('updateTubeGeometry'))
    );
  }

  /** PUT: update the tubegeometrydb on the server */
  update(tubegeometrydb: TubeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {
    return this.updateTubeGeometry(tubegeometrydb, Name, frontRepo)
  }
  updateTubeGeometry(tubegeometrydb: TubeGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TubeGeometryAPI> {
    const id = typeof tubegeometrydb === 'number' ? tubegeometrydb : tubegeometrydb.ID;
    const url = `${this.tubegeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TubeGeometryAPI>(url, tubegeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated tubegeometrydb id=${tubegeometrydb.ID}`)
      }),
      catchError(this.handleError<TubeGeometryAPI>('updateTubeGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TubeGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("TubeGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
