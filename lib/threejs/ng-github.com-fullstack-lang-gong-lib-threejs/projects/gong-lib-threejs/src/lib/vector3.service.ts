// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { Vector3API } from './vector3-api'
import { Vector3, CopyVector3ToVector3API } from './vector3'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class Vector3Service {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  Vector3ServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private vector3sUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.vector3sUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/vector3s';
  }

  /** GET vector3s from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<Vector3API[]> {
    return this.getVector3s(Name, frontRepo)
  }
  getVector3s(Name: string, frontRepo: FrontRepo): Observable<Vector3API[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<Vector3API[]>(this.vector3sUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<Vector3API[]>('getVector3s', []))
      );
  }

  /** GET vector3 by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {
    return this.getVector3(id, Name, frontRepo)
  }
  getVector3(id: number, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.vector3sUrl}/${id}`;
    return this.http.get<Vector3API>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched vector3 id=${id}`)),
      catchError(this.handleError<Vector3API>(`getVector3 id=${id}`))
    );
  }

  // postFront copy vector3 to a version with encoded pointers and post to the back
  postFront(vector3: Vector3, Name: string): Observable<Vector3API> {
    let vector3API = new Vector3API
    CopyVector3ToVector3API(vector3, vector3API)
    const id = typeof vector3API === 'number' ? vector3API : vector3API.ID
    const url = `${this.vector3sUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<Vector3API>(url, vector3API, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<Vector3API>('postVector3'))
    );
  }

  /** POST: add a new vector3 to the server */
  post(vector3db: Vector3API, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {
    return this.postVector3(vector3db, Name, frontRepo)
  }
  postVector3(vector3db: Vector3API, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<Vector3API>(this.vector3sUrl, vector3db, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted vector3db id=${vector3db.ID}`)
      }),
      catchError(this.handleError<Vector3API>('postVector3'))
    );
  }

  /** DELETE: delete the vector3db from the server */
  delete(vector3db: Vector3API | number, Name: string): Observable<Vector3API> {
    return this.deleteVector3(vector3db, Name)
  }
  deleteVector3(vector3db: Vector3API | number, Name: string): Observable<Vector3API> {
    const id = typeof vector3db === 'number' ? vector3db : vector3db.ID;
    const url = `${this.vector3sUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<Vector3API>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted vector3db id=${id}`)),
      catchError(this.handleError<Vector3API>('deleteVector3'))
    );
  }

  // updateFront copy vector3 to a version with encoded pointers and update to the back
  updateFront(vector3: Vector3, Name: string): Observable<Vector3API> {
    let vector3API = new Vector3API
    CopyVector3ToVector3API(vector3, vector3API)
    const id = typeof vector3API === 'number' ? vector3API : vector3API.ID
    const url = `${this.vector3sUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<Vector3API>(url, vector3API, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<Vector3API>('updateVector3'))
    );
  }

  /** PUT: update the vector3db on the server */
  update(vector3db: Vector3API, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {
    return this.updateVector3(vector3db, Name, frontRepo)
  }
  updateVector3(vector3db: Vector3API, Name: string, frontRepo: FrontRepo): Observable<Vector3API> {
    const id = typeof vector3db === 'number' ? vector3db : vector3db.ID;
    const url = `${this.vector3sUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<Vector3API>(url, vector3db, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated vector3db id=${vector3db.ID}`)
      }),
      catchError(this.handleError<Vector3API>('updateVector3'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in Vector3Service', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("Vector3Service" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
