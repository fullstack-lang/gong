// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { AmbiantLightAPI } from './ambiantlight-api'
import { AmbiantLight, CopyAmbiantLightToAmbiantLightAPI } from './ambiantlight'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class AmbiantLightService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  AmbiantLightServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private ambiantlightsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.ambiantlightsUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/ambiantlights';
  }

  /** GET ambiantlights from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI[]> {
    return this.getAmbiantLights(Name, frontRepo)
  }
  getAmbiantLights(Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<AmbiantLightAPI[]>(this.ambiantlightsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<AmbiantLightAPI[]>('getAmbiantLights', []))
      );
  }

  /** GET ambiantlight by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {
    return this.getAmbiantLight(id, Name, frontRepo)
  }
  getAmbiantLight(id: number, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.ambiantlightsUrl}/${id}`;
    return this.http.get<AmbiantLightAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched ambiantlight id=${id}`)),
      catchError(this.handleError<AmbiantLightAPI>(`getAmbiantLight id=${id}`))
    );
  }

  // postFront copy ambiantlight to a version with encoded pointers and post to the back
  postFront(ambiantlight: AmbiantLight, Name: string): Observable<AmbiantLightAPI> {
    let ambiantlightAPI = new AmbiantLightAPI
    CopyAmbiantLightToAmbiantLightAPI(ambiantlight, ambiantlightAPI)
    const id = typeof ambiantlightAPI === 'number' ? ambiantlightAPI : ambiantlightAPI.ID
    const url = `${this.ambiantlightsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<AmbiantLightAPI>(url, ambiantlightAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<AmbiantLightAPI>('postAmbiantLight'))
    );
  }

  /** POST: add a new ambiantlight to the server */
  post(ambiantlightdb: AmbiantLightAPI, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {
    return this.postAmbiantLight(ambiantlightdb, Name, frontRepo)
  }
  postAmbiantLight(ambiantlightdb: AmbiantLightAPI, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<AmbiantLightAPI>(this.ambiantlightsUrl, ambiantlightdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted ambiantlightdb id=${ambiantlightdb.ID}`)
      }),
      catchError(this.handleError<AmbiantLightAPI>('postAmbiantLight'))
    );
  }

  /** DELETE: delete the ambiantlightdb from the server */
  delete(ambiantlightdb: AmbiantLightAPI | number, Name: string): Observable<AmbiantLightAPI> {
    return this.deleteAmbiantLight(ambiantlightdb, Name)
  }
  deleteAmbiantLight(ambiantlightdb: AmbiantLightAPI | number, Name: string): Observable<AmbiantLightAPI> {
    const id = typeof ambiantlightdb === 'number' ? ambiantlightdb : ambiantlightdb.ID;
    const url = `${this.ambiantlightsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<AmbiantLightAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted ambiantlightdb id=${id}`)),
      catchError(this.handleError<AmbiantLightAPI>('deleteAmbiantLight'))
    );
  }

  // updateFront copy ambiantlight to a version with encoded pointers and update to the back
  updateFront(ambiantlight: AmbiantLight, Name: string): Observable<AmbiantLightAPI> {
    let ambiantlightAPI = new AmbiantLightAPI
    CopyAmbiantLightToAmbiantLightAPI(ambiantlight, ambiantlightAPI)
    const id = typeof ambiantlightAPI === 'number' ? ambiantlightAPI : ambiantlightAPI.ID
    const url = `${this.ambiantlightsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<AmbiantLightAPI>(url, ambiantlightAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<AmbiantLightAPI>('updateAmbiantLight'))
    );
  }

  /** PUT: update the ambiantlightdb on the server */
  update(ambiantlightdb: AmbiantLightAPI, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {
    return this.updateAmbiantLight(ambiantlightdb, Name, frontRepo)
  }
  updateAmbiantLight(ambiantlightdb: AmbiantLightAPI, Name: string, frontRepo: FrontRepo): Observable<AmbiantLightAPI> {
    const id = typeof ambiantlightdb === 'number' ? ambiantlightdb : ambiantlightdb.ID;
    const url = `${this.ambiantlightsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<AmbiantLightAPI>(url, ambiantlightdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated ambiantlightdb id=${ambiantlightdb.ID}`)
      }),
      catchError(this.handleError<AmbiantLightAPI>('updateAmbiantLight'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in AmbiantLightService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("AmbiantLightService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
