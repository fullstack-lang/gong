// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { TorusGeometryAPI } from './torusgeometry-api'
import { TorusGeometry, CopyTorusGeometryToTorusGeometryAPI } from './torusgeometry'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class TorusGeometryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  TorusGeometryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private torusgeometrysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.torusgeometrysUrl = origin + '/api/github.com/fullstack-lang/gong-lib-threejs/go/v1/torusgeometrys';
  }

  /** GET torusgeometrys from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI[]> {
    return this.getTorusGeometrys(Name, frontRepo)
  }
  getTorusGeometrys(Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<TorusGeometryAPI[]>(this.torusgeometrysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<TorusGeometryAPI[]>('getTorusGeometrys', []))
      );
  }

  /** GET torusgeometry by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {
    return this.getTorusGeometry(id, Name, frontRepo)
  }
  getTorusGeometry(id: number, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.torusgeometrysUrl}/${id}`;
    return this.http.get<TorusGeometryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched torusgeometry id=${id}`)),
      catchError(this.handleError<TorusGeometryAPI>(`getTorusGeometry id=${id}`))
    );
  }

  // postFront copy torusgeometry to a version with encoded pointers and post to the back
  postFront(torusgeometry: TorusGeometry, Name: string): Observable<TorusGeometryAPI> {
    let torusgeometryAPI = new TorusGeometryAPI
    CopyTorusGeometryToTorusGeometryAPI(torusgeometry, torusgeometryAPI)
    const id = typeof torusgeometryAPI === 'number' ? torusgeometryAPI : torusgeometryAPI.ID
    const url = `${this.torusgeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TorusGeometryAPI>(url, torusgeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TorusGeometryAPI>('postTorusGeometry'))
    );
  }

  /** POST: add a new torusgeometry to the server */
  post(torusgeometrydb: TorusGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {
    return this.postTorusGeometry(torusgeometrydb, Name, frontRepo)
  }
  postTorusGeometry(torusgeometrydb: TorusGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<TorusGeometryAPI>(this.torusgeometrysUrl, torusgeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted torusgeometrydb id=${torusgeometrydb.ID}`)
      }),
      catchError(this.handleError<TorusGeometryAPI>('postTorusGeometry'))
    );
  }

  /** DELETE: delete the torusgeometrydb from the server */
  delete(torusgeometrydb: TorusGeometryAPI | number, Name: string): Observable<TorusGeometryAPI> {
    return this.deleteTorusGeometry(torusgeometrydb, Name)
  }
  deleteTorusGeometry(torusgeometrydb: TorusGeometryAPI | number, Name: string): Observable<TorusGeometryAPI> {
    const id = typeof torusgeometrydb === 'number' ? torusgeometrydb : torusgeometrydb.ID;
    const url = `${this.torusgeometrysUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<TorusGeometryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted torusgeometrydb id=${id}`)),
      catchError(this.handleError<TorusGeometryAPI>('deleteTorusGeometry'))
    );
  }

  // updateFront copy torusgeometry to a version with encoded pointers and update to the back
  updateFront(torusgeometry: TorusGeometry, Name: string): Observable<TorusGeometryAPI> {
    let torusgeometryAPI = new TorusGeometryAPI
    CopyTorusGeometryToTorusGeometryAPI(torusgeometry, torusgeometryAPI)
    const id = typeof torusgeometryAPI === 'number' ? torusgeometryAPI : torusgeometryAPI.ID
    const url = `${this.torusgeometrysUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<TorusGeometryAPI>(url, torusgeometryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<TorusGeometryAPI>('updateTorusGeometry'))
    );
  }

  /** PUT: update the torusgeometrydb on the server */
  update(torusgeometrydb: TorusGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {
    return this.updateTorusGeometry(torusgeometrydb, Name, frontRepo)
  }
  updateTorusGeometry(torusgeometrydb: TorusGeometryAPI, Name: string, frontRepo: FrontRepo): Observable<TorusGeometryAPI> {
    const id = typeof torusgeometrydb === 'number' ? torusgeometrydb : torusgeometrydb.ID;
    const url = `${this.torusgeometrysUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<TorusGeometryAPI>(url, torusgeometrydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated torusgeometrydb id=${torusgeometrydb.ID}`)
      }),
      catchError(this.handleError<TorusGeometryAPI>('updateTorusGeometry'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in TorusGeometryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("TorusGeometryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
