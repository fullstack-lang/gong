// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { PngImageAPI } from './pngimage-api'
import { PngImage, CopyPngImageToPngImageAPI } from './pngimage'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class PngImageService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  PngImageServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private pngimagesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.pngimagesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/markdown/go/v1/pngimages';
  }

  /** GET pngimages from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<PngImageAPI[]> {
    return this.getPngImages(Name, frontRepo)
  }
  getPngImages(Name: string, frontRepo: FrontRepo): Observable<PngImageAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<PngImageAPI[]>(this.pngimagesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<PngImageAPI[]>('getPngImages', []))
      );
  }

  /** GET pngimage by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {
    return this.getPngImage(id, Name, frontRepo)
  }
  getPngImage(id: number, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.pngimagesUrl}/${id}`;
    return this.http.get<PngImageAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched pngimage id=${id}`)),
      catchError(this.handleError<PngImageAPI>(`getPngImage id=${id}`))
    );
  }

  // postFront copy pngimage to a version with encoded pointers and post to the back
  postFront(pngimage: PngImage, Name: string): Observable<PngImageAPI> {
    let pngimageAPI = new PngImageAPI
    CopyPngImageToPngImageAPI(pngimage, pngimageAPI)
    const id = typeof pngimageAPI === 'number' ? pngimageAPI : pngimageAPI.ID
    const url = `${this.pngimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<PngImageAPI>(url, pngimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<PngImageAPI>('postPngImage'))
    );
  }

  /** POST: add a new pngimage to the server */
  post(pngimagedb: PngImageAPI, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {
    return this.postPngImage(pngimagedb, Name, frontRepo)
  }
  postPngImage(pngimagedb: PngImageAPI, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<PngImageAPI>(this.pngimagesUrl, pngimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted pngimagedb id=${pngimagedb.ID}`)
      }),
      catchError(this.handleError<PngImageAPI>('postPngImage'))
    );
  }

  /** DELETE: delete the pngimagedb from the server */
  delete(pngimagedb: PngImageAPI | number, Name: string): Observable<PngImageAPI> {
    return this.deletePngImage(pngimagedb, Name)
  }
  deletePngImage(pngimagedb: PngImageAPI | number, Name: string): Observable<PngImageAPI> {
    const id = typeof pngimagedb === 'number' ? pngimagedb : pngimagedb.ID;
    const url = `${this.pngimagesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<PngImageAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted pngimagedb id=${id}`)),
      catchError(this.handleError<PngImageAPI>('deletePngImage'))
    );
  }

  // updateFront copy pngimage to a version with encoded pointers and update to the back
  updateFront(pngimage: PngImage, Name: string): Observable<PngImageAPI> {
    let pngimageAPI = new PngImageAPI
    CopyPngImageToPngImageAPI(pngimage, pngimageAPI)
    const id = typeof pngimageAPI === 'number' ? pngimageAPI : pngimageAPI.ID
    const url = `${this.pngimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<PngImageAPI>(url, pngimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<PngImageAPI>('updatePngImage'))
    );
  }

  /** PUT: update the pngimagedb on the server */
  update(pngimagedb: PngImageAPI, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {
    return this.updatePngImage(pngimagedb, Name, frontRepo)
  }
  updatePngImage(pngimagedb: PngImageAPI, Name: string, frontRepo: FrontRepo): Observable<PngImageAPI> {
    const id = typeof pngimagedb === 'number' ? pngimagedb : pngimagedb.ID;
    const url = `${this.pngimagesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<PngImageAPI>(url, pngimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated pngimagedb id=${pngimagedb.ID}`)
      }),
      catchError(this.handleError<PngImageAPI>('updatePngImage'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in PngImageService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("PngImageService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
