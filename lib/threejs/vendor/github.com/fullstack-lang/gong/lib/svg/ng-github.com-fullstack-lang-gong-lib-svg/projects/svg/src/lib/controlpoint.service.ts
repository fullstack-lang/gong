// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { ControlPointAPI } from './controlpoint-api'
import { ControlPoint, CopyControlPointToControlPointAPI } from './controlpoint'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { RectAPI } from './rect-api'

@Injectable({
  providedIn: 'root'
})
export class ControlPointService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ControlPointServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private controlpointsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.controlpointsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/svg/go/v1/controlpoints';
  }

  /** GET controlpoints from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI[]> {
    return this.getControlPoints(Name, frontRepo)
  }
  getControlPoints(Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<ControlPointAPI[]>(this.controlpointsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<ControlPointAPI[]>('getControlPoints', []))
      );
  }

  /** GET controlpoint by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {
    return this.getControlPoint(id, Name, frontRepo)
  }
  getControlPoint(id: number, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.controlpointsUrl}/${id}`;
    return this.http.get<ControlPointAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched controlpoint id=${id}`)),
      catchError(this.handleError<ControlPointAPI>(`getControlPoint id=${id}`))
    );
  }

  // postFront copy controlpoint to a version with encoded pointers and post to the back
  postFront(controlpoint: ControlPoint, Name: string): Observable<ControlPointAPI> {
    let controlpointAPI = new ControlPointAPI
    CopyControlPointToControlPointAPI(controlpoint, controlpointAPI)
    const id = typeof controlpointAPI === 'number' ? controlpointAPI : controlpointAPI.ID
    const url = `${this.controlpointsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ControlPointAPI>(url, controlpointAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ControlPointAPI>('postControlPoint'))
    );
  }

  /** POST: add a new controlpoint to the server */
  post(controlpointdb: ControlPointAPI, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {
    return this.postControlPoint(controlpointdb, Name, frontRepo)
  }
  postControlPoint(controlpointdb: ControlPointAPI, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ControlPointAPI>(this.controlpointsUrl, controlpointdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted controlpointdb id=${controlpointdb.ID}`)
      }),
      catchError(this.handleError<ControlPointAPI>('postControlPoint'))
    );
  }

  /** DELETE: delete the controlpointdb from the server */
  delete(controlpointdb: ControlPointAPI | number, Name: string): Observable<ControlPointAPI> {
    return this.deleteControlPoint(controlpointdb, Name)
  }
  deleteControlPoint(controlpointdb: ControlPointAPI | number, Name: string): Observable<ControlPointAPI> {
    const id = typeof controlpointdb === 'number' ? controlpointdb : controlpointdb.ID;
    const url = `${this.controlpointsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ControlPointAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted controlpointdb id=${id}`)),
      catchError(this.handleError<ControlPointAPI>('deleteControlPoint'))
    );
  }

  // updateFront copy controlpoint to a version with encoded pointers and update to the back
  updateFront(controlpoint: ControlPoint, Name: string): Observable<ControlPointAPI> {
    let controlpointAPI = new ControlPointAPI
    CopyControlPointToControlPointAPI(controlpoint, controlpointAPI)
    const id = typeof controlpointAPI === 'number' ? controlpointAPI : controlpointAPI.ID
    const url = `${this.controlpointsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<ControlPointAPI>(url, controlpointAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ControlPointAPI>('updateControlPoint'))
    );
  }

  /** PUT: update the controlpointdb on the server */
  update(controlpointdb: ControlPointAPI, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {
    return this.updateControlPoint(controlpointdb, Name, frontRepo)
  }
  updateControlPoint(controlpointdb: ControlPointAPI, Name: string, frontRepo: FrontRepo): Observable<ControlPointAPI> {
    const id = typeof controlpointdb === 'number' ? controlpointdb : controlpointdb.ID;
    const url = `${this.controlpointsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ControlPointAPI>(url, controlpointdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated controlpointdb id=${controlpointdb.ID}`)
      }),
      catchError(this.handleError<ControlPointAPI>('updateControlPoint'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ControlPointService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("ControlPointService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
