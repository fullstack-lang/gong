// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { RectAnchoredPngImageAPI } from './rectanchoredpngimage-api'
import { RectAnchoredPngImage, CopyRectAnchoredPngImageToRectAnchoredPngImageAPI } from './rectanchoredpngimage'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class RectAnchoredPngImageService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  RectAnchoredPngImageServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private rectanchoredpngimagesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.rectanchoredpngimagesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/svg/go/v1/rectanchoredpngimages';
  }

  /** GET rectanchoredpngimages from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI[]> {
    return this.getRectAnchoredPngImages(Name, frontRepo)
  }
  getRectAnchoredPngImages(Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<RectAnchoredPngImageAPI[]>(this.rectanchoredpngimagesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<RectAnchoredPngImageAPI[]>('getRectAnchoredPngImages', []))
      );
  }

  /** GET rectanchoredpngimage by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {
    return this.getRectAnchoredPngImage(id, Name, frontRepo)
  }
  getRectAnchoredPngImage(id: number, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.rectanchoredpngimagesUrl}/${id}`;
    return this.http.get<RectAnchoredPngImageAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched rectanchoredpngimage id=${id}`)),
      catchError(this.handleError<RectAnchoredPngImageAPI>(`getRectAnchoredPngImage id=${id}`))
    );
  }

  // postFront copy rectanchoredpngimage to a version with encoded pointers and post to the back
  postFront(rectanchoredpngimage: RectAnchoredPngImage, Name: string): Observable<RectAnchoredPngImageAPI> {
    let rectanchoredpngimageAPI = new RectAnchoredPngImageAPI
    CopyRectAnchoredPngImageToRectAnchoredPngImageAPI(rectanchoredpngimage, rectanchoredpngimageAPI)
    const id = typeof rectanchoredpngimageAPI === 'number' ? rectanchoredpngimageAPI : rectanchoredpngimageAPI.ID
    const url = `${this.rectanchoredpngimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<RectAnchoredPngImageAPI>(url, rectanchoredpngimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<RectAnchoredPngImageAPI>('postRectAnchoredPngImage'))
    );
  }

  /** POST: add a new rectanchoredpngimage to the server */
  post(rectanchoredpngimagedb: RectAnchoredPngImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {
    return this.postRectAnchoredPngImage(rectanchoredpngimagedb, Name, frontRepo)
  }
  postRectAnchoredPngImage(rectanchoredpngimagedb: RectAnchoredPngImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<RectAnchoredPngImageAPI>(this.rectanchoredpngimagesUrl, rectanchoredpngimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted rectanchoredpngimagedb id=${rectanchoredpngimagedb.ID}`)
      }),
      catchError(this.handleError<RectAnchoredPngImageAPI>('postRectAnchoredPngImage'))
    );
  }

  /** DELETE: delete the rectanchoredpngimagedb from the server */
  delete(rectanchoredpngimagedb: RectAnchoredPngImageAPI | number, Name: string): Observable<RectAnchoredPngImageAPI> {
    return this.deleteRectAnchoredPngImage(rectanchoredpngimagedb, Name)
  }
  deleteRectAnchoredPngImage(rectanchoredpngimagedb: RectAnchoredPngImageAPI | number, Name: string): Observable<RectAnchoredPngImageAPI> {
    const id = typeof rectanchoredpngimagedb === 'number' ? rectanchoredpngimagedb : rectanchoredpngimagedb.ID;
    const url = `${this.rectanchoredpngimagesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<RectAnchoredPngImageAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted rectanchoredpngimagedb id=${id}`)),
      catchError(this.handleError<RectAnchoredPngImageAPI>('deleteRectAnchoredPngImage'))
    );
  }

  // updateFront copy rectanchoredpngimage to a version with encoded pointers and update to the back
  updateFront(rectanchoredpngimage: RectAnchoredPngImage, Name: string): Observable<RectAnchoredPngImageAPI> {
    let rectanchoredpngimageAPI = new RectAnchoredPngImageAPI
    CopyRectAnchoredPngImageToRectAnchoredPngImageAPI(rectanchoredpngimage, rectanchoredpngimageAPI)
    const id = typeof rectanchoredpngimageAPI === 'number' ? rectanchoredpngimageAPI : rectanchoredpngimageAPI.ID
    const url = `${this.rectanchoredpngimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<RectAnchoredPngImageAPI>(url, rectanchoredpngimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<RectAnchoredPngImageAPI>('updateRectAnchoredPngImage'))
    );
  }

  /** PUT: update the rectanchoredpngimagedb on the server */
  update(rectanchoredpngimagedb: RectAnchoredPngImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {
    return this.updateRectAnchoredPngImage(rectanchoredpngimagedb, Name, frontRepo)
  }
  updateRectAnchoredPngImage(rectanchoredpngimagedb: RectAnchoredPngImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredPngImageAPI> {
    const id = typeof rectanchoredpngimagedb === 'number' ? rectanchoredpngimagedb : rectanchoredpngimagedb.ID;
    const url = `${this.rectanchoredpngimagesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<RectAnchoredPngImageAPI>(url, rectanchoredpngimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated rectanchoredpngimagedb id=${rectanchoredpngimagedb.ID}`)
      }),
      catchError(this.handleError<RectAnchoredPngImageAPI>('updateRectAnchoredPngImage'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in RectAnchoredPngImageService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("RectAnchoredPngImageService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
