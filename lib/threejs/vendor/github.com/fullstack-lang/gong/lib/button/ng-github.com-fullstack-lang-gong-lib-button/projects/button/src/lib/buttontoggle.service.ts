// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { ButtonToggleAPI } from './buttontoggle-api'
import { ButtonToggle, CopyButtonToggleToButtonToggleAPI } from './buttontoggle'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class ButtonToggleService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ButtonToggleServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private buttontogglesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.buttontogglesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/button/go/v1/buttontoggles';
  }

  /** GET buttontoggles from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI[]> {
    return this.getButtonToggles(Name, frontRepo)
  }
  getButtonToggles(Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<ButtonToggleAPI[]>(this.buttontogglesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<ButtonToggleAPI[]>('getButtonToggles', []))
      );
  }

  /** GET buttontoggle by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {
    return this.getButtonToggle(id, Name, frontRepo)
  }
  getButtonToggle(id: number, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.buttontogglesUrl}/${id}`;
    return this.http.get<ButtonToggleAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched buttontoggle id=${id}`)),
      catchError(this.handleError<ButtonToggleAPI>(`getButtonToggle id=${id}`))
    );
  }

  // postFront copy buttontoggle to a version with encoded pointers and post to the back
  postFront(buttontoggle: ButtonToggle, Name: string): Observable<ButtonToggleAPI> {
    let buttontoggleAPI = new ButtonToggleAPI
    CopyButtonToggleToButtonToggleAPI(buttontoggle, buttontoggleAPI)
    const id = typeof buttontoggleAPI === 'number' ? buttontoggleAPI : buttontoggleAPI.ID
    const url = `${this.buttontogglesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ButtonToggleAPI>(url, buttontoggleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ButtonToggleAPI>('postButtonToggle'))
    );
  }

  /** POST: add a new buttontoggle to the server */
  post(buttontoggledb: ButtonToggleAPI, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {
    return this.postButtonToggle(buttontoggledb, Name, frontRepo)
  }
  postButtonToggle(buttontoggledb: ButtonToggleAPI, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ButtonToggleAPI>(this.buttontogglesUrl, buttontoggledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted buttontoggledb id=${buttontoggledb.ID}`)
      }),
      catchError(this.handleError<ButtonToggleAPI>('postButtonToggle'))
    );
  }

  /** DELETE: delete the buttontoggledb from the server */
  delete(buttontoggledb: ButtonToggleAPI | number, Name: string): Observable<ButtonToggleAPI> {
    return this.deleteButtonToggle(buttontoggledb, Name)
  }
  deleteButtonToggle(buttontoggledb: ButtonToggleAPI | number, Name: string): Observable<ButtonToggleAPI> {
    const id = typeof buttontoggledb === 'number' ? buttontoggledb : buttontoggledb.ID;
    const url = `${this.buttontogglesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ButtonToggleAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted buttontoggledb id=${id}`)),
      catchError(this.handleError<ButtonToggleAPI>('deleteButtonToggle'))
    );
  }

  // updateFront copy buttontoggle to a version with encoded pointers and update to the back
  updateFront(buttontoggle: ButtonToggle, Name: string): Observable<ButtonToggleAPI> {
    let buttontoggleAPI = new ButtonToggleAPI
    CopyButtonToggleToButtonToggleAPI(buttontoggle, buttontoggleAPI)
    const id = typeof buttontoggleAPI === 'number' ? buttontoggleAPI : buttontoggleAPI.ID
    const url = `${this.buttontogglesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<ButtonToggleAPI>(url, buttontoggleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ButtonToggleAPI>('updateButtonToggle'))
    );
  }

  /** PUT: update the buttontoggledb on the server */
  update(buttontoggledb: ButtonToggleAPI, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {
    return this.updateButtonToggle(buttontoggledb, Name, frontRepo)
  }
  updateButtonToggle(buttontoggledb: ButtonToggleAPI, Name: string, frontRepo: FrontRepo): Observable<ButtonToggleAPI> {
    const id = typeof buttontoggledb === 'number' ? buttontoggledb : buttontoggledb.ID;
    const url = `${this.buttontogglesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ButtonToggleAPI>(url, buttontoggledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated buttontoggledb id=${buttontoggledb.ID}`)
      }),
      catchError(this.handleError<ButtonToggleAPI>('updateButtonToggle'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ButtonToggleService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("ButtonToggleService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
