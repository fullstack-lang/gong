// generated code, do not edit
// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { CellStringAPI } from './cellstring-api'
import { CellString, CopyCellStringToCellStringAPI } from './cellstring'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class CellStringService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CellStringServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private cellstringsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.cellstringsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/table/go/v1/cellstrings';
  }

  // updateFront copy cellstring to a version with encoded pointers and update to the back
  updateFront(cellstring: CellString, Name: string): Observable<CellStringAPI> {
    let cellstringAPI = new CellStringAPI
    CopyCellStringToCellStringAPI(cellstring, cellstringAPI)
    const id = typeof cellstringAPI === 'number' ? cellstringAPI : cellstringAPI.ID
    const url = `${this.cellstringsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<CellStringAPI>(url, cellstringAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<CellStringAPI>('updateCellString'))
    );
  }

  /** PUT: update the cellstringdb on the server */
  update(cellstringdb: CellStringAPI, Name: string, frontRepo: FrontRepo): Observable<CellStringAPI> {
    return this.updateCellString(cellstringdb, Name, frontRepo)
  }
  updateCellString(cellstringdb: CellStringAPI, Name: string, frontRepo: FrontRepo): Observable<CellStringAPI> {
    const id = typeof cellstringdb === 'number' ? cellstringdb : cellstringdb.ID;
    const url = `${this.cellstringsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CellStringAPI>(url, cellstringdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated cellstringdb id=${cellstringdb.ID}`)
      }),
      catchError(this.handleError<CellStringAPI>('updateCellString'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CellStringService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("CellStringService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
