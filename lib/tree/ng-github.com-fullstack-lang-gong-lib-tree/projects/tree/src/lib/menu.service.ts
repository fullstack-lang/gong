// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { MenuAPI } from './menu-api'
import { Menu, CopyMenuToMenuAPI } from './menu'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ButtonAPI } from './button-api'

@Injectable({
  providedIn: 'root'
})
export class MenuService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  MenuServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private menusUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.menusUrl = origin + '/api/github.com/fullstack-lang/gong/lib/tree/go/v1/menus';
  }

  /** GET menus from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<MenuAPI[]> {
    return this.getMenus(Name, frontRepo)
  }
  getMenus(Name: string, frontRepo: FrontRepo): Observable<MenuAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<MenuAPI[]>(this.menusUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<MenuAPI[]>('getMenus', []))
      );
  }

  /** GET menu by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {
    return this.getMenu(id, Name, frontRepo)
  }
  getMenu(id: number, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.menusUrl}/${id}`;
    return this.http.get<MenuAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched menu id=${id}`)),
      catchError(this.handleError<MenuAPI>(`getMenu id=${id}`))
    );
  }

  // postFront copy menu to a version with encoded pointers and post to the back
  postFront(menu: Menu, Name: string): Observable<MenuAPI> {
    let menuAPI = new MenuAPI
    CopyMenuToMenuAPI(menu, menuAPI)
    const id = typeof menuAPI === 'number' ? menuAPI : menuAPI.ID
    const url = `${this.menusUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MenuAPI>(url, menuAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MenuAPI>('postMenu'))
    );
  }

  /** POST: add a new menu to the server */
  post(menudb: MenuAPI, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {
    return this.postMenu(menudb, Name, frontRepo)
  }
  postMenu(menudb: MenuAPI, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MenuAPI>(this.menusUrl, menudb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted menudb id=${menudb.ID}`)
      }),
      catchError(this.handleError<MenuAPI>('postMenu'))
    );
  }

  /** DELETE: delete the menudb from the server */
  delete(menudb: MenuAPI | number, Name: string): Observable<MenuAPI> {
    return this.deleteMenu(menudb, Name)
  }
  deleteMenu(menudb: MenuAPI | number, Name: string): Observable<MenuAPI> {
    const id = typeof menudb === 'number' ? menudb : menudb.ID;
    const url = `${this.menusUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<MenuAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted menudb id=${id}`)),
      catchError(this.handleError<MenuAPI>('deleteMenu'))
    );
  }

  // updateFront copy menu to a version with encoded pointers and update to the back
  updateFront(menu: Menu, Name: string): Observable<MenuAPI> {
    let menuAPI = new MenuAPI
    CopyMenuToMenuAPI(menu, menuAPI)
    const id = typeof menuAPI === 'number' ? menuAPI : menuAPI.ID
    const url = `${this.menusUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<MenuAPI>(url, menuAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MenuAPI>('updateMenu'))
    );
  }

  /** PUT: update the menudb on the server */
  update(menudb: MenuAPI, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {
    return this.updateMenu(menudb, Name, frontRepo)
  }
  updateMenu(menudb: MenuAPI, Name: string, frontRepo: FrontRepo): Observable<MenuAPI> {
    const id = typeof menudb === 'number' ? menudb : menudb.ID;
    const url = `${this.menusUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<MenuAPI>(url, menudb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated menudb id=${menudb.ID}`)
      }),
      catchError(this.handleError<MenuAPI>('updateMenu'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in MenuService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("MenuService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
