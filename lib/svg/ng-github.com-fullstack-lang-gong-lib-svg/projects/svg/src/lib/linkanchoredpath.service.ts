// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { LinkAnchoredPathAPI } from './linkanchoredpath-api'
import { LinkAnchoredPath, CopyLinkAnchoredPathToLinkAnchoredPathAPI } from './linkanchoredpath'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class LinkAnchoredPathService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  LinkAnchoredPathServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private linkanchoredpathsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.linkanchoredpathsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/svg/go/v1/linkanchoredpaths';
  }

  /** GET linkanchoredpaths from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI[]> {
    return this.getLinkAnchoredPaths(Name, frontRepo)
  }
  getLinkAnchoredPaths(Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<LinkAnchoredPathAPI[]>(this.linkanchoredpathsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<LinkAnchoredPathAPI[]>('getLinkAnchoredPaths', []))
      );
  }

  /** GET linkanchoredpath by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {
    return this.getLinkAnchoredPath(id, Name, frontRepo)
  }
  getLinkAnchoredPath(id: number, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.linkanchoredpathsUrl}/${id}`;
    return this.http.get<LinkAnchoredPathAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched linkanchoredpath id=${id}`)),
      catchError(this.handleError<LinkAnchoredPathAPI>(`getLinkAnchoredPath id=${id}`))
    );
  }

  // postFront copy linkanchoredpath to a version with encoded pointers and post to the back
  postFront(linkanchoredpath: LinkAnchoredPath, Name: string): Observable<LinkAnchoredPathAPI> {
    let linkanchoredpathAPI = new LinkAnchoredPathAPI
    CopyLinkAnchoredPathToLinkAnchoredPathAPI(linkanchoredpath, linkanchoredpathAPI)
    const id = typeof linkanchoredpathAPI === 'number' ? linkanchoredpathAPI : linkanchoredpathAPI.ID
    const url = `${this.linkanchoredpathsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<LinkAnchoredPathAPI>(url, linkanchoredpathAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<LinkAnchoredPathAPI>('postLinkAnchoredPath'))
    );
  }

  /** POST: add a new linkanchoredpath to the server */
  post(linkanchoredpathdb: LinkAnchoredPathAPI, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {
    return this.postLinkAnchoredPath(linkanchoredpathdb, Name, frontRepo)
  }
  postLinkAnchoredPath(linkanchoredpathdb: LinkAnchoredPathAPI, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<LinkAnchoredPathAPI>(this.linkanchoredpathsUrl, linkanchoredpathdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted linkanchoredpathdb id=${linkanchoredpathdb.ID}`)
      }),
      catchError(this.handleError<LinkAnchoredPathAPI>('postLinkAnchoredPath'))
    );
  }

  /** DELETE: delete the linkanchoredpathdb from the server */
  delete(linkanchoredpathdb: LinkAnchoredPathAPI | number, Name: string): Observable<LinkAnchoredPathAPI> {
    return this.deleteLinkAnchoredPath(linkanchoredpathdb, Name)
  }
  deleteLinkAnchoredPath(linkanchoredpathdb: LinkAnchoredPathAPI | number, Name: string): Observable<LinkAnchoredPathAPI> {
    const id = typeof linkanchoredpathdb === 'number' ? linkanchoredpathdb : linkanchoredpathdb.ID;
    const url = `${this.linkanchoredpathsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<LinkAnchoredPathAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted linkanchoredpathdb id=${id}`)),
      catchError(this.handleError<LinkAnchoredPathAPI>('deleteLinkAnchoredPath'))
    );
  }

  // updateFront copy linkanchoredpath to a version with encoded pointers and update to the back
  updateFront(linkanchoredpath: LinkAnchoredPath, Name: string): Observable<LinkAnchoredPathAPI> {
    let linkanchoredpathAPI = new LinkAnchoredPathAPI
    CopyLinkAnchoredPathToLinkAnchoredPathAPI(linkanchoredpath, linkanchoredpathAPI)
    const id = typeof linkanchoredpathAPI === 'number' ? linkanchoredpathAPI : linkanchoredpathAPI.ID
    const url = `${this.linkanchoredpathsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<LinkAnchoredPathAPI>(url, linkanchoredpathAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<LinkAnchoredPathAPI>('updateLinkAnchoredPath'))
    );
  }

  /** PUT: update the linkanchoredpathdb on the server */
  update(linkanchoredpathdb: LinkAnchoredPathAPI, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {
    return this.updateLinkAnchoredPath(linkanchoredpathdb, Name, frontRepo)
  }
  updateLinkAnchoredPath(linkanchoredpathdb: LinkAnchoredPathAPI, Name: string, frontRepo: FrontRepo): Observable<LinkAnchoredPathAPI> {
    const id = typeof linkanchoredpathdb === 'number' ? linkanchoredpathdb : linkanchoredpathdb.ID;
    const url = `${this.linkanchoredpathsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<LinkAnchoredPathAPI>(url, linkanchoredpathdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated linkanchoredpathdb id=${linkanchoredpathdb.ID}`)
      }),
      catchError(this.handleError<LinkAnchoredPathAPI>('updateLinkAnchoredPath'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in LinkAnchoredPathService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("LinkAnchoredPathService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
