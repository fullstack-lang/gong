// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { RectAnchoredSvgImageAPI } from './rectanchoredsvgimage-api'
import { RectAnchoredSvgImage, CopyRectAnchoredSvgImageToRectAnchoredSvgImageAPI } from './rectanchoredsvgimage'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class RectAnchoredSvgImageService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  RectAnchoredSvgImageServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private rectanchoredsvgimagesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.rectanchoredsvgimagesUrl = origin + '/api/github.com/fullstack-lang/gong/lib/svg/go/v1/rectanchoredsvgimages';
  }

  /** GET rectanchoredsvgimages from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI[]> {
    return this.getRectAnchoredSvgImages(Name, frontRepo)
  }
  getRectAnchoredSvgImages(Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<RectAnchoredSvgImageAPI[]>(this.rectanchoredsvgimagesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<RectAnchoredSvgImageAPI[]>('getRectAnchoredSvgImages', []))
      );
  }

  /** GET rectanchoredsvgimage by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {
    return this.getRectAnchoredSvgImage(id, Name, frontRepo)
  }
  getRectAnchoredSvgImage(id: number, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.rectanchoredsvgimagesUrl}/${id}`;
    return this.http.get<RectAnchoredSvgImageAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched rectanchoredsvgimage id=${id}`)),
      catchError(this.handleError<RectAnchoredSvgImageAPI>(`getRectAnchoredSvgImage id=${id}`))
    );
  }

  // postFront copy rectanchoredsvgimage to a version with encoded pointers and post to the back
  postFront(rectanchoredsvgimage: RectAnchoredSvgImage, Name: string): Observable<RectAnchoredSvgImageAPI> {
    let rectanchoredsvgimageAPI = new RectAnchoredSvgImageAPI
    CopyRectAnchoredSvgImageToRectAnchoredSvgImageAPI(rectanchoredsvgimage, rectanchoredsvgimageAPI)
    const id = typeof rectanchoredsvgimageAPI === 'number' ? rectanchoredsvgimageAPI : rectanchoredsvgimageAPI.ID
    const url = `${this.rectanchoredsvgimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<RectAnchoredSvgImageAPI>(url, rectanchoredsvgimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<RectAnchoredSvgImageAPI>('postRectAnchoredSvgImage'))
    );
  }

  /** POST: add a new rectanchoredsvgimage to the server */
  post(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {
    return this.postRectAnchoredSvgImage(rectanchoredsvgimagedb, Name, frontRepo)
  }
  postRectAnchoredSvgImage(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<RectAnchoredSvgImageAPI>(this.rectanchoredsvgimagesUrl, rectanchoredsvgimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted rectanchoredsvgimagedb id=${rectanchoredsvgimagedb.ID}`)
      }),
      catchError(this.handleError<RectAnchoredSvgImageAPI>('postRectAnchoredSvgImage'))
    );
  }

  /** DELETE: delete the rectanchoredsvgimagedb from the server */
  delete(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI | number, Name: string): Observable<RectAnchoredSvgImageAPI> {
    return this.deleteRectAnchoredSvgImage(rectanchoredsvgimagedb, Name)
  }
  deleteRectAnchoredSvgImage(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI | number, Name: string): Observable<RectAnchoredSvgImageAPI> {
    const id = typeof rectanchoredsvgimagedb === 'number' ? rectanchoredsvgimagedb : rectanchoredsvgimagedb.ID;
    const url = `${this.rectanchoredsvgimagesUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<RectAnchoredSvgImageAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted rectanchoredsvgimagedb id=${id}`)),
      catchError(this.handleError<RectAnchoredSvgImageAPI>('deleteRectAnchoredSvgImage'))
    );
  }

  // updateFront copy rectanchoredsvgimage to a version with encoded pointers and update to the back
  updateFront(rectanchoredsvgimage: RectAnchoredSvgImage, Name: string): Observable<RectAnchoredSvgImageAPI> {
    let rectanchoredsvgimageAPI = new RectAnchoredSvgImageAPI
    CopyRectAnchoredSvgImageToRectAnchoredSvgImageAPI(rectanchoredsvgimage, rectanchoredsvgimageAPI)
    const id = typeof rectanchoredsvgimageAPI === 'number' ? rectanchoredsvgimageAPI : rectanchoredsvgimageAPI.ID
    const url = `${this.rectanchoredsvgimagesUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<RectAnchoredSvgImageAPI>(url, rectanchoredsvgimageAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<RectAnchoredSvgImageAPI>('updateRectAnchoredSvgImage'))
    );
  }

  /** PUT: update the rectanchoredsvgimagedb on the server */
  update(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {
    return this.updateRectAnchoredSvgImage(rectanchoredsvgimagedb, Name, frontRepo)
  }
  updateRectAnchoredSvgImage(rectanchoredsvgimagedb: RectAnchoredSvgImageAPI, Name: string, frontRepo: FrontRepo): Observable<RectAnchoredSvgImageAPI> {
    const id = typeof rectanchoredsvgimagedb === 'number' ? rectanchoredsvgimagedb : rectanchoredsvgimagedb.ID;
    const url = `${this.rectanchoredsvgimagesUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<RectAnchoredSvgImageAPI>(url, rectanchoredsvgimagedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated rectanchoredsvgimagedb id=${rectanchoredsvgimagedb.ID}`)
      }),
      catchError(this.handleError<RectAnchoredSvgImageAPI>('updateRectAnchoredSvgImage'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in RectAnchoredSvgImageService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("RectAnchoredSvgImageService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
