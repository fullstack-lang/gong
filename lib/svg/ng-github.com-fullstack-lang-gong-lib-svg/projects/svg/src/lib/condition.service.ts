// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { ConditionAPI } from './condition-api'
import { Condition, CopyConditionToConditionAPI } from './condition'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class ConditionService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ConditionServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private conditionsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.conditionsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/svg/go/v1/conditions';
  }

  /** GET conditions from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<ConditionAPI[]> {
    return this.getConditions(Name, frontRepo)
  }
  getConditions(Name: string, frontRepo: FrontRepo): Observable<ConditionAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<ConditionAPI[]>(this.conditionsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<ConditionAPI[]>('getConditions', []))
      );
  }

  /** GET condition by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {
    return this.getCondition(id, Name, frontRepo)
  }
  getCondition(id: number, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.conditionsUrl}/${id}`;
    return this.http.get<ConditionAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched condition id=${id}`)),
      catchError(this.handleError<ConditionAPI>(`getCondition id=${id}`))
    );
  }

  // postFront copy condition to a version with encoded pointers and post to the back
  postFront(condition: Condition, Name: string): Observable<ConditionAPI> {
    let conditionAPI = new ConditionAPI
    CopyConditionToConditionAPI(condition, conditionAPI)
    const id = typeof conditionAPI === 'number' ? conditionAPI : conditionAPI.ID
    const url = `${this.conditionsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ConditionAPI>(url, conditionAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ConditionAPI>('postCondition'))
    );
  }

  /** POST: add a new condition to the server */
  post(conditiondb: ConditionAPI, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {
    return this.postCondition(conditiondb, Name, frontRepo)
  }
  postCondition(conditiondb: ConditionAPI, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ConditionAPI>(this.conditionsUrl, conditiondb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted conditiondb id=${conditiondb.ID}`)
      }),
      catchError(this.handleError<ConditionAPI>('postCondition'))
    );
  }

  /** DELETE: delete the conditiondb from the server */
  delete(conditiondb: ConditionAPI | number, Name: string): Observable<ConditionAPI> {
    return this.deleteCondition(conditiondb, Name)
  }
  deleteCondition(conditiondb: ConditionAPI | number, Name: string): Observable<ConditionAPI> {
    const id = typeof conditiondb === 'number' ? conditiondb : conditiondb.ID;
    const url = `${this.conditionsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ConditionAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted conditiondb id=${id}`)),
      catchError(this.handleError<ConditionAPI>('deleteCondition'))
    );
  }

  // updateFront copy condition to a version with encoded pointers and update to the back
  updateFront(condition: Condition, Name: string): Observable<ConditionAPI> {
    let conditionAPI = new ConditionAPI
    CopyConditionToConditionAPI(condition, conditionAPI)
    const id = typeof conditionAPI === 'number' ? conditionAPI : conditionAPI.ID
    const url = `${this.conditionsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<ConditionAPI>(url, conditionAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ConditionAPI>('updateCondition'))
    );
  }

  /** PUT: update the conditiondb on the server */
  update(conditiondb: ConditionAPI, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {
    return this.updateCondition(conditiondb, Name, frontRepo)
  }
  updateCondition(conditiondb: ConditionAPI, Name: string, frontRepo: FrontRepo): Observable<ConditionAPI> {
    const id = typeof conditiondb === 'number' ? conditiondb : conditiondb.ID;
    const url = `${this.conditionsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ConditionAPI>(url, conditiondb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated conditiondb id=${conditiondb.ID}`)
      }),
      catchError(this.handleError<ConditionAPI>('updateCondition'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ConditionService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("ConditionService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
