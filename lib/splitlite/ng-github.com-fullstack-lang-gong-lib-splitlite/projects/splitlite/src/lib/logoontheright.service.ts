// generated code, do not edit
// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { LogoOnTheRightAPI } from './logoontheright-api'
import { LogoOnTheRight, CopyLogoOnTheRightToLogoOnTheRightAPI } from './logoontheright'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class LogoOnTheRightService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  LogoOnTheRightServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private logoontherightsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.logoontherightsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/splitlite/go/v1/logoontherights';
  }

  // updateFront copy logoontheright to a version with encoded pointers and update to the back
  updateFront(logoontheright: LogoOnTheRight, Name: string): Observable<LogoOnTheRightAPI> {
    let logoontherightAPI = new LogoOnTheRightAPI
    CopyLogoOnTheRightToLogoOnTheRightAPI(logoontheright, logoontherightAPI)
    const id = typeof logoontherightAPI === 'number' ? logoontherightAPI : logoontherightAPI.ID
    const url = `${this.logoontherightsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<LogoOnTheRightAPI>(url, logoontherightAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<LogoOnTheRightAPI>('updateLogoOnTheRight'))
    );
  }

  /** PUT: update the logoontherightdb on the server */
  update(logoontherightdb: LogoOnTheRightAPI, Name: string, frontRepo: FrontRepo): Observable<LogoOnTheRightAPI> {
    return this.updateLogoOnTheRight(logoontherightdb, Name, frontRepo)
  }
  updateLogoOnTheRight(logoontherightdb: LogoOnTheRightAPI, Name: string, frontRepo: FrontRepo): Observable<LogoOnTheRightAPI> {
    const id = typeof logoontherightdb === 'number' ? logoontherightdb : logoontherightdb.ID;
    const url = `${this.logoontherightsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<LogoOnTheRightAPI>(url, logoontherightdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated logoontherightdb id=${logoontherightdb.ID}`)
      }),
      catchError(this.handleError<LogoOnTheRightAPI>('updateLogoOnTheRight'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in LogoOnTheRightService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("LogoOnTheRightService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
