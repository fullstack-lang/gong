// generated code, do not edit
// generated by ng_file_service_ts
import { Injectable, Component, Inject, DOCUMENT } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { Location } from '@angular/common'
/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { LogoOnTheLeftAPI } from './logoontheleft-api'
import { LogoOnTheLeft, CopyLogoOnTheLeftToLogoOnTheLeftAPI } from './logoontheleft'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class LogoOnTheLeftService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  LogoOnTheLeftServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private logoontheleftsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.logoontheleftsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/splitlite/go/v1/logoonthelefts';
  }

  // updateFront copy logoontheleft to a version with encoded pointers and update to the back
  updateFront(logoontheleft: LogoOnTheLeft, Name: string): Observable<LogoOnTheLeftAPI> {
    let logoontheleftAPI = new LogoOnTheLeftAPI
    CopyLogoOnTheLeftToLogoOnTheLeftAPI(logoontheleft, logoontheleftAPI)
    const id = typeof logoontheleftAPI === 'number' ? logoontheleftAPI : logoontheleftAPI.ID
    const url = `${this.logoontheleftsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<LogoOnTheLeftAPI>(url, logoontheleftAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<LogoOnTheLeftAPI>('updateLogoOnTheLeft'))
    );
  }

  /** PUT: update the logoontheleftdb on the server */
  update(logoontheleftdb: LogoOnTheLeftAPI, Name: string, frontRepo: FrontRepo): Observable<LogoOnTheLeftAPI> {
    return this.updateLogoOnTheLeft(logoontheleftdb, Name, frontRepo)
  }
  updateLogoOnTheLeft(logoontheleftdb: LogoOnTheLeftAPI, Name: string, frontRepo: FrontRepo): Observable<LogoOnTheLeftAPI> {
    const id = typeof logoontheleftdb === 'number' ? logoontheleftdb : logoontheleftdb.ID;
    const url = `${this.logoontheleftsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<LogoOnTheLeftAPI>(url, logoontheleftdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated logoontheleftdb id=${logoontheleftdb.ID}`)
      }),
      catchError(this.handleError<LogoOnTheLeftAPI>('updateLogoOnTheLeft'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in LogoOnTheLeftService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging
      console.error("LogoOnTheLeftService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
