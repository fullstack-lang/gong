// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { MarkdownAPI } from './markdown-api'
import { Markdown, CopyMarkdownToMarkdownAPI } from './markdown'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class MarkdownService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  MarkdownServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private markdownsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.markdownsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/split/go/v1/markdowns';
  }

  /** GET markdowns from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI[]> {
    return this.getMarkdowns(Name, frontRepo)
  }
  getMarkdowns(Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<MarkdownAPI[]>(this.markdownsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<MarkdownAPI[]>('getMarkdowns', []))
      );
  }

  /** GET markdown by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {
    return this.getMarkdown(id, Name, frontRepo)
  }
  getMarkdown(id: number, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.markdownsUrl}/${id}`;
    return this.http.get<MarkdownAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched markdown id=${id}`)),
      catchError(this.handleError<MarkdownAPI>(`getMarkdown id=${id}`))
    );
  }

  // postFront copy markdown to a version with encoded pointers and post to the back
  postFront(markdown: Markdown, Name: string): Observable<MarkdownAPI> {
    let markdownAPI = new MarkdownAPI
    CopyMarkdownToMarkdownAPI(markdown, markdownAPI)
    const id = typeof markdownAPI === 'number' ? markdownAPI : markdownAPI.ID
    const url = `${this.markdownsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MarkdownAPI>(url, markdownAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MarkdownAPI>('postMarkdown'))
    );
  }
  
  /** POST: add a new markdown to the server */
  post(markdowndb: MarkdownAPI, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {
    return this.postMarkdown(markdowndb, Name, frontRepo)
  }
  postMarkdown(markdowndb: MarkdownAPI, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<MarkdownAPI>(this.markdownsUrl, markdowndb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted markdowndb id=${markdowndb.ID}`)
      }),
      catchError(this.handleError<MarkdownAPI>('postMarkdown'))
    );
  }

  /** DELETE: delete the markdowndb from the server */
  delete(markdowndb: MarkdownAPI | number, Name: string): Observable<MarkdownAPI> {
    return this.deleteMarkdown(markdowndb, Name)
  }
  deleteMarkdown(markdowndb: MarkdownAPI | number, Name: string): Observable<MarkdownAPI> {
    const id = typeof markdowndb === 'number' ? markdowndb : markdowndb.ID;
    const url = `${this.markdownsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<MarkdownAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted markdowndb id=${id}`)),
      catchError(this.handleError<MarkdownAPI>('deleteMarkdown'))
    );
  }

  // updateFront copy markdown to a version with encoded pointers and update to the back
  updateFront(markdown: Markdown, Name: string): Observable<MarkdownAPI> {
    let markdownAPI = new MarkdownAPI
    CopyMarkdownToMarkdownAPI(markdown, markdownAPI)
    const id = typeof markdownAPI === 'number' ? markdownAPI : markdownAPI.ID
    const url = `${this.markdownsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<MarkdownAPI>(url, markdownAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<MarkdownAPI>('updateMarkdown'))
    );
  }

  /** PUT: update the markdowndb on the server */
  update(markdowndb: MarkdownAPI, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {
    return this.updateMarkdown(markdowndb, Name, frontRepo)
  }
  updateMarkdown(markdowndb: MarkdownAPI, Name: string, frontRepo: FrontRepo): Observable<MarkdownAPI> {
    const id = typeof markdowndb === 'number' ? markdowndb : markdowndb.ID;
    const url = `${this.markdownsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<MarkdownAPI>(url, markdowndb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated markdowndb id=${markdowndb.ID}`)
      }),
      catchError(this.handleError<MarkdownAPI>('updateMarkdown'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in MarkdownService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("MarkdownService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
