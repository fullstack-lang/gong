// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SplitAreaAPI } from './splitarea-api'
import { SplitArea, CopySplitAreaToSplitAreaAPI } from './splitarea'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class SplitAreaService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SplitAreaServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private splitareasUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.splitareasUrl = origin + '/api/github.com/fullstack-lang/gong/lib/split/go/v1/splitareas';
  }

  /** GET splitareas from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI[]> {
    return this.getSplitAreas(GONG__StackPath, frontRepo)
  }
  getSplitAreas(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SplitAreaAPI[]>(this.splitareasUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SplitAreaAPI[]>('getSplitAreas', []))
      );
  }

  /** GET splitarea by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {
    return this.getSplitArea(id, GONG__StackPath, frontRepo)
  }
  getSplitArea(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.splitareasUrl}/${id}`;
    return this.http.get<SplitAreaAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched splitarea id=${id}`)),
      catchError(this.handleError<SplitAreaAPI>(`getSplitArea id=${id}`))
    );
  }

  // postFront copy splitarea to a version with encoded pointers and post to the back
  postFront(splitarea: SplitArea, GONG__StackPath: string): Observable<SplitAreaAPI> {
    let splitareaAPI = new SplitAreaAPI
    CopySplitAreaToSplitAreaAPI(splitarea, splitareaAPI)
    const id = typeof splitareaAPI === 'number' ? splitareaAPI : splitareaAPI.ID
    const url = `${this.splitareasUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SplitAreaAPI>(url, splitareaAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SplitAreaAPI>('postSplitArea'))
    );
  }
  
  /** POST: add a new splitarea to the server */
  post(splitareadb: SplitAreaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {
    return this.postSplitArea(splitareadb, GONG__StackPath, frontRepo)
  }
  postSplitArea(splitareadb: SplitAreaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SplitAreaAPI>(this.splitareasUrl, splitareadb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted splitareadb id=${splitareadb.ID}`)
      }),
      catchError(this.handleError<SplitAreaAPI>('postSplitArea'))
    );
  }

  /** DELETE: delete the splitareadb from the server */
  delete(splitareadb: SplitAreaAPI | number, GONG__StackPath: string): Observable<SplitAreaAPI> {
    return this.deleteSplitArea(splitareadb, GONG__StackPath)
  }
  deleteSplitArea(splitareadb: SplitAreaAPI | number, GONG__StackPath: string): Observable<SplitAreaAPI> {
    const id = typeof splitareadb === 'number' ? splitareadb : splitareadb.ID;
    const url = `${this.splitareasUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SplitAreaAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted splitareadb id=${id}`)),
      catchError(this.handleError<SplitAreaAPI>('deleteSplitArea'))
    );
  }

  // updateFront copy splitarea to a version with encoded pointers and update to the back
  updateFront(splitarea: SplitArea, GONG__StackPath: string): Observable<SplitAreaAPI> {
    let splitareaAPI = new SplitAreaAPI
    CopySplitAreaToSplitAreaAPI(splitarea, splitareaAPI)
    const id = typeof splitareaAPI === 'number' ? splitareaAPI : splitareaAPI.ID
    const url = `${this.splitareasUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SplitAreaAPI>(url, splitareaAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SplitAreaAPI>('updateSplitArea'))
    );
  }

  /** PUT: update the splitareadb on the server */
  update(splitareadb: SplitAreaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {
    return this.updateSplitArea(splitareadb, GONG__StackPath, frontRepo)
  }
  updateSplitArea(splitareadb: SplitAreaAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SplitAreaAPI> {
    const id = typeof splitareadb === 'number' ? splitareadb : splitareadb.ID;
    const url = `${this.splitareasUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SplitAreaAPI>(url, splitareadb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated splitareadb id=${splitareadb.ID}`)
      }),
      catchError(this.handleError<SplitAreaAPI>('updateSplitArea'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SplitAreaService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SplitAreaService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
