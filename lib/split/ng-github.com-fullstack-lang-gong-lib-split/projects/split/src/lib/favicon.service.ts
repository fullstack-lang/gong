// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { FavIconAPI } from './favicon-api'
import { FavIcon, CopyFavIconToFavIconAPI } from './favicon'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class FavIconService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FavIconServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private faviconsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.faviconsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/split/go/v1/favicons';
  }

  /** GET favicons from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<FavIconAPI[]> {
    return this.getFavIcons(Name, frontRepo)
  }
  getFavIcons(Name: string, frontRepo: FrontRepo): Observable<FavIconAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<FavIconAPI[]>(this.faviconsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<FavIconAPI[]>('getFavIcons', []))
      );
  }

  /** GET favicon by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {
    return this.getFavIcon(id, Name, frontRepo)
  }
  getFavIcon(id: number, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.faviconsUrl}/${id}`;
    return this.http.get<FavIconAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched favicon id=${id}`)),
      catchError(this.handleError<FavIconAPI>(`getFavIcon id=${id}`))
    );
  }

  // postFront copy favicon to a version with encoded pointers and post to the back
  postFront(favicon: FavIcon, Name: string): Observable<FavIconAPI> {
    let faviconAPI = new FavIconAPI
    CopyFavIconToFavIconAPI(favicon, faviconAPI)
    const id = typeof faviconAPI === 'number' ? faviconAPI : faviconAPI.ID
    const url = `${this.faviconsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FavIconAPI>(url, faviconAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FavIconAPI>('postFavIcon'))
    );
  }
  
  /** POST: add a new favicon to the server */
  post(favicondb: FavIconAPI, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {
    return this.postFavIcon(favicondb, Name, frontRepo)
  }
  postFavIcon(favicondb: FavIconAPI, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FavIconAPI>(this.faviconsUrl, favicondb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted favicondb id=${favicondb.ID}`)
      }),
      catchError(this.handleError<FavIconAPI>('postFavIcon'))
    );
  }

  /** DELETE: delete the favicondb from the server */
  delete(favicondb: FavIconAPI | number, Name: string): Observable<FavIconAPI> {
    return this.deleteFavIcon(favicondb, Name)
  }
  deleteFavIcon(favicondb: FavIconAPI | number, Name: string): Observable<FavIconAPI> {
    const id = typeof favicondb === 'number' ? favicondb : favicondb.ID;
    const url = `${this.faviconsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FavIconAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted favicondb id=${id}`)),
      catchError(this.handleError<FavIconAPI>('deleteFavIcon'))
    );
  }

  // updateFront copy favicon to a version with encoded pointers and update to the back
  updateFront(favicon: FavIcon, Name: string): Observable<FavIconAPI> {
    let faviconAPI = new FavIconAPI
    CopyFavIconToFavIconAPI(favicon, faviconAPI)
    const id = typeof faviconAPI === 'number' ? faviconAPI : faviconAPI.ID
    const url = `${this.faviconsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<FavIconAPI>(url, faviconAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FavIconAPI>('updateFavIcon'))
    );
  }

  /** PUT: update the favicondb on the server */
  update(favicondb: FavIconAPI, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {
    return this.updateFavIcon(favicondb, Name, frontRepo)
  }
  updateFavIcon(favicondb: FavIconAPI, Name: string, frontRepo: FrontRepo): Observable<FavIconAPI> {
    const id = typeof favicondb === 'number' ? favicondb : favicondb.ID;
    const url = `${this.faviconsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FavIconAPI>(url, favicondb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated favicondb id=${favicondb.ID}`)
      }),
      catchError(this.handleError<FavIconAPI>('updateFavIcon'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FavIconService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FavIconService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
