// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { XlsxAPI } from './xlsx-api'
import { Xlsx, CopyXlsxToXlsxAPI } from './xlsx'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class XlsxService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  XlsxServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private xlsxsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.xlsxsUrl = origin + '/api/github.com/fullstack-lang/gong/lib/split/go/v1/xlsxs';
  }

  /** GET xlsxs from the server */
  // gets is more robust to refactoring
  gets(Name: string, frontRepo: FrontRepo): Observable<XlsxAPI[]> {
    return this.getXlsxs(Name, frontRepo)
  }
  getXlsxs(Name: string, frontRepo: FrontRepo): Observable<XlsxAPI[]> {

    let params = new HttpParams().set("Name", Name)

    return this.http.get<XlsxAPI[]>(this.xlsxsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<XlsxAPI[]>('getXlsxs', []))
      );
  }

  /** GET xlsx by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {
    return this.getXlsx(id, Name, frontRepo)
  }
  getXlsx(id: number, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {

    let params = new HttpParams().set("Name", Name)

    const url = `${this.xlsxsUrl}/${id}`;
    return this.http.get<XlsxAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched xlsx id=${id}`)),
      catchError(this.handleError<XlsxAPI>(`getXlsx id=${id}`))
    );
  }

  // postFront copy xlsx to a version with encoded pointers and post to the back
  postFront(xlsx: Xlsx, Name: string): Observable<XlsxAPI> {
    let xlsxAPI = new XlsxAPI
    CopyXlsxToXlsxAPI(xlsx, xlsxAPI)
    const id = typeof xlsxAPI === 'number' ? xlsxAPI : xlsxAPI.ID
    const url = `${this.xlsxsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<XlsxAPI>(url, xlsxAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<XlsxAPI>('postXlsx'))
    );
  }
  
  /** POST: add a new xlsx to the server */
  post(xlsxdb: XlsxAPI, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {
    return this.postXlsx(xlsxdb, Name, frontRepo)
  }
  postXlsx(xlsxdb: XlsxAPI, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<XlsxAPI>(this.xlsxsUrl, xlsxdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted xlsxdb id=${xlsxdb.ID}`)
      }),
      catchError(this.handleError<XlsxAPI>('postXlsx'))
    );
  }

  /** DELETE: delete the xlsxdb from the server */
  delete(xlsxdb: XlsxAPI | number, Name: string): Observable<XlsxAPI> {
    return this.deleteXlsx(xlsxdb, Name)
  }
  deleteXlsx(xlsxdb: XlsxAPI | number, Name: string): Observable<XlsxAPI> {
    const id = typeof xlsxdb === 'number' ? xlsxdb : xlsxdb.ID;
    const url = `${this.xlsxsUrl}/${id}`;

    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<XlsxAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted xlsxdb id=${id}`)),
      catchError(this.handleError<XlsxAPI>('deleteXlsx'))
    );
  }

  // updateFront copy xlsx to a version with encoded pointers and update to the back
  updateFront(xlsx: Xlsx, Name: string): Observable<XlsxAPI> {
    let xlsxAPI = new XlsxAPI
    CopyXlsxToXlsxAPI(xlsx, xlsxAPI)
    const id = typeof xlsxAPI === 'number' ? xlsxAPI : xlsxAPI.ID
    const url = `${this.xlsxsUrl}/${id}`;
    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<XlsxAPI>(url, xlsxAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<XlsxAPI>('updateXlsx'))
    );
  }

  /** PUT: update the xlsxdb on the server */
  update(xlsxdb: XlsxAPI, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {
    return this.updateXlsx(xlsxdb, Name, frontRepo)
  }
  updateXlsx(xlsxdb: XlsxAPI, Name: string, frontRepo: FrontRepo): Observable<XlsxAPI> {
    const id = typeof xlsxdb === 'number' ? xlsxdb : xlsxdb.ID;
    const url = `${this.xlsxsUrl}/${id}`;


    let params = new HttpParams().set("Name", Name)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<XlsxAPI>(url, xlsxdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated xlsxdb id=${xlsxdb.ID}`)
      }),
      catchError(this.handleError<XlsxAPI>('updateXlsx'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in XlsxService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("XlsxService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
