// generated by ModelGongEnumFileTemplate
package models

// insertion point of enum utility functions
// Utility function for EditorType
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (editortype EditorType) ToString() (res string) {

	// migration of former implementation of enum
	switch editortype {
	// insertion code per enum code
	case EDITOR_ADDER:
		res = "EDITOR_ADDER"
	case EDITOR_UPDATER:
		res = "EDITOR_UPDATER"
	}
	return
}

func (editortype *EditorType) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	case "EDITOR_ADDER":
		*editortype = EDITOR_ADDER
	case "EDITOR_UPDATER":
		*editortype = EDITOR_UPDATER
	default:
		return errUnkownEnum
	}
	return
}

func (editortype *EditorType) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	case "EDITOR_ADDER":
		*editortype = EDITOR_ADDER
	case "EDITOR_UPDATER":
		*editortype = EDITOR_UPDATER
	default:
		return errUnkownEnum
	}
	return
}

func (editortype *EditorType) ToCodeString() (res string) {

	switch *editortype {
	// insertion code per enum code
	case EDITOR_ADDER:
		res = "EDITOR_ADDER"
	case EDITOR_UPDATER:
		res = "EDITOR_UPDATER"
	}
	return
}

// Last line of the template
