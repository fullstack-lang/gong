// generated by gong
import { Component, OnInit, AfterViewInit, ViewChild, Inject, Optional, Input } from '@angular/core';
import { BehaviorSubject } from 'rxjs'
import { MatSort } from '@angular/material/sort';
import { MatPaginator } from '@angular/material/paginator';
import { MatTableDataSource } from '@angular/material/table';
import { MatButton } from '@angular/material/button'

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData, FrontRepoService, FrontRepo, SelectionMode } from '../front-repo.service'
import { NullInt64 } from '../null-int64'
import { SelectionModel } from '@angular/cdk/collections';

const allowMultiSelect = true;

import { ActivatedRoute, Router, RouterState } from '@angular/router';
import { FormFieldFloat64DB } from '../formfieldfloat64-db'
import { FormFieldFloat64Service } from '../formfieldfloat64.service'

// insertion point for additional imports

import { RouteService } from '../route-service';

// TableComponent is initilizaed from different routes
// TableComponentMode detail different cases 
enum TableComponentMode {
  DISPLAY_MODE,
  ONE_MANY_ASSOCIATION_MODE,
  MANY_MANY_ASSOCIATION_MODE,
}

// generated table component
@Component({
  selector: 'app-formfieldfloat64stable',
  templateUrl: './formfieldfloat64s-table.component.html',
  styleUrls: ['./formfieldfloat64s-table.component.css'],
})
export class FormFieldFloat64sTableComponent implements OnInit {

  @Input() GONG__StackPath: string = ""

  // mode at invocation
  mode: TableComponentMode = TableComponentMode.DISPLAY_MODE

  // used if the component is called as a selection component of FormFieldFloat64 instances
  selection: SelectionModel<FormFieldFloat64DB> = new (SelectionModel)
  initialSelection = new Array<FormFieldFloat64DB>()

  // the data source for the table
  formfieldfloat64s: FormFieldFloat64DB[] = []
  matTableDataSource: MatTableDataSource<FormFieldFloat64DB> = new (MatTableDataSource)

  // front repo, that will be referenced by this.formfieldfloat64s
  frontRepo: FrontRepo = new (FrontRepo)

  // displayedColumns is referenced by the MatTable component for specify what columns
  // have to be displayed and in what order
  displayedColumns: string[];

  // for sorting & pagination
  @ViewChild(MatSort)
  sort: MatSort | undefined
  @ViewChild(MatPaginator)
  paginator: MatPaginator | undefined;

  ngAfterViewInit() {

    // enable sorting on all fields (including pointers and reverse pointer)
    this.matTableDataSource.sortingDataAccessor = (formfieldfloat64DB: FormFieldFloat64DB, property: string) => {
      switch (property) {
        case 'ID':
          return formfieldfloat64DB.ID

        // insertion point for specific sorting accessor
        case 'Name':
          return formfieldfloat64DB.Name;

        case 'Value':
          return formfieldfloat64DB.Value;

        case 'HasMinValidator':
          return formfieldfloat64DB.HasMinValidator ? "true" : "false";

        case 'MinValue':
          return formfieldfloat64DB.MinValue;

        case 'HasMaxValidator':
          return formfieldfloat64DB.HasMaxValidator ? "true" : "false";

        case 'MaxValue':
          return formfieldfloat64DB.MaxValue;

        default:
          console.assert(false, "Unknown field")
          return "";
      }
    };

    // enable filtering on all fields (including pointers and reverse pointer, which is not done by default)
    this.matTableDataSource.filterPredicate = (formfieldfloat64DB: FormFieldFloat64DB, filter: string) => {

      // filtering is based on finding a lower case filter into a concatenated string
      // the formfieldfloat64DB properties
      let mergedContent = ""

      // insertion point for merging of fields
      mergedContent += formfieldfloat64DB.Name.toLowerCase()
      mergedContent += formfieldfloat64DB.Value.toString()
      mergedContent += formfieldfloat64DB.MinValue.toString()
      mergedContent += formfieldfloat64DB.MaxValue.toString()

      let isSelected = mergedContent.includes(filter.toLowerCase())
      return isSelected
    };

    this.matTableDataSource.sort = this.sort!
    this.matTableDataSource.paginator = this.paginator!
  }

  applyFilter(event: Event) {
    const filterValue = (event.target as HTMLInputElement).value;
    this.matTableDataSource.filter = filterValue.trim().toLowerCase();
  }

  constructor(
    private formfieldfloat64Service: FormFieldFloat64Service,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of formfieldfloat64 instances
    public dialogRef: MatDialogRef<FormFieldFloat64sTableComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
    private activatedRoute: ActivatedRoute,

    private routeService: RouteService,
  ) {

    // compute mode
    if (dialogData == undefined) {
      this.mode = TableComponentMode.DISPLAY_MODE
    } else {
      this.GONG__StackPath = dialogData.GONG__StackPath
      switch (dialogData.SelectionMode) {
        case SelectionMode.ONE_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.ONE_MANY_ASSOCIATION_MODE
          break
        case SelectionMode.MANY_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.MANY_MANY_ASSOCIATION_MODE
          break
        default:
      }
    }

    // observable for changes in structs
    this.formfieldfloat64Service.FormFieldFloat64ServiceChanged.subscribe(
      message => {
        if (message == "post" || message == "update" || message == "delete") {
          this.getFormFieldFloat64s()
        }
      }
    )
    if (this.mode == TableComponentMode.DISPLAY_MODE) {
      this.displayedColumns = ['ID', 'Delete', // insertion point for columns to display
        "Name",
        "Value",
        "HasMinValidator",
        "MinValue",
        "HasMaxValidator",
        "MaxValue",
      ]
    } else {
      this.displayedColumns = ['select', 'ID', // insertion point for columns to display
        "Name",
        "Value",
        "HasMinValidator",
        "MinValue",
        "HasMaxValidator",
        "MaxValue",
      ]
      this.selection = new SelectionModel<FormFieldFloat64DB>(allowMultiSelect, this.initialSelection);
    }

  }

  ngOnInit(): void {
    let stackPath = this.activatedRoute.snapshot.paramMap.get('GONG__StackPath')
    if (stackPath != undefined) {
      this.GONG__StackPath = stackPath
    }

    this.getFormFieldFloat64s()

    this.matTableDataSource = new MatTableDataSource(this.formfieldfloat64s)
  }

  getFormFieldFloat64s(): void {
    this.frontRepoService.pull(this.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        this.formfieldfloat64s = this.frontRepo.FormFieldFloat64s_array;

        // insertion point for time duration Recoveries
        // insertion point for enum int Recoveries

        // in case the component is called as a selection component
        if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {
          for (let formfieldfloat64 of this.formfieldfloat64s) {
            let ID = this.dialogData.ID
            let revPointer = formfieldfloat64[this.dialogData.ReversePointer as keyof FormFieldFloat64DB] as unknown as NullInt64
            if (revPointer.Int64 == ID) {
              this.initialSelection.push(formfieldfloat64)
            }
            this.selection = new SelectionModel<FormFieldFloat64DB>(allowMultiSelect, this.initialSelection);
          }
        }

        if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

          let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, FormFieldFloat64DB>
          let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

          // we associates on sourceInstance of type SourceStruct with a MANY MANY associations to FormFieldFloat64DB
          // the field name is sourceField
          let sourceFieldArray = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]! as unknown as FormFieldFloat64DB[]
          if (sourceFieldArray != null) {
            for (let associationInstance of sourceFieldArray) {
              let formfieldfloat64 = associationInstance[this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as FormFieldFloat64DB
              this.initialSelection.push(formfieldfloat64)
            }
          }

          this.selection = new SelectionModel<FormFieldFloat64DB>(allowMultiSelect, this.initialSelection);
        }

        // update the mat table data source
        this.matTableDataSource.data = this.formfieldfloat64s
      }
    )
  }

  // newFormFieldFloat64 initiate a new formfieldfloat64
  // create a new FormFieldFloat64 objet
  newFormFieldFloat64() {
  }

  deleteFormFieldFloat64(formfieldfloat64ID: number, formfieldfloat64: FormFieldFloat64DB) {
    // list of formfieldfloat64s is truncated of formfieldfloat64 before the delete
    this.formfieldfloat64s = this.formfieldfloat64s.filter(h => h !== formfieldfloat64);

    this.formfieldfloat64Service.deleteFormFieldFloat64(formfieldfloat64ID, this.GONG__StackPath).subscribe(
      formfieldfloat64 => {
        this.formfieldfloat64Service.FormFieldFloat64ServiceChanged.next("delete")
      }
    );
  }

  editFormFieldFloat64(formfieldfloat64ID: number, formfieldfloat64: FormFieldFloat64DB) {

  }

  // set editor outlet
  setEditorRouterOutlet(formfieldfloat64ID: number) {
    let outletName = this.routeService.getEditorOutlet(this.GONG__StackPath)
    let fullPath = this.routeService.getPathRoot() + "-" + "formfieldfloat64" + "-detail"

    let outletConf: any = {}
    outletConf[outletName] = [fullPath, formfieldfloat64ID, this.GONG__StackPath]

    this.router.navigate([{ outlets: outletConf }])
  }

  /** Whether the number of selected elements matches the total number of rows. */
  isAllSelected() {
    const numSelected = this.selection.selected.length;
    const numRows = this.formfieldfloat64s.length;
    return numSelected === numRows;
  }

  /** Selects all rows if they are not all selected; otherwise clear selection. */
  masterToggle() {
    this.isAllSelected() ?
      this.selection.clear() :
      this.formfieldfloat64s.forEach(row => this.selection.select(row));
  }

  save() {

    if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {

      let toUpdate = new Set<FormFieldFloat64DB>()

      // reset all initial selection of formfieldfloat64 that belong to formfieldfloat64
      for (let formfieldfloat64 of this.initialSelection) {
        let index = formfieldfloat64[this.dialogData.ReversePointer as keyof FormFieldFloat64DB] as unknown as NullInt64
        index.Int64 = 0
        index.Valid = true
        toUpdate.add(formfieldfloat64)

      }

      // from selection, set formfieldfloat64 that belong to formfieldfloat64
      for (let formfieldfloat64 of this.selection.selected) {
        let ID = this.dialogData.ID as number
        let reversePointer = formfieldfloat64[this.dialogData.ReversePointer as keyof FormFieldFloat64DB] as unknown as NullInt64
        reversePointer.Int64 = ID
        reversePointer.Valid = true
        toUpdate.add(formfieldfloat64)
      }


      // update all formfieldfloat64 (only update selection & initial selection)
      for (let formfieldfloat64 of toUpdate) {
        this.formfieldfloat64Service.updateFormFieldFloat64(formfieldfloat64, this.GONG__StackPath)
          .subscribe(formfieldfloat64 => {
            this.formfieldfloat64Service.FormFieldFloat64ServiceChanged.next("update")
          });
      }
    }

    if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

      // get the source instance via the map of instances in the front repo
      let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, FormFieldFloat64DB>
      let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

      // First, parse all instance of the association struct and remove the instance
      // that have unselect
      let unselectedFormFieldFloat64 = new Set<number>()
      for (let formfieldfloat64 of this.initialSelection) {
        if (this.selection.selected.includes(formfieldfloat64)) {
          // console.log("formfieldfloat64 " + formfieldfloat64.Name + " is still selected")
        } else {
          console.log("formfieldfloat64 " + formfieldfloat64.Name + " has been unselected")
          unselectedFormFieldFloat64.add(formfieldfloat64.ID)
          console.log("is unselected " + unselectedFormFieldFloat64.has(formfieldfloat64.ID))
        }
      }

      // delete the association instance
      let associationInstance = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]
      let formfieldfloat64 = associationInstance![this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as FormFieldFloat64DB
      if (unselectedFormFieldFloat64.has(formfieldfloat64.ID)) {
        this.frontRepoService.deleteService(this.dialogData.IntermediateStruct, associationInstance)


      }

      // is the source array is empty create it
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] == undefined) {
        (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] as unknown as Array<FormFieldFloat64DB>) = new Array<FormFieldFloat64DB>()
      }

      // second, parse all instance of the selected
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]) {
        this.selection.selected.forEach(
          formfieldfloat64 => {
            if (!this.initialSelection.includes(formfieldfloat64)) {
              // console.log("formfieldfloat64 " + formfieldfloat64.Name + " has been added to the selection")

              let associationInstance = {
                Name: sourceInstance["Name"] + "-" + formfieldfloat64.Name,
              }

              let index = associationInstance[this.dialogData.IntermediateStructField + "ID" as keyof typeof associationInstance] as unknown as NullInt64
              index.Int64 = formfieldfloat64.ID
              index.Valid = true

              let indexDB = associationInstance[this.dialogData.IntermediateStructField + "DBID" as keyof typeof associationInstance] as unknown as NullInt64
              indexDB.Int64 = formfieldfloat64.ID
              index.Valid = true

              this.frontRepoService.postService(this.dialogData.IntermediateStruct, associationInstance)

            } else {
              // console.log("formfieldfloat64 " + formfieldfloat64.Name + " is still selected")
            }
          }
        )
      }

      // this.selection = new SelectionModel<FormFieldFloat64DB>(allowMultiSelect, this.initialSelection);
    }

    // why pizza ?
    this.dialogRef.close('Pizza!');
  }
}
