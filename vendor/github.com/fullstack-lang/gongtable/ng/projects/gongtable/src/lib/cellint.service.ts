// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { CellIntDB } from './cellint-db';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class CellIntService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CellIntServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private cellintsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.cellintsUrl = origin + '/api/github.com/fullstack-lang/gongtable/go/v1/cellints';
  }

  /** GET cellints from the server */
  getCellInts(GONG__StackPath: string): Observable<CellIntDB[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<CellIntDB[]>(this.cellintsUrl, { params: params })
      .pipe(
        tap(),
		// tap(_ => this.log('fetched cellints')),
        catchError(this.handleError<CellIntDB[]>('getCellInts', []))
      );
  }

  /** GET cellint by id. Will 404 if id not found */
  getCellInt(id: number, GONG__StackPath: string): Observable<CellIntDB> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.cellintsUrl}/${id}`;
    return this.http.get<CellIntDB>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched cellint id=${id}`)),
      catchError(this.handleError<CellIntDB>(`getCellInt id=${id}`))
    );
  }

  /** POST: add a new cellint to the server */
  postCellInt(cellintdb: CellIntDB, GONG__StackPath: string): Observable<CellIntDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<CellIntDB>(this.cellintsUrl, cellintdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`posted cellintdb id=${cellintdb.ID}`)
      }),
      catchError(this.handleError<CellIntDB>('postCellInt'))
    );
  }

  /** DELETE: delete the cellintdb from the server */
  deleteCellInt(cellintdb: CellIntDB | number, GONG__StackPath: string): Observable<CellIntDB> {
    const id = typeof cellintdb === 'number' ? cellintdb : cellintdb.ID;
    const url = `${this.cellintsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<CellIntDB>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted cellintdb id=${id}`)),
      catchError(this.handleError<CellIntDB>('deleteCellInt'))
    );
  }

  /** PUT: update the cellintdb on the server */
  updateCellInt(cellintdb: CellIntDB, GONG__StackPath: string): Observable<CellIntDB> {
    const id = typeof cellintdb === 'number' ? cellintdb : cellintdb.ID;
    const url = `${this.cellintsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<CellIntDB>(url, cellintdb, httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        // this.log(`updated cellintdb id=${cellintdb.ID}`)
      }),
      catchError(this.handleError<CellIntDB>('updateCellInt'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in CellIntService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("CellIntService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
      console.log(message)
  }
}
