// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GongdocStatusDB } from './gongdocstatus-db';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class GongdocStatusService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GongdocStatusServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private gongdocstatussUrl: string

  constructor(
    private http: HttpClient,
    private location: Location,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.gongdocstatussUrl = origin + '/api/github.com/fullstack-lang/gongdoc/go/v1/gongdocstatuss';
  }

  /** GET gongdocstatuss from the server */
  getGongdocStatuss(): Observable<GongdocStatusDB[]> {
    return this.http.get<GongdocStatusDB[]>(this.gongdocstatussUrl)
      .pipe(
        tap(_ => this.log('fetched gongdocstatuss')),
        catchError(this.handleError<GongdocStatusDB[]>('getGongdocStatuss', []))
      );
  }

  /** GET gongdocstatus by id. Will 404 if id not found */
  getGongdocStatus(id: number): Observable<GongdocStatusDB> {
    const url = `${this.gongdocstatussUrl}/${id}`;
    return this.http.get<GongdocStatusDB>(url).pipe(
      tap(_ => this.log(`fetched gongdocstatus id=${id}`)),
      catchError(this.handleError<GongdocStatusDB>(`getGongdocStatus id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new gongdocstatus to the server */
  postGongdocStatus(gongdocstatusdb: GongdocStatusDB): Observable<GongdocStatusDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.post<GongdocStatusDB>(this.gongdocstatussUrl, gongdocstatusdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`posted gongdocstatusdb id=${gongdocstatusdb.ID}`)
      }),
      catchError(this.handleError<GongdocStatusDB>('postGongdocStatus'))
    );
  }

  /** DELETE: delete the gongdocstatusdb from the server */
  deleteGongdocStatus(gongdocstatusdb: GongdocStatusDB | number): Observable<GongdocStatusDB> {
    const id = typeof gongdocstatusdb === 'number' ? gongdocstatusdb : gongdocstatusdb.ID;
    const url = `${this.gongdocstatussUrl}/${id}`;

    return this.http.delete<GongdocStatusDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted gongdocstatusdb id=${id}`)),
      catchError(this.handleError<GongdocStatusDB>('deleteGongdocStatus'))
    );
  }

  /** PUT: update the gongdocstatusdb on the server */
  updateGongdocStatus(gongdocstatusdb: GongdocStatusDB): Observable<GongdocStatusDB> {
    const id = typeof gongdocstatusdb === 'number' ? gongdocstatusdb : gongdocstatusdb.ID;
    const url = `${this.gongdocstatussUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put<GongdocStatusDB>(url, gongdocstatusdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated gongdocstatusdb id=${gongdocstatusdb.ID}`)
      }),
      catchError(this.handleError<GongdocStatusDB>('updateGongdocStatus'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
