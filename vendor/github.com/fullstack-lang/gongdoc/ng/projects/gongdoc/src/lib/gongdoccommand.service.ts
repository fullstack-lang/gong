// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GongdocCommandDB } from './gongdoccommand-db';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class GongdocCommandService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GongdocCommandServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private gongdoccommandsUrl: string

  constructor(
    private http: HttpClient,
    private location: Location,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.gongdoccommandsUrl = origin + '/api/github.com/fullstack-lang/gongdoc/go/v1/gongdoccommands';
  }

  /** GET gongdoccommands from the server */
  getGongdocCommands(): Observable<GongdocCommandDB[]> {
    return this.http.get<GongdocCommandDB[]>(this.gongdoccommandsUrl)
      .pipe(
        tap(_ => this.log('fetched gongdoccommands')),
        catchError(this.handleError<GongdocCommandDB[]>('getGongdocCommands', []))
      );
  }

  /** GET gongdoccommand by id. Will 404 if id not found */
  getGongdocCommand(id: number): Observable<GongdocCommandDB> {
    const url = `${this.gongdoccommandsUrl}/${id}`;
    return this.http.get<GongdocCommandDB>(url).pipe(
      tap(_ => this.log(`fetched gongdoccommand id=${id}`)),
      catchError(this.handleError<GongdocCommandDB>(`getGongdocCommand id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new gongdoccommand to the server */
  postGongdocCommand(gongdoccommanddb: GongdocCommandDB): Observable<GongdocCommandDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.post<GongdocCommandDB>(this.gongdoccommandsUrl, gongdoccommanddb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`posted gongdoccommanddb id=${gongdoccommanddb.ID}`)
      }),
      catchError(this.handleError<GongdocCommandDB>('postGongdocCommand'))
    );
  }

  /** DELETE: delete the gongdoccommanddb from the server */
  deleteGongdocCommand(gongdoccommanddb: GongdocCommandDB | number): Observable<GongdocCommandDB> {
    const id = typeof gongdoccommanddb === 'number' ? gongdoccommanddb : gongdoccommanddb.ID;
    const url = `${this.gongdoccommandsUrl}/${id}`;

    return this.http.delete<GongdocCommandDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted gongdoccommanddb id=${id}`)),
      catchError(this.handleError<GongdocCommandDB>('deleteGongdocCommand'))
    );
  }

  /** PUT: update the gongdoccommanddb on the server */
  updateGongdocCommand(gongdoccommanddb: GongdocCommandDB): Observable<GongdocCommandDB> {
    const id = typeof gongdoccommanddb === 'number' ? gongdoccommanddb : gongdoccommanddb.ID;
    const url = `${this.gongdoccommandsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put<GongdocCommandDB>(url, gongdoccommanddb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated gongdoccommanddb id=${gongdoccommanddb.ID}`)
      }),
      catchError(this.handleError<GongdocCommandDB>('updateGongdocCommand'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
