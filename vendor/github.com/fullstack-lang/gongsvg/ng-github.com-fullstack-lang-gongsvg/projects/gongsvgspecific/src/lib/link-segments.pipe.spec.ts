import { LinkSegmentsPipe } from './link-segments.pipe';

describe('LinkSegmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkSegmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
