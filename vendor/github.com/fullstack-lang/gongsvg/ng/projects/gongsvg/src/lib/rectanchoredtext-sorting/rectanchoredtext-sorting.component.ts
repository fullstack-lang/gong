// generated by gong
import { Component, OnInit, Inject, Optional } from '@angular/core';
import { TypeofExpr } from '@angular/compiler';
import { CdkDragDrop, moveItemInArray } from '@angular/cdk/drag-drop';

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData } from '../front-repo.service'
import { SelectionModel } from '@angular/cdk/collections';

import { Router, RouterState } from '@angular/router';
import { RectAnchoredTextDB } from '../rectanchoredtext-db'
import { RectAnchoredTextService } from '../rectanchoredtext.service'

import { FrontRepoService, FrontRepo } from '../front-repo.service'
import { NullInt64 } from '../null-int64'

@Component({
  selector: 'lib-rectanchoredtext-sorting',
  templateUrl: './rectanchoredtext-sorting.component.html',
  styleUrls: ['./rectanchoredtext-sorting.component.css']
})
export class RectAnchoredTextSortingComponent implements OnInit {

  frontRepo: FrontRepo = new (FrontRepo)

  // array of RectAnchoredText instances that are in the association
  associatedRectAnchoredTexts = new Array<RectAnchoredTextDB>();

  constructor(
    private rectanchoredtextService: RectAnchoredTextService,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of rectanchoredtext instances
    public dialogRef: MatDialogRef<RectAnchoredTextSortingComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
  ) {
    this.router.routeReuseStrategy.shouldReuseRoute = function () {
      return false;
    };
  }

  ngOnInit(): void {
    this.getRectAnchoredTexts()
  }

  getRectAnchoredTexts(): void {
    this.frontRepoService.pull(this.dialogData.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        let index = 0
        for (let rectanchoredtext of this.frontRepo.RectAnchoredTexts_array) {
          let ID = this.dialogData.ID
          let revPointerID = rectanchoredtext[this.dialogData.ReversePointer as keyof RectAnchoredTextDB] as unknown as NullInt64
          let revPointerID_Index = rectanchoredtext[this.dialogData.ReversePointer + "_Index" as keyof RectAnchoredTextDB] as unknown as NullInt64
          if (revPointerID.Int64 == ID) {
            if (revPointerID_Index == undefined) {
              revPointerID_Index = new NullInt64
              revPointerID_Index.Valid = true
              revPointerID_Index.Int64 = index++
            }
            this.associatedRectAnchoredTexts.push(rectanchoredtext)
          }
        }

        // sort associated rectanchoredtext according to order
        this.associatedRectAnchoredTexts.sort((t1, t2) => {
          let t1_revPointerID_Index = t1[this.dialogData.ReversePointer + "_Index" as keyof typeof t1] as unknown as NullInt64
          let t2_revPointerID_Index = t2[this.dialogData.ReversePointer + "_Index" as keyof typeof t2] as unknown as NullInt64
          if (t1_revPointerID_Index && t2_revPointerID_Index) {
            if (t1_revPointerID_Index.Int64 > t2_revPointerID_Index.Int64) {
              return 1;
            }
            if (t1_revPointerID_Index.Int64 < t2_revPointerID_Index.Int64) {
              return -1;
            }
          }
          return 0;
        });
      }
    )
  }

  drop(event: CdkDragDrop<string[]>) {
    moveItemInArray(this.associatedRectAnchoredTexts, event.previousIndex, event.currentIndex);

    // set the order of RectAnchoredText instances
    let index = 0

    for (let rectanchoredtext of this.associatedRectAnchoredTexts) {
      let revPointerID_Index = rectanchoredtext[this.dialogData.ReversePointer + "_Index" as keyof RectAnchoredTextDB] as unknown as NullInt64
      revPointerID_Index.Valid = true
      revPointerID_Index.Int64 = index++
    }
  }

  save() {

    this.associatedRectAnchoredTexts.forEach(
      rectanchoredtext => {
        this.rectanchoredtextService.updateRectAnchoredText(rectanchoredtext, this.dialogData.GONG__StackPath)
          .subscribe(rectanchoredtext => {
            this.rectanchoredtextService.RectAnchoredTextServiceChanged.next("update")
          });
      }
    )

    this.dialogRef.close('Sorting of ' + this.dialogData.ReversePointer + ' done');
  }
}
