// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { EditorAPI } from './editor-api';
import { EditorDB } from './editor-db';

@Injectable({
  providedIn: 'root'
})
export class EditorService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  EditorServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private editorsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/examples/bookstore/go/v1/editors';

  constructor(
    private http: HttpClient
  ) { }

  /** GET editors from the server */
  getEditors(): Observable<EditorDB[]> {
    return this.http.get<EditorDB[]>(this.editorsUrl)
      .pipe(
        tap(_ => this.log('fetched editors')),
        catchError(this.handleError<EditorDB[]>('getEditors', []))
      );
  }

  /** GET editor by id. Will 404 if id not found */
  getEditor(id: number): Observable<EditorDB> {
    const url = `${this.editorsUrl}/${id}`;
    return this.http.get<EditorDB>(url).pipe(
      tap(_ => this.log(`fetched editor id=${id}`)),
      catchError(this.handleError<EditorDB>(`getEditor id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new editor to the server */
  postEditor(editorAPI: EditorAPI): Observable<EditorDB> {
    return this.http.post<EditorDB>(this.editorsUrl, editorAPI, this.httpOptions).pipe(
      tap((newEditor: EditorDB) => { })
    );
  }

  /** DELETE: delete the editordb from the server */
  deleteEditor(editordb: EditorDB | number): Observable<EditorDB> {
    const id = typeof editordb === 'number' ? editordb : editordb.ID;
    const url = `${this.editorsUrl}/${id}`;

    return this.http.delete<EditorDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted editordb id=${id}`)),
      catchError(this.handleError<EditorDB>('deleteEditor'))
    );
  }

  /** PUT: update the editordb on the server */
  updateEditor(editordb: EditorDB): Observable<EditorDB> {
    const id = typeof editordb === 'number' ? editordb : editordb.ID;
    const url = `${this.editorsUrl}/${id}`;

    // insertion point for reset of reverse pointers (to avoid circular JSON)
    editordb.Books = []

    return this.http.put(url, editordb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated editordb id=${editordb.ID}`)
      }),
      catchError(this.handleError<EditorDB>('updateEditor'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
