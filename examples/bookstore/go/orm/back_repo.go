// generated by genORMTranslation.go
package orm

import (
	"github.com/fullstack-lang/examples/bookstore/go/models"
	"github.com/jinzhu/gorm"
)

// BackRepoStruct supports callback functions
type BackRepoStruct struct {
	// insertion point for per struct back repo declarations
	BackRepoArea BackRepoAreaStruct

	BackRepoBook BackRepoBookStruct

	BackRepoEditor BackRepoEditorStruct

	CommitNb uint // this ng is updated at the BackRepo level but also at the BackRepo<GongStruct> level
}

func (backRepo *BackRepoStruct) GetLastCommitNb() uint {
	return backRepo.CommitNb
}

func (backRepo *BackRepoStruct) IncrementCommitNb() uint {
	backRepo.CommitNb = backRepo.CommitNb + 1
	return backRepo.CommitNb
}

// Init the BackRepoStruct inner variables and link to the database
func (backRepo *BackRepoStruct) Init(db *gorm.DB) {
	// insertion point for per struct back repo declarations
	backRepo.BackRepoArea.Init(db)
	backRepo.BackRepoBook.Init(db)
	backRepo.BackRepoEditor.Init(db)

	models.Stage.BackRepo = backRepo
}

// Commit the BackRepoStruct inner variables and link to the database
func (backRepo *BackRepoStruct) Commit(stage *models.StageStruct) {
	// insertion point for per struct back repo phase one commit
	backRepo.BackRepoArea.CommitPhaseOne(stage)
	backRepo.BackRepoBook.CommitPhaseOne(stage)
	backRepo.BackRepoEditor.CommitPhaseOne(stage)

	// insertion point for per struct back repo phase two commit
	backRepo.BackRepoArea.CommitPhaseTwo(backRepo)
	backRepo.BackRepoBook.CommitPhaseTwo(backRepo)
	backRepo.BackRepoEditor.CommitPhaseTwo(backRepo)

	backRepo.IncrementCommitNb()
}

// Checkout the database into the stage
func (backRepo *BackRepoStruct) Checkout(stage *models.StageStruct) {
	// insertion point for per struct back repo phase one commit
	backRepo.BackRepoArea.CheckoutPhaseOne()
	backRepo.BackRepoBook.CheckoutPhaseOne()
	backRepo.BackRepoEditor.CheckoutPhaseOne()

	// insertion point for per struct back repo phase two commit
	backRepo.BackRepoArea.CheckoutPhaseTwo(backRepo)
	backRepo.BackRepoBook.CheckoutPhaseTwo(backRepo)
	backRepo.BackRepoEditor.CheckoutPhaseTwo(backRepo)
}

var BackRepo BackRepoStruct

func GetLastCommitNb() uint {
	return BackRepo.GetLastCommitNb()
}
