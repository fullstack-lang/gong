// generated code - do not edit
package models

import (
	"fmt"
	"log"
	"sort"
	"strings"
	"time"
)

var (
	__GongSliceTemplate_time__dummyDeclaration time.Duration
	_                                          = __GongSliceTemplate_time__dummyDeclaration
)

// ComputeReverseMaps computes the reverse map, for all intances, for all slice to pointers field
// Its complexity is in O(n)O(p) where p is the number of pointers
func (stage *Stage) ComputeReverseMaps() {
	// insertion point per named struct
	// Compute reverse map for named struct A_directive
	// insertion point per field

	// Compute reverse map for named struct A_measure
	// insertion point per field
	stage.A_measure_Note_reverseMap = make(map[*Note]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _note := range a_measure.Note {
			stage.A_measure_Note_reverseMap[_note] = a_measure
		}
	}
	stage.A_measure_Backup_reverseMap = make(map[*Backup]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _backup := range a_measure.Backup {
			stage.A_measure_Backup_reverseMap[_backup] = a_measure
		}
	}
	stage.A_measure_Forward_reverseMap = make(map[*Forward]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _forward := range a_measure.Forward {
			stage.A_measure_Forward_reverseMap[_forward] = a_measure
		}
	}
	stage.A_measure_Direction_reverseMap = make(map[*Direction]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _direction := range a_measure.Direction {
			stage.A_measure_Direction_reverseMap[_direction] = a_measure
		}
	}
	stage.A_measure_Attributes_reverseMap = make(map[*Attributes]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _attributes := range a_measure.Attributes {
			stage.A_measure_Attributes_reverseMap[_attributes] = a_measure
		}
	}
	stage.A_measure_Harmony_reverseMap = make(map[*Harmony]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _harmony := range a_measure.Harmony {
			stage.A_measure_Harmony_reverseMap[_harmony] = a_measure
		}
	}
	stage.A_measure_Figured_bass_reverseMap = make(map[*Figured_bass]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _figured_bass := range a_measure.Figured_bass {
			stage.A_measure_Figured_bass_reverseMap[_figured_bass] = a_measure
		}
	}
	stage.A_measure_Print_reverseMap = make(map[*Print]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _print := range a_measure.Print {
			stage.A_measure_Print_reverseMap[_print] = a_measure
		}
	}
	stage.A_measure_Sound_reverseMap = make(map[*Sound]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _sound := range a_measure.Sound {
			stage.A_measure_Sound_reverseMap[_sound] = a_measure
		}
	}
	stage.A_measure_Listening_reverseMap = make(map[*Listening]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _listening := range a_measure.Listening {
			stage.A_measure_Listening_reverseMap[_listening] = a_measure
		}
	}
	stage.A_measure_Barline_reverseMap = make(map[*Barline]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _barline := range a_measure.Barline {
			stage.A_measure_Barline_reverseMap[_barline] = a_measure
		}
	}
	stage.A_measure_Grouping_reverseMap = make(map[*Grouping]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _grouping := range a_measure.Grouping {
			stage.A_measure_Grouping_reverseMap[_grouping] = a_measure
		}
	}
	stage.A_measure_Link_reverseMap = make(map[*Link]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _link := range a_measure.Link {
			stage.A_measure_Link_reverseMap[_link] = a_measure
		}
	}
	stage.A_measure_Bookmark_reverseMap = make(map[*Bookmark]*A_measure)
	for a_measure := range stage.A_measures {
		_ = a_measure
		for _, _bookmark := range a_measure.Bookmark {
			stage.A_measure_Bookmark_reverseMap[_bookmark] = a_measure
		}
	}

	// Compute reverse map for named struct A_measure_1
	// insertion point per field
	stage.A_measure_1_Part_reverseMap = make(map[*A_part_1]*A_measure_1)
	for a_measure_1 := range stage.A_measure_1s {
		_ = a_measure_1
		for _, _a_part_1 := range a_measure_1.Part {
			stage.A_measure_1_Part_reverseMap[_a_part_1] = a_measure_1
		}
	}

	// Compute reverse map for named struct A_part
	// insertion point per field
	stage.A_part_Measure_reverseMap = make(map[*A_measure]*A_part)
	for a_part := range stage.A_parts {
		_ = a_part
		for _, _a_measure := range a_part.Measure {
			stage.A_part_Measure_reverseMap[_a_measure] = a_part
		}
	}

	// Compute reverse map for named struct A_part_1
	// insertion point per field
	stage.A_part_1_Note_reverseMap = make(map[*Note]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _note := range a_part_1.Note {
			stage.A_part_1_Note_reverseMap[_note] = a_part_1
		}
	}
	stage.A_part_1_Backup_reverseMap = make(map[*Backup]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _backup := range a_part_1.Backup {
			stage.A_part_1_Backup_reverseMap[_backup] = a_part_1
		}
	}
	stage.A_part_1_Forward_reverseMap = make(map[*Forward]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _forward := range a_part_1.Forward {
			stage.A_part_1_Forward_reverseMap[_forward] = a_part_1
		}
	}
	stage.A_part_1_Direction_reverseMap = make(map[*Direction]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _direction := range a_part_1.Direction {
			stage.A_part_1_Direction_reverseMap[_direction] = a_part_1
		}
	}
	stage.A_part_1_Attributes_reverseMap = make(map[*Attributes]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _attributes := range a_part_1.Attributes {
			stage.A_part_1_Attributes_reverseMap[_attributes] = a_part_1
		}
	}
	stage.A_part_1_Harmony_reverseMap = make(map[*Harmony]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _harmony := range a_part_1.Harmony {
			stage.A_part_1_Harmony_reverseMap[_harmony] = a_part_1
		}
	}
	stage.A_part_1_Figured_bass_reverseMap = make(map[*Figured_bass]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _figured_bass := range a_part_1.Figured_bass {
			stage.A_part_1_Figured_bass_reverseMap[_figured_bass] = a_part_1
		}
	}
	stage.A_part_1_Print_reverseMap = make(map[*Print]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _print := range a_part_1.Print {
			stage.A_part_1_Print_reverseMap[_print] = a_part_1
		}
	}
	stage.A_part_1_Sound_reverseMap = make(map[*Sound]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _sound := range a_part_1.Sound {
			stage.A_part_1_Sound_reverseMap[_sound] = a_part_1
		}
	}
	stage.A_part_1_Listening_reverseMap = make(map[*Listening]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _listening := range a_part_1.Listening {
			stage.A_part_1_Listening_reverseMap[_listening] = a_part_1
		}
	}
	stage.A_part_1_Barline_reverseMap = make(map[*Barline]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _barline := range a_part_1.Barline {
			stage.A_part_1_Barline_reverseMap[_barline] = a_part_1
		}
	}
	stage.A_part_1_Grouping_reverseMap = make(map[*Grouping]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _grouping := range a_part_1.Grouping {
			stage.A_part_1_Grouping_reverseMap[_grouping] = a_part_1
		}
	}
	stage.A_part_1_Link_reverseMap = make(map[*Link]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _link := range a_part_1.Link {
			stage.A_part_1_Link_reverseMap[_link] = a_part_1
		}
	}
	stage.A_part_1_Bookmark_reverseMap = make(map[*Bookmark]*A_part_1)
	for a_part_1 := range stage.A_part_1s {
		_ = a_part_1
		for _, _bookmark := range a_part_1.Bookmark {
			stage.A_part_1_Bookmark_reverseMap[_bookmark] = a_part_1
		}
	}

	// Compute reverse map for named struct Accidental
	// insertion point per field

	// Compute reverse map for named struct Accidental_mark
	// insertion point per field

	// Compute reverse map for named struct Accidental_text
	// insertion point per field

	// Compute reverse map for named struct Accord
	// insertion point per field

	// Compute reverse map for named struct Accordion_registration
	// insertion point per field

	// Compute reverse map for named struct Appearance
	// insertion point per field
	stage.Appearance_Line_width_reverseMap = make(map[*Line_width]*Appearance)
	for appearance := range stage.Appearances {
		_ = appearance
		for _, _line_width := range appearance.Line_width {
			stage.Appearance_Line_width_reverseMap[_line_width] = appearance
		}
	}
	stage.Appearance_Note_size_reverseMap = make(map[*Note_size]*Appearance)
	for appearance := range stage.Appearances {
		_ = appearance
		for _, _note_size := range appearance.Note_size {
			stage.Appearance_Note_size_reverseMap[_note_size] = appearance
		}
	}
	stage.Appearance_Distance_reverseMap = make(map[*Distance]*Appearance)
	for appearance := range stage.Appearances {
		_ = appearance
		for _, _distance := range appearance.Distance {
			stage.Appearance_Distance_reverseMap[_distance] = appearance
		}
	}
	stage.Appearance_Glyph_reverseMap = make(map[*Glyph]*Appearance)
	for appearance := range stage.Appearances {
		_ = appearance
		for _, _glyph := range appearance.Glyph {
			stage.Appearance_Glyph_reverseMap[_glyph] = appearance
		}
	}
	stage.Appearance_Other_appearance_reverseMap = make(map[*Other_appearance]*Appearance)
	for appearance := range stage.Appearances {
		_ = appearance
		for _, _other_appearance := range appearance.Other_appearance {
			stage.Appearance_Other_appearance_reverseMap[_other_appearance] = appearance
		}
	}

	// Compute reverse map for named struct Arpeggiate
	// insertion point per field

	// Compute reverse map for named struct Arrow
	// insertion point per field

	// Compute reverse map for named struct Articulations
	// insertion point per field
	stage.Articulations_Accent_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Accent {
			stage.Articulations_Accent_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Strong_accent_reverseMap = make(map[*Strong_accent]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _strong_accent := range articulations.Strong_accent {
			stage.Articulations_Strong_accent_reverseMap[_strong_accent] = articulations
		}
	}
	stage.Articulations_Staccato_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Staccato {
			stage.Articulations_Staccato_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Tenuto_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Tenuto {
			stage.Articulations_Tenuto_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Detached_legato_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Detached_legato {
			stage.Articulations_Detached_legato_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Staccatissimo_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Staccatissimo {
			stage.Articulations_Staccatissimo_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Spiccato_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Spiccato {
			stage.Articulations_Spiccato_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Scoop_reverseMap = make(map[*Empty_line]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_line := range articulations.Scoop {
			stage.Articulations_Scoop_reverseMap[_empty_line] = articulations
		}
	}
	stage.Articulations_Plop_reverseMap = make(map[*Empty_line]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_line := range articulations.Plop {
			stage.Articulations_Plop_reverseMap[_empty_line] = articulations
		}
	}
	stage.Articulations_Doit_reverseMap = make(map[*Empty_line]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_line := range articulations.Doit {
			stage.Articulations_Doit_reverseMap[_empty_line] = articulations
		}
	}
	stage.Articulations_Falloff_reverseMap = make(map[*Empty_line]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_line := range articulations.Falloff {
			stage.Articulations_Falloff_reverseMap[_empty_line] = articulations
		}
	}
	stage.Articulations_Breath_mark_reverseMap = make(map[*Breath_mark]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _breath_mark := range articulations.Breath_mark {
			stage.Articulations_Breath_mark_reverseMap[_breath_mark] = articulations
		}
	}
	stage.Articulations_Caesura_reverseMap = make(map[*Caesura]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _caesura := range articulations.Caesura {
			stage.Articulations_Caesura_reverseMap[_caesura] = articulations
		}
	}
	stage.Articulations_Stress_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Stress {
			stage.Articulations_Stress_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Unstress_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Unstress {
			stage.Articulations_Unstress_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Soft_accent_reverseMap = make(map[*Empty_placement]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _empty_placement := range articulations.Soft_accent {
			stage.Articulations_Soft_accent_reverseMap[_empty_placement] = articulations
		}
	}
	stage.Articulations_Other_articulation_reverseMap = make(map[*Other_placement_text]*Articulations)
	for articulations := range stage.Articulationss {
		_ = articulations
		for _, _other_placement_text := range articulations.Other_articulation {
			stage.Articulations_Other_articulation_reverseMap[_other_placement_text] = articulations
		}
	}

	// Compute reverse map for named struct Assess
	// insertion point per field

	// Compute reverse map for named struct Attributes
	// insertion point per field
	stage.Attributes_Key_reverseMap = make(map[*Key]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _key := range attributes.Key {
			stage.Attributes_Key_reverseMap[_key] = attributes
		}
	}
	stage.Attributes_Time_reverseMap = make(map[*Time]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _time := range attributes.Time {
			stage.Attributes_Time_reverseMap[_time] = attributes
		}
	}
	stage.Attributes_Clef_reverseMap = make(map[*Clef]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _clef := range attributes.Clef {
			stage.Attributes_Clef_reverseMap[_clef] = attributes
		}
	}
	stage.Attributes_Staff_details_reverseMap = make(map[*Staff_details]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _staff_details := range attributes.Staff_details {
			stage.Attributes_Staff_details_reverseMap[_staff_details] = attributes
		}
	}
	stage.Attributes_Transpose_reverseMap = make(map[*Transpose]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _transpose := range attributes.Transpose {
			stage.Attributes_Transpose_reverseMap[_transpose] = attributes
		}
	}
	stage.Attributes_For_part_reverseMap = make(map[*For_part]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _for_part := range attributes.For_part {
			stage.Attributes_For_part_reverseMap[_for_part] = attributes
		}
	}
	stage.Attributes_Directive_reverseMap = make(map[*A_directive]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _a_directive := range attributes.Directive {
			stage.Attributes_Directive_reverseMap[_a_directive] = attributes
		}
	}
	stage.Attributes_Measure_style_reverseMap = make(map[*Measure_style]*Attributes)
	for attributes := range stage.Attributess {
		_ = attributes
		for _, _measure_style := range attributes.Measure_style {
			stage.Attributes_Measure_style_reverseMap[_measure_style] = attributes
		}
	}

	// Compute reverse map for named struct Backup
	// insertion point per field

	// Compute reverse map for named struct Bar_style_color
	// insertion point per field

	// Compute reverse map for named struct Barline
	// insertion point per field

	// Compute reverse map for named struct Barre
	// insertion point per field

	// Compute reverse map for named struct Bass
	// insertion point per field

	// Compute reverse map for named struct Bass_step
	// insertion point per field

	// Compute reverse map for named struct Beam
	// insertion point per field

	// Compute reverse map for named struct Beat_repeat
	// insertion point per field

	// Compute reverse map for named struct Beat_unit_tied
	// insertion point per field

	// Compute reverse map for named struct Beater
	// insertion point per field

	// Compute reverse map for named struct Bend
	// insertion point per field

	// Compute reverse map for named struct Bookmark
	// insertion point per field

	// Compute reverse map for named struct Bracket
	// insertion point per field

	// Compute reverse map for named struct Breath_mark
	// insertion point per field

	// Compute reverse map for named struct Caesura
	// insertion point per field

	// Compute reverse map for named struct Cancel
	// insertion point per field

	// Compute reverse map for named struct Clef
	// insertion point per field

	// Compute reverse map for named struct Coda
	// insertion point per field

	// Compute reverse map for named struct Credit
	// insertion point per field
	stage.Credit_Link_reverseMap = make(map[*Link]*Credit)
	for credit := range stage.Credits {
		_ = credit
		for _, _link := range credit.Link {
			stage.Credit_Link_reverseMap[_link] = credit
		}
	}
	stage.Credit_Bookmark_reverseMap = make(map[*Bookmark]*Credit)
	for credit := range stage.Credits {
		_ = credit
		for _, _bookmark := range credit.Bookmark {
			stage.Credit_Bookmark_reverseMap[_bookmark] = credit
		}
	}
	stage.Credit_Credit_words_reverseMap = make(map[*Formatted_text_id]*Credit)
	for credit := range stage.Credits {
		_ = credit
		for _, _formatted_text_id := range credit.Credit_words {
			stage.Credit_Credit_words_reverseMap[_formatted_text_id] = credit
		}
	}
	stage.Credit_Credit_symbol_reverseMap = make(map[*Formatted_symbol_id]*Credit)
	for credit := range stage.Credits {
		_ = credit
		for _, _formatted_symbol_id := range credit.Credit_symbol {
			stage.Credit_Credit_symbol_reverseMap[_formatted_symbol_id] = credit
		}
	}

	// Compute reverse map for named struct Dashes
	// insertion point per field

	// Compute reverse map for named struct Defaults
	// insertion point per field
	stage.Defaults_Staff_layout_reverseMap = make(map[*Staff_layout]*Defaults)
	for defaults := range stage.Defaultss {
		_ = defaults
		for _, _staff_layout := range defaults.Staff_layout {
			stage.Defaults_Staff_layout_reverseMap[_staff_layout] = defaults
		}
	}
	stage.Defaults_Lyric_font_reverseMap = make(map[*Lyric_font]*Defaults)
	for defaults := range stage.Defaultss {
		_ = defaults
		for _, _lyric_font := range defaults.Lyric_font {
			stage.Defaults_Lyric_font_reverseMap[_lyric_font] = defaults
		}
	}
	stage.Defaults_Lyric_language_reverseMap = make(map[*Lyric_language]*Defaults)
	for defaults := range stage.Defaultss {
		_ = defaults
		for _, _lyric_language := range defaults.Lyric_language {
			stage.Defaults_Lyric_language_reverseMap[_lyric_language] = defaults
		}
	}

	// Compute reverse map for named struct Degree
	// insertion point per field

	// Compute reverse map for named struct Degree_alter
	// insertion point per field

	// Compute reverse map for named struct Degree_type
	// insertion point per field

	// Compute reverse map for named struct Degree_value
	// insertion point per field

	// Compute reverse map for named struct Direction
	// insertion point per field
	stage.Direction_Direction_type_reverseMap = make(map[*Direction_type]*Direction)
	for direction := range stage.Directions {
		_ = direction
		for _, _direction_type := range direction.Direction_type {
			stage.Direction_Direction_type_reverseMap[_direction_type] = direction
		}
	}

	// Compute reverse map for named struct Direction_type
	// insertion point per field
	stage.Direction_type_Rehearsal_reverseMap = make(map[*Formatted_text_id]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _formatted_text_id := range direction_type.Rehearsal {
			stage.Direction_type_Rehearsal_reverseMap[_formatted_text_id] = direction_type
		}
	}
	stage.Direction_type_Segno_reverseMap = make(map[*Segno]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _segno := range direction_type.Segno {
			stage.Direction_type_Segno_reverseMap[_segno] = direction_type
		}
	}
	stage.Direction_type_Coda_reverseMap = make(map[*Coda]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _coda := range direction_type.Coda {
			stage.Direction_type_Coda_reverseMap[_coda] = direction_type
		}
	}
	stage.Direction_type_Words_reverseMap = make(map[*Formatted_text_id]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _formatted_text_id := range direction_type.Words {
			stage.Direction_type_Words_reverseMap[_formatted_text_id] = direction_type
		}
	}
	stage.Direction_type_Symbol_reverseMap = make(map[*Formatted_symbol_id]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _formatted_symbol_id := range direction_type.Symbol {
			stage.Direction_type_Symbol_reverseMap[_formatted_symbol_id] = direction_type
		}
	}
	stage.Direction_type_Dynamics_reverseMap = make(map[*Dynamics]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _dynamics := range direction_type.Dynamics {
			stage.Direction_type_Dynamics_reverseMap[_dynamics] = direction_type
		}
	}
	stage.Direction_type_Percussion_reverseMap = make(map[*Percussion]*Direction_type)
	for direction_type := range stage.Direction_types {
		_ = direction_type
		for _, _percussion := range direction_type.Percussion {
			stage.Direction_type_Percussion_reverseMap[_percussion] = direction_type
		}
	}

	// Compute reverse map for named struct Distance
	// insertion point per field

	// Compute reverse map for named struct Double
	// insertion point per field

	// Compute reverse map for named struct Dynamics
	// insertion point per field
	stage.Dynamics_Other_dynamics_reverseMap = make(map[*Other_text]*Dynamics)
	for dynamics := range stage.Dynamicss {
		_ = dynamics
		for _, _other_text := range dynamics.Other_dynamics {
			stage.Dynamics_Other_dynamics_reverseMap[_other_text] = dynamics
		}
	}

	// Compute reverse map for named struct Effect
	// insertion point per field

	// Compute reverse map for named struct Elision
	// insertion point per field

	// Compute reverse map for named struct Empty
	// insertion point per field

	// Compute reverse map for named struct Empty_font
	// insertion point per field

	// Compute reverse map for named struct Empty_line
	// insertion point per field

	// Compute reverse map for named struct Empty_placement
	// insertion point per field

	// Compute reverse map for named struct Empty_placement_smufl
	// insertion point per field

	// Compute reverse map for named struct Empty_print_object_style_align
	// insertion point per field

	// Compute reverse map for named struct Empty_print_style
	// insertion point per field

	// Compute reverse map for named struct Empty_print_style_align
	// insertion point per field

	// Compute reverse map for named struct Empty_print_style_align_id
	// insertion point per field

	// Compute reverse map for named struct Empty_trill_sound
	// insertion point per field

	// Compute reverse map for named struct Encoding
	// insertion point per field
	stage.Encoding_Encoder_reverseMap = make(map[*Typed_text]*Encoding)
	for encoding := range stage.Encodings {
		_ = encoding
		for _, _typed_text := range encoding.Encoder {
			stage.Encoding_Encoder_reverseMap[_typed_text] = encoding
		}
	}
	stage.Encoding_Supports_reverseMap = make(map[*Supports]*Encoding)
	for encoding := range stage.Encodings {
		_ = encoding
		for _, _supports := range encoding.Supports {
			stage.Encoding_Supports_reverseMap[_supports] = encoding
		}
	}

	// Compute reverse map for named struct Ending
	// insertion point per field

	// Compute reverse map for named struct Extend
	// insertion point per field

	// Compute reverse map for named struct Feature
	// insertion point per field

	// Compute reverse map for named struct Fermata
	// insertion point per field

	// Compute reverse map for named struct Figure
	// insertion point per field

	// Compute reverse map for named struct Figured_bass
	// insertion point per field
	stage.Figured_bass_Figure_reverseMap = make(map[*Figure]*Figured_bass)
	for figured_bass := range stage.Figured_basss {
		_ = figured_bass
		for _, _figure := range figured_bass.Figure {
			stage.Figured_bass_Figure_reverseMap[_figure] = figured_bass
		}
	}

	// Compute reverse map for named struct Fingering
	// insertion point per field

	// Compute reverse map for named struct First_fret
	// insertion point per field

	// Compute reverse map for named struct For_part
	// insertion point per field

	// Compute reverse map for named struct Formatted_symbol
	// insertion point per field

	// Compute reverse map for named struct Formatted_symbol_id
	// insertion point per field

	// Compute reverse map for named struct Formatted_text
	// insertion point per field

	// Compute reverse map for named struct Formatted_text_id
	// insertion point per field

	// Compute reverse map for named struct Forward
	// insertion point per field

	// Compute reverse map for named struct Frame
	// insertion point per field
	stage.Frame_Frame_note_reverseMap = make(map[*Frame_note]*Frame)
	for frame := range stage.Frames {
		_ = frame
		for _, _frame_note := range frame.Frame_note {
			stage.Frame_Frame_note_reverseMap[_frame_note] = frame
		}
	}

	// Compute reverse map for named struct Frame_note
	// insertion point per field

	// Compute reverse map for named struct Fret
	// insertion point per field

	// Compute reverse map for named struct Glass
	// insertion point per field

	// Compute reverse map for named struct Glissando
	// insertion point per field

	// Compute reverse map for named struct Glyph
	// insertion point per field

	// Compute reverse map for named struct Grace
	// insertion point per field

	// Compute reverse map for named struct Group_barline
	// insertion point per field

	// Compute reverse map for named struct Group_name
	// insertion point per field

	// Compute reverse map for named struct Group_symbol
	// insertion point per field

	// Compute reverse map for named struct Grouping
	// insertion point per field
	stage.Grouping_Feature_reverseMap = make(map[*Feature]*Grouping)
	for grouping := range stage.Groupings {
		_ = grouping
		for _, _feature := range grouping.Feature {
			stage.Grouping_Feature_reverseMap[_feature] = grouping
		}
	}

	// Compute reverse map for named struct Hammer_on_pull_off
	// insertion point per field

	// Compute reverse map for named struct Handbell
	// insertion point per field

	// Compute reverse map for named struct Harmon_closed
	// insertion point per field

	// Compute reverse map for named struct Harmon_mute
	// insertion point per field

	// Compute reverse map for named struct Harmonic
	// insertion point per field

	// Compute reverse map for named struct Harmony
	// insertion point per field
	stage.Harmony_Degree_reverseMap = make(map[*Degree]*Harmony)
	for harmony := range stage.Harmonys {
		_ = harmony
		for _, _degree := range harmony.Degree {
			stage.Harmony_Degree_reverseMap[_degree] = harmony
		}
	}

	// Compute reverse map for named struct Harmony_alter
	// insertion point per field

	// Compute reverse map for named struct Harp_pedals
	// insertion point per field
	stage.Harp_pedals_Pedal_tuning_reverseMap = make(map[*Pedal_tuning]*Harp_pedals)
	for harp_pedals := range stage.Harp_pedalss {
		_ = harp_pedals
		for _, _pedal_tuning := range harp_pedals.Pedal_tuning {
			stage.Harp_pedals_Pedal_tuning_reverseMap[_pedal_tuning] = harp_pedals
		}
	}

	// Compute reverse map for named struct Heel_toe
	// insertion point per field

	// Compute reverse map for named struct Hole
	// insertion point per field

	// Compute reverse map for named struct Hole_closed
	// insertion point per field

	// Compute reverse map for named struct Horizontal_turn
	// insertion point per field

	// Compute reverse map for named struct Identification
	// insertion point per field
	stage.Identification_Creator_reverseMap = make(map[*Typed_text]*Identification)
	for identification := range stage.Identifications {
		_ = identification
		for _, _typed_text := range identification.Creator {
			stage.Identification_Creator_reverseMap[_typed_text] = identification
		}
	}
	stage.Identification_Rights_reverseMap = make(map[*Typed_text]*Identification)
	for identification := range stage.Identifications {
		_ = identification
		for _, _typed_text := range identification.Rights {
			stage.Identification_Rights_reverseMap[_typed_text] = identification
		}
	}
	stage.Identification_Relation_reverseMap = make(map[*Typed_text]*Identification)
	for identification := range stage.Identifications {
		_ = identification
		for _, _typed_text := range identification.Relation {
			stage.Identification_Relation_reverseMap[_typed_text] = identification
		}
	}

	// Compute reverse map for named struct Image
	// insertion point per field

	// Compute reverse map for named struct Instrument
	// insertion point per field

	// Compute reverse map for named struct Instrument_change
	// insertion point per field

	// Compute reverse map for named struct Instrument_link
	// insertion point per field

	// Compute reverse map for named struct Interchangeable
	// insertion point per field

	// Compute reverse map for named struct Inversion
	// insertion point per field

	// Compute reverse map for named struct Key
	// insertion point per field
	stage.Key_Key_octave_reverseMap = make(map[*Key_octave]*Key)
	for key := range stage.Keys {
		_ = key
		for _, _key_octave := range key.Key_octave {
			stage.Key_Key_octave_reverseMap[_key_octave] = key
		}
	}

	// Compute reverse map for named struct Key_accidental
	// insertion point per field

	// Compute reverse map for named struct Key_octave
	// insertion point per field

	// Compute reverse map for named struct Kind
	// insertion point per field

	// Compute reverse map for named struct Level
	// insertion point per field

	// Compute reverse map for named struct Line_detail
	// insertion point per field

	// Compute reverse map for named struct Line_width
	// insertion point per field

	// Compute reverse map for named struct Link
	// insertion point per field

	// Compute reverse map for named struct Listen
	// insertion point per field
	stage.Listen_Assess_reverseMap = make(map[*Assess]*Listen)
	for listen := range stage.Listens {
		_ = listen
		for _, _assess := range listen.Assess {
			stage.Listen_Assess_reverseMap[_assess] = listen
		}
	}
	stage.Listen_Wait_reverseMap = make(map[*Wait]*Listen)
	for listen := range stage.Listens {
		_ = listen
		for _, _wait := range listen.Wait {
			stage.Listen_Wait_reverseMap[_wait] = listen
		}
	}
	stage.Listen_Other_listen_reverseMap = make(map[*Other_listening]*Listen)
	for listen := range stage.Listens {
		_ = listen
		for _, _other_listening := range listen.Other_listen {
			stage.Listen_Other_listen_reverseMap[_other_listening] = listen
		}
	}

	// Compute reverse map for named struct Listening
	// insertion point per field
	stage.Listening_Sync_reverseMap = make(map[*Sync]*Listening)
	for listening := range stage.Listenings {
		_ = listening
		for _, _sync := range listening.Sync {
			stage.Listening_Sync_reverseMap[_sync] = listening
		}
	}
	stage.Listening_Other_listening_reverseMap = make(map[*Other_listening]*Listening)
	for listening := range stage.Listenings {
		_ = listening
		for _, _other_listening := range listening.Other_listening {
			stage.Listening_Other_listening_reverseMap[_other_listening] = listening
		}
	}

	// Compute reverse map for named struct Lyric
	// insertion point per field
	stage.Lyric_Elision_reverseMap = make(map[*Elision]*Lyric)
	for lyric := range stage.Lyrics {
		_ = lyric
		for _, _elision := range lyric.Elision {
			stage.Lyric_Elision_reverseMap[_elision] = lyric
		}
	}
	stage.Lyric_Text_reverseMap = make(map[*Text_element_data]*Lyric)
	for lyric := range stage.Lyrics {
		_ = lyric
		for _, _text_element_data := range lyric.Text {
			stage.Lyric_Text_reverseMap[_text_element_data] = lyric
		}
	}

	// Compute reverse map for named struct Lyric_font
	// insertion point per field

	// Compute reverse map for named struct Lyric_language
	// insertion point per field

	// Compute reverse map for named struct Measure_layout
	// insertion point per field

	// Compute reverse map for named struct Measure_numbering
	// insertion point per field

	// Compute reverse map for named struct Measure_repeat
	// insertion point per field

	// Compute reverse map for named struct Measure_style
	// insertion point per field

	// Compute reverse map for named struct Membrane
	// insertion point per field

	// Compute reverse map for named struct Metal
	// insertion point per field

	// Compute reverse map for named struct Metronome
	// insertion point per field
	stage.Metronome_Beat_unit_tied_reverseMap = make(map[*Beat_unit_tied]*Metronome)
	for metronome := range stage.Metronomes {
		_ = metronome
		for _, _beat_unit_tied := range metronome.Beat_unit_tied {
			stage.Metronome_Beat_unit_tied_reverseMap[_beat_unit_tied] = metronome
		}
	}
	stage.Metronome_Metronome_note_reverseMap = make(map[*Metronome_note]*Metronome)
	for metronome := range stage.Metronomes {
		_ = metronome
		for _, _metronome_note := range metronome.Metronome_note {
			stage.Metronome_Metronome_note_reverseMap[_metronome_note] = metronome
		}
	}

	// Compute reverse map for named struct Metronome_beam
	// insertion point per field

	// Compute reverse map for named struct Metronome_note
	// insertion point per field
	stage.Metronome_note_Metronome_beam_reverseMap = make(map[*Metronome_beam]*Metronome_note)
	for metronome_note := range stage.Metronome_notes {
		_ = metronome_note
		for _, _metronome_beam := range metronome_note.Metronome_beam {
			stage.Metronome_note_Metronome_beam_reverseMap[_metronome_beam] = metronome_note
		}
	}

	// Compute reverse map for named struct Metronome_tied
	// insertion point per field

	// Compute reverse map for named struct Metronome_tuplet
	// insertion point per field

	// Compute reverse map for named struct Midi_device
	// insertion point per field

	// Compute reverse map for named struct Midi_instrument
	// insertion point per field

	// Compute reverse map for named struct Miscellaneous
	// insertion point per field
	stage.Miscellaneous_Miscellaneous_field_reverseMap = make(map[*Miscellaneous_field]*Miscellaneous)
	for miscellaneous := range stage.Miscellaneouss {
		_ = miscellaneous
		for _, _miscellaneous_field := range miscellaneous.Miscellaneous_field {
			stage.Miscellaneous_Miscellaneous_field_reverseMap[_miscellaneous_field] = miscellaneous
		}
	}

	// Compute reverse map for named struct Miscellaneous_field
	// insertion point per field

	// Compute reverse map for named struct Mordent
	// insertion point per field

	// Compute reverse map for named struct Multiple_rest
	// insertion point per field

	// Compute reverse map for named struct Name_display
	// insertion point per field
	stage.Name_display_Display_text_reverseMap = make(map[*Formatted_text]*Name_display)
	for name_display := range stage.Name_displays {
		_ = name_display
		for _, _formatted_text := range name_display.Display_text {
			stage.Name_display_Display_text_reverseMap[_formatted_text] = name_display
		}
	}
	stage.Name_display_Accidental_text_reverseMap = make(map[*Accidental_text]*Name_display)
	for name_display := range stage.Name_displays {
		_ = name_display
		for _, _accidental_text := range name_display.Accidental_text {
			stage.Name_display_Accidental_text_reverseMap[_accidental_text] = name_display
		}
	}

	// Compute reverse map for named struct Non_arpeggiate
	// insertion point per field

	// Compute reverse map for named struct Notations
	// insertion point per field
	stage.Notations_Tied_reverseMap = make(map[*Tied]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _tied := range notations.Tied {
			stage.Notations_Tied_reverseMap[_tied] = notations
		}
	}
	stage.Notations_Slur_reverseMap = make(map[*Slur]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _slur := range notations.Slur {
			stage.Notations_Slur_reverseMap[_slur] = notations
		}
	}
	stage.Notations_Tuplet_reverseMap = make(map[*Tuplet]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _tuplet := range notations.Tuplet {
			stage.Notations_Tuplet_reverseMap[_tuplet] = notations
		}
	}
	stage.Notations_Glissando_reverseMap = make(map[*Glissando]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _glissando := range notations.Glissando {
			stage.Notations_Glissando_reverseMap[_glissando] = notations
		}
	}
	stage.Notations_Slide_reverseMap = make(map[*Slide]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _slide := range notations.Slide {
			stage.Notations_Slide_reverseMap[_slide] = notations
		}
	}
	stage.Notations_Ornaments_reverseMap = make(map[*Ornaments]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _ornaments := range notations.Ornaments {
			stage.Notations_Ornaments_reverseMap[_ornaments] = notations
		}
	}
	stage.Notations_Technical_reverseMap = make(map[*Technical]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _technical := range notations.Technical {
			stage.Notations_Technical_reverseMap[_technical] = notations
		}
	}
	stage.Notations_Articulations_reverseMap = make(map[*Articulations]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _articulations := range notations.Articulations {
			stage.Notations_Articulations_reverseMap[_articulations] = notations
		}
	}
	stage.Notations_Dynamics_reverseMap = make(map[*Dynamics]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _dynamics := range notations.Dynamics {
			stage.Notations_Dynamics_reverseMap[_dynamics] = notations
		}
	}
	stage.Notations_Fermata_reverseMap = make(map[*Fermata]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _fermata := range notations.Fermata {
			stage.Notations_Fermata_reverseMap[_fermata] = notations
		}
	}
	stage.Notations_Arpeggiate_reverseMap = make(map[*Arpeggiate]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _arpeggiate := range notations.Arpeggiate {
			stage.Notations_Arpeggiate_reverseMap[_arpeggiate] = notations
		}
	}
	stage.Notations_Non_arpeggiate_reverseMap = make(map[*Non_arpeggiate]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _non_arpeggiate := range notations.Non_arpeggiate {
			stage.Notations_Non_arpeggiate_reverseMap[_non_arpeggiate] = notations
		}
	}
	stage.Notations_Accidental_mark_reverseMap = make(map[*Accidental_mark]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _accidental_mark := range notations.Accidental_mark {
			stage.Notations_Accidental_mark_reverseMap[_accidental_mark] = notations
		}
	}
	stage.Notations_Other_notation_reverseMap = make(map[*Other_notation]*Notations)
	for notations := range stage.Notationss {
		_ = notations
		for _, _other_notation := range notations.Other_notation {
			stage.Notations_Other_notation_reverseMap[_other_notation] = notations
		}
	}

	// Compute reverse map for named struct Note
	// insertion point per field
	stage.Note_Instrument_reverseMap = make(map[*Instrument]*Note)
	for note := range stage.Notes {
		_ = note
		for _, _instrument := range note.Instrument {
			stage.Note_Instrument_reverseMap[_instrument] = note
		}
	}
	stage.Note_Dot_reverseMap = make(map[*Empty_placement]*Note)
	for note := range stage.Notes {
		_ = note
		for _, _empty_placement := range note.Dot {
			stage.Note_Dot_reverseMap[_empty_placement] = note
		}
	}
	stage.Note_Notations_reverseMap = make(map[*Notations]*Note)
	for note := range stage.Notes {
		_ = note
		for _, _notations := range note.Notations {
			stage.Note_Notations_reverseMap[_notations] = note
		}
	}
	stage.Note_Lyric_reverseMap = make(map[*Lyric]*Note)
	for note := range stage.Notes {
		_ = note
		for _, _lyric := range note.Lyric {
			stage.Note_Lyric_reverseMap[_lyric] = note
		}
	}

	// Compute reverse map for named struct Note_size
	// insertion point per field

	// Compute reverse map for named struct Note_type
	// insertion point per field

	// Compute reverse map for named struct Notehead
	// insertion point per field

	// Compute reverse map for named struct Notehead_text
	// insertion point per field
	stage.Notehead_text_Display_text_reverseMap = make(map[*Formatted_text]*Notehead_text)
	for notehead_text := range stage.Notehead_texts {
		_ = notehead_text
		for _, _formatted_text := range notehead_text.Display_text {
			stage.Notehead_text_Display_text_reverseMap[_formatted_text] = notehead_text
		}
	}
	stage.Notehead_text_Accidental_text_reverseMap = make(map[*Accidental_text]*Notehead_text)
	for notehead_text := range stage.Notehead_texts {
		_ = notehead_text
		for _, _accidental_text := range notehead_text.Accidental_text {
			stage.Notehead_text_Accidental_text_reverseMap[_accidental_text] = notehead_text
		}
	}

	// Compute reverse map for named struct Numeral
	// insertion point per field

	// Compute reverse map for named struct Numeral_key
	// insertion point per field

	// Compute reverse map for named struct Numeral_root
	// insertion point per field

	// Compute reverse map for named struct Octave_shift
	// insertion point per field

	// Compute reverse map for named struct Offset
	// insertion point per field

	// Compute reverse map for named struct Opus
	// insertion point per field

	// Compute reverse map for named struct Ornaments
	// insertion point per field
	stage.Ornaments_Trill_mark_reverseMap = make(map[*Empty_trill_sound]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_trill_sound := range ornaments.Trill_mark {
			stage.Ornaments_Trill_mark_reverseMap[_empty_trill_sound] = ornaments
		}
	}
	stage.Ornaments_Turn_reverseMap = make(map[*Horizontal_turn]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _horizontal_turn := range ornaments.Turn {
			stage.Ornaments_Turn_reverseMap[_horizontal_turn] = ornaments
		}
	}
	stage.Ornaments_Delayed_turn_reverseMap = make(map[*Horizontal_turn]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _horizontal_turn := range ornaments.Delayed_turn {
			stage.Ornaments_Delayed_turn_reverseMap[_horizontal_turn] = ornaments
		}
	}
	stage.Ornaments_Inverted_turn_reverseMap = make(map[*Horizontal_turn]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _horizontal_turn := range ornaments.Inverted_turn {
			stage.Ornaments_Inverted_turn_reverseMap[_horizontal_turn] = ornaments
		}
	}
	stage.Ornaments_Delayed_inverted_turn_reverseMap = make(map[*Horizontal_turn]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _horizontal_turn := range ornaments.Delayed_inverted_turn {
			stage.Ornaments_Delayed_inverted_turn_reverseMap[_horizontal_turn] = ornaments
		}
	}
	stage.Ornaments_Vertical_turn_reverseMap = make(map[*Empty_trill_sound]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_trill_sound := range ornaments.Vertical_turn {
			stage.Ornaments_Vertical_turn_reverseMap[_empty_trill_sound] = ornaments
		}
	}
	stage.Ornaments_Inverted_vertical_turn_reverseMap = make(map[*Empty_trill_sound]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_trill_sound := range ornaments.Inverted_vertical_turn {
			stage.Ornaments_Inverted_vertical_turn_reverseMap[_empty_trill_sound] = ornaments
		}
	}
	stage.Ornaments_Shake_reverseMap = make(map[*Empty_trill_sound]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_trill_sound := range ornaments.Shake {
			stage.Ornaments_Shake_reverseMap[_empty_trill_sound] = ornaments
		}
	}
	stage.Ornaments_Wavy_line_reverseMap = make(map[*Wavy_line]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _wavy_line := range ornaments.Wavy_line {
			stage.Ornaments_Wavy_line_reverseMap[_wavy_line] = ornaments
		}
	}
	stage.Ornaments_Mordent_reverseMap = make(map[*Mordent]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _mordent := range ornaments.Mordent {
			stage.Ornaments_Mordent_reverseMap[_mordent] = ornaments
		}
	}
	stage.Ornaments_Inverted_mordent_reverseMap = make(map[*Mordent]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _mordent := range ornaments.Inverted_mordent {
			stage.Ornaments_Inverted_mordent_reverseMap[_mordent] = ornaments
		}
	}
	stage.Ornaments_Schleifer_reverseMap = make(map[*Empty_placement]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_placement := range ornaments.Schleifer {
			stage.Ornaments_Schleifer_reverseMap[_empty_placement] = ornaments
		}
	}
	stage.Ornaments_Tremolo_reverseMap = make(map[*Tremolo]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _tremolo := range ornaments.Tremolo {
			stage.Ornaments_Tremolo_reverseMap[_tremolo] = ornaments
		}
	}
	stage.Ornaments_Haydn_reverseMap = make(map[*Empty_trill_sound]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _empty_trill_sound := range ornaments.Haydn {
			stage.Ornaments_Haydn_reverseMap[_empty_trill_sound] = ornaments
		}
	}
	stage.Ornaments_Other_ornament_reverseMap = make(map[*Other_placement_text]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _other_placement_text := range ornaments.Other_ornament {
			stage.Ornaments_Other_ornament_reverseMap[_other_placement_text] = ornaments
		}
	}
	stage.Ornaments_Accidental_mark_reverseMap = make(map[*Accidental_mark]*Ornaments)
	for ornaments := range stage.Ornamentss {
		_ = ornaments
		for _, _accidental_mark := range ornaments.Accidental_mark {
			stage.Ornaments_Accidental_mark_reverseMap[_accidental_mark] = ornaments
		}
	}

	// Compute reverse map for named struct Other_appearance
	// insertion point per field

	// Compute reverse map for named struct Other_direction
	// insertion point per field

	// Compute reverse map for named struct Other_listening
	// insertion point per field

	// Compute reverse map for named struct Other_notation
	// insertion point per field

	// Compute reverse map for named struct Other_placement_text
	// insertion point per field

	// Compute reverse map for named struct Other_play
	// insertion point per field

	// Compute reverse map for named struct Other_text
	// insertion point per field

	// Compute reverse map for named struct Page_layout
	// insertion point per field

	// Compute reverse map for named struct Page_margins
	// insertion point per field

	// Compute reverse map for named struct Part_clef
	// insertion point per field

	// Compute reverse map for named struct Part_group
	// insertion point per field

	// Compute reverse map for named struct Part_link
	// insertion point per field
	stage.Part_link_Instrument_link_reverseMap = make(map[*Instrument_link]*Part_link)
	for part_link := range stage.Part_links {
		_ = part_link
		for _, _instrument_link := range part_link.Instrument_link {
			stage.Part_link_Instrument_link_reverseMap[_instrument_link] = part_link
		}
	}

	// Compute reverse map for named struct Part_list
	// insertion point per field

	// Compute reverse map for named struct Part_name
	// insertion point per field

	// Compute reverse map for named struct Part_symbol
	// insertion point per field

	// Compute reverse map for named struct Part_transpose
	// insertion point per field

	// Compute reverse map for named struct Pedal
	// insertion point per field

	// Compute reverse map for named struct Pedal_tuning
	// insertion point per field

	// Compute reverse map for named struct Per_minute
	// insertion point per field

	// Compute reverse map for named struct Percussion
	// insertion point per field

	// Compute reverse map for named struct Pitch
	// insertion point per field

	// Compute reverse map for named struct Pitched
	// insertion point per field

	// Compute reverse map for named struct Placement_text
	// insertion point per field

	// Compute reverse map for named struct Play
	// insertion point per field
	stage.Play_Other_play_reverseMap = make(map[*Other_play]*Play)
	for play := range stage.Plays {
		_ = play
		for _, _other_play := range play.Other_play {
			stage.Play_Other_play_reverseMap[_other_play] = play
		}
	}

	// Compute reverse map for named struct Player
	// insertion point per field

	// Compute reverse map for named struct Principal_voice
	// insertion point per field

	// Compute reverse map for named struct Print
	// insertion point per field
	stage.Print_Staff_layout_reverseMap = make(map[*Staff_layout]*Print)
	for print := range stage.Prints {
		_ = print
		for _, _staff_layout := range print.Staff_layout {
			stage.Print_Staff_layout_reverseMap[_staff_layout] = print
		}
	}

	// Compute reverse map for named struct Release
	// insertion point per field

	// Compute reverse map for named struct Repeat
	// insertion point per field

	// Compute reverse map for named struct Rest
	// insertion point per field

	// Compute reverse map for named struct Root
	// insertion point per field

	// Compute reverse map for named struct Root_step
	// insertion point per field

	// Compute reverse map for named struct Scaling
	// insertion point per field

	// Compute reverse map for named struct Scordatura
	// insertion point per field
	stage.Scordatura_Accord_reverseMap = make(map[*Accord]*Scordatura)
	for scordatura := range stage.Scordaturas {
		_ = scordatura
		for _, _accord := range scordatura.Accord {
			stage.Scordatura_Accord_reverseMap[_accord] = scordatura
		}
	}

	// Compute reverse map for named struct Score_instrument
	// insertion point per field

	// Compute reverse map for named struct Score_part
	// insertion point per field
	stage.Score_part_Part_link_reverseMap = make(map[*Part_link]*Score_part)
	for score_part := range stage.Score_parts {
		_ = score_part
		for _, _part_link := range score_part.Part_link {
			stage.Score_part_Part_link_reverseMap[_part_link] = score_part
		}
	}
	stage.Score_part_Score_instrument_reverseMap = make(map[*Score_instrument]*Score_part)
	for score_part := range stage.Score_parts {
		_ = score_part
		for _, _score_instrument := range score_part.Score_instrument {
			stage.Score_part_Score_instrument_reverseMap[_score_instrument] = score_part
		}
	}
	stage.Score_part_Player_reverseMap = make(map[*Player]*Score_part)
	for score_part := range stage.Score_parts {
		_ = score_part
		for _, _player := range score_part.Player {
			stage.Score_part_Player_reverseMap[_player] = score_part
		}
	}
	stage.Score_part_Midi_device_reverseMap = make(map[*Midi_device]*Score_part)
	for score_part := range stage.Score_parts {
		_ = score_part
		for _, _midi_device := range score_part.Midi_device {
			stage.Score_part_Midi_device_reverseMap[_midi_device] = score_part
		}
	}
	stage.Score_part_Midi_instrument_reverseMap = make(map[*Midi_instrument]*Score_part)
	for score_part := range stage.Score_parts {
		_ = score_part
		for _, _midi_instrument := range score_part.Midi_instrument {
			stage.Score_part_Midi_instrument_reverseMap[_midi_instrument] = score_part
		}
	}

	// Compute reverse map for named struct Score_partwise
	// insertion point per field
	stage.Score_partwise_Credit_reverseMap = make(map[*Credit]*Score_partwise)
	for score_partwise := range stage.Score_partwises {
		_ = score_partwise
		for _, _credit := range score_partwise.Credit {
			stage.Score_partwise_Credit_reverseMap[_credit] = score_partwise
		}
	}
	stage.Score_partwise_Part_reverseMap = make(map[*A_part]*Score_partwise)
	for score_partwise := range stage.Score_partwises {
		_ = score_partwise
		for _, _a_part := range score_partwise.Part {
			stage.Score_partwise_Part_reverseMap[_a_part] = score_partwise
		}
	}

	// Compute reverse map for named struct Score_timewise
	// insertion point per field
	stage.Score_timewise_Credit_reverseMap = make(map[*Credit]*Score_timewise)
	for score_timewise := range stage.Score_timewises {
		_ = score_timewise
		for _, _credit := range score_timewise.Credit {
			stage.Score_timewise_Credit_reverseMap[_credit] = score_timewise
		}
	}
	stage.Score_timewise_Measure_reverseMap = make(map[*A_measure_1]*Score_timewise)
	for score_timewise := range stage.Score_timewises {
		_ = score_timewise
		for _, _a_measure_1 := range score_timewise.Measure {
			stage.Score_timewise_Measure_reverseMap[_a_measure_1] = score_timewise
		}
	}

	// Compute reverse map for named struct Segno
	// insertion point per field

	// Compute reverse map for named struct Slash
	// insertion point per field

	// Compute reverse map for named struct Slide
	// insertion point per field

	// Compute reverse map for named struct Slur
	// insertion point per field

	// Compute reverse map for named struct Sound
	// insertion point per field
	stage.Sound_Instrument_change_reverseMap = make(map[*Instrument_change]*Sound)
	for sound := range stage.Sounds {
		_ = sound
		for _, _instrument_change := range sound.Instrument_change {
			stage.Sound_Instrument_change_reverseMap[_instrument_change] = sound
		}
	}
	stage.Sound_Midi_device_reverseMap = make(map[*Midi_device]*Sound)
	for sound := range stage.Sounds {
		_ = sound
		for _, _midi_device := range sound.Midi_device {
			stage.Sound_Midi_device_reverseMap[_midi_device] = sound
		}
	}
	stage.Sound_Midi_instrument_reverseMap = make(map[*Midi_instrument]*Sound)
	for sound := range stage.Sounds {
		_ = sound
		for _, _midi_instrument := range sound.Midi_instrument {
			stage.Sound_Midi_instrument_reverseMap[_midi_instrument] = sound
		}
	}
	stage.Sound_Play_reverseMap = make(map[*Play]*Sound)
	for sound := range stage.Sounds {
		_ = sound
		for _, _play := range sound.Play {
			stage.Sound_Play_reverseMap[_play] = sound
		}
	}

	// Compute reverse map for named struct Staff_details
	// insertion point per field
	stage.Staff_details_Line_detail_reverseMap = make(map[*Line_detail]*Staff_details)
	for staff_details := range stage.Staff_detailss {
		_ = staff_details
		for _, _line_detail := range staff_details.Line_detail {
			stage.Staff_details_Line_detail_reverseMap[_line_detail] = staff_details
		}
	}
	stage.Staff_details_Staff_tuning_reverseMap = make(map[*Staff_tuning]*Staff_details)
	for staff_details := range stage.Staff_detailss {
		_ = staff_details
		for _, _staff_tuning := range staff_details.Staff_tuning {
			stage.Staff_details_Staff_tuning_reverseMap[_staff_tuning] = staff_details
		}
	}

	// Compute reverse map for named struct Staff_divide
	// insertion point per field

	// Compute reverse map for named struct Staff_layout
	// insertion point per field

	// Compute reverse map for named struct Staff_size
	// insertion point per field

	// Compute reverse map for named struct Staff_tuning
	// insertion point per field

	// Compute reverse map for named struct Stem
	// insertion point per field

	// Compute reverse map for named struct Stick
	// insertion point per field

	// Compute reverse map for named struct String_mute
	// insertion point per field

	// Compute reverse map for named struct String_type
	// insertion point per field

	// Compute reverse map for named struct Strong_accent
	// insertion point per field

	// Compute reverse map for named struct Style_text
	// insertion point per field

	// Compute reverse map for named struct Supports
	// insertion point per field

	// Compute reverse map for named struct Swing
	// insertion point per field

	// Compute reverse map for named struct Sync
	// insertion point per field

	// Compute reverse map for named struct System_dividers
	// insertion point per field

	// Compute reverse map for named struct System_layout
	// insertion point per field

	// Compute reverse map for named struct System_margins
	// insertion point per field

	// Compute reverse map for named struct Tap
	// insertion point per field

	// Compute reverse map for named struct Technical
	// insertion point per field
	stage.Technical_Up_bow_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Up_bow {
			stage.Technical_Up_bow_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Down_bow_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Down_bow {
			stage.Technical_Down_bow_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Harmonic_reverseMap = make(map[*Harmonic]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _harmonic := range technical.Harmonic {
			stage.Technical_Harmonic_reverseMap[_harmonic] = technical
		}
	}
	stage.Technical_Open_string_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Open_string {
			stage.Technical_Open_string_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Thumb_position_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Thumb_position {
			stage.Technical_Thumb_position_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Fingering_reverseMap = make(map[*Fingering]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _fingering := range technical.Fingering {
			stage.Technical_Fingering_reverseMap[_fingering] = technical
		}
	}
	stage.Technical_Pluck_reverseMap = make(map[*Placement_text]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _placement_text := range technical.Pluck {
			stage.Technical_Pluck_reverseMap[_placement_text] = technical
		}
	}
	stage.Technical_Double_tongue_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Double_tongue {
			stage.Technical_Double_tongue_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Triple_tongue_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Triple_tongue {
			stage.Technical_Triple_tongue_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Stopped_reverseMap = make(map[*Empty_placement_smufl]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement_smufl := range technical.Stopped {
			stage.Technical_Stopped_reverseMap[_empty_placement_smufl] = technical
		}
	}
	stage.Technical_Snap_pizzicato_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Snap_pizzicato {
			stage.Technical_Snap_pizzicato_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Fret_reverseMap = make(map[*Fret]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _fret := range technical.Fret {
			stage.Technical_Fret_reverseMap[_fret] = technical
		}
	}
	stage.Technical_String_reverseMap = make(map[*String_type]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _string_type := range technical.String {
			stage.Technical_String_reverseMap[_string_type] = technical
		}
	}
	stage.Technical_Hammer_on_reverseMap = make(map[*Hammer_on_pull_off]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _hammer_on_pull_off := range technical.Hammer_on {
			stage.Technical_Hammer_on_reverseMap[_hammer_on_pull_off] = technical
		}
	}
	stage.Technical_Pull_off_reverseMap = make(map[*Hammer_on_pull_off]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _hammer_on_pull_off := range technical.Pull_off {
			stage.Technical_Pull_off_reverseMap[_hammer_on_pull_off] = technical
		}
	}
	stage.Technical_Bend_reverseMap = make(map[*Bend]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _bend := range technical.Bend {
			stage.Technical_Bend_reverseMap[_bend] = technical
		}
	}
	stage.Technical_Tap_reverseMap = make(map[*Tap]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _tap := range technical.Tap {
			stage.Technical_Tap_reverseMap[_tap] = technical
		}
	}
	stage.Technical_Heel_reverseMap = make(map[*Heel_toe]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _heel_toe := range technical.Heel {
			stage.Technical_Heel_reverseMap[_heel_toe] = technical
		}
	}
	stage.Technical_Toe_reverseMap = make(map[*Heel_toe]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _heel_toe := range technical.Toe {
			stage.Technical_Toe_reverseMap[_heel_toe] = technical
		}
	}
	stage.Technical_Fingernails_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Fingernails {
			stage.Technical_Fingernails_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Hole_reverseMap = make(map[*Hole]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _hole := range technical.Hole {
			stage.Technical_Hole_reverseMap[_hole] = technical
		}
	}
	stage.Technical_Arrow_reverseMap = make(map[*Arrow]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _arrow := range technical.Arrow {
			stage.Technical_Arrow_reverseMap[_arrow] = technical
		}
	}
	stage.Technical_Handbell_reverseMap = make(map[*Handbell]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _handbell := range technical.Handbell {
			stage.Technical_Handbell_reverseMap[_handbell] = technical
		}
	}
	stage.Technical_Brass_bend_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Brass_bend {
			stage.Technical_Brass_bend_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Flip_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Flip {
			stage.Technical_Flip_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Smear_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Smear {
			stage.Technical_Smear_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Open_reverseMap = make(map[*Empty_placement_smufl]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement_smufl := range technical.Open {
			stage.Technical_Open_reverseMap[_empty_placement_smufl] = technical
		}
	}
	stage.Technical_Half_muted_reverseMap = make(map[*Empty_placement_smufl]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement_smufl := range technical.Half_muted {
			stage.Technical_Half_muted_reverseMap[_empty_placement_smufl] = technical
		}
	}
	stage.Technical_Harmon_mute_reverseMap = make(map[*Harmon_mute]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _harmon_mute := range technical.Harmon_mute {
			stage.Technical_Harmon_mute_reverseMap[_harmon_mute] = technical
		}
	}
	stage.Technical_Golpe_reverseMap = make(map[*Empty_placement]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _empty_placement := range technical.Golpe {
			stage.Technical_Golpe_reverseMap[_empty_placement] = technical
		}
	}
	stage.Technical_Other_technical_reverseMap = make(map[*Other_placement_text]*Technical)
	for technical := range stage.Technicals {
		_ = technical
		for _, _other_placement_text := range technical.Other_technical {
			stage.Technical_Other_technical_reverseMap[_other_placement_text] = technical
		}
	}

	// Compute reverse map for named struct Text_element_data
	// insertion point per field

	// Compute reverse map for named struct Tie
	// insertion point per field

	// Compute reverse map for named struct Tied
	// insertion point per field

	// Compute reverse map for named struct Time
	// insertion point per field

	// Compute reverse map for named struct Time_modification
	// insertion point per field

	// Compute reverse map for named struct Timpani
	// insertion point per field

	// Compute reverse map for named struct Transpose
	// insertion point per field

	// Compute reverse map for named struct Tremolo
	// insertion point per field

	// Compute reverse map for named struct Tuplet
	// insertion point per field

	// Compute reverse map for named struct Tuplet_dot
	// insertion point per field

	// Compute reverse map for named struct Tuplet_number
	// insertion point per field

	// Compute reverse map for named struct Tuplet_portion
	// insertion point per field
	stage.Tuplet_portion_Tuplet_dot_reverseMap = make(map[*Tuplet_dot]*Tuplet_portion)
	for tuplet_portion := range stage.Tuplet_portions {
		_ = tuplet_portion
		for _, _tuplet_dot := range tuplet_portion.Tuplet_dot {
			stage.Tuplet_portion_Tuplet_dot_reverseMap[_tuplet_dot] = tuplet_portion
		}
	}

	// Compute reverse map for named struct Tuplet_type
	// insertion point per field

	// Compute reverse map for named struct Typed_text
	// insertion point per field

	// Compute reverse map for named struct Unpitched
	// insertion point per field

	// Compute reverse map for named struct Virtual_instrument
	// insertion point per field

	// Compute reverse map for named struct Wait
	// insertion point per field

	// Compute reverse map for named struct Wavy_line
	// insertion point per field

	// Compute reverse map for named struct Wedge
	// insertion point per field

	// Compute reverse map for named struct Wood
	// insertion point per field

	// Compute reverse map for named struct Work
	// insertion point per field

	// end of insertion point per named struct
}

func (stage *Stage) GetInstances() (res []GongstructIF) {
	// insertion point per named struct
	for instance := range stage.A_directives {
		res = append(res, instance)
	}

	for instance := range stage.A_measures {
		res = append(res, instance)
	}

	for instance := range stage.A_measure_1s {
		res = append(res, instance)
	}

	for instance := range stage.A_parts {
		res = append(res, instance)
	}

	for instance := range stage.A_part_1s {
		res = append(res, instance)
	}

	for instance := range stage.Accidentals {
		res = append(res, instance)
	}

	for instance := range stage.Accidental_marks {
		res = append(res, instance)
	}

	for instance := range stage.Accidental_texts {
		res = append(res, instance)
	}

	for instance := range stage.Accords {
		res = append(res, instance)
	}

	for instance := range stage.Accordion_registrations {
		res = append(res, instance)
	}

	for instance := range stage.Appearances {
		res = append(res, instance)
	}

	for instance := range stage.Arpeggiates {
		res = append(res, instance)
	}

	for instance := range stage.Arrows {
		res = append(res, instance)
	}

	for instance := range stage.Articulationss {
		res = append(res, instance)
	}

	for instance := range stage.Assesss {
		res = append(res, instance)
	}

	for instance := range stage.Attributess {
		res = append(res, instance)
	}

	for instance := range stage.Backups {
		res = append(res, instance)
	}

	for instance := range stage.Bar_style_colors {
		res = append(res, instance)
	}

	for instance := range stage.Barlines {
		res = append(res, instance)
	}

	for instance := range stage.Barres {
		res = append(res, instance)
	}

	for instance := range stage.Basss {
		res = append(res, instance)
	}

	for instance := range stage.Bass_steps {
		res = append(res, instance)
	}

	for instance := range stage.Beams {
		res = append(res, instance)
	}

	for instance := range stage.Beat_repeats {
		res = append(res, instance)
	}

	for instance := range stage.Beat_unit_tieds {
		res = append(res, instance)
	}

	for instance := range stage.Beaters {
		res = append(res, instance)
	}

	for instance := range stage.Bends {
		res = append(res, instance)
	}

	for instance := range stage.Bookmarks {
		res = append(res, instance)
	}

	for instance := range stage.Brackets {
		res = append(res, instance)
	}

	for instance := range stage.Breath_marks {
		res = append(res, instance)
	}

	for instance := range stage.Caesuras {
		res = append(res, instance)
	}

	for instance := range stage.Cancels {
		res = append(res, instance)
	}

	for instance := range stage.Clefs {
		res = append(res, instance)
	}

	for instance := range stage.Codas {
		res = append(res, instance)
	}

	for instance := range stage.Credits {
		res = append(res, instance)
	}

	for instance := range stage.Dashess {
		res = append(res, instance)
	}

	for instance := range stage.Defaultss {
		res = append(res, instance)
	}

	for instance := range stage.Degrees {
		res = append(res, instance)
	}

	for instance := range stage.Degree_alters {
		res = append(res, instance)
	}

	for instance := range stage.Degree_types {
		res = append(res, instance)
	}

	for instance := range stage.Degree_values {
		res = append(res, instance)
	}

	for instance := range stage.Directions {
		res = append(res, instance)
	}

	for instance := range stage.Direction_types {
		res = append(res, instance)
	}

	for instance := range stage.Distances {
		res = append(res, instance)
	}

	for instance := range stage.Doubles {
		res = append(res, instance)
	}

	for instance := range stage.Dynamicss {
		res = append(res, instance)
	}

	for instance := range stage.Effects {
		res = append(res, instance)
	}

	for instance := range stage.Elisions {
		res = append(res, instance)
	}

	for instance := range stage.Emptys {
		res = append(res, instance)
	}

	for instance := range stage.Empty_fonts {
		res = append(res, instance)
	}

	for instance := range stage.Empty_lines {
		res = append(res, instance)
	}

	for instance := range stage.Empty_placements {
		res = append(res, instance)
	}

	for instance := range stage.Empty_placement_smufls {
		res = append(res, instance)
	}

	for instance := range stage.Empty_print_object_style_aligns {
		res = append(res, instance)
	}

	for instance := range stage.Empty_print_styles {
		res = append(res, instance)
	}

	for instance := range stage.Empty_print_style_aligns {
		res = append(res, instance)
	}

	for instance := range stage.Empty_print_style_align_ids {
		res = append(res, instance)
	}

	for instance := range stage.Empty_trill_sounds {
		res = append(res, instance)
	}

	for instance := range stage.Encodings {
		res = append(res, instance)
	}

	for instance := range stage.Endings {
		res = append(res, instance)
	}

	for instance := range stage.Extends {
		res = append(res, instance)
	}

	for instance := range stage.Features {
		res = append(res, instance)
	}

	for instance := range stage.Fermatas {
		res = append(res, instance)
	}

	for instance := range stage.Figures {
		res = append(res, instance)
	}

	for instance := range stage.Figured_basss {
		res = append(res, instance)
	}

	for instance := range stage.Fingerings {
		res = append(res, instance)
	}

	for instance := range stage.First_frets {
		res = append(res, instance)
	}

	for instance := range stage.For_parts {
		res = append(res, instance)
	}

	for instance := range stage.Formatted_symbols {
		res = append(res, instance)
	}

	for instance := range stage.Formatted_symbol_ids {
		res = append(res, instance)
	}

	for instance := range stage.Formatted_texts {
		res = append(res, instance)
	}

	for instance := range stage.Formatted_text_ids {
		res = append(res, instance)
	}

	for instance := range stage.Forwards {
		res = append(res, instance)
	}

	for instance := range stage.Frames {
		res = append(res, instance)
	}

	for instance := range stage.Frame_notes {
		res = append(res, instance)
	}

	for instance := range stage.Frets {
		res = append(res, instance)
	}

	for instance := range stage.Glasss {
		res = append(res, instance)
	}

	for instance := range stage.Glissandos {
		res = append(res, instance)
	}

	for instance := range stage.Glyphs {
		res = append(res, instance)
	}

	for instance := range stage.Graces {
		res = append(res, instance)
	}

	for instance := range stage.Group_barlines {
		res = append(res, instance)
	}

	for instance := range stage.Group_names {
		res = append(res, instance)
	}

	for instance := range stage.Group_symbols {
		res = append(res, instance)
	}

	for instance := range stage.Groupings {
		res = append(res, instance)
	}

	for instance := range stage.Hammer_on_pull_offs {
		res = append(res, instance)
	}

	for instance := range stage.Handbells {
		res = append(res, instance)
	}

	for instance := range stage.Harmon_closeds {
		res = append(res, instance)
	}

	for instance := range stage.Harmon_mutes {
		res = append(res, instance)
	}

	for instance := range stage.Harmonics {
		res = append(res, instance)
	}

	for instance := range stage.Harmonys {
		res = append(res, instance)
	}

	for instance := range stage.Harmony_alters {
		res = append(res, instance)
	}

	for instance := range stage.Harp_pedalss {
		res = append(res, instance)
	}

	for instance := range stage.Heel_toes {
		res = append(res, instance)
	}

	for instance := range stage.Holes {
		res = append(res, instance)
	}

	for instance := range stage.Hole_closeds {
		res = append(res, instance)
	}

	for instance := range stage.Horizontal_turns {
		res = append(res, instance)
	}

	for instance := range stage.Identifications {
		res = append(res, instance)
	}

	for instance := range stage.Images {
		res = append(res, instance)
	}

	for instance := range stage.Instruments {
		res = append(res, instance)
	}

	for instance := range stage.Instrument_changes {
		res = append(res, instance)
	}

	for instance := range stage.Instrument_links {
		res = append(res, instance)
	}

	for instance := range stage.Interchangeables {
		res = append(res, instance)
	}

	for instance := range stage.Inversions {
		res = append(res, instance)
	}

	for instance := range stage.Keys {
		res = append(res, instance)
	}

	for instance := range stage.Key_accidentals {
		res = append(res, instance)
	}

	for instance := range stage.Key_octaves {
		res = append(res, instance)
	}

	for instance := range stage.Kinds {
		res = append(res, instance)
	}

	for instance := range stage.Levels {
		res = append(res, instance)
	}

	for instance := range stage.Line_details {
		res = append(res, instance)
	}

	for instance := range stage.Line_widths {
		res = append(res, instance)
	}

	for instance := range stage.Links {
		res = append(res, instance)
	}

	for instance := range stage.Listens {
		res = append(res, instance)
	}

	for instance := range stage.Listenings {
		res = append(res, instance)
	}

	for instance := range stage.Lyrics {
		res = append(res, instance)
	}

	for instance := range stage.Lyric_fonts {
		res = append(res, instance)
	}

	for instance := range stage.Lyric_languages {
		res = append(res, instance)
	}

	for instance := range stage.Measure_layouts {
		res = append(res, instance)
	}

	for instance := range stage.Measure_numberings {
		res = append(res, instance)
	}

	for instance := range stage.Measure_repeats {
		res = append(res, instance)
	}

	for instance := range stage.Measure_styles {
		res = append(res, instance)
	}

	for instance := range stage.Membranes {
		res = append(res, instance)
	}

	for instance := range stage.Metals {
		res = append(res, instance)
	}

	for instance := range stage.Metronomes {
		res = append(res, instance)
	}

	for instance := range stage.Metronome_beams {
		res = append(res, instance)
	}

	for instance := range stage.Metronome_notes {
		res = append(res, instance)
	}

	for instance := range stage.Metronome_tieds {
		res = append(res, instance)
	}

	for instance := range stage.Metronome_tuplets {
		res = append(res, instance)
	}

	for instance := range stage.Midi_devices {
		res = append(res, instance)
	}

	for instance := range stage.Midi_instruments {
		res = append(res, instance)
	}

	for instance := range stage.Miscellaneouss {
		res = append(res, instance)
	}

	for instance := range stage.Miscellaneous_fields {
		res = append(res, instance)
	}

	for instance := range stage.Mordents {
		res = append(res, instance)
	}

	for instance := range stage.Multiple_rests {
		res = append(res, instance)
	}

	for instance := range stage.Name_displays {
		res = append(res, instance)
	}

	for instance := range stage.Non_arpeggiates {
		res = append(res, instance)
	}

	for instance := range stage.Notationss {
		res = append(res, instance)
	}

	for instance := range stage.Notes {
		res = append(res, instance)
	}

	for instance := range stage.Note_sizes {
		res = append(res, instance)
	}

	for instance := range stage.Note_types {
		res = append(res, instance)
	}

	for instance := range stage.Noteheads {
		res = append(res, instance)
	}

	for instance := range stage.Notehead_texts {
		res = append(res, instance)
	}

	for instance := range stage.Numerals {
		res = append(res, instance)
	}

	for instance := range stage.Numeral_keys {
		res = append(res, instance)
	}

	for instance := range stage.Numeral_roots {
		res = append(res, instance)
	}

	for instance := range stage.Octave_shifts {
		res = append(res, instance)
	}

	for instance := range stage.Offsets {
		res = append(res, instance)
	}

	for instance := range stage.Opuss {
		res = append(res, instance)
	}

	for instance := range stage.Ornamentss {
		res = append(res, instance)
	}

	for instance := range stage.Other_appearances {
		res = append(res, instance)
	}

	for instance := range stage.Other_directions {
		res = append(res, instance)
	}

	for instance := range stage.Other_listenings {
		res = append(res, instance)
	}

	for instance := range stage.Other_notations {
		res = append(res, instance)
	}

	for instance := range stage.Other_placement_texts {
		res = append(res, instance)
	}

	for instance := range stage.Other_plays {
		res = append(res, instance)
	}

	for instance := range stage.Other_texts {
		res = append(res, instance)
	}

	for instance := range stage.Page_layouts {
		res = append(res, instance)
	}

	for instance := range stage.Page_marginss {
		res = append(res, instance)
	}

	for instance := range stage.Part_clefs {
		res = append(res, instance)
	}

	for instance := range stage.Part_groups {
		res = append(res, instance)
	}

	for instance := range stage.Part_links {
		res = append(res, instance)
	}

	for instance := range stage.Part_lists {
		res = append(res, instance)
	}

	for instance := range stage.Part_names {
		res = append(res, instance)
	}

	for instance := range stage.Part_symbols {
		res = append(res, instance)
	}

	for instance := range stage.Part_transposes {
		res = append(res, instance)
	}

	for instance := range stage.Pedals {
		res = append(res, instance)
	}

	for instance := range stage.Pedal_tunings {
		res = append(res, instance)
	}

	for instance := range stage.Per_minutes {
		res = append(res, instance)
	}

	for instance := range stage.Percussions {
		res = append(res, instance)
	}

	for instance := range stage.Pitchs {
		res = append(res, instance)
	}

	for instance := range stage.Pitcheds {
		res = append(res, instance)
	}

	for instance := range stage.Placement_texts {
		res = append(res, instance)
	}

	for instance := range stage.Plays {
		res = append(res, instance)
	}

	for instance := range stage.Players {
		res = append(res, instance)
	}

	for instance := range stage.Principal_voices {
		res = append(res, instance)
	}

	for instance := range stage.Prints {
		res = append(res, instance)
	}

	for instance := range stage.Releases {
		res = append(res, instance)
	}

	for instance := range stage.Repeats {
		res = append(res, instance)
	}

	for instance := range stage.Rests {
		res = append(res, instance)
	}

	for instance := range stage.Roots {
		res = append(res, instance)
	}

	for instance := range stage.Root_steps {
		res = append(res, instance)
	}

	for instance := range stage.Scalings {
		res = append(res, instance)
	}

	for instance := range stage.Scordaturas {
		res = append(res, instance)
	}

	for instance := range stage.Score_instruments {
		res = append(res, instance)
	}

	for instance := range stage.Score_parts {
		res = append(res, instance)
	}

	for instance := range stage.Score_partwises {
		res = append(res, instance)
	}

	for instance := range stage.Score_timewises {
		res = append(res, instance)
	}

	for instance := range stage.Segnos {
		res = append(res, instance)
	}

	for instance := range stage.Slashs {
		res = append(res, instance)
	}

	for instance := range stage.Slides {
		res = append(res, instance)
	}

	for instance := range stage.Slurs {
		res = append(res, instance)
	}

	for instance := range stage.Sounds {
		res = append(res, instance)
	}

	for instance := range stage.Staff_detailss {
		res = append(res, instance)
	}

	for instance := range stage.Staff_divides {
		res = append(res, instance)
	}

	for instance := range stage.Staff_layouts {
		res = append(res, instance)
	}

	for instance := range stage.Staff_sizes {
		res = append(res, instance)
	}

	for instance := range stage.Staff_tunings {
		res = append(res, instance)
	}

	for instance := range stage.Stems {
		res = append(res, instance)
	}

	for instance := range stage.Sticks {
		res = append(res, instance)
	}

	for instance := range stage.String_mutes {
		res = append(res, instance)
	}

	for instance := range stage.String_types {
		res = append(res, instance)
	}

	for instance := range stage.Strong_accents {
		res = append(res, instance)
	}

	for instance := range stage.Style_texts {
		res = append(res, instance)
	}

	for instance := range stage.Supportss {
		res = append(res, instance)
	}

	for instance := range stage.Swings {
		res = append(res, instance)
	}

	for instance := range stage.Syncs {
		res = append(res, instance)
	}

	for instance := range stage.System_dividerss {
		res = append(res, instance)
	}

	for instance := range stage.System_layouts {
		res = append(res, instance)
	}

	for instance := range stage.System_marginss {
		res = append(res, instance)
	}

	for instance := range stage.Taps {
		res = append(res, instance)
	}

	for instance := range stage.Technicals {
		res = append(res, instance)
	}

	for instance := range stage.Text_element_datas {
		res = append(res, instance)
	}

	for instance := range stage.Ties {
		res = append(res, instance)
	}

	for instance := range stage.Tieds {
		res = append(res, instance)
	}

	for instance := range stage.Times {
		res = append(res, instance)
	}

	for instance := range stage.Time_modifications {
		res = append(res, instance)
	}

	for instance := range stage.Timpanis {
		res = append(res, instance)
	}

	for instance := range stage.Transposes {
		res = append(res, instance)
	}

	for instance := range stage.Tremolos {
		res = append(res, instance)
	}

	for instance := range stage.Tuplets {
		res = append(res, instance)
	}

	for instance := range stage.Tuplet_dots {
		res = append(res, instance)
	}

	for instance := range stage.Tuplet_numbers {
		res = append(res, instance)
	}

	for instance := range stage.Tuplet_portions {
		res = append(res, instance)
	}

	for instance := range stage.Tuplet_types {
		res = append(res, instance)
	}

	for instance := range stage.Typed_texts {
		res = append(res, instance)
	}

	for instance := range stage.Unpitcheds {
		res = append(res, instance)
	}

	for instance := range stage.Virtual_instruments {
		res = append(res, instance)
	}

	for instance := range stage.Waits {
		res = append(res, instance)
	}

	for instance := range stage.Wavy_lines {
		res = append(res, instance)
	}

	for instance := range stage.Wedges {
		res = append(res, instance)
	}

	for instance := range stage.Woods {
		res = append(res, instance)
	}

	for instance := range stage.Works {
		res = append(res, instance)
	}

	return
}

// insertion point per named struct
func (a_directive *A_directive) GongCopy() GongstructIF {
	newInstance := new(A_directive)
	a_directive.CopyBasicFields(newInstance)
	return newInstance
}

func (a_measure *A_measure) GongCopy() GongstructIF {
	newInstance := new(A_measure)
	a_measure.CopyBasicFields(newInstance)
	return newInstance
}

func (a_measure_1 *A_measure_1) GongCopy() GongstructIF {
	newInstance := new(A_measure_1)
	a_measure_1.CopyBasicFields(newInstance)
	return newInstance
}

func (a_part *A_part) GongCopy() GongstructIF {
	newInstance := new(A_part)
	a_part.CopyBasicFields(newInstance)
	return newInstance
}

func (a_part_1 *A_part_1) GongCopy() GongstructIF {
	newInstance := new(A_part_1)
	a_part_1.CopyBasicFields(newInstance)
	return newInstance
}

func (accidental *Accidental) GongCopy() GongstructIF {
	newInstance := new(Accidental)
	accidental.CopyBasicFields(newInstance)
	return newInstance
}

func (accidental_mark *Accidental_mark) GongCopy() GongstructIF {
	newInstance := new(Accidental_mark)
	accidental_mark.CopyBasicFields(newInstance)
	return newInstance
}

func (accidental_text *Accidental_text) GongCopy() GongstructIF {
	newInstance := new(Accidental_text)
	accidental_text.CopyBasicFields(newInstance)
	return newInstance
}

func (accord *Accord) GongCopy() GongstructIF {
	newInstance := new(Accord)
	accord.CopyBasicFields(newInstance)
	return newInstance
}

func (accordion_registration *Accordion_registration) GongCopy() GongstructIF {
	newInstance := new(Accordion_registration)
	accordion_registration.CopyBasicFields(newInstance)
	return newInstance
}

func (appearance *Appearance) GongCopy() GongstructIF {
	newInstance := new(Appearance)
	appearance.CopyBasicFields(newInstance)
	return newInstance
}

func (arpeggiate *Arpeggiate) GongCopy() GongstructIF {
	newInstance := new(Arpeggiate)
	arpeggiate.CopyBasicFields(newInstance)
	return newInstance
}

func (arrow *Arrow) GongCopy() GongstructIF {
	newInstance := new(Arrow)
	arrow.CopyBasicFields(newInstance)
	return newInstance
}

func (articulations *Articulations) GongCopy() GongstructIF {
	newInstance := new(Articulations)
	articulations.CopyBasicFields(newInstance)
	return newInstance
}

func (assess *Assess) GongCopy() GongstructIF {
	newInstance := new(Assess)
	assess.CopyBasicFields(newInstance)
	return newInstance
}

func (attributes *Attributes) GongCopy() GongstructIF {
	newInstance := new(Attributes)
	attributes.CopyBasicFields(newInstance)
	return newInstance
}

func (backup *Backup) GongCopy() GongstructIF {
	newInstance := new(Backup)
	backup.CopyBasicFields(newInstance)
	return newInstance
}

func (bar_style_color *Bar_style_color) GongCopy() GongstructIF {
	newInstance := new(Bar_style_color)
	bar_style_color.CopyBasicFields(newInstance)
	return newInstance
}

func (barline *Barline) GongCopy() GongstructIF {
	newInstance := new(Barline)
	barline.CopyBasicFields(newInstance)
	return newInstance
}

func (barre *Barre) GongCopy() GongstructIF {
	newInstance := new(Barre)
	barre.CopyBasicFields(newInstance)
	return newInstance
}

func (bass *Bass) GongCopy() GongstructIF {
	newInstance := new(Bass)
	bass.CopyBasicFields(newInstance)
	return newInstance
}

func (bass_step *Bass_step) GongCopy() GongstructIF {
	newInstance := new(Bass_step)
	bass_step.CopyBasicFields(newInstance)
	return newInstance
}

func (beam *Beam) GongCopy() GongstructIF {
	newInstance := new(Beam)
	beam.CopyBasicFields(newInstance)
	return newInstance
}

func (beat_repeat *Beat_repeat) GongCopy() GongstructIF {
	newInstance := new(Beat_repeat)
	beat_repeat.CopyBasicFields(newInstance)
	return newInstance
}

func (beat_unit_tied *Beat_unit_tied) GongCopy() GongstructIF {
	newInstance := new(Beat_unit_tied)
	beat_unit_tied.CopyBasicFields(newInstance)
	return newInstance
}

func (beater *Beater) GongCopy() GongstructIF {
	newInstance := new(Beater)
	beater.CopyBasicFields(newInstance)
	return newInstance
}

func (bend *Bend) GongCopy() GongstructIF {
	newInstance := new(Bend)
	bend.CopyBasicFields(newInstance)
	return newInstance
}

func (bookmark *Bookmark) GongCopy() GongstructIF {
	newInstance := new(Bookmark)
	bookmark.CopyBasicFields(newInstance)
	return newInstance
}

func (bracket *Bracket) GongCopy() GongstructIF {
	newInstance := new(Bracket)
	bracket.CopyBasicFields(newInstance)
	return newInstance
}

func (breath_mark *Breath_mark) GongCopy() GongstructIF {
	newInstance := new(Breath_mark)
	breath_mark.CopyBasicFields(newInstance)
	return newInstance
}

func (caesura *Caesura) GongCopy() GongstructIF {
	newInstance := new(Caesura)
	caesura.CopyBasicFields(newInstance)
	return newInstance
}

func (cancel *Cancel) GongCopy() GongstructIF {
	newInstance := new(Cancel)
	cancel.CopyBasicFields(newInstance)
	return newInstance
}

func (clef *Clef) GongCopy() GongstructIF {
	newInstance := new(Clef)
	clef.CopyBasicFields(newInstance)
	return newInstance
}

func (coda *Coda) GongCopy() GongstructIF {
	newInstance := new(Coda)
	coda.CopyBasicFields(newInstance)
	return newInstance
}

func (credit *Credit) GongCopy() GongstructIF {
	newInstance := new(Credit)
	credit.CopyBasicFields(newInstance)
	return newInstance
}

func (dashes *Dashes) GongCopy() GongstructIF {
	newInstance := new(Dashes)
	dashes.CopyBasicFields(newInstance)
	return newInstance
}

func (defaults *Defaults) GongCopy() GongstructIF {
	newInstance := new(Defaults)
	defaults.CopyBasicFields(newInstance)
	return newInstance
}

func (degree *Degree) GongCopy() GongstructIF {
	newInstance := new(Degree)
	degree.CopyBasicFields(newInstance)
	return newInstance
}

func (degree_alter *Degree_alter) GongCopy() GongstructIF {
	newInstance := new(Degree_alter)
	degree_alter.CopyBasicFields(newInstance)
	return newInstance
}

func (degree_type *Degree_type) GongCopy() GongstructIF {
	newInstance := new(Degree_type)
	degree_type.CopyBasicFields(newInstance)
	return newInstance
}

func (degree_value *Degree_value) GongCopy() GongstructIF {
	newInstance := new(Degree_value)
	degree_value.CopyBasicFields(newInstance)
	return newInstance
}

func (direction *Direction) GongCopy() GongstructIF {
	newInstance := new(Direction)
	direction.CopyBasicFields(newInstance)
	return newInstance
}

func (direction_type *Direction_type) GongCopy() GongstructIF {
	newInstance := new(Direction_type)
	direction_type.CopyBasicFields(newInstance)
	return newInstance
}

func (distance *Distance) GongCopy() GongstructIF {
	newInstance := new(Distance)
	distance.CopyBasicFields(newInstance)
	return newInstance
}

func (double *Double) GongCopy() GongstructIF {
	newInstance := new(Double)
	double.CopyBasicFields(newInstance)
	return newInstance
}

func (dynamics *Dynamics) GongCopy() GongstructIF {
	newInstance := new(Dynamics)
	dynamics.CopyBasicFields(newInstance)
	return newInstance
}

func (effect *Effect) GongCopy() GongstructIF {
	newInstance := new(Effect)
	effect.CopyBasicFields(newInstance)
	return newInstance
}

func (elision *Elision) GongCopy() GongstructIF {
	newInstance := new(Elision)
	elision.CopyBasicFields(newInstance)
	return newInstance
}

func (empty *Empty) GongCopy() GongstructIF {
	newInstance := new(Empty)
	empty.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_font *Empty_font) GongCopy() GongstructIF {
	newInstance := new(Empty_font)
	empty_font.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_line *Empty_line) GongCopy() GongstructIF {
	newInstance := new(Empty_line)
	empty_line.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_placement *Empty_placement) GongCopy() GongstructIF {
	newInstance := new(Empty_placement)
	empty_placement.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_placement_smufl *Empty_placement_smufl) GongCopy() GongstructIF {
	newInstance := new(Empty_placement_smufl)
	empty_placement_smufl.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongCopy() GongstructIF {
	newInstance := new(Empty_print_object_style_align)
	empty_print_object_style_align.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_print_style *Empty_print_style) GongCopy() GongstructIF {
	newInstance := new(Empty_print_style)
	empty_print_style.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_print_style_align *Empty_print_style_align) GongCopy() GongstructIF {
	newInstance := new(Empty_print_style_align)
	empty_print_style_align.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongCopy() GongstructIF {
	newInstance := new(Empty_print_style_align_id)
	empty_print_style_align_id.CopyBasicFields(newInstance)
	return newInstance
}

func (empty_trill_sound *Empty_trill_sound) GongCopy() GongstructIF {
	newInstance := new(Empty_trill_sound)
	empty_trill_sound.CopyBasicFields(newInstance)
	return newInstance
}

func (encoding *Encoding) GongCopy() GongstructIF {
	newInstance := new(Encoding)
	encoding.CopyBasicFields(newInstance)
	return newInstance
}

func (ending *Ending) GongCopy() GongstructIF {
	newInstance := new(Ending)
	ending.CopyBasicFields(newInstance)
	return newInstance
}

func (extend *Extend) GongCopy() GongstructIF {
	newInstance := new(Extend)
	extend.CopyBasicFields(newInstance)
	return newInstance
}

func (feature *Feature) GongCopy() GongstructIF {
	newInstance := new(Feature)
	feature.CopyBasicFields(newInstance)
	return newInstance
}

func (fermata *Fermata) GongCopy() GongstructIF {
	newInstance := new(Fermata)
	fermata.CopyBasicFields(newInstance)
	return newInstance
}

func (figure *Figure) GongCopy() GongstructIF {
	newInstance := new(Figure)
	figure.CopyBasicFields(newInstance)
	return newInstance
}

func (figured_bass *Figured_bass) GongCopy() GongstructIF {
	newInstance := new(Figured_bass)
	figured_bass.CopyBasicFields(newInstance)
	return newInstance
}

func (fingering *Fingering) GongCopy() GongstructIF {
	newInstance := new(Fingering)
	fingering.CopyBasicFields(newInstance)
	return newInstance
}

func (first_fret *First_fret) GongCopy() GongstructIF {
	newInstance := new(First_fret)
	first_fret.CopyBasicFields(newInstance)
	return newInstance
}

func (for_part *For_part) GongCopy() GongstructIF {
	newInstance := new(For_part)
	for_part.CopyBasicFields(newInstance)
	return newInstance
}

func (formatted_symbol *Formatted_symbol) GongCopy() GongstructIF {
	newInstance := new(Formatted_symbol)
	formatted_symbol.CopyBasicFields(newInstance)
	return newInstance
}

func (formatted_symbol_id *Formatted_symbol_id) GongCopy() GongstructIF {
	newInstance := new(Formatted_symbol_id)
	formatted_symbol_id.CopyBasicFields(newInstance)
	return newInstance
}

func (formatted_text *Formatted_text) GongCopy() GongstructIF {
	newInstance := new(Formatted_text)
	formatted_text.CopyBasicFields(newInstance)
	return newInstance
}

func (formatted_text_id *Formatted_text_id) GongCopy() GongstructIF {
	newInstance := new(Formatted_text_id)
	formatted_text_id.CopyBasicFields(newInstance)
	return newInstance
}

func (forward *Forward) GongCopy() GongstructIF {
	newInstance := new(Forward)
	forward.CopyBasicFields(newInstance)
	return newInstance
}

func (frame *Frame) GongCopy() GongstructIF {
	newInstance := new(Frame)
	frame.CopyBasicFields(newInstance)
	return newInstance
}

func (frame_note *Frame_note) GongCopy() GongstructIF {
	newInstance := new(Frame_note)
	frame_note.CopyBasicFields(newInstance)
	return newInstance
}

func (fret *Fret) GongCopy() GongstructIF {
	newInstance := new(Fret)
	fret.CopyBasicFields(newInstance)
	return newInstance
}

func (glass *Glass) GongCopy() GongstructIF {
	newInstance := new(Glass)
	glass.CopyBasicFields(newInstance)
	return newInstance
}

func (glissando *Glissando) GongCopy() GongstructIF {
	newInstance := new(Glissando)
	glissando.CopyBasicFields(newInstance)
	return newInstance
}

func (glyph *Glyph) GongCopy() GongstructIF {
	newInstance := new(Glyph)
	glyph.CopyBasicFields(newInstance)
	return newInstance
}

func (grace *Grace) GongCopy() GongstructIF {
	newInstance := new(Grace)
	grace.CopyBasicFields(newInstance)
	return newInstance
}

func (group_barline *Group_barline) GongCopy() GongstructIF {
	newInstance := new(Group_barline)
	group_barline.CopyBasicFields(newInstance)
	return newInstance
}

func (group_name *Group_name) GongCopy() GongstructIF {
	newInstance := new(Group_name)
	group_name.CopyBasicFields(newInstance)
	return newInstance
}

func (group_symbol *Group_symbol) GongCopy() GongstructIF {
	newInstance := new(Group_symbol)
	group_symbol.CopyBasicFields(newInstance)
	return newInstance
}

func (grouping *Grouping) GongCopy() GongstructIF {
	newInstance := new(Grouping)
	grouping.CopyBasicFields(newInstance)
	return newInstance
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongCopy() GongstructIF {
	newInstance := new(Hammer_on_pull_off)
	hammer_on_pull_off.CopyBasicFields(newInstance)
	return newInstance
}

func (handbell *Handbell) GongCopy() GongstructIF {
	newInstance := new(Handbell)
	handbell.CopyBasicFields(newInstance)
	return newInstance
}

func (harmon_closed *Harmon_closed) GongCopy() GongstructIF {
	newInstance := new(Harmon_closed)
	harmon_closed.CopyBasicFields(newInstance)
	return newInstance
}

func (harmon_mute *Harmon_mute) GongCopy() GongstructIF {
	newInstance := new(Harmon_mute)
	harmon_mute.CopyBasicFields(newInstance)
	return newInstance
}

func (harmonic *Harmonic) GongCopy() GongstructIF {
	newInstance := new(Harmonic)
	harmonic.CopyBasicFields(newInstance)
	return newInstance
}

func (harmony *Harmony) GongCopy() GongstructIF {
	newInstance := new(Harmony)
	harmony.CopyBasicFields(newInstance)
	return newInstance
}

func (harmony_alter *Harmony_alter) GongCopy() GongstructIF {
	newInstance := new(Harmony_alter)
	harmony_alter.CopyBasicFields(newInstance)
	return newInstance
}

func (harp_pedals *Harp_pedals) GongCopy() GongstructIF {
	newInstance := new(Harp_pedals)
	harp_pedals.CopyBasicFields(newInstance)
	return newInstance
}

func (heel_toe *Heel_toe) GongCopy() GongstructIF {
	newInstance := new(Heel_toe)
	heel_toe.CopyBasicFields(newInstance)
	return newInstance
}

func (hole *Hole) GongCopy() GongstructIF {
	newInstance := new(Hole)
	hole.CopyBasicFields(newInstance)
	return newInstance
}

func (hole_closed *Hole_closed) GongCopy() GongstructIF {
	newInstance := new(Hole_closed)
	hole_closed.CopyBasicFields(newInstance)
	return newInstance
}

func (horizontal_turn *Horizontal_turn) GongCopy() GongstructIF {
	newInstance := new(Horizontal_turn)
	horizontal_turn.CopyBasicFields(newInstance)
	return newInstance
}

func (identification *Identification) GongCopy() GongstructIF {
	newInstance := new(Identification)
	identification.CopyBasicFields(newInstance)
	return newInstance
}

func (image *Image) GongCopy() GongstructIF {
	newInstance := new(Image)
	image.CopyBasicFields(newInstance)
	return newInstance
}

func (instrument *Instrument) GongCopy() GongstructIF {
	newInstance := new(Instrument)
	instrument.CopyBasicFields(newInstance)
	return newInstance
}

func (instrument_change *Instrument_change) GongCopy() GongstructIF {
	newInstance := new(Instrument_change)
	instrument_change.CopyBasicFields(newInstance)
	return newInstance
}

func (instrument_link *Instrument_link) GongCopy() GongstructIF {
	newInstance := new(Instrument_link)
	instrument_link.CopyBasicFields(newInstance)
	return newInstance
}

func (interchangeable *Interchangeable) GongCopy() GongstructIF {
	newInstance := new(Interchangeable)
	interchangeable.CopyBasicFields(newInstance)
	return newInstance
}

func (inversion *Inversion) GongCopy() GongstructIF {
	newInstance := new(Inversion)
	inversion.CopyBasicFields(newInstance)
	return newInstance
}

func (key *Key) GongCopy() GongstructIF {
	newInstance := new(Key)
	key.CopyBasicFields(newInstance)
	return newInstance
}

func (key_accidental *Key_accidental) GongCopy() GongstructIF {
	newInstance := new(Key_accidental)
	key_accidental.CopyBasicFields(newInstance)
	return newInstance
}

func (key_octave *Key_octave) GongCopy() GongstructIF {
	newInstance := new(Key_octave)
	key_octave.CopyBasicFields(newInstance)
	return newInstance
}

func (kind *Kind) GongCopy() GongstructIF {
	newInstance := new(Kind)
	kind.CopyBasicFields(newInstance)
	return newInstance
}

func (level *Level) GongCopy() GongstructIF {
	newInstance := new(Level)
	level.CopyBasicFields(newInstance)
	return newInstance
}

func (line_detail *Line_detail) GongCopy() GongstructIF {
	newInstance := new(Line_detail)
	line_detail.CopyBasicFields(newInstance)
	return newInstance
}

func (line_width *Line_width) GongCopy() GongstructIF {
	newInstance := new(Line_width)
	line_width.CopyBasicFields(newInstance)
	return newInstance
}

func (link *Link) GongCopy() GongstructIF {
	newInstance := new(Link)
	link.CopyBasicFields(newInstance)
	return newInstance
}

func (listen *Listen) GongCopy() GongstructIF {
	newInstance := new(Listen)
	listen.CopyBasicFields(newInstance)
	return newInstance
}

func (listening *Listening) GongCopy() GongstructIF {
	newInstance := new(Listening)
	listening.CopyBasicFields(newInstance)
	return newInstance
}

func (lyric *Lyric) GongCopy() GongstructIF {
	newInstance := new(Lyric)
	lyric.CopyBasicFields(newInstance)
	return newInstance
}

func (lyric_font *Lyric_font) GongCopy() GongstructIF {
	newInstance := new(Lyric_font)
	lyric_font.CopyBasicFields(newInstance)
	return newInstance
}

func (lyric_language *Lyric_language) GongCopy() GongstructIF {
	newInstance := new(Lyric_language)
	lyric_language.CopyBasicFields(newInstance)
	return newInstance
}

func (measure_layout *Measure_layout) GongCopy() GongstructIF {
	newInstance := new(Measure_layout)
	measure_layout.CopyBasicFields(newInstance)
	return newInstance
}

func (measure_numbering *Measure_numbering) GongCopy() GongstructIF {
	newInstance := new(Measure_numbering)
	measure_numbering.CopyBasicFields(newInstance)
	return newInstance
}

func (measure_repeat *Measure_repeat) GongCopy() GongstructIF {
	newInstance := new(Measure_repeat)
	measure_repeat.CopyBasicFields(newInstance)
	return newInstance
}

func (measure_style *Measure_style) GongCopy() GongstructIF {
	newInstance := new(Measure_style)
	measure_style.CopyBasicFields(newInstance)
	return newInstance
}

func (membrane *Membrane) GongCopy() GongstructIF {
	newInstance := new(Membrane)
	membrane.CopyBasicFields(newInstance)
	return newInstance
}

func (metal *Metal) GongCopy() GongstructIF {
	newInstance := new(Metal)
	metal.CopyBasicFields(newInstance)
	return newInstance
}

func (metronome *Metronome) GongCopy() GongstructIF {
	newInstance := new(Metronome)
	metronome.CopyBasicFields(newInstance)
	return newInstance
}

func (metronome_beam *Metronome_beam) GongCopy() GongstructIF {
	newInstance := new(Metronome_beam)
	metronome_beam.CopyBasicFields(newInstance)
	return newInstance
}

func (metronome_note *Metronome_note) GongCopy() GongstructIF {
	newInstance := new(Metronome_note)
	metronome_note.CopyBasicFields(newInstance)
	return newInstance
}

func (metronome_tied *Metronome_tied) GongCopy() GongstructIF {
	newInstance := new(Metronome_tied)
	metronome_tied.CopyBasicFields(newInstance)
	return newInstance
}

func (metronome_tuplet *Metronome_tuplet) GongCopy() GongstructIF {
	newInstance := new(Metronome_tuplet)
	metronome_tuplet.CopyBasicFields(newInstance)
	return newInstance
}

func (midi_device *Midi_device) GongCopy() GongstructIF {
	newInstance := new(Midi_device)
	midi_device.CopyBasicFields(newInstance)
	return newInstance
}

func (midi_instrument *Midi_instrument) GongCopy() GongstructIF {
	newInstance := new(Midi_instrument)
	midi_instrument.CopyBasicFields(newInstance)
	return newInstance
}

func (miscellaneous *Miscellaneous) GongCopy() GongstructIF {
	newInstance := new(Miscellaneous)
	miscellaneous.CopyBasicFields(newInstance)
	return newInstance
}

func (miscellaneous_field *Miscellaneous_field) GongCopy() GongstructIF {
	newInstance := new(Miscellaneous_field)
	miscellaneous_field.CopyBasicFields(newInstance)
	return newInstance
}

func (mordent *Mordent) GongCopy() GongstructIF {
	newInstance := new(Mordent)
	mordent.CopyBasicFields(newInstance)
	return newInstance
}

func (multiple_rest *Multiple_rest) GongCopy() GongstructIF {
	newInstance := new(Multiple_rest)
	multiple_rest.CopyBasicFields(newInstance)
	return newInstance
}

func (name_display *Name_display) GongCopy() GongstructIF {
	newInstance := new(Name_display)
	name_display.CopyBasicFields(newInstance)
	return newInstance
}

func (non_arpeggiate *Non_arpeggiate) GongCopy() GongstructIF {
	newInstance := new(Non_arpeggiate)
	non_arpeggiate.CopyBasicFields(newInstance)
	return newInstance
}

func (notations *Notations) GongCopy() GongstructIF {
	newInstance := new(Notations)
	notations.CopyBasicFields(newInstance)
	return newInstance
}

func (note *Note) GongCopy() GongstructIF {
	newInstance := new(Note)
	note.CopyBasicFields(newInstance)
	return newInstance
}

func (note_size *Note_size) GongCopy() GongstructIF {
	newInstance := new(Note_size)
	note_size.CopyBasicFields(newInstance)
	return newInstance
}

func (note_type *Note_type) GongCopy() GongstructIF {
	newInstance := new(Note_type)
	note_type.CopyBasicFields(newInstance)
	return newInstance
}

func (notehead *Notehead) GongCopy() GongstructIF {
	newInstance := new(Notehead)
	notehead.CopyBasicFields(newInstance)
	return newInstance
}

func (notehead_text *Notehead_text) GongCopy() GongstructIF {
	newInstance := new(Notehead_text)
	notehead_text.CopyBasicFields(newInstance)
	return newInstance
}

func (numeral *Numeral) GongCopy() GongstructIF {
	newInstance := new(Numeral)
	numeral.CopyBasicFields(newInstance)
	return newInstance
}

func (numeral_key *Numeral_key) GongCopy() GongstructIF {
	newInstance := new(Numeral_key)
	numeral_key.CopyBasicFields(newInstance)
	return newInstance
}

func (numeral_root *Numeral_root) GongCopy() GongstructIF {
	newInstance := new(Numeral_root)
	numeral_root.CopyBasicFields(newInstance)
	return newInstance
}

func (octave_shift *Octave_shift) GongCopy() GongstructIF {
	newInstance := new(Octave_shift)
	octave_shift.CopyBasicFields(newInstance)
	return newInstance
}

func (offset *Offset) GongCopy() GongstructIF {
	newInstance := new(Offset)
	offset.CopyBasicFields(newInstance)
	return newInstance
}

func (opus *Opus) GongCopy() GongstructIF {
	newInstance := new(Opus)
	opus.CopyBasicFields(newInstance)
	return newInstance
}

func (ornaments *Ornaments) GongCopy() GongstructIF {
	newInstance := new(Ornaments)
	ornaments.CopyBasicFields(newInstance)
	return newInstance
}

func (other_appearance *Other_appearance) GongCopy() GongstructIF {
	newInstance := new(Other_appearance)
	other_appearance.CopyBasicFields(newInstance)
	return newInstance
}

func (other_direction *Other_direction) GongCopy() GongstructIF {
	newInstance := new(Other_direction)
	other_direction.CopyBasicFields(newInstance)
	return newInstance
}

func (other_listening *Other_listening) GongCopy() GongstructIF {
	newInstance := new(Other_listening)
	other_listening.CopyBasicFields(newInstance)
	return newInstance
}

func (other_notation *Other_notation) GongCopy() GongstructIF {
	newInstance := new(Other_notation)
	other_notation.CopyBasicFields(newInstance)
	return newInstance
}

func (other_placement_text *Other_placement_text) GongCopy() GongstructIF {
	newInstance := new(Other_placement_text)
	other_placement_text.CopyBasicFields(newInstance)
	return newInstance
}

func (other_play *Other_play) GongCopy() GongstructIF {
	newInstance := new(Other_play)
	other_play.CopyBasicFields(newInstance)
	return newInstance
}

func (other_text *Other_text) GongCopy() GongstructIF {
	newInstance := new(Other_text)
	other_text.CopyBasicFields(newInstance)
	return newInstance
}

func (page_layout *Page_layout) GongCopy() GongstructIF {
	newInstance := new(Page_layout)
	page_layout.CopyBasicFields(newInstance)
	return newInstance
}

func (page_margins *Page_margins) GongCopy() GongstructIF {
	newInstance := new(Page_margins)
	page_margins.CopyBasicFields(newInstance)
	return newInstance
}

func (part_clef *Part_clef) GongCopy() GongstructIF {
	newInstance := new(Part_clef)
	part_clef.CopyBasicFields(newInstance)
	return newInstance
}

func (part_group *Part_group) GongCopy() GongstructIF {
	newInstance := new(Part_group)
	part_group.CopyBasicFields(newInstance)
	return newInstance
}

func (part_link *Part_link) GongCopy() GongstructIF {
	newInstance := new(Part_link)
	part_link.CopyBasicFields(newInstance)
	return newInstance
}

func (part_list *Part_list) GongCopy() GongstructIF {
	newInstance := new(Part_list)
	part_list.CopyBasicFields(newInstance)
	return newInstance
}

func (part_name *Part_name) GongCopy() GongstructIF {
	newInstance := new(Part_name)
	part_name.CopyBasicFields(newInstance)
	return newInstance
}

func (part_symbol *Part_symbol) GongCopy() GongstructIF {
	newInstance := new(Part_symbol)
	part_symbol.CopyBasicFields(newInstance)
	return newInstance
}

func (part_transpose *Part_transpose) GongCopy() GongstructIF {
	newInstance := new(Part_transpose)
	part_transpose.CopyBasicFields(newInstance)
	return newInstance
}

func (pedal *Pedal) GongCopy() GongstructIF {
	newInstance := new(Pedal)
	pedal.CopyBasicFields(newInstance)
	return newInstance
}

func (pedal_tuning *Pedal_tuning) GongCopy() GongstructIF {
	newInstance := new(Pedal_tuning)
	pedal_tuning.CopyBasicFields(newInstance)
	return newInstance
}

func (per_minute *Per_minute) GongCopy() GongstructIF {
	newInstance := new(Per_minute)
	per_minute.CopyBasicFields(newInstance)
	return newInstance
}

func (percussion *Percussion) GongCopy() GongstructIF {
	newInstance := new(Percussion)
	percussion.CopyBasicFields(newInstance)
	return newInstance
}

func (pitch *Pitch) GongCopy() GongstructIF {
	newInstance := new(Pitch)
	pitch.CopyBasicFields(newInstance)
	return newInstance
}

func (pitched *Pitched) GongCopy() GongstructIF {
	newInstance := new(Pitched)
	pitched.CopyBasicFields(newInstance)
	return newInstance
}

func (placement_text *Placement_text) GongCopy() GongstructIF {
	newInstance := new(Placement_text)
	placement_text.CopyBasicFields(newInstance)
	return newInstance
}

func (play *Play) GongCopy() GongstructIF {
	newInstance := new(Play)
	play.CopyBasicFields(newInstance)
	return newInstance
}

func (player *Player) GongCopy() GongstructIF {
	newInstance := new(Player)
	player.CopyBasicFields(newInstance)
	return newInstance
}

func (principal_voice *Principal_voice) GongCopy() GongstructIF {
	newInstance := new(Principal_voice)
	principal_voice.CopyBasicFields(newInstance)
	return newInstance
}

func (print *Print) GongCopy() GongstructIF {
	newInstance := new(Print)
	print.CopyBasicFields(newInstance)
	return newInstance
}

func (release *Release) GongCopy() GongstructIF {
	newInstance := new(Release)
	release.CopyBasicFields(newInstance)
	return newInstance
}

func (repeat *Repeat) GongCopy() GongstructIF {
	newInstance := new(Repeat)
	repeat.CopyBasicFields(newInstance)
	return newInstance
}

func (rest *Rest) GongCopy() GongstructIF {
	newInstance := new(Rest)
	rest.CopyBasicFields(newInstance)
	return newInstance
}

func (root *Root) GongCopy() GongstructIF {
	newInstance := new(Root)
	root.CopyBasicFields(newInstance)
	return newInstance
}

func (root_step *Root_step) GongCopy() GongstructIF {
	newInstance := new(Root_step)
	root_step.CopyBasicFields(newInstance)
	return newInstance
}

func (scaling *Scaling) GongCopy() GongstructIF {
	newInstance := new(Scaling)
	scaling.CopyBasicFields(newInstance)
	return newInstance
}

func (scordatura *Scordatura) GongCopy() GongstructIF {
	newInstance := new(Scordatura)
	scordatura.CopyBasicFields(newInstance)
	return newInstance
}

func (score_instrument *Score_instrument) GongCopy() GongstructIF {
	newInstance := new(Score_instrument)
	score_instrument.CopyBasicFields(newInstance)
	return newInstance
}

func (score_part *Score_part) GongCopy() GongstructIF {
	newInstance := new(Score_part)
	score_part.CopyBasicFields(newInstance)
	return newInstance
}

func (score_partwise *Score_partwise) GongCopy() GongstructIF {
	newInstance := new(Score_partwise)
	score_partwise.CopyBasicFields(newInstance)
	return newInstance
}

func (score_timewise *Score_timewise) GongCopy() GongstructIF {
	newInstance := new(Score_timewise)
	score_timewise.CopyBasicFields(newInstance)
	return newInstance
}

func (segno *Segno) GongCopy() GongstructIF {
	newInstance := new(Segno)
	segno.CopyBasicFields(newInstance)
	return newInstance
}

func (slash *Slash) GongCopy() GongstructIF {
	newInstance := new(Slash)
	slash.CopyBasicFields(newInstance)
	return newInstance
}

func (slide *Slide) GongCopy() GongstructIF {
	newInstance := new(Slide)
	slide.CopyBasicFields(newInstance)
	return newInstance
}

func (slur *Slur) GongCopy() GongstructIF {
	newInstance := new(Slur)
	slur.CopyBasicFields(newInstance)
	return newInstance
}

func (sound *Sound) GongCopy() GongstructIF {
	newInstance := new(Sound)
	sound.CopyBasicFields(newInstance)
	return newInstance
}

func (staff_details *Staff_details) GongCopy() GongstructIF {
	newInstance := new(Staff_details)
	staff_details.CopyBasicFields(newInstance)
	return newInstance
}

func (staff_divide *Staff_divide) GongCopy() GongstructIF {
	newInstance := new(Staff_divide)
	staff_divide.CopyBasicFields(newInstance)
	return newInstance
}

func (staff_layout *Staff_layout) GongCopy() GongstructIF {
	newInstance := new(Staff_layout)
	staff_layout.CopyBasicFields(newInstance)
	return newInstance
}

func (staff_size *Staff_size) GongCopy() GongstructIF {
	newInstance := new(Staff_size)
	staff_size.CopyBasicFields(newInstance)
	return newInstance
}

func (staff_tuning *Staff_tuning) GongCopy() GongstructIF {
	newInstance := new(Staff_tuning)
	staff_tuning.CopyBasicFields(newInstance)
	return newInstance
}

func (stem *Stem) GongCopy() GongstructIF {
	newInstance := new(Stem)
	stem.CopyBasicFields(newInstance)
	return newInstance
}

func (stick *Stick) GongCopy() GongstructIF {
	newInstance := new(Stick)
	stick.CopyBasicFields(newInstance)
	return newInstance
}

func (string_mute *String_mute) GongCopy() GongstructIF {
	newInstance := new(String_mute)
	string_mute.CopyBasicFields(newInstance)
	return newInstance
}

func (string_type *String_type) GongCopy() GongstructIF {
	newInstance := new(String_type)
	string_type.CopyBasicFields(newInstance)
	return newInstance
}

func (strong_accent *Strong_accent) GongCopy() GongstructIF {
	newInstance := new(Strong_accent)
	strong_accent.CopyBasicFields(newInstance)
	return newInstance
}

func (style_text *Style_text) GongCopy() GongstructIF {
	newInstance := new(Style_text)
	style_text.CopyBasicFields(newInstance)
	return newInstance
}

func (supports *Supports) GongCopy() GongstructIF {
	newInstance := new(Supports)
	supports.CopyBasicFields(newInstance)
	return newInstance
}

func (swing *Swing) GongCopy() GongstructIF {
	newInstance := new(Swing)
	swing.CopyBasicFields(newInstance)
	return newInstance
}

func (sync *Sync) GongCopy() GongstructIF {
	newInstance := new(Sync)
	sync.CopyBasicFields(newInstance)
	return newInstance
}

func (system_dividers *System_dividers) GongCopy() GongstructIF {
	newInstance := new(System_dividers)
	system_dividers.CopyBasicFields(newInstance)
	return newInstance
}

func (system_layout *System_layout) GongCopy() GongstructIF {
	newInstance := new(System_layout)
	system_layout.CopyBasicFields(newInstance)
	return newInstance
}

func (system_margins *System_margins) GongCopy() GongstructIF {
	newInstance := new(System_margins)
	system_margins.CopyBasicFields(newInstance)
	return newInstance
}

func (tap *Tap) GongCopy() GongstructIF {
	newInstance := new(Tap)
	tap.CopyBasicFields(newInstance)
	return newInstance
}

func (technical *Technical) GongCopy() GongstructIF {
	newInstance := new(Technical)
	technical.CopyBasicFields(newInstance)
	return newInstance
}

func (text_element_data *Text_element_data) GongCopy() GongstructIF {
	newInstance := new(Text_element_data)
	text_element_data.CopyBasicFields(newInstance)
	return newInstance
}

func (tie *Tie) GongCopy() GongstructIF {
	newInstance := new(Tie)
	tie.CopyBasicFields(newInstance)
	return newInstance
}

func (tied *Tied) GongCopy() GongstructIF {
	newInstance := new(Tied)
	tied.CopyBasicFields(newInstance)
	return newInstance
}

func (time *Time) GongCopy() GongstructIF {
	newInstance := new(Time)
	time.CopyBasicFields(newInstance)
	return newInstance
}

func (time_modification *Time_modification) GongCopy() GongstructIF {
	newInstance := new(Time_modification)
	time_modification.CopyBasicFields(newInstance)
	return newInstance
}

func (timpani *Timpani) GongCopy() GongstructIF {
	newInstance := new(Timpani)
	timpani.CopyBasicFields(newInstance)
	return newInstance
}

func (transpose *Transpose) GongCopy() GongstructIF {
	newInstance := new(Transpose)
	transpose.CopyBasicFields(newInstance)
	return newInstance
}

func (tremolo *Tremolo) GongCopy() GongstructIF {
	newInstance := new(Tremolo)
	tremolo.CopyBasicFields(newInstance)
	return newInstance
}

func (tuplet *Tuplet) GongCopy() GongstructIF {
	newInstance := new(Tuplet)
	tuplet.CopyBasicFields(newInstance)
	return newInstance
}

func (tuplet_dot *Tuplet_dot) GongCopy() GongstructIF {
	newInstance := new(Tuplet_dot)
	tuplet_dot.CopyBasicFields(newInstance)
	return newInstance
}

func (tuplet_number *Tuplet_number) GongCopy() GongstructIF {
	newInstance := new(Tuplet_number)
	tuplet_number.CopyBasicFields(newInstance)
	return newInstance
}

func (tuplet_portion *Tuplet_portion) GongCopy() GongstructIF {
	newInstance := new(Tuplet_portion)
	tuplet_portion.CopyBasicFields(newInstance)
	return newInstance
}

func (tuplet_type *Tuplet_type) GongCopy() GongstructIF {
	newInstance := new(Tuplet_type)
	tuplet_type.CopyBasicFields(newInstance)
	return newInstance
}

func (typed_text *Typed_text) GongCopy() GongstructIF {
	newInstance := new(Typed_text)
	typed_text.CopyBasicFields(newInstance)
	return newInstance
}

func (unpitched *Unpitched) GongCopy() GongstructIF {
	newInstance := new(Unpitched)
	unpitched.CopyBasicFields(newInstance)
	return newInstance
}

func (virtual_instrument *Virtual_instrument) GongCopy() GongstructIF {
	newInstance := new(Virtual_instrument)
	virtual_instrument.CopyBasicFields(newInstance)
	return newInstance
}

func (wait *Wait) GongCopy() GongstructIF {
	newInstance := new(Wait)
	wait.CopyBasicFields(newInstance)
	return newInstance
}

func (wavy_line *Wavy_line) GongCopy() GongstructIF {
	newInstance := new(Wavy_line)
	wavy_line.CopyBasicFields(newInstance)
	return newInstance
}

func (wedge *Wedge) GongCopy() GongstructIF {
	newInstance := new(Wedge)
	wedge.CopyBasicFields(newInstance)
	return newInstance
}

func (wood *Wood) GongCopy() GongstructIF {
	newInstance := new(Wood)
	wood.CopyBasicFields(newInstance)
	return newInstance
}

func (work *Work) GongCopy() GongstructIF {
	newInstance := new(Work)
	work.CopyBasicFields(newInstance)
	return newInstance
}

// insertion point per named struct
func (a_directive *A_directive) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(a_directive), uint64(GetOrderPointerGongstruct(stage, a_directive)))
	return
}

func (a_measure *A_measure) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(a_measure), uint64(GetOrderPointerGongstruct(stage, a_measure)))
	return
}

func (a_measure_1 *A_measure_1) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(a_measure_1), uint64(GetOrderPointerGongstruct(stage, a_measure_1)))
	return
}

func (a_part *A_part) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(a_part), uint64(GetOrderPointerGongstruct(stage, a_part)))
	return
}

func (a_part_1 *A_part_1) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(a_part_1), uint64(GetOrderPointerGongstruct(stage, a_part_1)))
	return
}

func (accidental *Accidental) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(accidental), uint64(GetOrderPointerGongstruct(stage, accidental)))
	return
}

func (accidental_mark *Accidental_mark) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(accidental_mark), uint64(GetOrderPointerGongstruct(stage, accidental_mark)))
	return
}

func (accidental_text *Accidental_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(accidental_text), uint64(GetOrderPointerGongstruct(stage, accidental_text)))
	return
}

func (accord *Accord) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(accord), uint64(GetOrderPointerGongstruct(stage, accord)))
	return
}

func (accordion_registration *Accordion_registration) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(accordion_registration), uint64(GetOrderPointerGongstruct(stage, accordion_registration)))
	return
}

func (appearance *Appearance) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(appearance), uint64(GetOrderPointerGongstruct(stage, appearance)))
	return
}

func (arpeggiate *Arpeggiate) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(arpeggiate), uint64(GetOrderPointerGongstruct(stage, arpeggiate)))
	return
}

func (arrow *Arrow) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(arrow), uint64(GetOrderPointerGongstruct(stage, arrow)))
	return
}

func (articulations *Articulations) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(articulations), uint64(GetOrderPointerGongstruct(stage, articulations)))
	return
}

func (assess *Assess) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(assess), uint64(GetOrderPointerGongstruct(stage, assess)))
	return
}

func (attributes *Attributes) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(attributes), uint64(GetOrderPointerGongstruct(stage, attributes)))
	return
}

func (backup *Backup) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(backup), uint64(GetOrderPointerGongstruct(stage, backup)))
	return
}

func (bar_style_color *Bar_style_color) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bar_style_color), uint64(GetOrderPointerGongstruct(stage, bar_style_color)))
	return
}

func (barline *Barline) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(barline), uint64(GetOrderPointerGongstruct(stage, barline)))
	return
}

func (barre *Barre) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(barre), uint64(GetOrderPointerGongstruct(stage, barre)))
	return
}

func (bass *Bass) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bass), uint64(GetOrderPointerGongstruct(stage, bass)))
	return
}

func (bass_step *Bass_step) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bass_step), uint64(GetOrderPointerGongstruct(stage, bass_step)))
	return
}

func (beam *Beam) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(beam), uint64(GetOrderPointerGongstruct(stage, beam)))
	return
}

func (beat_repeat *Beat_repeat) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(beat_repeat), uint64(GetOrderPointerGongstruct(stage, beat_repeat)))
	return
}

func (beat_unit_tied *Beat_unit_tied) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(beat_unit_tied), uint64(GetOrderPointerGongstruct(stage, beat_unit_tied)))
	return
}

func (beater *Beater) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(beater), uint64(GetOrderPointerGongstruct(stage, beater)))
	return
}

func (bend *Bend) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bend), uint64(GetOrderPointerGongstruct(stage, bend)))
	return
}

func (bookmark *Bookmark) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bookmark), uint64(GetOrderPointerGongstruct(stage, bookmark)))
	return
}

func (bracket *Bracket) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(bracket), uint64(GetOrderPointerGongstruct(stage, bracket)))
	return
}

func (breath_mark *Breath_mark) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(breath_mark), uint64(GetOrderPointerGongstruct(stage, breath_mark)))
	return
}

func (caesura *Caesura) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(caesura), uint64(GetOrderPointerGongstruct(stage, caesura)))
	return
}

func (cancel *Cancel) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(cancel), uint64(GetOrderPointerGongstruct(stage, cancel)))
	return
}

func (clef *Clef) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(clef), uint64(GetOrderPointerGongstruct(stage, clef)))
	return
}

func (coda *Coda) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(coda), uint64(GetOrderPointerGongstruct(stage, coda)))
	return
}

func (credit *Credit) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(credit), uint64(GetOrderPointerGongstruct(stage, credit)))
	return
}

func (dashes *Dashes) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(dashes), uint64(GetOrderPointerGongstruct(stage, dashes)))
	return
}

func (defaults *Defaults) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(defaults), uint64(GetOrderPointerGongstruct(stage, defaults)))
	return
}

func (degree *Degree) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(degree), uint64(GetOrderPointerGongstruct(stage, degree)))
	return
}

func (degree_alter *Degree_alter) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(degree_alter), uint64(GetOrderPointerGongstruct(stage, degree_alter)))
	return
}

func (degree_type *Degree_type) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(degree_type), uint64(GetOrderPointerGongstruct(stage, degree_type)))
	return
}

func (degree_value *Degree_value) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(degree_value), uint64(GetOrderPointerGongstruct(stage, degree_value)))
	return
}

func (direction *Direction) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(direction), uint64(GetOrderPointerGongstruct(stage, direction)))
	return
}

func (direction_type *Direction_type) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(direction_type), uint64(GetOrderPointerGongstruct(stage, direction_type)))
	return
}

func (distance *Distance) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(distance), uint64(GetOrderPointerGongstruct(stage, distance)))
	return
}

func (double *Double) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(double), uint64(GetOrderPointerGongstruct(stage, double)))
	return
}

func (dynamics *Dynamics) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(dynamics), uint64(GetOrderPointerGongstruct(stage, dynamics)))
	return
}

func (effect *Effect) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(effect), uint64(GetOrderPointerGongstruct(stage, effect)))
	return
}

func (elision *Elision) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(elision), uint64(GetOrderPointerGongstruct(stage, elision)))
	return
}

func (empty *Empty) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty), uint64(GetOrderPointerGongstruct(stage, empty)))
	return
}

func (empty_font *Empty_font) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_font), uint64(GetOrderPointerGongstruct(stage, empty_font)))
	return
}

func (empty_line *Empty_line) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_line), uint64(GetOrderPointerGongstruct(stage, empty_line)))
	return
}

func (empty_placement *Empty_placement) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_placement), uint64(GetOrderPointerGongstruct(stage, empty_placement)))
	return
}

func (empty_placement_smufl *Empty_placement_smufl) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_placement_smufl), uint64(GetOrderPointerGongstruct(stage, empty_placement_smufl)))
	return
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_print_object_style_align), uint64(GetOrderPointerGongstruct(stage, empty_print_object_style_align)))
	return
}

func (empty_print_style *Empty_print_style) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_print_style), uint64(GetOrderPointerGongstruct(stage, empty_print_style)))
	return
}

func (empty_print_style_align *Empty_print_style_align) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_print_style_align), uint64(GetOrderPointerGongstruct(stage, empty_print_style_align)))
	return
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_print_style_align_id), uint64(GetOrderPointerGongstruct(stage, empty_print_style_align_id)))
	return
}

func (empty_trill_sound *Empty_trill_sound) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(empty_trill_sound), uint64(GetOrderPointerGongstruct(stage, empty_trill_sound)))
	return
}

func (encoding *Encoding) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(encoding), uint64(GetOrderPointerGongstruct(stage, encoding)))
	return
}

func (ending *Ending) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(ending), uint64(GetOrderPointerGongstruct(stage, ending)))
	return
}

func (extend *Extend) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(extend), uint64(GetOrderPointerGongstruct(stage, extend)))
	return
}

func (feature *Feature) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(feature), uint64(GetOrderPointerGongstruct(stage, feature)))
	return
}

func (fermata *Fermata) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(fermata), uint64(GetOrderPointerGongstruct(stage, fermata)))
	return
}

func (figure *Figure) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(figure), uint64(GetOrderPointerGongstruct(stage, figure)))
	return
}

func (figured_bass *Figured_bass) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(figured_bass), uint64(GetOrderPointerGongstruct(stage, figured_bass)))
	return
}

func (fingering *Fingering) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(fingering), uint64(GetOrderPointerGongstruct(stage, fingering)))
	return
}

func (first_fret *First_fret) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(first_fret), uint64(GetOrderPointerGongstruct(stage, first_fret)))
	return
}

func (for_part *For_part) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(for_part), uint64(GetOrderPointerGongstruct(stage, for_part)))
	return
}

func (formatted_symbol *Formatted_symbol) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(formatted_symbol), uint64(GetOrderPointerGongstruct(stage, formatted_symbol)))
	return
}

func (formatted_symbol_id *Formatted_symbol_id) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(formatted_symbol_id), uint64(GetOrderPointerGongstruct(stage, formatted_symbol_id)))
	return
}

func (formatted_text *Formatted_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(formatted_text), uint64(GetOrderPointerGongstruct(stage, formatted_text)))
	return
}

func (formatted_text_id *Formatted_text_id) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(formatted_text_id), uint64(GetOrderPointerGongstruct(stage, formatted_text_id)))
	return
}

func (forward *Forward) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(forward), uint64(GetOrderPointerGongstruct(stage, forward)))
	return
}

func (frame *Frame) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(frame), uint64(GetOrderPointerGongstruct(stage, frame)))
	return
}

func (frame_note *Frame_note) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(frame_note), uint64(GetOrderPointerGongstruct(stage, frame_note)))
	return
}

func (fret *Fret) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(fret), uint64(GetOrderPointerGongstruct(stage, fret)))
	return
}

func (glass *Glass) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(glass), uint64(GetOrderPointerGongstruct(stage, glass)))
	return
}

func (glissando *Glissando) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(glissando), uint64(GetOrderPointerGongstruct(stage, glissando)))
	return
}

func (glyph *Glyph) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(glyph), uint64(GetOrderPointerGongstruct(stage, glyph)))
	return
}

func (grace *Grace) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(grace), uint64(GetOrderPointerGongstruct(stage, grace)))
	return
}

func (group_barline *Group_barline) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(group_barline), uint64(GetOrderPointerGongstruct(stage, group_barline)))
	return
}

func (group_name *Group_name) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(group_name), uint64(GetOrderPointerGongstruct(stage, group_name)))
	return
}

func (group_symbol *Group_symbol) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(group_symbol), uint64(GetOrderPointerGongstruct(stage, group_symbol)))
	return
}

func (grouping *Grouping) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(grouping), uint64(GetOrderPointerGongstruct(stage, grouping)))
	return
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(hammer_on_pull_off), uint64(GetOrderPointerGongstruct(stage, hammer_on_pull_off)))
	return
}

func (handbell *Handbell) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(handbell), uint64(GetOrderPointerGongstruct(stage, handbell)))
	return
}

func (harmon_closed *Harmon_closed) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harmon_closed), uint64(GetOrderPointerGongstruct(stage, harmon_closed)))
	return
}

func (harmon_mute *Harmon_mute) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harmon_mute), uint64(GetOrderPointerGongstruct(stage, harmon_mute)))
	return
}

func (harmonic *Harmonic) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harmonic), uint64(GetOrderPointerGongstruct(stage, harmonic)))
	return
}

func (harmony *Harmony) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harmony), uint64(GetOrderPointerGongstruct(stage, harmony)))
	return
}

func (harmony_alter *Harmony_alter) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harmony_alter), uint64(GetOrderPointerGongstruct(stage, harmony_alter)))
	return
}

func (harp_pedals *Harp_pedals) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(harp_pedals), uint64(GetOrderPointerGongstruct(stage, harp_pedals)))
	return
}

func (heel_toe *Heel_toe) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(heel_toe), uint64(GetOrderPointerGongstruct(stage, heel_toe)))
	return
}

func (hole *Hole) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(hole), uint64(GetOrderPointerGongstruct(stage, hole)))
	return
}

func (hole_closed *Hole_closed) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(hole_closed), uint64(GetOrderPointerGongstruct(stage, hole_closed)))
	return
}

func (horizontal_turn *Horizontal_turn) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(horizontal_turn), uint64(GetOrderPointerGongstruct(stage, horizontal_turn)))
	return
}

func (identification *Identification) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(identification), uint64(GetOrderPointerGongstruct(stage, identification)))
	return
}

func (image *Image) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(image), uint64(GetOrderPointerGongstruct(stage, image)))
	return
}

func (instrument *Instrument) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(instrument), uint64(GetOrderPointerGongstruct(stage, instrument)))
	return
}

func (instrument_change *Instrument_change) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(instrument_change), uint64(GetOrderPointerGongstruct(stage, instrument_change)))
	return
}

func (instrument_link *Instrument_link) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(instrument_link), uint64(GetOrderPointerGongstruct(stage, instrument_link)))
	return
}

func (interchangeable *Interchangeable) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(interchangeable), uint64(GetOrderPointerGongstruct(stage, interchangeable)))
	return
}

func (inversion *Inversion) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(inversion), uint64(GetOrderPointerGongstruct(stage, inversion)))
	return
}

func (key *Key) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(key), uint64(GetOrderPointerGongstruct(stage, key)))
	return
}

func (key_accidental *Key_accidental) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(key_accidental), uint64(GetOrderPointerGongstruct(stage, key_accidental)))
	return
}

func (key_octave *Key_octave) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(key_octave), uint64(GetOrderPointerGongstruct(stage, key_octave)))
	return
}

func (kind *Kind) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(kind), uint64(GetOrderPointerGongstruct(stage, kind)))
	return
}

func (level *Level) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(level), uint64(GetOrderPointerGongstruct(stage, level)))
	return
}

func (line_detail *Line_detail) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(line_detail), uint64(GetOrderPointerGongstruct(stage, line_detail)))
	return
}

func (line_width *Line_width) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(line_width), uint64(GetOrderPointerGongstruct(stage, line_width)))
	return
}

func (link *Link) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(link), uint64(GetOrderPointerGongstruct(stage, link)))
	return
}

func (listen *Listen) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(listen), uint64(GetOrderPointerGongstruct(stage, listen)))
	return
}

func (listening *Listening) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(listening), uint64(GetOrderPointerGongstruct(stage, listening)))
	return
}

func (lyric *Lyric) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(lyric), uint64(GetOrderPointerGongstruct(stage, lyric)))
	return
}

func (lyric_font *Lyric_font) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(lyric_font), uint64(GetOrderPointerGongstruct(stage, lyric_font)))
	return
}

func (lyric_language *Lyric_language) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(lyric_language), uint64(GetOrderPointerGongstruct(stage, lyric_language)))
	return
}

func (measure_layout *Measure_layout) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(measure_layout), uint64(GetOrderPointerGongstruct(stage, measure_layout)))
	return
}

func (measure_numbering *Measure_numbering) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(measure_numbering), uint64(GetOrderPointerGongstruct(stage, measure_numbering)))
	return
}

func (measure_repeat *Measure_repeat) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(measure_repeat), uint64(GetOrderPointerGongstruct(stage, measure_repeat)))
	return
}

func (measure_style *Measure_style) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(measure_style), uint64(GetOrderPointerGongstruct(stage, measure_style)))
	return
}

func (membrane *Membrane) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(membrane), uint64(GetOrderPointerGongstruct(stage, membrane)))
	return
}

func (metal *Metal) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metal), uint64(GetOrderPointerGongstruct(stage, metal)))
	return
}

func (metronome *Metronome) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metronome), uint64(GetOrderPointerGongstruct(stage, metronome)))
	return
}

func (metronome_beam *Metronome_beam) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metronome_beam), uint64(GetOrderPointerGongstruct(stage, metronome_beam)))
	return
}

func (metronome_note *Metronome_note) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metronome_note), uint64(GetOrderPointerGongstruct(stage, metronome_note)))
	return
}

func (metronome_tied *Metronome_tied) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metronome_tied), uint64(GetOrderPointerGongstruct(stage, metronome_tied)))
	return
}

func (metronome_tuplet *Metronome_tuplet) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(metronome_tuplet), uint64(GetOrderPointerGongstruct(stage, metronome_tuplet)))
	return
}

func (midi_device *Midi_device) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(midi_device), uint64(GetOrderPointerGongstruct(stage, midi_device)))
	return
}

func (midi_instrument *Midi_instrument) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(midi_instrument), uint64(GetOrderPointerGongstruct(stage, midi_instrument)))
	return
}

func (miscellaneous *Miscellaneous) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(miscellaneous), uint64(GetOrderPointerGongstruct(stage, miscellaneous)))
	return
}

func (miscellaneous_field *Miscellaneous_field) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(miscellaneous_field), uint64(GetOrderPointerGongstruct(stage, miscellaneous_field)))
	return
}

func (mordent *Mordent) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(mordent), uint64(GetOrderPointerGongstruct(stage, mordent)))
	return
}

func (multiple_rest *Multiple_rest) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(multiple_rest), uint64(GetOrderPointerGongstruct(stage, multiple_rest)))
	return
}

func (name_display *Name_display) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(name_display), uint64(GetOrderPointerGongstruct(stage, name_display)))
	return
}

func (non_arpeggiate *Non_arpeggiate) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(non_arpeggiate), uint64(GetOrderPointerGongstruct(stage, non_arpeggiate)))
	return
}

func (notations *Notations) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(notations), uint64(GetOrderPointerGongstruct(stage, notations)))
	return
}

func (note *Note) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(note), uint64(GetOrderPointerGongstruct(stage, note)))
	return
}

func (note_size *Note_size) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(note_size), uint64(GetOrderPointerGongstruct(stage, note_size)))
	return
}

func (note_type *Note_type) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(note_type), uint64(GetOrderPointerGongstruct(stage, note_type)))
	return
}

func (notehead *Notehead) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(notehead), uint64(GetOrderPointerGongstruct(stage, notehead)))
	return
}

func (notehead_text *Notehead_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(notehead_text), uint64(GetOrderPointerGongstruct(stage, notehead_text)))
	return
}

func (numeral *Numeral) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(numeral), uint64(GetOrderPointerGongstruct(stage, numeral)))
	return
}

func (numeral_key *Numeral_key) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(numeral_key), uint64(GetOrderPointerGongstruct(stage, numeral_key)))
	return
}

func (numeral_root *Numeral_root) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(numeral_root), uint64(GetOrderPointerGongstruct(stage, numeral_root)))
	return
}

func (octave_shift *Octave_shift) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(octave_shift), uint64(GetOrderPointerGongstruct(stage, octave_shift)))
	return
}

func (offset *Offset) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(offset), uint64(GetOrderPointerGongstruct(stage, offset)))
	return
}

func (opus *Opus) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(opus), uint64(GetOrderPointerGongstruct(stage, opus)))
	return
}

func (ornaments *Ornaments) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(ornaments), uint64(GetOrderPointerGongstruct(stage, ornaments)))
	return
}

func (other_appearance *Other_appearance) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_appearance), uint64(GetOrderPointerGongstruct(stage, other_appearance)))
	return
}

func (other_direction *Other_direction) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_direction), uint64(GetOrderPointerGongstruct(stage, other_direction)))
	return
}

func (other_listening *Other_listening) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_listening), uint64(GetOrderPointerGongstruct(stage, other_listening)))
	return
}

func (other_notation *Other_notation) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_notation), uint64(GetOrderPointerGongstruct(stage, other_notation)))
	return
}

func (other_placement_text *Other_placement_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_placement_text), uint64(GetOrderPointerGongstruct(stage, other_placement_text)))
	return
}

func (other_play *Other_play) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_play), uint64(GetOrderPointerGongstruct(stage, other_play)))
	return
}

func (other_text *Other_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(other_text), uint64(GetOrderPointerGongstruct(stage, other_text)))
	return
}

func (page_layout *Page_layout) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(page_layout), uint64(GetOrderPointerGongstruct(stage, page_layout)))
	return
}

func (page_margins *Page_margins) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(page_margins), uint64(GetOrderPointerGongstruct(stage, page_margins)))
	return
}

func (part_clef *Part_clef) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_clef), uint64(GetOrderPointerGongstruct(stage, part_clef)))
	return
}

func (part_group *Part_group) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_group), uint64(GetOrderPointerGongstruct(stage, part_group)))
	return
}

func (part_link *Part_link) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_link), uint64(GetOrderPointerGongstruct(stage, part_link)))
	return
}

func (part_list *Part_list) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_list), uint64(GetOrderPointerGongstruct(stage, part_list)))
	return
}

func (part_name *Part_name) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_name), uint64(GetOrderPointerGongstruct(stage, part_name)))
	return
}

func (part_symbol *Part_symbol) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_symbol), uint64(GetOrderPointerGongstruct(stage, part_symbol)))
	return
}

func (part_transpose *Part_transpose) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(part_transpose), uint64(GetOrderPointerGongstruct(stage, part_transpose)))
	return
}

func (pedal *Pedal) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(pedal), uint64(GetOrderPointerGongstruct(stage, pedal)))
	return
}

func (pedal_tuning *Pedal_tuning) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(pedal_tuning), uint64(GetOrderPointerGongstruct(stage, pedal_tuning)))
	return
}

func (per_minute *Per_minute) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(per_minute), uint64(GetOrderPointerGongstruct(stage, per_minute)))
	return
}

func (percussion *Percussion) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(percussion), uint64(GetOrderPointerGongstruct(stage, percussion)))
	return
}

func (pitch *Pitch) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(pitch), uint64(GetOrderPointerGongstruct(stage, pitch)))
	return
}

func (pitched *Pitched) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(pitched), uint64(GetOrderPointerGongstruct(stage, pitched)))
	return
}

func (placement_text *Placement_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(placement_text), uint64(GetOrderPointerGongstruct(stage, placement_text)))
	return
}

func (play *Play) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(play), uint64(GetOrderPointerGongstruct(stage, play)))
	return
}

func (player *Player) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(player), uint64(GetOrderPointerGongstruct(stage, player)))
	return
}

func (principal_voice *Principal_voice) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(principal_voice), uint64(GetOrderPointerGongstruct(stage, principal_voice)))
	return
}

func (print *Print) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(print), uint64(GetOrderPointerGongstruct(stage, print)))
	return
}

func (release *Release) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(release), uint64(GetOrderPointerGongstruct(stage, release)))
	return
}

func (repeat *Repeat) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(repeat), uint64(GetOrderPointerGongstruct(stage, repeat)))
	return
}

func (rest *Rest) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(rest), uint64(GetOrderPointerGongstruct(stage, rest)))
	return
}

func (root *Root) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(root), uint64(GetOrderPointerGongstruct(stage, root)))
	return
}

func (root_step *Root_step) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(root_step), uint64(GetOrderPointerGongstruct(stage, root_step)))
	return
}

func (scaling *Scaling) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(scaling), uint64(GetOrderPointerGongstruct(stage, scaling)))
	return
}

func (scordatura *Scordatura) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(scordatura), uint64(GetOrderPointerGongstruct(stage, scordatura)))
	return
}

func (score_instrument *Score_instrument) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(score_instrument), uint64(GetOrderPointerGongstruct(stage, score_instrument)))
	return
}

func (score_part *Score_part) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(score_part), uint64(GetOrderPointerGongstruct(stage, score_part)))
	return
}

func (score_partwise *Score_partwise) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(score_partwise), uint64(GetOrderPointerGongstruct(stage, score_partwise)))
	return
}

func (score_timewise *Score_timewise) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(score_timewise), uint64(GetOrderPointerGongstruct(stage, score_timewise)))
	return
}

func (segno *Segno) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(segno), uint64(GetOrderPointerGongstruct(stage, segno)))
	return
}

func (slash *Slash) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(slash), uint64(GetOrderPointerGongstruct(stage, slash)))
	return
}

func (slide *Slide) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(slide), uint64(GetOrderPointerGongstruct(stage, slide)))
	return
}

func (slur *Slur) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(slur), uint64(GetOrderPointerGongstruct(stage, slur)))
	return
}

func (sound *Sound) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(sound), uint64(GetOrderPointerGongstruct(stage, sound)))
	return
}

func (staff_details *Staff_details) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(staff_details), uint64(GetOrderPointerGongstruct(stage, staff_details)))
	return
}

func (staff_divide *Staff_divide) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(staff_divide), uint64(GetOrderPointerGongstruct(stage, staff_divide)))
	return
}

func (staff_layout *Staff_layout) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(staff_layout), uint64(GetOrderPointerGongstruct(stage, staff_layout)))
	return
}

func (staff_size *Staff_size) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(staff_size), uint64(GetOrderPointerGongstruct(stage, staff_size)))
	return
}

func (staff_tuning *Staff_tuning) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(staff_tuning), uint64(GetOrderPointerGongstruct(stage, staff_tuning)))
	return
}

func (stem *Stem) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(stem), uint64(GetOrderPointerGongstruct(stage, stem)))
	return
}

func (stick *Stick) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(stick), uint64(GetOrderPointerGongstruct(stage, stick)))
	return
}

func (string_mute *String_mute) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(string_mute), uint64(GetOrderPointerGongstruct(stage, string_mute)))
	return
}

func (string_type *String_type) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(string_type), uint64(GetOrderPointerGongstruct(stage, string_type)))
	return
}

func (strong_accent *Strong_accent) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(strong_accent), uint64(GetOrderPointerGongstruct(stage, strong_accent)))
	return
}

func (style_text *Style_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(style_text), uint64(GetOrderPointerGongstruct(stage, style_text)))
	return
}

func (supports *Supports) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(supports), uint64(GetOrderPointerGongstruct(stage, supports)))
	return
}

func (swing *Swing) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(swing), uint64(GetOrderPointerGongstruct(stage, swing)))
	return
}

func (sync *Sync) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(sync), uint64(GetOrderPointerGongstruct(stage, sync)))
	return
}

func (system_dividers *System_dividers) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(system_dividers), uint64(GetOrderPointerGongstruct(stage, system_dividers)))
	return
}

func (system_layout *System_layout) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(system_layout), uint64(GetOrderPointerGongstruct(stage, system_layout)))
	return
}

func (system_margins *System_margins) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(system_margins), uint64(GetOrderPointerGongstruct(stage, system_margins)))
	return
}

func (tap *Tap) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tap), uint64(GetOrderPointerGongstruct(stage, tap)))
	return
}

func (technical *Technical) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(technical), uint64(GetOrderPointerGongstruct(stage, technical)))
	return
}

func (text_element_data *Text_element_data) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(text_element_data), uint64(GetOrderPointerGongstruct(stage, text_element_data)))
	return
}

func (tie *Tie) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tie), uint64(GetOrderPointerGongstruct(stage, tie)))
	return
}

func (tied *Tied) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tied), uint64(GetOrderPointerGongstruct(stage, tied)))
	return
}

func (time *Time) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(time), uint64(GetOrderPointerGongstruct(stage, time)))
	return
}

func (time_modification *Time_modification) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(time_modification), uint64(GetOrderPointerGongstruct(stage, time_modification)))
	return
}

func (timpani *Timpani) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(timpani), uint64(GetOrderPointerGongstruct(stage, timpani)))
	return
}

func (transpose *Transpose) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(transpose), uint64(GetOrderPointerGongstruct(stage, transpose)))
	return
}

func (tremolo *Tremolo) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tremolo), uint64(GetOrderPointerGongstruct(stage, tremolo)))
	return
}

func (tuplet *Tuplet) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tuplet), uint64(GetOrderPointerGongstruct(stage, tuplet)))
	return
}

func (tuplet_dot *Tuplet_dot) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tuplet_dot), uint64(GetOrderPointerGongstruct(stage, tuplet_dot)))
	return
}

func (tuplet_number *Tuplet_number) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tuplet_number), uint64(GetOrderPointerGongstruct(stage, tuplet_number)))
	return
}

func (tuplet_portion *Tuplet_portion) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tuplet_portion), uint64(GetOrderPointerGongstruct(stage, tuplet_portion)))
	return
}

func (tuplet_type *Tuplet_type) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(tuplet_type), uint64(GetOrderPointerGongstruct(stage, tuplet_type)))
	return
}

func (typed_text *Typed_text) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(typed_text), uint64(GetOrderPointerGongstruct(stage, typed_text)))
	return
}

func (unpitched *Unpitched) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(unpitched), uint64(GetOrderPointerGongstruct(stage, unpitched)))
	return
}

func (virtual_instrument *Virtual_instrument) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(virtual_instrument), uint64(GetOrderPointerGongstruct(stage, virtual_instrument)))
	return
}

func (wait *Wait) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(wait), uint64(GetOrderPointerGongstruct(stage, wait)))
	return
}

func (wavy_line *Wavy_line) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(wavy_line), uint64(GetOrderPointerGongstruct(stage, wavy_line)))
	return
}

func (wedge *Wedge) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(wedge), uint64(GetOrderPointerGongstruct(stage, wedge)))
	return
}

func (wood *Wood) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(wood), uint64(GetOrderPointerGongstruct(stage, wood)))
	return
}

func (work *Work) GongGetUUID(stage *Stage) (uuid string) {
	uuid = GenerateReproducibleUUIDv4(GetGongstructNameFromPointer(work), uint64(GetOrderPointerGongstruct(stage, work)))
	return
}

func (stage *Stage) ComputeForwardAndBackwardCommits() {
	var lenNewInstances int
	var lenModifiedInstances int
	var lenDeletedInstances int

	var newInstancesSlice []string
	var fieldsEditSlice []string
	var deletedInstancesSlice []string

	var newInstancesReverseSlice []string
	var fieldsEditReverseSlice []string
	var deletedInstancesReverseSlice []string

	// first clean the staging area to remove non staged instances
	// from pointers fields and slices of pointers fields
	stage.Clean()

	// insertion point per named struct
	var a_directives_newInstances []*A_directive
	var a_directives_deletedInstances []*A_directive

	// parse all staged instances and check if they have a reference
	for a_directive := range stage.A_directives {
		if ref, ok := stage.A_directives_reference[a_directive]; !ok {
			a_directives_newInstances = append(a_directives_newInstances, a_directive)
			newInstancesSlice = append(newInstancesSlice, a_directive.GongMarshallIdentifier(stage))
			if stage.A_directives_referenceOrder == nil {
				stage.A_directives_referenceOrder = make(map[*A_directive]uint)
			}
			stage.A_directives_referenceOrder[a_directive] = stage.A_directive_stagedOrder[a_directive]
			newInstancesReverseSlice = append(newInstancesReverseSlice, a_directive.GongMarshallUnstaging(stage))
			// delete(stage.A_directives_referenceOrder, a_directive)
			fieldInitializers, pointersInitializations := a_directive.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.A_directive_stagedOrder[ref] = stage.A_directive_stagedOrder[a_directive]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := a_directive.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, a_directive)
			// delete(stage.A_directive_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", a_directive.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.A_directives_reference {
		instance := stage.A_directives_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.A_directives[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			a_directives_deletedInstances = append(a_directives_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(a_directives_newInstances)
	lenDeletedInstances += len(a_directives_deletedInstances)
	var a_measures_newInstances []*A_measure
	var a_measures_deletedInstances []*A_measure

	// parse all staged instances and check if they have a reference
	for a_measure := range stage.A_measures {
		if ref, ok := stage.A_measures_reference[a_measure]; !ok {
			a_measures_newInstances = append(a_measures_newInstances, a_measure)
			newInstancesSlice = append(newInstancesSlice, a_measure.GongMarshallIdentifier(stage))
			if stage.A_measures_referenceOrder == nil {
				stage.A_measures_referenceOrder = make(map[*A_measure]uint)
			}
			stage.A_measures_referenceOrder[a_measure] = stage.A_measure_stagedOrder[a_measure]
			newInstancesReverseSlice = append(newInstancesReverseSlice, a_measure.GongMarshallUnstaging(stage))
			// delete(stage.A_measures_referenceOrder, a_measure)
			fieldInitializers, pointersInitializations := a_measure.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.A_measure_stagedOrder[ref] = stage.A_measure_stagedOrder[a_measure]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := a_measure.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, a_measure)
			// delete(stage.A_measure_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", a_measure.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.A_measures_reference {
		instance := stage.A_measures_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.A_measures[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			a_measures_deletedInstances = append(a_measures_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(a_measures_newInstances)
	lenDeletedInstances += len(a_measures_deletedInstances)
	var a_measure_1s_newInstances []*A_measure_1
	var a_measure_1s_deletedInstances []*A_measure_1

	// parse all staged instances and check if they have a reference
	for a_measure_1 := range stage.A_measure_1s {
		if ref, ok := stage.A_measure_1s_reference[a_measure_1]; !ok {
			a_measure_1s_newInstances = append(a_measure_1s_newInstances, a_measure_1)
			newInstancesSlice = append(newInstancesSlice, a_measure_1.GongMarshallIdentifier(stage))
			if stage.A_measure_1s_referenceOrder == nil {
				stage.A_measure_1s_referenceOrder = make(map[*A_measure_1]uint)
			}
			stage.A_measure_1s_referenceOrder[a_measure_1] = stage.A_measure_1_stagedOrder[a_measure_1]
			newInstancesReverseSlice = append(newInstancesReverseSlice, a_measure_1.GongMarshallUnstaging(stage))
			// delete(stage.A_measure_1s_referenceOrder, a_measure_1)
			fieldInitializers, pointersInitializations := a_measure_1.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.A_measure_1_stagedOrder[ref] = stage.A_measure_1_stagedOrder[a_measure_1]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := a_measure_1.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, a_measure_1)
			// delete(stage.A_measure_1_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", a_measure_1.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.A_measure_1s_reference {
		instance := stage.A_measure_1s_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.A_measure_1s[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			a_measure_1s_deletedInstances = append(a_measure_1s_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(a_measure_1s_newInstances)
	lenDeletedInstances += len(a_measure_1s_deletedInstances)
	var a_parts_newInstances []*A_part
	var a_parts_deletedInstances []*A_part

	// parse all staged instances and check if they have a reference
	for a_part := range stage.A_parts {
		if ref, ok := stage.A_parts_reference[a_part]; !ok {
			a_parts_newInstances = append(a_parts_newInstances, a_part)
			newInstancesSlice = append(newInstancesSlice, a_part.GongMarshallIdentifier(stage))
			if stage.A_parts_referenceOrder == nil {
				stage.A_parts_referenceOrder = make(map[*A_part]uint)
			}
			stage.A_parts_referenceOrder[a_part] = stage.A_part_stagedOrder[a_part]
			newInstancesReverseSlice = append(newInstancesReverseSlice, a_part.GongMarshallUnstaging(stage))
			// delete(stage.A_parts_referenceOrder, a_part)
			fieldInitializers, pointersInitializations := a_part.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.A_part_stagedOrder[ref] = stage.A_part_stagedOrder[a_part]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := a_part.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, a_part)
			// delete(stage.A_part_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", a_part.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.A_parts_reference {
		instance := stage.A_parts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.A_parts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			a_parts_deletedInstances = append(a_parts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(a_parts_newInstances)
	lenDeletedInstances += len(a_parts_deletedInstances)
	var a_part_1s_newInstances []*A_part_1
	var a_part_1s_deletedInstances []*A_part_1

	// parse all staged instances and check if they have a reference
	for a_part_1 := range stage.A_part_1s {
		if ref, ok := stage.A_part_1s_reference[a_part_1]; !ok {
			a_part_1s_newInstances = append(a_part_1s_newInstances, a_part_1)
			newInstancesSlice = append(newInstancesSlice, a_part_1.GongMarshallIdentifier(stage))
			if stage.A_part_1s_referenceOrder == nil {
				stage.A_part_1s_referenceOrder = make(map[*A_part_1]uint)
			}
			stage.A_part_1s_referenceOrder[a_part_1] = stage.A_part_1_stagedOrder[a_part_1]
			newInstancesReverseSlice = append(newInstancesReverseSlice, a_part_1.GongMarshallUnstaging(stage))
			// delete(stage.A_part_1s_referenceOrder, a_part_1)
			fieldInitializers, pointersInitializations := a_part_1.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.A_part_1_stagedOrder[ref] = stage.A_part_1_stagedOrder[a_part_1]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := a_part_1.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, a_part_1)
			// delete(stage.A_part_1_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", a_part_1.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.A_part_1s_reference {
		instance := stage.A_part_1s_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.A_part_1s[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			a_part_1s_deletedInstances = append(a_part_1s_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(a_part_1s_newInstances)
	lenDeletedInstances += len(a_part_1s_deletedInstances)
	var accidentals_newInstances []*Accidental
	var accidentals_deletedInstances []*Accidental

	// parse all staged instances and check if they have a reference
	for accidental := range stage.Accidentals {
		if ref, ok := stage.Accidentals_reference[accidental]; !ok {
			accidentals_newInstances = append(accidentals_newInstances, accidental)
			newInstancesSlice = append(newInstancesSlice, accidental.GongMarshallIdentifier(stage))
			if stage.Accidentals_referenceOrder == nil {
				stage.Accidentals_referenceOrder = make(map[*Accidental]uint)
			}
			stage.Accidentals_referenceOrder[accidental] = stage.Accidental_stagedOrder[accidental]
			newInstancesReverseSlice = append(newInstancesReverseSlice, accidental.GongMarshallUnstaging(stage))
			// delete(stage.Accidentals_referenceOrder, accidental)
			fieldInitializers, pointersInitializations := accidental.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Accidental_stagedOrder[ref] = stage.Accidental_stagedOrder[accidental]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := accidental.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, accidental)
			// delete(stage.Accidental_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", accidental.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Accidentals_reference {
		instance := stage.Accidentals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Accidentals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			accidentals_deletedInstances = append(accidentals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(accidentals_newInstances)
	lenDeletedInstances += len(accidentals_deletedInstances)
	var accidental_marks_newInstances []*Accidental_mark
	var accidental_marks_deletedInstances []*Accidental_mark

	// parse all staged instances and check if they have a reference
	for accidental_mark := range stage.Accidental_marks {
		if ref, ok := stage.Accidental_marks_reference[accidental_mark]; !ok {
			accidental_marks_newInstances = append(accidental_marks_newInstances, accidental_mark)
			newInstancesSlice = append(newInstancesSlice, accidental_mark.GongMarshallIdentifier(stage))
			if stage.Accidental_marks_referenceOrder == nil {
				stage.Accidental_marks_referenceOrder = make(map[*Accidental_mark]uint)
			}
			stage.Accidental_marks_referenceOrder[accidental_mark] = stage.Accidental_mark_stagedOrder[accidental_mark]
			newInstancesReverseSlice = append(newInstancesReverseSlice, accidental_mark.GongMarshallUnstaging(stage))
			// delete(stage.Accidental_marks_referenceOrder, accidental_mark)
			fieldInitializers, pointersInitializations := accidental_mark.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Accidental_mark_stagedOrder[ref] = stage.Accidental_mark_stagedOrder[accidental_mark]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := accidental_mark.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, accidental_mark)
			// delete(stage.Accidental_mark_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", accidental_mark.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Accidental_marks_reference {
		instance := stage.Accidental_marks_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Accidental_marks[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			accidental_marks_deletedInstances = append(accidental_marks_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(accidental_marks_newInstances)
	lenDeletedInstances += len(accidental_marks_deletedInstances)
	var accidental_texts_newInstances []*Accidental_text
	var accidental_texts_deletedInstances []*Accidental_text

	// parse all staged instances and check if they have a reference
	for accidental_text := range stage.Accidental_texts {
		if ref, ok := stage.Accidental_texts_reference[accidental_text]; !ok {
			accidental_texts_newInstances = append(accidental_texts_newInstances, accidental_text)
			newInstancesSlice = append(newInstancesSlice, accidental_text.GongMarshallIdentifier(stage))
			if stage.Accidental_texts_referenceOrder == nil {
				stage.Accidental_texts_referenceOrder = make(map[*Accidental_text]uint)
			}
			stage.Accidental_texts_referenceOrder[accidental_text] = stage.Accidental_text_stagedOrder[accidental_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, accidental_text.GongMarshallUnstaging(stage))
			// delete(stage.Accidental_texts_referenceOrder, accidental_text)
			fieldInitializers, pointersInitializations := accidental_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Accidental_text_stagedOrder[ref] = stage.Accidental_text_stagedOrder[accidental_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := accidental_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, accidental_text)
			// delete(stage.Accidental_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", accidental_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Accidental_texts_reference {
		instance := stage.Accidental_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Accidental_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			accidental_texts_deletedInstances = append(accidental_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(accidental_texts_newInstances)
	lenDeletedInstances += len(accidental_texts_deletedInstances)
	var accords_newInstances []*Accord
	var accords_deletedInstances []*Accord

	// parse all staged instances and check if they have a reference
	for accord := range stage.Accords {
		if ref, ok := stage.Accords_reference[accord]; !ok {
			accords_newInstances = append(accords_newInstances, accord)
			newInstancesSlice = append(newInstancesSlice, accord.GongMarshallIdentifier(stage))
			if stage.Accords_referenceOrder == nil {
				stage.Accords_referenceOrder = make(map[*Accord]uint)
			}
			stage.Accords_referenceOrder[accord] = stage.Accord_stagedOrder[accord]
			newInstancesReverseSlice = append(newInstancesReverseSlice, accord.GongMarshallUnstaging(stage))
			// delete(stage.Accords_referenceOrder, accord)
			fieldInitializers, pointersInitializations := accord.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Accord_stagedOrder[ref] = stage.Accord_stagedOrder[accord]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := accord.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, accord)
			// delete(stage.Accord_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", accord.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Accords_reference {
		instance := stage.Accords_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Accords[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			accords_deletedInstances = append(accords_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(accords_newInstances)
	lenDeletedInstances += len(accords_deletedInstances)
	var accordion_registrations_newInstances []*Accordion_registration
	var accordion_registrations_deletedInstances []*Accordion_registration

	// parse all staged instances and check if they have a reference
	for accordion_registration := range stage.Accordion_registrations {
		if ref, ok := stage.Accordion_registrations_reference[accordion_registration]; !ok {
			accordion_registrations_newInstances = append(accordion_registrations_newInstances, accordion_registration)
			newInstancesSlice = append(newInstancesSlice, accordion_registration.GongMarshallIdentifier(stage))
			if stage.Accordion_registrations_referenceOrder == nil {
				stage.Accordion_registrations_referenceOrder = make(map[*Accordion_registration]uint)
			}
			stage.Accordion_registrations_referenceOrder[accordion_registration] = stage.Accordion_registration_stagedOrder[accordion_registration]
			newInstancesReverseSlice = append(newInstancesReverseSlice, accordion_registration.GongMarshallUnstaging(stage))
			// delete(stage.Accordion_registrations_referenceOrder, accordion_registration)
			fieldInitializers, pointersInitializations := accordion_registration.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Accordion_registration_stagedOrder[ref] = stage.Accordion_registration_stagedOrder[accordion_registration]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := accordion_registration.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, accordion_registration)
			// delete(stage.Accordion_registration_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", accordion_registration.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Accordion_registrations_reference {
		instance := stage.Accordion_registrations_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Accordion_registrations[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			accordion_registrations_deletedInstances = append(accordion_registrations_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(accordion_registrations_newInstances)
	lenDeletedInstances += len(accordion_registrations_deletedInstances)
	var appearances_newInstances []*Appearance
	var appearances_deletedInstances []*Appearance

	// parse all staged instances and check if they have a reference
	for appearance := range stage.Appearances {
		if ref, ok := stage.Appearances_reference[appearance]; !ok {
			appearances_newInstances = append(appearances_newInstances, appearance)
			newInstancesSlice = append(newInstancesSlice, appearance.GongMarshallIdentifier(stage))
			if stage.Appearances_referenceOrder == nil {
				stage.Appearances_referenceOrder = make(map[*Appearance]uint)
			}
			stage.Appearances_referenceOrder[appearance] = stage.Appearance_stagedOrder[appearance]
			newInstancesReverseSlice = append(newInstancesReverseSlice, appearance.GongMarshallUnstaging(stage))
			// delete(stage.Appearances_referenceOrder, appearance)
			fieldInitializers, pointersInitializations := appearance.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Appearance_stagedOrder[ref] = stage.Appearance_stagedOrder[appearance]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := appearance.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, appearance)
			// delete(stage.Appearance_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", appearance.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Appearances_reference {
		instance := stage.Appearances_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Appearances[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			appearances_deletedInstances = append(appearances_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(appearances_newInstances)
	lenDeletedInstances += len(appearances_deletedInstances)
	var arpeggiates_newInstances []*Arpeggiate
	var arpeggiates_deletedInstances []*Arpeggiate

	// parse all staged instances and check if they have a reference
	for arpeggiate := range stage.Arpeggiates {
		if ref, ok := stage.Arpeggiates_reference[arpeggiate]; !ok {
			arpeggiates_newInstances = append(arpeggiates_newInstances, arpeggiate)
			newInstancesSlice = append(newInstancesSlice, arpeggiate.GongMarshallIdentifier(stage))
			if stage.Arpeggiates_referenceOrder == nil {
				stage.Arpeggiates_referenceOrder = make(map[*Arpeggiate]uint)
			}
			stage.Arpeggiates_referenceOrder[arpeggiate] = stage.Arpeggiate_stagedOrder[arpeggiate]
			newInstancesReverseSlice = append(newInstancesReverseSlice, arpeggiate.GongMarshallUnstaging(stage))
			// delete(stage.Arpeggiates_referenceOrder, arpeggiate)
			fieldInitializers, pointersInitializations := arpeggiate.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Arpeggiate_stagedOrder[ref] = stage.Arpeggiate_stagedOrder[arpeggiate]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := arpeggiate.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, arpeggiate)
			// delete(stage.Arpeggiate_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", arpeggiate.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Arpeggiates_reference {
		instance := stage.Arpeggiates_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Arpeggiates[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			arpeggiates_deletedInstances = append(arpeggiates_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(arpeggiates_newInstances)
	lenDeletedInstances += len(arpeggiates_deletedInstances)
	var arrows_newInstances []*Arrow
	var arrows_deletedInstances []*Arrow

	// parse all staged instances and check if they have a reference
	for arrow := range stage.Arrows {
		if ref, ok := stage.Arrows_reference[arrow]; !ok {
			arrows_newInstances = append(arrows_newInstances, arrow)
			newInstancesSlice = append(newInstancesSlice, arrow.GongMarshallIdentifier(stage))
			if stage.Arrows_referenceOrder == nil {
				stage.Arrows_referenceOrder = make(map[*Arrow]uint)
			}
			stage.Arrows_referenceOrder[arrow] = stage.Arrow_stagedOrder[arrow]
			newInstancesReverseSlice = append(newInstancesReverseSlice, arrow.GongMarshallUnstaging(stage))
			// delete(stage.Arrows_referenceOrder, arrow)
			fieldInitializers, pointersInitializations := arrow.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Arrow_stagedOrder[ref] = stage.Arrow_stagedOrder[arrow]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := arrow.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, arrow)
			// delete(stage.Arrow_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", arrow.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Arrows_reference {
		instance := stage.Arrows_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Arrows[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			arrows_deletedInstances = append(arrows_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(arrows_newInstances)
	lenDeletedInstances += len(arrows_deletedInstances)
	var articulationss_newInstances []*Articulations
	var articulationss_deletedInstances []*Articulations

	// parse all staged instances and check if they have a reference
	for articulations := range stage.Articulationss {
		if ref, ok := stage.Articulationss_reference[articulations]; !ok {
			articulationss_newInstances = append(articulationss_newInstances, articulations)
			newInstancesSlice = append(newInstancesSlice, articulations.GongMarshallIdentifier(stage))
			if stage.Articulationss_referenceOrder == nil {
				stage.Articulationss_referenceOrder = make(map[*Articulations]uint)
			}
			stage.Articulationss_referenceOrder[articulations] = stage.Articulations_stagedOrder[articulations]
			newInstancesReverseSlice = append(newInstancesReverseSlice, articulations.GongMarshallUnstaging(stage))
			// delete(stage.Articulationss_referenceOrder, articulations)
			fieldInitializers, pointersInitializations := articulations.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Articulations_stagedOrder[ref] = stage.Articulations_stagedOrder[articulations]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := articulations.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, articulations)
			// delete(stage.Articulations_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", articulations.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Articulationss_reference {
		instance := stage.Articulationss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Articulationss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			articulationss_deletedInstances = append(articulationss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(articulationss_newInstances)
	lenDeletedInstances += len(articulationss_deletedInstances)
	var assesss_newInstances []*Assess
	var assesss_deletedInstances []*Assess

	// parse all staged instances and check if they have a reference
	for assess := range stage.Assesss {
		if ref, ok := stage.Assesss_reference[assess]; !ok {
			assesss_newInstances = append(assesss_newInstances, assess)
			newInstancesSlice = append(newInstancesSlice, assess.GongMarshallIdentifier(stage))
			if stage.Assesss_referenceOrder == nil {
				stage.Assesss_referenceOrder = make(map[*Assess]uint)
			}
			stage.Assesss_referenceOrder[assess] = stage.Assess_stagedOrder[assess]
			newInstancesReverseSlice = append(newInstancesReverseSlice, assess.GongMarshallUnstaging(stage))
			// delete(stage.Assesss_referenceOrder, assess)
			fieldInitializers, pointersInitializations := assess.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Assess_stagedOrder[ref] = stage.Assess_stagedOrder[assess]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := assess.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, assess)
			// delete(stage.Assess_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", assess.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Assesss_reference {
		instance := stage.Assesss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Assesss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			assesss_deletedInstances = append(assesss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(assesss_newInstances)
	lenDeletedInstances += len(assesss_deletedInstances)
	var attributess_newInstances []*Attributes
	var attributess_deletedInstances []*Attributes

	// parse all staged instances and check if they have a reference
	for attributes := range stage.Attributess {
		if ref, ok := stage.Attributess_reference[attributes]; !ok {
			attributess_newInstances = append(attributess_newInstances, attributes)
			newInstancesSlice = append(newInstancesSlice, attributes.GongMarshallIdentifier(stage))
			if stage.Attributess_referenceOrder == nil {
				stage.Attributess_referenceOrder = make(map[*Attributes]uint)
			}
			stage.Attributess_referenceOrder[attributes] = stage.Attributes_stagedOrder[attributes]
			newInstancesReverseSlice = append(newInstancesReverseSlice, attributes.GongMarshallUnstaging(stage))
			// delete(stage.Attributess_referenceOrder, attributes)
			fieldInitializers, pointersInitializations := attributes.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Attributes_stagedOrder[ref] = stage.Attributes_stagedOrder[attributes]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := attributes.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, attributes)
			// delete(stage.Attributes_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", attributes.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Attributess_reference {
		instance := stage.Attributess_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Attributess[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			attributess_deletedInstances = append(attributess_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(attributess_newInstances)
	lenDeletedInstances += len(attributess_deletedInstances)
	var backups_newInstances []*Backup
	var backups_deletedInstances []*Backup

	// parse all staged instances and check if they have a reference
	for backup := range stage.Backups {
		if ref, ok := stage.Backups_reference[backup]; !ok {
			backups_newInstances = append(backups_newInstances, backup)
			newInstancesSlice = append(newInstancesSlice, backup.GongMarshallIdentifier(stage))
			if stage.Backups_referenceOrder == nil {
				stage.Backups_referenceOrder = make(map[*Backup]uint)
			}
			stage.Backups_referenceOrder[backup] = stage.Backup_stagedOrder[backup]
			newInstancesReverseSlice = append(newInstancesReverseSlice, backup.GongMarshallUnstaging(stage))
			// delete(stage.Backups_referenceOrder, backup)
			fieldInitializers, pointersInitializations := backup.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Backup_stagedOrder[ref] = stage.Backup_stagedOrder[backup]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := backup.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, backup)
			// delete(stage.Backup_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", backup.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Backups_reference {
		instance := stage.Backups_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Backups[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			backups_deletedInstances = append(backups_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(backups_newInstances)
	lenDeletedInstances += len(backups_deletedInstances)
	var bar_style_colors_newInstances []*Bar_style_color
	var bar_style_colors_deletedInstances []*Bar_style_color

	// parse all staged instances and check if they have a reference
	for bar_style_color := range stage.Bar_style_colors {
		if ref, ok := stage.Bar_style_colors_reference[bar_style_color]; !ok {
			bar_style_colors_newInstances = append(bar_style_colors_newInstances, bar_style_color)
			newInstancesSlice = append(newInstancesSlice, bar_style_color.GongMarshallIdentifier(stage))
			if stage.Bar_style_colors_referenceOrder == nil {
				stage.Bar_style_colors_referenceOrder = make(map[*Bar_style_color]uint)
			}
			stage.Bar_style_colors_referenceOrder[bar_style_color] = stage.Bar_style_color_stagedOrder[bar_style_color]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bar_style_color.GongMarshallUnstaging(stage))
			// delete(stage.Bar_style_colors_referenceOrder, bar_style_color)
			fieldInitializers, pointersInitializations := bar_style_color.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bar_style_color_stagedOrder[ref] = stage.Bar_style_color_stagedOrder[bar_style_color]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bar_style_color.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bar_style_color)
			// delete(stage.Bar_style_color_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bar_style_color.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Bar_style_colors_reference {
		instance := stage.Bar_style_colors_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Bar_style_colors[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			bar_style_colors_deletedInstances = append(bar_style_colors_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(bar_style_colors_newInstances)
	lenDeletedInstances += len(bar_style_colors_deletedInstances)
	var barlines_newInstances []*Barline
	var barlines_deletedInstances []*Barline

	// parse all staged instances and check if they have a reference
	for barline := range stage.Barlines {
		if ref, ok := stage.Barlines_reference[barline]; !ok {
			barlines_newInstances = append(barlines_newInstances, barline)
			newInstancesSlice = append(newInstancesSlice, barline.GongMarshallIdentifier(stage))
			if stage.Barlines_referenceOrder == nil {
				stage.Barlines_referenceOrder = make(map[*Barline]uint)
			}
			stage.Barlines_referenceOrder[barline] = stage.Barline_stagedOrder[barline]
			newInstancesReverseSlice = append(newInstancesReverseSlice, barline.GongMarshallUnstaging(stage))
			// delete(stage.Barlines_referenceOrder, barline)
			fieldInitializers, pointersInitializations := barline.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Barline_stagedOrder[ref] = stage.Barline_stagedOrder[barline]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := barline.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, barline)
			// delete(stage.Barline_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", barline.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Barlines_reference {
		instance := stage.Barlines_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Barlines[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			barlines_deletedInstances = append(barlines_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(barlines_newInstances)
	lenDeletedInstances += len(barlines_deletedInstances)
	var barres_newInstances []*Barre
	var barres_deletedInstances []*Barre

	// parse all staged instances and check if they have a reference
	for barre := range stage.Barres {
		if ref, ok := stage.Barres_reference[barre]; !ok {
			barres_newInstances = append(barres_newInstances, barre)
			newInstancesSlice = append(newInstancesSlice, barre.GongMarshallIdentifier(stage))
			if stage.Barres_referenceOrder == nil {
				stage.Barres_referenceOrder = make(map[*Barre]uint)
			}
			stage.Barres_referenceOrder[barre] = stage.Barre_stagedOrder[barre]
			newInstancesReverseSlice = append(newInstancesReverseSlice, barre.GongMarshallUnstaging(stage))
			// delete(stage.Barres_referenceOrder, barre)
			fieldInitializers, pointersInitializations := barre.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Barre_stagedOrder[ref] = stage.Barre_stagedOrder[barre]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := barre.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, barre)
			// delete(stage.Barre_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", barre.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Barres_reference {
		instance := stage.Barres_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Barres[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			barres_deletedInstances = append(barres_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(barres_newInstances)
	lenDeletedInstances += len(barres_deletedInstances)
	var basss_newInstances []*Bass
	var basss_deletedInstances []*Bass

	// parse all staged instances and check if they have a reference
	for bass := range stage.Basss {
		if ref, ok := stage.Basss_reference[bass]; !ok {
			basss_newInstances = append(basss_newInstances, bass)
			newInstancesSlice = append(newInstancesSlice, bass.GongMarshallIdentifier(stage))
			if stage.Basss_referenceOrder == nil {
				stage.Basss_referenceOrder = make(map[*Bass]uint)
			}
			stage.Basss_referenceOrder[bass] = stage.Bass_stagedOrder[bass]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bass.GongMarshallUnstaging(stage))
			// delete(stage.Basss_referenceOrder, bass)
			fieldInitializers, pointersInitializations := bass.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bass_stagedOrder[ref] = stage.Bass_stagedOrder[bass]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bass.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bass)
			// delete(stage.Bass_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bass.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Basss_reference {
		instance := stage.Basss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Basss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			basss_deletedInstances = append(basss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(basss_newInstances)
	lenDeletedInstances += len(basss_deletedInstances)
	var bass_steps_newInstances []*Bass_step
	var bass_steps_deletedInstances []*Bass_step

	// parse all staged instances and check if they have a reference
	for bass_step := range stage.Bass_steps {
		if ref, ok := stage.Bass_steps_reference[bass_step]; !ok {
			bass_steps_newInstances = append(bass_steps_newInstances, bass_step)
			newInstancesSlice = append(newInstancesSlice, bass_step.GongMarshallIdentifier(stage))
			if stage.Bass_steps_referenceOrder == nil {
				stage.Bass_steps_referenceOrder = make(map[*Bass_step]uint)
			}
			stage.Bass_steps_referenceOrder[bass_step] = stage.Bass_step_stagedOrder[bass_step]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bass_step.GongMarshallUnstaging(stage))
			// delete(stage.Bass_steps_referenceOrder, bass_step)
			fieldInitializers, pointersInitializations := bass_step.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bass_step_stagedOrder[ref] = stage.Bass_step_stagedOrder[bass_step]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bass_step.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bass_step)
			// delete(stage.Bass_step_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bass_step.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Bass_steps_reference {
		instance := stage.Bass_steps_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Bass_steps[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			bass_steps_deletedInstances = append(bass_steps_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(bass_steps_newInstances)
	lenDeletedInstances += len(bass_steps_deletedInstances)
	var beams_newInstances []*Beam
	var beams_deletedInstances []*Beam

	// parse all staged instances and check if they have a reference
	for beam := range stage.Beams {
		if ref, ok := stage.Beams_reference[beam]; !ok {
			beams_newInstances = append(beams_newInstances, beam)
			newInstancesSlice = append(newInstancesSlice, beam.GongMarshallIdentifier(stage))
			if stage.Beams_referenceOrder == nil {
				stage.Beams_referenceOrder = make(map[*Beam]uint)
			}
			stage.Beams_referenceOrder[beam] = stage.Beam_stagedOrder[beam]
			newInstancesReverseSlice = append(newInstancesReverseSlice, beam.GongMarshallUnstaging(stage))
			// delete(stage.Beams_referenceOrder, beam)
			fieldInitializers, pointersInitializations := beam.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Beam_stagedOrder[ref] = stage.Beam_stagedOrder[beam]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := beam.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, beam)
			// delete(stage.Beam_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", beam.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Beams_reference {
		instance := stage.Beams_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Beams[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			beams_deletedInstances = append(beams_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(beams_newInstances)
	lenDeletedInstances += len(beams_deletedInstances)
	var beat_repeats_newInstances []*Beat_repeat
	var beat_repeats_deletedInstances []*Beat_repeat

	// parse all staged instances and check if they have a reference
	for beat_repeat := range stage.Beat_repeats {
		if ref, ok := stage.Beat_repeats_reference[beat_repeat]; !ok {
			beat_repeats_newInstances = append(beat_repeats_newInstances, beat_repeat)
			newInstancesSlice = append(newInstancesSlice, beat_repeat.GongMarshallIdentifier(stage))
			if stage.Beat_repeats_referenceOrder == nil {
				stage.Beat_repeats_referenceOrder = make(map[*Beat_repeat]uint)
			}
			stage.Beat_repeats_referenceOrder[beat_repeat] = stage.Beat_repeat_stagedOrder[beat_repeat]
			newInstancesReverseSlice = append(newInstancesReverseSlice, beat_repeat.GongMarshallUnstaging(stage))
			// delete(stage.Beat_repeats_referenceOrder, beat_repeat)
			fieldInitializers, pointersInitializations := beat_repeat.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Beat_repeat_stagedOrder[ref] = stage.Beat_repeat_stagedOrder[beat_repeat]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := beat_repeat.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, beat_repeat)
			// delete(stage.Beat_repeat_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", beat_repeat.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Beat_repeats_reference {
		instance := stage.Beat_repeats_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Beat_repeats[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			beat_repeats_deletedInstances = append(beat_repeats_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(beat_repeats_newInstances)
	lenDeletedInstances += len(beat_repeats_deletedInstances)
	var beat_unit_tieds_newInstances []*Beat_unit_tied
	var beat_unit_tieds_deletedInstances []*Beat_unit_tied

	// parse all staged instances and check if they have a reference
	for beat_unit_tied := range stage.Beat_unit_tieds {
		if ref, ok := stage.Beat_unit_tieds_reference[beat_unit_tied]; !ok {
			beat_unit_tieds_newInstances = append(beat_unit_tieds_newInstances, beat_unit_tied)
			newInstancesSlice = append(newInstancesSlice, beat_unit_tied.GongMarshallIdentifier(stage))
			if stage.Beat_unit_tieds_referenceOrder == nil {
				stage.Beat_unit_tieds_referenceOrder = make(map[*Beat_unit_tied]uint)
			}
			stage.Beat_unit_tieds_referenceOrder[beat_unit_tied] = stage.Beat_unit_tied_stagedOrder[beat_unit_tied]
			newInstancesReverseSlice = append(newInstancesReverseSlice, beat_unit_tied.GongMarshallUnstaging(stage))
			// delete(stage.Beat_unit_tieds_referenceOrder, beat_unit_tied)
			fieldInitializers, pointersInitializations := beat_unit_tied.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Beat_unit_tied_stagedOrder[ref] = stage.Beat_unit_tied_stagedOrder[beat_unit_tied]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := beat_unit_tied.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, beat_unit_tied)
			// delete(stage.Beat_unit_tied_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", beat_unit_tied.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Beat_unit_tieds_reference {
		instance := stage.Beat_unit_tieds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Beat_unit_tieds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			beat_unit_tieds_deletedInstances = append(beat_unit_tieds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(beat_unit_tieds_newInstances)
	lenDeletedInstances += len(beat_unit_tieds_deletedInstances)
	var beaters_newInstances []*Beater
	var beaters_deletedInstances []*Beater

	// parse all staged instances and check if they have a reference
	for beater := range stage.Beaters {
		if ref, ok := stage.Beaters_reference[beater]; !ok {
			beaters_newInstances = append(beaters_newInstances, beater)
			newInstancesSlice = append(newInstancesSlice, beater.GongMarshallIdentifier(stage))
			if stage.Beaters_referenceOrder == nil {
				stage.Beaters_referenceOrder = make(map[*Beater]uint)
			}
			stage.Beaters_referenceOrder[beater] = stage.Beater_stagedOrder[beater]
			newInstancesReverseSlice = append(newInstancesReverseSlice, beater.GongMarshallUnstaging(stage))
			// delete(stage.Beaters_referenceOrder, beater)
			fieldInitializers, pointersInitializations := beater.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Beater_stagedOrder[ref] = stage.Beater_stagedOrder[beater]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := beater.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, beater)
			// delete(stage.Beater_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", beater.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Beaters_reference {
		instance := stage.Beaters_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Beaters[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			beaters_deletedInstances = append(beaters_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(beaters_newInstances)
	lenDeletedInstances += len(beaters_deletedInstances)
	var bends_newInstances []*Bend
	var bends_deletedInstances []*Bend

	// parse all staged instances and check if they have a reference
	for bend := range stage.Bends {
		if ref, ok := stage.Bends_reference[bend]; !ok {
			bends_newInstances = append(bends_newInstances, bend)
			newInstancesSlice = append(newInstancesSlice, bend.GongMarshallIdentifier(stage))
			if stage.Bends_referenceOrder == nil {
				stage.Bends_referenceOrder = make(map[*Bend]uint)
			}
			stage.Bends_referenceOrder[bend] = stage.Bend_stagedOrder[bend]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bend.GongMarshallUnstaging(stage))
			// delete(stage.Bends_referenceOrder, bend)
			fieldInitializers, pointersInitializations := bend.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bend_stagedOrder[ref] = stage.Bend_stagedOrder[bend]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bend.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bend)
			// delete(stage.Bend_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bend.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Bends_reference {
		instance := stage.Bends_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Bends[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			bends_deletedInstances = append(bends_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(bends_newInstances)
	lenDeletedInstances += len(bends_deletedInstances)
	var bookmarks_newInstances []*Bookmark
	var bookmarks_deletedInstances []*Bookmark

	// parse all staged instances and check if they have a reference
	for bookmark := range stage.Bookmarks {
		if ref, ok := stage.Bookmarks_reference[bookmark]; !ok {
			bookmarks_newInstances = append(bookmarks_newInstances, bookmark)
			newInstancesSlice = append(newInstancesSlice, bookmark.GongMarshallIdentifier(stage))
			if stage.Bookmarks_referenceOrder == nil {
				stage.Bookmarks_referenceOrder = make(map[*Bookmark]uint)
			}
			stage.Bookmarks_referenceOrder[bookmark] = stage.Bookmark_stagedOrder[bookmark]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bookmark.GongMarshallUnstaging(stage))
			// delete(stage.Bookmarks_referenceOrder, bookmark)
			fieldInitializers, pointersInitializations := bookmark.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bookmark_stagedOrder[ref] = stage.Bookmark_stagedOrder[bookmark]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bookmark.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bookmark)
			// delete(stage.Bookmark_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bookmark.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Bookmarks_reference {
		instance := stage.Bookmarks_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Bookmarks[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			bookmarks_deletedInstances = append(bookmarks_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(bookmarks_newInstances)
	lenDeletedInstances += len(bookmarks_deletedInstances)
	var brackets_newInstances []*Bracket
	var brackets_deletedInstances []*Bracket

	// parse all staged instances and check if they have a reference
	for bracket := range stage.Brackets {
		if ref, ok := stage.Brackets_reference[bracket]; !ok {
			brackets_newInstances = append(brackets_newInstances, bracket)
			newInstancesSlice = append(newInstancesSlice, bracket.GongMarshallIdentifier(stage))
			if stage.Brackets_referenceOrder == nil {
				stage.Brackets_referenceOrder = make(map[*Bracket]uint)
			}
			stage.Brackets_referenceOrder[bracket] = stage.Bracket_stagedOrder[bracket]
			newInstancesReverseSlice = append(newInstancesReverseSlice, bracket.GongMarshallUnstaging(stage))
			// delete(stage.Brackets_referenceOrder, bracket)
			fieldInitializers, pointersInitializations := bracket.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Bracket_stagedOrder[ref] = stage.Bracket_stagedOrder[bracket]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := bracket.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, bracket)
			// delete(stage.Bracket_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", bracket.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Brackets_reference {
		instance := stage.Brackets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Brackets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			brackets_deletedInstances = append(brackets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(brackets_newInstances)
	lenDeletedInstances += len(brackets_deletedInstances)
	var breath_marks_newInstances []*Breath_mark
	var breath_marks_deletedInstances []*Breath_mark

	// parse all staged instances and check if they have a reference
	for breath_mark := range stage.Breath_marks {
		if ref, ok := stage.Breath_marks_reference[breath_mark]; !ok {
			breath_marks_newInstances = append(breath_marks_newInstances, breath_mark)
			newInstancesSlice = append(newInstancesSlice, breath_mark.GongMarshallIdentifier(stage))
			if stage.Breath_marks_referenceOrder == nil {
				stage.Breath_marks_referenceOrder = make(map[*Breath_mark]uint)
			}
			stage.Breath_marks_referenceOrder[breath_mark] = stage.Breath_mark_stagedOrder[breath_mark]
			newInstancesReverseSlice = append(newInstancesReverseSlice, breath_mark.GongMarshallUnstaging(stage))
			// delete(stage.Breath_marks_referenceOrder, breath_mark)
			fieldInitializers, pointersInitializations := breath_mark.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Breath_mark_stagedOrder[ref] = stage.Breath_mark_stagedOrder[breath_mark]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := breath_mark.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, breath_mark)
			// delete(stage.Breath_mark_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", breath_mark.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Breath_marks_reference {
		instance := stage.Breath_marks_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Breath_marks[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			breath_marks_deletedInstances = append(breath_marks_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(breath_marks_newInstances)
	lenDeletedInstances += len(breath_marks_deletedInstances)
	var caesuras_newInstances []*Caesura
	var caesuras_deletedInstances []*Caesura

	// parse all staged instances and check if they have a reference
	for caesura := range stage.Caesuras {
		if ref, ok := stage.Caesuras_reference[caesura]; !ok {
			caesuras_newInstances = append(caesuras_newInstances, caesura)
			newInstancesSlice = append(newInstancesSlice, caesura.GongMarshallIdentifier(stage))
			if stage.Caesuras_referenceOrder == nil {
				stage.Caesuras_referenceOrder = make(map[*Caesura]uint)
			}
			stage.Caesuras_referenceOrder[caesura] = stage.Caesura_stagedOrder[caesura]
			newInstancesReverseSlice = append(newInstancesReverseSlice, caesura.GongMarshallUnstaging(stage))
			// delete(stage.Caesuras_referenceOrder, caesura)
			fieldInitializers, pointersInitializations := caesura.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Caesura_stagedOrder[ref] = stage.Caesura_stagedOrder[caesura]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := caesura.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, caesura)
			// delete(stage.Caesura_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", caesura.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Caesuras_reference {
		instance := stage.Caesuras_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Caesuras[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			caesuras_deletedInstances = append(caesuras_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(caesuras_newInstances)
	lenDeletedInstances += len(caesuras_deletedInstances)
	var cancels_newInstances []*Cancel
	var cancels_deletedInstances []*Cancel

	// parse all staged instances and check if they have a reference
	for cancel := range stage.Cancels {
		if ref, ok := stage.Cancels_reference[cancel]; !ok {
			cancels_newInstances = append(cancels_newInstances, cancel)
			newInstancesSlice = append(newInstancesSlice, cancel.GongMarshallIdentifier(stage))
			if stage.Cancels_referenceOrder == nil {
				stage.Cancels_referenceOrder = make(map[*Cancel]uint)
			}
			stage.Cancels_referenceOrder[cancel] = stage.Cancel_stagedOrder[cancel]
			newInstancesReverseSlice = append(newInstancesReverseSlice, cancel.GongMarshallUnstaging(stage))
			// delete(stage.Cancels_referenceOrder, cancel)
			fieldInitializers, pointersInitializations := cancel.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Cancel_stagedOrder[ref] = stage.Cancel_stagedOrder[cancel]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := cancel.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, cancel)
			// delete(stage.Cancel_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", cancel.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Cancels_reference {
		instance := stage.Cancels_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Cancels[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			cancels_deletedInstances = append(cancels_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(cancels_newInstances)
	lenDeletedInstances += len(cancels_deletedInstances)
	var clefs_newInstances []*Clef
	var clefs_deletedInstances []*Clef

	// parse all staged instances and check if they have a reference
	for clef := range stage.Clefs {
		if ref, ok := stage.Clefs_reference[clef]; !ok {
			clefs_newInstances = append(clefs_newInstances, clef)
			newInstancesSlice = append(newInstancesSlice, clef.GongMarshallIdentifier(stage))
			if stage.Clefs_referenceOrder == nil {
				stage.Clefs_referenceOrder = make(map[*Clef]uint)
			}
			stage.Clefs_referenceOrder[clef] = stage.Clef_stagedOrder[clef]
			newInstancesReverseSlice = append(newInstancesReverseSlice, clef.GongMarshallUnstaging(stage))
			// delete(stage.Clefs_referenceOrder, clef)
			fieldInitializers, pointersInitializations := clef.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Clef_stagedOrder[ref] = stage.Clef_stagedOrder[clef]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := clef.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, clef)
			// delete(stage.Clef_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", clef.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Clefs_reference {
		instance := stage.Clefs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Clefs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			clefs_deletedInstances = append(clefs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(clefs_newInstances)
	lenDeletedInstances += len(clefs_deletedInstances)
	var codas_newInstances []*Coda
	var codas_deletedInstances []*Coda

	// parse all staged instances and check if they have a reference
	for coda := range stage.Codas {
		if ref, ok := stage.Codas_reference[coda]; !ok {
			codas_newInstances = append(codas_newInstances, coda)
			newInstancesSlice = append(newInstancesSlice, coda.GongMarshallIdentifier(stage))
			if stage.Codas_referenceOrder == nil {
				stage.Codas_referenceOrder = make(map[*Coda]uint)
			}
			stage.Codas_referenceOrder[coda] = stage.Coda_stagedOrder[coda]
			newInstancesReverseSlice = append(newInstancesReverseSlice, coda.GongMarshallUnstaging(stage))
			// delete(stage.Codas_referenceOrder, coda)
			fieldInitializers, pointersInitializations := coda.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Coda_stagedOrder[ref] = stage.Coda_stagedOrder[coda]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := coda.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, coda)
			// delete(stage.Coda_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", coda.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Codas_reference {
		instance := stage.Codas_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Codas[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			codas_deletedInstances = append(codas_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(codas_newInstances)
	lenDeletedInstances += len(codas_deletedInstances)
	var credits_newInstances []*Credit
	var credits_deletedInstances []*Credit

	// parse all staged instances and check if they have a reference
	for credit := range stage.Credits {
		if ref, ok := stage.Credits_reference[credit]; !ok {
			credits_newInstances = append(credits_newInstances, credit)
			newInstancesSlice = append(newInstancesSlice, credit.GongMarshallIdentifier(stage))
			if stage.Credits_referenceOrder == nil {
				stage.Credits_referenceOrder = make(map[*Credit]uint)
			}
			stage.Credits_referenceOrder[credit] = stage.Credit_stagedOrder[credit]
			newInstancesReverseSlice = append(newInstancesReverseSlice, credit.GongMarshallUnstaging(stage))
			// delete(stage.Credits_referenceOrder, credit)
			fieldInitializers, pointersInitializations := credit.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Credit_stagedOrder[ref] = stage.Credit_stagedOrder[credit]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := credit.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, credit)
			// delete(stage.Credit_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", credit.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Credits_reference {
		instance := stage.Credits_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Credits[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			credits_deletedInstances = append(credits_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(credits_newInstances)
	lenDeletedInstances += len(credits_deletedInstances)
	var dashess_newInstances []*Dashes
	var dashess_deletedInstances []*Dashes

	// parse all staged instances and check if they have a reference
	for dashes := range stage.Dashess {
		if ref, ok := stage.Dashess_reference[dashes]; !ok {
			dashess_newInstances = append(dashess_newInstances, dashes)
			newInstancesSlice = append(newInstancesSlice, dashes.GongMarshallIdentifier(stage))
			if stage.Dashess_referenceOrder == nil {
				stage.Dashess_referenceOrder = make(map[*Dashes]uint)
			}
			stage.Dashess_referenceOrder[dashes] = stage.Dashes_stagedOrder[dashes]
			newInstancesReverseSlice = append(newInstancesReverseSlice, dashes.GongMarshallUnstaging(stage))
			// delete(stage.Dashess_referenceOrder, dashes)
			fieldInitializers, pointersInitializations := dashes.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Dashes_stagedOrder[ref] = stage.Dashes_stagedOrder[dashes]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := dashes.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, dashes)
			// delete(stage.Dashes_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", dashes.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Dashess_reference {
		instance := stage.Dashess_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Dashess[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			dashess_deletedInstances = append(dashess_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(dashess_newInstances)
	lenDeletedInstances += len(dashess_deletedInstances)
	var defaultss_newInstances []*Defaults
	var defaultss_deletedInstances []*Defaults

	// parse all staged instances and check if they have a reference
	for defaults := range stage.Defaultss {
		if ref, ok := stage.Defaultss_reference[defaults]; !ok {
			defaultss_newInstances = append(defaultss_newInstances, defaults)
			newInstancesSlice = append(newInstancesSlice, defaults.GongMarshallIdentifier(stage))
			if stage.Defaultss_referenceOrder == nil {
				stage.Defaultss_referenceOrder = make(map[*Defaults]uint)
			}
			stage.Defaultss_referenceOrder[defaults] = stage.Defaults_stagedOrder[defaults]
			newInstancesReverseSlice = append(newInstancesReverseSlice, defaults.GongMarshallUnstaging(stage))
			// delete(stage.Defaultss_referenceOrder, defaults)
			fieldInitializers, pointersInitializations := defaults.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Defaults_stagedOrder[ref] = stage.Defaults_stagedOrder[defaults]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := defaults.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, defaults)
			// delete(stage.Defaults_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", defaults.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Defaultss_reference {
		instance := stage.Defaultss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Defaultss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			defaultss_deletedInstances = append(defaultss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(defaultss_newInstances)
	lenDeletedInstances += len(defaultss_deletedInstances)
	var degrees_newInstances []*Degree
	var degrees_deletedInstances []*Degree

	// parse all staged instances and check if they have a reference
	for degree := range stage.Degrees {
		if ref, ok := stage.Degrees_reference[degree]; !ok {
			degrees_newInstances = append(degrees_newInstances, degree)
			newInstancesSlice = append(newInstancesSlice, degree.GongMarshallIdentifier(stage))
			if stage.Degrees_referenceOrder == nil {
				stage.Degrees_referenceOrder = make(map[*Degree]uint)
			}
			stage.Degrees_referenceOrder[degree] = stage.Degree_stagedOrder[degree]
			newInstancesReverseSlice = append(newInstancesReverseSlice, degree.GongMarshallUnstaging(stage))
			// delete(stage.Degrees_referenceOrder, degree)
			fieldInitializers, pointersInitializations := degree.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Degree_stagedOrder[ref] = stage.Degree_stagedOrder[degree]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := degree.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, degree)
			// delete(stage.Degree_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", degree.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Degrees_reference {
		instance := stage.Degrees_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Degrees[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			degrees_deletedInstances = append(degrees_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(degrees_newInstances)
	lenDeletedInstances += len(degrees_deletedInstances)
	var degree_alters_newInstances []*Degree_alter
	var degree_alters_deletedInstances []*Degree_alter

	// parse all staged instances and check if they have a reference
	for degree_alter := range stage.Degree_alters {
		if ref, ok := stage.Degree_alters_reference[degree_alter]; !ok {
			degree_alters_newInstances = append(degree_alters_newInstances, degree_alter)
			newInstancesSlice = append(newInstancesSlice, degree_alter.GongMarshallIdentifier(stage))
			if stage.Degree_alters_referenceOrder == nil {
				stage.Degree_alters_referenceOrder = make(map[*Degree_alter]uint)
			}
			stage.Degree_alters_referenceOrder[degree_alter] = stage.Degree_alter_stagedOrder[degree_alter]
			newInstancesReverseSlice = append(newInstancesReverseSlice, degree_alter.GongMarshallUnstaging(stage))
			// delete(stage.Degree_alters_referenceOrder, degree_alter)
			fieldInitializers, pointersInitializations := degree_alter.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Degree_alter_stagedOrder[ref] = stage.Degree_alter_stagedOrder[degree_alter]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := degree_alter.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, degree_alter)
			// delete(stage.Degree_alter_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", degree_alter.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Degree_alters_reference {
		instance := stage.Degree_alters_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Degree_alters[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			degree_alters_deletedInstances = append(degree_alters_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(degree_alters_newInstances)
	lenDeletedInstances += len(degree_alters_deletedInstances)
	var degree_types_newInstances []*Degree_type
	var degree_types_deletedInstances []*Degree_type

	// parse all staged instances and check if they have a reference
	for degree_type := range stage.Degree_types {
		if ref, ok := stage.Degree_types_reference[degree_type]; !ok {
			degree_types_newInstances = append(degree_types_newInstances, degree_type)
			newInstancesSlice = append(newInstancesSlice, degree_type.GongMarshallIdentifier(stage))
			if stage.Degree_types_referenceOrder == nil {
				stage.Degree_types_referenceOrder = make(map[*Degree_type]uint)
			}
			stage.Degree_types_referenceOrder[degree_type] = stage.Degree_type_stagedOrder[degree_type]
			newInstancesReverseSlice = append(newInstancesReverseSlice, degree_type.GongMarshallUnstaging(stage))
			// delete(stage.Degree_types_referenceOrder, degree_type)
			fieldInitializers, pointersInitializations := degree_type.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Degree_type_stagedOrder[ref] = stage.Degree_type_stagedOrder[degree_type]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := degree_type.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, degree_type)
			// delete(stage.Degree_type_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", degree_type.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Degree_types_reference {
		instance := stage.Degree_types_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Degree_types[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			degree_types_deletedInstances = append(degree_types_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(degree_types_newInstances)
	lenDeletedInstances += len(degree_types_deletedInstances)
	var degree_values_newInstances []*Degree_value
	var degree_values_deletedInstances []*Degree_value

	// parse all staged instances and check if they have a reference
	for degree_value := range stage.Degree_values {
		if ref, ok := stage.Degree_values_reference[degree_value]; !ok {
			degree_values_newInstances = append(degree_values_newInstances, degree_value)
			newInstancesSlice = append(newInstancesSlice, degree_value.GongMarshallIdentifier(stage))
			if stage.Degree_values_referenceOrder == nil {
				stage.Degree_values_referenceOrder = make(map[*Degree_value]uint)
			}
			stage.Degree_values_referenceOrder[degree_value] = stage.Degree_value_stagedOrder[degree_value]
			newInstancesReverseSlice = append(newInstancesReverseSlice, degree_value.GongMarshallUnstaging(stage))
			// delete(stage.Degree_values_referenceOrder, degree_value)
			fieldInitializers, pointersInitializations := degree_value.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Degree_value_stagedOrder[ref] = stage.Degree_value_stagedOrder[degree_value]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := degree_value.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, degree_value)
			// delete(stage.Degree_value_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", degree_value.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Degree_values_reference {
		instance := stage.Degree_values_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Degree_values[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			degree_values_deletedInstances = append(degree_values_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(degree_values_newInstances)
	lenDeletedInstances += len(degree_values_deletedInstances)
	var directions_newInstances []*Direction
	var directions_deletedInstances []*Direction

	// parse all staged instances and check if they have a reference
	for direction := range stage.Directions {
		if ref, ok := stage.Directions_reference[direction]; !ok {
			directions_newInstances = append(directions_newInstances, direction)
			newInstancesSlice = append(newInstancesSlice, direction.GongMarshallIdentifier(stage))
			if stage.Directions_referenceOrder == nil {
				stage.Directions_referenceOrder = make(map[*Direction]uint)
			}
			stage.Directions_referenceOrder[direction] = stage.Direction_stagedOrder[direction]
			newInstancesReverseSlice = append(newInstancesReverseSlice, direction.GongMarshallUnstaging(stage))
			// delete(stage.Directions_referenceOrder, direction)
			fieldInitializers, pointersInitializations := direction.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Direction_stagedOrder[ref] = stage.Direction_stagedOrder[direction]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := direction.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, direction)
			// delete(stage.Direction_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", direction.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Directions_reference {
		instance := stage.Directions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Directions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			directions_deletedInstances = append(directions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(directions_newInstances)
	lenDeletedInstances += len(directions_deletedInstances)
	var direction_types_newInstances []*Direction_type
	var direction_types_deletedInstances []*Direction_type

	// parse all staged instances and check if they have a reference
	for direction_type := range stage.Direction_types {
		if ref, ok := stage.Direction_types_reference[direction_type]; !ok {
			direction_types_newInstances = append(direction_types_newInstances, direction_type)
			newInstancesSlice = append(newInstancesSlice, direction_type.GongMarshallIdentifier(stage))
			if stage.Direction_types_referenceOrder == nil {
				stage.Direction_types_referenceOrder = make(map[*Direction_type]uint)
			}
			stage.Direction_types_referenceOrder[direction_type] = stage.Direction_type_stagedOrder[direction_type]
			newInstancesReverseSlice = append(newInstancesReverseSlice, direction_type.GongMarshallUnstaging(stage))
			// delete(stage.Direction_types_referenceOrder, direction_type)
			fieldInitializers, pointersInitializations := direction_type.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Direction_type_stagedOrder[ref] = stage.Direction_type_stagedOrder[direction_type]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := direction_type.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, direction_type)
			// delete(stage.Direction_type_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", direction_type.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Direction_types_reference {
		instance := stage.Direction_types_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Direction_types[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			direction_types_deletedInstances = append(direction_types_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(direction_types_newInstances)
	lenDeletedInstances += len(direction_types_deletedInstances)
	var distances_newInstances []*Distance
	var distances_deletedInstances []*Distance

	// parse all staged instances and check if they have a reference
	for distance := range stage.Distances {
		if ref, ok := stage.Distances_reference[distance]; !ok {
			distances_newInstances = append(distances_newInstances, distance)
			newInstancesSlice = append(newInstancesSlice, distance.GongMarshallIdentifier(stage))
			if stage.Distances_referenceOrder == nil {
				stage.Distances_referenceOrder = make(map[*Distance]uint)
			}
			stage.Distances_referenceOrder[distance] = stage.Distance_stagedOrder[distance]
			newInstancesReverseSlice = append(newInstancesReverseSlice, distance.GongMarshallUnstaging(stage))
			// delete(stage.Distances_referenceOrder, distance)
			fieldInitializers, pointersInitializations := distance.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Distance_stagedOrder[ref] = stage.Distance_stagedOrder[distance]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := distance.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, distance)
			// delete(stage.Distance_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", distance.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Distances_reference {
		instance := stage.Distances_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Distances[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			distances_deletedInstances = append(distances_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(distances_newInstances)
	lenDeletedInstances += len(distances_deletedInstances)
	var doubles_newInstances []*Double
	var doubles_deletedInstances []*Double

	// parse all staged instances and check if they have a reference
	for double := range stage.Doubles {
		if ref, ok := stage.Doubles_reference[double]; !ok {
			doubles_newInstances = append(doubles_newInstances, double)
			newInstancesSlice = append(newInstancesSlice, double.GongMarshallIdentifier(stage))
			if stage.Doubles_referenceOrder == nil {
				stage.Doubles_referenceOrder = make(map[*Double]uint)
			}
			stage.Doubles_referenceOrder[double] = stage.Double_stagedOrder[double]
			newInstancesReverseSlice = append(newInstancesReverseSlice, double.GongMarshallUnstaging(stage))
			// delete(stage.Doubles_referenceOrder, double)
			fieldInitializers, pointersInitializations := double.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Double_stagedOrder[ref] = stage.Double_stagedOrder[double]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := double.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, double)
			// delete(stage.Double_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", double.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Doubles_reference {
		instance := stage.Doubles_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Doubles[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			doubles_deletedInstances = append(doubles_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(doubles_newInstances)
	lenDeletedInstances += len(doubles_deletedInstances)
	var dynamicss_newInstances []*Dynamics
	var dynamicss_deletedInstances []*Dynamics

	// parse all staged instances and check if they have a reference
	for dynamics := range stage.Dynamicss {
		if ref, ok := stage.Dynamicss_reference[dynamics]; !ok {
			dynamicss_newInstances = append(dynamicss_newInstances, dynamics)
			newInstancesSlice = append(newInstancesSlice, dynamics.GongMarshallIdentifier(stage))
			if stage.Dynamicss_referenceOrder == nil {
				stage.Dynamicss_referenceOrder = make(map[*Dynamics]uint)
			}
			stage.Dynamicss_referenceOrder[dynamics] = stage.Dynamics_stagedOrder[dynamics]
			newInstancesReverseSlice = append(newInstancesReverseSlice, dynamics.GongMarshallUnstaging(stage))
			// delete(stage.Dynamicss_referenceOrder, dynamics)
			fieldInitializers, pointersInitializations := dynamics.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Dynamics_stagedOrder[ref] = stage.Dynamics_stagedOrder[dynamics]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := dynamics.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, dynamics)
			// delete(stage.Dynamics_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", dynamics.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Dynamicss_reference {
		instance := stage.Dynamicss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Dynamicss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			dynamicss_deletedInstances = append(dynamicss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(dynamicss_newInstances)
	lenDeletedInstances += len(dynamicss_deletedInstances)
	var effects_newInstances []*Effect
	var effects_deletedInstances []*Effect

	// parse all staged instances and check if they have a reference
	for effect := range stage.Effects {
		if ref, ok := stage.Effects_reference[effect]; !ok {
			effects_newInstances = append(effects_newInstances, effect)
			newInstancesSlice = append(newInstancesSlice, effect.GongMarshallIdentifier(stage))
			if stage.Effects_referenceOrder == nil {
				stage.Effects_referenceOrder = make(map[*Effect]uint)
			}
			stage.Effects_referenceOrder[effect] = stage.Effect_stagedOrder[effect]
			newInstancesReverseSlice = append(newInstancesReverseSlice, effect.GongMarshallUnstaging(stage))
			// delete(stage.Effects_referenceOrder, effect)
			fieldInitializers, pointersInitializations := effect.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Effect_stagedOrder[ref] = stage.Effect_stagedOrder[effect]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := effect.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, effect)
			// delete(stage.Effect_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", effect.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Effects_reference {
		instance := stage.Effects_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Effects[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			effects_deletedInstances = append(effects_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(effects_newInstances)
	lenDeletedInstances += len(effects_deletedInstances)
	var elisions_newInstances []*Elision
	var elisions_deletedInstances []*Elision

	// parse all staged instances and check if they have a reference
	for elision := range stage.Elisions {
		if ref, ok := stage.Elisions_reference[elision]; !ok {
			elisions_newInstances = append(elisions_newInstances, elision)
			newInstancesSlice = append(newInstancesSlice, elision.GongMarshallIdentifier(stage))
			if stage.Elisions_referenceOrder == nil {
				stage.Elisions_referenceOrder = make(map[*Elision]uint)
			}
			stage.Elisions_referenceOrder[elision] = stage.Elision_stagedOrder[elision]
			newInstancesReverseSlice = append(newInstancesReverseSlice, elision.GongMarshallUnstaging(stage))
			// delete(stage.Elisions_referenceOrder, elision)
			fieldInitializers, pointersInitializations := elision.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Elision_stagedOrder[ref] = stage.Elision_stagedOrder[elision]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := elision.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, elision)
			// delete(stage.Elision_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", elision.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Elisions_reference {
		instance := stage.Elisions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Elisions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			elisions_deletedInstances = append(elisions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(elisions_newInstances)
	lenDeletedInstances += len(elisions_deletedInstances)
	var emptys_newInstances []*Empty
	var emptys_deletedInstances []*Empty

	// parse all staged instances and check if they have a reference
	for empty := range stage.Emptys {
		if ref, ok := stage.Emptys_reference[empty]; !ok {
			emptys_newInstances = append(emptys_newInstances, empty)
			newInstancesSlice = append(newInstancesSlice, empty.GongMarshallIdentifier(stage))
			if stage.Emptys_referenceOrder == nil {
				stage.Emptys_referenceOrder = make(map[*Empty]uint)
			}
			stage.Emptys_referenceOrder[empty] = stage.Empty_stagedOrder[empty]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty.GongMarshallUnstaging(stage))
			// delete(stage.Emptys_referenceOrder, empty)
			fieldInitializers, pointersInitializations := empty.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_stagedOrder[ref] = stage.Empty_stagedOrder[empty]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty)
			// delete(stage.Empty_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Emptys_reference {
		instance := stage.Emptys_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Emptys[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			emptys_deletedInstances = append(emptys_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(emptys_newInstances)
	lenDeletedInstances += len(emptys_deletedInstances)
	var empty_fonts_newInstances []*Empty_font
	var empty_fonts_deletedInstances []*Empty_font

	// parse all staged instances and check if they have a reference
	for empty_font := range stage.Empty_fonts {
		if ref, ok := stage.Empty_fonts_reference[empty_font]; !ok {
			empty_fonts_newInstances = append(empty_fonts_newInstances, empty_font)
			newInstancesSlice = append(newInstancesSlice, empty_font.GongMarshallIdentifier(stage))
			if stage.Empty_fonts_referenceOrder == nil {
				stage.Empty_fonts_referenceOrder = make(map[*Empty_font]uint)
			}
			stage.Empty_fonts_referenceOrder[empty_font] = stage.Empty_font_stagedOrder[empty_font]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_font.GongMarshallUnstaging(stage))
			// delete(stage.Empty_fonts_referenceOrder, empty_font)
			fieldInitializers, pointersInitializations := empty_font.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_font_stagedOrder[ref] = stage.Empty_font_stagedOrder[empty_font]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_font.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_font)
			// delete(stage.Empty_font_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_font.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_fonts_reference {
		instance := stage.Empty_fonts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_fonts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_fonts_deletedInstances = append(empty_fonts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_fonts_newInstances)
	lenDeletedInstances += len(empty_fonts_deletedInstances)
	var empty_lines_newInstances []*Empty_line
	var empty_lines_deletedInstances []*Empty_line

	// parse all staged instances and check if they have a reference
	for empty_line := range stage.Empty_lines {
		if ref, ok := stage.Empty_lines_reference[empty_line]; !ok {
			empty_lines_newInstances = append(empty_lines_newInstances, empty_line)
			newInstancesSlice = append(newInstancesSlice, empty_line.GongMarshallIdentifier(stage))
			if stage.Empty_lines_referenceOrder == nil {
				stage.Empty_lines_referenceOrder = make(map[*Empty_line]uint)
			}
			stage.Empty_lines_referenceOrder[empty_line] = stage.Empty_line_stagedOrder[empty_line]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_line.GongMarshallUnstaging(stage))
			// delete(stage.Empty_lines_referenceOrder, empty_line)
			fieldInitializers, pointersInitializations := empty_line.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_line_stagedOrder[ref] = stage.Empty_line_stagedOrder[empty_line]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_line.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_line)
			// delete(stage.Empty_line_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_line.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_lines_reference {
		instance := stage.Empty_lines_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_lines[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_lines_deletedInstances = append(empty_lines_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_lines_newInstances)
	lenDeletedInstances += len(empty_lines_deletedInstances)
	var empty_placements_newInstances []*Empty_placement
	var empty_placements_deletedInstances []*Empty_placement

	// parse all staged instances and check if they have a reference
	for empty_placement := range stage.Empty_placements {
		if ref, ok := stage.Empty_placements_reference[empty_placement]; !ok {
			empty_placements_newInstances = append(empty_placements_newInstances, empty_placement)
			newInstancesSlice = append(newInstancesSlice, empty_placement.GongMarshallIdentifier(stage))
			if stage.Empty_placements_referenceOrder == nil {
				stage.Empty_placements_referenceOrder = make(map[*Empty_placement]uint)
			}
			stage.Empty_placements_referenceOrder[empty_placement] = stage.Empty_placement_stagedOrder[empty_placement]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_placement.GongMarshallUnstaging(stage))
			// delete(stage.Empty_placements_referenceOrder, empty_placement)
			fieldInitializers, pointersInitializations := empty_placement.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_placement_stagedOrder[ref] = stage.Empty_placement_stagedOrder[empty_placement]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_placement.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_placement)
			// delete(stage.Empty_placement_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_placement.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_placements_reference {
		instance := stage.Empty_placements_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_placements[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_placements_deletedInstances = append(empty_placements_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_placements_newInstances)
	lenDeletedInstances += len(empty_placements_deletedInstances)
	var empty_placement_smufls_newInstances []*Empty_placement_smufl
	var empty_placement_smufls_deletedInstances []*Empty_placement_smufl

	// parse all staged instances and check if they have a reference
	for empty_placement_smufl := range stage.Empty_placement_smufls {
		if ref, ok := stage.Empty_placement_smufls_reference[empty_placement_smufl]; !ok {
			empty_placement_smufls_newInstances = append(empty_placement_smufls_newInstances, empty_placement_smufl)
			newInstancesSlice = append(newInstancesSlice, empty_placement_smufl.GongMarshallIdentifier(stage))
			if stage.Empty_placement_smufls_referenceOrder == nil {
				stage.Empty_placement_smufls_referenceOrder = make(map[*Empty_placement_smufl]uint)
			}
			stage.Empty_placement_smufls_referenceOrder[empty_placement_smufl] = stage.Empty_placement_smufl_stagedOrder[empty_placement_smufl]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_placement_smufl.GongMarshallUnstaging(stage))
			// delete(stage.Empty_placement_smufls_referenceOrder, empty_placement_smufl)
			fieldInitializers, pointersInitializations := empty_placement_smufl.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_placement_smufl_stagedOrder[ref] = stage.Empty_placement_smufl_stagedOrder[empty_placement_smufl]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_placement_smufl.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_placement_smufl)
			// delete(stage.Empty_placement_smufl_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_placement_smufl.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_placement_smufls_reference {
		instance := stage.Empty_placement_smufls_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_placement_smufls[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_placement_smufls_deletedInstances = append(empty_placement_smufls_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_placement_smufls_newInstances)
	lenDeletedInstances += len(empty_placement_smufls_deletedInstances)
	var empty_print_object_style_aligns_newInstances []*Empty_print_object_style_align
	var empty_print_object_style_aligns_deletedInstances []*Empty_print_object_style_align

	// parse all staged instances and check if they have a reference
	for empty_print_object_style_align := range stage.Empty_print_object_style_aligns {
		if ref, ok := stage.Empty_print_object_style_aligns_reference[empty_print_object_style_align]; !ok {
			empty_print_object_style_aligns_newInstances = append(empty_print_object_style_aligns_newInstances, empty_print_object_style_align)
			newInstancesSlice = append(newInstancesSlice, empty_print_object_style_align.GongMarshallIdentifier(stage))
			if stage.Empty_print_object_style_aligns_referenceOrder == nil {
				stage.Empty_print_object_style_aligns_referenceOrder = make(map[*Empty_print_object_style_align]uint)
			}
			stage.Empty_print_object_style_aligns_referenceOrder[empty_print_object_style_align] = stage.Empty_print_object_style_align_stagedOrder[empty_print_object_style_align]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_print_object_style_align.GongMarshallUnstaging(stage))
			// delete(stage.Empty_print_object_style_aligns_referenceOrder, empty_print_object_style_align)
			fieldInitializers, pointersInitializations := empty_print_object_style_align.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_print_object_style_align_stagedOrder[ref] = stage.Empty_print_object_style_align_stagedOrder[empty_print_object_style_align]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_print_object_style_align.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_print_object_style_align)
			// delete(stage.Empty_print_object_style_align_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_print_object_style_align.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_print_object_style_aligns_reference {
		instance := stage.Empty_print_object_style_aligns_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_print_object_style_aligns[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_print_object_style_aligns_deletedInstances = append(empty_print_object_style_aligns_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_print_object_style_aligns_newInstances)
	lenDeletedInstances += len(empty_print_object_style_aligns_deletedInstances)
	var empty_print_styles_newInstances []*Empty_print_style
	var empty_print_styles_deletedInstances []*Empty_print_style

	// parse all staged instances and check if they have a reference
	for empty_print_style := range stage.Empty_print_styles {
		if ref, ok := stage.Empty_print_styles_reference[empty_print_style]; !ok {
			empty_print_styles_newInstances = append(empty_print_styles_newInstances, empty_print_style)
			newInstancesSlice = append(newInstancesSlice, empty_print_style.GongMarshallIdentifier(stage))
			if stage.Empty_print_styles_referenceOrder == nil {
				stage.Empty_print_styles_referenceOrder = make(map[*Empty_print_style]uint)
			}
			stage.Empty_print_styles_referenceOrder[empty_print_style] = stage.Empty_print_style_stagedOrder[empty_print_style]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_print_style.GongMarshallUnstaging(stage))
			// delete(stage.Empty_print_styles_referenceOrder, empty_print_style)
			fieldInitializers, pointersInitializations := empty_print_style.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_print_style_stagedOrder[ref] = stage.Empty_print_style_stagedOrder[empty_print_style]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_print_style.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_print_style)
			// delete(stage.Empty_print_style_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_print_style.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_print_styles_reference {
		instance := stage.Empty_print_styles_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_print_styles[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_print_styles_deletedInstances = append(empty_print_styles_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_print_styles_newInstances)
	lenDeletedInstances += len(empty_print_styles_deletedInstances)
	var empty_print_style_aligns_newInstances []*Empty_print_style_align
	var empty_print_style_aligns_deletedInstances []*Empty_print_style_align

	// parse all staged instances and check if they have a reference
	for empty_print_style_align := range stage.Empty_print_style_aligns {
		if ref, ok := stage.Empty_print_style_aligns_reference[empty_print_style_align]; !ok {
			empty_print_style_aligns_newInstances = append(empty_print_style_aligns_newInstances, empty_print_style_align)
			newInstancesSlice = append(newInstancesSlice, empty_print_style_align.GongMarshallIdentifier(stage))
			if stage.Empty_print_style_aligns_referenceOrder == nil {
				stage.Empty_print_style_aligns_referenceOrder = make(map[*Empty_print_style_align]uint)
			}
			stage.Empty_print_style_aligns_referenceOrder[empty_print_style_align] = stage.Empty_print_style_align_stagedOrder[empty_print_style_align]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_print_style_align.GongMarshallUnstaging(stage))
			// delete(stage.Empty_print_style_aligns_referenceOrder, empty_print_style_align)
			fieldInitializers, pointersInitializations := empty_print_style_align.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_print_style_align_stagedOrder[ref] = stage.Empty_print_style_align_stagedOrder[empty_print_style_align]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_print_style_align.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_print_style_align)
			// delete(stage.Empty_print_style_align_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_print_style_align.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_print_style_aligns_reference {
		instance := stage.Empty_print_style_aligns_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_print_style_aligns[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_print_style_aligns_deletedInstances = append(empty_print_style_aligns_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_print_style_aligns_newInstances)
	lenDeletedInstances += len(empty_print_style_aligns_deletedInstances)
	var empty_print_style_align_ids_newInstances []*Empty_print_style_align_id
	var empty_print_style_align_ids_deletedInstances []*Empty_print_style_align_id

	// parse all staged instances and check if they have a reference
	for empty_print_style_align_id := range stage.Empty_print_style_align_ids {
		if ref, ok := stage.Empty_print_style_align_ids_reference[empty_print_style_align_id]; !ok {
			empty_print_style_align_ids_newInstances = append(empty_print_style_align_ids_newInstances, empty_print_style_align_id)
			newInstancesSlice = append(newInstancesSlice, empty_print_style_align_id.GongMarshallIdentifier(stage))
			if stage.Empty_print_style_align_ids_referenceOrder == nil {
				stage.Empty_print_style_align_ids_referenceOrder = make(map[*Empty_print_style_align_id]uint)
			}
			stage.Empty_print_style_align_ids_referenceOrder[empty_print_style_align_id] = stage.Empty_print_style_align_id_stagedOrder[empty_print_style_align_id]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_print_style_align_id.GongMarshallUnstaging(stage))
			// delete(stage.Empty_print_style_align_ids_referenceOrder, empty_print_style_align_id)
			fieldInitializers, pointersInitializations := empty_print_style_align_id.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_print_style_align_id_stagedOrder[ref] = stage.Empty_print_style_align_id_stagedOrder[empty_print_style_align_id]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_print_style_align_id.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_print_style_align_id)
			// delete(stage.Empty_print_style_align_id_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_print_style_align_id.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_print_style_align_ids_reference {
		instance := stage.Empty_print_style_align_ids_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_print_style_align_ids[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_print_style_align_ids_deletedInstances = append(empty_print_style_align_ids_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_print_style_align_ids_newInstances)
	lenDeletedInstances += len(empty_print_style_align_ids_deletedInstances)
	var empty_trill_sounds_newInstances []*Empty_trill_sound
	var empty_trill_sounds_deletedInstances []*Empty_trill_sound

	// parse all staged instances and check if they have a reference
	for empty_trill_sound := range stage.Empty_trill_sounds {
		if ref, ok := stage.Empty_trill_sounds_reference[empty_trill_sound]; !ok {
			empty_trill_sounds_newInstances = append(empty_trill_sounds_newInstances, empty_trill_sound)
			newInstancesSlice = append(newInstancesSlice, empty_trill_sound.GongMarshallIdentifier(stage))
			if stage.Empty_trill_sounds_referenceOrder == nil {
				stage.Empty_trill_sounds_referenceOrder = make(map[*Empty_trill_sound]uint)
			}
			stage.Empty_trill_sounds_referenceOrder[empty_trill_sound] = stage.Empty_trill_sound_stagedOrder[empty_trill_sound]
			newInstancesReverseSlice = append(newInstancesReverseSlice, empty_trill_sound.GongMarshallUnstaging(stage))
			// delete(stage.Empty_trill_sounds_referenceOrder, empty_trill_sound)
			fieldInitializers, pointersInitializations := empty_trill_sound.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Empty_trill_sound_stagedOrder[ref] = stage.Empty_trill_sound_stagedOrder[empty_trill_sound]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := empty_trill_sound.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, empty_trill_sound)
			// delete(stage.Empty_trill_sound_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", empty_trill_sound.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Empty_trill_sounds_reference {
		instance := stage.Empty_trill_sounds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Empty_trill_sounds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			empty_trill_sounds_deletedInstances = append(empty_trill_sounds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(empty_trill_sounds_newInstances)
	lenDeletedInstances += len(empty_trill_sounds_deletedInstances)
	var encodings_newInstances []*Encoding
	var encodings_deletedInstances []*Encoding

	// parse all staged instances and check if they have a reference
	for encoding := range stage.Encodings {
		if ref, ok := stage.Encodings_reference[encoding]; !ok {
			encodings_newInstances = append(encodings_newInstances, encoding)
			newInstancesSlice = append(newInstancesSlice, encoding.GongMarshallIdentifier(stage))
			if stage.Encodings_referenceOrder == nil {
				stage.Encodings_referenceOrder = make(map[*Encoding]uint)
			}
			stage.Encodings_referenceOrder[encoding] = stage.Encoding_stagedOrder[encoding]
			newInstancesReverseSlice = append(newInstancesReverseSlice, encoding.GongMarshallUnstaging(stage))
			// delete(stage.Encodings_referenceOrder, encoding)
			fieldInitializers, pointersInitializations := encoding.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Encoding_stagedOrder[ref] = stage.Encoding_stagedOrder[encoding]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := encoding.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, encoding)
			// delete(stage.Encoding_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", encoding.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Encodings_reference {
		instance := stage.Encodings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Encodings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			encodings_deletedInstances = append(encodings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(encodings_newInstances)
	lenDeletedInstances += len(encodings_deletedInstances)
	var endings_newInstances []*Ending
	var endings_deletedInstances []*Ending

	// parse all staged instances and check if they have a reference
	for ending := range stage.Endings {
		if ref, ok := stage.Endings_reference[ending]; !ok {
			endings_newInstances = append(endings_newInstances, ending)
			newInstancesSlice = append(newInstancesSlice, ending.GongMarshallIdentifier(stage))
			if stage.Endings_referenceOrder == nil {
				stage.Endings_referenceOrder = make(map[*Ending]uint)
			}
			stage.Endings_referenceOrder[ending] = stage.Ending_stagedOrder[ending]
			newInstancesReverseSlice = append(newInstancesReverseSlice, ending.GongMarshallUnstaging(stage))
			// delete(stage.Endings_referenceOrder, ending)
			fieldInitializers, pointersInitializations := ending.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Ending_stagedOrder[ref] = stage.Ending_stagedOrder[ending]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := ending.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, ending)
			// delete(stage.Ending_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", ending.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Endings_reference {
		instance := stage.Endings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Endings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			endings_deletedInstances = append(endings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(endings_newInstances)
	lenDeletedInstances += len(endings_deletedInstances)
	var extends_newInstances []*Extend
	var extends_deletedInstances []*Extend

	// parse all staged instances and check if they have a reference
	for extend := range stage.Extends {
		if ref, ok := stage.Extends_reference[extend]; !ok {
			extends_newInstances = append(extends_newInstances, extend)
			newInstancesSlice = append(newInstancesSlice, extend.GongMarshallIdentifier(stage))
			if stage.Extends_referenceOrder == nil {
				stage.Extends_referenceOrder = make(map[*Extend]uint)
			}
			stage.Extends_referenceOrder[extend] = stage.Extend_stagedOrder[extend]
			newInstancesReverseSlice = append(newInstancesReverseSlice, extend.GongMarshallUnstaging(stage))
			// delete(stage.Extends_referenceOrder, extend)
			fieldInitializers, pointersInitializations := extend.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Extend_stagedOrder[ref] = stage.Extend_stagedOrder[extend]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := extend.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, extend)
			// delete(stage.Extend_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", extend.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Extends_reference {
		instance := stage.Extends_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Extends[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			extends_deletedInstances = append(extends_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(extends_newInstances)
	lenDeletedInstances += len(extends_deletedInstances)
	var features_newInstances []*Feature
	var features_deletedInstances []*Feature

	// parse all staged instances and check if they have a reference
	for feature := range stage.Features {
		if ref, ok := stage.Features_reference[feature]; !ok {
			features_newInstances = append(features_newInstances, feature)
			newInstancesSlice = append(newInstancesSlice, feature.GongMarshallIdentifier(stage))
			if stage.Features_referenceOrder == nil {
				stage.Features_referenceOrder = make(map[*Feature]uint)
			}
			stage.Features_referenceOrder[feature] = stage.Feature_stagedOrder[feature]
			newInstancesReverseSlice = append(newInstancesReverseSlice, feature.GongMarshallUnstaging(stage))
			// delete(stage.Features_referenceOrder, feature)
			fieldInitializers, pointersInitializations := feature.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Feature_stagedOrder[ref] = stage.Feature_stagedOrder[feature]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := feature.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, feature)
			// delete(stage.Feature_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", feature.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Features_reference {
		instance := stage.Features_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Features[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			features_deletedInstances = append(features_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(features_newInstances)
	lenDeletedInstances += len(features_deletedInstances)
	var fermatas_newInstances []*Fermata
	var fermatas_deletedInstances []*Fermata

	// parse all staged instances and check if they have a reference
	for fermata := range stage.Fermatas {
		if ref, ok := stage.Fermatas_reference[fermata]; !ok {
			fermatas_newInstances = append(fermatas_newInstances, fermata)
			newInstancesSlice = append(newInstancesSlice, fermata.GongMarshallIdentifier(stage))
			if stage.Fermatas_referenceOrder == nil {
				stage.Fermatas_referenceOrder = make(map[*Fermata]uint)
			}
			stage.Fermatas_referenceOrder[fermata] = stage.Fermata_stagedOrder[fermata]
			newInstancesReverseSlice = append(newInstancesReverseSlice, fermata.GongMarshallUnstaging(stage))
			// delete(stage.Fermatas_referenceOrder, fermata)
			fieldInitializers, pointersInitializations := fermata.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Fermata_stagedOrder[ref] = stage.Fermata_stagedOrder[fermata]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := fermata.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, fermata)
			// delete(stage.Fermata_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", fermata.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Fermatas_reference {
		instance := stage.Fermatas_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Fermatas[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			fermatas_deletedInstances = append(fermatas_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(fermatas_newInstances)
	lenDeletedInstances += len(fermatas_deletedInstances)
	var figures_newInstances []*Figure
	var figures_deletedInstances []*Figure

	// parse all staged instances and check if they have a reference
	for figure := range stage.Figures {
		if ref, ok := stage.Figures_reference[figure]; !ok {
			figures_newInstances = append(figures_newInstances, figure)
			newInstancesSlice = append(newInstancesSlice, figure.GongMarshallIdentifier(stage))
			if stage.Figures_referenceOrder == nil {
				stage.Figures_referenceOrder = make(map[*Figure]uint)
			}
			stage.Figures_referenceOrder[figure] = stage.Figure_stagedOrder[figure]
			newInstancesReverseSlice = append(newInstancesReverseSlice, figure.GongMarshallUnstaging(stage))
			// delete(stage.Figures_referenceOrder, figure)
			fieldInitializers, pointersInitializations := figure.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Figure_stagedOrder[ref] = stage.Figure_stagedOrder[figure]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := figure.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, figure)
			// delete(stage.Figure_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", figure.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Figures_reference {
		instance := stage.Figures_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Figures[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			figures_deletedInstances = append(figures_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(figures_newInstances)
	lenDeletedInstances += len(figures_deletedInstances)
	var figured_basss_newInstances []*Figured_bass
	var figured_basss_deletedInstances []*Figured_bass

	// parse all staged instances and check if they have a reference
	for figured_bass := range stage.Figured_basss {
		if ref, ok := stage.Figured_basss_reference[figured_bass]; !ok {
			figured_basss_newInstances = append(figured_basss_newInstances, figured_bass)
			newInstancesSlice = append(newInstancesSlice, figured_bass.GongMarshallIdentifier(stage))
			if stage.Figured_basss_referenceOrder == nil {
				stage.Figured_basss_referenceOrder = make(map[*Figured_bass]uint)
			}
			stage.Figured_basss_referenceOrder[figured_bass] = stage.Figured_bass_stagedOrder[figured_bass]
			newInstancesReverseSlice = append(newInstancesReverseSlice, figured_bass.GongMarshallUnstaging(stage))
			// delete(stage.Figured_basss_referenceOrder, figured_bass)
			fieldInitializers, pointersInitializations := figured_bass.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Figured_bass_stagedOrder[ref] = stage.Figured_bass_stagedOrder[figured_bass]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := figured_bass.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, figured_bass)
			// delete(stage.Figured_bass_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", figured_bass.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Figured_basss_reference {
		instance := stage.Figured_basss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Figured_basss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			figured_basss_deletedInstances = append(figured_basss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(figured_basss_newInstances)
	lenDeletedInstances += len(figured_basss_deletedInstances)
	var fingerings_newInstances []*Fingering
	var fingerings_deletedInstances []*Fingering

	// parse all staged instances and check if they have a reference
	for fingering := range stage.Fingerings {
		if ref, ok := stage.Fingerings_reference[fingering]; !ok {
			fingerings_newInstances = append(fingerings_newInstances, fingering)
			newInstancesSlice = append(newInstancesSlice, fingering.GongMarshallIdentifier(stage))
			if stage.Fingerings_referenceOrder == nil {
				stage.Fingerings_referenceOrder = make(map[*Fingering]uint)
			}
			stage.Fingerings_referenceOrder[fingering] = stage.Fingering_stagedOrder[fingering]
			newInstancesReverseSlice = append(newInstancesReverseSlice, fingering.GongMarshallUnstaging(stage))
			// delete(stage.Fingerings_referenceOrder, fingering)
			fieldInitializers, pointersInitializations := fingering.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Fingering_stagedOrder[ref] = stage.Fingering_stagedOrder[fingering]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := fingering.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, fingering)
			// delete(stage.Fingering_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", fingering.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Fingerings_reference {
		instance := stage.Fingerings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Fingerings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			fingerings_deletedInstances = append(fingerings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(fingerings_newInstances)
	lenDeletedInstances += len(fingerings_deletedInstances)
	var first_frets_newInstances []*First_fret
	var first_frets_deletedInstances []*First_fret

	// parse all staged instances and check if they have a reference
	for first_fret := range stage.First_frets {
		if ref, ok := stage.First_frets_reference[first_fret]; !ok {
			first_frets_newInstances = append(first_frets_newInstances, first_fret)
			newInstancesSlice = append(newInstancesSlice, first_fret.GongMarshallIdentifier(stage))
			if stage.First_frets_referenceOrder == nil {
				stage.First_frets_referenceOrder = make(map[*First_fret]uint)
			}
			stage.First_frets_referenceOrder[first_fret] = stage.First_fret_stagedOrder[first_fret]
			newInstancesReverseSlice = append(newInstancesReverseSlice, first_fret.GongMarshallUnstaging(stage))
			// delete(stage.First_frets_referenceOrder, first_fret)
			fieldInitializers, pointersInitializations := first_fret.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.First_fret_stagedOrder[ref] = stage.First_fret_stagedOrder[first_fret]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := first_fret.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, first_fret)
			// delete(stage.First_fret_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", first_fret.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.First_frets_reference {
		instance := stage.First_frets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.First_frets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			first_frets_deletedInstances = append(first_frets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(first_frets_newInstances)
	lenDeletedInstances += len(first_frets_deletedInstances)
	var for_parts_newInstances []*For_part
	var for_parts_deletedInstances []*For_part

	// parse all staged instances and check if they have a reference
	for for_part := range stage.For_parts {
		if ref, ok := stage.For_parts_reference[for_part]; !ok {
			for_parts_newInstances = append(for_parts_newInstances, for_part)
			newInstancesSlice = append(newInstancesSlice, for_part.GongMarshallIdentifier(stage))
			if stage.For_parts_referenceOrder == nil {
				stage.For_parts_referenceOrder = make(map[*For_part]uint)
			}
			stage.For_parts_referenceOrder[for_part] = stage.For_part_stagedOrder[for_part]
			newInstancesReverseSlice = append(newInstancesReverseSlice, for_part.GongMarshallUnstaging(stage))
			// delete(stage.For_parts_referenceOrder, for_part)
			fieldInitializers, pointersInitializations := for_part.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.For_part_stagedOrder[ref] = stage.For_part_stagedOrder[for_part]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := for_part.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, for_part)
			// delete(stage.For_part_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", for_part.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.For_parts_reference {
		instance := stage.For_parts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.For_parts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			for_parts_deletedInstances = append(for_parts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(for_parts_newInstances)
	lenDeletedInstances += len(for_parts_deletedInstances)
	var formatted_symbols_newInstances []*Formatted_symbol
	var formatted_symbols_deletedInstances []*Formatted_symbol

	// parse all staged instances and check if they have a reference
	for formatted_symbol := range stage.Formatted_symbols {
		if ref, ok := stage.Formatted_symbols_reference[formatted_symbol]; !ok {
			formatted_symbols_newInstances = append(formatted_symbols_newInstances, formatted_symbol)
			newInstancesSlice = append(newInstancesSlice, formatted_symbol.GongMarshallIdentifier(stage))
			if stage.Formatted_symbols_referenceOrder == nil {
				stage.Formatted_symbols_referenceOrder = make(map[*Formatted_symbol]uint)
			}
			stage.Formatted_symbols_referenceOrder[formatted_symbol] = stage.Formatted_symbol_stagedOrder[formatted_symbol]
			newInstancesReverseSlice = append(newInstancesReverseSlice, formatted_symbol.GongMarshallUnstaging(stage))
			// delete(stage.Formatted_symbols_referenceOrder, formatted_symbol)
			fieldInitializers, pointersInitializations := formatted_symbol.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Formatted_symbol_stagedOrder[ref] = stage.Formatted_symbol_stagedOrder[formatted_symbol]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := formatted_symbol.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, formatted_symbol)
			// delete(stage.Formatted_symbol_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", formatted_symbol.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Formatted_symbols_reference {
		instance := stage.Formatted_symbols_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Formatted_symbols[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			formatted_symbols_deletedInstances = append(formatted_symbols_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(formatted_symbols_newInstances)
	lenDeletedInstances += len(formatted_symbols_deletedInstances)
	var formatted_symbol_ids_newInstances []*Formatted_symbol_id
	var formatted_symbol_ids_deletedInstances []*Formatted_symbol_id

	// parse all staged instances and check if they have a reference
	for formatted_symbol_id := range stage.Formatted_symbol_ids {
		if ref, ok := stage.Formatted_symbol_ids_reference[formatted_symbol_id]; !ok {
			formatted_symbol_ids_newInstances = append(formatted_symbol_ids_newInstances, formatted_symbol_id)
			newInstancesSlice = append(newInstancesSlice, formatted_symbol_id.GongMarshallIdentifier(stage))
			if stage.Formatted_symbol_ids_referenceOrder == nil {
				stage.Formatted_symbol_ids_referenceOrder = make(map[*Formatted_symbol_id]uint)
			}
			stage.Formatted_symbol_ids_referenceOrder[formatted_symbol_id] = stage.Formatted_symbol_id_stagedOrder[formatted_symbol_id]
			newInstancesReverseSlice = append(newInstancesReverseSlice, formatted_symbol_id.GongMarshallUnstaging(stage))
			// delete(stage.Formatted_symbol_ids_referenceOrder, formatted_symbol_id)
			fieldInitializers, pointersInitializations := formatted_symbol_id.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Formatted_symbol_id_stagedOrder[ref] = stage.Formatted_symbol_id_stagedOrder[formatted_symbol_id]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := formatted_symbol_id.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, formatted_symbol_id)
			// delete(stage.Formatted_symbol_id_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", formatted_symbol_id.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Formatted_symbol_ids_reference {
		instance := stage.Formatted_symbol_ids_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Formatted_symbol_ids[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			formatted_symbol_ids_deletedInstances = append(formatted_symbol_ids_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(formatted_symbol_ids_newInstances)
	lenDeletedInstances += len(formatted_symbol_ids_deletedInstances)
	var formatted_texts_newInstances []*Formatted_text
	var formatted_texts_deletedInstances []*Formatted_text

	// parse all staged instances and check if they have a reference
	for formatted_text := range stage.Formatted_texts {
		if ref, ok := stage.Formatted_texts_reference[formatted_text]; !ok {
			formatted_texts_newInstances = append(formatted_texts_newInstances, formatted_text)
			newInstancesSlice = append(newInstancesSlice, formatted_text.GongMarshallIdentifier(stage))
			if stage.Formatted_texts_referenceOrder == nil {
				stage.Formatted_texts_referenceOrder = make(map[*Formatted_text]uint)
			}
			stage.Formatted_texts_referenceOrder[formatted_text] = stage.Formatted_text_stagedOrder[formatted_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, formatted_text.GongMarshallUnstaging(stage))
			// delete(stage.Formatted_texts_referenceOrder, formatted_text)
			fieldInitializers, pointersInitializations := formatted_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Formatted_text_stagedOrder[ref] = stage.Formatted_text_stagedOrder[formatted_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := formatted_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, formatted_text)
			// delete(stage.Formatted_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", formatted_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Formatted_texts_reference {
		instance := stage.Formatted_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Formatted_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			formatted_texts_deletedInstances = append(formatted_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(formatted_texts_newInstances)
	lenDeletedInstances += len(formatted_texts_deletedInstances)
	var formatted_text_ids_newInstances []*Formatted_text_id
	var formatted_text_ids_deletedInstances []*Formatted_text_id

	// parse all staged instances and check if they have a reference
	for formatted_text_id := range stage.Formatted_text_ids {
		if ref, ok := stage.Formatted_text_ids_reference[formatted_text_id]; !ok {
			formatted_text_ids_newInstances = append(formatted_text_ids_newInstances, formatted_text_id)
			newInstancesSlice = append(newInstancesSlice, formatted_text_id.GongMarshallIdentifier(stage))
			if stage.Formatted_text_ids_referenceOrder == nil {
				stage.Formatted_text_ids_referenceOrder = make(map[*Formatted_text_id]uint)
			}
			stage.Formatted_text_ids_referenceOrder[formatted_text_id] = stage.Formatted_text_id_stagedOrder[formatted_text_id]
			newInstancesReverseSlice = append(newInstancesReverseSlice, formatted_text_id.GongMarshallUnstaging(stage))
			// delete(stage.Formatted_text_ids_referenceOrder, formatted_text_id)
			fieldInitializers, pointersInitializations := formatted_text_id.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Formatted_text_id_stagedOrder[ref] = stage.Formatted_text_id_stagedOrder[formatted_text_id]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := formatted_text_id.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, formatted_text_id)
			// delete(stage.Formatted_text_id_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", formatted_text_id.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Formatted_text_ids_reference {
		instance := stage.Formatted_text_ids_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Formatted_text_ids[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			formatted_text_ids_deletedInstances = append(formatted_text_ids_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(formatted_text_ids_newInstances)
	lenDeletedInstances += len(formatted_text_ids_deletedInstances)
	var forwards_newInstances []*Forward
	var forwards_deletedInstances []*Forward

	// parse all staged instances and check if they have a reference
	for forward := range stage.Forwards {
		if ref, ok := stage.Forwards_reference[forward]; !ok {
			forwards_newInstances = append(forwards_newInstances, forward)
			newInstancesSlice = append(newInstancesSlice, forward.GongMarshallIdentifier(stage))
			if stage.Forwards_referenceOrder == nil {
				stage.Forwards_referenceOrder = make(map[*Forward]uint)
			}
			stage.Forwards_referenceOrder[forward] = stage.Forward_stagedOrder[forward]
			newInstancesReverseSlice = append(newInstancesReverseSlice, forward.GongMarshallUnstaging(stage))
			// delete(stage.Forwards_referenceOrder, forward)
			fieldInitializers, pointersInitializations := forward.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Forward_stagedOrder[ref] = stage.Forward_stagedOrder[forward]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := forward.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, forward)
			// delete(stage.Forward_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", forward.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Forwards_reference {
		instance := stage.Forwards_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Forwards[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			forwards_deletedInstances = append(forwards_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(forwards_newInstances)
	lenDeletedInstances += len(forwards_deletedInstances)
	var frames_newInstances []*Frame
	var frames_deletedInstances []*Frame

	// parse all staged instances and check if they have a reference
	for frame := range stage.Frames {
		if ref, ok := stage.Frames_reference[frame]; !ok {
			frames_newInstances = append(frames_newInstances, frame)
			newInstancesSlice = append(newInstancesSlice, frame.GongMarshallIdentifier(stage))
			if stage.Frames_referenceOrder == nil {
				stage.Frames_referenceOrder = make(map[*Frame]uint)
			}
			stage.Frames_referenceOrder[frame] = stage.Frame_stagedOrder[frame]
			newInstancesReverseSlice = append(newInstancesReverseSlice, frame.GongMarshallUnstaging(stage))
			// delete(stage.Frames_referenceOrder, frame)
			fieldInitializers, pointersInitializations := frame.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Frame_stagedOrder[ref] = stage.Frame_stagedOrder[frame]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := frame.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, frame)
			// delete(stage.Frame_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", frame.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Frames_reference {
		instance := stage.Frames_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Frames[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			frames_deletedInstances = append(frames_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(frames_newInstances)
	lenDeletedInstances += len(frames_deletedInstances)
	var frame_notes_newInstances []*Frame_note
	var frame_notes_deletedInstances []*Frame_note

	// parse all staged instances and check if they have a reference
	for frame_note := range stage.Frame_notes {
		if ref, ok := stage.Frame_notes_reference[frame_note]; !ok {
			frame_notes_newInstances = append(frame_notes_newInstances, frame_note)
			newInstancesSlice = append(newInstancesSlice, frame_note.GongMarshallIdentifier(stage))
			if stage.Frame_notes_referenceOrder == nil {
				stage.Frame_notes_referenceOrder = make(map[*Frame_note]uint)
			}
			stage.Frame_notes_referenceOrder[frame_note] = stage.Frame_note_stagedOrder[frame_note]
			newInstancesReverseSlice = append(newInstancesReverseSlice, frame_note.GongMarshallUnstaging(stage))
			// delete(stage.Frame_notes_referenceOrder, frame_note)
			fieldInitializers, pointersInitializations := frame_note.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Frame_note_stagedOrder[ref] = stage.Frame_note_stagedOrder[frame_note]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := frame_note.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, frame_note)
			// delete(stage.Frame_note_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", frame_note.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Frame_notes_reference {
		instance := stage.Frame_notes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Frame_notes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			frame_notes_deletedInstances = append(frame_notes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(frame_notes_newInstances)
	lenDeletedInstances += len(frame_notes_deletedInstances)
	var frets_newInstances []*Fret
	var frets_deletedInstances []*Fret

	// parse all staged instances and check if they have a reference
	for fret := range stage.Frets {
		if ref, ok := stage.Frets_reference[fret]; !ok {
			frets_newInstances = append(frets_newInstances, fret)
			newInstancesSlice = append(newInstancesSlice, fret.GongMarshallIdentifier(stage))
			if stage.Frets_referenceOrder == nil {
				stage.Frets_referenceOrder = make(map[*Fret]uint)
			}
			stage.Frets_referenceOrder[fret] = stage.Fret_stagedOrder[fret]
			newInstancesReverseSlice = append(newInstancesReverseSlice, fret.GongMarshallUnstaging(stage))
			// delete(stage.Frets_referenceOrder, fret)
			fieldInitializers, pointersInitializations := fret.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Fret_stagedOrder[ref] = stage.Fret_stagedOrder[fret]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := fret.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, fret)
			// delete(stage.Fret_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", fret.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Frets_reference {
		instance := stage.Frets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Frets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			frets_deletedInstances = append(frets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(frets_newInstances)
	lenDeletedInstances += len(frets_deletedInstances)
	var glasss_newInstances []*Glass
	var glasss_deletedInstances []*Glass

	// parse all staged instances and check if they have a reference
	for glass := range stage.Glasss {
		if ref, ok := stage.Glasss_reference[glass]; !ok {
			glasss_newInstances = append(glasss_newInstances, glass)
			newInstancesSlice = append(newInstancesSlice, glass.GongMarshallIdentifier(stage))
			if stage.Glasss_referenceOrder == nil {
				stage.Glasss_referenceOrder = make(map[*Glass]uint)
			}
			stage.Glasss_referenceOrder[glass] = stage.Glass_stagedOrder[glass]
			newInstancesReverseSlice = append(newInstancesReverseSlice, glass.GongMarshallUnstaging(stage))
			// delete(stage.Glasss_referenceOrder, glass)
			fieldInitializers, pointersInitializations := glass.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Glass_stagedOrder[ref] = stage.Glass_stagedOrder[glass]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := glass.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, glass)
			// delete(stage.Glass_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", glass.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Glasss_reference {
		instance := stage.Glasss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Glasss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			glasss_deletedInstances = append(glasss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(glasss_newInstances)
	lenDeletedInstances += len(glasss_deletedInstances)
	var glissandos_newInstances []*Glissando
	var glissandos_deletedInstances []*Glissando

	// parse all staged instances and check if they have a reference
	for glissando := range stage.Glissandos {
		if ref, ok := stage.Glissandos_reference[glissando]; !ok {
			glissandos_newInstances = append(glissandos_newInstances, glissando)
			newInstancesSlice = append(newInstancesSlice, glissando.GongMarshallIdentifier(stage))
			if stage.Glissandos_referenceOrder == nil {
				stage.Glissandos_referenceOrder = make(map[*Glissando]uint)
			}
			stage.Glissandos_referenceOrder[glissando] = stage.Glissando_stagedOrder[glissando]
			newInstancesReverseSlice = append(newInstancesReverseSlice, glissando.GongMarshallUnstaging(stage))
			// delete(stage.Glissandos_referenceOrder, glissando)
			fieldInitializers, pointersInitializations := glissando.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Glissando_stagedOrder[ref] = stage.Glissando_stagedOrder[glissando]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := glissando.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, glissando)
			// delete(stage.Glissando_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", glissando.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Glissandos_reference {
		instance := stage.Glissandos_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Glissandos[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			glissandos_deletedInstances = append(glissandos_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(glissandos_newInstances)
	lenDeletedInstances += len(glissandos_deletedInstances)
	var glyphs_newInstances []*Glyph
	var glyphs_deletedInstances []*Glyph

	// parse all staged instances and check if they have a reference
	for glyph := range stage.Glyphs {
		if ref, ok := stage.Glyphs_reference[glyph]; !ok {
			glyphs_newInstances = append(glyphs_newInstances, glyph)
			newInstancesSlice = append(newInstancesSlice, glyph.GongMarshallIdentifier(stage))
			if stage.Glyphs_referenceOrder == nil {
				stage.Glyphs_referenceOrder = make(map[*Glyph]uint)
			}
			stage.Glyphs_referenceOrder[glyph] = stage.Glyph_stagedOrder[glyph]
			newInstancesReverseSlice = append(newInstancesReverseSlice, glyph.GongMarshallUnstaging(stage))
			// delete(stage.Glyphs_referenceOrder, glyph)
			fieldInitializers, pointersInitializations := glyph.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Glyph_stagedOrder[ref] = stage.Glyph_stagedOrder[glyph]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := glyph.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, glyph)
			// delete(stage.Glyph_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", glyph.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Glyphs_reference {
		instance := stage.Glyphs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Glyphs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			glyphs_deletedInstances = append(glyphs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(glyphs_newInstances)
	lenDeletedInstances += len(glyphs_deletedInstances)
	var graces_newInstances []*Grace
	var graces_deletedInstances []*Grace

	// parse all staged instances and check if they have a reference
	for grace := range stage.Graces {
		if ref, ok := stage.Graces_reference[grace]; !ok {
			graces_newInstances = append(graces_newInstances, grace)
			newInstancesSlice = append(newInstancesSlice, grace.GongMarshallIdentifier(stage))
			if stage.Graces_referenceOrder == nil {
				stage.Graces_referenceOrder = make(map[*Grace]uint)
			}
			stage.Graces_referenceOrder[grace] = stage.Grace_stagedOrder[grace]
			newInstancesReverseSlice = append(newInstancesReverseSlice, grace.GongMarshallUnstaging(stage))
			// delete(stage.Graces_referenceOrder, grace)
			fieldInitializers, pointersInitializations := grace.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Grace_stagedOrder[ref] = stage.Grace_stagedOrder[grace]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := grace.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, grace)
			// delete(stage.Grace_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", grace.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Graces_reference {
		instance := stage.Graces_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Graces[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			graces_deletedInstances = append(graces_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(graces_newInstances)
	lenDeletedInstances += len(graces_deletedInstances)
	var group_barlines_newInstances []*Group_barline
	var group_barlines_deletedInstances []*Group_barline

	// parse all staged instances and check if they have a reference
	for group_barline := range stage.Group_barlines {
		if ref, ok := stage.Group_barlines_reference[group_barline]; !ok {
			group_barlines_newInstances = append(group_barlines_newInstances, group_barline)
			newInstancesSlice = append(newInstancesSlice, group_barline.GongMarshallIdentifier(stage))
			if stage.Group_barlines_referenceOrder == nil {
				stage.Group_barlines_referenceOrder = make(map[*Group_barline]uint)
			}
			stage.Group_barlines_referenceOrder[group_barline] = stage.Group_barline_stagedOrder[group_barline]
			newInstancesReverseSlice = append(newInstancesReverseSlice, group_barline.GongMarshallUnstaging(stage))
			// delete(stage.Group_barlines_referenceOrder, group_barline)
			fieldInitializers, pointersInitializations := group_barline.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Group_barline_stagedOrder[ref] = stage.Group_barline_stagedOrder[group_barline]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := group_barline.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, group_barline)
			// delete(stage.Group_barline_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", group_barline.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Group_barlines_reference {
		instance := stage.Group_barlines_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Group_barlines[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			group_barlines_deletedInstances = append(group_barlines_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(group_barlines_newInstances)
	lenDeletedInstances += len(group_barlines_deletedInstances)
	var group_names_newInstances []*Group_name
	var group_names_deletedInstances []*Group_name

	// parse all staged instances and check if they have a reference
	for group_name := range stage.Group_names {
		if ref, ok := stage.Group_names_reference[group_name]; !ok {
			group_names_newInstances = append(group_names_newInstances, group_name)
			newInstancesSlice = append(newInstancesSlice, group_name.GongMarshallIdentifier(stage))
			if stage.Group_names_referenceOrder == nil {
				stage.Group_names_referenceOrder = make(map[*Group_name]uint)
			}
			stage.Group_names_referenceOrder[group_name] = stage.Group_name_stagedOrder[group_name]
			newInstancesReverseSlice = append(newInstancesReverseSlice, group_name.GongMarshallUnstaging(stage))
			// delete(stage.Group_names_referenceOrder, group_name)
			fieldInitializers, pointersInitializations := group_name.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Group_name_stagedOrder[ref] = stage.Group_name_stagedOrder[group_name]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := group_name.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, group_name)
			// delete(stage.Group_name_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", group_name.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Group_names_reference {
		instance := stage.Group_names_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Group_names[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			group_names_deletedInstances = append(group_names_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(group_names_newInstances)
	lenDeletedInstances += len(group_names_deletedInstances)
	var group_symbols_newInstances []*Group_symbol
	var group_symbols_deletedInstances []*Group_symbol

	// parse all staged instances and check if they have a reference
	for group_symbol := range stage.Group_symbols {
		if ref, ok := stage.Group_symbols_reference[group_symbol]; !ok {
			group_symbols_newInstances = append(group_symbols_newInstances, group_symbol)
			newInstancesSlice = append(newInstancesSlice, group_symbol.GongMarshallIdentifier(stage))
			if stage.Group_symbols_referenceOrder == nil {
				stage.Group_symbols_referenceOrder = make(map[*Group_symbol]uint)
			}
			stage.Group_symbols_referenceOrder[group_symbol] = stage.Group_symbol_stagedOrder[group_symbol]
			newInstancesReverseSlice = append(newInstancesReverseSlice, group_symbol.GongMarshallUnstaging(stage))
			// delete(stage.Group_symbols_referenceOrder, group_symbol)
			fieldInitializers, pointersInitializations := group_symbol.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Group_symbol_stagedOrder[ref] = stage.Group_symbol_stagedOrder[group_symbol]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := group_symbol.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, group_symbol)
			// delete(stage.Group_symbol_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", group_symbol.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Group_symbols_reference {
		instance := stage.Group_symbols_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Group_symbols[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			group_symbols_deletedInstances = append(group_symbols_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(group_symbols_newInstances)
	lenDeletedInstances += len(group_symbols_deletedInstances)
	var groupings_newInstances []*Grouping
	var groupings_deletedInstances []*Grouping

	// parse all staged instances and check if they have a reference
	for grouping := range stage.Groupings {
		if ref, ok := stage.Groupings_reference[grouping]; !ok {
			groupings_newInstances = append(groupings_newInstances, grouping)
			newInstancesSlice = append(newInstancesSlice, grouping.GongMarshallIdentifier(stage))
			if stage.Groupings_referenceOrder == nil {
				stage.Groupings_referenceOrder = make(map[*Grouping]uint)
			}
			stage.Groupings_referenceOrder[grouping] = stage.Grouping_stagedOrder[grouping]
			newInstancesReverseSlice = append(newInstancesReverseSlice, grouping.GongMarshallUnstaging(stage))
			// delete(stage.Groupings_referenceOrder, grouping)
			fieldInitializers, pointersInitializations := grouping.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Grouping_stagedOrder[ref] = stage.Grouping_stagedOrder[grouping]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := grouping.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, grouping)
			// delete(stage.Grouping_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", grouping.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Groupings_reference {
		instance := stage.Groupings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Groupings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			groupings_deletedInstances = append(groupings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(groupings_newInstances)
	lenDeletedInstances += len(groupings_deletedInstances)
	var hammer_on_pull_offs_newInstances []*Hammer_on_pull_off
	var hammer_on_pull_offs_deletedInstances []*Hammer_on_pull_off

	// parse all staged instances and check if they have a reference
	for hammer_on_pull_off := range stage.Hammer_on_pull_offs {
		if ref, ok := stage.Hammer_on_pull_offs_reference[hammer_on_pull_off]; !ok {
			hammer_on_pull_offs_newInstances = append(hammer_on_pull_offs_newInstances, hammer_on_pull_off)
			newInstancesSlice = append(newInstancesSlice, hammer_on_pull_off.GongMarshallIdentifier(stage))
			if stage.Hammer_on_pull_offs_referenceOrder == nil {
				stage.Hammer_on_pull_offs_referenceOrder = make(map[*Hammer_on_pull_off]uint)
			}
			stage.Hammer_on_pull_offs_referenceOrder[hammer_on_pull_off] = stage.Hammer_on_pull_off_stagedOrder[hammer_on_pull_off]
			newInstancesReverseSlice = append(newInstancesReverseSlice, hammer_on_pull_off.GongMarshallUnstaging(stage))
			// delete(stage.Hammer_on_pull_offs_referenceOrder, hammer_on_pull_off)
			fieldInitializers, pointersInitializations := hammer_on_pull_off.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Hammer_on_pull_off_stagedOrder[ref] = stage.Hammer_on_pull_off_stagedOrder[hammer_on_pull_off]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := hammer_on_pull_off.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, hammer_on_pull_off)
			// delete(stage.Hammer_on_pull_off_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", hammer_on_pull_off.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Hammer_on_pull_offs_reference {
		instance := stage.Hammer_on_pull_offs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Hammer_on_pull_offs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			hammer_on_pull_offs_deletedInstances = append(hammer_on_pull_offs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(hammer_on_pull_offs_newInstances)
	lenDeletedInstances += len(hammer_on_pull_offs_deletedInstances)
	var handbells_newInstances []*Handbell
	var handbells_deletedInstances []*Handbell

	// parse all staged instances and check if they have a reference
	for handbell := range stage.Handbells {
		if ref, ok := stage.Handbells_reference[handbell]; !ok {
			handbells_newInstances = append(handbells_newInstances, handbell)
			newInstancesSlice = append(newInstancesSlice, handbell.GongMarshallIdentifier(stage))
			if stage.Handbells_referenceOrder == nil {
				stage.Handbells_referenceOrder = make(map[*Handbell]uint)
			}
			stage.Handbells_referenceOrder[handbell] = stage.Handbell_stagedOrder[handbell]
			newInstancesReverseSlice = append(newInstancesReverseSlice, handbell.GongMarshallUnstaging(stage))
			// delete(stage.Handbells_referenceOrder, handbell)
			fieldInitializers, pointersInitializations := handbell.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Handbell_stagedOrder[ref] = stage.Handbell_stagedOrder[handbell]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := handbell.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, handbell)
			// delete(stage.Handbell_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", handbell.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Handbells_reference {
		instance := stage.Handbells_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Handbells[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			handbells_deletedInstances = append(handbells_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(handbells_newInstances)
	lenDeletedInstances += len(handbells_deletedInstances)
	var harmon_closeds_newInstances []*Harmon_closed
	var harmon_closeds_deletedInstances []*Harmon_closed

	// parse all staged instances and check if they have a reference
	for harmon_closed := range stage.Harmon_closeds {
		if ref, ok := stage.Harmon_closeds_reference[harmon_closed]; !ok {
			harmon_closeds_newInstances = append(harmon_closeds_newInstances, harmon_closed)
			newInstancesSlice = append(newInstancesSlice, harmon_closed.GongMarshallIdentifier(stage))
			if stage.Harmon_closeds_referenceOrder == nil {
				stage.Harmon_closeds_referenceOrder = make(map[*Harmon_closed]uint)
			}
			stage.Harmon_closeds_referenceOrder[harmon_closed] = stage.Harmon_closed_stagedOrder[harmon_closed]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harmon_closed.GongMarshallUnstaging(stage))
			// delete(stage.Harmon_closeds_referenceOrder, harmon_closed)
			fieldInitializers, pointersInitializations := harmon_closed.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harmon_closed_stagedOrder[ref] = stage.Harmon_closed_stagedOrder[harmon_closed]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harmon_closed.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harmon_closed)
			// delete(stage.Harmon_closed_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harmon_closed.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harmon_closeds_reference {
		instance := stage.Harmon_closeds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harmon_closeds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harmon_closeds_deletedInstances = append(harmon_closeds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harmon_closeds_newInstances)
	lenDeletedInstances += len(harmon_closeds_deletedInstances)
	var harmon_mutes_newInstances []*Harmon_mute
	var harmon_mutes_deletedInstances []*Harmon_mute

	// parse all staged instances and check if they have a reference
	for harmon_mute := range stage.Harmon_mutes {
		if ref, ok := stage.Harmon_mutes_reference[harmon_mute]; !ok {
			harmon_mutes_newInstances = append(harmon_mutes_newInstances, harmon_mute)
			newInstancesSlice = append(newInstancesSlice, harmon_mute.GongMarshallIdentifier(stage))
			if stage.Harmon_mutes_referenceOrder == nil {
				stage.Harmon_mutes_referenceOrder = make(map[*Harmon_mute]uint)
			}
			stage.Harmon_mutes_referenceOrder[harmon_mute] = stage.Harmon_mute_stagedOrder[harmon_mute]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harmon_mute.GongMarshallUnstaging(stage))
			// delete(stage.Harmon_mutes_referenceOrder, harmon_mute)
			fieldInitializers, pointersInitializations := harmon_mute.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harmon_mute_stagedOrder[ref] = stage.Harmon_mute_stagedOrder[harmon_mute]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harmon_mute.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harmon_mute)
			// delete(stage.Harmon_mute_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harmon_mute.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harmon_mutes_reference {
		instance := stage.Harmon_mutes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harmon_mutes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harmon_mutes_deletedInstances = append(harmon_mutes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harmon_mutes_newInstances)
	lenDeletedInstances += len(harmon_mutes_deletedInstances)
	var harmonics_newInstances []*Harmonic
	var harmonics_deletedInstances []*Harmonic

	// parse all staged instances and check if they have a reference
	for harmonic := range stage.Harmonics {
		if ref, ok := stage.Harmonics_reference[harmonic]; !ok {
			harmonics_newInstances = append(harmonics_newInstances, harmonic)
			newInstancesSlice = append(newInstancesSlice, harmonic.GongMarshallIdentifier(stage))
			if stage.Harmonics_referenceOrder == nil {
				stage.Harmonics_referenceOrder = make(map[*Harmonic]uint)
			}
			stage.Harmonics_referenceOrder[harmonic] = stage.Harmonic_stagedOrder[harmonic]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harmonic.GongMarshallUnstaging(stage))
			// delete(stage.Harmonics_referenceOrder, harmonic)
			fieldInitializers, pointersInitializations := harmonic.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harmonic_stagedOrder[ref] = stage.Harmonic_stagedOrder[harmonic]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harmonic.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harmonic)
			// delete(stage.Harmonic_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harmonic.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harmonics_reference {
		instance := stage.Harmonics_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harmonics[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harmonics_deletedInstances = append(harmonics_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harmonics_newInstances)
	lenDeletedInstances += len(harmonics_deletedInstances)
	var harmonys_newInstances []*Harmony
	var harmonys_deletedInstances []*Harmony

	// parse all staged instances and check if they have a reference
	for harmony := range stage.Harmonys {
		if ref, ok := stage.Harmonys_reference[harmony]; !ok {
			harmonys_newInstances = append(harmonys_newInstances, harmony)
			newInstancesSlice = append(newInstancesSlice, harmony.GongMarshallIdentifier(stage))
			if stage.Harmonys_referenceOrder == nil {
				stage.Harmonys_referenceOrder = make(map[*Harmony]uint)
			}
			stage.Harmonys_referenceOrder[harmony] = stage.Harmony_stagedOrder[harmony]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harmony.GongMarshallUnstaging(stage))
			// delete(stage.Harmonys_referenceOrder, harmony)
			fieldInitializers, pointersInitializations := harmony.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harmony_stagedOrder[ref] = stage.Harmony_stagedOrder[harmony]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harmony.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harmony)
			// delete(stage.Harmony_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harmony.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harmonys_reference {
		instance := stage.Harmonys_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harmonys[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harmonys_deletedInstances = append(harmonys_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harmonys_newInstances)
	lenDeletedInstances += len(harmonys_deletedInstances)
	var harmony_alters_newInstances []*Harmony_alter
	var harmony_alters_deletedInstances []*Harmony_alter

	// parse all staged instances and check if they have a reference
	for harmony_alter := range stage.Harmony_alters {
		if ref, ok := stage.Harmony_alters_reference[harmony_alter]; !ok {
			harmony_alters_newInstances = append(harmony_alters_newInstances, harmony_alter)
			newInstancesSlice = append(newInstancesSlice, harmony_alter.GongMarshallIdentifier(stage))
			if stage.Harmony_alters_referenceOrder == nil {
				stage.Harmony_alters_referenceOrder = make(map[*Harmony_alter]uint)
			}
			stage.Harmony_alters_referenceOrder[harmony_alter] = stage.Harmony_alter_stagedOrder[harmony_alter]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harmony_alter.GongMarshallUnstaging(stage))
			// delete(stage.Harmony_alters_referenceOrder, harmony_alter)
			fieldInitializers, pointersInitializations := harmony_alter.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harmony_alter_stagedOrder[ref] = stage.Harmony_alter_stagedOrder[harmony_alter]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harmony_alter.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harmony_alter)
			// delete(stage.Harmony_alter_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harmony_alter.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harmony_alters_reference {
		instance := stage.Harmony_alters_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harmony_alters[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harmony_alters_deletedInstances = append(harmony_alters_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harmony_alters_newInstances)
	lenDeletedInstances += len(harmony_alters_deletedInstances)
	var harp_pedalss_newInstances []*Harp_pedals
	var harp_pedalss_deletedInstances []*Harp_pedals

	// parse all staged instances and check if they have a reference
	for harp_pedals := range stage.Harp_pedalss {
		if ref, ok := stage.Harp_pedalss_reference[harp_pedals]; !ok {
			harp_pedalss_newInstances = append(harp_pedalss_newInstances, harp_pedals)
			newInstancesSlice = append(newInstancesSlice, harp_pedals.GongMarshallIdentifier(stage))
			if stage.Harp_pedalss_referenceOrder == nil {
				stage.Harp_pedalss_referenceOrder = make(map[*Harp_pedals]uint)
			}
			stage.Harp_pedalss_referenceOrder[harp_pedals] = stage.Harp_pedals_stagedOrder[harp_pedals]
			newInstancesReverseSlice = append(newInstancesReverseSlice, harp_pedals.GongMarshallUnstaging(stage))
			// delete(stage.Harp_pedalss_referenceOrder, harp_pedals)
			fieldInitializers, pointersInitializations := harp_pedals.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Harp_pedals_stagedOrder[ref] = stage.Harp_pedals_stagedOrder[harp_pedals]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := harp_pedals.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, harp_pedals)
			// delete(stage.Harp_pedals_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", harp_pedals.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Harp_pedalss_reference {
		instance := stage.Harp_pedalss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Harp_pedalss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			harp_pedalss_deletedInstances = append(harp_pedalss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(harp_pedalss_newInstances)
	lenDeletedInstances += len(harp_pedalss_deletedInstances)
	var heel_toes_newInstances []*Heel_toe
	var heel_toes_deletedInstances []*Heel_toe

	// parse all staged instances and check if they have a reference
	for heel_toe := range stage.Heel_toes {
		if ref, ok := stage.Heel_toes_reference[heel_toe]; !ok {
			heel_toes_newInstances = append(heel_toes_newInstances, heel_toe)
			newInstancesSlice = append(newInstancesSlice, heel_toe.GongMarshallIdentifier(stage))
			if stage.Heel_toes_referenceOrder == nil {
				stage.Heel_toes_referenceOrder = make(map[*Heel_toe]uint)
			}
			stage.Heel_toes_referenceOrder[heel_toe] = stage.Heel_toe_stagedOrder[heel_toe]
			newInstancesReverseSlice = append(newInstancesReverseSlice, heel_toe.GongMarshallUnstaging(stage))
			// delete(stage.Heel_toes_referenceOrder, heel_toe)
			fieldInitializers, pointersInitializations := heel_toe.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Heel_toe_stagedOrder[ref] = stage.Heel_toe_stagedOrder[heel_toe]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := heel_toe.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, heel_toe)
			// delete(stage.Heel_toe_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", heel_toe.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Heel_toes_reference {
		instance := stage.Heel_toes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Heel_toes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			heel_toes_deletedInstances = append(heel_toes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(heel_toes_newInstances)
	lenDeletedInstances += len(heel_toes_deletedInstances)
	var holes_newInstances []*Hole
	var holes_deletedInstances []*Hole

	// parse all staged instances and check if they have a reference
	for hole := range stage.Holes {
		if ref, ok := stage.Holes_reference[hole]; !ok {
			holes_newInstances = append(holes_newInstances, hole)
			newInstancesSlice = append(newInstancesSlice, hole.GongMarshallIdentifier(stage))
			if stage.Holes_referenceOrder == nil {
				stage.Holes_referenceOrder = make(map[*Hole]uint)
			}
			stage.Holes_referenceOrder[hole] = stage.Hole_stagedOrder[hole]
			newInstancesReverseSlice = append(newInstancesReverseSlice, hole.GongMarshallUnstaging(stage))
			// delete(stage.Holes_referenceOrder, hole)
			fieldInitializers, pointersInitializations := hole.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Hole_stagedOrder[ref] = stage.Hole_stagedOrder[hole]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := hole.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, hole)
			// delete(stage.Hole_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", hole.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Holes_reference {
		instance := stage.Holes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Holes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			holes_deletedInstances = append(holes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(holes_newInstances)
	lenDeletedInstances += len(holes_deletedInstances)
	var hole_closeds_newInstances []*Hole_closed
	var hole_closeds_deletedInstances []*Hole_closed

	// parse all staged instances and check if they have a reference
	for hole_closed := range stage.Hole_closeds {
		if ref, ok := stage.Hole_closeds_reference[hole_closed]; !ok {
			hole_closeds_newInstances = append(hole_closeds_newInstances, hole_closed)
			newInstancesSlice = append(newInstancesSlice, hole_closed.GongMarshallIdentifier(stage))
			if stage.Hole_closeds_referenceOrder == nil {
				stage.Hole_closeds_referenceOrder = make(map[*Hole_closed]uint)
			}
			stage.Hole_closeds_referenceOrder[hole_closed] = stage.Hole_closed_stagedOrder[hole_closed]
			newInstancesReverseSlice = append(newInstancesReverseSlice, hole_closed.GongMarshallUnstaging(stage))
			// delete(stage.Hole_closeds_referenceOrder, hole_closed)
			fieldInitializers, pointersInitializations := hole_closed.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Hole_closed_stagedOrder[ref] = stage.Hole_closed_stagedOrder[hole_closed]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := hole_closed.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, hole_closed)
			// delete(stage.Hole_closed_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", hole_closed.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Hole_closeds_reference {
		instance := stage.Hole_closeds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Hole_closeds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			hole_closeds_deletedInstances = append(hole_closeds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(hole_closeds_newInstances)
	lenDeletedInstances += len(hole_closeds_deletedInstances)
	var horizontal_turns_newInstances []*Horizontal_turn
	var horizontal_turns_deletedInstances []*Horizontal_turn

	// parse all staged instances and check if they have a reference
	for horizontal_turn := range stage.Horizontal_turns {
		if ref, ok := stage.Horizontal_turns_reference[horizontal_turn]; !ok {
			horizontal_turns_newInstances = append(horizontal_turns_newInstances, horizontal_turn)
			newInstancesSlice = append(newInstancesSlice, horizontal_turn.GongMarshallIdentifier(stage))
			if stage.Horizontal_turns_referenceOrder == nil {
				stage.Horizontal_turns_referenceOrder = make(map[*Horizontal_turn]uint)
			}
			stage.Horizontal_turns_referenceOrder[horizontal_turn] = stage.Horizontal_turn_stagedOrder[horizontal_turn]
			newInstancesReverseSlice = append(newInstancesReverseSlice, horizontal_turn.GongMarshallUnstaging(stage))
			// delete(stage.Horizontal_turns_referenceOrder, horizontal_turn)
			fieldInitializers, pointersInitializations := horizontal_turn.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Horizontal_turn_stagedOrder[ref] = stage.Horizontal_turn_stagedOrder[horizontal_turn]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := horizontal_turn.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, horizontal_turn)
			// delete(stage.Horizontal_turn_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", horizontal_turn.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Horizontal_turns_reference {
		instance := stage.Horizontal_turns_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Horizontal_turns[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			horizontal_turns_deletedInstances = append(horizontal_turns_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(horizontal_turns_newInstances)
	lenDeletedInstances += len(horizontal_turns_deletedInstances)
	var identifications_newInstances []*Identification
	var identifications_deletedInstances []*Identification

	// parse all staged instances and check if they have a reference
	for identification := range stage.Identifications {
		if ref, ok := stage.Identifications_reference[identification]; !ok {
			identifications_newInstances = append(identifications_newInstances, identification)
			newInstancesSlice = append(newInstancesSlice, identification.GongMarshallIdentifier(stage))
			if stage.Identifications_referenceOrder == nil {
				stage.Identifications_referenceOrder = make(map[*Identification]uint)
			}
			stage.Identifications_referenceOrder[identification] = stage.Identification_stagedOrder[identification]
			newInstancesReverseSlice = append(newInstancesReverseSlice, identification.GongMarshallUnstaging(stage))
			// delete(stage.Identifications_referenceOrder, identification)
			fieldInitializers, pointersInitializations := identification.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Identification_stagedOrder[ref] = stage.Identification_stagedOrder[identification]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := identification.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, identification)
			// delete(stage.Identification_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", identification.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Identifications_reference {
		instance := stage.Identifications_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Identifications[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			identifications_deletedInstances = append(identifications_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(identifications_newInstances)
	lenDeletedInstances += len(identifications_deletedInstances)
	var images_newInstances []*Image
	var images_deletedInstances []*Image

	// parse all staged instances and check if they have a reference
	for image := range stage.Images {
		if ref, ok := stage.Images_reference[image]; !ok {
			images_newInstances = append(images_newInstances, image)
			newInstancesSlice = append(newInstancesSlice, image.GongMarshallIdentifier(stage))
			if stage.Images_referenceOrder == nil {
				stage.Images_referenceOrder = make(map[*Image]uint)
			}
			stage.Images_referenceOrder[image] = stage.Image_stagedOrder[image]
			newInstancesReverseSlice = append(newInstancesReverseSlice, image.GongMarshallUnstaging(stage))
			// delete(stage.Images_referenceOrder, image)
			fieldInitializers, pointersInitializations := image.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Image_stagedOrder[ref] = stage.Image_stagedOrder[image]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := image.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, image)
			// delete(stage.Image_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", image.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Images_reference {
		instance := stage.Images_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Images[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			images_deletedInstances = append(images_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(images_newInstances)
	lenDeletedInstances += len(images_deletedInstances)
	var instruments_newInstances []*Instrument
	var instruments_deletedInstances []*Instrument

	// parse all staged instances and check if they have a reference
	for instrument := range stage.Instruments {
		if ref, ok := stage.Instruments_reference[instrument]; !ok {
			instruments_newInstances = append(instruments_newInstances, instrument)
			newInstancesSlice = append(newInstancesSlice, instrument.GongMarshallIdentifier(stage))
			if stage.Instruments_referenceOrder == nil {
				stage.Instruments_referenceOrder = make(map[*Instrument]uint)
			}
			stage.Instruments_referenceOrder[instrument] = stage.Instrument_stagedOrder[instrument]
			newInstancesReverseSlice = append(newInstancesReverseSlice, instrument.GongMarshallUnstaging(stage))
			// delete(stage.Instruments_referenceOrder, instrument)
			fieldInitializers, pointersInitializations := instrument.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Instrument_stagedOrder[ref] = stage.Instrument_stagedOrder[instrument]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := instrument.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, instrument)
			// delete(stage.Instrument_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", instrument.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Instruments_reference {
		instance := stage.Instruments_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Instruments[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			instruments_deletedInstances = append(instruments_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(instruments_newInstances)
	lenDeletedInstances += len(instruments_deletedInstances)
	var instrument_changes_newInstances []*Instrument_change
	var instrument_changes_deletedInstances []*Instrument_change

	// parse all staged instances and check if they have a reference
	for instrument_change := range stage.Instrument_changes {
		if ref, ok := stage.Instrument_changes_reference[instrument_change]; !ok {
			instrument_changes_newInstances = append(instrument_changes_newInstances, instrument_change)
			newInstancesSlice = append(newInstancesSlice, instrument_change.GongMarshallIdentifier(stage))
			if stage.Instrument_changes_referenceOrder == nil {
				stage.Instrument_changes_referenceOrder = make(map[*Instrument_change]uint)
			}
			stage.Instrument_changes_referenceOrder[instrument_change] = stage.Instrument_change_stagedOrder[instrument_change]
			newInstancesReverseSlice = append(newInstancesReverseSlice, instrument_change.GongMarshallUnstaging(stage))
			// delete(stage.Instrument_changes_referenceOrder, instrument_change)
			fieldInitializers, pointersInitializations := instrument_change.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Instrument_change_stagedOrder[ref] = stage.Instrument_change_stagedOrder[instrument_change]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := instrument_change.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, instrument_change)
			// delete(stage.Instrument_change_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", instrument_change.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Instrument_changes_reference {
		instance := stage.Instrument_changes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Instrument_changes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			instrument_changes_deletedInstances = append(instrument_changes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(instrument_changes_newInstances)
	lenDeletedInstances += len(instrument_changes_deletedInstances)
	var instrument_links_newInstances []*Instrument_link
	var instrument_links_deletedInstances []*Instrument_link

	// parse all staged instances and check if they have a reference
	for instrument_link := range stage.Instrument_links {
		if ref, ok := stage.Instrument_links_reference[instrument_link]; !ok {
			instrument_links_newInstances = append(instrument_links_newInstances, instrument_link)
			newInstancesSlice = append(newInstancesSlice, instrument_link.GongMarshallIdentifier(stage))
			if stage.Instrument_links_referenceOrder == nil {
				stage.Instrument_links_referenceOrder = make(map[*Instrument_link]uint)
			}
			stage.Instrument_links_referenceOrder[instrument_link] = stage.Instrument_link_stagedOrder[instrument_link]
			newInstancesReverseSlice = append(newInstancesReverseSlice, instrument_link.GongMarshallUnstaging(stage))
			// delete(stage.Instrument_links_referenceOrder, instrument_link)
			fieldInitializers, pointersInitializations := instrument_link.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Instrument_link_stagedOrder[ref] = stage.Instrument_link_stagedOrder[instrument_link]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := instrument_link.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, instrument_link)
			// delete(stage.Instrument_link_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", instrument_link.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Instrument_links_reference {
		instance := stage.Instrument_links_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Instrument_links[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			instrument_links_deletedInstances = append(instrument_links_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(instrument_links_newInstances)
	lenDeletedInstances += len(instrument_links_deletedInstances)
	var interchangeables_newInstances []*Interchangeable
	var interchangeables_deletedInstances []*Interchangeable

	// parse all staged instances and check if they have a reference
	for interchangeable := range stage.Interchangeables {
		if ref, ok := stage.Interchangeables_reference[interchangeable]; !ok {
			interchangeables_newInstances = append(interchangeables_newInstances, interchangeable)
			newInstancesSlice = append(newInstancesSlice, interchangeable.GongMarshallIdentifier(stage))
			if stage.Interchangeables_referenceOrder == nil {
				stage.Interchangeables_referenceOrder = make(map[*Interchangeable]uint)
			}
			stage.Interchangeables_referenceOrder[interchangeable] = stage.Interchangeable_stagedOrder[interchangeable]
			newInstancesReverseSlice = append(newInstancesReverseSlice, interchangeable.GongMarshallUnstaging(stage))
			// delete(stage.Interchangeables_referenceOrder, interchangeable)
			fieldInitializers, pointersInitializations := interchangeable.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Interchangeable_stagedOrder[ref] = stage.Interchangeable_stagedOrder[interchangeable]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := interchangeable.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, interchangeable)
			// delete(stage.Interchangeable_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", interchangeable.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Interchangeables_reference {
		instance := stage.Interchangeables_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Interchangeables[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			interchangeables_deletedInstances = append(interchangeables_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(interchangeables_newInstances)
	lenDeletedInstances += len(interchangeables_deletedInstances)
	var inversions_newInstances []*Inversion
	var inversions_deletedInstances []*Inversion

	// parse all staged instances and check if they have a reference
	for inversion := range stage.Inversions {
		if ref, ok := stage.Inversions_reference[inversion]; !ok {
			inversions_newInstances = append(inversions_newInstances, inversion)
			newInstancesSlice = append(newInstancesSlice, inversion.GongMarshallIdentifier(stage))
			if stage.Inversions_referenceOrder == nil {
				stage.Inversions_referenceOrder = make(map[*Inversion]uint)
			}
			stage.Inversions_referenceOrder[inversion] = stage.Inversion_stagedOrder[inversion]
			newInstancesReverseSlice = append(newInstancesReverseSlice, inversion.GongMarshallUnstaging(stage))
			// delete(stage.Inversions_referenceOrder, inversion)
			fieldInitializers, pointersInitializations := inversion.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Inversion_stagedOrder[ref] = stage.Inversion_stagedOrder[inversion]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := inversion.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, inversion)
			// delete(stage.Inversion_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", inversion.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Inversions_reference {
		instance := stage.Inversions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Inversions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			inversions_deletedInstances = append(inversions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(inversions_newInstances)
	lenDeletedInstances += len(inversions_deletedInstances)
	var keys_newInstances []*Key
	var keys_deletedInstances []*Key

	// parse all staged instances and check if they have a reference
	for key := range stage.Keys {
		if ref, ok := stage.Keys_reference[key]; !ok {
			keys_newInstances = append(keys_newInstances, key)
			newInstancesSlice = append(newInstancesSlice, key.GongMarshallIdentifier(stage))
			if stage.Keys_referenceOrder == nil {
				stage.Keys_referenceOrder = make(map[*Key]uint)
			}
			stage.Keys_referenceOrder[key] = stage.Key_stagedOrder[key]
			newInstancesReverseSlice = append(newInstancesReverseSlice, key.GongMarshallUnstaging(stage))
			// delete(stage.Keys_referenceOrder, key)
			fieldInitializers, pointersInitializations := key.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Key_stagedOrder[ref] = stage.Key_stagedOrder[key]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := key.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, key)
			// delete(stage.Key_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", key.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Keys_reference {
		instance := stage.Keys_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Keys[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			keys_deletedInstances = append(keys_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(keys_newInstances)
	lenDeletedInstances += len(keys_deletedInstances)
	var key_accidentals_newInstances []*Key_accidental
	var key_accidentals_deletedInstances []*Key_accidental

	// parse all staged instances and check if they have a reference
	for key_accidental := range stage.Key_accidentals {
		if ref, ok := stage.Key_accidentals_reference[key_accidental]; !ok {
			key_accidentals_newInstances = append(key_accidentals_newInstances, key_accidental)
			newInstancesSlice = append(newInstancesSlice, key_accidental.GongMarshallIdentifier(stage))
			if stage.Key_accidentals_referenceOrder == nil {
				stage.Key_accidentals_referenceOrder = make(map[*Key_accidental]uint)
			}
			stage.Key_accidentals_referenceOrder[key_accidental] = stage.Key_accidental_stagedOrder[key_accidental]
			newInstancesReverseSlice = append(newInstancesReverseSlice, key_accidental.GongMarshallUnstaging(stage))
			// delete(stage.Key_accidentals_referenceOrder, key_accidental)
			fieldInitializers, pointersInitializations := key_accidental.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Key_accidental_stagedOrder[ref] = stage.Key_accidental_stagedOrder[key_accidental]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := key_accidental.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, key_accidental)
			// delete(stage.Key_accidental_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", key_accidental.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Key_accidentals_reference {
		instance := stage.Key_accidentals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Key_accidentals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			key_accidentals_deletedInstances = append(key_accidentals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(key_accidentals_newInstances)
	lenDeletedInstances += len(key_accidentals_deletedInstances)
	var key_octaves_newInstances []*Key_octave
	var key_octaves_deletedInstances []*Key_octave

	// parse all staged instances and check if they have a reference
	for key_octave := range stage.Key_octaves {
		if ref, ok := stage.Key_octaves_reference[key_octave]; !ok {
			key_octaves_newInstances = append(key_octaves_newInstances, key_octave)
			newInstancesSlice = append(newInstancesSlice, key_octave.GongMarshallIdentifier(stage))
			if stage.Key_octaves_referenceOrder == nil {
				stage.Key_octaves_referenceOrder = make(map[*Key_octave]uint)
			}
			stage.Key_octaves_referenceOrder[key_octave] = stage.Key_octave_stagedOrder[key_octave]
			newInstancesReverseSlice = append(newInstancesReverseSlice, key_octave.GongMarshallUnstaging(stage))
			// delete(stage.Key_octaves_referenceOrder, key_octave)
			fieldInitializers, pointersInitializations := key_octave.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Key_octave_stagedOrder[ref] = stage.Key_octave_stagedOrder[key_octave]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := key_octave.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, key_octave)
			// delete(stage.Key_octave_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", key_octave.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Key_octaves_reference {
		instance := stage.Key_octaves_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Key_octaves[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			key_octaves_deletedInstances = append(key_octaves_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(key_octaves_newInstances)
	lenDeletedInstances += len(key_octaves_deletedInstances)
	var kinds_newInstances []*Kind
	var kinds_deletedInstances []*Kind

	// parse all staged instances and check if they have a reference
	for kind := range stage.Kinds {
		if ref, ok := stage.Kinds_reference[kind]; !ok {
			kinds_newInstances = append(kinds_newInstances, kind)
			newInstancesSlice = append(newInstancesSlice, kind.GongMarshallIdentifier(stage))
			if stage.Kinds_referenceOrder == nil {
				stage.Kinds_referenceOrder = make(map[*Kind]uint)
			}
			stage.Kinds_referenceOrder[kind] = stage.Kind_stagedOrder[kind]
			newInstancesReverseSlice = append(newInstancesReverseSlice, kind.GongMarshallUnstaging(stage))
			// delete(stage.Kinds_referenceOrder, kind)
			fieldInitializers, pointersInitializations := kind.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Kind_stagedOrder[ref] = stage.Kind_stagedOrder[kind]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := kind.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, kind)
			// delete(stage.Kind_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", kind.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Kinds_reference {
		instance := stage.Kinds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Kinds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			kinds_deletedInstances = append(kinds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(kinds_newInstances)
	lenDeletedInstances += len(kinds_deletedInstances)
	var levels_newInstances []*Level
	var levels_deletedInstances []*Level

	// parse all staged instances and check if they have a reference
	for level := range stage.Levels {
		if ref, ok := stage.Levels_reference[level]; !ok {
			levels_newInstances = append(levels_newInstances, level)
			newInstancesSlice = append(newInstancesSlice, level.GongMarshallIdentifier(stage))
			if stage.Levels_referenceOrder == nil {
				stage.Levels_referenceOrder = make(map[*Level]uint)
			}
			stage.Levels_referenceOrder[level] = stage.Level_stagedOrder[level]
			newInstancesReverseSlice = append(newInstancesReverseSlice, level.GongMarshallUnstaging(stage))
			// delete(stage.Levels_referenceOrder, level)
			fieldInitializers, pointersInitializations := level.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Level_stagedOrder[ref] = stage.Level_stagedOrder[level]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := level.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, level)
			// delete(stage.Level_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", level.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Levels_reference {
		instance := stage.Levels_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Levels[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			levels_deletedInstances = append(levels_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(levels_newInstances)
	lenDeletedInstances += len(levels_deletedInstances)
	var line_details_newInstances []*Line_detail
	var line_details_deletedInstances []*Line_detail

	// parse all staged instances and check if they have a reference
	for line_detail := range stage.Line_details {
		if ref, ok := stage.Line_details_reference[line_detail]; !ok {
			line_details_newInstances = append(line_details_newInstances, line_detail)
			newInstancesSlice = append(newInstancesSlice, line_detail.GongMarshallIdentifier(stage))
			if stage.Line_details_referenceOrder == nil {
				stage.Line_details_referenceOrder = make(map[*Line_detail]uint)
			}
			stage.Line_details_referenceOrder[line_detail] = stage.Line_detail_stagedOrder[line_detail]
			newInstancesReverseSlice = append(newInstancesReverseSlice, line_detail.GongMarshallUnstaging(stage))
			// delete(stage.Line_details_referenceOrder, line_detail)
			fieldInitializers, pointersInitializations := line_detail.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Line_detail_stagedOrder[ref] = stage.Line_detail_stagedOrder[line_detail]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := line_detail.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, line_detail)
			// delete(stage.Line_detail_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", line_detail.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Line_details_reference {
		instance := stage.Line_details_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Line_details[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			line_details_deletedInstances = append(line_details_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(line_details_newInstances)
	lenDeletedInstances += len(line_details_deletedInstances)
	var line_widths_newInstances []*Line_width
	var line_widths_deletedInstances []*Line_width

	// parse all staged instances and check if they have a reference
	for line_width := range stage.Line_widths {
		if ref, ok := stage.Line_widths_reference[line_width]; !ok {
			line_widths_newInstances = append(line_widths_newInstances, line_width)
			newInstancesSlice = append(newInstancesSlice, line_width.GongMarshallIdentifier(stage))
			if stage.Line_widths_referenceOrder == nil {
				stage.Line_widths_referenceOrder = make(map[*Line_width]uint)
			}
			stage.Line_widths_referenceOrder[line_width] = stage.Line_width_stagedOrder[line_width]
			newInstancesReverseSlice = append(newInstancesReverseSlice, line_width.GongMarshallUnstaging(stage))
			// delete(stage.Line_widths_referenceOrder, line_width)
			fieldInitializers, pointersInitializations := line_width.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Line_width_stagedOrder[ref] = stage.Line_width_stagedOrder[line_width]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := line_width.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, line_width)
			// delete(stage.Line_width_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", line_width.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Line_widths_reference {
		instance := stage.Line_widths_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Line_widths[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			line_widths_deletedInstances = append(line_widths_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(line_widths_newInstances)
	lenDeletedInstances += len(line_widths_deletedInstances)
	var links_newInstances []*Link
	var links_deletedInstances []*Link

	// parse all staged instances and check if they have a reference
	for link := range stage.Links {
		if ref, ok := stage.Links_reference[link]; !ok {
			links_newInstances = append(links_newInstances, link)
			newInstancesSlice = append(newInstancesSlice, link.GongMarshallIdentifier(stage))
			if stage.Links_referenceOrder == nil {
				stage.Links_referenceOrder = make(map[*Link]uint)
			}
			stage.Links_referenceOrder[link] = stage.Link_stagedOrder[link]
			newInstancesReverseSlice = append(newInstancesReverseSlice, link.GongMarshallUnstaging(stage))
			// delete(stage.Links_referenceOrder, link)
			fieldInitializers, pointersInitializations := link.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Link_stagedOrder[ref] = stage.Link_stagedOrder[link]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := link.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, link)
			// delete(stage.Link_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", link.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Links_reference {
		instance := stage.Links_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Links[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			links_deletedInstances = append(links_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(links_newInstances)
	lenDeletedInstances += len(links_deletedInstances)
	var listens_newInstances []*Listen
	var listens_deletedInstances []*Listen

	// parse all staged instances and check if they have a reference
	for listen := range stage.Listens {
		if ref, ok := stage.Listens_reference[listen]; !ok {
			listens_newInstances = append(listens_newInstances, listen)
			newInstancesSlice = append(newInstancesSlice, listen.GongMarshallIdentifier(stage))
			if stage.Listens_referenceOrder == nil {
				stage.Listens_referenceOrder = make(map[*Listen]uint)
			}
			stage.Listens_referenceOrder[listen] = stage.Listen_stagedOrder[listen]
			newInstancesReverseSlice = append(newInstancesReverseSlice, listen.GongMarshallUnstaging(stage))
			// delete(stage.Listens_referenceOrder, listen)
			fieldInitializers, pointersInitializations := listen.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Listen_stagedOrder[ref] = stage.Listen_stagedOrder[listen]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := listen.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, listen)
			// delete(stage.Listen_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", listen.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Listens_reference {
		instance := stage.Listens_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Listens[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			listens_deletedInstances = append(listens_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(listens_newInstances)
	lenDeletedInstances += len(listens_deletedInstances)
	var listenings_newInstances []*Listening
	var listenings_deletedInstances []*Listening

	// parse all staged instances and check if they have a reference
	for listening := range stage.Listenings {
		if ref, ok := stage.Listenings_reference[listening]; !ok {
			listenings_newInstances = append(listenings_newInstances, listening)
			newInstancesSlice = append(newInstancesSlice, listening.GongMarshallIdentifier(stage))
			if stage.Listenings_referenceOrder == nil {
				stage.Listenings_referenceOrder = make(map[*Listening]uint)
			}
			stage.Listenings_referenceOrder[listening] = stage.Listening_stagedOrder[listening]
			newInstancesReverseSlice = append(newInstancesReverseSlice, listening.GongMarshallUnstaging(stage))
			// delete(stage.Listenings_referenceOrder, listening)
			fieldInitializers, pointersInitializations := listening.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Listening_stagedOrder[ref] = stage.Listening_stagedOrder[listening]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := listening.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, listening)
			// delete(stage.Listening_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", listening.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Listenings_reference {
		instance := stage.Listenings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Listenings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			listenings_deletedInstances = append(listenings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(listenings_newInstances)
	lenDeletedInstances += len(listenings_deletedInstances)
	var lyrics_newInstances []*Lyric
	var lyrics_deletedInstances []*Lyric

	// parse all staged instances and check if they have a reference
	for lyric := range stage.Lyrics {
		if ref, ok := stage.Lyrics_reference[lyric]; !ok {
			lyrics_newInstances = append(lyrics_newInstances, lyric)
			newInstancesSlice = append(newInstancesSlice, lyric.GongMarshallIdentifier(stage))
			if stage.Lyrics_referenceOrder == nil {
				stage.Lyrics_referenceOrder = make(map[*Lyric]uint)
			}
			stage.Lyrics_referenceOrder[lyric] = stage.Lyric_stagedOrder[lyric]
			newInstancesReverseSlice = append(newInstancesReverseSlice, lyric.GongMarshallUnstaging(stage))
			// delete(stage.Lyrics_referenceOrder, lyric)
			fieldInitializers, pointersInitializations := lyric.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Lyric_stagedOrder[ref] = stage.Lyric_stagedOrder[lyric]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := lyric.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, lyric)
			// delete(stage.Lyric_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", lyric.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Lyrics_reference {
		instance := stage.Lyrics_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Lyrics[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			lyrics_deletedInstances = append(lyrics_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(lyrics_newInstances)
	lenDeletedInstances += len(lyrics_deletedInstances)
	var lyric_fonts_newInstances []*Lyric_font
	var lyric_fonts_deletedInstances []*Lyric_font

	// parse all staged instances and check if they have a reference
	for lyric_font := range stage.Lyric_fonts {
		if ref, ok := stage.Lyric_fonts_reference[lyric_font]; !ok {
			lyric_fonts_newInstances = append(lyric_fonts_newInstances, lyric_font)
			newInstancesSlice = append(newInstancesSlice, lyric_font.GongMarshallIdentifier(stage))
			if stage.Lyric_fonts_referenceOrder == nil {
				stage.Lyric_fonts_referenceOrder = make(map[*Lyric_font]uint)
			}
			stage.Lyric_fonts_referenceOrder[lyric_font] = stage.Lyric_font_stagedOrder[lyric_font]
			newInstancesReverseSlice = append(newInstancesReverseSlice, lyric_font.GongMarshallUnstaging(stage))
			// delete(stage.Lyric_fonts_referenceOrder, lyric_font)
			fieldInitializers, pointersInitializations := lyric_font.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Lyric_font_stagedOrder[ref] = stage.Lyric_font_stagedOrder[lyric_font]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := lyric_font.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, lyric_font)
			// delete(stage.Lyric_font_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", lyric_font.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Lyric_fonts_reference {
		instance := stage.Lyric_fonts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Lyric_fonts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			lyric_fonts_deletedInstances = append(lyric_fonts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(lyric_fonts_newInstances)
	lenDeletedInstances += len(lyric_fonts_deletedInstances)
	var lyric_languages_newInstances []*Lyric_language
	var lyric_languages_deletedInstances []*Lyric_language

	// parse all staged instances and check if they have a reference
	for lyric_language := range stage.Lyric_languages {
		if ref, ok := stage.Lyric_languages_reference[lyric_language]; !ok {
			lyric_languages_newInstances = append(lyric_languages_newInstances, lyric_language)
			newInstancesSlice = append(newInstancesSlice, lyric_language.GongMarshallIdentifier(stage))
			if stage.Lyric_languages_referenceOrder == nil {
				stage.Lyric_languages_referenceOrder = make(map[*Lyric_language]uint)
			}
			stage.Lyric_languages_referenceOrder[lyric_language] = stage.Lyric_language_stagedOrder[lyric_language]
			newInstancesReverseSlice = append(newInstancesReverseSlice, lyric_language.GongMarshallUnstaging(stage))
			// delete(stage.Lyric_languages_referenceOrder, lyric_language)
			fieldInitializers, pointersInitializations := lyric_language.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Lyric_language_stagedOrder[ref] = stage.Lyric_language_stagedOrder[lyric_language]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := lyric_language.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, lyric_language)
			// delete(stage.Lyric_language_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", lyric_language.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Lyric_languages_reference {
		instance := stage.Lyric_languages_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Lyric_languages[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			lyric_languages_deletedInstances = append(lyric_languages_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(lyric_languages_newInstances)
	lenDeletedInstances += len(lyric_languages_deletedInstances)
	var measure_layouts_newInstances []*Measure_layout
	var measure_layouts_deletedInstances []*Measure_layout

	// parse all staged instances and check if they have a reference
	for measure_layout := range stage.Measure_layouts {
		if ref, ok := stage.Measure_layouts_reference[measure_layout]; !ok {
			measure_layouts_newInstances = append(measure_layouts_newInstances, measure_layout)
			newInstancesSlice = append(newInstancesSlice, measure_layout.GongMarshallIdentifier(stage))
			if stage.Measure_layouts_referenceOrder == nil {
				stage.Measure_layouts_referenceOrder = make(map[*Measure_layout]uint)
			}
			stage.Measure_layouts_referenceOrder[measure_layout] = stage.Measure_layout_stagedOrder[measure_layout]
			newInstancesReverseSlice = append(newInstancesReverseSlice, measure_layout.GongMarshallUnstaging(stage))
			// delete(stage.Measure_layouts_referenceOrder, measure_layout)
			fieldInitializers, pointersInitializations := measure_layout.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Measure_layout_stagedOrder[ref] = stage.Measure_layout_stagedOrder[measure_layout]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := measure_layout.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, measure_layout)
			// delete(stage.Measure_layout_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", measure_layout.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Measure_layouts_reference {
		instance := stage.Measure_layouts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Measure_layouts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			measure_layouts_deletedInstances = append(measure_layouts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(measure_layouts_newInstances)
	lenDeletedInstances += len(measure_layouts_deletedInstances)
	var measure_numberings_newInstances []*Measure_numbering
	var measure_numberings_deletedInstances []*Measure_numbering

	// parse all staged instances and check if they have a reference
	for measure_numbering := range stage.Measure_numberings {
		if ref, ok := stage.Measure_numberings_reference[measure_numbering]; !ok {
			measure_numberings_newInstances = append(measure_numberings_newInstances, measure_numbering)
			newInstancesSlice = append(newInstancesSlice, measure_numbering.GongMarshallIdentifier(stage))
			if stage.Measure_numberings_referenceOrder == nil {
				stage.Measure_numberings_referenceOrder = make(map[*Measure_numbering]uint)
			}
			stage.Measure_numberings_referenceOrder[measure_numbering] = stage.Measure_numbering_stagedOrder[measure_numbering]
			newInstancesReverseSlice = append(newInstancesReverseSlice, measure_numbering.GongMarshallUnstaging(stage))
			// delete(stage.Measure_numberings_referenceOrder, measure_numbering)
			fieldInitializers, pointersInitializations := measure_numbering.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Measure_numbering_stagedOrder[ref] = stage.Measure_numbering_stagedOrder[measure_numbering]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := measure_numbering.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, measure_numbering)
			// delete(stage.Measure_numbering_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", measure_numbering.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Measure_numberings_reference {
		instance := stage.Measure_numberings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Measure_numberings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			measure_numberings_deletedInstances = append(measure_numberings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(measure_numberings_newInstances)
	lenDeletedInstances += len(measure_numberings_deletedInstances)
	var measure_repeats_newInstances []*Measure_repeat
	var measure_repeats_deletedInstances []*Measure_repeat

	// parse all staged instances and check if they have a reference
	for measure_repeat := range stage.Measure_repeats {
		if ref, ok := stage.Measure_repeats_reference[measure_repeat]; !ok {
			measure_repeats_newInstances = append(measure_repeats_newInstances, measure_repeat)
			newInstancesSlice = append(newInstancesSlice, measure_repeat.GongMarshallIdentifier(stage))
			if stage.Measure_repeats_referenceOrder == nil {
				stage.Measure_repeats_referenceOrder = make(map[*Measure_repeat]uint)
			}
			stage.Measure_repeats_referenceOrder[measure_repeat] = stage.Measure_repeat_stagedOrder[measure_repeat]
			newInstancesReverseSlice = append(newInstancesReverseSlice, measure_repeat.GongMarshallUnstaging(stage))
			// delete(stage.Measure_repeats_referenceOrder, measure_repeat)
			fieldInitializers, pointersInitializations := measure_repeat.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Measure_repeat_stagedOrder[ref] = stage.Measure_repeat_stagedOrder[measure_repeat]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := measure_repeat.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, measure_repeat)
			// delete(stage.Measure_repeat_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", measure_repeat.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Measure_repeats_reference {
		instance := stage.Measure_repeats_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Measure_repeats[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			measure_repeats_deletedInstances = append(measure_repeats_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(measure_repeats_newInstances)
	lenDeletedInstances += len(measure_repeats_deletedInstances)
	var measure_styles_newInstances []*Measure_style
	var measure_styles_deletedInstances []*Measure_style

	// parse all staged instances and check if they have a reference
	for measure_style := range stage.Measure_styles {
		if ref, ok := stage.Measure_styles_reference[measure_style]; !ok {
			measure_styles_newInstances = append(measure_styles_newInstances, measure_style)
			newInstancesSlice = append(newInstancesSlice, measure_style.GongMarshallIdentifier(stage))
			if stage.Measure_styles_referenceOrder == nil {
				stage.Measure_styles_referenceOrder = make(map[*Measure_style]uint)
			}
			stage.Measure_styles_referenceOrder[measure_style] = stage.Measure_style_stagedOrder[measure_style]
			newInstancesReverseSlice = append(newInstancesReverseSlice, measure_style.GongMarshallUnstaging(stage))
			// delete(stage.Measure_styles_referenceOrder, measure_style)
			fieldInitializers, pointersInitializations := measure_style.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Measure_style_stagedOrder[ref] = stage.Measure_style_stagedOrder[measure_style]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := measure_style.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, measure_style)
			// delete(stage.Measure_style_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", measure_style.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Measure_styles_reference {
		instance := stage.Measure_styles_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Measure_styles[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			measure_styles_deletedInstances = append(measure_styles_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(measure_styles_newInstances)
	lenDeletedInstances += len(measure_styles_deletedInstances)
	var membranes_newInstances []*Membrane
	var membranes_deletedInstances []*Membrane

	// parse all staged instances and check if they have a reference
	for membrane := range stage.Membranes {
		if ref, ok := stage.Membranes_reference[membrane]; !ok {
			membranes_newInstances = append(membranes_newInstances, membrane)
			newInstancesSlice = append(newInstancesSlice, membrane.GongMarshallIdentifier(stage))
			if stage.Membranes_referenceOrder == nil {
				stage.Membranes_referenceOrder = make(map[*Membrane]uint)
			}
			stage.Membranes_referenceOrder[membrane] = stage.Membrane_stagedOrder[membrane]
			newInstancesReverseSlice = append(newInstancesReverseSlice, membrane.GongMarshallUnstaging(stage))
			// delete(stage.Membranes_referenceOrder, membrane)
			fieldInitializers, pointersInitializations := membrane.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Membrane_stagedOrder[ref] = stage.Membrane_stagedOrder[membrane]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := membrane.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, membrane)
			// delete(stage.Membrane_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", membrane.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Membranes_reference {
		instance := stage.Membranes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Membranes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			membranes_deletedInstances = append(membranes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(membranes_newInstances)
	lenDeletedInstances += len(membranes_deletedInstances)
	var metals_newInstances []*Metal
	var metals_deletedInstances []*Metal

	// parse all staged instances and check if they have a reference
	for metal := range stage.Metals {
		if ref, ok := stage.Metals_reference[metal]; !ok {
			metals_newInstances = append(metals_newInstances, metal)
			newInstancesSlice = append(newInstancesSlice, metal.GongMarshallIdentifier(stage))
			if stage.Metals_referenceOrder == nil {
				stage.Metals_referenceOrder = make(map[*Metal]uint)
			}
			stage.Metals_referenceOrder[metal] = stage.Metal_stagedOrder[metal]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metal.GongMarshallUnstaging(stage))
			// delete(stage.Metals_referenceOrder, metal)
			fieldInitializers, pointersInitializations := metal.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metal_stagedOrder[ref] = stage.Metal_stagedOrder[metal]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metal.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metal)
			// delete(stage.Metal_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metal.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metals_reference {
		instance := stage.Metals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metals_deletedInstances = append(metals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metals_newInstances)
	lenDeletedInstances += len(metals_deletedInstances)
	var metronomes_newInstances []*Metronome
	var metronomes_deletedInstances []*Metronome

	// parse all staged instances and check if they have a reference
	for metronome := range stage.Metronomes {
		if ref, ok := stage.Metronomes_reference[metronome]; !ok {
			metronomes_newInstances = append(metronomes_newInstances, metronome)
			newInstancesSlice = append(newInstancesSlice, metronome.GongMarshallIdentifier(stage))
			if stage.Metronomes_referenceOrder == nil {
				stage.Metronomes_referenceOrder = make(map[*Metronome]uint)
			}
			stage.Metronomes_referenceOrder[metronome] = stage.Metronome_stagedOrder[metronome]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metronome.GongMarshallUnstaging(stage))
			// delete(stage.Metronomes_referenceOrder, metronome)
			fieldInitializers, pointersInitializations := metronome.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metronome_stagedOrder[ref] = stage.Metronome_stagedOrder[metronome]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metronome.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metronome)
			// delete(stage.Metronome_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metronome.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metronomes_reference {
		instance := stage.Metronomes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metronomes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metronomes_deletedInstances = append(metronomes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metronomes_newInstances)
	lenDeletedInstances += len(metronomes_deletedInstances)
	var metronome_beams_newInstances []*Metronome_beam
	var metronome_beams_deletedInstances []*Metronome_beam

	// parse all staged instances and check if they have a reference
	for metronome_beam := range stage.Metronome_beams {
		if ref, ok := stage.Metronome_beams_reference[metronome_beam]; !ok {
			metronome_beams_newInstances = append(metronome_beams_newInstances, metronome_beam)
			newInstancesSlice = append(newInstancesSlice, metronome_beam.GongMarshallIdentifier(stage))
			if stage.Metronome_beams_referenceOrder == nil {
				stage.Metronome_beams_referenceOrder = make(map[*Metronome_beam]uint)
			}
			stage.Metronome_beams_referenceOrder[metronome_beam] = stage.Metronome_beam_stagedOrder[metronome_beam]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metronome_beam.GongMarshallUnstaging(stage))
			// delete(stage.Metronome_beams_referenceOrder, metronome_beam)
			fieldInitializers, pointersInitializations := metronome_beam.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metronome_beam_stagedOrder[ref] = stage.Metronome_beam_stagedOrder[metronome_beam]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metronome_beam.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metronome_beam)
			// delete(stage.Metronome_beam_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metronome_beam.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metronome_beams_reference {
		instance := stage.Metronome_beams_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metronome_beams[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metronome_beams_deletedInstances = append(metronome_beams_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metronome_beams_newInstances)
	lenDeletedInstances += len(metronome_beams_deletedInstances)
	var metronome_notes_newInstances []*Metronome_note
	var metronome_notes_deletedInstances []*Metronome_note

	// parse all staged instances and check if they have a reference
	for metronome_note := range stage.Metronome_notes {
		if ref, ok := stage.Metronome_notes_reference[metronome_note]; !ok {
			metronome_notes_newInstances = append(metronome_notes_newInstances, metronome_note)
			newInstancesSlice = append(newInstancesSlice, metronome_note.GongMarshallIdentifier(stage))
			if stage.Metronome_notes_referenceOrder == nil {
				stage.Metronome_notes_referenceOrder = make(map[*Metronome_note]uint)
			}
			stage.Metronome_notes_referenceOrder[metronome_note] = stage.Metronome_note_stagedOrder[metronome_note]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metronome_note.GongMarshallUnstaging(stage))
			// delete(stage.Metronome_notes_referenceOrder, metronome_note)
			fieldInitializers, pointersInitializations := metronome_note.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metronome_note_stagedOrder[ref] = stage.Metronome_note_stagedOrder[metronome_note]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metronome_note.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metronome_note)
			// delete(stage.Metronome_note_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metronome_note.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metronome_notes_reference {
		instance := stage.Metronome_notes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metronome_notes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metronome_notes_deletedInstances = append(metronome_notes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metronome_notes_newInstances)
	lenDeletedInstances += len(metronome_notes_deletedInstances)
	var metronome_tieds_newInstances []*Metronome_tied
	var metronome_tieds_deletedInstances []*Metronome_tied

	// parse all staged instances and check if they have a reference
	for metronome_tied := range stage.Metronome_tieds {
		if ref, ok := stage.Metronome_tieds_reference[metronome_tied]; !ok {
			metronome_tieds_newInstances = append(metronome_tieds_newInstances, metronome_tied)
			newInstancesSlice = append(newInstancesSlice, metronome_tied.GongMarshallIdentifier(stage))
			if stage.Metronome_tieds_referenceOrder == nil {
				stage.Metronome_tieds_referenceOrder = make(map[*Metronome_tied]uint)
			}
			stage.Metronome_tieds_referenceOrder[metronome_tied] = stage.Metronome_tied_stagedOrder[metronome_tied]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metronome_tied.GongMarshallUnstaging(stage))
			// delete(stage.Metronome_tieds_referenceOrder, metronome_tied)
			fieldInitializers, pointersInitializations := metronome_tied.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metronome_tied_stagedOrder[ref] = stage.Metronome_tied_stagedOrder[metronome_tied]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metronome_tied.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metronome_tied)
			// delete(stage.Metronome_tied_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metronome_tied.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metronome_tieds_reference {
		instance := stage.Metronome_tieds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metronome_tieds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metronome_tieds_deletedInstances = append(metronome_tieds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metronome_tieds_newInstances)
	lenDeletedInstances += len(metronome_tieds_deletedInstances)
	var metronome_tuplets_newInstances []*Metronome_tuplet
	var metronome_tuplets_deletedInstances []*Metronome_tuplet

	// parse all staged instances and check if they have a reference
	for metronome_tuplet := range stage.Metronome_tuplets {
		if ref, ok := stage.Metronome_tuplets_reference[metronome_tuplet]; !ok {
			metronome_tuplets_newInstances = append(metronome_tuplets_newInstances, metronome_tuplet)
			newInstancesSlice = append(newInstancesSlice, metronome_tuplet.GongMarshallIdentifier(stage))
			if stage.Metronome_tuplets_referenceOrder == nil {
				stage.Metronome_tuplets_referenceOrder = make(map[*Metronome_tuplet]uint)
			}
			stage.Metronome_tuplets_referenceOrder[metronome_tuplet] = stage.Metronome_tuplet_stagedOrder[metronome_tuplet]
			newInstancesReverseSlice = append(newInstancesReverseSlice, metronome_tuplet.GongMarshallUnstaging(stage))
			// delete(stage.Metronome_tuplets_referenceOrder, metronome_tuplet)
			fieldInitializers, pointersInitializations := metronome_tuplet.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Metronome_tuplet_stagedOrder[ref] = stage.Metronome_tuplet_stagedOrder[metronome_tuplet]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := metronome_tuplet.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, metronome_tuplet)
			// delete(stage.Metronome_tuplet_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", metronome_tuplet.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Metronome_tuplets_reference {
		instance := stage.Metronome_tuplets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Metronome_tuplets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			metronome_tuplets_deletedInstances = append(metronome_tuplets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(metronome_tuplets_newInstances)
	lenDeletedInstances += len(metronome_tuplets_deletedInstances)
	var midi_devices_newInstances []*Midi_device
	var midi_devices_deletedInstances []*Midi_device

	// parse all staged instances and check if they have a reference
	for midi_device := range stage.Midi_devices {
		if ref, ok := stage.Midi_devices_reference[midi_device]; !ok {
			midi_devices_newInstances = append(midi_devices_newInstances, midi_device)
			newInstancesSlice = append(newInstancesSlice, midi_device.GongMarshallIdentifier(stage))
			if stage.Midi_devices_referenceOrder == nil {
				stage.Midi_devices_referenceOrder = make(map[*Midi_device]uint)
			}
			stage.Midi_devices_referenceOrder[midi_device] = stage.Midi_device_stagedOrder[midi_device]
			newInstancesReverseSlice = append(newInstancesReverseSlice, midi_device.GongMarshallUnstaging(stage))
			// delete(stage.Midi_devices_referenceOrder, midi_device)
			fieldInitializers, pointersInitializations := midi_device.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Midi_device_stagedOrder[ref] = stage.Midi_device_stagedOrder[midi_device]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := midi_device.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, midi_device)
			// delete(stage.Midi_device_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", midi_device.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Midi_devices_reference {
		instance := stage.Midi_devices_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Midi_devices[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			midi_devices_deletedInstances = append(midi_devices_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(midi_devices_newInstances)
	lenDeletedInstances += len(midi_devices_deletedInstances)
	var midi_instruments_newInstances []*Midi_instrument
	var midi_instruments_deletedInstances []*Midi_instrument

	// parse all staged instances and check if they have a reference
	for midi_instrument := range stage.Midi_instruments {
		if ref, ok := stage.Midi_instruments_reference[midi_instrument]; !ok {
			midi_instruments_newInstances = append(midi_instruments_newInstances, midi_instrument)
			newInstancesSlice = append(newInstancesSlice, midi_instrument.GongMarshallIdentifier(stage))
			if stage.Midi_instruments_referenceOrder == nil {
				stage.Midi_instruments_referenceOrder = make(map[*Midi_instrument]uint)
			}
			stage.Midi_instruments_referenceOrder[midi_instrument] = stage.Midi_instrument_stagedOrder[midi_instrument]
			newInstancesReverseSlice = append(newInstancesReverseSlice, midi_instrument.GongMarshallUnstaging(stage))
			// delete(stage.Midi_instruments_referenceOrder, midi_instrument)
			fieldInitializers, pointersInitializations := midi_instrument.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Midi_instrument_stagedOrder[ref] = stage.Midi_instrument_stagedOrder[midi_instrument]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := midi_instrument.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, midi_instrument)
			// delete(stage.Midi_instrument_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", midi_instrument.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Midi_instruments_reference {
		instance := stage.Midi_instruments_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Midi_instruments[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			midi_instruments_deletedInstances = append(midi_instruments_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(midi_instruments_newInstances)
	lenDeletedInstances += len(midi_instruments_deletedInstances)
	var miscellaneouss_newInstances []*Miscellaneous
	var miscellaneouss_deletedInstances []*Miscellaneous

	// parse all staged instances and check if they have a reference
	for miscellaneous := range stage.Miscellaneouss {
		if ref, ok := stage.Miscellaneouss_reference[miscellaneous]; !ok {
			miscellaneouss_newInstances = append(miscellaneouss_newInstances, miscellaneous)
			newInstancesSlice = append(newInstancesSlice, miscellaneous.GongMarshallIdentifier(stage))
			if stage.Miscellaneouss_referenceOrder == nil {
				stage.Miscellaneouss_referenceOrder = make(map[*Miscellaneous]uint)
			}
			stage.Miscellaneouss_referenceOrder[miscellaneous] = stage.Miscellaneous_stagedOrder[miscellaneous]
			newInstancesReverseSlice = append(newInstancesReverseSlice, miscellaneous.GongMarshallUnstaging(stage))
			// delete(stage.Miscellaneouss_referenceOrder, miscellaneous)
			fieldInitializers, pointersInitializations := miscellaneous.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Miscellaneous_stagedOrder[ref] = stage.Miscellaneous_stagedOrder[miscellaneous]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := miscellaneous.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, miscellaneous)
			// delete(stage.Miscellaneous_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", miscellaneous.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Miscellaneouss_reference {
		instance := stage.Miscellaneouss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Miscellaneouss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			miscellaneouss_deletedInstances = append(miscellaneouss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(miscellaneouss_newInstances)
	lenDeletedInstances += len(miscellaneouss_deletedInstances)
	var miscellaneous_fields_newInstances []*Miscellaneous_field
	var miscellaneous_fields_deletedInstances []*Miscellaneous_field

	// parse all staged instances and check if they have a reference
	for miscellaneous_field := range stage.Miscellaneous_fields {
		if ref, ok := stage.Miscellaneous_fields_reference[miscellaneous_field]; !ok {
			miscellaneous_fields_newInstances = append(miscellaneous_fields_newInstances, miscellaneous_field)
			newInstancesSlice = append(newInstancesSlice, miscellaneous_field.GongMarshallIdentifier(stage))
			if stage.Miscellaneous_fields_referenceOrder == nil {
				stage.Miscellaneous_fields_referenceOrder = make(map[*Miscellaneous_field]uint)
			}
			stage.Miscellaneous_fields_referenceOrder[miscellaneous_field] = stage.Miscellaneous_field_stagedOrder[miscellaneous_field]
			newInstancesReverseSlice = append(newInstancesReverseSlice, miscellaneous_field.GongMarshallUnstaging(stage))
			// delete(stage.Miscellaneous_fields_referenceOrder, miscellaneous_field)
			fieldInitializers, pointersInitializations := miscellaneous_field.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Miscellaneous_field_stagedOrder[ref] = stage.Miscellaneous_field_stagedOrder[miscellaneous_field]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := miscellaneous_field.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, miscellaneous_field)
			// delete(stage.Miscellaneous_field_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", miscellaneous_field.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Miscellaneous_fields_reference {
		instance := stage.Miscellaneous_fields_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Miscellaneous_fields[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			miscellaneous_fields_deletedInstances = append(miscellaneous_fields_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(miscellaneous_fields_newInstances)
	lenDeletedInstances += len(miscellaneous_fields_deletedInstances)
	var mordents_newInstances []*Mordent
	var mordents_deletedInstances []*Mordent

	// parse all staged instances and check if they have a reference
	for mordent := range stage.Mordents {
		if ref, ok := stage.Mordents_reference[mordent]; !ok {
			mordents_newInstances = append(mordents_newInstances, mordent)
			newInstancesSlice = append(newInstancesSlice, mordent.GongMarshallIdentifier(stage))
			if stage.Mordents_referenceOrder == nil {
				stage.Mordents_referenceOrder = make(map[*Mordent]uint)
			}
			stage.Mordents_referenceOrder[mordent] = stage.Mordent_stagedOrder[mordent]
			newInstancesReverseSlice = append(newInstancesReverseSlice, mordent.GongMarshallUnstaging(stage))
			// delete(stage.Mordents_referenceOrder, mordent)
			fieldInitializers, pointersInitializations := mordent.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Mordent_stagedOrder[ref] = stage.Mordent_stagedOrder[mordent]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := mordent.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, mordent)
			// delete(stage.Mordent_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", mordent.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Mordents_reference {
		instance := stage.Mordents_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Mordents[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			mordents_deletedInstances = append(mordents_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(mordents_newInstances)
	lenDeletedInstances += len(mordents_deletedInstances)
	var multiple_rests_newInstances []*Multiple_rest
	var multiple_rests_deletedInstances []*Multiple_rest

	// parse all staged instances and check if they have a reference
	for multiple_rest := range stage.Multiple_rests {
		if ref, ok := stage.Multiple_rests_reference[multiple_rest]; !ok {
			multiple_rests_newInstances = append(multiple_rests_newInstances, multiple_rest)
			newInstancesSlice = append(newInstancesSlice, multiple_rest.GongMarshallIdentifier(stage))
			if stage.Multiple_rests_referenceOrder == nil {
				stage.Multiple_rests_referenceOrder = make(map[*Multiple_rest]uint)
			}
			stage.Multiple_rests_referenceOrder[multiple_rest] = stage.Multiple_rest_stagedOrder[multiple_rest]
			newInstancesReverseSlice = append(newInstancesReverseSlice, multiple_rest.GongMarshallUnstaging(stage))
			// delete(stage.Multiple_rests_referenceOrder, multiple_rest)
			fieldInitializers, pointersInitializations := multiple_rest.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Multiple_rest_stagedOrder[ref] = stage.Multiple_rest_stagedOrder[multiple_rest]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := multiple_rest.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, multiple_rest)
			// delete(stage.Multiple_rest_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", multiple_rest.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Multiple_rests_reference {
		instance := stage.Multiple_rests_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Multiple_rests[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			multiple_rests_deletedInstances = append(multiple_rests_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(multiple_rests_newInstances)
	lenDeletedInstances += len(multiple_rests_deletedInstances)
	var name_displays_newInstances []*Name_display
	var name_displays_deletedInstances []*Name_display

	// parse all staged instances and check if they have a reference
	for name_display := range stage.Name_displays {
		if ref, ok := stage.Name_displays_reference[name_display]; !ok {
			name_displays_newInstances = append(name_displays_newInstances, name_display)
			newInstancesSlice = append(newInstancesSlice, name_display.GongMarshallIdentifier(stage))
			if stage.Name_displays_referenceOrder == nil {
				stage.Name_displays_referenceOrder = make(map[*Name_display]uint)
			}
			stage.Name_displays_referenceOrder[name_display] = stage.Name_display_stagedOrder[name_display]
			newInstancesReverseSlice = append(newInstancesReverseSlice, name_display.GongMarshallUnstaging(stage))
			// delete(stage.Name_displays_referenceOrder, name_display)
			fieldInitializers, pointersInitializations := name_display.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Name_display_stagedOrder[ref] = stage.Name_display_stagedOrder[name_display]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := name_display.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, name_display)
			// delete(stage.Name_display_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", name_display.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Name_displays_reference {
		instance := stage.Name_displays_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Name_displays[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			name_displays_deletedInstances = append(name_displays_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(name_displays_newInstances)
	lenDeletedInstances += len(name_displays_deletedInstances)
	var non_arpeggiates_newInstances []*Non_arpeggiate
	var non_arpeggiates_deletedInstances []*Non_arpeggiate

	// parse all staged instances and check if they have a reference
	for non_arpeggiate := range stage.Non_arpeggiates {
		if ref, ok := stage.Non_arpeggiates_reference[non_arpeggiate]; !ok {
			non_arpeggiates_newInstances = append(non_arpeggiates_newInstances, non_arpeggiate)
			newInstancesSlice = append(newInstancesSlice, non_arpeggiate.GongMarshallIdentifier(stage))
			if stage.Non_arpeggiates_referenceOrder == nil {
				stage.Non_arpeggiates_referenceOrder = make(map[*Non_arpeggiate]uint)
			}
			stage.Non_arpeggiates_referenceOrder[non_arpeggiate] = stage.Non_arpeggiate_stagedOrder[non_arpeggiate]
			newInstancesReverseSlice = append(newInstancesReverseSlice, non_arpeggiate.GongMarshallUnstaging(stage))
			// delete(stage.Non_arpeggiates_referenceOrder, non_arpeggiate)
			fieldInitializers, pointersInitializations := non_arpeggiate.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Non_arpeggiate_stagedOrder[ref] = stage.Non_arpeggiate_stagedOrder[non_arpeggiate]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := non_arpeggiate.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, non_arpeggiate)
			// delete(stage.Non_arpeggiate_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", non_arpeggiate.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Non_arpeggiates_reference {
		instance := stage.Non_arpeggiates_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Non_arpeggiates[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			non_arpeggiates_deletedInstances = append(non_arpeggiates_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(non_arpeggiates_newInstances)
	lenDeletedInstances += len(non_arpeggiates_deletedInstances)
	var notationss_newInstances []*Notations
	var notationss_deletedInstances []*Notations

	// parse all staged instances and check if they have a reference
	for notations := range stage.Notationss {
		if ref, ok := stage.Notationss_reference[notations]; !ok {
			notationss_newInstances = append(notationss_newInstances, notations)
			newInstancesSlice = append(newInstancesSlice, notations.GongMarshallIdentifier(stage))
			if stage.Notationss_referenceOrder == nil {
				stage.Notationss_referenceOrder = make(map[*Notations]uint)
			}
			stage.Notationss_referenceOrder[notations] = stage.Notations_stagedOrder[notations]
			newInstancesReverseSlice = append(newInstancesReverseSlice, notations.GongMarshallUnstaging(stage))
			// delete(stage.Notationss_referenceOrder, notations)
			fieldInitializers, pointersInitializations := notations.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Notations_stagedOrder[ref] = stage.Notations_stagedOrder[notations]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := notations.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, notations)
			// delete(stage.Notations_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", notations.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Notationss_reference {
		instance := stage.Notationss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Notationss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			notationss_deletedInstances = append(notationss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(notationss_newInstances)
	lenDeletedInstances += len(notationss_deletedInstances)
	var notes_newInstances []*Note
	var notes_deletedInstances []*Note

	// parse all staged instances and check if they have a reference
	for note := range stage.Notes {
		if ref, ok := stage.Notes_reference[note]; !ok {
			notes_newInstances = append(notes_newInstances, note)
			newInstancesSlice = append(newInstancesSlice, note.GongMarshallIdentifier(stage))
			if stage.Notes_referenceOrder == nil {
				stage.Notes_referenceOrder = make(map[*Note]uint)
			}
			stage.Notes_referenceOrder[note] = stage.Note_stagedOrder[note]
			newInstancesReverseSlice = append(newInstancesReverseSlice, note.GongMarshallUnstaging(stage))
			// delete(stage.Notes_referenceOrder, note)
			fieldInitializers, pointersInitializations := note.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Note_stagedOrder[ref] = stage.Note_stagedOrder[note]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := note.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, note)
			// delete(stage.Note_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", note.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Notes_reference {
		instance := stage.Notes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Notes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			notes_deletedInstances = append(notes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(notes_newInstances)
	lenDeletedInstances += len(notes_deletedInstances)
	var note_sizes_newInstances []*Note_size
	var note_sizes_deletedInstances []*Note_size

	// parse all staged instances and check if they have a reference
	for note_size := range stage.Note_sizes {
		if ref, ok := stage.Note_sizes_reference[note_size]; !ok {
			note_sizes_newInstances = append(note_sizes_newInstances, note_size)
			newInstancesSlice = append(newInstancesSlice, note_size.GongMarshallIdentifier(stage))
			if stage.Note_sizes_referenceOrder == nil {
				stage.Note_sizes_referenceOrder = make(map[*Note_size]uint)
			}
			stage.Note_sizes_referenceOrder[note_size] = stage.Note_size_stagedOrder[note_size]
			newInstancesReverseSlice = append(newInstancesReverseSlice, note_size.GongMarshallUnstaging(stage))
			// delete(stage.Note_sizes_referenceOrder, note_size)
			fieldInitializers, pointersInitializations := note_size.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Note_size_stagedOrder[ref] = stage.Note_size_stagedOrder[note_size]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := note_size.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, note_size)
			// delete(stage.Note_size_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", note_size.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Note_sizes_reference {
		instance := stage.Note_sizes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Note_sizes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			note_sizes_deletedInstances = append(note_sizes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(note_sizes_newInstances)
	lenDeletedInstances += len(note_sizes_deletedInstances)
	var note_types_newInstances []*Note_type
	var note_types_deletedInstances []*Note_type

	// parse all staged instances and check if they have a reference
	for note_type := range stage.Note_types {
		if ref, ok := stage.Note_types_reference[note_type]; !ok {
			note_types_newInstances = append(note_types_newInstances, note_type)
			newInstancesSlice = append(newInstancesSlice, note_type.GongMarshallIdentifier(stage))
			if stage.Note_types_referenceOrder == nil {
				stage.Note_types_referenceOrder = make(map[*Note_type]uint)
			}
			stage.Note_types_referenceOrder[note_type] = stage.Note_type_stagedOrder[note_type]
			newInstancesReverseSlice = append(newInstancesReverseSlice, note_type.GongMarshallUnstaging(stage))
			// delete(stage.Note_types_referenceOrder, note_type)
			fieldInitializers, pointersInitializations := note_type.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Note_type_stagedOrder[ref] = stage.Note_type_stagedOrder[note_type]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := note_type.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, note_type)
			// delete(stage.Note_type_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", note_type.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Note_types_reference {
		instance := stage.Note_types_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Note_types[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			note_types_deletedInstances = append(note_types_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(note_types_newInstances)
	lenDeletedInstances += len(note_types_deletedInstances)
	var noteheads_newInstances []*Notehead
	var noteheads_deletedInstances []*Notehead

	// parse all staged instances and check if they have a reference
	for notehead := range stage.Noteheads {
		if ref, ok := stage.Noteheads_reference[notehead]; !ok {
			noteheads_newInstances = append(noteheads_newInstances, notehead)
			newInstancesSlice = append(newInstancesSlice, notehead.GongMarshallIdentifier(stage))
			if stage.Noteheads_referenceOrder == nil {
				stage.Noteheads_referenceOrder = make(map[*Notehead]uint)
			}
			stage.Noteheads_referenceOrder[notehead] = stage.Notehead_stagedOrder[notehead]
			newInstancesReverseSlice = append(newInstancesReverseSlice, notehead.GongMarshallUnstaging(stage))
			// delete(stage.Noteheads_referenceOrder, notehead)
			fieldInitializers, pointersInitializations := notehead.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Notehead_stagedOrder[ref] = stage.Notehead_stagedOrder[notehead]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := notehead.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, notehead)
			// delete(stage.Notehead_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", notehead.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Noteheads_reference {
		instance := stage.Noteheads_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Noteheads[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			noteheads_deletedInstances = append(noteheads_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(noteheads_newInstances)
	lenDeletedInstances += len(noteheads_deletedInstances)
	var notehead_texts_newInstances []*Notehead_text
	var notehead_texts_deletedInstances []*Notehead_text

	// parse all staged instances and check if they have a reference
	for notehead_text := range stage.Notehead_texts {
		if ref, ok := stage.Notehead_texts_reference[notehead_text]; !ok {
			notehead_texts_newInstances = append(notehead_texts_newInstances, notehead_text)
			newInstancesSlice = append(newInstancesSlice, notehead_text.GongMarshallIdentifier(stage))
			if stage.Notehead_texts_referenceOrder == nil {
				stage.Notehead_texts_referenceOrder = make(map[*Notehead_text]uint)
			}
			stage.Notehead_texts_referenceOrder[notehead_text] = stage.Notehead_text_stagedOrder[notehead_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, notehead_text.GongMarshallUnstaging(stage))
			// delete(stage.Notehead_texts_referenceOrder, notehead_text)
			fieldInitializers, pointersInitializations := notehead_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Notehead_text_stagedOrder[ref] = stage.Notehead_text_stagedOrder[notehead_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := notehead_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, notehead_text)
			// delete(stage.Notehead_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", notehead_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Notehead_texts_reference {
		instance := stage.Notehead_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Notehead_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			notehead_texts_deletedInstances = append(notehead_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(notehead_texts_newInstances)
	lenDeletedInstances += len(notehead_texts_deletedInstances)
	var numerals_newInstances []*Numeral
	var numerals_deletedInstances []*Numeral

	// parse all staged instances and check if they have a reference
	for numeral := range stage.Numerals {
		if ref, ok := stage.Numerals_reference[numeral]; !ok {
			numerals_newInstances = append(numerals_newInstances, numeral)
			newInstancesSlice = append(newInstancesSlice, numeral.GongMarshallIdentifier(stage))
			if stage.Numerals_referenceOrder == nil {
				stage.Numerals_referenceOrder = make(map[*Numeral]uint)
			}
			stage.Numerals_referenceOrder[numeral] = stage.Numeral_stagedOrder[numeral]
			newInstancesReverseSlice = append(newInstancesReverseSlice, numeral.GongMarshallUnstaging(stage))
			// delete(stage.Numerals_referenceOrder, numeral)
			fieldInitializers, pointersInitializations := numeral.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Numeral_stagedOrder[ref] = stage.Numeral_stagedOrder[numeral]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := numeral.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, numeral)
			// delete(stage.Numeral_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", numeral.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Numerals_reference {
		instance := stage.Numerals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Numerals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			numerals_deletedInstances = append(numerals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(numerals_newInstances)
	lenDeletedInstances += len(numerals_deletedInstances)
	var numeral_keys_newInstances []*Numeral_key
	var numeral_keys_deletedInstances []*Numeral_key

	// parse all staged instances and check if they have a reference
	for numeral_key := range stage.Numeral_keys {
		if ref, ok := stage.Numeral_keys_reference[numeral_key]; !ok {
			numeral_keys_newInstances = append(numeral_keys_newInstances, numeral_key)
			newInstancesSlice = append(newInstancesSlice, numeral_key.GongMarshallIdentifier(stage))
			if stage.Numeral_keys_referenceOrder == nil {
				stage.Numeral_keys_referenceOrder = make(map[*Numeral_key]uint)
			}
			stage.Numeral_keys_referenceOrder[numeral_key] = stage.Numeral_key_stagedOrder[numeral_key]
			newInstancesReverseSlice = append(newInstancesReverseSlice, numeral_key.GongMarshallUnstaging(stage))
			// delete(stage.Numeral_keys_referenceOrder, numeral_key)
			fieldInitializers, pointersInitializations := numeral_key.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Numeral_key_stagedOrder[ref] = stage.Numeral_key_stagedOrder[numeral_key]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := numeral_key.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, numeral_key)
			// delete(stage.Numeral_key_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", numeral_key.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Numeral_keys_reference {
		instance := stage.Numeral_keys_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Numeral_keys[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			numeral_keys_deletedInstances = append(numeral_keys_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(numeral_keys_newInstances)
	lenDeletedInstances += len(numeral_keys_deletedInstances)
	var numeral_roots_newInstances []*Numeral_root
	var numeral_roots_deletedInstances []*Numeral_root

	// parse all staged instances and check if they have a reference
	for numeral_root := range stage.Numeral_roots {
		if ref, ok := stage.Numeral_roots_reference[numeral_root]; !ok {
			numeral_roots_newInstances = append(numeral_roots_newInstances, numeral_root)
			newInstancesSlice = append(newInstancesSlice, numeral_root.GongMarshallIdentifier(stage))
			if stage.Numeral_roots_referenceOrder == nil {
				stage.Numeral_roots_referenceOrder = make(map[*Numeral_root]uint)
			}
			stage.Numeral_roots_referenceOrder[numeral_root] = stage.Numeral_root_stagedOrder[numeral_root]
			newInstancesReverseSlice = append(newInstancesReverseSlice, numeral_root.GongMarshallUnstaging(stage))
			// delete(stage.Numeral_roots_referenceOrder, numeral_root)
			fieldInitializers, pointersInitializations := numeral_root.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Numeral_root_stagedOrder[ref] = stage.Numeral_root_stagedOrder[numeral_root]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := numeral_root.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, numeral_root)
			// delete(stage.Numeral_root_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", numeral_root.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Numeral_roots_reference {
		instance := stage.Numeral_roots_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Numeral_roots[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			numeral_roots_deletedInstances = append(numeral_roots_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(numeral_roots_newInstances)
	lenDeletedInstances += len(numeral_roots_deletedInstances)
	var octave_shifts_newInstances []*Octave_shift
	var octave_shifts_deletedInstances []*Octave_shift

	// parse all staged instances and check if they have a reference
	for octave_shift := range stage.Octave_shifts {
		if ref, ok := stage.Octave_shifts_reference[octave_shift]; !ok {
			octave_shifts_newInstances = append(octave_shifts_newInstances, octave_shift)
			newInstancesSlice = append(newInstancesSlice, octave_shift.GongMarshallIdentifier(stage))
			if stage.Octave_shifts_referenceOrder == nil {
				stage.Octave_shifts_referenceOrder = make(map[*Octave_shift]uint)
			}
			stage.Octave_shifts_referenceOrder[octave_shift] = stage.Octave_shift_stagedOrder[octave_shift]
			newInstancesReverseSlice = append(newInstancesReverseSlice, octave_shift.GongMarshallUnstaging(stage))
			// delete(stage.Octave_shifts_referenceOrder, octave_shift)
			fieldInitializers, pointersInitializations := octave_shift.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Octave_shift_stagedOrder[ref] = stage.Octave_shift_stagedOrder[octave_shift]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := octave_shift.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, octave_shift)
			// delete(stage.Octave_shift_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", octave_shift.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Octave_shifts_reference {
		instance := stage.Octave_shifts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Octave_shifts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			octave_shifts_deletedInstances = append(octave_shifts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(octave_shifts_newInstances)
	lenDeletedInstances += len(octave_shifts_deletedInstances)
	var offsets_newInstances []*Offset
	var offsets_deletedInstances []*Offset

	// parse all staged instances and check if they have a reference
	for offset := range stage.Offsets {
		if ref, ok := stage.Offsets_reference[offset]; !ok {
			offsets_newInstances = append(offsets_newInstances, offset)
			newInstancesSlice = append(newInstancesSlice, offset.GongMarshallIdentifier(stage))
			if stage.Offsets_referenceOrder == nil {
				stage.Offsets_referenceOrder = make(map[*Offset]uint)
			}
			stage.Offsets_referenceOrder[offset] = stage.Offset_stagedOrder[offset]
			newInstancesReverseSlice = append(newInstancesReverseSlice, offset.GongMarshallUnstaging(stage))
			// delete(stage.Offsets_referenceOrder, offset)
			fieldInitializers, pointersInitializations := offset.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Offset_stagedOrder[ref] = stage.Offset_stagedOrder[offset]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := offset.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, offset)
			// delete(stage.Offset_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", offset.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Offsets_reference {
		instance := stage.Offsets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Offsets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			offsets_deletedInstances = append(offsets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(offsets_newInstances)
	lenDeletedInstances += len(offsets_deletedInstances)
	var opuss_newInstances []*Opus
	var opuss_deletedInstances []*Opus

	// parse all staged instances and check if they have a reference
	for opus := range stage.Opuss {
		if ref, ok := stage.Opuss_reference[opus]; !ok {
			opuss_newInstances = append(opuss_newInstances, opus)
			newInstancesSlice = append(newInstancesSlice, opus.GongMarshallIdentifier(stage))
			if stage.Opuss_referenceOrder == nil {
				stage.Opuss_referenceOrder = make(map[*Opus]uint)
			}
			stage.Opuss_referenceOrder[opus] = stage.Opus_stagedOrder[opus]
			newInstancesReverseSlice = append(newInstancesReverseSlice, opus.GongMarshallUnstaging(stage))
			// delete(stage.Opuss_referenceOrder, opus)
			fieldInitializers, pointersInitializations := opus.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Opus_stagedOrder[ref] = stage.Opus_stagedOrder[opus]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := opus.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, opus)
			// delete(stage.Opus_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", opus.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Opuss_reference {
		instance := stage.Opuss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Opuss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			opuss_deletedInstances = append(opuss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(opuss_newInstances)
	lenDeletedInstances += len(opuss_deletedInstances)
	var ornamentss_newInstances []*Ornaments
	var ornamentss_deletedInstances []*Ornaments

	// parse all staged instances and check if they have a reference
	for ornaments := range stage.Ornamentss {
		if ref, ok := stage.Ornamentss_reference[ornaments]; !ok {
			ornamentss_newInstances = append(ornamentss_newInstances, ornaments)
			newInstancesSlice = append(newInstancesSlice, ornaments.GongMarshallIdentifier(stage))
			if stage.Ornamentss_referenceOrder == nil {
				stage.Ornamentss_referenceOrder = make(map[*Ornaments]uint)
			}
			stage.Ornamentss_referenceOrder[ornaments] = stage.Ornaments_stagedOrder[ornaments]
			newInstancesReverseSlice = append(newInstancesReverseSlice, ornaments.GongMarshallUnstaging(stage))
			// delete(stage.Ornamentss_referenceOrder, ornaments)
			fieldInitializers, pointersInitializations := ornaments.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Ornaments_stagedOrder[ref] = stage.Ornaments_stagedOrder[ornaments]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := ornaments.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, ornaments)
			// delete(stage.Ornaments_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", ornaments.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Ornamentss_reference {
		instance := stage.Ornamentss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Ornamentss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			ornamentss_deletedInstances = append(ornamentss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(ornamentss_newInstances)
	lenDeletedInstances += len(ornamentss_deletedInstances)
	var other_appearances_newInstances []*Other_appearance
	var other_appearances_deletedInstances []*Other_appearance

	// parse all staged instances and check if they have a reference
	for other_appearance := range stage.Other_appearances {
		if ref, ok := stage.Other_appearances_reference[other_appearance]; !ok {
			other_appearances_newInstances = append(other_appearances_newInstances, other_appearance)
			newInstancesSlice = append(newInstancesSlice, other_appearance.GongMarshallIdentifier(stage))
			if stage.Other_appearances_referenceOrder == nil {
				stage.Other_appearances_referenceOrder = make(map[*Other_appearance]uint)
			}
			stage.Other_appearances_referenceOrder[other_appearance] = stage.Other_appearance_stagedOrder[other_appearance]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_appearance.GongMarshallUnstaging(stage))
			// delete(stage.Other_appearances_referenceOrder, other_appearance)
			fieldInitializers, pointersInitializations := other_appearance.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_appearance_stagedOrder[ref] = stage.Other_appearance_stagedOrder[other_appearance]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_appearance.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_appearance)
			// delete(stage.Other_appearance_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_appearance.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_appearances_reference {
		instance := stage.Other_appearances_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_appearances[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_appearances_deletedInstances = append(other_appearances_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_appearances_newInstances)
	lenDeletedInstances += len(other_appearances_deletedInstances)
	var other_directions_newInstances []*Other_direction
	var other_directions_deletedInstances []*Other_direction

	// parse all staged instances and check if they have a reference
	for other_direction := range stage.Other_directions {
		if ref, ok := stage.Other_directions_reference[other_direction]; !ok {
			other_directions_newInstances = append(other_directions_newInstances, other_direction)
			newInstancesSlice = append(newInstancesSlice, other_direction.GongMarshallIdentifier(stage))
			if stage.Other_directions_referenceOrder == nil {
				stage.Other_directions_referenceOrder = make(map[*Other_direction]uint)
			}
			stage.Other_directions_referenceOrder[other_direction] = stage.Other_direction_stagedOrder[other_direction]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_direction.GongMarshallUnstaging(stage))
			// delete(stage.Other_directions_referenceOrder, other_direction)
			fieldInitializers, pointersInitializations := other_direction.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_direction_stagedOrder[ref] = stage.Other_direction_stagedOrder[other_direction]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_direction.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_direction)
			// delete(stage.Other_direction_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_direction.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_directions_reference {
		instance := stage.Other_directions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_directions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_directions_deletedInstances = append(other_directions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_directions_newInstances)
	lenDeletedInstances += len(other_directions_deletedInstances)
	var other_listenings_newInstances []*Other_listening
	var other_listenings_deletedInstances []*Other_listening

	// parse all staged instances and check if they have a reference
	for other_listening := range stage.Other_listenings {
		if ref, ok := stage.Other_listenings_reference[other_listening]; !ok {
			other_listenings_newInstances = append(other_listenings_newInstances, other_listening)
			newInstancesSlice = append(newInstancesSlice, other_listening.GongMarshallIdentifier(stage))
			if stage.Other_listenings_referenceOrder == nil {
				stage.Other_listenings_referenceOrder = make(map[*Other_listening]uint)
			}
			stage.Other_listenings_referenceOrder[other_listening] = stage.Other_listening_stagedOrder[other_listening]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_listening.GongMarshallUnstaging(stage))
			// delete(stage.Other_listenings_referenceOrder, other_listening)
			fieldInitializers, pointersInitializations := other_listening.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_listening_stagedOrder[ref] = stage.Other_listening_stagedOrder[other_listening]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_listening.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_listening)
			// delete(stage.Other_listening_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_listening.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_listenings_reference {
		instance := stage.Other_listenings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_listenings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_listenings_deletedInstances = append(other_listenings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_listenings_newInstances)
	lenDeletedInstances += len(other_listenings_deletedInstances)
	var other_notations_newInstances []*Other_notation
	var other_notations_deletedInstances []*Other_notation

	// parse all staged instances and check if they have a reference
	for other_notation := range stage.Other_notations {
		if ref, ok := stage.Other_notations_reference[other_notation]; !ok {
			other_notations_newInstances = append(other_notations_newInstances, other_notation)
			newInstancesSlice = append(newInstancesSlice, other_notation.GongMarshallIdentifier(stage))
			if stage.Other_notations_referenceOrder == nil {
				stage.Other_notations_referenceOrder = make(map[*Other_notation]uint)
			}
			stage.Other_notations_referenceOrder[other_notation] = stage.Other_notation_stagedOrder[other_notation]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_notation.GongMarshallUnstaging(stage))
			// delete(stage.Other_notations_referenceOrder, other_notation)
			fieldInitializers, pointersInitializations := other_notation.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_notation_stagedOrder[ref] = stage.Other_notation_stagedOrder[other_notation]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_notation.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_notation)
			// delete(stage.Other_notation_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_notation.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_notations_reference {
		instance := stage.Other_notations_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_notations[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_notations_deletedInstances = append(other_notations_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_notations_newInstances)
	lenDeletedInstances += len(other_notations_deletedInstances)
	var other_placement_texts_newInstances []*Other_placement_text
	var other_placement_texts_deletedInstances []*Other_placement_text

	// parse all staged instances and check if they have a reference
	for other_placement_text := range stage.Other_placement_texts {
		if ref, ok := stage.Other_placement_texts_reference[other_placement_text]; !ok {
			other_placement_texts_newInstances = append(other_placement_texts_newInstances, other_placement_text)
			newInstancesSlice = append(newInstancesSlice, other_placement_text.GongMarshallIdentifier(stage))
			if stage.Other_placement_texts_referenceOrder == nil {
				stage.Other_placement_texts_referenceOrder = make(map[*Other_placement_text]uint)
			}
			stage.Other_placement_texts_referenceOrder[other_placement_text] = stage.Other_placement_text_stagedOrder[other_placement_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_placement_text.GongMarshallUnstaging(stage))
			// delete(stage.Other_placement_texts_referenceOrder, other_placement_text)
			fieldInitializers, pointersInitializations := other_placement_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_placement_text_stagedOrder[ref] = stage.Other_placement_text_stagedOrder[other_placement_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_placement_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_placement_text)
			// delete(stage.Other_placement_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_placement_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_placement_texts_reference {
		instance := stage.Other_placement_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_placement_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_placement_texts_deletedInstances = append(other_placement_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_placement_texts_newInstances)
	lenDeletedInstances += len(other_placement_texts_deletedInstances)
	var other_plays_newInstances []*Other_play
	var other_plays_deletedInstances []*Other_play

	// parse all staged instances and check if they have a reference
	for other_play := range stage.Other_plays {
		if ref, ok := stage.Other_plays_reference[other_play]; !ok {
			other_plays_newInstances = append(other_plays_newInstances, other_play)
			newInstancesSlice = append(newInstancesSlice, other_play.GongMarshallIdentifier(stage))
			if stage.Other_plays_referenceOrder == nil {
				stage.Other_plays_referenceOrder = make(map[*Other_play]uint)
			}
			stage.Other_plays_referenceOrder[other_play] = stage.Other_play_stagedOrder[other_play]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_play.GongMarshallUnstaging(stage))
			// delete(stage.Other_plays_referenceOrder, other_play)
			fieldInitializers, pointersInitializations := other_play.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_play_stagedOrder[ref] = stage.Other_play_stagedOrder[other_play]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_play.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_play)
			// delete(stage.Other_play_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_play.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_plays_reference {
		instance := stage.Other_plays_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_plays[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_plays_deletedInstances = append(other_plays_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_plays_newInstances)
	lenDeletedInstances += len(other_plays_deletedInstances)
	var other_texts_newInstances []*Other_text
	var other_texts_deletedInstances []*Other_text

	// parse all staged instances and check if they have a reference
	for other_text := range stage.Other_texts {
		if ref, ok := stage.Other_texts_reference[other_text]; !ok {
			other_texts_newInstances = append(other_texts_newInstances, other_text)
			newInstancesSlice = append(newInstancesSlice, other_text.GongMarshallIdentifier(stage))
			if stage.Other_texts_referenceOrder == nil {
				stage.Other_texts_referenceOrder = make(map[*Other_text]uint)
			}
			stage.Other_texts_referenceOrder[other_text] = stage.Other_text_stagedOrder[other_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, other_text.GongMarshallUnstaging(stage))
			// delete(stage.Other_texts_referenceOrder, other_text)
			fieldInitializers, pointersInitializations := other_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Other_text_stagedOrder[ref] = stage.Other_text_stagedOrder[other_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := other_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, other_text)
			// delete(stage.Other_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", other_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Other_texts_reference {
		instance := stage.Other_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Other_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			other_texts_deletedInstances = append(other_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(other_texts_newInstances)
	lenDeletedInstances += len(other_texts_deletedInstances)
	var page_layouts_newInstances []*Page_layout
	var page_layouts_deletedInstances []*Page_layout

	// parse all staged instances and check if they have a reference
	for page_layout := range stage.Page_layouts {
		if ref, ok := stage.Page_layouts_reference[page_layout]; !ok {
			page_layouts_newInstances = append(page_layouts_newInstances, page_layout)
			newInstancesSlice = append(newInstancesSlice, page_layout.GongMarshallIdentifier(stage))
			if stage.Page_layouts_referenceOrder == nil {
				stage.Page_layouts_referenceOrder = make(map[*Page_layout]uint)
			}
			stage.Page_layouts_referenceOrder[page_layout] = stage.Page_layout_stagedOrder[page_layout]
			newInstancesReverseSlice = append(newInstancesReverseSlice, page_layout.GongMarshallUnstaging(stage))
			// delete(stage.Page_layouts_referenceOrder, page_layout)
			fieldInitializers, pointersInitializations := page_layout.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Page_layout_stagedOrder[ref] = stage.Page_layout_stagedOrder[page_layout]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := page_layout.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, page_layout)
			// delete(stage.Page_layout_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", page_layout.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Page_layouts_reference {
		instance := stage.Page_layouts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Page_layouts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			page_layouts_deletedInstances = append(page_layouts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(page_layouts_newInstances)
	lenDeletedInstances += len(page_layouts_deletedInstances)
	var page_marginss_newInstances []*Page_margins
	var page_marginss_deletedInstances []*Page_margins

	// parse all staged instances and check if they have a reference
	for page_margins := range stage.Page_marginss {
		if ref, ok := stage.Page_marginss_reference[page_margins]; !ok {
			page_marginss_newInstances = append(page_marginss_newInstances, page_margins)
			newInstancesSlice = append(newInstancesSlice, page_margins.GongMarshallIdentifier(stage))
			if stage.Page_marginss_referenceOrder == nil {
				stage.Page_marginss_referenceOrder = make(map[*Page_margins]uint)
			}
			stage.Page_marginss_referenceOrder[page_margins] = stage.Page_margins_stagedOrder[page_margins]
			newInstancesReverseSlice = append(newInstancesReverseSlice, page_margins.GongMarshallUnstaging(stage))
			// delete(stage.Page_marginss_referenceOrder, page_margins)
			fieldInitializers, pointersInitializations := page_margins.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Page_margins_stagedOrder[ref] = stage.Page_margins_stagedOrder[page_margins]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := page_margins.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, page_margins)
			// delete(stage.Page_margins_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", page_margins.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Page_marginss_reference {
		instance := stage.Page_marginss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Page_marginss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			page_marginss_deletedInstances = append(page_marginss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(page_marginss_newInstances)
	lenDeletedInstances += len(page_marginss_deletedInstances)
	var part_clefs_newInstances []*Part_clef
	var part_clefs_deletedInstances []*Part_clef

	// parse all staged instances and check if they have a reference
	for part_clef := range stage.Part_clefs {
		if ref, ok := stage.Part_clefs_reference[part_clef]; !ok {
			part_clefs_newInstances = append(part_clefs_newInstances, part_clef)
			newInstancesSlice = append(newInstancesSlice, part_clef.GongMarshallIdentifier(stage))
			if stage.Part_clefs_referenceOrder == nil {
				stage.Part_clefs_referenceOrder = make(map[*Part_clef]uint)
			}
			stage.Part_clefs_referenceOrder[part_clef] = stage.Part_clef_stagedOrder[part_clef]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_clef.GongMarshallUnstaging(stage))
			// delete(stage.Part_clefs_referenceOrder, part_clef)
			fieldInitializers, pointersInitializations := part_clef.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_clef_stagedOrder[ref] = stage.Part_clef_stagedOrder[part_clef]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_clef.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_clef)
			// delete(stage.Part_clef_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_clef.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_clefs_reference {
		instance := stage.Part_clefs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_clefs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_clefs_deletedInstances = append(part_clefs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_clefs_newInstances)
	lenDeletedInstances += len(part_clefs_deletedInstances)
	var part_groups_newInstances []*Part_group
	var part_groups_deletedInstances []*Part_group

	// parse all staged instances and check if they have a reference
	for part_group := range stage.Part_groups {
		if ref, ok := stage.Part_groups_reference[part_group]; !ok {
			part_groups_newInstances = append(part_groups_newInstances, part_group)
			newInstancesSlice = append(newInstancesSlice, part_group.GongMarshallIdentifier(stage))
			if stage.Part_groups_referenceOrder == nil {
				stage.Part_groups_referenceOrder = make(map[*Part_group]uint)
			}
			stage.Part_groups_referenceOrder[part_group] = stage.Part_group_stagedOrder[part_group]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_group.GongMarshallUnstaging(stage))
			// delete(stage.Part_groups_referenceOrder, part_group)
			fieldInitializers, pointersInitializations := part_group.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_group_stagedOrder[ref] = stage.Part_group_stagedOrder[part_group]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_group.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_group)
			// delete(stage.Part_group_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_group.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_groups_reference {
		instance := stage.Part_groups_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_groups[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_groups_deletedInstances = append(part_groups_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_groups_newInstances)
	lenDeletedInstances += len(part_groups_deletedInstances)
	var part_links_newInstances []*Part_link
	var part_links_deletedInstances []*Part_link

	// parse all staged instances and check if they have a reference
	for part_link := range stage.Part_links {
		if ref, ok := stage.Part_links_reference[part_link]; !ok {
			part_links_newInstances = append(part_links_newInstances, part_link)
			newInstancesSlice = append(newInstancesSlice, part_link.GongMarshallIdentifier(stage))
			if stage.Part_links_referenceOrder == nil {
				stage.Part_links_referenceOrder = make(map[*Part_link]uint)
			}
			stage.Part_links_referenceOrder[part_link] = stage.Part_link_stagedOrder[part_link]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_link.GongMarshallUnstaging(stage))
			// delete(stage.Part_links_referenceOrder, part_link)
			fieldInitializers, pointersInitializations := part_link.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_link_stagedOrder[ref] = stage.Part_link_stagedOrder[part_link]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_link.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_link)
			// delete(stage.Part_link_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_link.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_links_reference {
		instance := stage.Part_links_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_links[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_links_deletedInstances = append(part_links_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_links_newInstances)
	lenDeletedInstances += len(part_links_deletedInstances)
	var part_lists_newInstances []*Part_list
	var part_lists_deletedInstances []*Part_list

	// parse all staged instances and check if they have a reference
	for part_list := range stage.Part_lists {
		if ref, ok := stage.Part_lists_reference[part_list]; !ok {
			part_lists_newInstances = append(part_lists_newInstances, part_list)
			newInstancesSlice = append(newInstancesSlice, part_list.GongMarshallIdentifier(stage))
			if stage.Part_lists_referenceOrder == nil {
				stage.Part_lists_referenceOrder = make(map[*Part_list]uint)
			}
			stage.Part_lists_referenceOrder[part_list] = stage.Part_list_stagedOrder[part_list]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_list.GongMarshallUnstaging(stage))
			// delete(stage.Part_lists_referenceOrder, part_list)
			fieldInitializers, pointersInitializations := part_list.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_list_stagedOrder[ref] = stage.Part_list_stagedOrder[part_list]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_list.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_list)
			// delete(stage.Part_list_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_list.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_lists_reference {
		instance := stage.Part_lists_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_lists[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_lists_deletedInstances = append(part_lists_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_lists_newInstances)
	lenDeletedInstances += len(part_lists_deletedInstances)
	var part_names_newInstances []*Part_name
	var part_names_deletedInstances []*Part_name

	// parse all staged instances and check if they have a reference
	for part_name := range stage.Part_names {
		if ref, ok := stage.Part_names_reference[part_name]; !ok {
			part_names_newInstances = append(part_names_newInstances, part_name)
			newInstancesSlice = append(newInstancesSlice, part_name.GongMarshallIdentifier(stage))
			if stage.Part_names_referenceOrder == nil {
				stage.Part_names_referenceOrder = make(map[*Part_name]uint)
			}
			stage.Part_names_referenceOrder[part_name] = stage.Part_name_stagedOrder[part_name]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_name.GongMarshallUnstaging(stage))
			// delete(stage.Part_names_referenceOrder, part_name)
			fieldInitializers, pointersInitializations := part_name.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_name_stagedOrder[ref] = stage.Part_name_stagedOrder[part_name]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_name.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_name)
			// delete(stage.Part_name_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_name.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_names_reference {
		instance := stage.Part_names_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_names[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_names_deletedInstances = append(part_names_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_names_newInstances)
	lenDeletedInstances += len(part_names_deletedInstances)
	var part_symbols_newInstances []*Part_symbol
	var part_symbols_deletedInstances []*Part_symbol

	// parse all staged instances and check if they have a reference
	for part_symbol := range stage.Part_symbols {
		if ref, ok := stage.Part_symbols_reference[part_symbol]; !ok {
			part_symbols_newInstances = append(part_symbols_newInstances, part_symbol)
			newInstancesSlice = append(newInstancesSlice, part_symbol.GongMarshallIdentifier(stage))
			if stage.Part_symbols_referenceOrder == nil {
				stage.Part_symbols_referenceOrder = make(map[*Part_symbol]uint)
			}
			stage.Part_symbols_referenceOrder[part_symbol] = stage.Part_symbol_stagedOrder[part_symbol]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_symbol.GongMarshallUnstaging(stage))
			// delete(stage.Part_symbols_referenceOrder, part_symbol)
			fieldInitializers, pointersInitializations := part_symbol.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_symbol_stagedOrder[ref] = stage.Part_symbol_stagedOrder[part_symbol]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_symbol.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_symbol)
			// delete(stage.Part_symbol_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_symbol.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_symbols_reference {
		instance := stage.Part_symbols_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_symbols[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_symbols_deletedInstances = append(part_symbols_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_symbols_newInstances)
	lenDeletedInstances += len(part_symbols_deletedInstances)
	var part_transposes_newInstances []*Part_transpose
	var part_transposes_deletedInstances []*Part_transpose

	// parse all staged instances and check if they have a reference
	for part_transpose := range stage.Part_transposes {
		if ref, ok := stage.Part_transposes_reference[part_transpose]; !ok {
			part_transposes_newInstances = append(part_transposes_newInstances, part_transpose)
			newInstancesSlice = append(newInstancesSlice, part_transpose.GongMarshallIdentifier(stage))
			if stage.Part_transposes_referenceOrder == nil {
				stage.Part_transposes_referenceOrder = make(map[*Part_transpose]uint)
			}
			stage.Part_transposes_referenceOrder[part_transpose] = stage.Part_transpose_stagedOrder[part_transpose]
			newInstancesReverseSlice = append(newInstancesReverseSlice, part_transpose.GongMarshallUnstaging(stage))
			// delete(stage.Part_transposes_referenceOrder, part_transpose)
			fieldInitializers, pointersInitializations := part_transpose.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Part_transpose_stagedOrder[ref] = stage.Part_transpose_stagedOrder[part_transpose]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := part_transpose.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, part_transpose)
			// delete(stage.Part_transpose_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", part_transpose.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Part_transposes_reference {
		instance := stage.Part_transposes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Part_transposes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			part_transposes_deletedInstances = append(part_transposes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(part_transposes_newInstances)
	lenDeletedInstances += len(part_transposes_deletedInstances)
	var pedals_newInstances []*Pedal
	var pedals_deletedInstances []*Pedal

	// parse all staged instances and check if they have a reference
	for pedal := range stage.Pedals {
		if ref, ok := stage.Pedals_reference[pedal]; !ok {
			pedals_newInstances = append(pedals_newInstances, pedal)
			newInstancesSlice = append(newInstancesSlice, pedal.GongMarshallIdentifier(stage))
			if stage.Pedals_referenceOrder == nil {
				stage.Pedals_referenceOrder = make(map[*Pedal]uint)
			}
			stage.Pedals_referenceOrder[pedal] = stage.Pedal_stagedOrder[pedal]
			newInstancesReverseSlice = append(newInstancesReverseSlice, pedal.GongMarshallUnstaging(stage))
			// delete(stage.Pedals_referenceOrder, pedal)
			fieldInitializers, pointersInitializations := pedal.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Pedal_stagedOrder[ref] = stage.Pedal_stagedOrder[pedal]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := pedal.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, pedal)
			// delete(stage.Pedal_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", pedal.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Pedals_reference {
		instance := stage.Pedals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Pedals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			pedals_deletedInstances = append(pedals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(pedals_newInstances)
	lenDeletedInstances += len(pedals_deletedInstances)
	var pedal_tunings_newInstances []*Pedal_tuning
	var pedal_tunings_deletedInstances []*Pedal_tuning

	// parse all staged instances and check if they have a reference
	for pedal_tuning := range stage.Pedal_tunings {
		if ref, ok := stage.Pedal_tunings_reference[pedal_tuning]; !ok {
			pedal_tunings_newInstances = append(pedal_tunings_newInstances, pedal_tuning)
			newInstancesSlice = append(newInstancesSlice, pedal_tuning.GongMarshallIdentifier(stage))
			if stage.Pedal_tunings_referenceOrder == nil {
				stage.Pedal_tunings_referenceOrder = make(map[*Pedal_tuning]uint)
			}
			stage.Pedal_tunings_referenceOrder[pedal_tuning] = stage.Pedal_tuning_stagedOrder[pedal_tuning]
			newInstancesReverseSlice = append(newInstancesReverseSlice, pedal_tuning.GongMarshallUnstaging(stage))
			// delete(stage.Pedal_tunings_referenceOrder, pedal_tuning)
			fieldInitializers, pointersInitializations := pedal_tuning.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Pedal_tuning_stagedOrder[ref] = stage.Pedal_tuning_stagedOrder[pedal_tuning]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := pedal_tuning.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, pedal_tuning)
			// delete(stage.Pedal_tuning_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", pedal_tuning.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Pedal_tunings_reference {
		instance := stage.Pedal_tunings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Pedal_tunings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			pedal_tunings_deletedInstances = append(pedal_tunings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(pedal_tunings_newInstances)
	lenDeletedInstances += len(pedal_tunings_deletedInstances)
	var per_minutes_newInstances []*Per_minute
	var per_minutes_deletedInstances []*Per_minute

	// parse all staged instances and check if they have a reference
	for per_minute := range stage.Per_minutes {
		if ref, ok := stage.Per_minutes_reference[per_minute]; !ok {
			per_minutes_newInstances = append(per_minutes_newInstances, per_minute)
			newInstancesSlice = append(newInstancesSlice, per_minute.GongMarshallIdentifier(stage))
			if stage.Per_minutes_referenceOrder == nil {
				stage.Per_minutes_referenceOrder = make(map[*Per_minute]uint)
			}
			stage.Per_minutes_referenceOrder[per_minute] = stage.Per_minute_stagedOrder[per_minute]
			newInstancesReverseSlice = append(newInstancesReverseSlice, per_minute.GongMarshallUnstaging(stage))
			// delete(stage.Per_minutes_referenceOrder, per_minute)
			fieldInitializers, pointersInitializations := per_minute.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Per_minute_stagedOrder[ref] = stage.Per_minute_stagedOrder[per_minute]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := per_minute.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, per_minute)
			// delete(stage.Per_minute_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", per_minute.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Per_minutes_reference {
		instance := stage.Per_minutes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Per_minutes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			per_minutes_deletedInstances = append(per_minutes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(per_minutes_newInstances)
	lenDeletedInstances += len(per_minutes_deletedInstances)
	var percussions_newInstances []*Percussion
	var percussions_deletedInstances []*Percussion

	// parse all staged instances and check if they have a reference
	for percussion := range stage.Percussions {
		if ref, ok := stage.Percussions_reference[percussion]; !ok {
			percussions_newInstances = append(percussions_newInstances, percussion)
			newInstancesSlice = append(newInstancesSlice, percussion.GongMarshallIdentifier(stage))
			if stage.Percussions_referenceOrder == nil {
				stage.Percussions_referenceOrder = make(map[*Percussion]uint)
			}
			stage.Percussions_referenceOrder[percussion] = stage.Percussion_stagedOrder[percussion]
			newInstancesReverseSlice = append(newInstancesReverseSlice, percussion.GongMarshallUnstaging(stage))
			// delete(stage.Percussions_referenceOrder, percussion)
			fieldInitializers, pointersInitializations := percussion.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Percussion_stagedOrder[ref] = stage.Percussion_stagedOrder[percussion]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := percussion.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, percussion)
			// delete(stage.Percussion_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", percussion.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Percussions_reference {
		instance := stage.Percussions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Percussions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			percussions_deletedInstances = append(percussions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(percussions_newInstances)
	lenDeletedInstances += len(percussions_deletedInstances)
	var pitchs_newInstances []*Pitch
	var pitchs_deletedInstances []*Pitch

	// parse all staged instances and check if they have a reference
	for pitch := range stage.Pitchs {
		if ref, ok := stage.Pitchs_reference[pitch]; !ok {
			pitchs_newInstances = append(pitchs_newInstances, pitch)
			newInstancesSlice = append(newInstancesSlice, pitch.GongMarshallIdentifier(stage))
			if stage.Pitchs_referenceOrder == nil {
				stage.Pitchs_referenceOrder = make(map[*Pitch]uint)
			}
			stage.Pitchs_referenceOrder[pitch] = stage.Pitch_stagedOrder[pitch]
			newInstancesReverseSlice = append(newInstancesReverseSlice, pitch.GongMarshallUnstaging(stage))
			// delete(stage.Pitchs_referenceOrder, pitch)
			fieldInitializers, pointersInitializations := pitch.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Pitch_stagedOrder[ref] = stage.Pitch_stagedOrder[pitch]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := pitch.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, pitch)
			// delete(stage.Pitch_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", pitch.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Pitchs_reference {
		instance := stage.Pitchs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Pitchs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			pitchs_deletedInstances = append(pitchs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(pitchs_newInstances)
	lenDeletedInstances += len(pitchs_deletedInstances)
	var pitcheds_newInstances []*Pitched
	var pitcheds_deletedInstances []*Pitched

	// parse all staged instances and check if they have a reference
	for pitched := range stage.Pitcheds {
		if ref, ok := stage.Pitcheds_reference[pitched]; !ok {
			pitcheds_newInstances = append(pitcheds_newInstances, pitched)
			newInstancesSlice = append(newInstancesSlice, pitched.GongMarshallIdentifier(stage))
			if stage.Pitcheds_referenceOrder == nil {
				stage.Pitcheds_referenceOrder = make(map[*Pitched]uint)
			}
			stage.Pitcheds_referenceOrder[pitched] = stage.Pitched_stagedOrder[pitched]
			newInstancesReverseSlice = append(newInstancesReverseSlice, pitched.GongMarshallUnstaging(stage))
			// delete(stage.Pitcheds_referenceOrder, pitched)
			fieldInitializers, pointersInitializations := pitched.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Pitched_stagedOrder[ref] = stage.Pitched_stagedOrder[pitched]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := pitched.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, pitched)
			// delete(stage.Pitched_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", pitched.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Pitcheds_reference {
		instance := stage.Pitcheds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Pitcheds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			pitcheds_deletedInstances = append(pitcheds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(pitcheds_newInstances)
	lenDeletedInstances += len(pitcheds_deletedInstances)
	var placement_texts_newInstances []*Placement_text
	var placement_texts_deletedInstances []*Placement_text

	// parse all staged instances and check if they have a reference
	for placement_text := range stage.Placement_texts {
		if ref, ok := stage.Placement_texts_reference[placement_text]; !ok {
			placement_texts_newInstances = append(placement_texts_newInstances, placement_text)
			newInstancesSlice = append(newInstancesSlice, placement_text.GongMarshallIdentifier(stage))
			if stage.Placement_texts_referenceOrder == nil {
				stage.Placement_texts_referenceOrder = make(map[*Placement_text]uint)
			}
			stage.Placement_texts_referenceOrder[placement_text] = stage.Placement_text_stagedOrder[placement_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, placement_text.GongMarshallUnstaging(stage))
			// delete(stage.Placement_texts_referenceOrder, placement_text)
			fieldInitializers, pointersInitializations := placement_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Placement_text_stagedOrder[ref] = stage.Placement_text_stagedOrder[placement_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := placement_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, placement_text)
			// delete(stage.Placement_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", placement_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Placement_texts_reference {
		instance := stage.Placement_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Placement_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			placement_texts_deletedInstances = append(placement_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(placement_texts_newInstances)
	lenDeletedInstances += len(placement_texts_deletedInstances)
	var plays_newInstances []*Play
	var plays_deletedInstances []*Play

	// parse all staged instances and check if they have a reference
	for play := range stage.Plays {
		if ref, ok := stage.Plays_reference[play]; !ok {
			plays_newInstances = append(plays_newInstances, play)
			newInstancesSlice = append(newInstancesSlice, play.GongMarshallIdentifier(stage))
			if stage.Plays_referenceOrder == nil {
				stage.Plays_referenceOrder = make(map[*Play]uint)
			}
			stage.Plays_referenceOrder[play] = stage.Play_stagedOrder[play]
			newInstancesReverseSlice = append(newInstancesReverseSlice, play.GongMarshallUnstaging(stage))
			// delete(stage.Plays_referenceOrder, play)
			fieldInitializers, pointersInitializations := play.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Play_stagedOrder[ref] = stage.Play_stagedOrder[play]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := play.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, play)
			// delete(stage.Play_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", play.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Plays_reference {
		instance := stage.Plays_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Plays[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			plays_deletedInstances = append(plays_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(plays_newInstances)
	lenDeletedInstances += len(plays_deletedInstances)
	var players_newInstances []*Player
	var players_deletedInstances []*Player

	// parse all staged instances and check if they have a reference
	for player := range stage.Players {
		if ref, ok := stage.Players_reference[player]; !ok {
			players_newInstances = append(players_newInstances, player)
			newInstancesSlice = append(newInstancesSlice, player.GongMarshallIdentifier(stage))
			if stage.Players_referenceOrder == nil {
				stage.Players_referenceOrder = make(map[*Player]uint)
			}
			stage.Players_referenceOrder[player] = stage.Player_stagedOrder[player]
			newInstancesReverseSlice = append(newInstancesReverseSlice, player.GongMarshallUnstaging(stage))
			// delete(stage.Players_referenceOrder, player)
			fieldInitializers, pointersInitializations := player.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Player_stagedOrder[ref] = stage.Player_stagedOrder[player]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := player.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, player)
			// delete(stage.Player_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", player.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Players_reference {
		instance := stage.Players_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Players[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			players_deletedInstances = append(players_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(players_newInstances)
	lenDeletedInstances += len(players_deletedInstances)
	var principal_voices_newInstances []*Principal_voice
	var principal_voices_deletedInstances []*Principal_voice

	// parse all staged instances and check if they have a reference
	for principal_voice := range stage.Principal_voices {
		if ref, ok := stage.Principal_voices_reference[principal_voice]; !ok {
			principal_voices_newInstances = append(principal_voices_newInstances, principal_voice)
			newInstancesSlice = append(newInstancesSlice, principal_voice.GongMarshallIdentifier(stage))
			if stage.Principal_voices_referenceOrder == nil {
				stage.Principal_voices_referenceOrder = make(map[*Principal_voice]uint)
			}
			stage.Principal_voices_referenceOrder[principal_voice] = stage.Principal_voice_stagedOrder[principal_voice]
			newInstancesReverseSlice = append(newInstancesReverseSlice, principal_voice.GongMarshallUnstaging(stage))
			// delete(stage.Principal_voices_referenceOrder, principal_voice)
			fieldInitializers, pointersInitializations := principal_voice.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Principal_voice_stagedOrder[ref] = stage.Principal_voice_stagedOrder[principal_voice]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := principal_voice.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, principal_voice)
			// delete(stage.Principal_voice_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", principal_voice.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Principal_voices_reference {
		instance := stage.Principal_voices_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Principal_voices[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			principal_voices_deletedInstances = append(principal_voices_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(principal_voices_newInstances)
	lenDeletedInstances += len(principal_voices_deletedInstances)
	var prints_newInstances []*Print
	var prints_deletedInstances []*Print

	// parse all staged instances and check if they have a reference
	for print := range stage.Prints {
		if ref, ok := stage.Prints_reference[print]; !ok {
			prints_newInstances = append(prints_newInstances, print)
			newInstancesSlice = append(newInstancesSlice, print.GongMarshallIdentifier(stage))
			if stage.Prints_referenceOrder == nil {
				stage.Prints_referenceOrder = make(map[*Print]uint)
			}
			stage.Prints_referenceOrder[print] = stage.Print_stagedOrder[print]
			newInstancesReverseSlice = append(newInstancesReverseSlice, print.GongMarshallUnstaging(stage))
			// delete(stage.Prints_referenceOrder, print)
			fieldInitializers, pointersInitializations := print.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Print_stagedOrder[ref] = stage.Print_stagedOrder[print]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := print.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, print)
			// delete(stage.Print_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", print.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Prints_reference {
		instance := stage.Prints_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Prints[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			prints_deletedInstances = append(prints_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(prints_newInstances)
	lenDeletedInstances += len(prints_deletedInstances)
	var releases_newInstances []*Release
	var releases_deletedInstances []*Release

	// parse all staged instances and check if they have a reference
	for release := range stage.Releases {
		if ref, ok := stage.Releases_reference[release]; !ok {
			releases_newInstances = append(releases_newInstances, release)
			newInstancesSlice = append(newInstancesSlice, release.GongMarshallIdentifier(stage))
			if stage.Releases_referenceOrder == nil {
				stage.Releases_referenceOrder = make(map[*Release]uint)
			}
			stage.Releases_referenceOrder[release] = stage.Release_stagedOrder[release]
			newInstancesReverseSlice = append(newInstancesReverseSlice, release.GongMarshallUnstaging(stage))
			// delete(stage.Releases_referenceOrder, release)
			fieldInitializers, pointersInitializations := release.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Release_stagedOrder[ref] = stage.Release_stagedOrder[release]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := release.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, release)
			// delete(stage.Release_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", release.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Releases_reference {
		instance := stage.Releases_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Releases[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			releases_deletedInstances = append(releases_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(releases_newInstances)
	lenDeletedInstances += len(releases_deletedInstances)
	var repeats_newInstances []*Repeat
	var repeats_deletedInstances []*Repeat

	// parse all staged instances and check if they have a reference
	for repeat := range stage.Repeats {
		if ref, ok := stage.Repeats_reference[repeat]; !ok {
			repeats_newInstances = append(repeats_newInstances, repeat)
			newInstancesSlice = append(newInstancesSlice, repeat.GongMarshallIdentifier(stage))
			if stage.Repeats_referenceOrder == nil {
				stage.Repeats_referenceOrder = make(map[*Repeat]uint)
			}
			stage.Repeats_referenceOrder[repeat] = stage.Repeat_stagedOrder[repeat]
			newInstancesReverseSlice = append(newInstancesReverseSlice, repeat.GongMarshallUnstaging(stage))
			// delete(stage.Repeats_referenceOrder, repeat)
			fieldInitializers, pointersInitializations := repeat.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Repeat_stagedOrder[ref] = stage.Repeat_stagedOrder[repeat]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := repeat.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, repeat)
			// delete(stage.Repeat_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", repeat.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Repeats_reference {
		instance := stage.Repeats_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Repeats[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			repeats_deletedInstances = append(repeats_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(repeats_newInstances)
	lenDeletedInstances += len(repeats_deletedInstances)
	var rests_newInstances []*Rest
	var rests_deletedInstances []*Rest

	// parse all staged instances and check if they have a reference
	for rest := range stage.Rests {
		if ref, ok := stage.Rests_reference[rest]; !ok {
			rests_newInstances = append(rests_newInstances, rest)
			newInstancesSlice = append(newInstancesSlice, rest.GongMarshallIdentifier(stage))
			if stage.Rests_referenceOrder == nil {
				stage.Rests_referenceOrder = make(map[*Rest]uint)
			}
			stage.Rests_referenceOrder[rest] = stage.Rest_stagedOrder[rest]
			newInstancesReverseSlice = append(newInstancesReverseSlice, rest.GongMarshallUnstaging(stage))
			// delete(stage.Rests_referenceOrder, rest)
			fieldInitializers, pointersInitializations := rest.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Rest_stagedOrder[ref] = stage.Rest_stagedOrder[rest]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := rest.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, rest)
			// delete(stage.Rest_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", rest.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Rests_reference {
		instance := stage.Rests_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Rests[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			rests_deletedInstances = append(rests_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(rests_newInstances)
	lenDeletedInstances += len(rests_deletedInstances)
	var roots_newInstances []*Root
	var roots_deletedInstances []*Root

	// parse all staged instances and check if they have a reference
	for root := range stage.Roots {
		if ref, ok := stage.Roots_reference[root]; !ok {
			roots_newInstances = append(roots_newInstances, root)
			newInstancesSlice = append(newInstancesSlice, root.GongMarshallIdentifier(stage))
			if stage.Roots_referenceOrder == nil {
				stage.Roots_referenceOrder = make(map[*Root]uint)
			}
			stage.Roots_referenceOrder[root] = stage.Root_stagedOrder[root]
			newInstancesReverseSlice = append(newInstancesReverseSlice, root.GongMarshallUnstaging(stage))
			// delete(stage.Roots_referenceOrder, root)
			fieldInitializers, pointersInitializations := root.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Root_stagedOrder[ref] = stage.Root_stagedOrder[root]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := root.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, root)
			// delete(stage.Root_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", root.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Roots_reference {
		instance := stage.Roots_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Roots[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			roots_deletedInstances = append(roots_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(roots_newInstances)
	lenDeletedInstances += len(roots_deletedInstances)
	var root_steps_newInstances []*Root_step
	var root_steps_deletedInstances []*Root_step

	// parse all staged instances and check if they have a reference
	for root_step := range stage.Root_steps {
		if ref, ok := stage.Root_steps_reference[root_step]; !ok {
			root_steps_newInstances = append(root_steps_newInstances, root_step)
			newInstancesSlice = append(newInstancesSlice, root_step.GongMarshallIdentifier(stage))
			if stage.Root_steps_referenceOrder == nil {
				stage.Root_steps_referenceOrder = make(map[*Root_step]uint)
			}
			stage.Root_steps_referenceOrder[root_step] = stage.Root_step_stagedOrder[root_step]
			newInstancesReverseSlice = append(newInstancesReverseSlice, root_step.GongMarshallUnstaging(stage))
			// delete(stage.Root_steps_referenceOrder, root_step)
			fieldInitializers, pointersInitializations := root_step.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Root_step_stagedOrder[ref] = stage.Root_step_stagedOrder[root_step]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := root_step.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, root_step)
			// delete(stage.Root_step_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", root_step.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Root_steps_reference {
		instance := stage.Root_steps_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Root_steps[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			root_steps_deletedInstances = append(root_steps_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(root_steps_newInstances)
	lenDeletedInstances += len(root_steps_deletedInstances)
	var scalings_newInstances []*Scaling
	var scalings_deletedInstances []*Scaling

	// parse all staged instances and check if they have a reference
	for scaling := range stage.Scalings {
		if ref, ok := stage.Scalings_reference[scaling]; !ok {
			scalings_newInstances = append(scalings_newInstances, scaling)
			newInstancesSlice = append(newInstancesSlice, scaling.GongMarshallIdentifier(stage))
			if stage.Scalings_referenceOrder == nil {
				stage.Scalings_referenceOrder = make(map[*Scaling]uint)
			}
			stage.Scalings_referenceOrder[scaling] = stage.Scaling_stagedOrder[scaling]
			newInstancesReverseSlice = append(newInstancesReverseSlice, scaling.GongMarshallUnstaging(stage))
			// delete(stage.Scalings_referenceOrder, scaling)
			fieldInitializers, pointersInitializations := scaling.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Scaling_stagedOrder[ref] = stage.Scaling_stagedOrder[scaling]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := scaling.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, scaling)
			// delete(stage.Scaling_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", scaling.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Scalings_reference {
		instance := stage.Scalings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Scalings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			scalings_deletedInstances = append(scalings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(scalings_newInstances)
	lenDeletedInstances += len(scalings_deletedInstances)
	var scordaturas_newInstances []*Scordatura
	var scordaturas_deletedInstances []*Scordatura

	// parse all staged instances and check if they have a reference
	for scordatura := range stage.Scordaturas {
		if ref, ok := stage.Scordaturas_reference[scordatura]; !ok {
			scordaturas_newInstances = append(scordaturas_newInstances, scordatura)
			newInstancesSlice = append(newInstancesSlice, scordatura.GongMarshallIdentifier(stage))
			if stage.Scordaturas_referenceOrder == nil {
				stage.Scordaturas_referenceOrder = make(map[*Scordatura]uint)
			}
			stage.Scordaturas_referenceOrder[scordatura] = stage.Scordatura_stagedOrder[scordatura]
			newInstancesReverseSlice = append(newInstancesReverseSlice, scordatura.GongMarshallUnstaging(stage))
			// delete(stage.Scordaturas_referenceOrder, scordatura)
			fieldInitializers, pointersInitializations := scordatura.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Scordatura_stagedOrder[ref] = stage.Scordatura_stagedOrder[scordatura]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := scordatura.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, scordatura)
			// delete(stage.Scordatura_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", scordatura.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Scordaturas_reference {
		instance := stage.Scordaturas_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Scordaturas[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			scordaturas_deletedInstances = append(scordaturas_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(scordaturas_newInstances)
	lenDeletedInstances += len(scordaturas_deletedInstances)
	var score_instruments_newInstances []*Score_instrument
	var score_instruments_deletedInstances []*Score_instrument

	// parse all staged instances and check if they have a reference
	for score_instrument := range stage.Score_instruments {
		if ref, ok := stage.Score_instruments_reference[score_instrument]; !ok {
			score_instruments_newInstances = append(score_instruments_newInstances, score_instrument)
			newInstancesSlice = append(newInstancesSlice, score_instrument.GongMarshallIdentifier(stage))
			if stage.Score_instruments_referenceOrder == nil {
				stage.Score_instruments_referenceOrder = make(map[*Score_instrument]uint)
			}
			stage.Score_instruments_referenceOrder[score_instrument] = stage.Score_instrument_stagedOrder[score_instrument]
			newInstancesReverseSlice = append(newInstancesReverseSlice, score_instrument.GongMarshallUnstaging(stage))
			// delete(stage.Score_instruments_referenceOrder, score_instrument)
			fieldInitializers, pointersInitializations := score_instrument.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Score_instrument_stagedOrder[ref] = stage.Score_instrument_stagedOrder[score_instrument]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := score_instrument.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, score_instrument)
			// delete(stage.Score_instrument_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", score_instrument.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Score_instruments_reference {
		instance := stage.Score_instruments_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Score_instruments[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			score_instruments_deletedInstances = append(score_instruments_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(score_instruments_newInstances)
	lenDeletedInstances += len(score_instruments_deletedInstances)
	var score_parts_newInstances []*Score_part
	var score_parts_deletedInstances []*Score_part

	// parse all staged instances and check if they have a reference
	for score_part := range stage.Score_parts {
		if ref, ok := stage.Score_parts_reference[score_part]; !ok {
			score_parts_newInstances = append(score_parts_newInstances, score_part)
			newInstancesSlice = append(newInstancesSlice, score_part.GongMarshallIdentifier(stage))
			if stage.Score_parts_referenceOrder == nil {
				stage.Score_parts_referenceOrder = make(map[*Score_part]uint)
			}
			stage.Score_parts_referenceOrder[score_part] = stage.Score_part_stagedOrder[score_part]
			newInstancesReverseSlice = append(newInstancesReverseSlice, score_part.GongMarshallUnstaging(stage))
			// delete(stage.Score_parts_referenceOrder, score_part)
			fieldInitializers, pointersInitializations := score_part.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Score_part_stagedOrder[ref] = stage.Score_part_stagedOrder[score_part]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := score_part.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, score_part)
			// delete(stage.Score_part_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", score_part.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Score_parts_reference {
		instance := stage.Score_parts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Score_parts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			score_parts_deletedInstances = append(score_parts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(score_parts_newInstances)
	lenDeletedInstances += len(score_parts_deletedInstances)
	var score_partwises_newInstances []*Score_partwise
	var score_partwises_deletedInstances []*Score_partwise

	// parse all staged instances and check if they have a reference
	for score_partwise := range stage.Score_partwises {
		if ref, ok := stage.Score_partwises_reference[score_partwise]; !ok {
			score_partwises_newInstances = append(score_partwises_newInstances, score_partwise)
			newInstancesSlice = append(newInstancesSlice, score_partwise.GongMarshallIdentifier(stage))
			if stage.Score_partwises_referenceOrder == nil {
				stage.Score_partwises_referenceOrder = make(map[*Score_partwise]uint)
			}
			stage.Score_partwises_referenceOrder[score_partwise] = stage.Score_partwise_stagedOrder[score_partwise]
			newInstancesReverseSlice = append(newInstancesReverseSlice, score_partwise.GongMarshallUnstaging(stage))
			// delete(stage.Score_partwises_referenceOrder, score_partwise)
			fieldInitializers, pointersInitializations := score_partwise.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Score_partwise_stagedOrder[ref] = stage.Score_partwise_stagedOrder[score_partwise]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := score_partwise.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, score_partwise)
			// delete(stage.Score_partwise_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", score_partwise.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Score_partwises_reference {
		instance := stage.Score_partwises_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Score_partwises[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			score_partwises_deletedInstances = append(score_partwises_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(score_partwises_newInstances)
	lenDeletedInstances += len(score_partwises_deletedInstances)
	var score_timewises_newInstances []*Score_timewise
	var score_timewises_deletedInstances []*Score_timewise

	// parse all staged instances and check if they have a reference
	for score_timewise := range stage.Score_timewises {
		if ref, ok := stage.Score_timewises_reference[score_timewise]; !ok {
			score_timewises_newInstances = append(score_timewises_newInstances, score_timewise)
			newInstancesSlice = append(newInstancesSlice, score_timewise.GongMarshallIdentifier(stage))
			if stage.Score_timewises_referenceOrder == nil {
				stage.Score_timewises_referenceOrder = make(map[*Score_timewise]uint)
			}
			stage.Score_timewises_referenceOrder[score_timewise] = stage.Score_timewise_stagedOrder[score_timewise]
			newInstancesReverseSlice = append(newInstancesReverseSlice, score_timewise.GongMarshallUnstaging(stage))
			// delete(stage.Score_timewises_referenceOrder, score_timewise)
			fieldInitializers, pointersInitializations := score_timewise.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Score_timewise_stagedOrder[ref] = stage.Score_timewise_stagedOrder[score_timewise]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := score_timewise.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, score_timewise)
			// delete(stage.Score_timewise_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", score_timewise.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Score_timewises_reference {
		instance := stage.Score_timewises_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Score_timewises[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			score_timewises_deletedInstances = append(score_timewises_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(score_timewises_newInstances)
	lenDeletedInstances += len(score_timewises_deletedInstances)
	var segnos_newInstances []*Segno
	var segnos_deletedInstances []*Segno

	// parse all staged instances and check if they have a reference
	for segno := range stage.Segnos {
		if ref, ok := stage.Segnos_reference[segno]; !ok {
			segnos_newInstances = append(segnos_newInstances, segno)
			newInstancesSlice = append(newInstancesSlice, segno.GongMarshallIdentifier(stage))
			if stage.Segnos_referenceOrder == nil {
				stage.Segnos_referenceOrder = make(map[*Segno]uint)
			}
			stage.Segnos_referenceOrder[segno] = stage.Segno_stagedOrder[segno]
			newInstancesReverseSlice = append(newInstancesReverseSlice, segno.GongMarshallUnstaging(stage))
			// delete(stage.Segnos_referenceOrder, segno)
			fieldInitializers, pointersInitializations := segno.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Segno_stagedOrder[ref] = stage.Segno_stagedOrder[segno]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := segno.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, segno)
			// delete(stage.Segno_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", segno.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Segnos_reference {
		instance := stage.Segnos_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Segnos[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			segnos_deletedInstances = append(segnos_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(segnos_newInstances)
	lenDeletedInstances += len(segnos_deletedInstances)
	var slashs_newInstances []*Slash
	var slashs_deletedInstances []*Slash

	// parse all staged instances and check if they have a reference
	for slash := range stage.Slashs {
		if ref, ok := stage.Slashs_reference[slash]; !ok {
			slashs_newInstances = append(slashs_newInstances, slash)
			newInstancesSlice = append(newInstancesSlice, slash.GongMarshallIdentifier(stage))
			if stage.Slashs_referenceOrder == nil {
				stage.Slashs_referenceOrder = make(map[*Slash]uint)
			}
			stage.Slashs_referenceOrder[slash] = stage.Slash_stagedOrder[slash]
			newInstancesReverseSlice = append(newInstancesReverseSlice, slash.GongMarshallUnstaging(stage))
			// delete(stage.Slashs_referenceOrder, slash)
			fieldInitializers, pointersInitializations := slash.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Slash_stagedOrder[ref] = stage.Slash_stagedOrder[slash]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := slash.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, slash)
			// delete(stage.Slash_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", slash.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Slashs_reference {
		instance := stage.Slashs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Slashs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			slashs_deletedInstances = append(slashs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(slashs_newInstances)
	lenDeletedInstances += len(slashs_deletedInstances)
	var slides_newInstances []*Slide
	var slides_deletedInstances []*Slide

	// parse all staged instances and check if they have a reference
	for slide := range stage.Slides {
		if ref, ok := stage.Slides_reference[slide]; !ok {
			slides_newInstances = append(slides_newInstances, slide)
			newInstancesSlice = append(newInstancesSlice, slide.GongMarshallIdentifier(stage))
			if stage.Slides_referenceOrder == nil {
				stage.Slides_referenceOrder = make(map[*Slide]uint)
			}
			stage.Slides_referenceOrder[slide] = stage.Slide_stagedOrder[slide]
			newInstancesReverseSlice = append(newInstancesReverseSlice, slide.GongMarshallUnstaging(stage))
			// delete(stage.Slides_referenceOrder, slide)
			fieldInitializers, pointersInitializations := slide.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Slide_stagedOrder[ref] = stage.Slide_stagedOrder[slide]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := slide.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, slide)
			// delete(stage.Slide_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", slide.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Slides_reference {
		instance := stage.Slides_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Slides[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			slides_deletedInstances = append(slides_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(slides_newInstances)
	lenDeletedInstances += len(slides_deletedInstances)
	var slurs_newInstances []*Slur
	var slurs_deletedInstances []*Slur

	// parse all staged instances and check if they have a reference
	for slur := range stage.Slurs {
		if ref, ok := stage.Slurs_reference[slur]; !ok {
			slurs_newInstances = append(slurs_newInstances, slur)
			newInstancesSlice = append(newInstancesSlice, slur.GongMarshallIdentifier(stage))
			if stage.Slurs_referenceOrder == nil {
				stage.Slurs_referenceOrder = make(map[*Slur]uint)
			}
			stage.Slurs_referenceOrder[slur] = stage.Slur_stagedOrder[slur]
			newInstancesReverseSlice = append(newInstancesReverseSlice, slur.GongMarshallUnstaging(stage))
			// delete(stage.Slurs_referenceOrder, slur)
			fieldInitializers, pointersInitializations := slur.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Slur_stagedOrder[ref] = stage.Slur_stagedOrder[slur]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := slur.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, slur)
			// delete(stage.Slur_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", slur.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Slurs_reference {
		instance := stage.Slurs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Slurs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			slurs_deletedInstances = append(slurs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(slurs_newInstances)
	lenDeletedInstances += len(slurs_deletedInstances)
	var sounds_newInstances []*Sound
	var sounds_deletedInstances []*Sound

	// parse all staged instances and check if they have a reference
	for sound := range stage.Sounds {
		if ref, ok := stage.Sounds_reference[sound]; !ok {
			sounds_newInstances = append(sounds_newInstances, sound)
			newInstancesSlice = append(newInstancesSlice, sound.GongMarshallIdentifier(stage))
			if stage.Sounds_referenceOrder == nil {
				stage.Sounds_referenceOrder = make(map[*Sound]uint)
			}
			stage.Sounds_referenceOrder[sound] = stage.Sound_stagedOrder[sound]
			newInstancesReverseSlice = append(newInstancesReverseSlice, sound.GongMarshallUnstaging(stage))
			// delete(stage.Sounds_referenceOrder, sound)
			fieldInitializers, pointersInitializations := sound.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Sound_stagedOrder[ref] = stage.Sound_stagedOrder[sound]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := sound.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, sound)
			// delete(stage.Sound_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", sound.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Sounds_reference {
		instance := stage.Sounds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Sounds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			sounds_deletedInstances = append(sounds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(sounds_newInstances)
	lenDeletedInstances += len(sounds_deletedInstances)
	var staff_detailss_newInstances []*Staff_details
	var staff_detailss_deletedInstances []*Staff_details

	// parse all staged instances and check if they have a reference
	for staff_details := range stage.Staff_detailss {
		if ref, ok := stage.Staff_detailss_reference[staff_details]; !ok {
			staff_detailss_newInstances = append(staff_detailss_newInstances, staff_details)
			newInstancesSlice = append(newInstancesSlice, staff_details.GongMarshallIdentifier(stage))
			if stage.Staff_detailss_referenceOrder == nil {
				stage.Staff_detailss_referenceOrder = make(map[*Staff_details]uint)
			}
			stage.Staff_detailss_referenceOrder[staff_details] = stage.Staff_details_stagedOrder[staff_details]
			newInstancesReverseSlice = append(newInstancesReverseSlice, staff_details.GongMarshallUnstaging(stage))
			// delete(stage.Staff_detailss_referenceOrder, staff_details)
			fieldInitializers, pointersInitializations := staff_details.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Staff_details_stagedOrder[ref] = stage.Staff_details_stagedOrder[staff_details]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := staff_details.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, staff_details)
			// delete(stage.Staff_details_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", staff_details.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Staff_detailss_reference {
		instance := stage.Staff_detailss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Staff_detailss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			staff_detailss_deletedInstances = append(staff_detailss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(staff_detailss_newInstances)
	lenDeletedInstances += len(staff_detailss_deletedInstances)
	var staff_divides_newInstances []*Staff_divide
	var staff_divides_deletedInstances []*Staff_divide

	// parse all staged instances and check if they have a reference
	for staff_divide := range stage.Staff_divides {
		if ref, ok := stage.Staff_divides_reference[staff_divide]; !ok {
			staff_divides_newInstances = append(staff_divides_newInstances, staff_divide)
			newInstancesSlice = append(newInstancesSlice, staff_divide.GongMarshallIdentifier(stage))
			if stage.Staff_divides_referenceOrder == nil {
				stage.Staff_divides_referenceOrder = make(map[*Staff_divide]uint)
			}
			stage.Staff_divides_referenceOrder[staff_divide] = stage.Staff_divide_stagedOrder[staff_divide]
			newInstancesReverseSlice = append(newInstancesReverseSlice, staff_divide.GongMarshallUnstaging(stage))
			// delete(stage.Staff_divides_referenceOrder, staff_divide)
			fieldInitializers, pointersInitializations := staff_divide.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Staff_divide_stagedOrder[ref] = stage.Staff_divide_stagedOrder[staff_divide]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := staff_divide.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, staff_divide)
			// delete(stage.Staff_divide_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", staff_divide.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Staff_divides_reference {
		instance := stage.Staff_divides_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Staff_divides[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			staff_divides_deletedInstances = append(staff_divides_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(staff_divides_newInstances)
	lenDeletedInstances += len(staff_divides_deletedInstances)
	var staff_layouts_newInstances []*Staff_layout
	var staff_layouts_deletedInstances []*Staff_layout

	// parse all staged instances and check if they have a reference
	for staff_layout := range stage.Staff_layouts {
		if ref, ok := stage.Staff_layouts_reference[staff_layout]; !ok {
			staff_layouts_newInstances = append(staff_layouts_newInstances, staff_layout)
			newInstancesSlice = append(newInstancesSlice, staff_layout.GongMarshallIdentifier(stage))
			if stage.Staff_layouts_referenceOrder == nil {
				stage.Staff_layouts_referenceOrder = make(map[*Staff_layout]uint)
			}
			stage.Staff_layouts_referenceOrder[staff_layout] = stage.Staff_layout_stagedOrder[staff_layout]
			newInstancesReverseSlice = append(newInstancesReverseSlice, staff_layout.GongMarshallUnstaging(stage))
			// delete(stage.Staff_layouts_referenceOrder, staff_layout)
			fieldInitializers, pointersInitializations := staff_layout.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Staff_layout_stagedOrder[ref] = stage.Staff_layout_stagedOrder[staff_layout]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := staff_layout.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, staff_layout)
			// delete(stage.Staff_layout_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", staff_layout.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Staff_layouts_reference {
		instance := stage.Staff_layouts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Staff_layouts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			staff_layouts_deletedInstances = append(staff_layouts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(staff_layouts_newInstances)
	lenDeletedInstances += len(staff_layouts_deletedInstances)
	var staff_sizes_newInstances []*Staff_size
	var staff_sizes_deletedInstances []*Staff_size

	// parse all staged instances and check if they have a reference
	for staff_size := range stage.Staff_sizes {
		if ref, ok := stage.Staff_sizes_reference[staff_size]; !ok {
			staff_sizes_newInstances = append(staff_sizes_newInstances, staff_size)
			newInstancesSlice = append(newInstancesSlice, staff_size.GongMarshallIdentifier(stage))
			if stage.Staff_sizes_referenceOrder == nil {
				stage.Staff_sizes_referenceOrder = make(map[*Staff_size]uint)
			}
			stage.Staff_sizes_referenceOrder[staff_size] = stage.Staff_size_stagedOrder[staff_size]
			newInstancesReverseSlice = append(newInstancesReverseSlice, staff_size.GongMarshallUnstaging(stage))
			// delete(stage.Staff_sizes_referenceOrder, staff_size)
			fieldInitializers, pointersInitializations := staff_size.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Staff_size_stagedOrder[ref] = stage.Staff_size_stagedOrder[staff_size]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := staff_size.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, staff_size)
			// delete(stage.Staff_size_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", staff_size.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Staff_sizes_reference {
		instance := stage.Staff_sizes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Staff_sizes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			staff_sizes_deletedInstances = append(staff_sizes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(staff_sizes_newInstances)
	lenDeletedInstances += len(staff_sizes_deletedInstances)
	var staff_tunings_newInstances []*Staff_tuning
	var staff_tunings_deletedInstances []*Staff_tuning

	// parse all staged instances and check if they have a reference
	for staff_tuning := range stage.Staff_tunings {
		if ref, ok := stage.Staff_tunings_reference[staff_tuning]; !ok {
			staff_tunings_newInstances = append(staff_tunings_newInstances, staff_tuning)
			newInstancesSlice = append(newInstancesSlice, staff_tuning.GongMarshallIdentifier(stage))
			if stage.Staff_tunings_referenceOrder == nil {
				stage.Staff_tunings_referenceOrder = make(map[*Staff_tuning]uint)
			}
			stage.Staff_tunings_referenceOrder[staff_tuning] = stage.Staff_tuning_stagedOrder[staff_tuning]
			newInstancesReverseSlice = append(newInstancesReverseSlice, staff_tuning.GongMarshallUnstaging(stage))
			// delete(stage.Staff_tunings_referenceOrder, staff_tuning)
			fieldInitializers, pointersInitializations := staff_tuning.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Staff_tuning_stagedOrder[ref] = stage.Staff_tuning_stagedOrder[staff_tuning]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := staff_tuning.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, staff_tuning)
			// delete(stage.Staff_tuning_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", staff_tuning.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Staff_tunings_reference {
		instance := stage.Staff_tunings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Staff_tunings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			staff_tunings_deletedInstances = append(staff_tunings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(staff_tunings_newInstances)
	lenDeletedInstances += len(staff_tunings_deletedInstances)
	var stems_newInstances []*Stem
	var stems_deletedInstances []*Stem

	// parse all staged instances and check if they have a reference
	for stem := range stage.Stems {
		if ref, ok := stage.Stems_reference[stem]; !ok {
			stems_newInstances = append(stems_newInstances, stem)
			newInstancesSlice = append(newInstancesSlice, stem.GongMarshallIdentifier(stage))
			if stage.Stems_referenceOrder == nil {
				stage.Stems_referenceOrder = make(map[*Stem]uint)
			}
			stage.Stems_referenceOrder[stem] = stage.Stem_stagedOrder[stem]
			newInstancesReverseSlice = append(newInstancesReverseSlice, stem.GongMarshallUnstaging(stage))
			// delete(stage.Stems_referenceOrder, stem)
			fieldInitializers, pointersInitializations := stem.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Stem_stagedOrder[ref] = stage.Stem_stagedOrder[stem]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := stem.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, stem)
			// delete(stage.Stem_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", stem.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Stems_reference {
		instance := stage.Stems_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Stems[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			stems_deletedInstances = append(stems_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(stems_newInstances)
	lenDeletedInstances += len(stems_deletedInstances)
	var sticks_newInstances []*Stick
	var sticks_deletedInstances []*Stick

	// parse all staged instances and check if they have a reference
	for stick := range stage.Sticks {
		if ref, ok := stage.Sticks_reference[stick]; !ok {
			sticks_newInstances = append(sticks_newInstances, stick)
			newInstancesSlice = append(newInstancesSlice, stick.GongMarshallIdentifier(stage))
			if stage.Sticks_referenceOrder == nil {
				stage.Sticks_referenceOrder = make(map[*Stick]uint)
			}
			stage.Sticks_referenceOrder[stick] = stage.Stick_stagedOrder[stick]
			newInstancesReverseSlice = append(newInstancesReverseSlice, stick.GongMarshallUnstaging(stage))
			// delete(stage.Sticks_referenceOrder, stick)
			fieldInitializers, pointersInitializations := stick.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Stick_stagedOrder[ref] = stage.Stick_stagedOrder[stick]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := stick.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, stick)
			// delete(stage.Stick_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", stick.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Sticks_reference {
		instance := stage.Sticks_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Sticks[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			sticks_deletedInstances = append(sticks_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(sticks_newInstances)
	lenDeletedInstances += len(sticks_deletedInstances)
	var string_mutes_newInstances []*String_mute
	var string_mutes_deletedInstances []*String_mute

	// parse all staged instances and check if they have a reference
	for string_mute := range stage.String_mutes {
		if ref, ok := stage.String_mutes_reference[string_mute]; !ok {
			string_mutes_newInstances = append(string_mutes_newInstances, string_mute)
			newInstancesSlice = append(newInstancesSlice, string_mute.GongMarshallIdentifier(stage))
			if stage.String_mutes_referenceOrder == nil {
				stage.String_mutes_referenceOrder = make(map[*String_mute]uint)
			}
			stage.String_mutes_referenceOrder[string_mute] = stage.String_mute_stagedOrder[string_mute]
			newInstancesReverseSlice = append(newInstancesReverseSlice, string_mute.GongMarshallUnstaging(stage))
			// delete(stage.String_mutes_referenceOrder, string_mute)
			fieldInitializers, pointersInitializations := string_mute.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.String_mute_stagedOrder[ref] = stage.String_mute_stagedOrder[string_mute]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := string_mute.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, string_mute)
			// delete(stage.String_mute_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", string_mute.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.String_mutes_reference {
		instance := stage.String_mutes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.String_mutes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			string_mutes_deletedInstances = append(string_mutes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(string_mutes_newInstances)
	lenDeletedInstances += len(string_mutes_deletedInstances)
	var string_types_newInstances []*String_type
	var string_types_deletedInstances []*String_type

	// parse all staged instances and check if they have a reference
	for string_type := range stage.String_types {
		if ref, ok := stage.String_types_reference[string_type]; !ok {
			string_types_newInstances = append(string_types_newInstances, string_type)
			newInstancesSlice = append(newInstancesSlice, string_type.GongMarshallIdentifier(stage))
			if stage.String_types_referenceOrder == nil {
				stage.String_types_referenceOrder = make(map[*String_type]uint)
			}
			stage.String_types_referenceOrder[string_type] = stage.String_type_stagedOrder[string_type]
			newInstancesReverseSlice = append(newInstancesReverseSlice, string_type.GongMarshallUnstaging(stage))
			// delete(stage.String_types_referenceOrder, string_type)
			fieldInitializers, pointersInitializations := string_type.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.String_type_stagedOrder[ref] = stage.String_type_stagedOrder[string_type]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := string_type.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, string_type)
			// delete(stage.String_type_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", string_type.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.String_types_reference {
		instance := stage.String_types_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.String_types[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			string_types_deletedInstances = append(string_types_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(string_types_newInstances)
	lenDeletedInstances += len(string_types_deletedInstances)
	var strong_accents_newInstances []*Strong_accent
	var strong_accents_deletedInstances []*Strong_accent

	// parse all staged instances and check if they have a reference
	for strong_accent := range stage.Strong_accents {
		if ref, ok := stage.Strong_accents_reference[strong_accent]; !ok {
			strong_accents_newInstances = append(strong_accents_newInstances, strong_accent)
			newInstancesSlice = append(newInstancesSlice, strong_accent.GongMarshallIdentifier(stage))
			if stage.Strong_accents_referenceOrder == nil {
				stage.Strong_accents_referenceOrder = make(map[*Strong_accent]uint)
			}
			stage.Strong_accents_referenceOrder[strong_accent] = stage.Strong_accent_stagedOrder[strong_accent]
			newInstancesReverseSlice = append(newInstancesReverseSlice, strong_accent.GongMarshallUnstaging(stage))
			// delete(stage.Strong_accents_referenceOrder, strong_accent)
			fieldInitializers, pointersInitializations := strong_accent.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Strong_accent_stagedOrder[ref] = stage.Strong_accent_stagedOrder[strong_accent]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := strong_accent.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, strong_accent)
			// delete(stage.Strong_accent_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", strong_accent.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Strong_accents_reference {
		instance := stage.Strong_accents_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Strong_accents[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			strong_accents_deletedInstances = append(strong_accents_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(strong_accents_newInstances)
	lenDeletedInstances += len(strong_accents_deletedInstances)
	var style_texts_newInstances []*Style_text
	var style_texts_deletedInstances []*Style_text

	// parse all staged instances and check if they have a reference
	for style_text := range stage.Style_texts {
		if ref, ok := stage.Style_texts_reference[style_text]; !ok {
			style_texts_newInstances = append(style_texts_newInstances, style_text)
			newInstancesSlice = append(newInstancesSlice, style_text.GongMarshallIdentifier(stage))
			if stage.Style_texts_referenceOrder == nil {
				stage.Style_texts_referenceOrder = make(map[*Style_text]uint)
			}
			stage.Style_texts_referenceOrder[style_text] = stage.Style_text_stagedOrder[style_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, style_text.GongMarshallUnstaging(stage))
			// delete(stage.Style_texts_referenceOrder, style_text)
			fieldInitializers, pointersInitializations := style_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Style_text_stagedOrder[ref] = stage.Style_text_stagedOrder[style_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := style_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, style_text)
			// delete(stage.Style_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", style_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Style_texts_reference {
		instance := stage.Style_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Style_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			style_texts_deletedInstances = append(style_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(style_texts_newInstances)
	lenDeletedInstances += len(style_texts_deletedInstances)
	var supportss_newInstances []*Supports
	var supportss_deletedInstances []*Supports

	// parse all staged instances and check if they have a reference
	for supports := range stage.Supportss {
		if ref, ok := stage.Supportss_reference[supports]; !ok {
			supportss_newInstances = append(supportss_newInstances, supports)
			newInstancesSlice = append(newInstancesSlice, supports.GongMarshallIdentifier(stage))
			if stage.Supportss_referenceOrder == nil {
				stage.Supportss_referenceOrder = make(map[*Supports]uint)
			}
			stage.Supportss_referenceOrder[supports] = stage.Supports_stagedOrder[supports]
			newInstancesReverseSlice = append(newInstancesReverseSlice, supports.GongMarshallUnstaging(stage))
			// delete(stage.Supportss_referenceOrder, supports)
			fieldInitializers, pointersInitializations := supports.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Supports_stagedOrder[ref] = stage.Supports_stagedOrder[supports]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := supports.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, supports)
			// delete(stage.Supports_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", supports.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Supportss_reference {
		instance := stage.Supportss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Supportss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			supportss_deletedInstances = append(supportss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(supportss_newInstances)
	lenDeletedInstances += len(supportss_deletedInstances)
	var swings_newInstances []*Swing
	var swings_deletedInstances []*Swing

	// parse all staged instances and check if they have a reference
	for swing := range stage.Swings {
		if ref, ok := stage.Swings_reference[swing]; !ok {
			swings_newInstances = append(swings_newInstances, swing)
			newInstancesSlice = append(newInstancesSlice, swing.GongMarshallIdentifier(stage))
			if stage.Swings_referenceOrder == nil {
				stage.Swings_referenceOrder = make(map[*Swing]uint)
			}
			stage.Swings_referenceOrder[swing] = stage.Swing_stagedOrder[swing]
			newInstancesReverseSlice = append(newInstancesReverseSlice, swing.GongMarshallUnstaging(stage))
			// delete(stage.Swings_referenceOrder, swing)
			fieldInitializers, pointersInitializations := swing.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Swing_stagedOrder[ref] = stage.Swing_stagedOrder[swing]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := swing.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, swing)
			// delete(stage.Swing_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", swing.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Swings_reference {
		instance := stage.Swings_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Swings[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			swings_deletedInstances = append(swings_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(swings_newInstances)
	lenDeletedInstances += len(swings_deletedInstances)
	var syncs_newInstances []*Sync
	var syncs_deletedInstances []*Sync

	// parse all staged instances and check if they have a reference
	for sync := range stage.Syncs {
		if ref, ok := stage.Syncs_reference[sync]; !ok {
			syncs_newInstances = append(syncs_newInstances, sync)
			newInstancesSlice = append(newInstancesSlice, sync.GongMarshallIdentifier(stage))
			if stage.Syncs_referenceOrder == nil {
				stage.Syncs_referenceOrder = make(map[*Sync]uint)
			}
			stage.Syncs_referenceOrder[sync] = stage.Sync_stagedOrder[sync]
			newInstancesReverseSlice = append(newInstancesReverseSlice, sync.GongMarshallUnstaging(stage))
			// delete(stage.Syncs_referenceOrder, sync)
			fieldInitializers, pointersInitializations := sync.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Sync_stagedOrder[ref] = stage.Sync_stagedOrder[sync]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := sync.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, sync)
			// delete(stage.Sync_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", sync.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Syncs_reference {
		instance := stage.Syncs_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Syncs[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			syncs_deletedInstances = append(syncs_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(syncs_newInstances)
	lenDeletedInstances += len(syncs_deletedInstances)
	var system_dividerss_newInstances []*System_dividers
	var system_dividerss_deletedInstances []*System_dividers

	// parse all staged instances and check if they have a reference
	for system_dividers := range stage.System_dividerss {
		if ref, ok := stage.System_dividerss_reference[system_dividers]; !ok {
			system_dividerss_newInstances = append(system_dividerss_newInstances, system_dividers)
			newInstancesSlice = append(newInstancesSlice, system_dividers.GongMarshallIdentifier(stage))
			if stage.System_dividerss_referenceOrder == nil {
				stage.System_dividerss_referenceOrder = make(map[*System_dividers]uint)
			}
			stage.System_dividerss_referenceOrder[system_dividers] = stage.System_dividers_stagedOrder[system_dividers]
			newInstancesReverseSlice = append(newInstancesReverseSlice, system_dividers.GongMarshallUnstaging(stage))
			// delete(stage.System_dividerss_referenceOrder, system_dividers)
			fieldInitializers, pointersInitializations := system_dividers.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.System_dividers_stagedOrder[ref] = stage.System_dividers_stagedOrder[system_dividers]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := system_dividers.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, system_dividers)
			// delete(stage.System_dividers_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", system_dividers.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.System_dividerss_reference {
		instance := stage.System_dividerss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.System_dividerss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			system_dividerss_deletedInstances = append(system_dividerss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(system_dividerss_newInstances)
	lenDeletedInstances += len(system_dividerss_deletedInstances)
	var system_layouts_newInstances []*System_layout
	var system_layouts_deletedInstances []*System_layout

	// parse all staged instances and check if they have a reference
	for system_layout := range stage.System_layouts {
		if ref, ok := stage.System_layouts_reference[system_layout]; !ok {
			system_layouts_newInstances = append(system_layouts_newInstances, system_layout)
			newInstancesSlice = append(newInstancesSlice, system_layout.GongMarshallIdentifier(stage))
			if stage.System_layouts_referenceOrder == nil {
				stage.System_layouts_referenceOrder = make(map[*System_layout]uint)
			}
			stage.System_layouts_referenceOrder[system_layout] = stage.System_layout_stagedOrder[system_layout]
			newInstancesReverseSlice = append(newInstancesReverseSlice, system_layout.GongMarshallUnstaging(stage))
			// delete(stage.System_layouts_referenceOrder, system_layout)
			fieldInitializers, pointersInitializations := system_layout.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.System_layout_stagedOrder[ref] = stage.System_layout_stagedOrder[system_layout]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := system_layout.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, system_layout)
			// delete(stage.System_layout_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", system_layout.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.System_layouts_reference {
		instance := stage.System_layouts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.System_layouts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			system_layouts_deletedInstances = append(system_layouts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(system_layouts_newInstances)
	lenDeletedInstances += len(system_layouts_deletedInstances)
	var system_marginss_newInstances []*System_margins
	var system_marginss_deletedInstances []*System_margins

	// parse all staged instances and check if they have a reference
	for system_margins := range stage.System_marginss {
		if ref, ok := stage.System_marginss_reference[system_margins]; !ok {
			system_marginss_newInstances = append(system_marginss_newInstances, system_margins)
			newInstancesSlice = append(newInstancesSlice, system_margins.GongMarshallIdentifier(stage))
			if stage.System_marginss_referenceOrder == nil {
				stage.System_marginss_referenceOrder = make(map[*System_margins]uint)
			}
			stage.System_marginss_referenceOrder[system_margins] = stage.System_margins_stagedOrder[system_margins]
			newInstancesReverseSlice = append(newInstancesReverseSlice, system_margins.GongMarshallUnstaging(stage))
			// delete(stage.System_marginss_referenceOrder, system_margins)
			fieldInitializers, pointersInitializations := system_margins.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.System_margins_stagedOrder[ref] = stage.System_margins_stagedOrder[system_margins]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := system_margins.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, system_margins)
			// delete(stage.System_margins_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", system_margins.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.System_marginss_reference {
		instance := stage.System_marginss_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.System_marginss[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			system_marginss_deletedInstances = append(system_marginss_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(system_marginss_newInstances)
	lenDeletedInstances += len(system_marginss_deletedInstances)
	var taps_newInstances []*Tap
	var taps_deletedInstances []*Tap

	// parse all staged instances and check if they have a reference
	for tap := range stage.Taps {
		if ref, ok := stage.Taps_reference[tap]; !ok {
			taps_newInstances = append(taps_newInstances, tap)
			newInstancesSlice = append(newInstancesSlice, tap.GongMarshallIdentifier(stage))
			if stage.Taps_referenceOrder == nil {
				stage.Taps_referenceOrder = make(map[*Tap]uint)
			}
			stage.Taps_referenceOrder[tap] = stage.Tap_stagedOrder[tap]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tap.GongMarshallUnstaging(stage))
			// delete(stage.Taps_referenceOrder, tap)
			fieldInitializers, pointersInitializations := tap.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tap_stagedOrder[ref] = stage.Tap_stagedOrder[tap]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tap.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tap)
			// delete(stage.Tap_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tap.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Taps_reference {
		instance := stage.Taps_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Taps[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			taps_deletedInstances = append(taps_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(taps_newInstances)
	lenDeletedInstances += len(taps_deletedInstances)
	var technicals_newInstances []*Technical
	var technicals_deletedInstances []*Technical

	// parse all staged instances and check if they have a reference
	for technical := range stage.Technicals {
		if ref, ok := stage.Technicals_reference[technical]; !ok {
			technicals_newInstances = append(technicals_newInstances, technical)
			newInstancesSlice = append(newInstancesSlice, technical.GongMarshallIdentifier(stage))
			if stage.Technicals_referenceOrder == nil {
				stage.Technicals_referenceOrder = make(map[*Technical]uint)
			}
			stage.Technicals_referenceOrder[technical] = stage.Technical_stagedOrder[technical]
			newInstancesReverseSlice = append(newInstancesReverseSlice, technical.GongMarshallUnstaging(stage))
			// delete(stage.Technicals_referenceOrder, technical)
			fieldInitializers, pointersInitializations := technical.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Technical_stagedOrder[ref] = stage.Technical_stagedOrder[technical]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := technical.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, technical)
			// delete(stage.Technical_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", technical.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Technicals_reference {
		instance := stage.Technicals_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Technicals[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			technicals_deletedInstances = append(technicals_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(technicals_newInstances)
	lenDeletedInstances += len(technicals_deletedInstances)
	var text_element_datas_newInstances []*Text_element_data
	var text_element_datas_deletedInstances []*Text_element_data

	// parse all staged instances and check if they have a reference
	for text_element_data := range stage.Text_element_datas {
		if ref, ok := stage.Text_element_datas_reference[text_element_data]; !ok {
			text_element_datas_newInstances = append(text_element_datas_newInstances, text_element_data)
			newInstancesSlice = append(newInstancesSlice, text_element_data.GongMarshallIdentifier(stage))
			if stage.Text_element_datas_referenceOrder == nil {
				stage.Text_element_datas_referenceOrder = make(map[*Text_element_data]uint)
			}
			stage.Text_element_datas_referenceOrder[text_element_data] = stage.Text_element_data_stagedOrder[text_element_data]
			newInstancesReverseSlice = append(newInstancesReverseSlice, text_element_data.GongMarshallUnstaging(stage))
			// delete(stage.Text_element_datas_referenceOrder, text_element_data)
			fieldInitializers, pointersInitializations := text_element_data.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Text_element_data_stagedOrder[ref] = stage.Text_element_data_stagedOrder[text_element_data]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := text_element_data.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, text_element_data)
			// delete(stage.Text_element_data_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", text_element_data.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Text_element_datas_reference {
		instance := stage.Text_element_datas_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Text_element_datas[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			text_element_datas_deletedInstances = append(text_element_datas_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(text_element_datas_newInstances)
	lenDeletedInstances += len(text_element_datas_deletedInstances)
	var ties_newInstances []*Tie
	var ties_deletedInstances []*Tie

	// parse all staged instances and check if they have a reference
	for tie := range stage.Ties {
		if ref, ok := stage.Ties_reference[tie]; !ok {
			ties_newInstances = append(ties_newInstances, tie)
			newInstancesSlice = append(newInstancesSlice, tie.GongMarshallIdentifier(stage))
			if stage.Ties_referenceOrder == nil {
				stage.Ties_referenceOrder = make(map[*Tie]uint)
			}
			stage.Ties_referenceOrder[tie] = stage.Tie_stagedOrder[tie]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tie.GongMarshallUnstaging(stage))
			// delete(stage.Ties_referenceOrder, tie)
			fieldInitializers, pointersInitializations := tie.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tie_stagedOrder[ref] = stage.Tie_stagedOrder[tie]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tie.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tie)
			// delete(stage.Tie_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tie.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Ties_reference {
		instance := stage.Ties_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Ties[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			ties_deletedInstances = append(ties_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(ties_newInstances)
	lenDeletedInstances += len(ties_deletedInstances)
	var tieds_newInstances []*Tied
	var tieds_deletedInstances []*Tied

	// parse all staged instances and check if they have a reference
	for tied := range stage.Tieds {
		if ref, ok := stage.Tieds_reference[tied]; !ok {
			tieds_newInstances = append(tieds_newInstances, tied)
			newInstancesSlice = append(newInstancesSlice, tied.GongMarshallIdentifier(stage))
			if stage.Tieds_referenceOrder == nil {
				stage.Tieds_referenceOrder = make(map[*Tied]uint)
			}
			stage.Tieds_referenceOrder[tied] = stage.Tied_stagedOrder[tied]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tied.GongMarshallUnstaging(stage))
			// delete(stage.Tieds_referenceOrder, tied)
			fieldInitializers, pointersInitializations := tied.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tied_stagedOrder[ref] = stage.Tied_stagedOrder[tied]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tied.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tied)
			// delete(stage.Tied_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tied.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tieds_reference {
		instance := stage.Tieds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tieds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tieds_deletedInstances = append(tieds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tieds_newInstances)
	lenDeletedInstances += len(tieds_deletedInstances)
	var times_newInstances []*Time
	var times_deletedInstances []*Time

	// parse all staged instances and check if they have a reference
	for time := range stage.Times {
		if ref, ok := stage.Times_reference[time]; !ok {
			times_newInstances = append(times_newInstances, time)
			newInstancesSlice = append(newInstancesSlice, time.GongMarshallIdentifier(stage))
			if stage.Times_referenceOrder == nil {
				stage.Times_referenceOrder = make(map[*Time]uint)
			}
			stage.Times_referenceOrder[time] = stage.Time_stagedOrder[time]
			newInstancesReverseSlice = append(newInstancesReverseSlice, time.GongMarshallUnstaging(stage))
			// delete(stage.Times_referenceOrder, time)
			fieldInitializers, pointersInitializations := time.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Time_stagedOrder[ref] = stage.Time_stagedOrder[time]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := time.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, time)
			// delete(stage.Time_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", time.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Times_reference {
		instance := stage.Times_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Times[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			times_deletedInstances = append(times_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(times_newInstances)
	lenDeletedInstances += len(times_deletedInstances)
	var time_modifications_newInstances []*Time_modification
	var time_modifications_deletedInstances []*Time_modification

	// parse all staged instances and check if they have a reference
	for time_modification := range stage.Time_modifications {
		if ref, ok := stage.Time_modifications_reference[time_modification]; !ok {
			time_modifications_newInstances = append(time_modifications_newInstances, time_modification)
			newInstancesSlice = append(newInstancesSlice, time_modification.GongMarshallIdentifier(stage))
			if stage.Time_modifications_referenceOrder == nil {
				stage.Time_modifications_referenceOrder = make(map[*Time_modification]uint)
			}
			stage.Time_modifications_referenceOrder[time_modification] = stage.Time_modification_stagedOrder[time_modification]
			newInstancesReverseSlice = append(newInstancesReverseSlice, time_modification.GongMarshallUnstaging(stage))
			// delete(stage.Time_modifications_referenceOrder, time_modification)
			fieldInitializers, pointersInitializations := time_modification.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Time_modification_stagedOrder[ref] = stage.Time_modification_stagedOrder[time_modification]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := time_modification.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, time_modification)
			// delete(stage.Time_modification_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", time_modification.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Time_modifications_reference {
		instance := stage.Time_modifications_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Time_modifications[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			time_modifications_deletedInstances = append(time_modifications_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(time_modifications_newInstances)
	lenDeletedInstances += len(time_modifications_deletedInstances)
	var timpanis_newInstances []*Timpani
	var timpanis_deletedInstances []*Timpani

	// parse all staged instances and check if they have a reference
	for timpani := range stage.Timpanis {
		if ref, ok := stage.Timpanis_reference[timpani]; !ok {
			timpanis_newInstances = append(timpanis_newInstances, timpani)
			newInstancesSlice = append(newInstancesSlice, timpani.GongMarshallIdentifier(stage))
			if stage.Timpanis_referenceOrder == nil {
				stage.Timpanis_referenceOrder = make(map[*Timpani]uint)
			}
			stage.Timpanis_referenceOrder[timpani] = stage.Timpani_stagedOrder[timpani]
			newInstancesReverseSlice = append(newInstancesReverseSlice, timpani.GongMarshallUnstaging(stage))
			// delete(stage.Timpanis_referenceOrder, timpani)
			fieldInitializers, pointersInitializations := timpani.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Timpani_stagedOrder[ref] = stage.Timpani_stagedOrder[timpani]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := timpani.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, timpani)
			// delete(stage.Timpani_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", timpani.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Timpanis_reference {
		instance := stage.Timpanis_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Timpanis[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			timpanis_deletedInstances = append(timpanis_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(timpanis_newInstances)
	lenDeletedInstances += len(timpanis_deletedInstances)
	var transposes_newInstances []*Transpose
	var transposes_deletedInstances []*Transpose

	// parse all staged instances and check if they have a reference
	for transpose := range stage.Transposes {
		if ref, ok := stage.Transposes_reference[transpose]; !ok {
			transposes_newInstances = append(transposes_newInstances, transpose)
			newInstancesSlice = append(newInstancesSlice, transpose.GongMarshallIdentifier(stage))
			if stage.Transposes_referenceOrder == nil {
				stage.Transposes_referenceOrder = make(map[*Transpose]uint)
			}
			stage.Transposes_referenceOrder[transpose] = stage.Transpose_stagedOrder[transpose]
			newInstancesReverseSlice = append(newInstancesReverseSlice, transpose.GongMarshallUnstaging(stage))
			// delete(stage.Transposes_referenceOrder, transpose)
			fieldInitializers, pointersInitializations := transpose.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Transpose_stagedOrder[ref] = stage.Transpose_stagedOrder[transpose]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := transpose.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, transpose)
			// delete(stage.Transpose_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", transpose.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Transposes_reference {
		instance := stage.Transposes_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Transposes[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			transposes_deletedInstances = append(transposes_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(transposes_newInstances)
	lenDeletedInstances += len(transposes_deletedInstances)
	var tremolos_newInstances []*Tremolo
	var tremolos_deletedInstances []*Tremolo

	// parse all staged instances and check if they have a reference
	for tremolo := range stage.Tremolos {
		if ref, ok := stage.Tremolos_reference[tremolo]; !ok {
			tremolos_newInstances = append(tremolos_newInstances, tremolo)
			newInstancesSlice = append(newInstancesSlice, tremolo.GongMarshallIdentifier(stage))
			if stage.Tremolos_referenceOrder == nil {
				stage.Tremolos_referenceOrder = make(map[*Tremolo]uint)
			}
			stage.Tremolos_referenceOrder[tremolo] = stage.Tremolo_stagedOrder[tremolo]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tremolo.GongMarshallUnstaging(stage))
			// delete(stage.Tremolos_referenceOrder, tremolo)
			fieldInitializers, pointersInitializations := tremolo.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tremolo_stagedOrder[ref] = stage.Tremolo_stagedOrder[tremolo]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tremolo.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tremolo)
			// delete(stage.Tremolo_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tremolo.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tremolos_reference {
		instance := stage.Tremolos_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tremolos[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tremolos_deletedInstances = append(tremolos_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tremolos_newInstances)
	lenDeletedInstances += len(tremolos_deletedInstances)
	var tuplets_newInstances []*Tuplet
	var tuplets_deletedInstances []*Tuplet

	// parse all staged instances and check if they have a reference
	for tuplet := range stage.Tuplets {
		if ref, ok := stage.Tuplets_reference[tuplet]; !ok {
			tuplets_newInstances = append(tuplets_newInstances, tuplet)
			newInstancesSlice = append(newInstancesSlice, tuplet.GongMarshallIdentifier(stage))
			if stage.Tuplets_referenceOrder == nil {
				stage.Tuplets_referenceOrder = make(map[*Tuplet]uint)
			}
			stage.Tuplets_referenceOrder[tuplet] = stage.Tuplet_stagedOrder[tuplet]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tuplet.GongMarshallUnstaging(stage))
			// delete(stage.Tuplets_referenceOrder, tuplet)
			fieldInitializers, pointersInitializations := tuplet.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tuplet_stagedOrder[ref] = stage.Tuplet_stagedOrder[tuplet]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tuplet.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tuplet)
			// delete(stage.Tuplet_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tuplet.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tuplets_reference {
		instance := stage.Tuplets_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tuplets[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tuplets_deletedInstances = append(tuplets_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tuplets_newInstances)
	lenDeletedInstances += len(tuplets_deletedInstances)
	var tuplet_dots_newInstances []*Tuplet_dot
	var tuplet_dots_deletedInstances []*Tuplet_dot

	// parse all staged instances and check if they have a reference
	for tuplet_dot := range stage.Tuplet_dots {
		if ref, ok := stage.Tuplet_dots_reference[tuplet_dot]; !ok {
			tuplet_dots_newInstances = append(tuplet_dots_newInstances, tuplet_dot)
			newInstancesSlice = append(newInstancesSlice, tuplet_dot.GongMarshallIdentifier(stage))
			if stage.Tuplet_dots_referenceOrder == nil {
				stage.Tuplet_dots_referenceOrder = make(map[*Tuplet_dot]uint)
			}
			stage.Tuplet_dots_referenceOrder[tuplet_dot] = stage.Tuplet_dot_stagedOrder[tuplet_dot]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tuplet_dot.GongMarshallUnstaging(stage))
			// delete(stage.Tuplet_dots_referenceOrder, tuplet_dot)
			fieldInitializers, pointersInitializations := tuplet_dot.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tuplet_dot_stagedOrder[ref] = stage.Tuplet_dot_stagedOrder[tuplet_dot]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tuplet_dot.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tuplet_dot)
			// delete(stage.Tuplet_dot_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tuplet_dot.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tuplet_dots_reference {
		instance := stage.Tuplet_dots_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tuplet_dots[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tuplet_dots_deletedInstances = append(tuplet_dots_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tuplet_dots_newInstances)
	lenDeletedInstances += len(tuplet_dots_deletedInstances)
	var tuplet_numbers_newInstances []*Tuplet_number
	var tuplet_numbers_deletedInstances []*Tuplet_number

	// parse all staged instances and check if they have a reference
	for tuplet_number := range stage.Tuplet_numbers {
		if ref, ok := stage.Tuplet_numbers_reference[tuplet_number]; !ok {
			tuplet_numbers_newInstances = append(tuplet_numbers_newInstances, tuplet_number)
			newInstancesSlice = append(newInstancesSlice, tuplet_number.GongMarshallIdentifier(stage))
			if stage.Tuplet_numbers_referenceOrder == nil {
				stage.Tuplet_numbers_referenceOrder = make(map[*Tuplet_number]uint)
			}
			stage.Tuplet_numbers_referenceOrder[tuplet_number] = stage.Tuplet_number_stagedOrder[tuplet_number]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tuplet_number.GongMarshallUnstaging(stage))
			// delete(stage.Tuplet_numbers_referenceOrder, tuplet_number)
			fieldInitializers, pointersInitializations := tuplet_number.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tuplet_number_stagedOrder[ref] = stage.Tuplet_number_stagedOrder[tuplet_number]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tuplet_number.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tuplet_number)
			// delete(stage.Tuplet_number_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tuplet_number.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tuplet_numbers_reference {
		instance := stage.Tuplet_numbers_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tuplet_numbers[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tuplet_numbers_deletedInstances = append(tuplet_numbers_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tuplet_numbers_newInstances)
	lenDeletedInstances += len(tuplet_numbers_deletedInstances)
	var tuplet_portions_newInstances []*Tuplet_portion
	var tuplet_portions_deletedInstances []*Tuplet_portion

	// parse all staged instances and check if they have a reference
	for tuplet_portion := range stage.Tuplet_portions {
		if ref, ok := stage.Tuplet_portions_reference[tuplet_portion]; !ok {
			tuplet_portions_newInstances = append(tuplet_portions_newInstances, tuplet_portion)
			newInstancesSlice = append(newInstancesSlice, tuplet_portion.GongMarshallIdentifier(stage))
			if stage.Tuplet_portions_referenceOrder == nil {
				stage.Tuplet_portions_referenceOrder = make(map[*Tuplet_portion]uint)
			}
			stage.Tuplet_portions_referenceOrder[tuplet_portion] = stage.Tuplet_portion_stagedOrder[tuplet_portion]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tuplet_portion.GongMarshallUnstaging(stage))
			// delete(stage.Tuplet_portions_referenceOrder, tuplet_portion)
			fieldInitializers, pointersInitializations := tuplet_portion.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tuplet_portion_stagedOrder[ref] = stage.Tuplet_portion_stagedOrder[tuplet_portion]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tuplet_portion.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tuplet_portion)
			// delete(stage.Tuplet_portion_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tuplet_portion.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tuplet_portions_reference {
		instance := stage.Tuplet_portions_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tuplet_portions[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tuplet_portions_deletedInstances = append(tuplet_portions_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tuplet_portions_newInstances)
	lenDeletedInstances += len(tuplet_portions_deletedInstances)
	var tuplet_types_newInstances []*Tuplet_type
	var tuplet_types_deletedInstances []*Tuplet_type

	// parse all staged instances and check if they have a reference
	for tuplet_type := range stage.Tuplet_types {
		if ref, ok := stage.Tuplet_types_reference[tuplet_type]; !ok {
			tuplet_types_newInstances = append(tuplet_types_newInstances, tuplet_type)
			newInstancesSlice = append(newInstancesSlice, tuplet_type.GongMarshallIdentifier(stage))
			if stage.Tuplet_types_referenceOrder == nil {
				stage.Tuplet_types_referenceOrder = make(map[*Tuplet_type]uint)
			}
			stage.Tuplet_types_referenceOrder[tuplet_type] = stage.Tuplet_type_stagedOrder[tuplet_type]
			newInstancesReverseSlice = append(newInstancesReverseSlice, tuplet_type.GongMarshallUnstaging(stage))
			// delete(stage.Tuplet_types_referenceOrder, tuplet_type)
			fieldInitializers, pointersInitializations := tuplet_type.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Tuplet_type_stagedOrder[ref] = stage.Tuplet_type_stagedOrder[tuplet_type]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := tuplet_type.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, tuplet_type)
			// delete(stage.Tuplet_type_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", tuplet_type.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Tuplet_types_reference {
		instance := stage.Tuplet_types_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Tuplet_types[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			tuplet_types_deletedInstances = append(tuplet_types_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(tuplet_types_newInstances)
	lenDeletedInstances += len(tuplet_types_deletedInstances)
	var typed_texts_newInstances []*Typed_text
	var typed_texts_deletedInstances []*Typed_text

	// parse all staged instances and check if they have a reference
	for typed_text := range stage.Typed_texts {
		if ref, ok := stage.Typed_texts_reference[typed_text]; !ok {
			typed_texts_newInstances = append(typed_texts_newInstances, typed_text)
			newInstancesSlice = append(newInstancesSlice, typed_text.GongMarshallIdentifier(stage))
			if stage.Typed_texts_referenceOrder == nil {
				stage.Typed_texts_referenceOrder = make(map[*Typed_text]uint)
			}
			stage.Typed_texts_referenceOrder[typed_text] = stage.Typed_text_stagedOrder[typed_text]
			newInstancesReverseSlice = append(newInstancesReverseSlice, typed_text.GongMarshallUnstaging(stage))
			// delete(stage.Typed_texts_referenceOrder, typed_text)
			fieldInitializers, pointersInitializations := typed_text.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Typed_text_stagedOrder[ref] = stage.Typed_text_stagedOrder[typed_text]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := typed_text.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, typed_text)
			// delete(stage.Typed_text_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", typed_text.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Typed_texts_reference {
		instance := stage.Typed_texts_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Typed_texts[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			typed_texts_deletedInstances = append(typed_texts_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(typed_texts_newInstances)
	lenDeletedInstances += len(typed_texts_deletedInstances)
	var unpitcheds_newInstances []*Unpitched
	var unpitcheds_deletedInstances []*Unpitched

	// parse all staged instances and check if they have a reference
	for unpitched := range stage.Unpitcheds {
		if ref, ok := stage.Unpitcheds_reference[unpitched]; !ok {
			unpitcheds_newInstances = append(unpitcheds_newInstances, unpitched)
			newInstancesSlice = append(newInstancesSlice, unpitched.GongMarshallIdentifier(stage))
			if stage.Unpitcheds_referenceOrder == nil {
				stage.Unpitcheds_referenceOrder = make(map[*Unpitched]uint)
			}
			stage.Unpitcheds_referenceOrder[unpitched] = stage.Unpitched_stagedOrder[unpitched]
			newInstancesReverseSlice = append(newInstancesReverseSlice, unpitched.GongMarshallUnstaging(stage))
			// delete(stage.Unpitcheds_referenceOrder, unpitched)
			fieldInitializers, pointersInitializations := unpitched.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Unpitched_stagedOrder[ref] = stage.Unpitched_stagedOrder[unpitched]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := unpitched.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, unpitched)
			// delete(stage.Unpitched_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", unpitched.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Unpitcheds_reference {
		instance := stage.Unpitcheds_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Unpitcheds[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			unpitcheds_deletedInstances = append(unpitcheds_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(unpitcheds_newInstances)
	lenDeletedInstances += len(unpitcheds_deletedInstances)
	var virtual_instruments_newInstances []*Virtual_instrument
	var virtual_instruments_deletedInstances []*Virtual_instrument

	// parse all staged instances and check if they have a reference
	for virtual_instrument := range stage.Virtual_instruments {
		if ref, ok := stage.Virtual_instruments_reference[virtual_instrument]; !ok {
			virtual_instruments_newInstances = append(virtual_instruments_newInstances, virtual_instrument)
			newInstancesSlice = append(newInstancesSlice, virtual_instrument.GongMarshallIdentifier(stage))
			if stage.Virtual_instruments_referenceOrder == nil {
				stage.Virtual_instruments_referenceOrder = make(map[*Virtual_instrument]uint)
			}
			stage.Virtual_instruments_referenceOrder[virtual_instrument] = stage.Virtual_instrument_stagedOrder[virtual_instrument]
			newInstancesReverseSlice = append(newInstancesReverseSlice, virtual_instrument.GongMarshallUnstaging(stage))
			// delete(stage.Virtual_instruments_referenceOrder, virtual_instrument)
			fieldInitializers, pointersInitializations := virtual_instrument.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Virtual_instrument_stagedOrder[ref] = stage.Virtual_instrument_stagedOrder[virtual_instrument]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := virtual_instrument.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, virtual_instrument)
			// delete(stage.Virtual_instrument_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", virtual_instrument.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Virtual_instruments_reference {
		instance := stage.Virtual_instruments_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Virtual_instruments[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			virtual_instruments_deletedInstances = append(virtual_instruments_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(virtual_instruments_newInstances)
	lenDeletedInstances += len(virtual_instruments_deletedInstances)
	var waits_newInstances []*Wait
	var waits_deletedInstances []*Wait

	// parse all staged instances and check if they have a reference
	for wait := range stage.Waits {
		if ref, ok := stage.Waits_reference[wait]; !ok {
			waits_newInstances = append(waits_newInstances, wait)
			newInstancesSlice = append(newInstancesSlice, wait.GongMarshallIdentifier(stage))
			if stage.Waits_referenceOrder == nil {
				stage.Waits_referenceOrder = make(map[*Wait]uint)
			}
			stage.Waits_referenceOrder[wait] = stage.Wait_stagedOrder[wait]
			newInstancesReverseSlice = append(newInstancesReverseSlice, wait.GongMarshallUnstaging(stage))
			// delete(stage.Waits_referenceOrder, wait)
			fieldInitializers, pointersInitializations := wait.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Wait_stagedOrder[ref] = stage.Wait_stagedOrder[wait]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := wait.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, wait)
			// delete(stage.Wait_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", wait.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Waits_reference {
		instance := stage.Waits_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Waits[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			waits_deletedInstances = append(waits_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(waits_newInstances)
	lenDeletedInstances += len(waits_deletedInstances)
	var wavy_lines_newInstances []*Wavy_line
	var wavy_lines_deletedInstances []*Wavy_line

	// parse all staged instances and check if they have a reference
	for wavy_line := range stage.Wavy_lines {
		if ref, ok := stage.Wavy_lines_reference[wavy_line]; !ok {
			wavy_lines_newInstances = append(wavy_lines_newInstances, wavy_line)
			newInstancesSlice = append(newInstancesSlice, wavy_line.GongMarshallIdentifier(stage))
			if stage.Wavy_lines_referenceOrder == nil {
				stage.Wavy_lines_referenceOrder = make(map[*Wavy_line]uint)
			}
			stage.Wavy_lines_referenceOrder[wavy_line] = stage.Wavy_line_stagedOrder[wavy_line]
			newInstancesReverseSlice = append(newInstancesReverseSlice, wavy_line.GongMarshallUnstaging(stage))
			// delete(stage.Wavy_lines_referenceOrder, wavy_line)
			fieldInitializers, pointersInitializations := wavy_line.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Wavy_line_stagedOrder[ref] = stage.Wavy_line_stagedOrder[wavy_line]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := wavy_line.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, wavy_line)
			// delete(stage.Wavy_line_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", wavy_line.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Wavy_lines_reference {
		instance := stage.Wavy_lines_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Wavy_lines[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			wavy_lines_deletedInstances = append(wavy_lines_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(wavy_lines_newInstances)
	lenDeletedInstances += len(wavy_lines_deletedInstances)
	var wedges_newInstances []*Wedge
	var wedges_deletedInstances []*Wedge

	// parse all staged instances and check if they have a reference
	for wedge := range stage.Wedges {
		if ref, ok := stage.Wedges_reference[wedge]; !ok {
			wedges_newInstances = append(wedges_newInstances, wedge)
			newInstancesSlice = append(newInstancesSlice, wedge.GongMarshallIdentifier(stage))
			if stage.Wedges_referenceOrder == nil {
				stage.Wedges_referenceOrder = make(map[*Wedge]uint)
			}
			stage.Wedges_referenceOrder[wedge] = stage.Wedge_stagedOrder[wedge]
			newInstancesReverseSlice = append(newInstancesReverseSlice, wedge.GongMarshallUnstaging(stage))
			// delete(stage.Wedges_referenceOrder, wedge)
			fieldInitializers, pointersInitializations := wedge.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Wedge_stagedOrder[ref] = stage.Wedge_stagedOrder[wedge]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := wedge.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, wedge)
			// delete(stage.Wedge_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", wedge.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Wedges_reference {
		instance := stage.Wedges_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Wedges[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			wedges_deletedInstances = append(wedges_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(wedges_newInstances)
	lenDeletedInstances += len(wedges_deletedInstances)
	var woods_newInstances []*Wood
	var woods_deletedInstances []*Wood

	// parse all staged instances and check if they have a reference
	for wood := range stage.Woods {
		if ref, ok := stage.Woods_reference[wood]; !ok {
			woods_newInstances = append(woods_newInstances, wood)
			newInstancesSlice = append(newInstancesSlice, wood.GongMarshallIdentifier(stage))
			if stage.Woods_referenceOrder == nil {
				stage.Woods_referenceOrder = make(map[*Wood]uint)
			}
			stage.Woods_referenceOrder[wood] = stage.Wood_stagedOrder[wood]
			newInstancesReverseSlice = append(newInstancesReverseSlice, wood.GongMarshallUnstaging(stage))
			// delete(stage.Woods_referenceOrder, wood)
			fieldInitializers, pointersInitializations := wood.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Wood_stagedOrder[ref] = stage.Wood_stagedOrder[wood]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := wood.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, wood)
			// delete(stage.Wood_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", wood.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Woods_reference {
		instance := stage.Woods_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Woods[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			woods_deletedInstances = append(woods_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(woods_newInstances)
	lenDeletedInstances += len(woods_deletedInstances)
	var works_newInstances []*Work
	var works_deletedInstances []*Work

	// parse all staged instances and check if they have a reference
	for work := range stage.Works {
		if ref, ok := stage.Works_reference[work]; !ok {
			works_newInstances = append(works_newInstances, work)
			newInstancesSlice = append(newInstancesSlice, work.GongMarshallIdentifier(stage))
			if stage.Works_referenceOrder == nil {
				stage.Works_referenceOrder = make(map[*Work]uint)
			}
			stage.Works_referenceOrder[work] = stage.Work_stagedOrder[work]
			newInstancesReverseSlice = append(newInstancesReverseSlice, work.GongMarshallUnstaging(stage))
			// delete(stage.Works_referenceOrder, work)
			fieldInitializers, pointersInitializations := work.GongMarshallAllFields(stage)
			fieldsEditSlice = append(fieldsEditSlice, fieldInitializers+pointersInitializations)
		} else {
			stage.Work_stagedOrder[ref] = stage.Work_stagedOrder[work]
			ref.GongReconstructPointersFromInstances(stage) // reconstruct ref with pointers from the stage
			diffs := work.GongDiff(stage, ref)
			reverseDiffs := ref.GongDiff(stage, work)
			// delete(stage.Work_stagedOrder, ref)
			if len(diffs) > 0 {
				var fieldsEdit string
				fieldsEdit += fmt.Sprintf("\n\t// %s", work.GetName())
				for _, diff := range diffs {
					fieldsEdit += diff
				}
				fieldsEditSlice = append(fieldsEditSlice, fieldsEdit)
				for _, reverseDiff := range reverseDiffs {
					fieldsEditReverseSlice = append(fieldsEditReverseSlice, reverseDiff)
				}
				lenModifiedInstances++
			}
		}
	}

	// parse all reference instances and check if they are still staged
	for _, ref := range stage.Works_reference {
		instance := stage.Works_instance[ref]    // get the instance corresponding to the reference
		if _, ok := stage.Works[instance]; !ok { // if the instance is not staged anymore,  it means it has been unstaged
			works_deletedInstances = append(works_deletedInstances, ref)
			deletedInstancesSlice = append(deletedInstancesSlice, ref.GongMarshallUnstaging(stage))
			deletedInstancesReverseSlice = append(deletedInstancesReverseSlice, ref.GongMarshallIdentifier(stage))
			fieldInitializers, pointersInitializations := ref.GongMarshallAllFields(stage)
			fieldsEditReverseSlice = append(fieldsEditReverseSlice, fieldInitializers+pointersInitializations)
		}
	}

	lenNewInstances += len(works_newInstances)
	lenDeletedInstances += len(works_deletedInstances)

	if lenNewInstances > 0 || lenDeletedInstances > 0 || lenModifiedInstances > 0 {

		// sort the stmt to have reproductible forward/backward commit
		sort.Strings(newInstancesSlice)
		newInstancesStmt := strings.Join(newInstancesSlice, "")
		sort.Strings(fieldsEditSlice)
		fieldsEditStmt := strings.Join(fieldsEditSlice, "")
		sort.Strings(deletedInstancesSlice)
		deletedInstancesStmt := strings.Join(deletedInstancesSlice, "")

		sort.Strings(newInstancesReverseSlice)
		newInstancesReverseStmt := strings.Join(newInstancesReverseSlice, "")
		sort.Strings(fieldsEditReverseSlice)
		fieldsEditReverseStmt := strings.Join(fieldsEditReverseSlice, "")
		sort.Strings(deletedInstancesReverseSlice)
		deletedInstancesReverseStmt := strings.Join(deletedInstancesReverseSlice, "")

		forwardCommit := newInstancesStmt + fieldsEditStmt + deletedInstancesStmt
		forwardCommit += "\n\tstage.Commit()"
		stage.forwardCommits = append(stage.forwardCommits, forwardCommit)

		backwardCommit := deletedInstancesReverseStmt + fieldsEditReverseStmt + newInstancesReverseStmt
		backwardCommit += "\n\tstage.Commit()"
		// append to the end of the backward commits slice
		stage.backwardCommits = append(stage.backwardCommits, backwardCommit)
		stage.modified = true
	} else {
		stage.modified = false
	}
}

// ComputeReferenceAndOrders will creates a deep copy of each of the staged elements
func (stage *Stage) ComputeReferenceAndOrders() {
	// insertion point per named struct
	stage.A_directives_reference = make(map[*A_directive]*A_directive)
	stage.A_directives_referenceOrder = make(map[*A_directive]uint) // diff Unstage needs the reference order
	stage.A_directives_instance = make(map[*A_directive]*A_directive)
	for instance := range stage.A_directives {
		_copy := instance.GongCopy().(*A_directive)
		stage.A_directives_reference[instance] = _copy
		stage.A_directives_instance[_copy] = instance
		stage.A_directives_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.A_measures_reference = make(map[*A_measure]*A_measure)
	stage.A_measures_referenceOrder = make(map[*A_measure]uint) // diff Unstage needs the reference order
	stage.A_measures_instance = make(map[*A_measure]*A_measure)
	for instance := range stage.A_measures {
		_copy := instance.GongCopy().(*A_measure)
		stage.A_measures_reference[instance] = _copy
		stage.A_measures_instance[_copy] = instance
		stage.A_measures_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.A_measure_1s_reference = make(map[*A_measure_1]*A_measure_1)
	stage.A_measure_1s_referenceOrder = make(map[*A_measure_1]uint) // diff Unstage needs the reference order
	stage.A_measure_1s_instance = make(map[*A_measure_1]*A_measure_1)
	for instance := range stage.A_measure_1s {
		_copy := instance.GongCopy().(*A_measure_1)
		stage.A_measure_1s_reference[instance] = _copy
		stage.A_measure_1s_instance[_copy] = instance
		stage.A_measure_1s_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.A_parts_reference = make(map[*A_part]*A_part)
	stage.A_parts_referenceOrder = make(map[*A_part]uint) // diff Unstage needs the reference order
	stage.A_parts_instance = make(map[*A_part]*A_part)
	for instance := range stage.A_parts {
		_copy := instance.GongCopy().(*A_part)
		stage.A_parts_reference[instance] = _copy
		stage.A_parts_instance[_copy] = instance
		stage.A_parts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.A_part_1s_reference = make(map[*A_part_1]*A_part_1)
	stage.A_part_1s_referenceOrder = make(map[*A_part_1]uint) // diff Unstage needs the reference order
	stage.A_part_1s_instance = make(map[*A_part_1]*A_part_1)
	for instance := range stage.A_part_1s {
		_copy := instance.GongCopy().(*A_part_1)
		stage.A_part_1s_reference[instance] = _copy
		stage.A_part_1s_instance[_copy] = instance
		stage.A_part_1s_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Accidentals_reference = make(map[*Accidental]*Accidental)
	stage.Accidentals_referenceOrder = make(map[*Accidental]uint) // diff Unstage needs the reference order
	stage.Accidentals_instance = make(map[*Accidental]*Accidental)
	for instance := range stage.Accidentals {
		_copy := instance.GongCopy().(*Accidental)
		stage.Accidentals_reference[instance] = _copy
		stage.Accidentals_instance[_copy] = instance
		stage.Accidentals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Accidental_marks_reference = make(map[*Accidental_mark]*Accidental_mark)
	stage.Accidental_marks_referenceOrder = make(map[*Accidental_mark]uint) // diff Unstage needs the reference order
	stage.Accidental_marks_instance = make(map[*Accidental_mark]*Accidental_mark)
	for instance := range stage.Accidental_marks {
		_copy := instance.GongCopy().(*Accidental_mark)
		stage.Accidental_marks_reference[instance] = _copy
		stage.Accidental_marks_instance[_copy] = instance
		stage.Accidental_marks_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Accidental_texts_reference = make(map[*Accidental_text]*Accidental_text)
	stage.Accidental_texts_referenceOrder = make(map[*Accidental_text]uint) // diff Unstage needs the reference order
	stage.Accidental_texts_instance = make(map[*Accidental_text]*Accidental_text)
	for instance := range stage.Accidental_texts {
		_copy := instance.GongCopy().(*Accidental_text)
		stage.Accidental_texts_reference[instance] = _copy
		stage.Accidental_texts_instance[_copy] = instance
		stage.Accidental_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Accords_reference = make(map[*Accord]*Accord)
	stage.Accords_referenceOrder = make(map[*Accord]uint) // diff Unstage needs the reference order
	stage.Accords_instance = make(map[*Accord]*Accord)
	for instance := range stage.Accords {
		_copy := instance.GongCopy().(*Accord)
		stage.Accords_reference[instance] = _copy
		stage.Accords_instance[_copy] = instance
		stage.Accords_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Accordion_registrations_reference = make(map[*Accordion_registration]*Accordion_registration)
	stage.Accordion_registrations_referenceOrder = make(map[*Accordion_registration]uint) // diff Unstage needs the reference order
	stage.Accordion_registrations_instance = make(map[*Accordion_registration]*Accordion_registration)
	for instance := range stage.Accordion_registrations {
		_copy := instance.GongCopy().(*Accordion_registration)
		stage.Accordion_registrations_reference[instance] = _copy
		stage.Accordion_registrations_instance[_copy] = instance
		stage.Accordion_registrations_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Appearances_reference = make(map[*Appearance]*Appearance)
	stage.Appearances_referenceOrder = make(map[*Appearance]uint) // diff Unstage needs the reference order
	stage.Appearances_instance = make(map[*Appearance]*Appearance)
	for instance := range stage.Appearances {
		_copy := instance.GongCopy().(*Appearance)
		stage.Appearances_reference[instance] = _copy
		stage.Appearances_instance[_copy] = instance
		stage.Appearances_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Arpeggiates_reference = make(map[*Arpeggiate]*Arpeggiate)
	stage.Arpeggiates_referenceOrder = make(map[*Arpeggiate]uint) // diff Unstage needs the reference order
	stage.Arpeggiates_instance = make(map[*Arpeggiate]*Arpeggiate)
	for instance := range stage.Arpeggiates {
		_copy := instance.GongCopy().(*Arpeggiate)
		stage.Arpeggiates_reference[instance] = _copy
		stage.Arpeggiates_instance[_copy] = instance
		stage.Arpeggiates_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Arrows_reference = make(map[*Arrow]*Arrow)
	stage.Arrows_referenceOrder = make(map[*Arrow]uint) // diff Unstage needs the reference order
	stage.Arrows_instance = make(map[*Arrow]*Arrow)
	for instance := range stage.Arrows {
		_copy := instance.GongCopy().(*Arrow)
		stage.Arrows_reference[instance] = _copy
		stage.Arrows_instance[_copy] = instance
		stage.Arrows_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Articulationss_reference = make(map[*Articulations]*Articulations)
	stage.Articulationss_referenceOrder = make(map[*Articulations]uint) // diff Unstage needs the reference order
	stage.Articulationss_instance = make(map[*Articulations]*Articulations)
	for instance := range stage.Articulationss {
		_copy := instance.GongCopy().(*Articulations)
		stage.Articulationss_reference[instance] = _copy
		stage.Articulationss_instance[_copy] = instance
		stage.Articulationss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Assesss_reference = make(map[*Assess]*Assess)
	stage.Assesss_referenceOrder = make(map[*Assess]uint) // diff Unstage needs the reference order
	stage.Assesss_instance = make(map[*Assess]*Assess)
	for instance := range stage.Assesss {
		_copy := instance.GongCopy().(*Assess)
		stage.Assesss_reference[instance] = _copy
		stage.Assesss_instance[_copy] = instance
		stage.Assesss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Attributess_reference = make(map[*Attributes]*Attributes)
	stage.Attributess_referenceOrder = make(map[*Attributes]uint) // diff Unstage needs the reference order
	stage.Attributess_instance = make(map[*Attributes]*Attributes)
	for instance := range stage.Attributess {
		_copy := instance.GongCopy().(*Attributes)
		stage.Attributess_reference[instance] = _copy
		stage.Attributess_instance[_copy] = instance
		stage.Attributess_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Backups_reference = make(map[*Backup]*Backup)
	stage.Backups_referenceOrder = make(map[*Backup]uint) // diff Unstage needs the reference order
	stage.Backups_instance = make(map[*Backup]*Backup)
	for instance := range stage.Backups {
		_copy := instance.GongCopy().(*Backup)
		stage.Backups_reference[instance] = _copy
		stage.Backups_instance[_copy] = instance
		stage.Backups_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Bar_style_colors_reference = make(map[*Bar_style_color]*Bar_style_color)
	stage.Bar_style_colors_referenceOrder = make(map[*Bar_style_color]uint) // diff Unstage needs the reference order
	stage.Bar_style_colors_instance = make(map[*Bar_style_color]*Bar_style_color)
	for instance := range stage.Bar_style_colors {
		_copy := instance.GongCopy().(*Bar_style_color)
		stage.Bar_style_colors_reference[instance] = _copy
		stage.Bar_style_colors_instance[_copy] = instance
		stage.Bar_style_colors_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Barlines_reference = make(map[*Barline]*Barline)
	stage.Barlines_referenceOrder = make(map[*Barline]uint) // diff Unstage needs the reference order
	stage.Barlines_instance = make(map[*Barline]*Barline)
	for instance := range stage.Barlines {
		_copy := instance.GongCopy().(*Barline)
		stage.Barlines_reference[instance] = _copy
		stage.Barlines_instance[_copy] = instance
		stage.Barlines_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Barres_reference = make(map[*Barre]*Barre)
	stage.Barres_referenceOrder = make(map[*Barre]uint) // diff Unstage needs the reference order
	stage.Barres_instance = make(map[*Barre]*Barre)
	for instance := range stage.Barres {
		_copy := instance.GongCopy().(*Barre)
		stage.Barres_reference[instance] = _copy
		stage.Barres_instance[_copy] = instance
		stage.Barres_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Basss_reference = make(map[*Bass]*Bass)
	stage.Basss_referenceOrder = make(map[*Bass]uint) // diff Unstage needs the reference order
	stage.Basss_instance = make(map[*Bass]*Bass)
	for instance := range stage.Basss {
		_copy := instance.GongCopy().(*Bass)
		stage.Basss_reference[instance] = _copy
		stage.Basss_instance[_copy] = instance
		stage.Basss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Bass_steps_reference = make(map[*Bass_step]*Bass_step)
	stage.Bass_steps_referenceOrder = make(map[*Bass_step]uint) // diff Unstage needs the reference order
	stage.Bass_steps_instance = make(map[*Bass_step]*Bass_step)
	for instance := range stage.Bass_steps {
		_copy := instance.GongCopy().(*Bass_step)
		stage.Bass_steps_reference[instance] = _copy
		stage.Bass_steps_instance[_copy] = instance
		stage.Bass_steps_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Beams_reference = make(map[*Beam]*Beam)
	stage.Beams_referenceOrder = make(map[*Beam]uint) // diff Unstage needs the reference order
	stage.Beams_instance = make(map[*Beam]*Beam)
	for instance := range stage.Beams {
		_copy := instance.GongCopy().(*Beam)
		stage.Beams_reference[instance] = _copy
		stage.Beams_instance[_copy] = instance
		stage.Beams_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Beat_repeats_reference = make(map[*Beat_repeat]*Beat_repeat)
	stage.Beat_repeats_referenceOrder = make(map[*Beat_repeat]uint) // diff Unstage needs the reference order
	stage.Beat_repeats_instance = make(map[*Beat_repeat]*Beat_repeat)
	for instance := range stage.Beat_repeats {
		_copy := instance.GongCopy().(*Beat_repeat)
		stage.Beat_repeats_reference[instance] = _copy
		stage.Beat_repeats_instance[_copy] = instance
		stage.Beat_repeats_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Beat_unit_tieds_reference = make(map[*Beat_unit_tied]*Beat_unit_tied)
	stage.Beat_unit_tieds_referenceOrder = make(map[*Beat_unit_tied]uint) // diff Unstage needs the reference order
	stage.Beat_unit_tieds_instance = make(map[*Beat_unit_tied]*Beat_unit_tied)
	for instance := range stage.Beat_unit_tieds {
		_copy := instance.GongCopy().(*Beat_unit_tied)
		stage.Beat_unit_tieds_reference[instance] = _copy
		stage.Beat_unit_tieds_instance[_copy] = instance
		stage.Beat_unit_tieds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Beaters_reference = make(map[*Beater]*Beater)
	stage.Beaters_referenceOrder = make(map[*Beater]uint) // diff Unstage needs the reference order
	stage.Beaters_instance = make(map[*Beater]*Beater)
	for instance := range stage.Beaters {
		_copy := instance.GongCopy().(*Beater)
		stage.Beaters_reference[instance] = _copy
		stage.Beaters_instance[_copy] = instance
		stage.Beaters_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Bends_reference = make(map[*Bend]*Bend)
	stage.Bends_referenceOrder = make(map[*Bend]uint) // diff Unstage needs the reference order
	stage.Bends_instance = make(map[*Bend]*Bend)
	for instance := range stage.Bends {
		_copy := instance.GongCopy().(*Bend)
		stage.Bends_reference[instance] = _copy
		stage.Bends_instance[_copy] = instance
		stage.Bends_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Bookmarks_reference = make(map[*Bookmark]*Bookmark)
	stage.Bookmarks_referenceOrder = make(map[*Bookmark]uint) // diff Unstage needs the reference order
	stage.Bookmarks_instance = make(map[*Bookmark]*Bookmark)
	for instance := range stage.Bookmarks {
		_copy := instance.GongCopy().(*Bookmark)
		stage.Bookmarks_reference[instance] = _copy
		stage.Bookmarks_instance[_copy] = instance
		stage.Bookmarks_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Brackets_reference = make(map[*Bracket]*Bracket)
	stage.Brackets_referenceOrder = make(map[*Bracket]uint) // diff Unstage needs the reference order
	stage.Brackets_instance = make(map[*Bracket]*Bracket)
	for instance := range stage.Brackets {
		_copy := instance.GongCopy().(*Bracket)
		stage.Brackets_reference[instance] = _copy
		stage.Brackets_instance[_copy] = instance
		stage.Brackets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Breath_marks_reference = make(map[*Breath_mark]*Breath_mark)
	stage.Breath_marks_referenceOrder = make(map[*Breath_mark]uint) // diff Unstage needs the reference order
	stage.Breath_marks_instance = make(map[*Breath_mark]*Breath_mark)
	for instance := range stage.Breath_marks {
		_copy := instance.GongCopy().(*Breath_mark)
		stage.Breath_marks_reference[instance] = _copy
		stage.Breath_marks_instance[_copy] = instance
		stage.Breath_marks_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Caesuras_reference = make(map[*Caesura]*Caesura)
	stage.Caesuras_referenceOrder = make(map[*Caesura]uint) // diff Unstage needs the reference order
	stage.Caesuras_instance = make(map[*Caesura]*Caesura)
	for instance := range stage.Caesuras {
		_copy := instance.GongCopy().(*Caesura)
		stage.Caesuras_reference[instance] = _copy
		stage.Caesuras_instance[_copy] = instance
		stage.Caesuras_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Cancels_reference = make(map[*Cancel]*Cancel)
	stage.Cancels_referenceOrder = make(map[*Cancel]uint) // diff Unstage needs the reference order
	stage.Cancels_instance = make(map[*Cancel]*Cancel)
	for instance := range stage.Cancels {
		_copy := instance.GongCopy().(*Cancel)
		stage.Cancels_reference[instance] = _copy
		stage.Cancels_instance[_copy] = instance
		stage.Cancels_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Clefs_reference = make(map[*Clef]*Clef)
	stage.Clefs_referenceOrder = make(map[*Clef]uint) // diff Unstage needs the reference order
	stage.Clefs_instance = make(map[*Clef]*Clef)
	for instance := range stage.Clefs {
		_copy := instance.GongCopy().(*Clef)
		stage.Clefs_reference[instance] = _copy
		stage.Clefs_instance[_copy] = instance
		stage.Clefs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Codas_reference = make(map[*Coda]*Coda)
	stage.Codas_referenceOrder = make(map[*Coda]uint) // diff Unstage needs the reference order
	stage.Codas_instance = make(map[*Coda]*Coda)
	for instance := range stage.Codas {
		_copy := instance.GongCopy().(*Coda)
		stage.Codas_reference[instance] = _copy
		stage.Codas_instance[_copy] = instance
		stage.Codas_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Credits_reference = make(map[*Credit]*Credit)
	stage.Credits_referenceOrder = make(map[*Credit]uint) // diff Unstage needs the reference order
	stage.Credits_instance = make(map[*Credit]*Credit)
	for instance := range stage.Credits {
		_copy := instance.GongCopy().(*Credit)
		stage.Credits_reference[instance] = _copy
		stage.Credits_instance[_copy] = instance
		stage.Credits_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Dashess_reference = make(map[*Dashes]*Dashes)
	stage.Dashess_referenceOrder = make(map[*Dashes]uint) // diff Unstage needs the reference order
	stage.Dashess_instance = make(map[*Dashes]*Dashes)
	for instance := range stage.Dashess {
		_copy := instance.GongCopy().(*Dashes)
		stage.Dashess_reference[instance] = _copy
		stage.Dashess_instance[_copy] = instance
		stage.Dashess_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Defaultss_reference = make(map[*Defaults]*Defaults)
	stage.Defaultss_referenceOrder = make(map[*Defaults]uint) // diff Unstage needs the reference order
	stage.Defaultss_instance = make(map[*Defaults]*Defaults)
	for instance := range stage.Defaultss {
		_copy := instance.GongCopy().(*Defaults)
		stage.Defaultss_reference[instance] = _copy
		stage.Defaultss_instance[_copy] = instance
		stage.Defaultss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Degrees_reference = make(map[*Degree]*Degree)
	stage.Degrees_referenceOrder = make(map[*Degree]uint) // diff Unstage needs the reference order
	stage.Degrees_instance = make(map[*Degree]*Degree)
	for instance := range stage.Degrees {
		_copy := instance.GongCopy().(*Degree)
		stage.Degrees_reference[instance] = _copy
		stage.Degrees_instance[_copy] = instance
		stage.Degrees_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Degree_alters_reference = make(map[*Degree_alter]*Degree_alter)
	stage.Degree_alters_referenceOrder = make(map[*Degree_alter]uint) // diff Unstage needs the reference order
	stage.Degree_alters_instance = make(map[*Degree_alter]*Degree_alter)
	for instance := range stage.Degree_alters {
		_copy := instance.GongCopy().(*Degree_alter)
		stage.Degree_alters_reference[instance] = _copy
		stage.Degree_alters_instance[_copy] = instance
		stage.Degree_alters_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Degree_types_reference = make(map[*Degree_type]*Degree_type)
	stage.Degree_types_referenceOrder = make(map[*Degree_type]uint) // diff Unstage needs the reference order
	stage.Degree_types_instance = make(map[*Degree_type]*Degree_type)
	for instance := range stage.Degree_types {
		_copy := instance.GongCopy().(*Degree_type)
		stage.Degree_types_reference[instance] = _copy
		stage.Degree_types_instance[_copy] = instance
		stage.Degree_types_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Degree_values_reference = make(map[*Degree_value]*Degree_value)
	stage.Degree_values_referenceOrder = make(map[*Degree_value]uint) // diff Unstage needs the reference order
	stage.Degree_values_instance = make(map[*Degree_value]*Degree_value)
	for instance := range stage.Degree_values {
		_copy := instance.GongCopy().(*Degree_value)
		stage.Degree_values_reference[instance] = _copy
		stage.Degree_values_instance[_copy] = instance
		stage.Degree_values_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Directions_reference = make(map[*Direction]*Direction)
	stage.Directions_referenceOrder = make(map[*Direction]uint) // diff Unstage needs the reference order
	stage.Directions_instance = make(map[*Direction]*Direction)
	for instance := range stage.Directions {
		_copy := instance.GongCopy().(*Direction)
		stage.Directions_reference[instance] = _copy
		stage.Directions_instance[_copy] = instance
		stage.Directions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Direction_types_reference = make(map[*Direction_type]*Direction_type)
	stage.Direction_types_referenceOrder = make(map[*Direction_type]uint) // diff Unstage needs the reference order
	stage.Direction_types_instance = make(map[*Direction_type]*Direction_type)
	for instance := range stage.Direction_types {
		_copy := instance.GongCopy().(*Direction_type)
		stage.Direction_types_reference[instance] = _copy
		stage.Direction_types_instance[_copy] = instance
		stage.Direction_types_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Distances_reference = make(map[*Distance]*Distance)
	stage.Distances_referenceOrder = make(map[*Distance]uint) // diff Unstage needs the reference order
	stage.Distances_instance = make(map[*Distance]*Distance)
	for instance := range stage.Distances {
		_copy := instance.GongCopy().(*Distance)
		stage.Distances_reference[instance] = _copy
		stage.Distances_instance[_copy] = instance
		stage.Distances_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Doubles_reference = make(map[*Double]*Double)
	stage.Doubles_referenceOrder = make(map[*Double]uint) // diff Unstage needs the reference order
	stage.Doubles_instance = make(map[*Double]*Double)
	for instance := range stage.Doubles {
		_copy := instance.GongCopy().(*Double)
		stage.Doubles_reference[instance] = _copy
		stage.Doubles_instance[_copy] = instance
		stage.Doubles_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Dynamicss_reference = make(map[*Dynamics]*Dynamics)
	stage.Dynamicss_referenceOrder = make(map[*Dynamics]uint) // diff Unstage needs the reference order
	stage.Dynamicss_instance = make(map[*Dynamics]*Dynamics)
	for instance := range stage.Dynamicss {
		_copy := instance.GongCopy().(*Dynamics)
		stage.Dynamicss_reference[instance] = _copy
		stage.Dynamicss_instance[_copy] = instance
		stage.Dynamicss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Effects_reference = make(map[*Effect]*Effect)
	stage.Effects_referenceOrder = make(map[*Effect]uint) // diff Unstage needs the reference order
	stage.Effects_instance = make(map[*Effect]*Effect)
	for instance := range stage.Effects {
		_copy := instance.GongCopy().(*Effect)
		stage.Effects_reference[instance] = _copy
		stage.Effects_instance[_copy] = instance
		stage.Effects_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Elisions_reference = make(map[*Elision]*Elision)
	stage.Elisions_referenceOrder = make(map[*Elision]uint) // diff Unstage needs the reference order
	stage.Elisions_instance = make(map[*Elision]*Elision)
	for instance := range stage.Elisions {
		_copy := instance.GongCopy().(*Elision)
		stage.Elisions_reference[instance] = _copy
		stage.Elisions_instance[_copy] = instance
		stage.Elisions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Emptys_reference = make(map[*Empty]*Empty)
	stage.Emptys_referenceOrder = make(map[*Empty]uint) // diff Unstage needs the reference order
	stage.Emptys_instance = make(map[*Empty]*Empty)
	for instance := range stage.Emptys {
		_copy := instance.GongCopy().(*Empty)
		stage.Emptys_reference[instance] = _copy
		stage.Emptys_instance[_copy] = instance
		stage.Emptys_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_fonts_reference = make(map[*Empty_font]*Empty_font)
	stage.Empty_fonts_referenceOrder = make(map[*Empty_font]uint) // diff Unstage needs the reference order
	stage.Empty_fonts_instance = make(map[*Empty_font]*Empty_font)
	for instance := range stage.Empty_fonts {
		_copy := instance.GongCopy().(*Empty_font)
		stage.Empty_fonts_reference[instance] = _copy
		stage.Empty_fonts_instance[_copy] = instance
		stage.Empty_fonts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_lines_reference = make(map[*Empty_line]*Empty_line)
	stage.Empty_lines_referenceOrder = make(map[*Empty_line]uint) // diff Unstage needs the reference order
	stage.Empty_lines_instance = make(map[*Empty_line]*Empty_line)
	for instance := range stage.Empty_lines {
		_copy := instance.GongCopy().(*Empty_line)
		stage.Empty_lines_reference[instance] = _copy
		stage.Empty_lines_instance[_copy] = instance
		stage.Empty_lines_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_placements_reference = make(map[*Empty_placement]*Empty_placement)
	stage.Empty_placements_referenceOrder = make(map[*Empty_placement]uint) // diff Unstage needs the reference order
	stage.Empty_placements_instance = make(map[*Empty_placement]*Empty_placement)
	for instance := range stage.Empty_placements {
		_copy := instance.GongCopy().(*Empty_placement)
		stage.Empty_placements_reference[instance] = _copy
		stage.Empty_placements_instance[_copy] = instance
		stage.Empty_placements_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_placement_smufls_reference = make(map[*Empty_placement_smufl]*Empty_placement_smufl)
	stage.Empty_placement_smufls_referenceOrder = make(map[*Empty_placement_smufl]uint) // diff Unstage needs the reference order
	stage.Empty_placement_smufls_instance = make(map[*Empty_placement_smufl]*Empty_placement_smufl)
	for instance := range stage.Empty_placement_smufls {
		_copy := instance.GongCopy().(*Empty_placement_smufl)
		stage.Empty_placement_smufls_reference[instance] = _copy
		stage.Empty_placement_smufls_instance[_copy] = instance
		stage.Empty_placement_smufls_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_print_object_style_aligns_reference = make(map[*Empty_print_object_style_align]*Empty_print_object_style_align)
	stage.Empty_print_object_style_aligns_referenceOrder = make(map[*Empty_print_object_style_align]uint) // diff Unstage needs the reference order
	stage.Empty_print_object_style_aligns_instance = make(map[*Empty_print_object_style_align]*Empty_print_object_style_align)
	for instance := range stage.Empty_print_object_style_aligns {
		_copy := instance.GongCopy().(*Empty_print_object_style_align)
		stage.Empty_print_object_style_aligns_reference[instance] = _copy
		stage.Empty_print_object_style_aligns_instance[_copy] = instance
		stage.Empty_print_object_style_aligns_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_print_styles_reference = make(map[*Empty_print_style]*Empty_print_style)
	stage.Empty_print_styles_referenceOrder = make(map[*Empty_print_style]uint) // diff Unstage needs the reference order
	stage.Empty_print_styles_instance = make(map[*Empty_print_style]*Empty_print_style)
	for instance := range stage.Empty_print_styles {
		_copy := instance.GongCopy().(*Empty_print_style)
		stage.Empty_print_styles_reference[instance] = _copy
		stage.Empty_print_styles_instance[_copy] = instance
		stage.Empty_print_styles_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_print_style_aligns_reference = make(map[*Empty_print_style_align]*Empty_print_style_align)
	stage.Empty_print_style_aligns_referenceOrder = make(map[*Empty_print_style_align]uint) // diff Unstage needs the reference order
	stage.Empty_print_style_aligns_instance = make(map[*Empty_print_style_align]*Empty_print_style_align)
	for instance := range stage.Empty_print_style_aligns {
		_copy := instance.GongCopy().(*Empty_print_style_align)
		stage.Empty_print_style_aligns_reference[instance] = _copy
		stage.Empty_print_style_aligns_instance[_copy] = instance
		stage.Empty_print_style_aligns_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_print_style_align_ids_reference = make(map[*Empty_print_style_align_id]*Empty_print_style_align_id)
	stage.Empty_print_style_align_ids_referenceOrder = make(map[*Empty_print_style_align_id]uint) // diff Unstage needs the reference order
	stage.Empty_print_style_align_ids_instance = make(map[*Empty_print_style_align_id]*Empty_print_style_align_id)
	for instance := range stage.Empty_print_style_align_ids {
		_copy := instance.GongCopy().(*Empty_print_style_align_id)
		stage.Empty_print_style_align_ids_reference[instance] = _copy
		stage.Empty_print_style_align_ids_instance[_copy] = instance
		stage.Empty_print_style_align_ids_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Empty_trill_sounds_reference = make(map[*Empty_trill_sound]*Empty_trill_sound)
	stage.Empty_trill_sounds_referenceOrder = make(map[*Empty_trill_sound]uint) // diff Unstage needs the reference order
	stage.Empty_trill_sounds_instance = make(map[*Empty_trill_sound]*Empty_trill_sound)
	for instance := range stage.Empty_trill_sounds {
		_copy := instance.GongCopy().(*Empty_trill_sound)
		stage.Empty_trill_sounds_reference[instance] = _copy
		stage.Empty_trill_sounds_instance[_copy] = instance
		stage.Empty_trill_sounds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Encodings_reference = make(map[*Encoding]*Encoding)
	stage.Encodings_referenceOrder = make(map[*Encoding]uint) // diff Unstage needs the reference order
	stage.Encodings_instance = make(map[*Encoding]*Encoding)
	for instance := range stage.Encodings {
		_copy := instance.GongCopy().(*Encoding)
		stage.Encodings_reference[instance] = _copy
		stage.Encodings_instance[_copy] = instance
		stage.Encodings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Endings_reference = make(map[*Ending]*Ending)
	stage.Endings_referenceOrder = make(map[*Ending]uint) // diff Unstage needs the reference order
	stage.Endings_instance = make(map[*Ending]*Ending)
	for instance := range stage.Endings {
		_copy := instance.GongCopy().(*Ending)
		stage.Endings_reference[instance] = _copy
		stage.Endings_instance[_copy] = instance
		stage.Endings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Extends_reference = make(map[*Extend]*Extend)
	stage.Extends_referenceOrder = make(map[*Extend]uint) // diff Unstage needs the reference order
	stage.Extends_instance = make(map[*Extend]*Extend)
	for instance := range stage.Extends {
		_copy := instance.GongCopy().(*Extend)
		stage.Extends_reference[instance] = _copy
		stage.Extends_instance[_copy] = instance
		stage.Extends_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Features_reference = make(map[*Feature]*Feature)
	stage.Features_referenceOrder = make(map[*Feature]uint) // diff Unstage needs the reference order
	stage.Features_instance = make(map[*Feature]*Feature)
	for instance := range stage.Features {
		_copy := instance.GongCopy().(*Feature)
		stage.Features_reference[instance] = _copy
		stage.Features_instance[_copy] = instance
		stage.Features_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Fermatas_reference = make(map[*Fermata]*Fermata)
	stage.Fermatas_referenceOrder = make(map[*Fermata]uint) // diff Unstage needs the reference order
	stage.Fermatas_instance = make(map[*Fermata]*Fermata)
	for instance := range stage.Fermatas {
		_copy := instance.GongCopy().(*Fermata)
		stage.Fermatas_reference[instance] = _copy
		stage.Fermatas_instance[_copy] = instance
		stage.Fermatas_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Figures_reference = make(map[*Figure]*Figure)
	stage.Figures_referenceOrder = make(map[*Figure]uint) // diff Unstage needs the reference order
	stage.Figures_instance = make(map[*Figure]*Figure)
	for instance := range stage.Figures {
		_copy := instance.GongCopy().(*Figure)
		stage.Figures_reference[instance] = _copy
		stage.Figures_instance[_copy] = instance
		stage.Figures_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Figured_basss_reference = make(map[*Figured_bass]*Figured_bass)
	stage.Figured_basss_referenceOrder = make(map[*Figured_bass]uint) // diff Unstage needs the reference order
	stage.Figured_basss_instance = make(map[*Figured_bass]*Figured_bass)
	for instance := range stage.Figured_basss {
		_copy := instance.GongCopy().(*Figured_bass)
		stage.Figured_basss_reference[instance] = _copy
		stage.Figured_basss_instance[_copy] = instance
		stage.Figured_basss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Fingerings_reference = make(map[*Fingering]*Fingering)
	stage.Fingerings_referenceOrder = make(map[*Fingering]uint) // diff Unstage needs the reference order
	stage.Fingerings_instance = make(map[*Fingering]*Fingering)
	for instance := range stage.Fingerings {
		_copy := instance.GongCopy().(*Fingering)
		stage.Fingerings_reference[instance] = _copy
		stage.Fingerings_instance[_copy] = instance
		stage.Fingerings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.First_frets_reference = make(map[*First_fret]*First_fret)
	stage.First_frets_referenceOrder = make(map[*First_fret]uint) // diff Unstage needs the reference order
	stage.First_frets_instance = make(map[*First_fret]*First_fret)
	for instance := range stage.First_frets {
		_copy := instance.GongCopy().(*First_fret)
		stage.First_frets_reference[instance] = _copy
		stage.First_frets_instance[_copy] = instance
		stage.First_frets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.For_parts_reference = make(map[*For_part]*For_part)
	stage.For_parts_referenceOrder = make(map[*For_part]uint) // diff Unstage needs the reference order
	stage.For_parts_instance = make(map[*For_part]*For_part)
	for instance := range stage.For_parts {
		_copy := instance.GongCopy().(*For_part)
		stage.For_parts_reference[instance] = _copy
		stage.For_parts_instance[_copy] = instance
		stage.For_parts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Formatted_symbols_reference = make(map[*Formatted_symbol]*Formatted_symbol)
	stage.Formatted_symbols_referenceOrder = make(map[*Formatted_symbol]uint) // diff Unstage needs the reference order
	stage.Formatted_symbols_instance = make(map[*Formatted_symbol]*Formatted_symbol)
	for instance := range stage.Formatted_symbols {
		_copy := instance.GongCopy().(*Formatted_symbol)
		stage.Formatted_symbols_reference[instance] = _copy
		stage.Formatted_symbols_instance[_copy] = instance
		stage.Formatted_symbols_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Formatted_symbol_ids_reference = make(map[*Formatted_symbol_id]*Formatted_symbol_id)
	stage.Formatted_symbol_ids_referenceOrder = make(map[*Formatted_symbol_id]uint) // diff Unstage needs the reference order
	stage.Formatted_symbol_ids_instance = make(map[*Formatted_symbol_id]*Formatted_symbol_id)
	for instance := range stage.Formatted_symbol_ids {
		_copy := instance.GongCopy().(*Formatted_symbol_id)
		stage.Formatted_symbol_ids_reference[instance] = _copy
		stage.Formatted_symbol_ids_instance[_copy] = instance
		stage.Formatted_symbol_ids_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Formatted_texts_reference = make(map[*Formatted_text]*Formatted_text)
	stage.Formatted_texts_referenceOrder = make(map[*Formatted_text]uint) // diff Unstage needs the reference order
	stage.Formatted_texts_instance = make(map[*Formatted_text]*Formatted_text)
	for instance := range stage.Formatted_texts {
		_copy := instance.GongCopy().(*Formatted_text)
		stage.Formatted_texts_reference[instance] = _copy
		stage.Formatted_texts_instance[_copy] = instance
		stage.Formatted_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Formatted_text_ids_reference = make(map[*Formatted_text_id]*Formatted_text_id)
	stage.Formatted_text_ids_referenceOrder = make(map[*Formatted_text_id]uint) // diff Unstage needs the reference order
	stage.Formatted_text_ids_instance = make(map[*Formatted_text_id]*Formatted_text_id)
	for instance := range stage.Formatted_text_ids {
		_copy := instance.GongCopy().(*Formatted_text_id)
		stage.Formatted_text_ids_reference[instance] = _copy
		stage.Formatted_text_ids_instance[_copy] = instance
		stage.Formatted_text_ids_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Forwards_reference = make(map[*Forward]*Forward)
	stage.Forwards_referenceOrder = make(map[*Forward]uint) // diff Unstage needs the reference order
	stage.Forwards_instance = make(map[*Forward]*Forward)
	for instance := range stage.Forwards {
		_copy := instance.GongCopy().(*Forward)
		stage.Forwards_reference[instance] = _copy
		stage.Forwards_instance[_copy] = instance
		stage.Forwards_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Frames_reference = make(map[*Frame]*Frame)
	stage.Frames_referenceOrder = make(map[*Frame]uint) // diff Unstage needs the reference order
	stage.Frames_instance = make(map[*Frame]*Frame)
	for instance := range stage.Frames {
		_copy := instance.GongCopy().(*Frame)
		stage.Frames_reference[instance] = _copy
		stage.Frames_instance[_copy] = instance
		stage.Frames_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Frame_notes_reference = make(map[*Frame_note]*Frame_note)
	stage.Frame_notes_referenceOrder = make(map[*Frame_note]uint) // diff Unstage needs the reference order
	stage.Frame_notes_instance = make(map[*Frame_note]*Frame_note)
	for instance := range stage.Frame_notes {
		_copy := instance.GongCopy().(*Frame_note)
		stage.Frame_notes_reference[instance] = _copy
		stage.Frame_notes_instance[_copy] = instance
		stage.Frame_notes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Frets_reference = make(map[*Fret]*Fret)
	stage.Frets_referenceOrder = make(map[*Fret]uint) // diff Unstage needs the reference order
	stage.Frets_instance = make(map[*Fret]*Fret)
	for instance := range stage.Frets {
		_copy := instance.GongCopy().(*Fret)
		stage.Frets_reference[instance] = _copy
		stage.Frets_instance[_copy] = instance
		stage.Frets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Glasss_reference = make(map[*Glass]*Glass)
	stage.Glasss_referenceOrder = make(map[*Glass]uint) // diff Unstage needs the reference order
	stage.Glasss_instance = make(map[*Glass]*Glass)
	for instance := range stage.Glasss {
		_copy := instance.GongCopy().(*Glass)
		stage.Glasss_reference[instance] = _copy
		stage.Glasss_instance[_copy] = instance
		stage.Glasss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Glissandos_reference = make(map[*Glissando]*Glissando)
	stage.Glissandos_referenceOrder = make(map[*Glissando]uint) // diff Unstage needs the reference order
	stage.Glissandos_instance = make(map[*Glissando]*Glissando)
	for instance := range stage.Glissandos {
		_copy := instance.GongCopy().(*Glissando)
		stage.Glissandos_reference[instance] = _copy
		stage.Glissandos_instance[_copy] = instance
		stage.Glissandos_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Glyphs_reference = make(map[*Glyph]*Glyph)
	stage.Glyphs_referenceOrder = make(map[*Glyph]uint) // diff Unstage needs the reference order
	stage.Glyphs_instance = make(map[*Glyph]*Glyph)
	for instance := range stage.Glyphs {
		_copy := instance.GongCopy().(*Glyph)
		stage.Glyphs_reference[instance] = _copy
		stage.Glyphs_instance[_copy] = instance
		stage.Glyphs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Graces_reference = make(map[*Grace]*Grace)
	stage.Graces_referenceOrder = make(map[*Grace]uint) // diff Unstage needs the reference order
	stage.Graces_instance = make(map[*Grace]*Grace)
	for instance := range stage.Graces {
		_copy := instance.GongCopy().(*Grace)
		stage.Graces_reference[instance] = _copy
		stage.Graces_instance[_copy] = instance
		stage.Graces_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Group_barlines_reference = make(map[*Group_barline]*Group_barline)
	stage.Group_barlines_referenceOrder = make(map[*Group_barline]uint) // diff Unstage needs the reference order
	stage.Group_barlines_instance = make(map[*Group_barline]*Group_barline)
	for instance := range stage.Group_barlines {
		_copy := instance.GongCopy().(*Group_barline)
		stage.Group_barlines_reference[instance] = _copy
		stage.Group_barlines_instance[_copy] = instance
		stage.Group_barlines_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Group_names_reference = make(map[*Group_name]*Group_name)
	stage.Group_names_referenceOrder = make(map[*Group_name]uint) // diff Unstage needs the reference order
	stage.Group_names_instance = make(map[*Group_name]*Group_name)
	for instance := range stage.Group_names {
		_copy := instance.GongCopy().(*Group_name)
		stage.Group_names_reference[instance] = _copy
		stage.Group_names_instance[_copy] = instance
		stage.Group_names_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Group_symbols_reference = make(map[*Group_symbol]*Group_symbol)
	stage.Group_symbols_referenceOrder = make(map[*Group_symbol]uint) // diff Unstage needs the reference order
	stage.Group_symbols_instance = make(map[*Group_symbol]*Group_symbol)
	for instance := range stage.Group_symbols {
		_copy := instance.GongCopy().(*Group_symbol)
		stage.Group_symbols_reference[instance] = _copy
		stage.Group_symbols_instance[_copy] = instance
		stage.Group_symbols_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Groupings_reference = make(map[*Grouping]*Grouping)
	stage.Groupings_referenceOrder = make(map[*Grouping]uint) // diff Unstage needs the reference order
	stage.Groupings_instance = make(map[*Grouping]*Grouping)
	for instance := range stage.Groupings {
		_copy := instance.GongCopy().(*Grouping)
		stage.Groupings_reference[instance] = _copy
		stage.Groupings_instance[_copy] = instance
		stage.Groupings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Hammer_on_pull_offs_reference = make(map[*Hammer_on_pull_off]*Hammer_on_pull_off)
	stage.Hammer_on_pull_offs_referenceOrder = make(map[*Hammer_on_pull_off]uint) // diff Unstage needs the reference order
	stage.Hammer_on_pull_offs_instance = make(map[*Hammer_on_pull_off]*Hammer_on_pull_off)
	for instance := range stage.Hammer_on_pull_offs {
		_copy := instance.GongCopy().(*Hammer_on_pull_off)
		stage.Hammer_on_pull_offs_reference[instance] = _copy
		stage.Hammer_on_pull_offs_instance[_copy] = instance
		stage.Hammer_on_pull_offs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Handbells_reference = make(map[*Handbell]*Handbell)
	stage.Handbells_referenceOrder = make(map[*Handbell]uint) // diff Unstage needs the reference order
	stage.Handbells_instance = make(map[*Handbell]*Handbell)
	for instance := range stage.Handbells {
		_copy := instance.GongCopy().(*Handbell)
		stage.Handbells_reference[instance] = _copy
		stage.Handbells_instance[_copy] = instance
		stage.Handbells_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harmon_closeds_reference = make(map[*Harmon_closed]*Harmon_closed)
	stage.Harmon_closeds_referenceOrder = make(map[*Harmon_closed]uint) // diff Unstage needs the reference order
	stage.Harmon_closeds_instance = make(map[*Harmon_closed]*Harmon_closed)
	for instance := range stage.Harmon_closeds {
		_copy := instance.GongCopy().(*Harmon_closed)
		stage.Harmon_closeds_reference[instance] = _copy
		stage.Harmon_closeds_instance[_copy] = instance
		stage.Harmon_closeds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harmon_mutes_reference = make(map[*Harmon_mute]*Harmon_mute)
	stage.Harmon_mutes_referenceOrder = make(map[*Harmon_mute]uint) // diff Unstage needs the reference order
	stage.Harmon_mutes_instance = make(map[*Harmon_mute]*Harmon_mute)
	for instance := range stage.Harmon_mutes {
		_copy := instance.GongCopy().(*Harmon_mute)
		stage.Harmon_mutes_reference[instance] = _copy
		stage.Harmon_mutes_instance[_copy] = instance
		stage.Harmon_mutes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harmonics_reference = make(map[*Harmonic]*Harmonic)
	stage.Harmonics_referenceOrder = make(map[*Harmonic]uint) // diff Unstage needs the reference order
	stage.Harmonics_instance = make(map[*Harmonic]*Harmonic)
	for instance := range stage.Harmonics {
		_copy := instance.GongCopy().(*Harmonic)
		stage.Harmonics_reference[instance] = _copy
		stage.Harmonics_instance[_copy] = instance
		stage.Harmonics_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harmonys_reference = make(map[*Harmony]*Harmony)
	stage.Harmonys_referenceOrder = make(map[*Harmony]uint) // diff Unstage needs the reference order
	stage.Harmonys_instance = make(map[*Harmony]*Harmony)
	for instance := range stage.Harmonys {
		_copy := instance.GongCopy().(*Harmony)
		stage.Harmonys_reference[instance] = _copy
		stage.Harmonys_instance[_copy] = instance
		stage.Harmonys_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harmony_alters_reference = make(map[*Harmony_alter]*Harmony_alter)
	stage.Harmony_alters_referenceOrder = make(map[*Harmony_alter]uint) // diff Unstage needs the reference order
	stage.Harmony_alters_instance = make(map[*Harmony_alter]*Harmony_alter)
	for instance := range stage.Harmony_alters {
		_copy := instance.GongCopy().(*Harmony_alter)
		stage.Harmony_alters_reference[instance] = _copy
		stage.Harmony_alters_instance[_copy] = instance
		stage.Harmony_alters_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Harp_pedalss_reference = make(map[*Harp_pedals]*Harp_pedals)
	stage.Harp_pedalss_referenceOrder = make(map[*Harp_pedals]uint) // diff Unstage needs the reference order
	stage.Harp_pedalss_instance = make(map[*Harp_pedals]*Harp_pedals)
	for instance := range stage.Harp_pedalss {
		_copy := instance.GongCopy().(*Harp_pedals)
		stage.Harp_pedalss_reference[instance] = _copy
		stage.Harp_pedalss_instance[_copy] = instance
		stage.Harp_pedalss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Heel_toes_reference = make(map[*Heel_toe]*Heel_toe)
	stage.Heel_toes_referenceOrder = make(map[*Heel_toe]uint) // diff Unstage needs the reference order
	stage.Heel_toes_instance = make(map[*Heel_toe]*Heel_toe)
	for instance := range stage.Heel_toes {
		_copy := instance.GongCopy().(*Heel_toe)
		stage.Heel_toes_reference[instance] = _copy
		stage.Heel_toes_instance[_copy] = instance
		stage.Heel_toes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Holes_reference = make(map[*Hole]*Hole)
	stage.Holes_referenceOrder = make(map[*Hole]uint) // diff Unstage needs the reference order
	stage.Holes_instance = make(map[*Hole]*Hole)
	for instance := range stage.Holes {
		_copy := instance.GongCopy().(*Hole)
		stage.Holes_reference[instance] = _copy
		stage.Holes_instance[_copy] = instance
		stage.Holes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Hole_closeds_reference = make(map[*Hole_closed]*Hole_closed)
	stage.Hole_closeds_referenceOrder = make(map[*Hole_closed]uint) // diff Unstage needs the reference order
	stage.Hole_closeds_instance = make(map[*Hole_closed]*Hole_closed)
	for instance := range stage.Hole_closeds {
		_copy := instance.GongCopy().(*Hole_closed)
		stage.Hole_closeds_reference[instance] = _copy
		stage.Hole_closeds_instance[_copy] = instance
		stage.Hole_closeds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Horizontal_turns_reference = make(map[*Horizontal_turn]*Horizontal_turn)
	stage.Horizontal_turns_referenceOrder = make(map[*Horizontal_turn]uint) // diff Unstage needs the reference order
	stage.Horizontal_turns_instance = make(map[*Horizontal_turn]*Horizontal_turn)
	for instance := range stage.Horizontal_turns {
		_copy := instance.GongCopy().(*Horizontal_turn)
		stage.Horizontal_turns_reference[instance] = _copy
		stage.Horizontal_turns_instance[_copy] = instance
		stage.Horizontal_turns_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Identifications_reference = make(map[*Identification]*Identification)
	stage.Identifications_referenceOrder = make(map[*Identification]uint) // diff Unstage needs the reference order
	stage.Identifications_instance = make(map[*Identification]*Identification)
	for instance := range stage.Identifications {
		_copy := instance.GongCopy().(*Identification)
		stage.Identifications_reference[instance] = _copy
		stage.Identifications_instance[_copy] = instance
		stage.Identifications_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Images_reference = make(map[*Image]*Image)
	stage.Images_referenceOrder = make(map[*Image]uint) // diff Unstage needs the reference order
	stage.Images_instance = make(map[*Image]*Image)
	for instance := range stage.Images {
		_copy := instance.GongCopy().(*Image)
		stage.Images_reference[instance] = _copy
		stage.Images_instance[_copy] = instance
		stage.Images_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Instruments_reference = make(map[*Instrument]*Instrument)
	stage.Instruments_referenceOrder = make(map[*Instrument]uint) // diff Unstage needs the reference order
	stage.Instruments_instance = make(map[*Instrument]*Instrument)
	for instance := range stage.Instruments {
		_copy := instance.GongCopy().(*Instrument)
		stage.Instruments_reference[instance] = _copy
		stage.Instruments_instance[_copy] = instance
		stage.Instruments_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Instrument_changes_reference = make(map[*Instrument_change]*Instrument_change)
	stage.Instrument_changes_referenceOrder = make(map[*Instrument_change]uint) // diff Unstage needs the reference order
	stage.Instrument_changes_instance = make(map[*Instrument_change]*Instrument_change)
	for instance := range stage.Instrument_changes {
		_copy := instance.GongCopy().(*Instrument_change)
		stage.Instrument_changes_reference[instance] = _copy
		stage.Instrument_changes_instance[_copy] = instance
		stage.Instrument_changes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Instrument_links_reference = make(map[*Instrument_link]*Instrument_link)
	stage.Instrument_links_referenceOrder = make(map[*Instrument_link]uint) // diff Unstage needs the reference order
	stage.Instrument_links_instance = make(map[*Instrument_link]*Instrument_link)
	for instance := range stage.Instrument_links {
		_copy := instance.GongCopy().(*Instrument_link)
		stage.Instrument_links_reference[instance] = _copy
		stage.Instrument_links_instance[_copy] = instance
		stage.Instrument_links_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Interchangeables_reference = make(map[*Interchangeable]*Interchangeable)
	stage.Interchangeables_referenceOrder = make(map[*Interchangeable]uint) // diff Unstage needs the reference order
	stage.Interchangeables_instance = make(map[*Interchangeable]*Interchangeable)
	for instance := range stage.Interchangeables {
		_copy := instance.GongCopy().(*Interchangeable)
		stage.Interchangeables_reference[instance] = _copy
		stage.Interchangeables_instance[_copy] = instance
		stage.Interchangeables_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Inversions_reference = make(map[*Inversion]*Inversion)
	stage.Inversions_referenceOrder = make(map[*Inversion]uint) // diff Unstage needs the reference order
	stage.Inversions_instance = make(map[*Inversion]*Inversion)
	for instance := range stage.Inversions {
		_copy := instance.GongCopy().(*Inversion)
		stage.Inversions_reference[instance] = _copy
		stage.Inversions_instance[_copy] = instance
		stage.Inversions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Keys_reference = make(map[*Key]*Key)
	stage.Keys_referenceOrder = make(map[*Key]uint) // diff Unstage needs the reference order
	stage.Keys_instance = make(map[*Key]*Key)
	for instance := range stage.Keys {
		_copy := instance.GongCopy().(*Key)
		stage.Keys_reference[instance] = _copy
		stage.Keys_instance[_copy] = instance
		stage.Keys_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Key_accidentals_reference = make(map[*Key_accidental]*Key_accidental)
	stage.Key_accidentals_referenceOrder = make(map[*Key_accidental]uint) // diff Unstage needs the reference order
	stage.Key_accidentals_instance = make(map[*Key_accidental]*Key_accidental)
	for instance := range stage.Key_accidentals {
		_copy := instance.GongCopy().(*Key_accidental)
		stage.Key_accidentals_reference[instance] = _copy
		stage.Key_accidentals_instance[_copy] = instance
		stage.Key_accidentals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Key_octaves_reference = make(map[*Key_octave]*Key_octave)
	stage.Key_octaves_referenceOrder = make(map[*Key_octave]uint) // diff Unstage needs the reference order
	stage.Key_octaves_instance = make(map[*Key_octave]*Key_octave)
	for instance := range stage.Key_octaves {
		_copy := instance.GongCopy().(*Key_octave)
		stage.Key_octaves_reference[instance] = _copy
		stage.Key_octaves_instance[_copy] = instance
		stage.Key_octaves_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Kinds_reference = make(map[*Kind]*Kind)
	stage.Kinds_referenceOrder = make(map[*Kind]uint) // diff Unstage needs the reference order
	stage.Kinds_instance = make(map[*Kind]*Kind)
	for instance := range stage.Kinds {
		_copy := instance.GongCopy().(*Kind)
		stage.Kinds_reference[instance] = _copy
		stage.Kinds_instance[_copy] = instance
		stage.Kinds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Levels_reference = make(map[*Level]*Level)
	stage.Levels_referenceOrder = make(map[*Level]uint) // diff Unstage needs the reference order
	stage.Levels_instance = make(map[*Level]*Level)
	for instance := range stage.Levels {
		_copy := instance.GongCopy().(*Level)
		stage.Levels_reference[instance] = _copy
		stage.Levels_instance[_copy] = instance
		stage.Levels_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Line_details_reference = make(map[*Line_detail]*Line_detail)
	stage.Line_details_referenceOrder = make(map[*Line_detail]uint) // diff Unstage needs the reference order
	stage.Line_details_instance = make(map[*Line_detail]*Line_detail)
	for instance := range stage.Line_details {
		_copy := instance.GongCopy().(*Line_detail)
		stage.Line_details_reference[instance] = _copy
		stage.Line_details_instance[_copy] = instance
		stage.Line_details_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Line_widths_reference = make(map[*Line_width]*Line_width)
	stage.Line_widths_referenceOrder = make(map[*Line_width]uint) // diff Unstage needs the reference order
	stage.Line_widths_instance = make(map[*Line_width]*Line_width)
	for instance := range stage.Line_widths {
		_copy := instance.GongCopy().(*Line_width)
		stage.Line_widths_reference[instance] = _copy
		stage.Line_widths_instance[_copy] = instance
		stage.Line_widths_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Links_reference = make(map[*Link]*Link)
	stage.Links_referenceOrder = make(map[*Link]uint) // diff Unstage needs the reference order
	stage.Links_instance = make(map[*Link]*Link)
	for instance := range stage.Links {
		_copy := instance.GongCopy().(*Link)
		stage.Links_reference[instance] = _copy
		stage.Links_instance[_copy] = instance
		stage.Links_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Listens_reference = make(map[*Listen]*Listen)
	stage.Listens_referenceOrder = make(map[*Listen]uint) // diff Unstage needs the reference order
	stage.Listens_instance = make(map[*Listen]*Listen)
	for instance := range stage.Listens {
		_copy := instance.GongCopy().(*Listen)
		stage.Listens_reference[instance] = _copy
		stage.Listens_instance[_copy] = instance
		stage.Listens_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Listenings_reference = make(map[*Listening]*Listening)
	stage.Listenings_referenceOrder = make(map[*Listening]uint) // diff Unstage needs the reference order
	stage.Listenings_instance = make(map[*Listening]*Listening)
	for instance := range stage.Listenings {
		_copy := instance.GongCopy().(*Listening)
		stage.Listenings_reference[instance] = _copy
		stage.Listenings_instance[_copy] = instance
		stage.Listenings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Lyrics_reference = make(map[*Lyric]*Lyric)
	stage.Lyrics_referenceOrder = make(map[*Lyric]uint) // diff Unstage needs the reference order
	stage.Lyrics_instance = make(map[*Lyric]*Lyric)
	for instance := range stage.Lyrics {
		_copy := instance.GongCopy().(*Lyric)
		stage.Lyrics_reference[instance] = _copy
		stage.Lyrics_instance[_copy] = instance
		stage.Lyrics_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Lyric_fonts_reference = make(map[*Lyric_font]*Lyric_font)
	stage.Lyric_fonts_referenceOrder = make(map[*Lyric_font]uint) // diff Unstage needs the reference order
	stage.Lyric_fonts_instance = make(map[*Lyric_font]*Lyric_font)
	for instance := range stage.Lyric_fonts {
		_copy := instance.GongCopy().(*Lyric_font)
		stage.Lyric_fonts_reference[instance] = _copy
		stage.Lyric_fonts_instance[_copy] = instance
		stage.Lyric_fonts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Lyric_languages_reference = make(map[*Lyric_language]*Lyric_language)
	stage.Lyric_languages_referenceOrder = make(map[*Lyric_language]uint) // diff Unstage needs the reference order
	stage.Lyric_languages_instance = make(map[*Lyric_language]*Lyric_language)
	for instance := range stage.Lyric_languages {
		_copy := instance.GongCopy().(*Lyric_language)
		stage.Lyric_languages_reference[instance] = _copy
		stage.Lyric_languages_instance[_copy] = instance
		stage.Lyric_languages_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Measure_layouts_reference = make(map[*Measure_layout]*Measure_layout)
	stage.Measure_layouts_referenceOrder = make(map[*Measure_layout]uint) // diff Unstage needs the reference order
	stage.Measure_layouts_instance = make(map[*Measure_layout]*Measure_layout)
	for instance := range stage.Measure_layouts {
		_copy := instance.GongCopy().(*Measure_layout)
		stage.Measure_layouts_reference[instance] = _copy
		stage.Measure_layouts_instance[_copy] = instance
		stage.Measure_layouts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Measure_numberings_reference = make(map[*Measure_numbering]*Measure_numbering)
	stage.Measure_numberings_referenceOrder = make(map[*Measure_numbering]uint) // diff Unstage needs the reference order
	stage.Measure_numberings_instance = make(map[*Measure_numbering]*Measure_numbering)
	for instance := range stage.Measure_numberings {
		_copy := instance.GongCopy().(*Measure_numbering)
		stage.Measure_numberings_reference[instance] = _copy
		stage.Measure_numberings_instance[_copy] = instance
		stage.Measure_numberings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Measure_repeats_reference = make(map[*Measure_repeat]*Measure_repeat)
	stage.Measure_repeats_referenceOrder = make(map[*Measure_repeat]uint) // diff Unstage needs the reference order
	stage.Measure_repeats_instance = make(map[*Measure_repeat]*Measure_repeat)
	for instance := range stage.Measure_repeats {
		_copy := instance.GongCopy().(*Measure_repeat)
		stage.Measure_repeats_reference[instance] = _copy
		stage.Measure_repeats_instance[_copy] = instance
		stage.Measure_repeats_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Measure_styles_reference = make(map[*Measure_style]*Measure_style)
	stage.Measure_styles_referenceOrder = make(map[*Measure_style]uint) // diff Unstage needs the reference order
	stage.Measure_styles_instance = make(map[*Measure_style]*Measure_style)
	for instance := range stage.Measure_styles {
		_copy := instance.GongCopy().(*Measure_style)
		stage.Measure_styles_reference[instance] = _copy
		stage.Measure_styles_instance[_copy] = instance
		stage.Measure_styles_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Membranes_reference = make(map[*Membrane]*Membrane)
	stage.Membranes_referenceOrder = make(map[*Membrane]uint) // diff Unstage needs the reference order
	stage.Membranes_instance = make(map[*Membrane]*Membrane)
	for instance := range stage.Membranes {
		_copy := instance.GongCopy().(*Membrane)
		stage.Membranes_reference[instance] = _copy
		stage.Membranes_instance[_copy] = instance
		stage.Membranes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metals_reference = make(map[*Metal]*Metal)
	stage.Metals_referenceOrder = make(map[*Metal]uint) // diff Unstage needs the reference order
	stage.Metals_instance = make(map[*Metal]*Metal)
	for instance := range stage.Metals {
		_copy := instance.GongCopy().(*Metal)
		stage.Metals_reference[instance] = _copy
		stage.Metals_instance[_copy] = instance
		stage.Metals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metronomes_reference = make(map[*Metronome]*Metronome)
	stage.Metronomes_referenceOrder = make(map[*Metronome]uint) // diff Unstage needs the reference order
	stage.Metronomes_instance = make(map[*Metronome]*Metronome)
	for instance := range stage.Metronomes {
		_copy := instance.GongCopy().(*Metronome)
		stage.Metronomes_reference[instance] = _copy
		stage.Metronomes_instance[_copy] = instance
		stage.Metronomes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metronome_beams_reference = make(map[*Metronome_beam]*Metronome_beam)
	stage.Metronome_beams_referenceOrder = make(map[*Metronome_beam]uint) // diff Unstage needs the reference order
	stage.Metronome_beams_instance = make(map[*Metronome_beam]*Metronome_beam)
	for instance := range stage.Metronome_beams {
		_copy := instance.GongCopy().(*Metronome_beam)
		stage.Metronome_beams_reference[instance] = _copy
		stage.Metronome_beams_instance[_copy] = instance
		stage.Metronome_beams_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metronome_notes_reference = make(map[*Metronome_note]*Metronome_note)
	stage.Metronome_notes_referenceOrder = make(map[*Metronome_note]uint) // diff Unstage needs the reference order
	stage.Metronome_notes_instance = make(map[*Metronome_note]*Metronome_note)
	for instance := range stage.Metronome_notes {
		_copy := instance.GongCopy().(*Metronome_note)
		stage.Metronome_notes_reference[instance] = _copy
		stage.Metronome_notes_instance[_copy] = instance
		stage.Metronome_notes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metronome_tieds_reference = make(map[*Metronome_tied]*Metronome_tied)
	stage.Metronome_tieds_referenceOrder = make(map[*Metronome_tied]uint) // diff Unstage needs the reference order
	stage.Metronome_tieds_instance = make(map[*Metronome_tied]*Metronome_tied)
	for instance := range stage.Metronome_tieds {
		_copy := instance.GongCopy().(*Metronome_tied)
		stage.Metronome_tieds_reference[instance] = _copy
		stage.Metronome_tieds_instance[_copy] = instance
		stage.Metronome_tieds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Metronome_tuplets_reference = make(map[*Metronome_tuplet]*Metronome_tuplet)
	stage.Metronome_tuplets_referenceOrder = make(map[*Metronome_tuplet]uint) // diff Unstage needs the reference order
	stage.Metronome_tuplets_instance = make(map[*Metronome_tuplet]*Metronome_tuplet)
	for instance := range stage.Metronome_tuplets {
		_copy := instance.GongCopy().(*Metronome_tuplet)
		stage.Metronome_tuplets_reference[instance] = _copy
		stage.Metronome_tuplets_instance[_copy] = instance
		stage.Metronome_tuplets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Midi_devices_reference = make(map[*Midi_device]*Midi_device)
	stage.Midi_devices_referenceOrder = make(map[*Midi_device]uint) // diff Unstage needs the reference order
	stage.Midi_devices_instance = make(map[*Midi_device]*Midi_device)
	for instance := range stage.Midi_devices {
		_copy := instance.GongCopy().(*Midi_device)
		stage.Midi_devices_reference[instance] = _copy
		stage.Midi_devices_instance[_copy] = instance
		stage.Midi_devices_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Midi_instruments_reference = make(map[*Midi_instrument]*Midi_instrument)
	stage.Midi_instruments_referenceOrder = make(map[*Midi_instrument]uint) // diff Unstage needs the reference order
	stage.Midi_instruments_instance = make(map[*Midi_instrument]*Midi_instrument)
	for instance := range stage.Midi_instruments {
		_copy := instance.GongCopy().(*Midi_instrument)
		stage.Midi_instruments_reference[instance] = _copy
		stage.Midi_instruments_instance[_copy] = instance
		stage.Midi_instruments_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Miscellaneouss_reference = make(map[*Miscellaneous]*Miscellaneous)
	stage.Miscellaneouss_referenceOrder = make(map[*Miscellaneous]uint) // diff Unstage needs the reference order
	stage.Miscellaneouss_instance = make(map[*Miscellaneous]*Miscellaneous)
	for instance := range stage.Miscellaneouss {
		_copy := instance.GongCopy().(*Miscellaneous)
		stage.Miscellaneouss_reference[instance] = _copy
		stage.Miscellaneouss_instance[_copy] = instance
		stage.Miscellaneouss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Miscellaneous_fields_reference = make(map[*Miscellaneous_field]*Miscellaneous_field)
	stage.Miscellaneous_fields_referenceOrder = make(map[*Miscellaneous_field]uint) // diff Unstage needs the reference order
	stage.Miscellaneous_fields_instance = make(map[*Miscellaneous_field]*Miscellaneous_field)
	for instance := range stage.Miscellaneous_fields {
		_copy := instance.GongCopy().(*Miscellaneous_field)
		stage.Miscellaneous_fields_reference[instance] = _copy
		stage.Miscellaneous_fields_instance[_copy] = instance
		stage.Miscellaneous_fields_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Mordents_reference = make(map[*Mordent]*Mordent)
	stage.Mordents_referenceOrder = make(map[*Mordent]uint) // diff Unstage needs the reference order
	stage.Mordents_instance = make(map[*Mordent]*Mordent)
	for instance := range stage.Mordents {
		_copy := instance.GongCopy().(*Mordent)
		stage.Mordents_reference[instance] = _copy
		stage.Mordents_instance[_copy] = instance
		stage.Mordents_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Multiple_rests_reference = make(map[*Multiple_rest]*Multiple_rest)
	stage.Multiple_rests_referenceOrder = make(map[*Multiple_rest]uint) // diff Unstage needs the reference order
	stage.Multiple_rests_instance = make(map[*Multiple_rest]*Multiple_rest)
	for instance := range stage.Multiple_rests {
		_copy := instance.GongCopy().(*Multiple_rest)
		stage.Multiple_rests_reference[instance] = _copy
		stage.Multiple_rests_instance[_copy] = instance
		stage.Multiple_rests_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Name_displays_reference = make(map[*Name_display]*Name_display)
	stage.Name_displays_referenceOrder = make(map[*Name_display]uint) // diff Unstage needs the reference order
	stage.Name_displays_instance = make(map[*Name_display]*Name_display)
	for instance := range stage.Name_displays {
		_copy := instance.GongCopy().(*Name_display)
		stage.Name_displays_reference[instance] = _copy
		stage.Name_displays_instance[_copy] = instance
		stage.Name_displays_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Non_arpeggiates_reference = make(map[*Non_arpeggiate]*Non_arpeggiate)
	stage.Non_arpeggiates_referenceOrder = make(map[*Non_arpeggiate]uint) // diff Unstage needs the reference order
	stage.Non_arpeggiates_instance = make(map[*Non_arpeggiate]*Non_arpeggiate)
	for instance := range stage.Non_arpeggiates {
		_copy := instance.GongCopy().(*Non_arpeggiate)
		stage.Non_arpeggiates_reference[instance] = _copy
		stage.Non_arpeggiates_instance[_copy] = instance
		stage.Non_arpeggiates_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Notationss_reference = make(map[*Notations]*Notations)
	stage.Notationss_referenceOrder = make(map[*Notations]uint) // diff Unstage needs the reference order
	stage.Notationss_instance = make(map[*Notations]*Notations)
	for instance := range stage.Notationss {
		_copy := instance.GongCopy().(*Notations)
		stage.Notationss_reference[instance] = _copy
		stage.Notationss_instance[_copy] = instance
		stage.Notationss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Notes_reference = make(map[*Note]*Note)
	stage.Notes_referenceOrder = make(map[*Note]uint) // diff Unstage needs the reference order
	stage.Notes_instance = make(map[*Note]*Note)
	for instance := range stage.Notes {
		_copy := instance.GongCopy().(*Note)
		stage.Notes_reference[instance] = _copy
		stage.Notes_instance[_copy] = instance
		stage.Notes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Note_sizes_reference = make(map[*Note_size]*Note_size)
	stage.Note_sizes_referenceOrder = make(map[*Note_size]uint) // diff Unstage needs the reference order
	stage.Note_sizes_instance = make(map[*Note_size]*Note_size)
	for instance := range stage.Note_sizes {
		_copy := instance.GongCopy().(*Note_size)
		stage.Note_sizes_reference[instance] = _copy
		stage.Note_sizes_instance[_copy] = instance
		stage.Note_sizes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Note_types_reference = make(map[*Note_type]*Note_type)
	stage.Note_types_referenceOrder = make(map[*Note_type]uint) // diff Unstage needs the reference order
	stage.Note_types_instance = make(map[*Note_type]*Note_type)
	for instance := range stage.Note_types {
		_copy := instance.GongCopy().(*Note_type)
		stage.Note_types_reference[instance] = _copy
		stage.Note_types_instance[_copy] = instance
		stage.Note_types_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Noteheads_reference = make(map[*Notehead]*Notehead)
	stage.Noteheads_referenceOrder = make(map[*Notehead]uint) // diff Unstage needs the reference order
	stage.Noteheads_instance = make(map[*Notehead]*Notehead)
	for instance := range stage.Noteheads {
		_copy := instance.GongCopy().(*Notehead)
		stage.Noteheads_reference[instance] = _copy
		stage.Noteheads_instance[_copy] = instance
		stage.Noteheads_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Notehead_texts_reference = make(map[*Notehead_text]*Notehead_text)
	stage.Notehead_texts_referenceOrder = make(map[*Notehead_text]uint) // diff Unstage needs the reference order
	stage.Notehead_texts_instance = make(map[*Notehead_text]*Notehead_text)
	for instance := range stage.Notehead_texts {
		_copy := instance.GongCopy().(*Notehead_text)
		stage.Notehead_texts_reference[instance] = _copy
		stage.Notehead_texts_instance[_copy] = instance
		stage.Notehead_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Numerals_reference = make(map[*Numeral]*Numeral)
	stage.Numerals_referenceOrder = make(map[*Numeral]uint) // diff Unstage needs the reference order
	stage.Numerals_instance = make(map[*Numeral]*Numeral)
	for instance := range stage.Numerals {
		_copy := instance.GongCopy().(*Numeral)
		stage.Numerals_reference[instance] = _copy
		stage.Numerals_instance[_copy] = instance
		stage.Numerals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Numeral_keys_reference = make(map[*Numeral_key]*Numeral_key)
	stage.Numeral_keys_referenceOrder = make(map[*Numeral_key]uint) // diff Unstage needs the reference order
	stage.Numeral_keys_instance = make(map[*Numeral_key]*Numeral_key)
	for instance := range stage.Numeral_keys {
		_copy := instance.GongCopy().(*Numeral_key)
		stage.Numeral_keys_reference[instance] = _copy
		stage.Numeral_keys_instance[_copy] = instance
		stage.Numeral_keys_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Numeral_roots_reference = make(map[*Numeral_root]*Numeral_root)
	stage.Numeral_roots_referenceOrder = make(map[*Numeral_root]uint) // diff Unstage needs the reference order
	stage.Numeral_roots_instance = make(map[*Numeral_root]*Numeral_root)
	for instance := range stage.Numeral_roots {
		_copy := instance.GongCopy().(*Numeral_root)
		stage.Numeral_roots_reference[instance] = _copy
		stage.Numeral_roots_instance[_copy] = instance
		stage.Numeral_roots_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Octave_shifts_reference = make(map[*Octave_shift]*Octave_shift)
	stage.Octave_shifts_referenceOrder = make(map[*Octave_shift]uint) // diff Unstage needs the reference order
	stage.Octave_shifts_instance = make(map[*Octave_shift]*Octave_shift)
	for instance := range stage.Octave_shifts {
		_copy := instance.GongCopy().(*Octave_shift)
		stage.Octave_shifts_reference[instance] = _copy
		stage.Octave_shifts_instance[_copy] = instance
		stage.Octave_shifts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Offsets_reference = make(map[*Offset]*Offset)
	stage.Offsets_referenceOrder = make(map[*Offset]uint) // diff Unstage needs the reference order
	stage.Offsets_instance = make(map[*Offset]*Offset)
	for instance := range stage.Offsets {
		_copy := instance.GongCopy().(*Offset)
		stage.Offsets_reference[instance] = _copy
		stage.Offsets_instance[_copy] = instance
		stage.Offsets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Opuss_reference = make(map[*Opus]*Opus)
	stage.Opuss_referenceOrder = make(map[*Opus]uint) // diff Unstage needs the reference order
	stage.Opuss_instance = make(map[*Opus]*Opus)
	for instance := range stage.Opuss {
		_copy := instance.GongCopy().(*Opus)
		stage.Opuss_reference[instance] = _copy
		stage.Opuss_instance[_copy] = instance
		stage.Opuss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Ornamentss_reference = make(map[*Ornaments]*Ornaments)
	stage.Ornamentss_referenceOrder = make(map[*Ornaments]uint) // diff Unstage needs the reference order
	stage.Ornamentss_instance = make(map[*Ornaments]*Ornaments)
	for instance := range stage.Ornamentss {
		_copy := instance.GongCopy().(*Ornaments)
		stage.Ornamentss_reference[instance] = _copy
		stage.Ornamentss_instance[_copy] = instance
		stage.Ornamentss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_appearances_reference = make(map[*Other_appearance]*Other_appearance)
	stage.Other_appearances_referenceOrder = make(map[*Other_appearance]uint) // diff Unstage needs the reference order
	stage.Other_appearances_instance = make(map[*Other_appearance]*Other_appearance)
	for instance := range stage.Other_appearances {
		_copy := instance.GongCopy().(*Other_appearance)
		stage.Other_appearances_reference[instance] = _copy
		stage.Other_appearances_instance[_copy] = instance
		stage.Other_appearances_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_directions_reference = make(map[*Other_direction]*Other_direction)
	stage.Other_directions_referenceOrder = make(map[*Other_direction]uint) // diff Unstage needs the reference order
	stage.Other_directions_instance = make(map[*Other_direction]*Other_direction)
	for instance := range stage.Other_directions {
		_copy := instance.GongCopy().(*Other_direction)
		stage.Other_directions_reference[instance] = _copy
		stage.Other_directions_instance[_copy] = instance
		stage.Other_directions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_listenings_reference = make(map[*Other_listening]*Other_listening)
	stage.Other_listenings_referenceOrder = make(map[*Other_listening]uint) // diff Unstage needs the reference order
	stage.Other_listenings_instance = make(map[*Other_listening]*Other_listening)
	for instance := range stage.Other_listenings {
		_copy := instance.GongCopy().(*Other_listening)
		stage.Other_listenings_reference[instance] = _copy
		stage.Other_listenings_instance[_copy] = instance
		stage.Other_listenings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_notations_reference = make(map[*Other_notation]*Other_notation)
	stage.Other_notations_referenceOrder = make(map[*Other_notation]uint) // diff Unstage needs the reference order
	stage.Other_notations_instance = make(map[*Other_notation]*Other_notation)
	for instance := range stage.Other_notations {
		_copy := instance.GongCopy().(*Other_notation)
		stage.Other_notations_reference[instance] = _copy
		stage.Other_notations_instance[_copy] = instance
		stage.Other_notations_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_placement_texts_reference = make(map[*Other_placement_text]*Other_placement_text)
	stage.Other_placement_texts_referenceOrder = make(map[*Other_placement_text]uint) // diff Unstage needs the reference order
	stage.Other_placement_texts_instance = make(map[*Other_placement_text]*Other_placement_text)
	for instance := range stage.Other_placement_texts {
		_copy := instance.GongCopy().(*Other_placement_text)
		stage.Other_placement_texts_reference[instance] = _copy
		stage.Other_placement_texts_instance[_copy] = instance
		stage.Other_placement_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_plays_reference = make(map[*Other_play]*Other_play)
	stage.Other_plays_referenceOrder = make(map[*Other_play]uint) // diff Unstage needs the reference order
	stage.Other_plays_instance = make(map[*Other_play]*Other_play)
	for instance := range stage.Other_plays {
		_copy := instance.GongCopy().(*Other_play)
		stage.Other_plays_reference[instance] = _copy
		stage.Other_plays_instance[_copy] = instance
		stage.Other_plays_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Other_texts_reference = make(map[*Other_text]*Other_text)
	stage.Other_texts_referenceOrder = make(map[*Other_text]uint) // diff Unstage needs the reference order
	stage.Other_texts_instance = make(map[*Other_text]*Other_text)
	for instance := range stage.Other_texts {
		_copy := instance.GongCopy().(*Other_text)
		stage.Other_texts_reference[instance] = _copy
		stage.Other_texts_instance[_copy] = instance
		stage.Other_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Page_layouts_reference = make(map[*Page_layout]*Page_layout)
	stage.Page_layouts_referenceOrder = make(map[*Page_layout]uint) // diff Unstage needs the reference order
	stage.Page_layouts_instance = make(map[*Page_layout]*Page_layout)
	for instance := range stage.Page_layouts {
		_copy := instance.GongCopy().(*Page_layout)
		stage.Page_layouts_reference[instance] = _copy
		stage.Page_layouts_instance[_copy] = instance
		stage.Page_layouts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Page_marginss_reference = make(map[*Page_margins]*Page_margins)
	stage.Page_marginss_referenceOrder = make(map[*Page_margins]uint) // diff Unstage needs the reference order
	stage.Page_marginss_instance = make(map[*Page_margins]*Page_margins)
	for instance := range stage.Page_marginss {
		_copy := instance.GongCopy().(*Page_margins)
		stage.Page_marginss_reference[instance] = _copy
		stage.Page_marginss_instance[_copy] = instance
		stage.Page_marginss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_clefs_reference = make(map[*Part_clef]*Part_clef)
	stage.Part_clefs_referenceOrder = make(map[*Part_clef]uint) // diff Unstage needs the reference order
	stage.Part_clefs_instance = make(map[*Part_clef]*Part_clef)
	for instance := range stage.Part_clefs {
		_copy := instance.GongCopy().(*Part_clef)
		stage.Part_clefs_reference[instance] = _copy
		stage.Part_clefs_instance[_copy] = instance
		stage.Part_clefs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_groups_reference = make(map[*Part_group]*Part_group)
	stage.Part_groups_referenceOrder = make(map[*Part_group]uint) // diff Unstage needs the reference order
	stage.Part_groups_instance = make(map[*Part_group]*Part_group)
	for instance := range stage.Part_groups {
		_copy := instance.GongCopy().(*Part_group)
		stage.Part_groups_reference[instance] = _copy
		stage.Part_groups_instance[_copy] = instance
		stage.Part_groups_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_links_reference = make(map[*Part_link]*Part_link)
	stage.Part_links_referenceOrder = make(map[*Part_link]uint) // diff Unstage needs the reference order
	stage.Part_links_instance = make(map[*Part_link]*Part_link)
	for instance := range stage.Part_links {
		_copy := instance.GongCopy().(*Part_link)
		stage.Part_links_reference[instance] = _copy
		stage.Part_links_instance[_copy] = instance
		stage.Part_links_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_lists_reference = make(map[*Part_list]*Part_list)
	stage.Part_lists_referenceOrder = make(map[*Part_list]uint) // diff Unstage needs the reference order
	stage.Part_lists_instance = make(map[*Part_list]*Part_list)
	for instance := range stage.Part_lists {
		_copy := instance.GongCopy().(*Part_list)
		stage.Part_lists_reference[instance] = _copy
		stage.Part_lists_instance[_copy] = instance
		stage.Part_lists_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_names_reference = make(map[*Part_name]*Part_name)
	stage.Part_names_referenceOrder = make(map[*Part_name]uint) // diff Unstage needs the reference order
	stage.Part_names_instance = make(map[*Part_name]*Part_name)
	for instance := range stage.Part_names {
		_copy := instance.GongCopy().(*Part_name)
		stage.Part_names_reference[instance] = _copy
		stage.Part_names_instance[_copy] = instance
		stage.Part_names_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_symbols_reference = make(map[*Part_symbol]*Part_symbol)
	stage.Part_symbols_referenceOrder = make(map[*Part_symbol]uint) // diff Unstage needs the reference order
	stage.Part_symbols_instance = make(map[*Part_symbol]*Part_symbol)
	for instance := range stage.Part_symbols {
		_copy := instance.GongCopy().(*Part_symbol)
		stage.Part_symbols_reference[instance] = _copy
		stage.Part_symbols_instance[_copy] = instance
		stage.Part_symbols_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Part_transposes_reference = make(map[*Part_transpose]*Part_transpose)
	stage.Part_transposes_referenceOrder = make(map[*Part_transpose]uint) // diff Unstage needs the reference order
	stage.Part_transposes_instance = make(map[*Part_transpose]*Part_transpose)
	for instance := range stage.Part_transposes {
		_copy := instance.GongCopy().(*Part_transpose)
		stage.Part_transposes_reference[instance] = _copy
		stage.Part_transposes_instance[_copy] = instance
		stage.Part_transposes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Pedals_reference = make(map[*Pedal]*Pedal)
	stage.Pedals_referenceOrder = make(map[*Pedal]uint) // diff Unstage needs the reference order
	stage.Pedals_instance = make(map[*Pedal]*Pedal)
	for instance := range stage.Pedals {
		_copy := instance.GongCopy().(*Pedal)
		stage.Pedals_reference[instance] = _copy
		stage.Pedals_instance[_copy] = instance
		stage.Pedals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Pedal_tunings_reference = make(map[*Pedal_tuning]*Pedal_tuning)
	stage.Pedal_tunings_referenceOrder = make(map[*Pedal_tuning]uint) // diff Unstage needs the reference order
	stage.Pedal_tunings_instance = make(map[*Pedal_tuning]*Pedal_tuning)
	for instance := range stage.Pedal_tunings {
		_copy := instance.GongCopy().(*Pedal_tuning)
		stage.Pedal_tunings_reference[instance] = _copy
		stage.Pedal_tunings_instance[_copy] = instance
		stage.Pedal_tunings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Per_minutes_reference = make(map[*Per_minute]*Per_minute)
	stage.Per_minutes_referenceOrder = make(map[*Per_minute]uint) // diff Unstage needs the reference order
	stage.Per_minutes_instance = make(map[*Per_minute]*Per_minute)
	for instance := range stage.Per_minutes {
		_copy := instance.GongCopy().(*Per_minute)
		stage.Per_minutes_reference[instance] = _copy
		stage.Per_minutes_instance[_copy] = instance
		stage.Per_minutes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Percussions_reference = make(map[*Percussion]*Percussion)
	stage.Percussions_referenceOrder = make(map[*Percussion]uint) // diff Unstage needs the reference order
	stage.Percussions_instance = make(map[*Percussion]*Percussion)
	for instance := range stage.Percussions {
		_copy := instance.GongCopy().(*Percussion)
		stage.Percussions_reference[instance] = _copy
		stage.Percussions_instance[_copy] = instance
		stage.Percussions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Pitchs_reference = make(map[*Pitch]*Pitch)
	stage.Pitchs_referenceOrder = make(map[*Pitch]uint) // diff Unstage needs the reference order
	stage.Pitchs_instance = make(map[*Pitch]*Pitch)
	for instance := range stage.Pitchs {
		_copy := instance.GongCopy().(*Pitch)
		stage.Pitchs_reference[instance] = _copy
		stage.Pitchs_instance[_copy] = instance
		stage.Pitchs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Pitcheds_reference = make(map[*Pitched]*Pitched)
	stage.Pitcheds_referenceOrder = make(map[*Pitched]uint) // diff Unstage needs the reference order
	stage.Pitcheds_instance = make(map[*Pitched]*Pitched)
	for instance := range stage.Pitcheds {
		_copy := instance.GongCopy().(*Pitched)
		stage.Pitcheds_reference[instance] = _copy
		stage.Pitcheds_instance[_copy] = instance
		stage.Pitcheds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Placement_texts_reference = make(map[*Placement_text]*Placement_text)
	stage.Placement_texts_referenceOrder = make(map[*Placement_text]uint) // diff Unstage needs the reference order
	stage.Placement_texts_instance = make(map[*Placement_text]*Placement_text)
	for instance := range stage.Placement_texts {
		_copy := instance.GongCopy().(*Placement_text)
		stage.Placement_texts_reference[instance] = _copy
		stage.Placement_texts_instance[_copy] = instance
		stage.Placement_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Plays_reference = make(map[*Play]*Play)
	stage.Plays_referenceOrder = make(map[*Play]uint) // diff Unstage needs the reference order
	stage.Plays_instance = make(map[*Play]*Play)
	for instance := range stage.Plays {
		_copy := instance.GongCopy().(*Play)
		stage.Plays_reference[instance] = _copy
		stage.Plays_instance[_copy] = instance
		stage.Plays_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Players_reference = make(map[*Player]*Player)
	stage.Players_referenceOrder = make(map[*Player]uint) // diff Unstage needs the reference order
	stage.Players_instance = make(map[*Player]*Player)
	for instance := range stage.Players {
		_copy := instance.GongCopy().(*Player)
		stage.Players_reference[instance] = _copy
		stage.Players_instance[_copy] = instance
		stage.Players_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Principal_voices_reference = make(map[*Principal_voice]*Principal_voice)
	stage.Principal_voices_referenceOrder = make(map[*Principal_voice]uint) // diff Unstage needs the reference order
	stage.Principal_voices_instance = make(map[*Principal_voice]*Principal_voice)
	for instance := range stage.Principal_voices {
		_copy := instance.GongCopy().(*Principal_voice)
		stage.Principal_voices_reference[instance] = _copy
		stage.Principal_voices_instance[_copy] = instance
		stage.Principal_voices_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Prints_reference = make(map[*Print]*Print)
	stage.Prints_referenceOrder = make(map[*Print]uint) // diff Unstage needs the reference order
	stage.Prints_instance = make(map[*Print]*Print)
	for instance := range stage.Prints {
		_copy := instance.GongCopy().(*Print)
		stage.Prints_reference[instance] = _copy
		stage.Prints_instance[_copy] = instance
		stage.Prints_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Releases_reference = make(map[*Release]*Release)
	stage.Releases_referenceOrder = make(map[*Release]uint) // diff Unstage needs the reference order
	stage.Releases_instance = make(map[*Release]*Release)
	for instance := range stage.Releases {
		_copy := instance.GongCopy().(*Release)
		stage.Releases_reference[instance] = _copy
		stage.Releases_instance[_copy] = instance
		stage.Releases_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Repeats_reference = make(map[*Repeat]*Repeat)
	stage.Repeats_referenceOrder = make(map[*Repeat]uint) // diff Unstage needs the reference order
	stage.Repeats_instance = make(map[*Repeat]*Repeat)
	for instance := range stage.Repeats {
		_copy := instance.GongCopy().(*Repeat)
		stage.Repeats_reference[instance] = _copy
		stage.Repeats_instance[_copy] = instance
		stage.Repeats_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Rests_reference = make(map[*Rest]*Rest)
	stage.Rests_referenceOrder = make(map[*Rest]uint) // diff Unstage needs the reference order
	stage.Rests_instance = make(map[*Rest]*Rest)
	for instance := range stage.Rests {
		_copy := instance.GongCopy().(*Rest)
		stage.Rests_reference[instance] = _copy
		stage.Rests_instance[_copy] = instance
		stage.Rests_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Roots_reference = make(map[*Root]*Root)
	stage.Roots_referenceOrder = make(map[*Root]uint) // diff Unstage needs the reference order
	stage.Roots_instance = make(map[*Root]*Root)
	for instance := range stage.Roots {
		_copy := instance.GongCopy().(*Root)
		stage.Roots_reference[instance] = _copy
		stage.Roots_instance[_copy] = instance
		stage.Roots_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Root_steps_reference = make(map[*Root_step]*Root_step)
	stage.Root_steps_referenceOrder = make(map[*Root_step]uint) // diff Unstage needs the reference order
	stage.Root_steps_instance = make(map[*Root_step]*Root_step)
	for instance := range stage.Root_steps {
		_copy := instance.GongCopy().(*Root_step)
		stage.Root_steps_reference[instance] = _copy
		stage.Root_steps_instance[_copy] = instance
		stage.Root_steps_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Scalings_reference = make(map[*Scaling]*Scaling)
	stage.Scalings_referenceOrder = make(map[*Scaling]uint) // diff Unstage needs the reference order
	stage.Scalings_instance = make(map[*Scaling]*Scaling)
	for instance := range stage.Scalings {
		_copy := instance.GongCopy().(*Scaling)
		stage.Scalings_reference[instance] = _copy
		stage.Scalings_instance[_copy] = instance
		stage.Scalings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Scordaturas_reference = make(map[*Scordatura]*Scordatura)
	stage.Scordaturas_referenceOrder = make(map[*Scordatura]uint) // diff Unstage needs the reference order
	stage.Scordaturas_instance = make(map[*Scordatura]*Scordatura)
	for instance := range stage.Scordaturas {
		_copy := instance.GongCopy().(*Scordatura)
		stage.Scordaturas_reference[instance] = _copy
		stage.Scordaturas_instance[_copy] = instance
		stage.Scordaturas_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Score_instruments_reference = make(map[*Score_instrument]*Score_instrument)
	stage.Score_instruments_referenceOrder = make(map[*Score_instrument]uint) // diff Unstage needs the reference order
	stage.Score_instruments_instance = make(map[*Score_instrument]*Score_instrument)
	for instance := range stage.Score_instruments {
		_copy := instance.GongCopy().(*Score_instrument)
		stage.Score_instruments_reference[instance] = _copy
		stage.Score_instruments_instance[_copy] = instance
		stage.Score_instruments_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Score_parts_reference = make(map[*Score_part]*Score_part)
	stage.Score_parts_referenceOrder = make(map[*Score_part]uint) // diff Unstage needs the reference order
	stage.Score_parts_instance = make(map[*Score_part]*Score_part)
	for instance := range stage.Score_parts {
		_copy := instance.GongCopy().(*Score_part)
		stage.Score_parts_reference[instance] = _copy
		stage.Score_parts_instance[_copy] = instance
		stage.Score_parts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Score_partwises_reference = make(map[*Score_partwise]*Score_partwise)
	stage.Score_partwises_referenceOrder = make(map[*Score_partwise]uint) // diff Unstage needs the reference order
	stage.Score_partwises_instance = make(map[*Score_partwise]*Score_partwise)
	for instance := range stage.Score_partwises {
		_copy := instance.GongCopy().(*Score_partwise)
		stage.Score_partwises_reference[instance] = _copy
		stage.Score_partwises_instance[_copy] = instance
		stage.Score_partwises_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Score_timewises_reference = make(map[*Score_timewise]*Score_timewise)
	stage.Score_timewises_referenceOrder = make(map[*Score_timewise]uint) // diff Unstage needs the reference order
	stage.Score_timewises_instance = make(map[*Score_timewise]*Score_timewise)
	for instance := range stage.Score_timewises {
		_copy := instance.GongCopy().(*Score_timewise)
		stage.Score_timewises_reference[instance] = _copy
		stage.Score_timewises_instance[_copy] = instance
		stage.Score_timewises_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Segnos_reference = make(map[*Segno]*Segno)
	stage.Segnos_referenceOrder = make(map[*Segno]uint) // diff Unstage needs the reference order
	stage.Segnos_instance = make(map[*Segno]*Segno)
	for instance := range stage.Segnos {
		_copy := instance.GongCopy().(*Segno)
		stage.Segnos_reference[instance] = _copy
		stage.Segnos_instance[_copy] = instance
		stage.Segnos_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Slashs_reference = make(map[*Slash]*Slash)
	stage.Slashs_referenceOrder = make(map[*Slash]uint) // diff Unstage needs the reference order
	stage.Slashs_instance = make(map[*Slash]*Slash)
	for instance := range stage.Slashs {
		_copy := instance.GongCopy().(*Slash)
		stage.Slashs_reference[instance] = _copy
		stage.Slashs_instance[_copy] = instance
		stage.Slashs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Slides_reference = make(map[*Slide]*Slide)
	stage.Slides_referenceOrder = make(map[*Slide]uint) // diff Unstage needs the reference order
	stage.Slides_instance = make(map[*Slide]*Slide)
	for instance := range stage.Slides {
		_copy := instance.GongCopy().(*Slide)
		stage.Slides_reference[instance] = _copy
		stage.Slides_instance[_copy] = instance
		stage.Slides_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Slurs_reference = make(map[*Slur]*Slur)
	stage.Slurs_referenceOrder = make(map[*Slur]uint) // diff Unstage needs the reference order
	stage.Slurs_instance = make(map[*Slur]*Slur)
	for instance := range stage.Slurs {
		_copy := instance.GongCopy().(*Slur)
		stage.Slurs_reference[instance] = _copy
		stage.Slurs_instance[_copy] = instance
		stage.Slurs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Sounds_reference = make(map[*Sound]*Sound)
	stage.Sounds_referenceOrder = make(map[*Sound]uint) // diff Unstage needs the reference order
	stage.Sounds_instance = make(map[*Sound]*Sound)
	for instance := range stage.Sounds {
		_copy := instance.GongCopy().(*Sound)
		stage.Sounds_reference[instance] = _copy
		stage.Sounds_instance[_copy] = instance
		stage.Sounds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Staff_detailss_reference = make(map[*Staff_details]*Staff_details)
	stage.Staff_detailss_referenceOrder = make(map[*Staff_details]uint) // diff Unstage needs the reference order
	stage.Staff_detailss_instance = make(map[*Staff_details]*Staff_details)
	for instance := range stage.Staff_detailss {
		_copy := instance.GongCopy().(*Staff_details)
		stage.Staff_detailss_reference[instance] = _copy
		stage.Staff_detailss_instance[_copy] = instance
		stage.Staff_detailss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Staff_divides_reference = make(map[*Staff_divide]*Staff_divide)
	stage.Staff_divides_referenceOrder = make(map[*Staff_divide]uint) // diff Unstage needs the reference order
	stage.Staff_divides_instance = make(map[*Staff_divide]*Staff_divide)
	for instance := range stage.Staff_divides {
		_copy := instance.GongCopy().(*Staff_divide)
		stage.Staff_divides_reference[instance] = _copy
		stage.Staff_divides_instance[_copy] = instance
		stage.Staff_divides_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Staff_layouts_reference = make(map[*Staff_layout]*Staff_layout)
	stage.Staff_layouts_referenceOrder = make(map[*Staff_layout]uint) // diff Unstage needs the reference order
	stage.Staff_layouts_instance = make(map[*Staff_layout]*Staff_layout)
	for instance := range stage.Staff_layouts {
		_copy := instance.GongCopy().(*Staff_layout)
		stage.Staff_layouts_reference[instance] = _copy
		stage.Staff_layouts_instance[_copy] = instance
		stage.Staff_layouts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Staff_sizes_reference = make(map[*Staff_size]*Staff_size)
	stage.Staff_sizes_referenceOrder = make(map[*Staff_size]uint) // diff Unstage needs the reference order
	stage.Staff_sizes_instance = make(map[*Staff_size]*Staff_size)
	for instance := range stage.Staff_sizes {
		_copy := instance.GongCopy().(*Staff_size)
		stage.Staff_sizes_reference[instance] = _copy
		stage.Staff_sizes_instance[_copy] = instance
		stage.Staff_sizes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Staff_tunings_reference = make(map[*Staff_tuning]*Staff_tuning)
	stage.Staff_tunings_referenceOrder = make(map[*Staff_tuning]uint) // diff Unstage needs the reference order
	stage.Staff_tunings_instance = make(map[*Staff_tuning]*Staff_tuning)
	for instance := range stage.Staff_tunings {
		_copy := instance.GongCopy().(*Staff_tuning)
		stage.Staff_tunings_reference[instance] = _copy
		stage.Staff_tunings_instance[_copy] = instance
		stage.Staff_tunings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Stems_reference = make(map[*Stem]*Stem)
	stage.Stems_referenceOrder = make(map[*Stem]uint) // diff Unstage needs the reference order
	stage.Stems_instance = make(map[*Stem]*Stem)
	for instance := range stage.Stems {
		_copy := instance.GongCopy().(*Stem)
		stage.Stems_reference[instance] = _copy
		stage.Stems_instance[_copy] = instance
		stage.Stems_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Sticks_reference = make(map[*Stick]*Stick)
	stage.Sticks_referenceOrder = make(map[*Stick]uint) // diff Unstage needs the reference order
	stage.Sticks_instance = make(map[*Stick]*Stick)
	for instance := range stage.Sticks {
		_copy := instance.GongCopy().(*Stick)
		stage.Sticks_reference[instance] = _copy
		stage.Sticks_instance[_copy] = instance
		stage.Sticks_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.String_mutes_reference = make(map[*String_mute]*String_mute)
	stage.String_mutes_referenceOrder = make(map[*String_mute]uint) // diff Unstage needs the reference order
	stage.String_mutes_instance = make(map[*String_mute]*String_mute)
	for instance := range stage.String_mutes {
		_copy := instance.GongCopy().(*String_mute)
		stage.String_mutes_reference[instance] = _copy
		stage.String_mutes_instance[_copy] = instance
		stage.String_mutes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.String_types_reference = make(map[*String_type]*String_type)
	stage.String_types_referenceOrder = make(map[*String_type]uint) // diff Unstage needs the reference order
	stage.String_types_instance = make(map[*String_type]*String_type)
	for instance := range stage.String_types {
		_copy := instance.GongCopy().(*String_type)
		stage.String_types_reference[instance] = _copy
		stage.String_types_instance[_copy] = instance
		stage.String_types_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Strong_accents_reference = make(map[*Strong_accent]*Strong_accent)
	stage.Strong_accents_referenceOrder = make(map[*Strong_accent]uint) // diff Unstage needs the reference order
	stage.Strong_accents_instance = make(map[*Strong_accent]*Strong_accent)
	for instance := range stage.Strong_accents {
		_copy := instance.GongCopy().(*Strong_accent)
		stage.Strong_accents_reference[instance] = _copy
		stage.Strong_accents_instance[_copy] = instance
		stage.Strong_accents_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Style_texts_reference = make(map[*Style_text]*Style_text)
	stage.Style_texts_referenceOrder = make(map[*Style_text]uint) // diff Unstage needs the reference order
	stage.Style_texts_instance = make(map[*Style_text]*Style_text)
	for instance := range stage.Style_texts {
		_copy := instance.GongCopy().(*Style_text)
		stage.Style_texts_reference[instance] = _copy
		stage.Style_texts_instance[_copy] = instance
		stage.Style_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Supportss_reference = make(map[*Supports]*Supports)
	stage.Supportss_referenceOrder = make(map[*Supports]uint) // diff Unstage needs the reference order
	stage.Supportss_instance = make(map[*Supports]*Supports)
	for instance := range stage.Supportss {
		_copy := instance.GongCopy().(*Supports)
		stage.Supportss_reference[instance] = _copy
		stage.Supportss_instance[_copy] = instance
		stage.Supportss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Swings_reference = make(map[*Swing]*Swing)
	stage.Swings_referenceOrder = make(map[*Swing]uint) // diff Unstage needs the reference order
	stage.Swings_instance = make(map[*Swing]*Swing)
	for instance := range stage.Swings {
		_copy := instance.GongCopy().(*Swing)
		stage.Swings_reference[instance] = _copy
		stage.Swings_instance[_copy] = instance
		stage.Swings_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Syncs_reference = make(map[*Sync]*Sync)
	stage.Syncs_referenceOrder = make(map[*Sync]uint) // diff Unstage needs the reference order
	stage.Syncs_instance = make(map[*Sync]*Sync)
	for instance := range stage.Syncs {
		_copy := instance.GongCopy().(*Sync)
		stage.Syncs_reference[instance] = _copy
		stage.Syncs_instance[_copy] = instance
		stage.Syncs_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.System_dividerss_reference = make(map[*System_dividers]*System_dividers)
	stage.System_dividerss_referenceOrder = make(map[*System_dividers]uint) // diff Unstage needs the reference order
	stage.System_dividerss_instance = make(map[*System_dividers]*System_dividers)
	for instance := range stage.System_dividerss {
		_copy := instance.GongCopy().(*System_dividers)
		stage.System_dividerss_reference[instance] = _copy
		stage.System_dividerss_instance[_copy] = instance
		stage.System_dividerss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.System_layouts_reference = make(map[*System_layout]*System_layout)
	stage.System_layouts_referenceOrder = make(map[*System_layout]uint) // diff Unstage needs the reference order
	stage.System_layouts_instance = make(map[*System_layout]*System_layout)
	for instance := range stage.System_layouts {
		_copy := instance.GongCopy().(*System_layout)
		stage.System_layouts_reference[instance] = _copy
		stage.System_layouts_instance[_copy] = instance
		stage.System_layouts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.System_marginss_reference = make(map[*System_margins]*System_margins)
	stage.System_marginss_referenceOrder = make(map[*System_margins]uint) // diff Unstage needs the reference order
	stage.System_marginss_instance = make(map[*System_margins]*System_margins)
	for instance := range stage.System_marginss {
		_copy := instance.GongCopy().(*System_margins)
		stage.System_marginss_reference[instance] = _copy
		stage.System_marginss_instance[_copy] = instance
		stage.System_marginss_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Taps_reference = make(map[*Tap]*Tap)
	stage.Taps_referenceOrder = make(map[*Tap]uint) // diff Unstage needs the reference order
	stage.Taps_instance = make(map[*Tap]*Tap)
	for instance := range stage.Taps {
		_copy := instance.GongCopy().(*Tap)
		stage.Taps_reference[instance] = _copy
		stage.Taps_instance[_copy] = instance
		stage.Taps_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Technicals_reference = make(map[*Technical]*Technical)
	stage.Technicals_referenceOrder = make(map[*Technical]uint) // diff Unstage needs the reference order
	stage.Technicals_instance = make(map[*Technical]*Technical)
	for instance := range stage.Technicals {
		_copy := instance.GongCopy().(*Technical)
		stage.Technicals_reference[instance] = _copy
		stage.Technicals_instance[_copy] = instance
		stage.Technicals_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Text_element_datas_reference = make(map[*Text_element_data]*Text_element_data)
	stage.Text_element_datas_referenceOrder = make(map[*Text_element_data]uint) // diff Unstage needs the reference order
	stage.Text_element_datas_instance = make(map[*Text_element_data]*Text_element_data)
	for instance := range stage.Text_element_datas {
		_copy := instance.GongCopy().(*Text_element_data)
		stage.Text_element_datas_reference[instance] = _copy
		stage.Text_element_datas_instance[_copy] = instance
		stage.Text_element_datas_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Ties_reference = make(map[*Tie]*Tie)
	stage.Ties_referenceOrder = make(map[*Tie]uint) // diff Unstage needs the reference order
	stage.Ties_instance = make(map[*Tie]*Tie)
	for instance := range stage.Ties {
		_copy := instance.GongCopy().(*Tie)
		stage.Ties_reference[instance] = _copy
		stage.Ties_instance[_copy] = instance
		stage.Ties_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tieds_reference = make(map[*Tied]*Tied)
	stage.Tieds_referenceOrder = make(map[*Tied]uint) // diff Unstage needs the reference order
	stage.Tieds_instance = make(map[*Tied]*Tied)
	for instance := range stage.Tieds {
		_copy := instance.GongCopy().(*Tied)
		stage.Tieds_reference[instance] = _copy
		stage.Tieds_instance[_copy] = instance
		stage.Tieds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Times_reference = make(map[*Time]*Time)
	stage.Times_referenceOrder = make(map[*Time]uint) // diff Unstage needs the reference order
	stage.Times_instance = make(map[*Time]*Time)
	for instance := range stage.Times {
		_copy := instance.GongCopy().(*Time)
		stage.Times_reference[instance] = _copy
		stage.Times_instance[_copy] = instance
		stage.Times_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Time_modifications_reference = make(map[*Time_modification]*Time_modification)
	stage.Time_modifications_referenceOrder = make(map[*Time_modification]uint) // diff Unstage needs the reference order
	stage.Time_modifications_instance = make(map[*Time_modification]*Time_modification)
	for instance := range stage.Time_modifications {
		_copy := instance.GongCopy().(*Time_modification)
		stage.Time_modifications_reference[instance] = _copy
		stage.Time_modifications_instance[_copy] = instance
		stage.Time_modifications_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Timpanis_reference = make(map[*Timpani]*Timpani)
	stage.Timpanis_referenceOrder = make(map[*Timpani]uint) // diff Unstage needs the reference order
	stage.Timpanis_instance = make(map[*Timpani]*Timpani)
	for instance := range stage.Timpanis {
		_copy := instance.GongCopy().(*Timpani)
		stage.Timpanis_reference[instance] = _copy
		stage.Timpanis_instance[_copy] = instance
		stage.Timpanis_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Transposes_reference = make(map[*Transpose]*Transpose)
	stage.Transposes_referenceOrder = make(map[*Transpose]uint) // diff Unstage needs the reference order
	stage.Transposes_instance = make(map[*Transpose]*Transpose)
	for instance := range stage.Transposes {
		_copy := instance.GongCopy().(*Transpose)
		stage.Transposes_reference[instance] = _copy
		stage.Transposes_instance[_copy] = instance
		stage.Transposes_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tremolos_reference = make(map[*Tremolo]*Tremolo)
	stage.Tremolos_referenceOrder = make(map[*Tremolo]uint) // diff Unstage needs the reference order
	stage.Tremolos_instance = make(map[*Tremolo]*Tremolo)
	for instance := range stage.Tremolos {
		_copy := instance.GongCopy().(*Tremolo)
		stage.Tremolos_reference[instance] = _copy
		stage.Tremolos_instance[_copy] = instance
		stage.Tremolos_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tuplets_reference = make(map[*Tuplet]*Tuplet)
	stage.Tuplets_referenceOrder = make(map[*Tuplet]uint) // diff Unstage needs the reference order
	stage.Tuplets_instance = make(map[*Tuplet]*Tuplet)
	for instance := range stage.Tuplets {
		_copy := instance.GongCopy().(*Tuplet)
		stage.Tuplets_reference[instance] = _copy
		stage.Tuplets_instance[_copy] = instance
		stage.Tuplets_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tuplet_dots_reference = make(map[*Tuplet_dot]*Tuplet_dot)
	stage.Tuplet_dots_referenceOrder = make(map[*Tuplet_dot]uint) // diff Unstage needs the reference order
	stage.Tuplet_dots_instance = make(map[*Tuplet_dot]*Tuplet_dot)
	for instance := range stage.Tuplet_dots {
		_copy := instance.GongCopy().(*Tuplet_dot)
		stage.Tuplet_dots_reference[instance] = _copy
		stage.Tuplet_dots_instance[_copy] = instance
		stage.Tuplet_dots_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tuplet_numbers_reference = make(map[*Tuplet_number]*Tuplet_number)
	stage.Tuplet_numbers_referenceOrder = make(map[*Tuplet_number]uint) // diff Unstage needs the reference order
	stage.Tuplet_numbers_instance = make(map[*Tuplet_number]*Tuplet_number)
	for instance := range stage.Tuplet_numbers {
		_copy := instance.GongCopy().(*Tuplet_number)
		stage.Tuplet_numbers_reference[instance] = _copy
		stage.Tuplet_numbers_instance[_copy] = instance
		stage.Tuplet_numbers_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tuplet_portions_reference = make(map[*Tuplet_portion]*Tuplet_portion)
	stage.Tuplet_portions_referenceOrder = make(map[*Tuplet_portion]uint) // diff Unstage needs the reference order
	stage.Tuplet_portions_instance = make(map[*Tuplet_portion]*Tuplet_portion)
	for instance := range stage.Tuplet_portions {
		_copy := instance.GongCopy().(*Tuplet_portion)
		stage.Tuplet_portions_reference[instance] = _copy
		stage.Tuplet_portions_instance[_copy] = instance
		stage.Tuplet_portions_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Tuplet_types_reference = make(map[*Tuplet_type]*Tuplet_type)
	stage.Tuplet_types_referenceOrder = make(map[*Tuplet_type]uint) // diff Unstage needs the reference order
	stage.Tuplet_types_instance = make(map[*Tuplet_type]*Tuplet_type)
	for instance := range stage.Tuplet_types {
		_copy := instance.GongCopy().(*Tuplet_type)
		stage.Tuplet_types_reference[instance] = _copy
		stage.Tuplet_types_instance[_copy] = instance
		stage.Tuplet_types_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Typed_texts_reference = make(map[*Typed_text]*Typed_text)
	stage.Typed_texts_referenceOrder = make(map[*Typed_text]uint) // diff Unstage needs the reference order
	stage.Typed_texts_instance = make(map[*Typed_text]*Typed_text)
	for instance := range stage.Typed_texts {
		_copy := instance.GongCopy().(*Typed_text)
		stage.Typed_texts_reference[instance] = _copy
		stage.Typed_texts_instance[_copy] = instance
		stage.Typed_texts_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Unpitcheds_reference = make(map[*Unpitched]*Unpitched)
	stage.Unpitcheds_referenceOrder = make(map[*Unpitched]uint) // diff Unstage needs the reference order
	stage.Unpitcheds_instance = make(map[*Unpitched]*Unpitched)
	for instance := range stage.Unpitcheds {
		_copy := instance.GongCopy().(*Unpitched)
		stage.Unpitcheds_reference[instance] = _copy
		stage.Unpitcheds_instance[_copy] = instance
		stage.Unpitcheds_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Virtual_instruments_reference = make(map[*Virtual_instrument]*Virtual_instrument)
	stage.Virtual_instruments_referenceOrder = make(map[*Virtual_instrument]uint) // diff Unstage needs the reference order
	stage.Virtual_instruments_instance = make(map[*Virtual_instrument]*Virtual_instrument)
	for instance := range stage.Virtual_instruments {
		_copy := instance.GongCopy().(*Virtual_instrument)
		stage.Virtual_instruments_reference[instance] = _copy
		stage.Virtual_instruments_instance[_copy] = instance
		stage.Virtual_instruments_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Waits_reference = make(map[*Wait]*Wait)
	stage.Waits_referenceOrder = make(map[*Wait]uint) // diff Unstage needs the reference order
	stage.Waits_instance = make(map[*Wait]*Wait)
	for instance := range stage.Waits {
		_copy := instance.GongCopy().(*Wait)
		stage.Waits_reference[instance] = _copy
		stage.Waits_instance[_copy] = instance
		stage.Waits_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Wavy_lines_reference = make(map[*Wavy_line]*Wavy_line)
	stage.Wavy_lines_referenceOrder = make(map[*Wavy_line]uint) // diff Unstage needs the reference order
	stage.Wavy_lines_instance = make(map[*Wavy_line]*Wavy_line)
	for instance := range stage.Wavy_lines {
		_copy := instance.GongCopy().(*Wavy_line)
		stage.Wavy_lines_reference[instance] = _copy
		stage.Wavy_lines_instance[_copy] = instance
		stage.Wavy_lines_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Wedges_reference = make(map[*Wedge]*Wedge)
	stage.Wedges_referenceOrder = make(map[*Wedge]uint) // diff Unstage needs the reference order
	stage.Wedges_instance = make(map[*Wedge]*Wedge)
	for instance := range stage.Wedges {
		_copy := instance.GongCopy().(*Wedge)
		stage.Wedges_reference[instance] = _copy
		stage.Wedges_instance[_copy] = instance
		stage.Wedges_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Woods_reference = make(map[*Wood]*Wood)
	stage.Woods_referenceOrder = make(map[*Wood]uint) // diff Unstage needs the reference order
	stage.Woods_instance = make(map[*Wood]*Wood)
	for instance := range stage.Woods {
		_copy := instance.GongCopy().(*Wood)
		stage.Woods_reference[instance] = _copy
		stage.Woods_instance[_copy] = instance
		stage.Woods_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	stage.Works_reference = make(map[*Work]*Work)
	stage.Works_referenceOrder = make(map[*Work]uint) // diff Unstage needs the reference order
	stage.Works_instance = make(map[*Work]*Work)
	for instance := range stage.Works {
		_copy := instance.GongCopy().(*Work)
		stage.Works_reference[instance] = _copy
		stage.Works_instance[_copy] = instance
		stage.Works_referenceOrder[_copy] = instance.GongGetOrder(stage)
	}

	// insertion point per named struct
	for instance := range stage.A_directives {
		reference := stage.A_directives_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.A_measures {
		reference := stage.A_measures_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.A_measure_1s {
		reference := stage.A_measure_1s_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.A_parts {
		reference := stage.A_parts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.A_part_1s {
		reference := stage.A_part_1s_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Accidentals {
		reference := stage.Accidentals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Accidental_marks {
		reference := stage.Accidental_marks_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Accidental_texts {
		reference := stage.Accidental_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Accords {
		reference := stage.Accords_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Accordion_registrations {
		reference := stage.Accordion_registrations_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Appearances {
		reference := stage.Appearances_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Arpeggiates {
		reference := stage.Arpeggiates_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Arrows {
		reference := stage.Arrows_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Articulationss {
		reference := stage.Articulationss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Assesss {
		reference := stage.Assesss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Attributess {
		reference := stage.Attributess_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Backups {
		reference := stage.Backups_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Bar_style_colors {
		reference := stage.Bar_style_colors_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Barlines {
		reference := stage.Barlines_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Barres {
		reference := stage.Barres_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Basss {
		reference := stage.Basss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Bass_steps {
		reference := stage.Bass_steps_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Beams {
		reference := stage.Beams_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Beat_repeats {
		reference := stage.Beat_repeats_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Beat_unit_tieds {
		reference := stage.Beat_unit_tieds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Beaters {
		reference := stage.Beaters_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Bends {
		reference := stage.Bends_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Bookmarks {
		reference := stage.Bookmarks_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Brackets {
		reference := stage.Brackets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Breath_marks {
		reference := stage.Breath_marks_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Caesuras {
		reference := stage.Caesuras_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Cancels {
		reference := stage.Cancels_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Clefs {
		reference := stage.Clefs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Codas {
		reference := stage.Codas_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Credits {
		reference := stage.Credits_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Dashess {
		reference := stage.Dashess_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Defaultss {
		reference := stage.Defaultss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Degrees {
		reference := stage.Degrees_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Degree_alters {
		reference := stage.Degree_alters_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Degree_types {
		reference := stage.Degree_types_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Degree_values {
		reference := stage.Degree_values_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Directions {
		reference := stage.Directions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Direction_types {
		reference := stage.Direction_types_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Distances {
		reference := stage.Distances_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Doubles {
		reference := stage.Doubles_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Dynamicss {
		reference := stage.Dynamicss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Effects {
		reference := stage.Effects_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Elisions {
		reference := stage.Elisions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Emptys {
		reference := stage.Emptys_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_fonts {
		reference := stage.Empty_fonts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_lines {
		reference := stage.Empty_lines_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_placements {
		reference := stage.Empty_placements_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_placement_smufls {
		reference := stage.Empty_placement_smufls_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_print_object_style_aligns {
		reference := stage.Empty_print_object_style_aligns_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_print_styles {
		reference := stage.Empty_print_styles_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_print_style_aligns {
		reference := stage.Empty_print_style_aligns_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_print_style_align_ids {
		reference := stage.Empty_print_style_align_ids_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Empty_trill_sounds {
		reference := stage.Empty_trill_sounds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Encodings {
		reference := stage.Encodings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Endings {
		reference := stage.Endings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Extends {
		reference := stage.Extends_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Features {
		reference := stage.Features_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Fermatas {
		reference := stage.Fermatas_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Figures {
		reference := stage.Figures_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Figured_basss {
		reference := stage.Figured_basss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Fingerings {
		reference := stage.Fingerings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.First_frets {
		reference := stage.First_frets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.For_parts {
		reference := stage.For_parts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Formatted_symbols {
		reference := stage.Formatted_symbols_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Formatted_symbol_ids {
		reference := stage.Formatted_symbol_ids_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Formatted_texts {
		reference := stage.Formatted_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Formatted_text_ids {
		reference := stage.Formatted_text_ids_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Forwards {
		reference := stage.Forwards_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Frames {
		reference := stage.Frames_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Frame_notes {
		reference := stage.Frame_notes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Frets {
		reference := stage.Frets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Glasss {
		reference := stage.Glasss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Glissandos {
		reference := stage.Glissandos_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Glyphs {
		reference := stage.Glyphs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Graces {
		reference := stage.Graces_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Group_barlines {
		reference := stage.Group_barlines_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Group_names {
		reference := stage.Group_names_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Group_symbols {
		reference := stage.Group_symbols_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Groupings {
		reference := stage.Groupings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Hammer_on_pull_offs {
		reference := stage.Hammer_on_pull_offs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Handbells {
		reference := stage.Handbells_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harmon_closeds {
		reference := stage.Harmon_closeds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harmon_mutes {
		reference := stage.Harmon_mutes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harmonics {
		reference := stage.Harmonics_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harmonys {
		reference := stage.Harmonys_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harmony_alters {
		reference := stage.Harmony_alters_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Harp_pedalss {
		reference := stage.Harp_pedalss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Heel_toes {
		reference := stage.Heel_toes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Holes {
		reference := stage.Holes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Hole_closeds {
		reference := stage.Hole_closeds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Horizontal_turns {
		reference := stage.Horizontal_turns_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Identifications {
		reference := stage.Identifications_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Images {
		reference := stage.Images_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Instruments {
		reference := stage.Instruments_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Instrument_changes {
		reference := stage.Instrument_changes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Instrument_links {
		reference := stage.Instrument_links_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Interchangeables {
		reference := stage.Interchangeables_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Inversions {
		reference := stage.Inversions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Keys {
		reference := stage.Keys_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Key_accidentals {
		reference := stage.Key_accidentals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Key_octaves {
		reference := stage.Key_octaves_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Kinds {
		reference := stage.Kinds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Levels {
		reference := stage.Levels_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Line_details {
		reference := stage.Line_details_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Line_widths {
		reference := stage.Line_widths_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Links {
		reference := stage.Links_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Listens {
		reference := stage.Listens_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Listenings {
		reference := stage.Listenings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Lyrics {
		reference := stage.Lyrics_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Lyric_fonts {
		reference := stage.Lyric_fonts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Lyric_languages {
		reference := stage.Lyric_languages_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Measure_layouts {
		reference := stage.Measure_layouts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Measure_numberings {
		reference := stage.Measure_numberings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Measure_repeats {
		reference := stage.Measure_repeats_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Measure_styles {
		reference := stage.Measure_styles_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Membranes {
		reference := stage.Membranes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metals {
		reference := stage.Metals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metronomes {
		reference := stage.Metronomes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metronome_beams {
		reference := stage.Metronome_beams_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metronome_notes {
		reference := stage.Metronome_notes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metronome_tieds {
		reference := stage.Metronome_tieds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Metronome_tuplets {
		reference := stage.Metronome_tuplets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Midi_devices {
		reference := stage.Midi_devices_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Midi_instruments {
		reference := stage.Midi_instruments_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Miscellaneouss {
		reference := stage.Miscellaneouss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Miscellaneous_fields {
		reference := stage.Miscellaneous_fields_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Mordents {
		reference := stage.Mordents_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Multiple_rests {
		reference := stage.Multiple_rests_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Name_displays {
		reference := stage.Name_displays_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Non_arpeggiates {
		reference := stage.Non_arpeggiates_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Notationss {
		reference := stage.Notationss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Notes {
		reference := stage.Notes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Note_sizes {
		reference := stage.Note_sizes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Note_types {
		reference := stage.Note_types_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Noteheads {
		reference := stage.Noteheads_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Notehead_texts {
		reference := stage.Notehead_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Numerals {
		reference := stage.Numerals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Numeral_keys {
		reference := stage.Numeral_keys_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Numeral_roots {
		reference := stage.Numeral_roots_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Octave_shifts {
		reference := stage.Octave_shifts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Offsets {
		reference := stage.Offsets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Opuss {
		reference := stage.Opuss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Ornamentss {
		reference := stage.Ornamentss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_appearances {
		reference := stage.Other_appearances_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_directions {
		reference := stage.Other_directions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_listenings {
		reference := stage.Other_listenings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_notations {
		reference := stage.Other_notations_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_placement_texts {
		reference := stage.Other_placement_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_plays {
		reference := stage.Other_plays_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Other_texts {
		reference := stage.Other_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Page_layouts {
		reference := stage.Page_layouts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Page_marginss {
		reference := stage.Page_marginss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_clefs {
		reference := stage.Part_clefs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_groups {
		reference := stage.Part_groups_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_links {
		reference := stage.Part_links_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_lists {
		reference := stage.Part_lists_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_names {
		reference := stage.Part_names_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_symbols {
		reference := stage.Part_symbols_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Part_transposes {
		reference := stage.Part_transposes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Pedals {
		reference := stage.Pedals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Pedal_tunings {
		reference := stage.Pedal_tunings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Per_minutes {
		reference := stage.Per_minutes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Percussions {
		reference := stage.Percussions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Pitchs {
		reference := stage.Pitchs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Pitcheds {
		reference := stage.Pitcheds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Placement_texts {
		reference := stage.Placement_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Plays {
		reference := stage.Plays_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Players {
		reference := stage.Players_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Principal_voices {
		reference := stage.Principal_voices_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Prints {
		reference := stage.Prints_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Releases {
		reference := stage.Releases_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Repeats {
		reference := stage.Repeats_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Rests {
		reference := stage.Rests_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Roots {
		reference := stage.Roots_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Root_steps {
		reference := stage.Root_steps_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Scalings {
		reference := stage.Scalings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Scordaturas {
		reference := stage.Scordaturas_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Score_instruments {
		reference := stage.Score_instruments_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Score_parts {
		reference := stage.Score_parts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Score_partwises {
		reference := stage.Score_partwises_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Score_timewises {
		reference := stage.Score_timewises_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Segnos {
		reference := stage.Segnos_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Slashs {
		reference := stage.Slashs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Slides {
		reference := stage.Slides_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Slurs {
		reference := stage.Slurs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Sounds {
		reference := stage.Sounds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Staff_detailss {
		reference := stage.Staff_detailss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Staff_divides {
		reference := stage.Staff_divides_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Staff_layouts {
		reference := stage.Staff_layouts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Staff_sizes {
		reference := stage.Staff_sizes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Staff_tunings {
		reference := stage.Staff_tunings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Stems {
		reference := stage.Stems_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Sticks {
		reference := stage.Sticks_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.String_mutes {
		reference := stage.String_mutes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.String_types {
		reference := stage.String_types_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Strong_accents {
		reference := stage.Strong_accents_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Style_texts {
		reference := stage.Style_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Supportss {
		reference := stage.Supportss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Swings {
		reference := stage.Swings_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Syncs {
		reference := stage.Syncs_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.System_dividerss {
		reference := stage.System_dividerss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.System_layouts {
		reference := stage.System_layouts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.System_marginss {
		reference := stage.System_marginss_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Taps {
		reference := stage.Taps_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Technicals {
		reference := stage.Technicals_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Text_element_datas {
		reference := stage.Text_element_datas_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Ties {
		reference := stage.Ties_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tieds {
		reference := stage.Tieds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Times {
		reference := stage.Times_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Time_modifications {
		reference := stage.Time_modifications_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Timpanis {
		reference := stage.Timpanis_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Transposes {
		reference := stage.Transposes_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tremolos {
		reference := stage.Tremolos_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tuplets {
		reference := stage.Tuplets_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tuplet_dots {
		reference := stage.Tuplet_dots_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tuplet_numbers {
		reference := stage.Tuplet_numbers_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tuplet_portions {
		reference := stage.Tuplet_portions_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Tuplet_types {
		reference := stage.Tuplet_types_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Typed_texts {
		reference := stage.Typed_texts_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Unpitcheds {
		reference := stage.Unpitcheds_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Virtual_instruments {
		reference := stage.Virtual_instruments_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Waits {
		reference := stage.Waits_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Wavy_lines {
		reference := stage.Wavy_lines_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Wedges {
		reference := stage.Wedges_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Woods {
		reference := stage.Woods_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	for instance := range stage.Works {
		reference := stage.Works_reference[instance]
		reference.GongReconstructPointersFromReferences(stage, instance)
	}

	stage.recomputeOrders()
}

// GongGetOrder returns the order of the instance in the staging area
// This order is set at staging time, and reflects the order of creation of the instances
// in the staging area
// It is used when rendering slices of GongstructIF to keep a deterministic order
// which is important for frontends such as web frontends
// to avoid unnecessary re-renderings
// insertion point per named struct
func (a_directive *A_directive) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.A_directive_stagedOrder[a_directive]; ok {
		return order
	}
	if order, ok := stage.A_directives_referenceOrder[a_directive]; ok {
		return order
	} else {
		log.Printf("instance %p of type A_directive was not staged and does not have a reference order", a_directive)
		return 0
	}
}

func (a_measure *A_measure) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.A_measure_stagedOrder[a_measure]; ok {
		return order
	}
	if order, ok := stage.A_measures_referenceOrder[a_measure]; ok {
		return order
	} else {
		log.Printf("instance %p of type A_measure was not staged and does not have a reference order", a_measure)
		return 0
	}
}

func (a_measure_1 *A_measure_1) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.A_measure_1_stagedOrder[a_measure_1]; ok {
		return order
	}
	if order, ok := stage.A_measure_1s_referenceOrder[a_measure_1]; ok {
		return order
	} else {
		log.Printf("instance %p of type A_measure_1 was not staged and does not have a reference order", a_measure_1)
		return 0
	}
}

func (a_part *A_part) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.A_part_stagedOrder[a_part]; ok {
		return order
	}
	if order, ok := stage.A_parts_referenceOrder[a_part]; ok {
		return order
	} else {
		log.Printf("instance %p of type A_part was not staged and does not have a reference order", a_part)
		return 0
	}
}

func (a_part_1 *A_part_1) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.A_part_1_stagedOrder[a_part_1]; ok {
		return order
	}
	if order, ok := stage.A_part_1s_referenceOrder[a_part_1]; ok {
		return order
	} else {
		log.Printf("instance %p of type A_part_1 was not staged and does not have a reference order", a_part_1)
		return 0
	}
}

func (accidental *Accidental) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Accidental_stagedOrder[accidental]; ok {
		return order
	}
	if order, ok := stage.Accidentals_referenceOrder[accidental]; ok {
		return order
	} else {
		log.Printf("instance %p of type Accidental was not staged and does not have a reference order", accidental)
		return 0
	}
}

func (accidental_mark *Accidental_mark) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Accidental_mark_stagedOrder[accidental_mark]; ok {
		return order
	}
	if order, ok := stage.Accidental_marks_referenceOrder[accidental_mark]; ok {
		return order
	} else {
		log.Printf("instance %p of type Accidental_mark was not staged and does not have a reference order", accidental_mark)
		return 0
	}
}

func (accidental_text *Accidental_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Accidental_text_stagedOrder[accidental_text]; ok {
		return order
	}
	if order, ok := stage.Accidental_texts_referenceOrder[accidental_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Accidental_text was not staged and does not have a reference order", accidental_text)
		return 0
	}
}

func (accord *Accord) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Accord_stagedOrder[accord]; ok {
		return order
	}
	if order, ok := stage.Accords_referenceOrder[accord]; ok {
		return order
	} else {
		log.Printf("instance %p of type Accord was not staged and does not have a reference order", accord)
		return 0
	}
}

func (accordion_registration *Accordion_registration) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Accordion_registration_stagedOrder[accordion_registration]; ok {
		return order
	}
	if order, ok := stage.Accordion_registrations_referenceOrder[accordion_registration]; ok {
		return order
	} else {
		log.Printf("instance %p of type Accordion_registration was not staged and does not have a reference order", accordion_registration)
		return 0
	}
}

func (appearance *Appearance) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Appearance_stagedOrder[appearance]; ok {
		return order
	}
	if order, ok := stage.Appearances_referenceOrder[appearance]; ok {
		return order
	} else {
		log.Printf("instance %p of type Appearance was not staged and does not have a reference order", appearance)
		return 0
	}
}

func (arpeggiate *Arpeggiate) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Arpeggiate_stagedOrder[arpeggiate]; ok {
		return order
	}
	if order, ok := stage.Arpeggiates_referenceOrder[arpeggiate]; ok {
		return order
	} else {
		log.Printf("instance %p of type Arpeggiate was not staged and does not have a reference order", arpeggiate)
		return 0
	}
}

func (arrow *Arrow) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Arrow_stagedOrder[arrow]; ok {
		return order
	}
	if order, ok := stage.Arrows_referenceOrder[arrow]; ok {
		return order
	} else {
		log.Printf("instance %p of type Arrow was not staged and does not have a reference order", arrow)
		return 0
	}
}

func (articulations *Articulations) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Articulations_stagedOrder[articulations]; ok {
		return order
	}
	if order, ok := stage.Articulationss_referenceOrder[articulations]; ok {
		return order
	} else {
		log.Printf("instance %p of type Articulations was not staged and does not have a reference order", articulations)
		return 0
	}
}

func (assess *Assess) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Assess_stagedOrder[assess]; ok {
		return order
	}
	if order, ok := stage.Assesss_referenceOrder[assess]; ok {
		return order
	} else {
		log.Printf("instance %p of type Assess was not staged and does not have a reference order", assess)
		return 0
	}
}

func (attributes *Attributes) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Attributes_stagedOrder[attributes]; ok {
		return order
	}
	if order, ok := stage.Attributess_referenceOrder[attributes]; ok {
		return order
	} else {
		log.Printf("instance %p of type Attributes was not staged and does not have a reference order", attributes)
		return 0
	}
}

func (backup *Backup) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Backup_stagedOrder[backup]; ok {
		return order
	}
	if order, ok := stage.Backups_referenceOrder[backup]; ok {
		return order
	} else {
		log.Printf("instance %p of type Backup was not staged and does not have a reference order", backup)
		return 0
	}
}

func (bar_style_color *Bar_style_color) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bar_style_color_stagedOrder[bar_style_color]; ok {
		return order
	}
	if order, ok := stage.Bar_style_colors_referenceOrder[bar_style_color]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bar_style_color was not staged and does not have a reference order", bar_style_color)
		return 0
	}
}

func (barline *Barline) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Barline_stagedOrder[barline]; ok {
		return order
	}
	if order, ok := stage.Barlines_referenceOrder[barline]; ok {
		return order
	} else {
		log.Printf("instance %p of type Barline was not staged and does not have a reference order", barline)
		return 0
	}
}

func (barre *Barre) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Barre_stagedOrder[barre]; ok {
		return order
	}
	if order, ok := stage.Barres_referenceOrder[barre]; ok {
		return order
	} else {
		log.Printf("instance %p of type Barre was not staged and does not have a reference order", barre)
		return 0
	}
}

func (bass *Bass) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bass_stagedOrder[bass]; ok {
		return order
	}
	if order, ok := stage.Basss_referenceOrder[bass]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bass was not staged and does not have a reference order", bass)
		return 0
	}
}

func (bass_step *Bass_step) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bass_step_stagedOrder[bass_step]; ok {
		return order
	}
	if order, ok := stage.Bass_steps_referenceOrder[bass_step]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bass_step was not staged and does not have a reference order", bass_step)
		return 0
	}
}

func (beam *Beam) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Beam_stagedOrder[beam]; ok {
		return order
	}
	if order, ok := stage.Beams_referenceOrder[beam]; ok {
		return order
	} else {
		log.Printf("instance %p of type Beam was not staged and does not have a reference order", beam)
		return 0
	}
}

func (beat_repeat *Beat_repeat) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Beat_repeat_stagedOrder[beat_repeat]; ok {
		return order
	}
	if order, ok := stage.Beat_repeats_referenceOrder[beat_repeat]; ok {
		return order
	} else {
		log.Printf("instance %p of type Beat_repeat was not staged and does not have a reference order", beat_repeat)
		return 0
	}
}

func (beat_unit_tied *Beat_unit_tied) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Beat_unit_tied_stagedOrder[beat_unit_tied]; ok {
		return order
	}
	if order, ok := stage.Beat_unit_tieds_referenceOrder[beat_unit_tied]; ok {
		return order
	} else {
		log.Printf("instance %p of type Beat_unit_tied was not staged and does not have a reference order", beat_unit_tied)
		return 0
	}
}

func (beater *Beater) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Beater_stagedOrder[beater]; ok {
		return order
	}
	if order, ok := stage.Beaters_referenceOrder[beater]; ok {
		return order
	} else {
		log.Printf("instance %p of type Beater was not staged and does not have a reference order", beater)
		return 0
	}
}

func (bend *Bend) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bend_stagedOrder[bend]; ok {
		return order
	}
	if order, ok := stage.Bends_referenceOrder[bend]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bend was not staged and does not have a reference order", bend)
		return 0
	}
}

func (bookmark *Bookmark) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bookmark_stagedOrder[bookmark]; ok {
		return order
	}
	if order, ok := stage.Bookmarks_referenceOrder[bookmark]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bookmark was not staged and does not have a reference order", bookmark)
		return 0
	}
}

func (bracket *Bracket) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Bracket_stagedOrder[bracket]; ok {
		return order
	}
	if order, ok := stage.Brackets_referenceOrder[bracket]; ok {
		return order
	} else {
		log.Printf("instance %p of type Bracket was not staged and does not have a reference order", bracket)
		return 0
	}
}

func (breath_mark *Breath_mark) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Breath_mark_stagedOrder[breath_mark]; ok {
		return order
	}
	if order, ok := stage.Breath_marks_referenceOrder[breath_mark]; ok {
		return order
	} else {
		log.Printf("instance %p of type Breath_mark was not staged and does not have a reference order", breath_mark)
		return 0
	}
}

func (caesura *Caesura) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Caesura_stagedOrder[caesura]; ok {
		return order
	}
	if order, ok := stage.Caesuras_referenceOrder[caesura]; ok {
		return order
	} else {
		log.Printf("instance %p of type Caesura was not staged and does not have a reference order", caesura)
		return 0
	}
}

func (cancel *Cancel) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Cancel_stagedOrder[cancel]; ok {
		return order
	}
	if order, ok := stage.Cancels_referenceOrder[cancel]; ok {
		return order
	} else {
		log.Printf("instance %p of type Cancel was not staged and does not have a reference order", cancel)
		return 0
	}
}

func (clef *Clef) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Clef_stagedOrder[clef]; ok {
		return order
	}
	if order, ok := stage.Clefs_referenceOrder[clef]; ok {
		return order
	} else {
		log.Printf("instance %p of type Clef was not staged and does not have a reference order", clef)
		return 0
	}
}

func (coda *Coda) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Coda_stagedOrder[coda]; ok {
		return order
	}
	if order, ok := stage.Codas_referenceOrder[coda]; ok {
		return order
	} else {
		log.Printf("instance %p of type Coda was not staged and does not have a reference order", coda)
		return 0
	}
}

func (credit *Credit) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Credit_stagedOrder[credit]; ok {
		return order
	}
	if order, ok := stage.Credits_referenceOrder[credit]; ok {
		return order
	} else {
		log.Printf("instance %p of type Credit was not staged and does not have a reference order", credit)
		return 0
	}
}

func (dashes *Dashes) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Dashes_stagedOrder[dashes]; ok {
		return order
	}
	if order, ok := stage.Dashess_referenceOrder[dashes]; ok {
		return order
	} else {
		log.Printf("instance %p of type Dashes was not staged and does not have a reference order", dashes)
		return 0
	}
}

func (defaults *Defaults) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Defaults_stagedOrder[defaults]; ok {
		return order
	}
	if order, ok := stage.Defaultss_referenceOrder[defaults]; ok {
		return order
	} else {
		log.Printf("instance %p of type Defaults was not staged and does not have a reference order", defaults)
		return 0
	}
}

func (degree *Degree) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Degree_stagedOrder[degree]; ok {
		return order
	}
	if order, ok := stage.Degrees_referenceOrder[degree]; ok {
		return order
	} else {
		log.Printf("instance %p of type Degree was not staged and does not have a reference order", degree)
		return 0
	}
}

func (degree_alter *Degree_alter) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Degree_alter_stagedOrder[degree_alter]; ok {
		return order
	}
	if order, ok := stage.Degree_alters_referenceOrder[degree_alter]; ok {
		return order
	} else {
		log.Printf("instance %p of type Degree_alter was not staged and does not have a reference order", degree_alter)
		return 0
	}
}

func (degree_type *Degree_type) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Degree_type_stagedOrder[degree_type]; ok {
		return order
	}
	if order, ok := stage.Degree_types_referenceOrder[degree_type]; ok {
		return order
	} else {
		log.Printf("instance %p of type Degree_type was not staged and does not have a reference order", degree_type)
		return 0
	}
}

func (degree_value *Degree_value) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Degree_value_stagedOrder[degree_value]; ok {
		return order
	}
	if order, ok := stage.Degree_values_referenceOrder[degree_value]; ok {
		return order
	} else {
		log.Printf("instance %p of type Degree_value was not staged and does not have a reference order", degree_value)
		return 0
	}
}

func (direction *Direction) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Direction_stagedOrder[direction]; ok {
		return order
	}
	if order, ok := stage.Directions_referenceOrder[direction]; ok {
		return order
	} else {
		log.Printf("instance %p of type Direction was not staged and does not have a reference order", direction)
		return 0
	}
}

func (direction_type *Direction_type) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Direction_type_stagedOrder[direction_type]; ok {
		return order
	}
	if order, ok := stage.Direction_types_referenceOrder[direction_type]; ok {
		return order
	} else {
		log.Printf("instance %p of type Direction_type was not staged and does not have a reference order", direction_type)
		return 0
	}
}

func (distance *Distance) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Distance_stagedOrder[distance]; ok {
		return order
	}
	if order, ok := stage.Distances_referenceOrder[distance]; ok {
		return order
	} else {
		log.Printf("instance %p of type Distance was not staged and does not have a reference order", distance)
		return 0
	}
}

func (double *Double) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Double_stagedOrder[double]; ok {
		return order
	}
	if order, ok := stage.Doubles_referenceOrder[double]; ok {
		return order
	} else {
		log.Printf("instance %p of type Double was not staged and does not have a reference order", double)
		return 0
	}
}

func (dynamics *Dynamics) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Dynamics_stagedOrder[dynamics]; ok {
		return order
	}
	if order, ok := stage.Dynamicss_referenceOrder[dynamics]; ok {
		return order
	} else {
		log.Printf("instance %p of type Dynamics was not staged and does not have a reference order", dynamics)
		return 0
	}
}

func (effect *Effect) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Effect_stagedOrder[effect]; ok {
		return order
	}
	if order, ok := stage.Effects_referenceOrder[effect]; ok {
		return order
	} else {
		log.Printf("instance %p of type Effect was not staged and does not have a reference order", effect)
		return 0
	}
}

func (elision *Elision) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Elision_stagedOrder[elision]; ok {
		return order
	}
	if order, ok := stage.Elisions_referenceOrder[elision]; ok {
		return order
	} else {
		log.Printf("instance %p of type Elision was not staged and does not have a reference order", elision)
		return 0
	}
}

func (empty *Empty) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_stagedOrder[empty]; ok {
		return order
	}
	if order, ok := stage.Emptys_referenceOrder[empty]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty was not staged and does not have a reference order", empty)
		return 0
	}
}

func (empty_font *Empty_font) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_font_stagedOrder[empty_font]; ok {
		return order
	}
	if order, ok := stage.Empty_fonts_referenceOrder[empty_font]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_font was not staged and does not have a reference order", empty_font)
		return 0
	}
}

func (empty_line *Empty_line) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_line_stagedOrder[empty_line]; ok {
		return order
	}
	if order, ok := stage.Empty_lines_referenceOrder[empty_line]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_line was not staged and does not have a reference order", empty_line)
		return 0
	}
}

func (empty_placement *Empty_placement) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_placement_stagedOrder[empty_placement]; ok {
		return order
	}
	if order, ok := stage.Empty_placements_referenceOrder[empty_placement]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_placement was not staged and does not have a reference order", empty_placement)
		return 0
	}
}

func (empty_placement_smufl *Empty_placement_smufl) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_placement_smufl_stagedOrder[empty_placement_smufl]; ok {
		return order
	}
	if order, ok := stage.Empty_placement_smufls_referenceOrder[empty_placement_smufl]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_placement_smufl was not staged and does not have a reference order", empty_placement_smufl)
		return 0
	}
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_print_object_style_align_stagedOrder[empty_print_object_style_align]; ok {
		return order
	}
	if order, ok := stage.Empty_print_object_style_aligns_referenceOrder[empty_print_object_style_align]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_print_object_style_align was not staged and does not have a reference order", empty_print_object_style_align)
		return 0
	}
}

func (empty_print_style *Empty_print_style) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_print_style_stagedOrder[empty_print_style]; ok {
		return order
	}
	if order, ok := stage.Empty_print_styles_referenceOrder[empty_print_style]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_print_style was not staged and does not have a reference order", empty_print_style)
		return 0
	}
}

func (empty_print_style_align *Empty_print_style_align) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_print_style_align_stagedOrder[empty_print_style_align]; ok {
		return order
	}
	if order, ok := stage.Empty_print_style_aligns_referenceOrder[empty_print_style_align]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_print_style_align was not staged and does not have a reference order", empty_print_style_align)
		return 0
	}
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_print_style_align_id_stagedOrder[empty_print_style_align_id]; ok {
		return order
	}
	if order, ok := stage.Empty_print_style_align_ids_referenceOrder[empty_print_style_align_id]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_print_style_align_id was not staged and does not have a reference order", empty_print_style_align_id)
		return 0
	}
}

func (empty_trill_sound *Empty_trill_sound) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Empty_trill_sound_stagedOrder[empty_trill_sound]; ok {
		return order
	}
	if order, ok := stage.Empty_trill_sounds_referenceOrder[empty_trill_sound]; ok {
		return order
	} else {
		log.Printf("instance %p of type Empty_trill_sound was not staged and does not have a reference order", empty_trill_sound)
		return 0
	}
}

func (encoding *Encoding) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Encoding_stagedOrder[encoding]; ok {
		return order
	}
	if order, ok := stage.Encodings_referenceOrder[encoding]; ok {
		return order
	} else {
		log.Printf("instance %p of type Encoding was not staged and does not have a reference order", encoding)
		return 0
	}
}

func (ending *Ending) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Ending_stagedOrder[ending]; ok {
		return order
	}
	if order, ok := stage.Endings_referenceOrder[ending]; ok {
		return order
	} else {
		log.Printf("instance %p of type Ending was not staged and does not have a reference order", ending)
		return 0
	}
}

func (extend *Extend) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Extend_stagedOrder[extend]; ok {
		return order
	}
	if order, ok := stage.Extends_referenceOrder[extend]; ok {
		return order
	} else {
		log.Printf("instance %p of type Extend was not staged and does not have a reference order", extend)
		return 0
	}
}

func (feature *Feature) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Feature_stagedOrder[feature]; ok {
		return order
	}
	if order, ok := stage.Features_referenceOrder[feature]; ok {
		return order
	} else {
		log.Printf("instance %p of type Feature was not staged and does not have a reference order", feature)
		return 0
	}
}

func (fermata *Fermata) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Fermata_stagedOrder[fermata]; ok {
		return order
	}
	if order, ok := stage.Fermatas_referenceOrder[fermata]; ok {
		return order
	} else {
		log.Printf("instance %p of type Fermata was not staged and does not have a reference order", fermata)
		return 0
	}
}

func (figure *Figure) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Figure_stagedOrder[figure]; ok {
		return order
	}
	if order, ok := stage.Figures_referenceOrder[figure]; ok {
		return order
	} else {
		log.Printf("instance %p of type Figure was not staged and does not have a reference order", figure)
		return 0
	}
}

func (figured_bass *Figured_bass) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Figured_bass_stagedOrder[figured_bass]; ok {
		return order
	}
	if order, ok := stage.Figured_basss_referenceOrder[figured_bass]; ok {
		return order
	} else {
		log.Printf("instance %p of type Figured_bass was not staged and does not have a reference order", figured_bass)
		return 0
	}
}

func (fingering *Fingering) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Fingering_stagedOrder[fingering]; ok {
		return order
	}
	if order, ok := stage.Fingerings_referenceOrder[fingering]; ok {
		return order
	} else {
		log.Printf("instance %p of type Fingering was not staged and does not have a reference order", fingering)
		return 0
	}
}

func (first_fret *First_fret) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.First_fret_stagedOrder[first_fret]; ok {
		return order
	}
	if order, ok := stage.First_frets_referenceOrder[first_fret]; ok {
		return order
	} else {
		log.Printf("instance %p of type First_fret was not staged and does not have a reference order", first_fret)
		return 0
	}
}

func (for_part *For_part) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.For_part_stagedOrder[for_part]; ok {
		return order
	}
	if order, ok := stage.For_parts_referenceOrder[for_part]; ok {
		return order
	} else {
		log.Printf("instance %p of type For_part was not staged and does not have a reference order", for_part)
		return 0
	}
}

func (formatted_symbol *Formatted_symbol) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Formatted_symbol_stagedOrder[formatted_symbol]; ok {
		return order
	}
	if order, ok := stage.Formatted_symbols_referenceOrder[formatted_symbol]; ok {
		return order
	} else {
		log.Printf("instance %p of type Formatted_symbol was not staged and does not have a reference order", formatted_symbol)
		return 0
	}
}

func (formatted_symbol_id *Formatted_symbol_id) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Formatted_symbol_id_stagedOrder[formatted_symbol_id]; ok {
		return order
	}
	if order, ok := stage.Formatted_symbol_ids_referenceOrder[formatted_symbol_id]; ok {
		return order
	} else {
		log.Printf("instance %p of type Formatted_symbol_id was not staged and does not have a reference order", formatted_symbol_id)
		return 0
	}
}

func (formatted_text *Formatted_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Formatted_text_stagedOrder[formatted_text]; ok {
		return order
	}
	if order, ok := stage.Formatted_texts_referenceOrder[formatted_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Formatted_text was not staged and does not have a reference order", formatted_text)
		return 0
	}
}

func (formatted_text_id *Formatted_text_id) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Formatted_text_id_stagedOrder[formatted_text_id]; ok {
		return order
	}
	if order, ok := stage.Formatted_text_ids_referenceOrder[formatted_text_id]; ok {
		return order
	} else {
		log.Printf("instance %p of type Formatted_text_id was not staged and does not have a reference order", formatted_text_id)
		return 0
	}
}

func (forward *Forward) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Forward_stagedOrder[forward]; ok {
		return order
	}
	if order, ok := stage.Forwards_referenceOrder[forward]; ok {
		return order
	} else {
		log.Printf("instance %p of type Forward was not staged and does not have a reference order", forward)
		return 0
	}
}

func (frame *Frame) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Frame_stagedOrder[frame]; ok {
		return order
	}
	if order, ok := stage.Frames_referenceOrder[frame]; ok {
		return order
	} else {
		log.Printf("instance %p of type Frame was not staged and does not have a reference order", frame)
		return 0
	}
}

func (frame_note *Frame_note) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Frame_note_stagedOrder[frame_note]; ok {
		return order
	}
	if order, ok := stage.Frame_notes_referenceOrder[frame_note]; ok {
		return order
	} else {
		log.Printf("instance %p of type Frame_note was not staged and does not have a reference order", frame_note)
		return 0
	}
}

func (fret *Fret) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Fret_stagedOrder[fret]; ok {
		return order
	}
	if order, ok := stage.Frets_referenceOrder[fret]; ok {
		return order
	} else {
		log.Printf("instance %p of type Fret was not staged and does not have a reference order", fret)
		return 0
	}
}

func (glass *Glass) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Glass_stagedOrder[glass]; ok {
		return order
	}
	if order, ok := stage.Glasss_referenceOrder[glass]; ok {
		return order
	} else {
		log.Printf("instance %p of type Glass was not staged and does not have a reference order", glass)
		return 0
	}
}

func (glissando *Glissando) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Glissando_stagedOrder[glissando]; ok {
		return order
	}
	if order, ok := stage.Glissandos_referenceOrder[glissando]; ok {
		return order
	} else {
		log.Printf("instance %p of type Glissando was not staged and does not have a reference order", glissando)
		return 0
	}
}

func (glyph *Glyph) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Glyph_stagedOrder[glyph]; ok {
		return order
	}
	if order, ok := stage.Glyphs_referenceOrder[glyph]; ok {
		return order
	} else {
		log.Printf("instance %p of type Glyph was not staged and does not have a reference order", glyph)
		return 0
	}
}

func (grace *Grace) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Grace_stagedOrder[grace]; ok {
		return order
	}
	if order, ok := stage.Graces_referenceOrder[grace]; ok {
		return order
	} else {
		log.Printf("instance %p of type Grace was not staged and does not have a reference order", grace)
		return 0
	}
}

func (group_barline *Group_barline) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Group_barline_stagedOrder[group_barline]; ok {
		return order
	}
	if order, ok := stage.Group_barlines_referenceOrder[group_barline]; ok {
		return order
	} else {
		log.Printf("instance %p of type Group_barline was not staged and does not have a reference order", group_barline)
		return 0
	}
}

func (group_name *Group_name) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Group_name_stagedOrder[group_name]; ok {
		return order
	}
	if order, ok := stage.Group_names_referenceOrder[group_name]; ok {
		return order
	} else {
		log.Printf("instance %p of type Group_name was not staged and does not have a reference order", group_name)
		return 0
	}
}

func (group_symbol *Group_symbol) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Group_symbol_stagedOrder[group_symbol]; ok {
		return order
	}
	if order, ok := stage.Group_symbols_referenceOrder[group_symbol]; ok {
		return order
	} else {
		log.Printf("instance %p of type Group_symbol was not staged and does not have a reference order", group_symbol)
		return 0
	}
}

func (grouping *Grouping) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Grouping_stagedOrder[grouping]; ok {
		return order
	}
	if order, ok := stage.Groupings_referenceOrder[grouping]; ok {
		return order
	} else {
		log.Printf("instance %p of type Grouping was not staged and does not have a reference order", grouping)
		return 0
	}
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Hammer_on_pull_off_stagedOrder[hammer_on_pull_off]; ok {
		return order
	}
	if order, ok := stage.Hammer_on_pull_offs_referenceOrder[hammer_on_pull_off]; ok {
		return order
	} else {
		log.Printf("instance %p of type Hammer_on_pull_off was not staged and does not have a reference order", hammer_on_pull_off)
		return 0
	}
}

func (handbell *Handbell) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Handbell_stagedOrder[handbell]; ok {
		return order
	}
	if order, ok := stage.Handbells_referenceOrder[handbell]; ok {
		return order
	} else {
		log.Printf("instance %p of type Handbell was not staged and does not have a reference order", handbell)
		return 0
	}
}

func (harmon_closed *Harmon_closed) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harmon_closed_stagedOrder[harmon_closed]; ok {
		return order
	}
	if order, ok := stage.Harmon_closeds_referenceOrder[harmon_closed]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harmon_closed was not staged and does not have a reference order", harmon_closed)
		return 0
	}
}

func (harmon_mute *Harmon_mute) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harmon_mute_stagedOrder[harmon_mute]; ok {
		return order
	}
	if order, ok := stage.Harmon_mutes_referenceOrder[harmon_mute]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harmon_mute was not staged and does not have a reference order", harmon_mute)
		return 0
	}
}

func (harmonic *Harmonic) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harmonic_stagedOrder[harmonic]; ok {
		return order
	}
	if order, ok := stage.Harmonics_referenceOrder[harmonic]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harmonic was not staged and does not have a reference order", harmonic)
		return 0
	}
}

func (harmony *Harmony) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harmony_stagedOrder[harmony]; ok {
		return order
	}
	if order, ok := stage.Harmonys_referenceOrder[harmony]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harmony was not staged and does not have a reference order", harmony)
		return 0
	}
}

func (harmony_alter *Harmony_alter) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harmony_alter_stagedOrder[harmony_alter]; ok {
		return order
	}
	if order, ok := stage.Harmony_alters_referenceOrder[harmony_alter]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harmony_alter was not staged and does not have a reference order", harmony_alter)
		return 0
	}
}

func (harp_pedals *Harp_pedals) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Harp_pedals_stagedOrder[harp_pedals]; ok {
		return order
	}
	if order, ok := stage.Harp_pedalss_referenceOrder[harp_pedals]; ok {
		return order
	} else {
		log.Printf("instance %p of type Harp_pedals was not staged and does not have a reference order", harp_pedals)
		return 0
	}
}

func (heel_toe *Heel_toe) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Heel_toe_stagedOrder[heel_toe]; ok {
		return order
	}
	if order, ok := stage.Heel_toes_referenceOrder[heel_toe]; ok {
		return order
	} else {
		log.Printf("instance %p of type Heel_toe was not staged and does not have a reference order", heel_toe)
		return 0
	}
}

func (hole *Hole) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Hole_stagedOrder[hole]; ok {
		return order
	}
	if order, ok := stage.Holes_referenceOrder[hole]; ok {
		return order
	} else {
		log.Printf("instance %p of type Hole was not staged and does not have a reference order", hole)
		return 0
	}
}

func (hole_closed *Hole_closed) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Hole_closed_stagedOrder[hole_closed]; ok {
		return order
	}
	if order, ok := stage.Hole_closeds_referenceOrder[hole_closed]; ok {
		return order
	} else {
		log.Printf("instance %p of type Hole_closed was not staged and does not have a reference order", hole_closed)
		return 0
	}
}

func (horizontal_turn *Horizontal_turn) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Horizontal_turn_stagedOrder[horizontal_turn]; ok {
		return order
	}
	if order, ok := stage.Horizontal_turns_referenceOrder[horizontal_turn]; ok {
		return order
	} else {
		log.Printf("instance %p of type Horizontal_turn was not staged and does not have a reference order", horizontal_turn)
		return 0
	}
}

func (identification *Identification) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Identification_stagedOrder[identification]; ok {
		return order
	}
	if order, ok := stage.Identifications_referenceOrder[identification]; ok {
		return order
	} else {
		log.Printf("instance %p of type Identification was not staged and does not have a reference order", identification)
		return 0
	}
}

func (image *Image) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Image_stagedOrder[image]; ok {
		return order
	}
	if order, ok := stage.Images_referenceOrder[image]; ok {
		return order
	} else {
		log.Printf("instance %p of type Image was not staged and does not have a reference order", image)
		return 0
	}
}

func (instrument *Instrument) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Instrument_stagedOrder[instrument]; ok {
		return order
	}
	if order, ok := stage.Instruments_referenceOrder[instrument]; ok {
		return order
	} else {
		log.Printf("instance %p of type Instrument was not staged and does not have a reference order", instrument)
		return 0
	}
}

func (instrument_change *Instrument_change) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Instrument_change_stagedOrder[instrument_change]; ok {
		return order
	}
	if order, ok := stage.Instrument_changes_referenceOrder[instrument_change]; ok {
		return order
	} else {
		log.Printf("instance %p of type Instrument_change was not staged and does not have a reference order", instrument_change)
		return 0
	}
}

func (instrument_link *Instrument_link) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Instrument_link_stagedOrder[instrument_link]; ok {
		return order
	}
	if order, ok := stage.Instrument_links_referenceOrder[instrument_link]; ok {
		return order
	} else {
		log.Printf("instance %p of type Instrument_link was not staged and does not have a reference order", instrument_link)
		return 0
	}
}

func (interchangeable *Interchangeable) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Interchangeable_stagedOrder[interchangeable]; ok {
		return order
	}
	if order, ok := stage.Interchangeables_referenceOrder[interchangeable]; ok {
		return order
	} else {
		log.Printf("instance %p of type Interchangeable was not staged and does not have a reference order", interchangeable)
		return 0
	}
}

func (inversion *Inversion) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Inversion_stagedOrder[inversion]; ok {
		return order
	}
	if order, ok := stage.Inversions_referenceOrder[inversion]; ok {
		return order
	} else {
		log.Printf("instance %p of type Inversion was not staged and does not have a reference order", inversion)
		return 0
	}
}

func (key *Key) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Key_stagedOrder[key]; ok {
		return order
	}
	if order, ok := stage.Keys_referenceOrder[key]; ok {
		return order
	} else {
		log.Printf("instance %p of type Key was not staged and does not have a reference order", key)
		return 0
	}
}

func (key_accidental *Key_accidental) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Key_accidental_stagedOrder[key_accidental]; ok {
		return order
	}
	if order, ok := stage.Key_accidentals_referenceOrder[key_accidental]; ok {
		return order
	} else {
		log.Printf("instance %p of type Key_accidental was not staged and does not have a reference order", key_accidental)
		return 0
	}
}

func (key_octave *Key_octave) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Key_octave_stagedOrder[key_octave]; ok {
		return order
	}
	if order, ok := stage.Key_octaves_referenceOrder[key_octave]; ok {
		return order
	} else {
		log.Printf("instance %p of type Key_octave was not staged and does not have a reference order", key_octave)
		return 0
	}
}

func (kind *Kind) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Kind_stagedOrder[kind]; ok {
		return order
	}
	if order, ok := stage.Kinds_referenceOrder[kind]; ok {
		return order
	} else {
		log.Printf("instance %p of type Kind was not staged and does not have a reference order", kind)
		return 0
	}
}

func (level *Level) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Level_stagedOrder[level]; ok {
		return order
	}
	if order, ok := stage.Levels_referenceOrder[level]; ok {
		return order
	} else {
		log.Printf("instance %p of type Level was not staged and does not have a reference order", level)
		return 0
	}
}

func (line_detail *Line_detail) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Line_detail_stagedOrder[line_detail]; ok {
		return order
	}
	if order, ok := stage.Line_details_referenceOrder[line_detail]; ok {
		return order
	} else {
		log.Printf("instance %p of type Line_detail was not staged and does not have a reference order", line_detail)
		return 0
	}
}

func (line_width *Line_width) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Line_width_stagedOrder[line_width]; ok {
		return order
	}
	if order, ok := stage.Line_widths_referenceOrder[line_width]; ok {
		return order
	} else {
		log.Printf("instance %p of type Line_width was not staged and does not have a reference order", line_width)
		return 0
	}
}

func (link *Link) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Link_stagedOrder[link]; ok {
		return order
	}
	if order, ok := stage.Links_referenceOrder[link]; ok {
		return order
	} else {
		log.Printf("instance %p of type Link was not staged and does not have a reference order", link)
		return 0
	}
}

func (listen *Listen) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Listen_stagedOrder[listen]; ok {
		return order
	}
	if order, ok := stage.Listens_referenceOrder[listen]; ok {
		return order
	} else {
		log.Printf("instance %p of type Listen was not staged and does not have a reference order", listen)
		return 0
	}
}

func (listening *Listening) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Listening_stagedOrder[listening]; ok {
		return order
	}
	if order, ok := stage.Listenings_referenceOrder[listening]; ok {
		return order
	} else {
		log.Printf("instance %p of type Listening was not staged and does not have a reference order", listening)
		return 0
	}
}

func (lyric *Lyric) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Lyric_stagedOrder[lyric]; ok {
		return order
	}
	if order, ok := stage.Lyrics_referenceOrder[lyric]; ok {
		return order
	} else {
		log.Printf("instance %p of type Lyric was not staged and does not have a reference order", lyric)
		return 0
	}
}

func (lyric_font *Lyric_font) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Lyric_font_stagedOrder[lyric_font]; ok {
		return order
	}
	if order, ok := stage.Lyric_fonts_referenceOrder[lyric_font]; ok {
		return order
	} else {
		log.Printf("instance %p of type Lyric_font was not staged and does not have a reference order", lyric_font)
		return 0
	}
}

func (lyric_language *Lyric_language) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Lyric_language_stagedOrder[lyric_language]; ok {
		return order
	}
	if order, ok := stage.Lyric_languages_referenceOrder[lyric_language]; ok {
		return order
	} else {
		log.Printf("instance %p of type Lyric_language was not staged and does not have a reference order", lyric_language)
		return 0
	}
}

func (measure_layout *Measure_layout) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Measure_layout_stagedOrder[measure_layout]; ok {
		return order
	}
	if order, ok := stage.Measure_layouts_referenceOrder[measure_layout]; ok {
		return order
	} else {
		log.Printf("instance %p of type Measure_layout was not staged and does not have a reference order", measure_layout)
		return 0
	}
}

func (measure_numbering *Measure_numbering) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Measure_numbering_stagedOrder[measure_numbering]; ok {
		return order
	}
	if order, ok := stage.Measure_numberings_referenceOrder[measure_numbering]; ok {
		return order
	} else {
		log.Printf("instance %p of type Measure_numbering was not staged and does not have a reference order", measure_numbering)
		return 0
	}
}

func (measure_repeat *Measure_repeat) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Measure_repeat_stagedOrder[measure_repeat]; ok {
		return order
	}
	if order, ok := stage.Measure_repeats_referenceOrder[measure_repeat]; ok {
		return order
	} else {
		log.Printf("instance %p of type Measure_repeat was not staged and does not have a reference order", measure_repeat)
		return 0
	}
}

func (measure_style *Measure_style) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Measure_style_stagedOrder[measure_style]; ok {
		return order
	}
	if order, ok := stage.Measure_styles_referenceOrder[measure_style]; ok {
		return order
	} else {
		log.Printf("instance %p of type Measure_style was not staged and does not have a reference order", measure_style)
		return 0
	}
}

func (membrane *Membrane) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Membrane_stagedOrder[membrane]; ok {
		return order
	}
	if order, ok := stage.Membranes_referenceOrder[membrane]; ok {
		return order
	} else {
		log.Printf("instance %p of type Membrane was not staged and does not have a reference order", membrane)
		return 0
	}
}

func (metal *Metal) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metal_stagedOrder[metal]; ok {
		return order
	}
	if order, ok := stage.Metals_referenceOrder[metal]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metal was not staged and does not have a reference order", metal)
		return 0
	}
}

func (metronome *Metronome) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metronome_stagedOrder[metronome]; ok {
		return order
	}
	if order, ok := stage.Metronomes_referenceOrder[metronome]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metronome was not staged and does not have a reference order", metronome)
		return 0
	}
}

func (metronome_beam *Metronome_beam) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metronome_beam_stagedOrder[metronome_beam]; ok {
		return order
	}
	if order, ok := stage.Metronome_beams_referenceOrder[metronome_beam]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metronome_beam was not staged and does not have a reference order", metronome_beam)
		return 0
	}
}

func (metronome_note *Metronome_note) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metronome_note_stagedOrder[metronome_note]; ok {
		return order
	}
	if order, ok := stage.Metronome_notes_referenceOrder[metronome_note]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metronome_note was not staged and does not have a reference order", metronome_note)
		return 0
	}
}

func (metronome_tied *Metronome_tied) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metronome_tied_stagedOrder[metronome_tied]; ok {
		return order
	}
	if order, ok := stage.Metronome_tieds_referenceOrder[metronome_tied]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metronome_tied was not staged and does not have a reference order", metronome_tied)
		return 0
	}
}

func (metronome_tuplet *Metronome_tuplet) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Metronome_tuplet_stagedOrder[metronome_tuplet]; ok {
		return order
	}
	if order, ok := stage.Metronome_tuplets_referenceOrder[metronome_tuplet]; ok {
		return order
	} else {
		log.Printf("instance %p of type Metronome_tuplet was not staged and does not have a reference order", metronome_tuplet)
		return 0
	}
}

func (midi_device *Midi_device) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Midi_device_stagedOrder[midi_device]; ok {
		return order
	}
	if order, ok := stage.Midi_devices_referenceOrder[midi_device]; ok {
		return order
	} else {
		log.Printf("instance %p of type Midi_device was not staged and does not have a reference order", midi_device)
		return 0
	}
}

func (midi_instrument *Midi_instrument) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Midi_instrument_stagedOrder[midi_instrument]; ok {
		return order
	}
	if order, ok := stage.Midi_instruments_referenceOrder[midi_instrument]; ok {
		return order
	} else {
		log.Printf("instance %p of type Midi_instrument was not staged and does not have a reference order", midi_instrument)
		return 0
	}
}

func (miscellaneous *Miscellaneous) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Miscellaneous_stagedOrder[miscellaneous]; ok {
		return order
	}
	if order, ok := stage.Miscellaneouss_referenceOrder[miscellaneous]; ok {
		return order
	} else {
		log.Printf("instance %p of type Miscellaneous was not staged and does not have a reference order", miscellaneous)
		return 0
	}
}

func (miscellaneous_field *Miscellaneous_field) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Miscellaneous_field_stagedOrder[miscellaneous_field]; ok {
		return order
	}
	if order, ok := stage.Miscellaneous_fields_referenceOrder[miscellaneous_field]; ok {
		return order
	} else {
		log.Printf("instance %p of type Miscellaneous_field was not staged and does not have a reference order", miscellaneous_field)
		return 0
	}
}

func (mordent *Mordent) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Mordent_stagedOrder[mordent]; ok {
		return order
	}
	if order, ok := stage.Mordents_referenceOrder[mordent]; ok {
		return order
	} else {
		log.Printf("instance %p of type Mordent was not staged and does not have a reference order", mordent)
		return 0
	}
}

func (multiple_rest *Multiple_rest) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Multiple_rest_stagedOrder[multiple_rest]; ok {
		return order
	}
	if order, ok := stage.Multiple_rests_referenceOrder[multiple_rest]; ok {
		return order
	} else {
		log.Printf("instance %p of type Multiple_rest was not staged and does not have a reference order", multiple_rest)
		return 0
	}
}

func (name_display *Name_display) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Name_display_stagedOrder[name_display]; ok {
		return order
	}
	if order, ok := stage.Name_displays_referenceOrder[name_display]; ok {
		return order
	} else {
		log.Printf("instance %p of type Name_display was not staged and does not have a reference order", name_display)
		return 0
	}
}

func (non_arpeggiate *Non_arpeggiate) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Non_arpeggiate_stagedOrder[non_arpeggiate]; ok {
		return order
	}
	if order, ok := stage.Non_arpeggiates_referenceOrder[non_arpeggiate]; ok {
		return order
	} else {
		log.Printf("instance %p of type Non_arpeggiate was not staged and does not have a reference order", non_arpeggiate)
		return 0
	}
}

func (notations *Notations) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Notations_stagedOrder[notations]; ok {
		return order
	}
	if order, ok := stage.Notationss_referenceOrder[notations]; ok {
		return order
	} else {
		log.Printf("instance %p of type Notations was not staged and does not have a reference order", notations)
		return 0
	}
}

func (note *Note) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Note_stagedOrder[note]; ok {
		return order
	}
	if order, ok := stage.Notes_referenceOrder[note]; ok {
		return order
	} else {
		log.Printf("instance %p of type Note was not staged and does not have a reference order", note)
		return 0
	}
}

func (note_size *Note_size) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Note_size_stagedOrder[note_size]; ok {
		return order
	}
	if order, ok := stage.Note_sizes_referenceOrder[note_size]; ok {
		return order
	} else {
		log.Printf("instance %p of type Note_size was not staged and does not have a reference order", note_size)
		return 0
	}
}

func (note_type *Note_type) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Note_type_stagedOrder[note_type]; ok {
		return order
	}
	if order, ok := stage.Note_types_referenceOrder[note_type]; ok {
		return order
	} else {
		log.Printf("instance %p of type Note_type was not staged and does not have a reference order", note_type)
		return 0
	}
}

func (notehead *Notehead) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Notehead_stagedOrder[notehead]; ok {
		return order
	}
	if order, ok := stage.Noteheads_referenceOrder[notehead]; ok {
		return order
	} else {
		log.Printf("instance %p of type Notehead was not staged and does not have a reference order", notehead)
		return 0
	}
}

func (notehead_text *Notehead_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Notehead_text_stagedOrder[notehead_text]; ok {
		return order
	}
	if order, ok := stage.Notehead_texts_referenceOrder[notehead_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Notehead_text was not staged and does not have a reference order", notehead_text)
		return 0
	}
}

func (numeral *Numeral) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Numeral_stagedOrder[numeral]; ok {
		return order
	}
	if order, ok := stage.Numerals_referenceOrder[numeral]; ok {
		return order
	} else {
		log.Printf("instance %p of type Numeral was not staged and does not have a reference order", numeral)
		return 0
	}
}

func (numeral_key *Numeral_key) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Numeral_key_stagedOrder[numeral_key]; ok {
		return order
	}
	if order, ok := stage.Numeral_keys_referenceOrder[numeral_key]; ok {
		return order
	} else {
		log.Printf("instance %p of type Numeral_key was not staged and does not have a reference order", numeral_key)
		return 0
	}
}

func (numeral_root *Numeral_root) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Numeral_root_stagedOrder[numeral_root]; ok {
		return order
	}
	if order, ok := stage.Numeral_roots_referenceOrder[numeral_root]; ok {
		return order
	} else {
		log.Printf("instance %p of type Numeral_root was not staged and does not have a reference order", numeral_root)
		return 0
	}
}

func (octave_shift *Octave_shift) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Octave_shift_stagedOrder[octave_shift]; ok {
		return order
	}
	if order, ok := stage.Octave_shifts_referenceOrder[octave_shift]; ok {
		return order
	} else {
		log.Printf("instance %p of type Octave_shift was not staged and does not have a reference order", octave_shift)
		return 0
	}
}

func (offset *Offset) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Offset_stagedOrder[offset]; ok {
		return order
	}
	if order, ok := stage.Offsets_referenceOrder[offset]; ok {
		return order
	} else {
		log.Printf("instance %p of type Offset was not staged and does not have a reference order", offset)
		return 0
	}
}

func (opus *Opus) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Opus_stagedOrder[opus]; ok {
		return order
	}
	if order, ok := stage.Opuss_referenceOrder[opus]; ok {
		return order
	} else {
		log.Printf("instance %p of type Opus was not staged and does not have a reference order", opus)
		return 0
	}
}

func (ornaments *Ornaments) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Ornaments_stagedOrder[ornaments]; ok {
		return order
	}
	if order, ok := stage.Ornamentss_referenceOrder[ornaments]; ok {
		return order
	} else {
		log.Printf("instance %p of type Ornaments was not staged and does not have a reference order", ornaments)
		return 0
	}
}

func (other_appearance *Other_appearance) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_appearance_stagedOrder[other_appearance]; ok {
		return order
	}
	if order, ok := stage.Other_appearances_referenceOrder[other_appearance]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_appearance was not staged and does not have a reference order", other_appearance)
		return 0
	}
}

func (other_direction *Other_direction) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_direction_stagedOrder[other_direction]; ok {
		return order
	}
	if order, ok := stage.Other_directions_referenceOrder[other_direction]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_direction was not staged and does not have a reference order", other_direction)
		return 0
	}
}

func (other_listening *Other_listening) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_listening_stagedOrder[other_listening]; ok {
		return order
	}
	if order, ok := stage.Other_listenings_referenceOrder[other_listening]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_listening was not staged and does not have a reference order", other_listening)
		return 0
	}
}

func (other_notation *Other_notation) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_notation_stagedOrder[other_notation]; ok {
		return order
	}
	if order, ok := stage.Other_notations_referenceOrder[other_notation]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_notation was not staged and does not have a reference order", other_notation)
		return 0
	}
}

func (other_placement_text *Other_placement_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_placement_text_stagedOrder[other_placement_text]; ok {
		return order
	}
	if order, ok := stage.Other_placement_texts_referenceOrder[other_placement_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_placement_text was not staged and does not have a reference order", other_placement_text)
		return 0
	}
}

func (other_play *Other_play) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_play_stagedOrder[other_play]; ok {
		return order
	}
	if order, ok := stage.Other_plays_referenceOrder[other_play]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_play was not staged and does not have a reference order", other_play)
		return 0
	}
}

func (other_text *Other_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Other_text_stagedOrder[other_text]; ok {
		return order
	}
	if order, ok := stage.Other_texts_referenceOrder[other_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Other_text was not staged and does not have a reference order", other_text)
		return 0
	}
}

func (page_layout *Page_layout) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Page_layout_stagedOrder[page_layout]; ok {
		return order
	}
	if order, ok := stage.Page_layouts_referenceOrder[page_layout]; ok {
		return order
	} else {
		log.Printf("instance %p of type Page_layout was not staged and does not have a reference order", page_layout)
		return 0
	}
}

func (page_margins *Page_margins) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Page_margins_stagedOrder[page_margins]; ok {
		return order
	}
	if order, ok := stage.Page_marginss_referenceOrder[page_margins]; ok {
		return order
	} else {
		log.Printf("instance %p of type Page_margins was not staged and does not have a reference order", page_margins)
		return 0
	}
}

func (part_clef *Part_clef) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_clef_stagedOrder[part_clef]; ok {
		return order
	}
	if order, ok := stage.Part_clefs_referenceOrder[part_clef]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_clef was not staged and does not have a reference order", part_clef)
		return 0
	}
}

func (part_group *Part_group) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_group_stagedOrder[part_group]; ok {
		return order
	}
	if order, ok := stage.Part_groups_referenceOrder[part_group]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_group was not staged and does not have a reference order", part_group)
		return 0
	}
}

func (part_link *Part_link) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_link_stagedOrder[part_link]; ok {
		return order
	}
	if order, ok := stage.Part_links_referenceOrder[part_link]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_link was not staged and does not have a reference order", part_link)
		return 0
	}
}

func (part_list *Part_list) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_list_stagedOrder[part_list]; ok {
		return order
	}
	if order, ok := stage.Part_lists_referenceOrder[part_list]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_list was not staged and does not have a reference order", part_list)
		return 0
	}
}

func (part_name *Part_name) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_name_stagedOrder[part_name]; ok {
		return order
	}
	if order, ok := stage.Part_names_referenceOrder[part_name]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_name was not staged and does not have a reference order", part_name)
		return 0
	}
}

func (part_symbol *Part_symbol) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_symbol_stagedOrder[part_symbol]; ok {
		return order
	}
	if order, ok := stage.Part_symbols_referenceOrder[part_symbol]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_symbol was not staged and does not have a reference order", part_symbol)
		return 0
	}
}

func (part_transpose *Part_transpose) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Part_transpose_stagedOrder[part_transpose]; ok {
		return order
	}
	if order, ok := stage.Part_transposes_referenceOrder[part_transpose]; ok {
		return order
	} else {
		log.Printf("instance %p of type Part_transpose was not staged and does not have a reference order", part_transpose)
		return 0
	}
}

func (pedal *Pedal) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Pedal_stagedOrder[pedal]; ok {
		return order
	}
	if order, ok := stage.Pedals_referenceOrder[pedal]; ok {
		return order
	} else {
		log.Printf("instance %p of type Pedal was not staged and does not have a reference order", pedal)
		return 0
	}
}

func (pedal_tuning *Pedal_tuning) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Pedal_tuning_stagedOrder[pedal_tuning]; ok {
		return order
	}
	if order, ok := stage.Pedal_tunings_referenceOrder[pedal_tuning]; ok {
		return order
	} else {
		log.Printf("instance %p of type Pedal_tuning was not staged and does not have a reference order", pedal_tuning)
		return 0
	}
}

func (per_minute *Per_minute) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Per_minute_stagedOrder[per_minute]; ok {
		return order
	}
	if order, ok := stage.Per_minutes_referenceOrder[per_minute]; ok {
		return order
	} else {
		log.Printf("instance %p of type Per_minute was not staged and does not have a reference order", per_minute)
		return 0
	}
}

func (percussion *Percussion) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Percussion_stagedOrder[percussion]; ok {
		return order
	}
	if order, ok := stage.Percussions_referenceOrder[percussion]; ok {
		return order
	} else {
		log.Printf("instance %p of type Percussion was not staged and does not have a reference order", percussion)
		return 0
	}
}

func (pitch *Pitch) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Pitch_stagedOrder[pitch]; ok {
		return order
	}
	if order, ok := stage.Pitchs_referenceOrder[pitch]; ok {
		return order
	} else {
		log.Printf("instance %p of type Pitch was not staged and does not have a reference order", pitch)
		return 0
	}
}

func (pitched *Pitched) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Pitched_stagedOrder[pitched]; ok {
		return order
	}
	if order, ok := stage.Pitcheds_referenceOrder[pitched]; ok {
		return order
	} else {
		log.Printf("instance %p of type Pitched was not staged and does not have a reference order", pitched)
		return 0
	}
}

func (placement_text *Placement_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Placement_text_stagedOrder[placement_text]; ok {
		return order
	}
	if order, ok := stage.Placement_texts_referenceOrder[placement_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Placement_text was not staged and does not have a reference order", placement_text)
		return 0
	}
}

func (play *Play) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Play_stagedOrder[play]; ok {
		return order
	}
	if order, ok := stage.Plays_referenceOrder[play]; ok {
		return order
	} else {
		log.Printf("instance %p of type Play was not staged and does not have a reference order", play)
		return 0
	}
}

func (player *Player) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Player_stagedOrder[player]; ok {
		return order
	}
	if order, ok := stage.Players_referenceOrder[player]; ok {
		return order
	} else {
		log.Printf("instance %p of type Player was not staged and does not have a reference order", player)
		return 0
	}
}

func (principal_voice *Principal_voice) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Principal_voice_stagedOrder[principal_voice]; ok {
		return order
	}
	if order, ok := stage.Principal_voices_referenceOrder[principal_voice]; ok {
		return order
	} else {
		log.Printf("instance %p of type Principal_voice was not staged and does not have a reference order", principal_voice)
		return 0
	}
}

func (print *Print) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Print_stagedOrder[print]; ok {
		return order
	}
	if order, ok := stage.Prints_referenceOrder[print]; ok {
		return order
	} else {
		log.Printf("instance %p of type Print was not staged and does not have a reference order", print)
		return 0
	}
}

func (release *Release) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Release_stagedOrder[release]; ok {
		return order
	}
	if order, ok := stage.Releases_referenceOrder[release]; ok {
		return order
	} else {
		log.Printf("instance %p of type Release was not staged and does not have a reference order", release)
		return 0
	}
}

func (repeat *Repeat) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Repeat_stagedOrder[repeat]; ok {
		return order
	}
	if order, ok := stage.Repeats_referenceOrder[repeat]; ok {
		return order
	} else {
		log.Printf("instance %p of type Repeat was not staged and does not have a reference order", repeat)
		return 0
	}
}

func (rest *Rest) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Rest_stagedOrder[rest]; ok {
		return order
	}
	if order, ok := stage.Rests_referenceOrder[rest]; ok {
		return order
	} else {
		log.Printf("instance %p of type Rest was not staged and does not have a reference order", rest)
		return 0
	}
}

func (root *Root) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Root_stagedOrder[root]; ok {
		return order
	}
	if order, ok := stage.Roots_referenceOrder[root]; ok {
		return order
	} else {
		log.Printf("instance %p of type Root was not staged and does not have a reference order", root)
		return 0
	}
}

func (root_step *Root_step) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Root_step_stagedOrder[root_step]; ok {
		return order
	}
	if order, ok := stage.Root_steps_referenceOrder[root_step]; ok {
		return order
	} else {
		log.Printf("instance %p of type Root_step was not staged and does not have a reference order", root_step)
		return 0
	}
}

func (scaling *Scaling) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Scaling_stagedOrder[scaling]; ok {
		return order
	}
	if order, ok := stage.Scalings_referenceOrder[scaling]; ok {
		return order
	} else {
		log.Printf("instance %p of type Scaling was not staged and does not have a reference order", scaling)
		return 0
	}
}

func (scordatura *Scordatura) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Scordatura_stagedOrder[scordatura]; ok {
		return order
	}
	if order, ok := stage.Scordaturas_referenceOrder[scordatura]; ok {
		return order
	} else {
		log.Printf("instance %p of type Scordatura was not staged and does not have a reference order", scordatura)
		return 0
	}
}

func (score_instrument *Score_instrument) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Score_instrument_stagedOrder[score_instrument]; ok {
		return order
	}
	if order, ok := stage.Score_instruments_referenceOrder[score_instrument]; ok {
		return order
	} else {
		log.Printf("instance %p of type Score_instrument was not staged and does not have a reference order", score_instrument)
		return 0
	}
}

func (score_part *Score_part) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Score_part_stagedOrder[score_part]; ok {
		return order
	}
	if order, ok := stage.Score_parts_referenceOrder[score_part]; ok {
		return order
	} else {
		log.Printf("instance %p of type Score_part was not staged and does not have a reference order", score_part)
		return 0
	}
}

func (score_partwise *Score_partwise) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Score_partwise_stagedOrder[score_partwise]; ok {
		return order
	}
	if order, ok := stage.Score_partwises_referenceOrder[score_partwise]; ok {
		return order
	} else {
		log.Printf("instance %p of type Score_partwise was not staged and does not have a reference order", score_partwise)
		return 0
	}
}

func (score_timewise *Score_timewise) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Score_timewise_stagedOrder[score_timewise]; ok {
		return order
	}
	if order, ok := stage.Score_timewises_referenceOrder[score_timewise]; ok {
		return order
	} else {
		log.Printf("instance %p of type Score_timewise was not staged and does not have a reference order", score_timewise)
		return 0
	}
}

func (segno *Segno) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Segno_stagedOrder[segno]; ok {
		return order
	}
	if order, ok := stage.Segnos_referenceOrder[segno]; ok {
		return order
	} else {
		log.Printf("instance %p of type Segno was not staged and does not have a reference order", segno)
		return 0
	}
}

func (slash *Slash) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Slash_stagedOrder[slash]; ok {
		return order
	}
	if order, ok := stage.Slashs_referenceOrder[slash]; ok {
		return order
	} else {
		log.Printf("instance %p of type Slash was not staged and does not have a reference order", slash)
		return 0
	}
}

func (slide *Slide) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Slide_stagedOrder[slide]; ok {
		return order
	}
	if order, ok := stage.Slides_referenceOrder[slide]; ok {
		return order
	} else {
		log.Printf("instance %p of type Slide was not staged and does not have a reference order", slide)
		return 0
	}
}

func (slur *Slur) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Slur_stagedOrder[slur]; ok {
		return order
	}
	if order, ok := stage.Slurs_referenceOrder[slur]; ok {
		return order
	} else {
		log.Printf("instance %p of type Slur was not staged and does not have a reference order", slur)
		return 0
	}
}

func (sound *Sound) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Sound_stagedOrder[sound]; ok {
		return order
	}
	if order, ok := stage.Sounds_referenceOrder[sound]; ok {
		return order
	} else {
		log.Printf("instance %p of type Sound was not staged and does not have a reference order", sound)
		return 0
	}
}

func (staff_details *Staff_details) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Staff_details_stagedOrder[staff_details]; ok {
		return order
	}
	if order, ok := stage.Staff_detailss_referenceOrder[staff_details]; ok {
		return order
	} else {
		log.Printf("instance %p of type Staff_details was not staged and does not have a reference order", staff_details)
		return 0
	}
}

func (staff_divide *Staff_divide) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Staff_divide_stagedOrder[staff_divide]; ok {
		return order
	}
	if order, ok := stage.Staff_divides_referenceOrder[staff_divide]; ok {
		return order
	} else {
		log.Printf("instance %p of type Staff_divide was not staged and does not have a reference order", staff_divide)
		return 0
	}
}

func (staff_layout *Staff_layout) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Staff_layout_stagedOrder[staff_layout]; ok {
		return order
	}
	if order, ok := stage.Staff_layouts_referenceOrder[staff_layout]; ok {
		return order
	} else {
		log.Printf("instance %p of type Staff_layout was not staged and does not have a reference order", staff_layout)
		return 0
	}
}

func (staff_size *Staff_size) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Staff_size_stagedOrder[staff_size]; ok {
		return order
	}
	if order, ok := stage.Staff_sizes_referenceOrder[staff_size]; ok {
		return order
	} else {
		log.Printf("instance %p of type Staff_size was not staged and does not have a reference order", staff_size)
		return 0
	}
}

func (staff_tuning *Staff_tuning) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Staff_tuning_stagedOrder[staff_tuning]; ok {
		return order
	}
	if order, ok := stage.Staff_tunings_referenceOrder[staff_tuning]; ok {
		return order
	} else {
		log.Printf("instance %p of type Staff_tuning was not staged and does not have a reference order", staff_tuning)
		return 0
	}
}

func (stem *Stem) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Stem_stagedOrder[stem]; ok {
		return order
	}
	if order, ok := stage.Stems_referenceOrder[stem]; ok {
		return order
	} else {
		log.Printf("instance %p of type Stem was not staged and does not have a reference order", stem)
		return 0
	}
}

func (stick *Stick) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Stick_stagedOrder[stick]; ok {
		return order
	}
	if order, ok := stage.Sticks_referenceOrder[stick]; ok {
		return order
	} else {
		log.Printf("instance %p of type Stick was not staged and does not have a reference order", stick)
		return 0
	}
}

func (string_mute *String_mute) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.String_mute_stagedOrder[string_mute]; ok {
		return order
	}
	if order, ok := stage.String_mutes_referenceOrder[string_mute]; ok {
		return order
	} else {
		log.Printf("instance %p of type String_mute was not staged and does not have a reference order", string_mute)
		return 0
	}
}

func (string_type *String_type) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.String_type_stagedOrder[string_type]; ok {
		return order
	}
	if order, ok := stage.String_types_referenceOrder[string_type]; ok {
		return order
	} else {
		log.Printf("instance %p of type String_type was not staged and does not have a reference order", string_type)
		return 0
	}
}

func (strong_accent *Strong_accent) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Strong_accent_stagedOrder[strong_accent]; ok {
		return order
	}
	if order, ok := stage.Strong_accents_referenceOrder[strong_accent]; ok {
		return order
	} else {
		log.Printf("instance %p of type Strong_accent was not staged and does not have a reference order", strong_accent)
		return 0
	}
}

func (style_text *Style_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Style_text_stagedOrder[style_text]; ok {
		return order
	}
	if order, ok := stage.Style_texts_referenceOrder[style_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Style_text was not staged and does not have a reference order", style_text)
		return 0
	}
}

func (supports *Supports) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Supports_stagedOrder[supports]; ok {
		return order
	}
	if order, ok := stage.Supportss_referenceOrder[supports]; ok {
		return order
	} else {
		log.Printf("instance %p of type Supports was not staged and does not have a reference order", supports)
		return 0
	}
}

func (swing *Swing) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Swing_stagedOrder[swing]; ok {
		return order
	}
	if order, ok := stage.Swings_referenceOrder[swing]; ok {
		return order
	} else {
		log.Printf("instance %p of type Swing was not staged and does not have a reference order", swing)
		return 0
	}
}

func (sync *Sync) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Sync_stagedOrder[sync]; ok {
		return order
	}
	if order, ok := stage.Syncs_referenceOrder[sync]; ok {
		return order
	} else {
		log.Printf("instance %p of type Sync was not staged and does not have a reference order", sync)
		return 0
	}
}

func (system_dividers *System_dividers) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.System_dividers_stagedOrder[system_dividers]; ok {
		return order
	}
	if order, ok := stage.System_dividerss_referenceOrder[system_dividers]; ok {
		return order
	} else {
		log.Printf("instance %p of type System_dividers was not staged and does not have a reference order", system_dividers)
		return 0
	}
}

func (system_layout *System_layout) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.System_layout_stagedOrder[system_layout]; ok {
		return order
	}
	if order, ok := stage.System_layouts_referenceOrder[system_layout]; ok {
		return order
	} else {
		log.Printf("instance %p of type System_layout was not staged and does not have a reference order", system_layout)
		return 0
	}
}

func (system_margins *System_margins) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.System_margins_stagedOrder[system_margins]; ok {
		return order
	}
	if order, ok := stage.System_marginss_referenceOrder[system_margins]; ok {
		return order
	} else {
		log.Printf("instance %p of type System_margins was not staged and does not have a reference order", system_margins)
		return 0
	}
}

func (tap *Tap) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tap_stagedOrder[tap]; ok {
		return order
	}
	if order, ok := stage.Taps_referenceOrder[tap]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tap was not staged and does not have a reference order", tap)
		return 0
	}
}

func (technical *Technical) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Technical_stagedOrder[technical]; ok {
		return order
	}
	if order, ok := stage.Technicals_referenceOrder[technical]; ok {
		return order
	} else {
		log.Printf("instance %p of type Technical was not staged and does not have a reference order", technical)
		return 0
	}
}

func (text_element_data *Text_element_data) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Text_element_data_stagedOrder[text_element_data]; ok {
		return order
	}
	if order, ok := stage.Text_element_datas_referenceOrder[text_element_data]; ok {
		return order
	} else {
		log.Printf("instance %p of type Text_element_data was not staged and does not have a reference order", text_element_data)
		return 0
	}
}

func (tie *Tie) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tie_stagedOrder[tie]; ok {
		return order
	}
	if order, ok := stage.Ties_referenceOrder[tie]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tie was not staged and does not have a reference order", tie)
		return 0
	}
}

func (tied *Tied) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tied_stagedOrder[tied]; ok {
		return order
	}
	if order, ok := stage.Tieds_referenceOrder[tied]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tied was not staged and does not have a reference order", tied)
		return 0
	}
}

func (time *Time) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Time_stagedOrder[time]; ok {
		return order
	}
	if order, ok := stage.Times_referenceOrder[time]; ok {
		return order
	} else {
		log.Printf("instance %p of type Time was not staged and does not have a reference order", time)
		return 0
	}
}

func (time_modification *Time_modification) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Time_modification_stagedOrder[time_modification]; ok {
		return order
	}
	if order, ok := stage.Time_modifications_referenceOrder[time_modification]; ok {
		return order
	} else {
		log.Printf("instance %p of type Time_modification was not staged and does not have a reference order", time_modification)
		return 0
	}
}

func (timpani *Timpani) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Timpani_stagedOrder[timpani]; ok {
		return order
	}
	if order, ok := stage.Timpanis_referenceOrder[timpani]; ok {
		return order
	} else {
		log.Printf("instance %p of type Timpani was not staged and does not have a reference order", timpani)
		return 0
	}
}

func (transpose *Transpose) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Transpose_stagedOrder[transpose]; ok {
		return order
	}
	if order, ok := stage.Transposes_referenceOrder[transpose]; ok {
		return order
	} else {
		log.Printf("instance %p of type Transpose was not staged and does not have a reference order", transpose)
		return 0
	}
}

func (tremolo *Tremolo) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tremolo_stagedOrder[tremolo]; ok {
		return order
	}
	if order, ok := stage.Tremolos_referenceOrder[tremolo]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tremolo was not staged and does not have a reference order", tremolo)
		return 0
	}
}

func (tuplet *Tuplet) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tuplet_stagedOrder[tuplet]; ok {
		return order
	}
	if order, ok := stage.Tuplets_referenceOrder[tuplet]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tuplet was not staged and does not have a reference order", tuplet)
		return 0
	}
}

func (tuplet_dot *Tuplet_dot) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tuplet_dot_stagedOrder[tuplet_dot]; ok {
		return order
	}
	if order, ok := stage.Tuplet_dots_referenceOrder[tuplet_dot]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tuplet_dot was not staged and does not have a reference order", tuplet_dot)
		return 0
	}
}

func (tuplet_number *Tuplet_number) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tuplet_number_stagedOrder[tuplet_number]; ok {
		return order
	}
	if order, ok := stage.Tuplet_numbers_referenceOrder[tuplet_number]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tuplet_number was not staged and does not have a reference order", tuplet_number)
		return 0
	}
}

func (tuplet_portion *Tuplet_portion) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tuplet_portion_stagedOrder[tuplet_portion]; ok {
		return order
	}
	if order, ok := stage.Tuplet_portions_referenceOrder[tuplet_portion]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tuplet_portion was not staged and does not have a reference order", tuplet_portion)
		return 0
	}
}

func (tuplet_type *Tuplet_type) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Tuplet_type_stagedOrder[tuplet_type]; ok {
		return order
	}
	if order, ok := stage.Tuplet_types_referenceOrder[tuplet_type]; ok {
		return order
	} else {
		log.Printf("instance %p of type Tuplet_type was not staged and does not have a reference order", tuplet_type)
		return 0
	}
}

func (typed_text *Typed_text) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Typed_text_stagedOrder[typed_text]; ok {
		return order
	}
	if order, ok := stage.Typed_texts_referenceOrder[typed_text]; ok {
		return order
	} else {
		log.Printf("instance %p of type Typed_text was not staged and does not have a reference order", typed_text)
		return 0
	}
}

func (unpitched *Unpitched) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Unpitched_stagedOrder[unpitched]; ok {
		return order
	}
	if order, ok := stage.Unpitcheds_referenceOrder[unpitched]; ok {
		return order
	} else {
		log.Printf("instance %p of type Unpitched was not staged and does not have a reference order", unpitched)
		return 0
	}
}

func (virtual_instrument *Virtual_instrument) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Virtual_instrument_stagedOrder[virtual_instrument]; ok {
		return order
	}
	if order, ok := stage.Virtual_instruments_referenceOrder[virtual_instrument]; ok {
		return order
	} else {
		log.Printf("instance %p of type Virtual_instrument was not staged and does not have a reference order", virtual_instrument)
		return 0
	}
}

func (wait *Wait) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Wait_stagedOrder[wait]; ok {
		return order
	}
	if order, ok := stage.Waits_referenceOrder[wait]; ok {
		return order
	} else {
		log.Printf("instance %p of type Wait was not staged and does not have a reference order", wait)
		return 0
	}
}

func (wavy_line *Wavy_line) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Wavy_line_stagedOrder[wavy_line]; ok {
		return order
	}
	if order, ok := stage.Wavy_lines_referenceOrder[wavy_line]; ok {
		return order
	} else {
		log.Printf("instance %p of type Wavy_line was not staged and does not have a reference order", wavy_line)
		return 0
	}
}

func (wedge *Wedge) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Wedge_stagedOrder[wedge]; ok {
		return order
	}
	if order, ok := stage.Wedges_referenceOrder[wedge]; ok {
		return order
	} else {
		log.Printf("instance %p of type Wedge was not staged and does not have a reference order", wedge)
		return 0
	}
}

func (wood *Wood) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Wood_stagedOrder[wood]; ok {
		return order
	}
	if order, ok := stage.Woods_referenceOrder[wood]; ok {
		return order
	} else {
		log.Printf("instance %p of type Wood was not staged and does not have a reference order", wood)
		return 0
	}
}

func (work *Work) GongGetOrder(stage *Stage) uint {
	if order, ok := stage.Work_stagedOrder[work]; ok {
		return order
	}
	if order, ok := stage.Works_referenceOrder[work]; ok {
		return order
	} else {
		log.Printf("instance %p of type Work was not staged and does not have a reference order", work)
		return 0
	}
}

// GongGetIdentifier returns a unique identifier of the instance in the staging area
// This identifier is composed of the Gongstruct name and the order of the instance
// in the staging area
// It is used to identify instances across sessions
// insertion point per named struct
func (a_directive *A_directive) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_directive.GongGetGongstructName(), a_directive.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (a_directive *A_directive) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_directive.GongGetGongstructName(), a_directive.GongGetOrder(stage))
}

func (a_measure *A_measure) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_measure.GongGetGongstructName(), a_measure.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (a_measure *A_measure) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_measure.GongGetGongstructName(), a_measure.GongGetOrder(stage))
}

func (a_measure_1 *A_measure_1) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_measure_1.GongGetGongstructName(), a_measure_1.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (a_measure_1 *A_measure_1) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_measure_1.GongGetGongstructName(), a_measure_1.GongGetOrder(stage))
}

func (a_part *A_part) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_part.GongGetGongstructName(), a_part.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (a_part *A_part) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_part.GongGetGongstructName(), a_part.GongGetOrder(stage))
}

func (a_part_1 *A_part_1) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_part_1.GongGetGongstructName(), a_part_1.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (a_part_1 *A_part_1) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", a_part_1.GongGetGongstructName(), a_part_1.GongGetOrder(stage))
}

func (accidental *Accidental) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental.GongGetGongstructName(), accidental.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (accidental *Accidental) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental.GongGetGongstructName(), accidental.GongGetOrder(stage))
}

func (accidental_mark *Accidental_mark) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental_mark.GongGetGongstructName(), accidental_mark.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (accidental_mark *Accidental_mark) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental_mark.GongGetGongstructName(), accidental_mark.GongGetOrder(stage))
}

func (accidental_text *Accidental_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental_text.GongGetGongstructName(), accidental_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (accidental_text *Accidental_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accidental_text.GongGetGongstructName(), accidental_text.GongGetOrder(stage))
}

func (accord *Accord) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accord.GongGetGongstructName(), accord.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (accord *Accord) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accord.GongGetGongstructName(), accord.GongGetOrder(stage))
}

func (accordion_registration *Accordion_registration) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accordion_registration.GongGetGongstructName(), accordion_registration.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (accordion_registration *Accordion_registration) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", accordion_registration.GongGetGongstructName(), accordion_registration.GongGetOrder(stage))
}

func (appearance *Appearance) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", appearance.GongGetGongstructName(), appearance.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (appearance *Appearance) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", appearance.GongGetGongstructName(), appearance.GongGetOrder(stage))
}

func (arpeggiate *Arpeggiate) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", arpeggiate.GongGetGongstructName(), arpeggiate.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (arpeggiate *Arpeggiate) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", arpeggiate.GongGetGongstructName(), arpeggiate.GongGetOrder(stage))
}

func (arrow *Arrow) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", arrow.GongGetGongstructName(), arrow.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (arrow *Arrow) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", arrow.GongGetGongstructName(), arrow.GongGetOrder(stage))
}

func (articulations *Articulations) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", articulations.GongGetGongstructName(), articulations.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (articulations *Articulations) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", articulations.GongGetGongstructName(), articulations.GongGetOrder(stage))
}

func (assess *Assess) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", assess.GongGetGongstructName(), assess.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (assess *Assess) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", assess.GongGetGongstructName(), assess.GongGetOrder(stage))
}

func (attributes *Attributes) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", attributes.GongGetGongstructName(), attributes.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (attributes *Attributes) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", attributes.GongGetGongstructName(), attributes.GongGetOrder(stage))
}

func (backup *Backup) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", backup.GongGetGongstructName(), backup.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (backup *Backup) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", backup.GongGetGongstructName(), backup.GongGetOrder(stage))
}

func (bar_style_color *Bar_style_color) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bar_style_color.GongGetGongstructName(), bar_style_color.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bar_style_color *Bar_style_color) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bar_style_color.GongGetGongstructName(), bar_style_color.GongGetOrder(stage))
}

func (barline *Barline) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", barline.GongGetGongstructName(), barline.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (barline *Barline) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", barline.GongGetGongstructName(), barline.GongGetOrder(stage))
}

func (barre *Barre) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", barre.GongGetGongstructName(), barre.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (barre *Barre) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", barre.GongGetGongstructName(), barre.GongGetOrder(stage))
}

func (bass *Bass) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bass.GongGetGongstructName(), bass.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bass *Bass) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bass.GongGetGongstructName(), bass.GongGetOrder(stage))
}

func (bass_step *Bass_step) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bass_step.GongGetGongstructName(), bass_step.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bass_step *Bass_step) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bass_step.GongGetGongstructName(), bass_step.GongGetOrder(stage))
}

func (beam *Beam) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beam.GongGetGongstructName(), beam.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (beam *Beam) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beam.GongGetGongstructName(), beam.GongGetOrder(stage))
}

func (beat_repeat *Beat_repeat) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beat_repeat.GongGetGongstructName(), beat_repeat.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (beat_repeat *Beat_repeat) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beat_repeat.GongGetGongstructName(), beat_repeat.GongGetOrder(stage))
}

func (beat_unit_tied *Beat_unit_tied) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beat_unit_tied.GongGetGongstructName(), beat_unit_tied.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (beat_unit_tied *Beat_unit_tied) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beat_unit_tied.GongGetGongstructName(), beat_unit_tied.GongGetOrder(stage))
}

func (beater *Beater) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beater.GongGetGongstructName(), beater.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (beater *Beater) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", beater.GongGetGongstructName(), beater.GongGetOrder(stage))
}

func (bend *Bend) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bend.GongGetGongstructName(), bend.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bend *Bend) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bend.GongGetGongstructName(), bend.GongGetOrder(stage))
}

func (bookmark *Bookmark) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bookmark.GongGetGongstructName(), bookmark.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bookmark *Bookmark) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bookmark.GongGetGongstructName(), bookmark.GongGetOrder(stage))
}

func (bracket *Bracket) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bracket.GongGetGongstructName(), bracket.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (bracket *Bracket) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", bracket.GongGetGongstructName(), bracket.GongGetOrder(stage))
}

func (breath_mark *Breath_mark) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", breath_mark.GongGetGongstructName(), breath_mark.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (breath_mark *Breath_mark) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", breath_mark.GongGetGongstructName(), breath_mark.GongGetOrder(stage))
}

func (caesura *Caesura) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", caesura.GongGetGongstructName(), caesura.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (caesura *Caesura) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", caesura.GongGetGongstructName(), caesura.GongGetOrder(stage))
}

func (cancel *Cancel) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", cancel.GongGetGongstructName(), cancel.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (cancel *Cancel) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", cancel.GongGetGongstructName(), cancel.GongGetOrder(stage))
}

func (clef *Clef) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", clef.GongGetGongstructName(), clef.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (clef *Clef) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", clef.GongGetGongstructName(), clef.GongGetOrder(stage))
}

func (coda *Coda) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", coda.GongGetGongstructName(), coda.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (coda *Coda) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", coda.GongGetGongstructName(), coda.GongGetOrder(stage))
}

func (credit *Credit) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", credit.GongGetGongstructName(), credit.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (credit *Credit) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", credit.GongGetGongstructName(), credit.GongGetOrder(stage))
}

func (dashes *Dashes) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", dashes.GongGetGongstructName(), dashes.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (dashes *Dashes) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", dashes.GongGetGongstructName(), dashes.GongGetOrder(stage))
}

func (defaults *Defaults) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", defaults.GongGetGongstructName(), defaults.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (defaults *Defaults) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", defaults.GongGetGongstructName(), defaults.GongGetOrder(stage))
}

func (degree *Degree) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree.GongGetGongstructName(), degree.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (degree *Degree) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree.GongGetGongstructName(), degree.GongGetOrder(stage))
}

func (degree_alter *Degree_alter) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_alter.GongGetGongstructName(), degree_alter.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (degree_alter *Degree_alter) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_alter.GongGetGongstructName(), degree_alter.GongGetOrder(stage))
}

func (degree_type *Degree_type) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_type.GongGetGongstructName(), degree_type.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (degree_type *Degree_type) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_type.GongGetGongstructName(), degree_type.GongGetOrder(stage))
}

func (degree_value *Degree_value) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_value.GongGetGongstructName(), degree_value.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (degree_value *Degree_value) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", degree_value.GongGetGongstructName(), degree_value.GongGetOrder(stage))
}

func (direction *Direction) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", direction.GongGetGongstructName(), direction.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (direction *Direction) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", direction.GongGetGongstructName(), direction.GongGetOrder(stage))
}

func (direction_type *Direction_type) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", direction_type.GongGetGongstructName(), direction_type.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (direction_type *Direction_type) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", direction_type.GongGetGongstructName(), direction_type.GongGetOrder(stage))
}

func (distance *Distance) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", distance.GongGetGongstructName(), distance.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (distance *Distance) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", distance.GongGetGongstructName(), distance.GongGetOrder(stage))
}

func (double *Double) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", double.GongGetGongstructName(), double.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (double *Double) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", double.GongGetGongstructName(), double.GongGetOrder(stage))
}

func (dynamics *Dynamics) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", dynamics.GongGetGongstructName(), dynamics.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (dynamics *Dynamics) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", dynamics.GongGetGongstructName(), dynamics.GongGetOrder(stage))
}

func (effect *Effect) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", effect.GongGetGongstructName(), effect.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (effect *Effect) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", effect.GongGetGongstructName(), effect.GongGetOrder(stage))
}

func (elision *Elision) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", elision.GongGetGongstructName(), elision.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (elision *Elision) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", elision.GongGetGongstructName(), elision.GongGetOrder(stage))
}

func (empty *Empty) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty.GongGetGongstructName(), empty.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty *Empty) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty.GongGetGongstructName(), empty.GongGetOrder(stage))
}

func (empty_font *Empty_font) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_font.GongGetGongstructName(), empty_font.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_font *Empty_font) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_font.GongGetGongstructName(), empty_font.GongGetOrder(stage))
}

func (empty_line *Empty_line) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_line.GongGetGongstructName(), empty_line.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_line *Empty_line) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_line.GongGetGongstructName(), empty_line.GongGetOrder(stage))
}

func (empty_placement *Empty_placement) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_placement.GongGetGongstructName(), empty_placement.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_placement *Empty_placement) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_placement.GongGetGongstructName(), empty_placement.GongGetOrder(stage))
}

func (empty_placement_smufl *Empty_placement_smufl) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_placement_smufl.GongGetGongstructName(), empty_placement_smufl.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_placement_smufl *Empty_placement_smufl) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_placement_smufl.GongGetGongstructName(), empty_placement_smufl.GongGetOrder(stage))
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_object_style_align.GongGetGongstructName(), empty_print_object_style_align.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_print_object_style_align *Empty_print_object_style_align) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_object_style_align.GongGetGongstructName(), empty_print_object_style_align.GongGetOrder(stage))
}

func (empty_print_style *Empty_print_style) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style.GongGetGongstructName(), empty_print_style.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_print_style *Empty_print_style) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style.GongGetGongstructName(), empty_print_style.GongGetOrder(stage))
}

func (empty_print_style_align *Empty_print_style_align) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style_align.GongGetGongstructName(), empty_print_style_align.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_print_style_align *Empty_print_style_align) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style_align.GongGetGongstructName(), empty_print_style_align.GongGetOrder(stage))
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style_align_id.GongGetGongstructName(), empty_print_style_align_id.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_print_style_align_id *Empty_print_style_align_id) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_print_style_align_id.GongGetGongstructName(), empty_print_style_align_id.GongGetOrder(stage))
}

func (empty_trill_sound *Empty_trill_sound) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_trill_sound.GongGetGongstructName(), empty_trill_sound.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (empty_trill_sound *Empty_trill_sound) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", empty_trill_sound.GongGetGongstructName(), empty_trill_sound.GongGetOrder(stage))
}

func (encoding *Encoding) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", encoding.GongGetGongstructName(), encoding.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (encoding *Encoding) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", encoding.GongGetGongstructName(), encoding.GongGetOrder(stage))
}

func (ending *Ending) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", ending.GongGetGongstructName(), ending.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (ending *Ending) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", ending.GongGetGongstructName(), ending.GongGetOrder(stage))
}

func (extend *Extend) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", extend.GongGetGongstructName(), extend.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (extend *Extend) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", extend.GongGetGongstructName(), extend.GongGetOrder(stage))
}

func (feature *Feature) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", feature.GongGetGongstructName(), feature.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (feature *Feature) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", feature.GongGetGongstructName(), feature.GongGetOrder(stage))
}

func (fermata *Fermata) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fermata.GongGetGongstructName(), fermata.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (fermata *Fermata) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fermata.GongGetGongstructName(), fermata.GongGetOrder(stage))
}

func (figure *Figure) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", figure.GongGetGongstructName(), figure.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (figure *Figure) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", figure.GongGetGongstructName(), figure.GongGetOrder(stage))
}

func (figured_bass *Figured_bass) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", figured_bass.GongGetGongstructName(), figured_bass.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (figured_bass *Figured_bass) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", figured_bass.GongGetGongstructName(), figured_bass.GongGetOrder(stage))
}

func (fingering *Fingering) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fingering.GongGetGongstructName(), fingering.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (fingering *Fingering) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fingering.GongGetGongstructName(), fingering.GongGetOrder(stage))
}

func (first_fret *First_fret) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", first_fret.GongGetGongstructName(), first_fret.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (first_fret *First_fret) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", first_fret.GongGetGongstructName(), first_fret.GongGetOrder(stage))
}

func (for_part *For_part) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", for_part.GongGetGongstructName(), for_part.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (for_part *For_part) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", for_part.GongGetGongstructName(), for_part.GongGetOrder(stage))
}

func (formatted_symbol *Formatted_symbol) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_symbol.GongGetGongstructName(), formatted_symbol.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (formatted_symbol *Formatted_symbol) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_symbol.GongGetGongstructName(), formatted_symbol.GongGetOrder(stage))
}

func (formatted_symbol_id *Formatted_symbol_id) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_symbol_id.GongGetGongstructName(), formatted_symbol_id.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (formatted_symbol_id *Formatted_symbol_id) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_symbol_id.GongGetGongstructName(), formatted_symbol_id.GongGetOrder(stage))
}

func (formatted_text *Formatted_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_text.GongGetGongstructName(), formatted_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (formatted_text *Formatted_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_text.GongGetGongstructName(), formatted_text.GongGetOrder(stage))
}

func (formatted_text_id *Formatted_text_id) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_text_id.GongGetGongstructName(), formatted_text_id.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (formatted_text_id *Formatted_text_id) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", formatted_text_id.GongGetGongstructName(), formatted_text_id.GongGetOrder(stage))
}

func (forward *Forward) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", forward.GongGetGongstructName(), forward.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (forward *Forward) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", forward.GongGetGongstructName(), forward.GongGetOrder(stage))
}

func (frame *Frame) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", frame.GongGetGongstructName(), frame.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (frame *Frame) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", frame.GongGetGongstructName(), frame.GongGetOrder(stage))
}

func (frame_note *Frame_note) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", frame_note.GongGetGongstructName(), frame_note.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (frame_note *Frame_note) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", frame_note.GongGetGongstructName(), frame_note.GongGetOrder(stage))
}

func (fret *Fret) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fret.GongGetGongstructName(), fret.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (fret *Fret) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", fret.GongGetGongstructName(), fret.GongGetOrder(stage))
}

func (glass *Glass) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glass.GongGetGongstructName(), glass.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (glass *Glass) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glass.GongGetGongstructName(), glass.GongGetOrder(stage))
}

func (glissando *Glissando) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glissando.GongGetGongstructName(), glissando.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (glissando *Glissando) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glissando.GongGetGongstructName(), glissando.GongGetOrder(stage))
}

func (glyph *Glyph) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glyph.GongGetGongstructName(), glyph.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (glyph *Glyph) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", glyph.GongGetGongstructName(), glyph.GongGetOrder(stage))
}

func (grace *Grace) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", grace.GongGetGongstructName(), grace.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (grace *Grace) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", grace.GongGetGongstructName(), grace.GongGetOrder(stage))
}

func (group_barline *Group_barline) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_barline.GongGetGongstructName(), group_barline.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (group_barline *Group_barline) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_barline.GongGetGongstructName(), group_barline.GongGetOrder(stage))
}

func (group_name *Group_name) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_name.GongGetGongstructName(), group_name.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (group_name *Group_name) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_name.GongGetGongstructName(), group_name.GongGetOrder(stage))
}

func (group_symbol *Group_symbol) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_symbol.GongGetGongstructName(), group_symbol.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (group_symbol *Group_symbol) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", group_symbol.GongGetGongstructName(), group_symbol.GongGetOrder(stage))
}

func (grouping *Grouping) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", grouping.GongGetGongstructName(), grouping.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (grouping *Grouping) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", grouping.GongGetGongstructName(), grouping.GongGetOrder(stage))
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hammer_on_pull_off.GongGetGongstructName(), hammer_on_pull_off.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (hammer_on_pull_off *Hammer_on_pull_off) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hammer_on_pull_off.GongGetGongstructName(), hammer_on_pull_off.GongGetOrder(stage))
}

func (handbell *Handbell) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", handbell.GongGetGongstructName(), handbell.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (handbell *Handbell) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", handbell.GongGetGongstructName(), handbell.GongGetOrder(stage))
}

func (harmon_closed *Harmon_closed) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmon_closed.GongGetGongstructName(), harmon_closed.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harmon_closed *Harmon_closed) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmon_closed.GongGetGongstructName(), harmon_closed.GongGetOrder(stage))
}

func (harmon_mute *Harmon_mute) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmon_mute.GongGetGongstructName(), harmon_mute.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harmon_mute *Harmon_mute) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmon_mute.GongGetGongstructName(), harmon_mute.GongGetOrder(stage))
}

func (harmonic *Harmonic) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmonic.GongGetGongstructName(), harmonic.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harmonic *Harmonic) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmonic.GongGetGongstructName(), harmonic.GongGetOrder(stage))
}

func (harmony *Harmony) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmony.GongGetGongstructName(), harmony.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harmony *Harmony) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmony.GongGetGongstructName(), harmony.GongGetOrder(stage))
}

func (harmony_alter *Harmony_alter) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmony_alter.GongGetGongstructName(), harmony_alter.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harmony_alter *Harmony_alter) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harmony_alter.GongGetGongstructName(), harmony_alter.GongGetOrder(stage))
}

func (harp_pedals *Harp_pedals) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harp_pedals.GongGetGongstructName(), harp_pedals.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (harp_pedals *Harp_pedals) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", harp_pedals.GongGetGongstructName(), harp_pedals.GongGetOrder(stage))
}

func (heel_toe *Heel_toe) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", heel_toe.GongGetGongstructName(), heel_toe.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (heel_toe *Heel_toe) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", heel_toe.GongGetGongstructName(), heel_toe.GongGetOrder(stage))
}

func (hole *Hole) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hole.GongGetGongstructName(), hole.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (hole *Hole) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hole.GongGetGongstructName(), hole.GongGetOrder(stage))
}

func (hole_closed *Hole_closed) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hole_closed.GongGetGongstructName(), hole_closed.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (hole_closed *Hole_closed) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", hole_closed.GongGetGongstructName(), hole_closed.GongGetOrder(stage))
}

func (horizontal_turn *Horizontal_turn) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", horizontal_turn.GongGetGongstructName(), horizontal_turn.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (horizontal_turn *Horizontal_turn) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", horizontal_turn.GongGetGongstructName(), horizontal_turn.GongGetOrder(stage))
}

func (identification *Identification) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", identification.GongGetGongstructName(), identification.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (identification *Identification) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", identification.GongGetGongstructName(), identification.GongGetOrder(stage))
}

func (image *Image) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", image.GongGetGongstructName(), image.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (image *Image) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", image.GongGetGongstructName(), image.GongGetOrder(stage))
}

func (instrument *Instrument) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument.GongGetGongstructName(), instrument.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (instrument *Instrument) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument.GongGetGongstructName(), instrument.GongGetOrder(stage))
}

func (instrument_change *Instrument_change) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument_change.GongGetGongstructName(), instrument_change.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (instrument_change *Instrument_change) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument_change.GongGetGongstructName(), instrument_change.GongGetOrder(stage))
}

func (instrument_link *Instrument_link) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument_link.GongGetGongstructName(), instrument_link.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (instrument_link *Instrument_link) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", instrument_link.GongGetGongstructName(), instrument_link.GongGetOrder(stage))
}

func (interchangeable *Interchangeable) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", interchangeable.GongGetGongstructName(), interchangeable.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (interchangeable *Interchangeable) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", interchangeable.GongGetGongstructName(), interchangeable.GongGetOrder(stage))
}

func (inversion *Inversion) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", inversion.GongGetGongstructName(), inversion.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (inversion *Inversion) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", inversion.GongGetGongstructName(), inversion.GongGetOrder(stage))
}

func (key *Key) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key.GongGetGongstructName(), key.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (key *Key) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key.GongGetGongstructName(), key.GongGetOrder(stage))
}

func (key_accidental *Key_accidental) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key_accidental.GongGetGongstructName(), key_accidental.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (key_accidental *Key_accidental) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key_accidental.GongGetGongstructName(), key_accidental.GongGetOrder(stage))
}

func (key_octave *Key_octave) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key_octave.GongGetGongstructName(), key_octave.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (key_octave *Key_octave) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", key_octave.GongGetGongstructName(), key_octave.GongGetOrder(stage))
}

func (kind *Kind) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", kind.GongGetGongstructName(), kind.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (kind *Kind) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", kind.GongGetGongstructName(), kind.GongGetOrder(stage))
}

func (level *Level) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", level.GongGetGongstructName(), level.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (level *Level) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", level.GongGetGongstructName(), level.GongGetOrder(stage))
}

func (line_detail *Line_detail) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", line_detail.GongGetGongstructName(), line_detail.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (line_detail *Line_detail) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", line_detail.GongGetGongstructName(), line_detail.GongGetOrder(stage))
}

func (line_width *Line_width) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", line_width.GongGetGongstructName(), line_width.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (line_width *Line_width) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", line_width.GongGetGongstructName(), line_width.GongGetOrder(stage))
}

func (link *Link) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", link.GongGetGongstructName(), link.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (link *Link) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", link.GongGetGongstructName(), link.GongGetOrder(stage))
}

func (listen *Listen) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", listen.GongGetGongstructName(), listen.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (listen *Listen) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", listen.GongGetGongstructName(), listen.GongGetOrder(stage))
}

func (listening *Listening) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", listening.GongGetGongstructName(), listening.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (listening *Listening) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", listening.GongGetGongstructName(), listening.GongGetOrder(stage))
}

func (lyric *Lyric) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric.GongGetGongstructName(), lyric.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (lyric *Lyric) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric.GongGetGongstructName(), lyric.GongGetOrder(stage))
}

func (lyric_font *Lyric_font) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric_font.GongGetGongstructName(), lyric_font.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (lyric_font *Lyric_font) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric_font.GongGetGongstructName(), lyric_font.GongGetOrder(stage))
}

func (lyric_language *Lyric_language) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric_language.GongGetGongstructName(), lyric_language.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (lyric_language *Lyric_language) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", lyric_language.GongGetGongstructName(), lyric_language.GongGetOrder(stage))
}

func (measure_layout *Measure_layout) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_layout.GongGetGongstructName(), measure_layout.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (measure_layout *Measure_layout) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_layout.GongGetGongstructName(), measure_layout.GongGetOrder(stage))
}

func (measure_numbering *Measure_numbering) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_numbering.GongGetGongstructName(), measure_numbering.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (measure_numbering *Measure_numbering) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_numbering.GongGetGongstructName(), measure_numbering.GongGetOrder(stage))
}

func (measure_repeat *Measure_repeat) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_repeat.GongGetGongstructName(), measure_repeat.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (measure_repeat *Measure_repeat) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_repeat.GongGetGongstructName(), measure_repeat.GongGetOrder(stage))
}

func (measure_style *Measure_style) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_style.GongGetGongstructName(), measure_style.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (measure_style *Measure_style) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", measure_style.GongGetGongstructName(), measure_style.GongGetOrder(stage))
}

func (membrane *Membrane) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", membrane.GongGetGongstructName(), membrane.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (membrane *Membrane) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", membrane.GongGetGongstructName(), membrane.GongGetOrder(stage))
}

func (metal *Metal) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metal.GongGetGongstructName(), metal.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metal *Metal) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metal.GongGetGongstructName(), metal.GongGetOrder(stage))
}

func (metronome *Metronome) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome.GongGetGongstructName(), metronome.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metronome *Metronome) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome.GongGetGongstructName(), metronome.GongGetOrder(stage))
}

func (metronome_beam *Metronome_beam) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_beam.GongGetGongstructName(), metronome_beam.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metronome_beam *Metronome_beam) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_beam.GongGetGongstructName(), metronome_beam.GongGetOrder(stage))
}

func (metronome_note *Metronome_note) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_note.GongGetGongstructName(), metronome_note.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metronome_note *Metronome_note) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_note.GongGetGongstructName(), metronome_note.GongGetOrder(stage))
}

func (metronome_tied *Metronome_tied) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_tied.GongGetGongstructName(), metronome_tied.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metronome_tied *Metronome_tied) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_tied.GongGetGongstructName(), metronome_tied.GongGetOrder(stage))
}

func (metronome_tuplet *Metronome_tuplet) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_tuplet.GongGetGongstructName(), metronome_tuplet.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (metronome_tuplet *Metronome_tuplet) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", metronome_tuplet.GongGetGongstructName(), metronome_tuplet.GongGetOrder(stage))
}

func (midi_device *Midi_device) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", midi_device.GongGetGongstructName(), midi_device.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (midi_device *Midi_device) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", midi_device.GongGetGongstructName(), midi_device.GongGetOrder(stage))
}

func (midi_instrument *Midi_instrument) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", midi_instrument.GongGetGongstructName(), midi_instrument.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (midi_instrument *Midi_instrument) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", midi_instrument.GongGetGongstructName(), midi_instrument.GongGetOrder(stage))
}

func (miscellaneous *Miscellaneous) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", miscellaneous.GongGetGongstructName(), miscellaneous.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (miscellaneous *Miscellaneous) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", miscellaneous.GongGetGongstructName(), miscellaneous.GongGetOrder(stage))
}

func (miscellaneous_field *Miscellaneous_field) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", miscellaneous_field.GongGetGongstructName(), miscellaneous_field.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (miscellaneous_field *Miscellaneous_field) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", miscellaneous_field.GongGetGongstructName(), miscellaneous_field.GongGetOrder(stage))
}

func (mordent *Mordent) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", mordent.GongGetGongstructName(), mordent.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (mordent *Mordent) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", mordent.GongGetGongstructName(), mordent.GongGetOrder(stage))
}

func (multiple_rest *Multiple_rest) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", multiple_rest.GongGetGongstructName(), multiple_rest.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (multiple_rest *Multiple_rest) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", multiple_rest.GongGetGongstructName(), multiple_rest.GongGetOrder(stage))
}

func (name_display *Name_display) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", name_display.GongGetGongstructName(), name_display.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (name_display *Name_display) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", name_display.GongGetGongstructName(), name_display.GongGetOrder(stage))
}

func (non_arpeggiate *Non_arpeggiate) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", non_arpeggiate.GongGetGongstructName(), non_arpeggiate.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (non_arpeggiate *Non_arpeggiate) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", non_arpeggiate.GongGetGongstructName(), non_arpeggiate.GongGetOrder(stage))
}

func (notations *Notations) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notations.GongGetGongstructName(), notations.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (notations *Notations) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notations.GongGetGongstructName(), notations.GongGetOrder(stage))
}

func (note *Note) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note.GongGetGongstructName(), note.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (note *Note) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note.GongGetGongstructName(), note.GongGetOrder(stage))
}

func (note_size *Note_size) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note_size.GongGetGongstructName(), note_size.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (note_size *Note_size) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note_size.GongGetGongstructName(), note_size.GongGetOrder(stage))
}

func (note_type *Note_type) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note_type.GongGetGongstructName(), note_type.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (note_type *Note_type) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", note_type.GongGetGongstructName(), note_type.GongGetOrder(stage))
}

func (notehead *Notehead) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notehead.GongGetGongstructName(), notehead.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (notehead *Notehead) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notehead.GongGetGongstructName(), notehead.GongGetOrder(stage))
}

func (notehead_text *Notehead_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notehead_text.GongGetGongstructName(), notehead_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (notehead_text *Notehead_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", notehead_text.GongGetGongstructName(), notehead_text.GongGetOrder(stage))
}

func (numeral *Numeral) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral.GongGetGongstructName(), numeral.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (numeral *Numeral) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral.GongGetGongstructName(), numeral.GongGetOrder(stage))
}

func (numeral_key *Numeral_key) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral_key.GongGetGongstructName(), numeral_key.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (numeral_key *Numeral_key) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral_key.GongGetGongstructName(), numeral_key.GongGetOrder(stage))
}

func (numeral_root *Numeral_root) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral_root.GongGetGongstructName(), numeral_root.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (numeral_root *Numeral_root) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", numeral_root.GongGetGongstructName(), numeral_root.GongGetOrder(stage))
}

func (octave_shift *Octave_shift) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", octave_shift.GongGetGongstructName(), octave_shift.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (octave_shift *Octave_shift) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", octave_shift.GongGetGongstructName(), octave_shift.GongGetOrder(stage))
}

func (offset *Offset) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", offset.GongGetGongstructName(), offset.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (offset *Offset) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", offset.GongGetGongstructName(), offset.GongGetOrder(stage))
}

func (opus *Opus) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", opus.GongGetGongstructName(), opus.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (opus *Opus) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", opus.GongGetGongstructName(), opus.GongGetOrder(stage))
}

func (ornaments *Ornaments) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", ornaments.GongGetGongstructName(), ornaments.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (ornaments *Ornaments) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", ornaments.GongGetGongstructName(), ornaments.GongGetOrder(stage))
}

func (other_appearance *Other_appearance) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_appearance.GongGetGongstructName(), other_appearance.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_appearance *Other_appearance) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_appearance.GongGetGongstructName(), other_appearance.GongGetOrder(stage))
}

func (other_direction *Other_direction) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_direction.GongGetGongstructName(), other_direction.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_direction *Other_direction) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_direction.GongGetGongstructName(), other_direction.GongGetOrder(stage))
}

func (other_listening *Other_listening) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_listening.GongGetGongstructName(), other_listening.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_listening *Other_listening) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_listening.GongGetGongstructName(), other_listening.GongGetOrder(stage))
}

func (other_notation *Other_notation) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_notation.GongGetGongstructName(), other_notation.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_notation *Other_notation) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_notation.GongGetGongstructName(), other_notation.GongGetOrder(stage))
}

func (other_placement_text *Other_placement_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_placement_text.GongGetGongstructName(), other_placement_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_placement_text *Other_placement_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_placement_text.GongGetGongstructName(), other_placement_text.GongGetOrder(stage))
}

func (other_play *Other_play) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_play.GongGetGongstructName(), other_play.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_play *Other_play) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_play.GongGetGongstructName(), other_play.GongGetOrder(stage))
}

func (other_text *Other_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_text.GongGetGongstructName(), other_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (other_text *Other_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", other_text.GongGetGongstructName(), other_text.GongGetOrder(stage))
}

func (page_layout *Page_layout) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", page_layout.GongGetGongstructName(), page_layout.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (page_layout *Page_layout) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", page_layout.GongGetGongstructName(), page_layout.GongGetOrder(stage))
}

func (page_margins *Page_margins) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", page_margins.GongGetGongstructName(), page_margins.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (page_margins *Page_margins) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", page_margins.GongGetGongstructName(), page_margins.GongGetOrder(stage))
}

func (part_clef *Part_clef) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_clef.GongGetGongstructName(), part_clef.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_clef *Part_clef) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_clef.GongGetGongstructName(), part_clef.GongGetOrder(stage))
}

func (part_group *Part_group) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_group.GongGetGongstructName(), part_group.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_group *Part_group) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_group.GongGetGongstructName(), part_group.GongGetOrder(stage))
}

func (part_link *Part_link) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_link.GongGetGongstructName(), part_link.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_link *Part_link) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_link.GongGetGongstructName(), part_link.GongGetOrder(stage))
}

func (part_list *Part_list) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_list.GongGetGongstructName(), part_list.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_list *Part_list) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_list.GongGetGongstructName(), part_list.GongGetOrder(stage))
}

func (part_name *Part_name) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_name.GongGetGongstructName(), part_name.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_name *Part_name) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_name.GongGetGongstructName(), part_name.GongGetOrder(stage))
}

func (part_symbol *Part_symbol) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_symbol.GongGetGongstructName(), part_symbol.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_symbol *Part_symbol) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_symbol.GongGetGongstructName(), part_symbol.GongGetOrder(stage))
}

func (part_transpose *Part_transpose) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_transpose.GongGetGongstructName(), part_transpose.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (part_transpose *Part_transpose) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", part_transpose.GongGetGongstructName(), part_transpose.GongGetOrder(stage))
}

func (pedal *Pedal) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pedal.GongGetGongstructName(), pedal.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (pedal *Pedal) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pedal.GongGetGongstructName(), pedal.GongGetOrder(stage))
}

func (pedal_tuning *Pedal_tuning) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pedal_tuning.GongGetGongstructName(), pedal_tuning.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (pedal_tuning *Pedal_tuning) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pedal_tuning.GongGetGongstructName(), pedal_tuning.GongGetOrder(stage))
}

func (per_minute *Per_minute) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", per_minute.GongGetGongstructName(), per_minute.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (per_minute *Per_minute) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", per_minute.GongGetGongstructName(), per_minute.GongGetOrder(stage))
}

func (percussion *Percussion) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", percussion.GongGetGongstructName(), percussion.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (percussion *Percussion) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", percussion.GongGetGongstructName(), percussion.GongGetOrder(stage))
}

func (pitch *Pitch) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pitch.GongGetGongstructName(), pitch.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (pitch *Pitch) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pitch.GongGetGongstructName(), pitch.GongGetOrder(stage))
}

func (pitched *Pitched) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pitched.GongGetGongstructName(), pitched.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (pitched *Pitched) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", pitched.GongGetGongstructName(), pitched.GongGetOrder(stage))
}

func (placement_text *Placement_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", placement_text.GongGetGongstructName(), placement_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (placement_text *Placement_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", placement_text.GongGetGongstructName(), placement_text.GongGetOrder(stage))
}

func (play *Play) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", play.GongGetGongstructName(), play.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (play *Play) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", play.GongGetGongstructName(), play.GongGetOrder(stage))
}

func (player *Player) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", player.GongGetGongstructName(), player.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (player *Player) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", player.GongGetGongstructName(), player.GongGetOrder(stage))
}

func (principal_voice *Principal_voice) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", principal_voice.GongGetGongstructName(), principal_voice.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (principal_voice *Principal_voice) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", principal_voice.GongGetGongstructName(), principal_voice.GongGetOrder(stage))
}

func (print *Print) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", print.GongGetGongstructName(), print.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (print *Print) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", print.GongGetGongstructName(), print.GongGetOrder(stage))
}

func (release *Release) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", release.GongGetGongstructName(), release.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (release *Release) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", release.GongGetGongstructName(), release.GongGetOrder(stage))
}

func (repeat *Repeat) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", repeat.GongGetGongstructName(), repeat.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (repeat *Repeat) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", repeat.GongGetGongstructName(), repeat.GongGetOrder(stage))
}

func (rest *Rest) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", rest.GongGetGongstructName(), rest.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (rest *Rest) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", rest.GongGetGongstructName(), rest.GongGetOrder(stage))
}

func (root *Root) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", root.GongGetGongstructName(), root.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (root *Root) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", root.GongGetGongstructName(), root.GongGetOrder(stage))
}

func (root_step *Root_step) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", root_step.GongGetGongstructName(), root_step.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (root_step *Root_step) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", root_step.GongGetGongstructName(), root_step.GongGetOrder(stage))
}

func (scaling *Scaling) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", scaling.GongGetGongstructName(), scaling.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (scaling *Scaling) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", scaling.GongGetGongstructName(), scaling.GongGetOrder(stage))
}

func (scordatura *Scordatura) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", scordatura.GongGetGongstructName(), scordatura.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (scordatura *Scordatura) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", scordatura.GongGetGongstructName(), scordatura.GongGetOrder(stage))
}

func (score_instrument *Score_instrument) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_instrument.GongGetGongstructName(), score_instrument.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (score_instrument *Score_instrument) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_instrument.GongGetGongstructName(), score_instrument.GongGetOrder(stage))
}

func (score_part *Score_part) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_part.GongGetGongstructName(), score_part.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (score_part *Score_part) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_part.GongGetGongstructName(), score_part.GongGetOrder(stage))
}

func (score_partwise *Score_partwise) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_partwise.GongGetGongstructName(), score_partwise.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (score_partwise *Score_partwise) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_partwise.GongGetGongstructName(), score_partwise.GongGetOrder(stage))
}

func (score_timewise *Score_timewise) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_timewise.GongGetGongstructName(), score_timewise.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (score_timewise *Score_timewise) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", score_timewise.GongGetGongstructName(), score_timewise.GongGetOrder(stage))
}

func (segno *Segno) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", segno.GongGetGongstructName(), segno.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (segno *Segno) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", segno.GongGetGongstructName(), segno.GongGetOrder(stage))
}

func (slash *Slash) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slash.GongGetGongstructName(), slash.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (slash *Slash) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slash.GongGetGongstructName(), slash.GongGetOrder(stage))
}

func (slide *Slide) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slide.GongGetGongstructName(), slide.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (slide *Slide) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slide.GongGetGongstructName(), slide.GongGetOrder(stage))
}

func (slur *Slur) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slur.GongGetGongstructName(), slur.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (slur *Slur) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", slur.GongGetGongstructName(), slur.GongGetOrder(stage))
}

func (sound *Sound) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", sound.GongGetGongstructName(), sound.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (sound *Sound) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", sound.GongGetGongstructName(), sound.GongGetOrder(stage))
}

func (staff_details *Staff_details) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_details.GongGetGongstructName(), staff_details.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (staff_details *Staff_details) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_details.GongGetGongstructName(), staff_details.GongGetOrder(stage))
}

func (staff_divide *Staff_divide) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_divide.GongGetGongstructName(), staff_divide.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (staff_divide *Staff_divide) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_divide.GongGetGongstructName(), staff_divide.GongGetOrder(stage))
}

func (staff_layout *Staff_layout) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_layout.GongGetGongstructName(), staff_layout.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (staff_layout *Staff_layout) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_layout.GongGetGongstructName(), staff_layout.GongGetOrder(stage))
}

func (staff_size *Staff_size) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_size.GongGetGongstructName(), staff_size.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (staff_size *Staff_size) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_size.GongGetGongstructName(), staff_size.GongGetOrder(stage))
}

func (staff_tuning *Staff_tuning) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_tuning.GongGetGongstructName(), staff_tuning.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (staff_tuning *Staff_tuning) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", staff_tuning.GongGetGongstructName(), staff_tuning.GongGetOrder(stage))
}

func (stem *Stem) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", stem.GongGetGongstructName(), stem.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (stem *Stem) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", stem.GongGetGongstructName(), stem.GongGetOrder(stage))
}

func (stick *Stick) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", stick.GongGetGongstructName(), stick.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (stick *Stick) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", stick.GongGetGongstructName(), stick.GongGetOrder(stage))
}

func (string_mute *String_mute) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", string_mute.GongGetGongstructName(), string_mute.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (string_mute *String_mute) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", string_mute.GongGetGongstructName(), string_mute.GongGetOrder(stage))
}

func (string_type *String_type) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", string_type.GongGetGongstructName(), string_type.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (string_type *String_type) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", string_type.GongGetGongstructName(), string_type.GongGetOrder(stage))
}

func (strong_accent *Strong_accent) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", strong_accent.GongGetGongstructName(), strong_accent.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (strong_accent *Strong_accent) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", strong_accent.GongGetGongstructName(), strong_accent.GongGetOrder(stage))
}

func (style_text *Style_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", style_text.GongGetGongstructName(), style_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (style_text *Style_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", style_text.GongGetGongstructName(), style_text.GongGetOrder(stage))
}

func (supports *Supports) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", supports.GongGetGongstructName(), supports.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (supports *Supports) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", supports.GongGetGongstructName(), supports.GongGetOrder(stage))
}

func (swing *Swing) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", swing.GongGetGongstructName(), swing.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (swing *Swing) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", swing.GongGetGongstructName(), swing.GongGetOrder(stage))
}

func (sync *Sync) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", sync.GongGetGongstructName(), sync.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (sync *Sync) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", sync.GongGetGongstructName(), sync.GongGetOrder(stage))
}

func (system_dividers *System_dividers) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_dividers.GongGetGongstructName(), system_dividers.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (system_dividers *System_dividers) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_dividers.GongGetGongstructName(), system_dividers.GongGetOrder(stage))
}

func (system_layout *System_layout) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_layout.GongGetGongstructName(), system_layout.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (system_layout *System_layout) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_layout.GongGetGongstructName(), system_layout.GongGetOrder(stage))
}

func (system_margins *System_margins) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_margins.GongGetGongstructName(), system_margins.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (system_margins *System_margins) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", system_margins.GongGetGongstructName(), system_margins.GongGetOrder(stage))
}

func (tap *Tap) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tap.GongGetGongstructName(), tap.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tap *Tap) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tap.GongGetGongstructName(), tap.GongGetOrder(stage))
}

func (technical *Technical) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", technical.GongGetGongstructName(), technical.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (technical *Technical) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", technical.GongGetGongstructName(), technical.GongGetOrder(stage))
}

func (text_element_data *Text_element_data) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", text_element_data.GongGetGongstructName(), text_element_data.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (text_element_data *Text_element_data) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", text_element_data.GongGetGongstructName(), text_element_data.GongGetOrder(stage))
}

func (tie *Tie) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tie.GongGetGongstructName(), tie.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tie *Tie) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tie.GongGetGongstructName(), tie.GongGetOrder(stage))
}

func (tied *Tied) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tied.GongGetGongstructName(), tied.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tied *Tied) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tied.GongGetGongstructName(), tied.GongGetOrder(stage))
}

func (time *Time) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", time.GongGetGongstructName(), time.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (time *Time) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", time.GongGetGongstructName(), time.GongGetOrder(stage))
}

func (time_modification *Time_modification) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", time_modification.GongGetGongstructName(), time_modification.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (time_modification *Time_modification) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", time_modification.GongGetGongstructName(), time_modification.GongGetOrder(stage))
}

func (timpani *Timpani) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", timpani.GongGetGongstructName(), timpani.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (timpani *Timpani) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", timpani.GongGetGongstructName(), timpani.GongGetOrder(stage))
}

func (transpose *Transpose) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", transpose.GongGetGongstructName(), transpose.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (transpose *Transpose) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", transpose.GongGetGongstructName(), transpose.GongGetOrder(stage))
}

func (tremolo *Tremolo) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tremolo.GongGetGongstructName(), tremolo.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tremolo *Tremolo) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tremolo.GongGetGongstructName(), tremolo.GongGetOrder(stage))
}

func (tuplet *Tuplet) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet.GongGetGongstructName(), tuplet.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tuplet *Tuplet) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet.GongGetGongstructName(), tuplet.GongGetOrder(stage))
}

func (tuplet_dot *Tuplet_dot) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_dot.GongGetGongstructName(), tuplet_dot.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tuplet_dot *Tuplet_dot) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_dot.GongGetGongstructName(), tuplet_dot.GongGetOrder(stage))
}

func (tuplet_number *Tuplet_number) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_number.GongGetGongstructName(), tuplet_number.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tuplet_number *Tuplet_number) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_number.GongGetGongstructName(), tuplet_number.GongGetOrder(stage))
}

func (tuplet_portion *Tuplet_portion) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_portion.GongGetGongstructName(), tuplet_portion.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tuplet_portion *Tuplet_portion) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_portion.GongGetGongstructName(), tuplet_portion.GongGetOrder(stage))
}

func (tuplet_type *Tuplet_type) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_type.GongGetGongstructName(), tuplet_type.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (tuplet_type *Tuplet_type) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", tuplet_type.GongGetGongstructName(), tuplet_type.GongGetOrder(stage))
}

func (typed_text *Typed_text) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", typed_text.GongGetGongstructName(), typed_text.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (typed_text *Typed_text) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", typed_text.GongGetGongstructName(), typed_text.GongGetOrder(stage))
}

func (unpitched *Unpitched) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", unpitched.GongGetGongstructName(), unpitched.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (unpitched *Unpitched) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", unpitched.GongGetGongstructName(), unpitched.GongGetOrder(stage))
}

func (virtual_instrument *Virtual_instrument) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", virtual_instrument.GongGetGongstructName(), virtual_instrument.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (virtual_instrument *Virtual_instrument) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", virtual_instrument.GongGetGongstructName(), virtual_instrument.GongGetOrder(stage))
}

func (wait *Wait) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wait.GongGetGongstructName(), wait.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (wait *Wait) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wait.GongGetGongstructName(), wait.GongGetOrder(stage))
}

func (wavy_line *Wavy_line) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wavy_line.GongGetGongstructName(), wavy_line.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (wavy_line *Wavy_line) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wavy_line.GongGetGongstructName(), wavy_line.GongGetOrder(stage))
}

func (wedge *Wedge) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wedge.GongGetGongstructName(), wedge.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (wedge *Wedge) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wedge.GongGetGongstructName(), wedge.GongGetOrder(stage))
}

func (wood *Wood) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wood.GongGetGongstructName(), wood.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (wood *Wood) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", wood.GongGetGongstructName(), wood.GongGetOrder(stage))
}

func (work *Work) GongGetIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", work.GongGetGongstructName(), work.GongGetOrder(stage))
}

// GongGetReferenceIdentifier returns an identifier when it was staged (it may have been unstaged since)
func (work *Work) GongGetReferenceIdentifier(stage *Stage) string {
	return fmt.Sprintf("__%s__%08d_", work.GongGetGongstructName(), work.GongGetOrder(stage))
}

// MarshallIdentifier returns the code to instantiate the instance
// in a marshalling file
// insertion point per named struct
func (a_directive *A_directive) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_directive.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "A_directive")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(a_directive.Name))
	return
}

func (a_measure *A_measure) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_measure.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "A_measure")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(a_measure.Name))
	return
}

func (a_measure_1 *A_measure_1) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_measure_1.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "A_measure_1")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(a_measure_1.Name))
	return
}

func (a_part *A_part) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_part.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "A_part")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(a_part.Name))
	return
}

func (a_part_1 *A_part_1) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_part_1.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "A_part_1")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(a_part_1.Name))
	return
}

func (accidental *Accidental) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Accidental")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(accidental.Name))
	return
}

func (accidental_mark *Accidental_mark) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental_mark.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Accidental_mark")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(accidental_mark.Name))
	return
}

func (accidental_text *Accidental_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Accidental_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(accidental_text.Name))
	return
}

func (accord *Accord) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accord.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Accord")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(accord.Name))
	return
}

func (accordion_registration *Accordion_registration) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accordion_registration.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Accordion_registration")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(accordion_registration.Name))
	return
}

func (appearance *Appearance) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", appearance.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Appearance")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(appearance.Name))
	return
}

func (arpeggiate *Arpeggiate) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", arpeggiate.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Arpeggiate")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(arpeggiate.Name))
	return
}

func (arrow *Arrow) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", arrow.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Arrow")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(arrow.Name))
	return
}

func (articulations *Articulations) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", articulations.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Articulations")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(articulations.Name))
	return
}

func (assess *Assess) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", assess.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Assess")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(assess.Name))
	return
}

func (attributes *Attributes) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", attributes.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Attributes")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(attributes.Name))
	return
}

func (backup *Backup) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", backup.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Backup")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(backup.Name))
	return
}

func (bar_style_color *Bar_style_color) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bar_style_color.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bar_style_color")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bar_style_color.Name))
	return
}

func (barline *Barline) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", barline.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Barline")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(barline.Name))
	return
}

func (barre *Barre) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", barre.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Barre")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(barre.Name))
	return
}

func (bass *Bass) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bass.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bass")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bass.Name))
	return
}

func (bass_step *Bass_step) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bass_step.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bass_step")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bass_step.Name))
	return
}

func (beam *Beam) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beam.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Beam")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(beam.Name))
	return
}

func (beat_repeat *Beat_repeat) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beat_repeat.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Beat_repeat")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(beat_repeat.Name))
	return
}

func (beat_unit_tied *Beat_unit_tied) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beat_unit_tied.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Beat_unit_tied")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(beat_unit_tied.Name))
	return
}

func (beater *Beater) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beater.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Beater")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(beater.Name))
	return
}

func (bend *Bend) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bend.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bend")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bend.Name))
	return
}

func (bookmark *Bookmark) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bookmark.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bookmark")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bookmark.Name))
	return
}

func (bracket *Bracket) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bracket.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Bracket")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(bracket.Name))
	return
}

func (breath_mark *Breath_mark) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", breath_mark.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Breath_mark")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(breath_mark.Name))
	return
}

func (caesura *Caesura) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", caesura.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Caesura")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(caesura.Name))
	return
}

func (cancel *Cancel) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", cancel.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Cancel")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(cancel.Name))
	return
}

func (clef *Clef) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", clef.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Clef")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(clef.Name))
	return
}

func (coda *Coda) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", coda.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Coda")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(coda.Name))
	return
}

func (credit *Credit) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", credit.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Credit")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(credit.Name))
	return
}

func (dashes *Dashes) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", dashes.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Dashes")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(dashes.Name))
	return
}

func (defaults *Defaults) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", defaults.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Defaults")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(defaults.Name))
	return
}

func (degree *Degree) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Degree")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(degree.Name))
	return
}

func (degree_alter *Degree_alter) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_alter.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Degree_alter")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(degree_alter.Name))
	return
}

func (degree_type *Degree_type) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_type.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Degree_type")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(degree_type.Name))
	return
}

func (degree_value *Degree_value) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_value.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Degree_value")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(degree_value.Name))
	return
}

func (direction *Direction) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", direction.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Direction")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(direction.Name))
	return
}

func (direction_type *Direction_type) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", direction_type.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Direction_type")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(direction_type.Name))
	return
}

func (distance *Distance) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", distance.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Distance")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(distance.Name))
	return
}

func (double *Double) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", double.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Double")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(double.Name))
	return
}

func (dynamics *Dynamics) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", dynamics.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Dynamics")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(dynamics.Name))
	return
}

func (effect *Effect) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", effect.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Effect")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(effect.Name))
	return
}

func (elision *Elision) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", elision.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Elision")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(elision.Name))
	return
}

func (empty *Empty) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty.Name))
	return
}

func (empty_font *Empty_font) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_font.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_font")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_font.Name))
	return
}

func (empty_line *Empty_line) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_line.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_line")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_line.Name))
	return
}

func (empty_placement *Empty_placement) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_placement.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_placement")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_placement.Name))
	return
}

func (empty_placement_smufl *Empty_placement_smufl) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_placement_smufl.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_placement_smufl")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_placement_smufl.Name))
	return
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_object_style_align.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_print_object_style_align")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_print_object_style_align.Name))
	return
}

func (empty_print_style *Empty_print_style) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_print_style")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_print_style.Name))
	return
}

func (empty_print_style_align *Empty_print_style_align) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style_align.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_print_style_align")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_print_style_align.Name))
	return
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style_align_id.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_print_style_align_id")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_print_style_align_id.Name))
	return
}

func (empty_trill_sound *Empty_trill_sound) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_trill_sound.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Empty_trill_sound")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(empty_trill_sound.Name))
	return
}

func (encoding *Encoding) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", encoding.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Encoding")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(encoding.Name))
	return
}

func (ending *Ending) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", ending.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Ending")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(ending.Name))
	return
}

func (extend *Extend) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", extend.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Extend")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(extend.Name))
	return
}

func (feature *Feature) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", feature.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Feature")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(feature.Name))
	return
}

func (fermata *Fermata) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fermata.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Fermata")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(fermata.Name))
	return
}

func (figure *Figure) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", figure.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Figure")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(figure.Name))
	return
}

func (figured_bass *Figured_bass) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", figured_bass.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Figured_bass")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(figured_bass.Name))
	return
}

func (fingering *Fingering) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fingering.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Fingering")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(fingering.Name))
	return
}

func (first_fret *First_fret) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", first_fret.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "First_fret")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(first_fret.Name))
	return
}

func (for_part *For_part) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", for_part.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "For_part")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(for_part.Name))
	return
}

func (formatted_symbol *Formatted_symbol) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_symbol.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Formatted_symbol")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(formatted_symbol.Name))
	return
}

func (formatted_symbol_id *Formatted_symbol_id) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_symbol_id.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Formatted_symbol_id")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(formatted_symbol_id.Name))
	return
}

func (formatted_text *Formatted_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Formatted_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(formatted_text.Name))
	return
}

func (formatted_text_id *Formatted_text_id) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_text_id.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Formatted_text_id")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(formatted_text_id.Name))
	return
}

func (forward *Forward) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", forward.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Forward")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(forward.Name))
	return
}

func (frame *Frame) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", frame.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Frame")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(frame.Name))
	return
}

func (frame_note *Frame_note) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", frame_note.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Frame_note")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(frame_note.Name))
	return
}

func (fret *Fret) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fret.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Fret")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(fret.Name))
	return
}

func (glass *Glass) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glass.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Glass")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(glass.Name))
	return
}

func (glissando *Glissando) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glissando.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Glissando")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(glissando.Name))
	return
}

func (glyph *Glyph) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glyph.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Glyph")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(glyph.Name))
	return
}

func (grace *Grace) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", grace.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Grace")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(grace.Name))
	return
}

func (group_barline *Group_barline) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_barline.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Group_barline")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(group_barline.Name))
	return
}

func (group_name *Group_name) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_name.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Group_name")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(group_name.Name))
	return
}

func (group_symbol *Group_symbol) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_symbol.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Group_symbol")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(group_symbol.Name))
	return
}

func (grouping *Grouping) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", grouping.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Grouping")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(grouping.Name))
	return
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hammer_on_pull_off.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Hammer_on_pull_off")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(hammer_on_pull_off.Name))
	return
}

func (handbell *Handbell) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", handbell.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Handbell")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(handbell.Name))
	return
}

func (harmon_closed *Harmon_closed) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmon_closed.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harmon_closed")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harmon_closed.Name))
	return
}

func (harmon_mute *Harmon_mute) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmon_mute.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harmon_mute")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harmon_mute.Name))
	return
}

func (harmonic *Harmonic) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmonic.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harmonic")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harmonic.Name))
	return
}

func (harmony *Harmony) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmony.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harmony")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harmony.Name))
	return
}

func (harmony_alter *Harmony_alter) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmony_alter.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harmony_alter")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harmony_alter.Name))
	return
}

func (harp_pedals *Harp_pedals) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harp_pedals.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Harp_pedals")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(harp_pedals.Name))
	return
}

func (heel_toe *Heel_toe) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", heel_toe.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Heel_toe")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(heel_toe.Name))
	return
}

func (hole *Hole) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hole.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Hole")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(hole.Name))
	return
}

func (hole_closed *Hole_closed) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hole_closed.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Hole_closed")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(hole_closed.Name))
	return
}

func (horizontal_turn *Horizontal_turn) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", horizontal_turn.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Horizontal_turn")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(horizontal_turn.Name))
	return
}

func (identification *Identification) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", identification.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Identification")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(identification.Name))
	return
}

func (image *Image) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", image.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Image")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(image.Name))
	return
}

func (instrument *Instrument) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Instrument")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(instrument.Name))
	return
}

func (instrument_change *Instrument_change) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument_change.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Instrument_change")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(instrument_change.Name))
	return
}

func (instrument_link *Instrument_link) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument_link.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Instrument_link")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(instrument_link.Name))
	return
}

func (interchangeable *Interchangeable) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", interchangeable.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Interchangeable")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(interchangeable.Name))
	return
}

func (inversion *Inversion) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", inversion.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Inversion")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(inversion.Name))
	return
}

func (key *Key) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Key")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(key.Name))
	return
}

func (key_accidental *Key_accidental) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key_accidental.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Key_accidental")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(key_accidental.Name))
	return
}

func (key_octave *Key_octave) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key_octave.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Key_octave")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(key_octave.Name))
	return
}

func (kind *Kind) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", kind.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Kind")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(kind.Name))
	return
}

func (level *Level) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", level.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Level")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(level.Name))
	return
}

func (line_detail *Line_detail) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", line_detail.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Line_detail")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(line_detail.Name))
	return
}

func (line_width *Line_width) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", line_width.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Line_width")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(line_width.Name))
	return
}

func (link *Link) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", link.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Link")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(link.Name))
	return
}

func (listen *Listen) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", listen.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Listen")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(listen.Name))
	return
}

func (listening *Listening) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", listening.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Listening")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(listening.Name))
	return
}

func (lyric *Lyric) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Lyric")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(lyric.Name))
	return
}

func (lyric_font *Lyric_font) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric_font.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Lyric_font")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(lyric_font.Name))
	return
}

func (lyric_language *Lyric_language) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric_language.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Lyric_language")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(lyric_language.Name))
	return
}

func (measure_layout *Measure_layout) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_layout.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Measure_layout")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(measure_layout.Name))
	return
}

func (measure_numbering *Measure_numbering) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_numbering.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Measure_numbering")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(measure_numbering.Name))
	return
}

func (measure_repeat *Measure_repeat) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_repeat.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Measure_repeat")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(measure_repeat.Name))
	return
}

func (measure_style *Measure_style) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_style.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Measure_style")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(measure_style.Name))
	return
}

func (membrane *Membrane) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", membrane.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Membrane")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(membrane.Name))
	return
}

func (metal *Metal) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metal.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metal")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metal.Name))
	return
}

func (metronome *Metronome) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metronome")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metronome.Name))
	return
}

func (metronome_beam *Metronome_beam) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_beam.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metronome_beam")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metronome_beam.Name))
	return
}

func (metronome_note *Metronome_note) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_note.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metronome_note")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metronome_note.Name))
	return
}

func (metronome_tied *Metronome_tied) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_tied.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metronome_tied")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metronome_tied.Name))
	return
}

func (metronome_tuplet *Metronome_tuplet) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_tuplet.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Metronome_tuplet")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(metronome_tuplet.Name))
	return
}

func (midi_device *Midi_device) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", midi_device.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Midi_device")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(midi_device.Name))
	return
}

func (midi_instrument *Midi_instrument) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", midi_instrument.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Midi_instrument")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(midi_instrument.Name))
	return
}

func (miscellaneous *Miscellaneous) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", miscellaneous.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Miscellaneous")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(miscellaneous.Name))
	return
}

func (miscellaneous_field *Miscellaneous_field) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", miscellaneous_field.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Miscellaneous_field")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(miscellaneous_field.Name))
	return
}

func (mordent *Mordent) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", mordent.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Mordent")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(mordent.Name))
	return
}

func (multiple_rest *Multiple_rest) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", multiple_rest.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Multiple_rest")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(multiple_rest.Name))
	return
}

func (name_display *Name_display) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", name_display.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Name_display")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(name_display.Name))
	return
}

func (non_arpeggiate *Non_arpeggiate) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", non_arpeggiate.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Non_arpeggiate")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(non_arpeggiate.Name))
	return
}

func (notations *Notations) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notations.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Notations")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(notations.Name))
	return
}

func (note *Note) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Note")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(note.Name))
	return
}

func (note_size *Note_size) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note_size.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Note_size")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(note_size.Name))
	return
}

func (note_type *Note_type) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note_type.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Note_type")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(note_type.Name))
	return
}

func (notehead *Notehead) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notehead.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Notehead")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(notehead.Name))
	return
}

func (notehead_text *Notehead_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notehead_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Notehead_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(notehead_text.Name))
	return
}

func (numeral *Numeral) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Numeral")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(numeral.Name))
	return
}

func (numeral_key *Numeral_key) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral_key.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Numeral_key")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(numeral_key.Name))
	return
}

func (numeral_root *Numeral_root) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral_root.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Numeral_root")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(numeral_root.Name))
	return
}

func (octave_shift *Octave_shift) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", octave_shift.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Octave_shift")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(octave_shift.Name))
	return
}

func (offset *Offset) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", offset.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Offset")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(offset.Name))
	return
}

func (opus *Opus) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", opus.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Opus")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(opus.Name))
	return
}

func (ornaments *Ornaments) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", ornaments.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Ornaments")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(ornaments.Name))
	return
}

func (other_appearance *Other_appearance) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_appearance.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_appearance")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_appearance.Name))
	return
}

func (other_direction *Other_direction) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_direction.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_direction")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_direction.Name))
	return
}

func (other_listening *Other_listening) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_listening.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_listening")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_listening.Name))
	return
}

func (other_notation *Other_notation) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_notation.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_notation")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_notation.Name))
	return
}

func (other_placement_text *Other_placement_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_placement_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_placement_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_placement_text.Name))
	return
}

func (other_play *Other_play) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_play.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_play")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_play.Name))
	return
}

func (other_text *Other_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Other_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(other_text.Name))
	return
}

func (page_layout *Page_layout) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", page_layout.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Page_layout")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(page_layout.Name))
	return
}

func (page_margins *Page_margins) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", page_margins.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Page_margins")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(page_margins.Name))
	return
}

func (part_clef *Part_clef) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_clef.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_clef")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_clef.Name))
	return
}

func (part_group *Part_group) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_group.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_group")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_group.Name))
	return
}

func (part_link *Part_link) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_link.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_link")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_link.Name))
	return
}

func (part_list *Part_list) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_list.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_list")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_list.Name))
	return
}

func (part_name *Part_name) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_name.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_name")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_name.Name))
	return
}

func (part_symbol *Part_symbol) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_symbol.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_symbol")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_symbol.Name))
	return
}

func (part_transpose *Part_transpose) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_transpose.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Part_transpose")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(part_transpose.Name))
	return
}

func (pedal *Pedal) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pedal.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Pedal")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(pedal.Name))
	return
}

func (pedal_tuning *Pedal_tuning) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pedal_tuning.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Pedal_tuning")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(pedal_tuning.Name))
	return
}

func (per_minute *Per_minute) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", per_minute.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Per_minute")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(per_minute.Name))
	return
}

func (percussion *Percussion) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", percussion.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Percussion")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(percussion.Name))
	return
}

func (pitch *Pitch) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pitch.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Pitch")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(pitch.Name))
	return
}

func (pitched *Pitched) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pitched.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Pitched")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(pitched.Name))
	return
}

func (placement_text *Placement_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", placement_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Placement_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(placement_text.Name))
	return
}

func (play *Play) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", play.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Play")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(play.Name))
	return
}

func (player *Player) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", player.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Player")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(player.Name))
	return
}

func (principal_voice *Principal_voice) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", principal_voice.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Principal_voice")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(principal_voice.Name))
	return
}

func (print *Print) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", print.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Print")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(print.Name))
	return
}

func (release *Release) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", release.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Release")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(release.Name))
	return
}

func (repeat *Repeat) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", repeat.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Repeat")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(repeat.Name))
	return
}

func (rest *Rest) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", rest.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Rest")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(rest.Name))
	return
}

func (root *Root) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", root.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Root")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(root.Name))
	return
}

func (root_step *Root_step) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", root_step.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Root_step")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(root_step.Name))
	return
}

func (scaling *Scaling) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", scaling.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Scaling")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(scaling.Name))
	return
}

func (scordatura *Scordatura) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", scordatura.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Scordatura")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(scordatura.Name))
	return
}

func (score_instrument *Score_instrument) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_instrument.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Score_instrument")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(score_instrument.Name))
	return
}

func (score_part *Score_part) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_part.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Score_part")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(score_part.Name))
	return
}

func (score_partwise *Score_partwise) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_partwise.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Score_partwise")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(score_partwise.Name))
	return
}

func (score_timewise *Score_timewise) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_timewise.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Score_timewise")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(score_timewise.Name))
	return
}

func (segno *Segno) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", segno.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Segno")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(segno.Name))
	return
}

func (slash *Slash) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slash.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Slash")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(slash.Name))
	return
}

func (slide *Slide) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slide.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Slide")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(slide.Name))
	return
}

func (slur *Slur) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slur.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Slur")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(slur.Name))
	return
}

func (sound *Sound) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", sound.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Sound")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(sound.Name))
	return
}

func (staff_details *Staff_details) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_details.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Staff_details")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(staff_details.Name))
	return
}

func (staff_divide *Staff_divide) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_divide.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Staff_divide")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(staff_divide.Name))
	return
}

func (staff_layout *Staff_layout) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_layout.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Staff_layout")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(staff_layout.Name))
	return
}

func (staff_size *Staff_size) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_size.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Staff_size")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(staff_size.Name))
	return
}

func (staff_tuning *Staff_tuning) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_tuning.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Staff_tuning")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(staff_tuning.Name))
	return
}

func (stem *Stem) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", stem.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Stem")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(stem.Name))
	return
}

func (stick *Stick) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", stick.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Stick")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(stick.Name))
	return
}

func (string_mute *String_mute) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", string_mute.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "String_mute")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(string_mute.Name))
	return
}

func (string_type *String_type) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", string_type.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "String_type")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(string_type.Name))
	return
}

func (strong_accent *Strong_accent) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", strong_accent.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Strong_accent")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(strong_accent.Name))
	return
}

func (style_text *Style_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", style_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Style_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(style_text.Name))
	return
}

func (supports *Supports) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", supports.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Supports")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(supports.Name))
	return
}

func (swing *Swing) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", swing.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Swing")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(swing.Name))
	return
}

func (sync *Sync) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", sync.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Sync")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(sync.Name))
	return
}

func (system_dividers *System_dividers) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_dividers.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "System_dividers")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(system_dividers.Name))
	return
}

func (system_layout *System_layout) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_layout.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "System_layout")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(system_layout.Name))
	return
}

func (system_margins *System_margins) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_margins.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "System_margins")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(system_margins.Name))
	return
}

func (tap *Tap) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tap.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tap")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tap.Name))
	return
}

func (technical *Technical) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", technical.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Technical")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(technical.Name))
	return
}

func (text_element_data *Text_element_data) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", text_element_data.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Text_element_data")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(text_element_data.Name))
	return
}

func (tie *Tie) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tie.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tie")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tie.Name))
	return
}

func (tied *Tied) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tied.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tied")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tied.Name))
	return
}

func (time *Time) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", time.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Time")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(time.Name))
	return
}

func (time_modification *Time_modification) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", time_modification.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Time_modification")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(time_modification.Name))
	return
}

func (timpani *Timpani) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", timpani.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Timpani")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(timpani.Name))
	return
}

func (transpose *Transpose) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", transpose.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Transpose")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(transpose.Name))
	return
}

func (tremolo *Tremolo) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tremolo.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tremolo")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tremolo.Name))
	return
}

func (tuplet *Tuplet) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tuplet")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tuplet.Name))
	return
}

func (tuplet_dot *Tuplet_dot) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_dot.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tuplet_dot")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tuplet_dot.Name))
	return
}

func (tuplet_number *Tuplet_number) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_number.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tuplet_number")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tuplet_number.Name))
	return
}

func (tuplet_portion *Tuplet_portion) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_portion.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tuplet_portion")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tuplet_portion.Name))
	return
}

func (tuplet_type *Tuplet_type) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_type.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Tuplet_type")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(tuplet_type.Name))
	return
}

func (typed_text *Typed_text) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", typed_text.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Typed_text")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(typed_text.Name))
	return
}

func (unpitched *Unpitched) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", unpitched.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Unpitched")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(unpitched.Name))
	return
}

func (virtual_instrument *Virtual_instrument) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", virtual_instrument.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Virtual_instrument")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(virtual_instrument.Name))
	return
}

func (wait *Wait) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wait.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Wait")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(wait.Name))
	return
}

func (wavy_line *Wavy_line) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wavy_line.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Wavy_line")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(wavy_line.Name))
	return
}

func (wedge *Wedge) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wedge.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Wedge")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(wedge.Name))
	return
}

func (wood *Wood) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wood.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Wood")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(wood.Name))
	return
}

func (work *Work) GongMarshallIdentifier(stage *Stage) (decl string) {
	decl = GongIdentifiersDecls
	decl = strings.ReplaceAll(decl, "{{Identifier}}", work.GongGetIdentifier(stage))
	decl = strings.ReplaceAll(decl, "{{GeneratedStructName}}", "Work")
	decl = strings.ReplaceAll(decl, "{{GeneratedFieldNameValue}}", ToRawStringLiteral(work.Name))
	return
}

// insertion point for unstaging
func (a_directive *A_directive) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_directive.GongGetReferenceIdentifier(stage))
	return
}

func (a_measure *A_measure) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_measure.GongGetReferenceIdentifier(stage))
	return
}

func (a_measure_1 *A_measure_1) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_measure_1.GongGetReferenceIdentifier(stage))
	return
}

func (a_part *A_part) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_part.GongGetReferenceIdentifier(stage))
	return
}

func (a_part_1 *A_part_1) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", a_part_1.GongGetReferenceIdentifier(stage))
	return
}

func (accidental *Accidental) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental.GongGetReferenceIdentifier(stage))
	return
}

func (accidental_mark *Accidental_mark) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental_mark.GongGetReferenceIdentifier(stage))
	return
}

func (accidental_text *Accidental_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accidental_text.GongGetReferenceIdentifier(stage))
	return
}

func (accord *Accord) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accord.GongGetReferenceIdentifier(stage))
	return
}

func (accordion_registration *Accordion_registration) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", accordion_registration.GongGetReferenceIdentifier(stage))
	return
}

func (appearance *Appearance) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", appearance.GongGetReferenceIdentifier(stage))
	return
}

func (arpeggiate *Arpeggiate) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", arpeggiate.GongGetReferenceIdentifier(stage))
	return
}

func (arrow *Arrow) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", arrow.GongGetReferenceIdentifier(stage))
	return
}

func (articulations *Articulations) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", articulations.GongGetReferenceIdentifier(stage))
	return
}

func (assess *Assess) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", assess.GongGetReferenceIdentifier(stage))
	return
}

func (attributes *Attributes) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", attributes.GongGetReferenceIdentifier(stage))
	return
}

func (backup *Backup) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", backup.GongGetReferenceIdentifier(stage))
	return
}

func (bar_style_color *Bar_style_color) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bar_style_color.GongGetReferenceIdentifier(stage))
	return
}

func (barline *Barline) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", barline.GongGetReferenceIdentifier(stage))
	return
}

func (barre *Barre) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", barre.GongGetReferenceIdentifier(stage))
	return
}

func (bass *Bass) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bass.GongGetReferenceIdentifier(stage))
	return
}

func (bass_step *Bass_step) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bass_step.GongGetReferenceIdentifier(stage))
	return
}

func (beam *Beam) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beam.GongGetReferenceIdentifier(stage))
	return
}

func (beat_repeat *Beat_repeat) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beat_repeat.GongGetReferenceIdentifier(stage))
	return
}

func (beat_unit_tied *Beat_unit_tied) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beat_unit_tied.GongGetReferenceIdentifier(stage))
	return
}

func (beater *Beater) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", beater.GongGetReferenceIdentifier(stage))
	return
}

func (bend *Bend) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bend.GongGetReferenceIdentifier(stage))
	return
}

func (bookmark *Bookmark) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bookmark.GongGetReferenceIdentifier(stage))
	return
}

func (bracket *Bracket) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", bracket.GongGetReferenceIdentifier(stage))
	return
}

func (breath_mark *Breath_mark) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", breath_mark.GongGetReferenceIdentifier(stage))
	return
}

func (caesura *Caesura) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", caesura.GongGetReferenceIdentifier(stage))
	return
}

func (cancel *Cancel) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", cancel.GongGetReferenceIdentifier(stage))
	return
}

func (clef *Clef) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", clef.GongGetReferenceIdentifier(stage))
	return
}

func (coda *Coda) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", coda.GongGetReferenceIdentifier(stage))
	return
}

func (credit *Credit) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", credit.GongGetReferenceIdentifier(stage))
	return
}

func (dashes *Dashes) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", dashes.GongGetReferenceIdentifier(stage))
	return
}

func (defaults *Defaults) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", defaults.GongGetReferenceIdentifier(stage))
	return
}

func (degree *Degree) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree.GongGetReferenceIdentifier(stage))
	return
}

func (degree_alter *Degree_alter) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_alter.GongGetReferenceIdentifier(stage))
	return
}

func (degree_type *Degree_type) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_type.GongGetReferenceIdentifier(stage))
	return
}

func (degree_value *Degree_value) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", degree_value.GongGetReferenceIdentifier(stage))
	return
}

func (direction *Direction) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", direction.GongGetReferenceIdentifier(stage))
	return
}

func (direction_type *Direction_type) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", direction_type.GongGetReferenceIdentifier(stage))
	return
}

func (distance *Distance) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", distance.GongGetReferenceIdentifier(stage))
	return
}

func (double *Double) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", double.GongGetReferenceIdentifier(stage))
	return
}

func (dynamics *Dynamics) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", dynamics.GongGetReferenceIdentifier(stage))
	return
}

func (effect *Effect) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", effect.GongGetReferenceIdentifier(stage))
	return
}

func (elision *Elision) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", elision.GongGetReferenceIdentifier(stage))
	return
}

func (empty *Empty) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty.GongGetReferenceIdentifier(stage))
	return
}

func (empty_font *Empty_font) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_font.GongGetReferenceIdentifier(stage))
	return
}

func (empty_line *Empty_line) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_line.GongGetReferenceIdentifier(stage))
	return
}

func (empty_placement *Empty_placement) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_placement.GongGetReferenceIdentifier(stage))
	return
}

func (empty_placement_smufl *Empty_placement_smufl) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_placement_smufl.GongGetReferenceIdentifier(stage))
	return
}

func (empty_print_object_style_align *Empty_print_object_style_align) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_object_style_align.GongGetReferenceIdentifier(stage))
	return
}

func (empty_print_style *Empty_print_style) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style.GongGetReferenceIdentifier(stage))
	return
}

func (empty_print_style_align *Empty_print_style_align) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style_align.GongGetReferenceIdentifier(stage))
	return
}

func (empty_print_style_align_id *Empty_print_style_align_id) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_print_style_align_id.GongGetReferenceIdentifier(stage))
	return
}

func (empty_trill_sound *Empty_trill_sound) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", empty_trill_sound.GongGetReferenceIdentifier(stage))
	return
}

func (encoding *Encoding) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", encoding.GongGetReferenceIdentifier(stage))
	return
}

func (ending *Ending) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", ending.GongGetReferenceIdentifier(stage))
	return
}

func (extend *Extend) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", extend.GongGetReferenceIdentifier(stage))
	return
}

func (feature *Feature) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", feature.GongGetReferenceIdentifier(stage))
	return
}

func (fermata *Fermata) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fermata.GongGetReferenceIdentifier(stage))
	return
}

func (figure *Figure) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", figure.GongGetReferenceIdentifier(stage))
	return
}

func (figured_bass *Figured_bass) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", figured_bass.GongGetReferenceIdentifier(stage))
	return
}

func (fingering *Fingering) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fingering.GongGetReferenceIdentifier(stage))
	return
}

func (first_fret *First_fret) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", first_fret.GongGetReferenceIdentifier(stage))
	return
}

func (for_part *For_part) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", for_part.GongGetReferenceIdentifier(stage))
	return
}

func (formatted_symbol *Formatted_symbol) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_symbol.GongGetReferenceIdentifier(stage))
	return
}

func (formatted_symbol_id *Formatted_symbol_id) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_symbol_id.GongGetReferenceIdentifier(stage))
	return
}

func (formatted_text *Formatted_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_text.GongGetReferenceIdentifier(stage))
	return
}

func (formatted_text_id *Formatted_text_id) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", formatted_text_id.GongGetReferenceIdentifier(stage))
	return
}

func (forward *Forward) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", forward.GongGetReferenceIdentifier(stage))
	return
}

func (frame *Frame) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", frame.GongGetReferenceIdentifier(stage))
	return
}

func (frame_note *Frame_note) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", frame_note.GongGetReferenceIdentifier(stage))
	return
}

func (fret *Fret) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", fret.GongGetReferenceIdentifier(stage))
	return
}

func (glass *Glass) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glass.GongGetReferenceIdentifier(stage))
	return
}

func (glissando *Glissando) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glissando.GongGetReferenceIdentifier(stage))
	return
}

func (glyph *Glyph) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", glyph.GongGetReferenceIdentifier(stage))
	return
}

func (grace *Grace) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", grace.GongGetReferenceIdentifier(stage))
	return
}

func (group_barline *Group_barline) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_barline.GongGetReferenceIdentifier(stage))
	return
}

func (group_name *Group_name) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_name.GongGetReferenceIdentifier(stage))
	return
}

func (group_symbol *Group_symbol) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", group_symbol.GongGetReferenceIdentifier(stage))
	return
}

func (grouping *Grouping) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", grouping.GongGetReferenceIdentifier(stage))
	return
}

func (hammer_on_pull_off *Hammer_on_pull_off) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hammer_on_pull_off.GongGetReferenceIdentifier(stage))
	return
}

func (handbell *Handbell) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", handbell.GongGetReferenceIdentifier(stage))
	return
}

func (harmon_closed *Harmon_closed) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmon_closed.GongGetReferenceIdentifier(stage))
	return
}

func (harmon_mute *Harmon_mute) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmon_mute.GongGetReferenceIdentifier(stage))
	return
}

func (harmonic *Harmonic) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmonic.GongGetReferenceIdentifier(stage))
	return
}

func (harmony *Harmony) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmony.GongGetReferenceIdentifier(stage))
	return
}

func (harmony_alter *Harmony_alter) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harmony_alter.GongGetReferenceIdentifier(stage))
	return
}

func (harp_pedals *Harp_pedals) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", harp_pedals.GongGetReferenceIdentifier(stage))
	return
}

func (heel_toe *Heel_toe) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", heel_toe.GongGetReferenceIdentifier(stage))
	return
}

func (hole *Hole) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hole.GongGetReferenceIdentifier(stage))
	return
}

func (hole_closed *Hole_closed) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", hole_closed.GongGetReferenceIdentifier(stage))
	return
}

func (horizontal_turn *Horizontal_turn) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", horizontal_turn.GongGetReferenceIdentifier(stage))
	return
}

func (identification *Identification) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", identification.GongGetReferenceIdentifier(stage))
	return
}

func (image *Image) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", image.GongGetReferenceIdentifier(stage))
	return
}

func (instrument *Instrument) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument.GongGetReferenceIdentifier(stage))
	return
}

func (instrument_change *Instrument_change) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument_change.GongGetReferenceIdentifier(stage))
	return
}

func (instrument_link *Instrument_link) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", instrument_link.GongGetReferenceIdentifier(stage))
	return
}

func (interchangeable *Interchangeable) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", interchangeable.GongGetReferenceIdentifier(stage))
	return
}

func (inversion *Inversion) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", inversion.GongGetReferenceIdentifier(stage))
	return
}

func (key *Key) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key.GongGetReferenceIdentifier(stage))
	return
}

func (key_accidental *Key_accidental) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key_accidental.GongGetReferenceIdentifier(stage))
	return
}

func (key_octave *Key_octave) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", key_octave.GongGetReferenceIdentifier(stage))
	return
}

func (kind *Kind) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", kind.GongGetReferenceIdentifier(stage))
	return
}

func (level *Level) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", level.GongGetReferenceIdentifier(stage))
	return
}

func (line_detail *Line_detail) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", line_detail.GongGetReferenceIdentifier(stage))
	return
}

func (line_width *Line_width) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", line_width.GongGetReferenceIdentifier(stage))
	return
}

func (link *Link) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", link.GongGetReferenceIdentifier(stage))
	return
}

func (listen *Listen) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", listen.GongGetReferenceIdentifier(stage))
	return
}

func (listening *Listening) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", listening.GongGetReferenceIdentifier(stage))
	return
}

func (lyric *Lyric) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric.GongGetReferenceIdentifier(stage))
	return
}

func (lyric_font *Lyric_font) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric_font.GongGetReferenceIdentifier(stage))
	return
}

func (lyric_language *Lyric_language) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", lyric_language.GongGetReferenceIdentifier(stage))
	return
}

func (measure_layout *Measure_layout) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_layout.GongGetReferenceIdentifier(stage))
	return
}

func (measure_numbering *Measure_numbering) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_numbering.GongGetReferenceIdentifier(stage))
	return
}

func (measure_repeat *Measure_repeat) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_repeat.GongGetReferenceIdentifier(stage))
	return
}

func (measure_style *Measure_style) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", measure_style.GongGetReferenceIdentifier(stage))
	return
}

func (membrane *Membrane) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", membrane.GongGetReferenceIdentifier(stage))
	return
}

func (metal *Metal) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metal.GongGetReferenceIdentifier(stage))
	return
}

func (metronome *Metronome) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome.GongGetReferenceIdentifier(stage))
	return
}

func (metronome_beam *Metronome_beam) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_beam.GongGetReferenceIdentifier(stage))
	return
}

func (metronome_note *Metronome_note) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_note.GongGetReferenceIdentifier(stage))
	return
}

func (metronome_tied *Metronome_tied) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_tied.GongGetReferenceIdentifier(stage))
	return
}

func (metronome_tuplet *Metronome_tuplet) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", metronome_tuplet.GongGetReferenceIdentifier(stage))
	return
}

func (midi_device *Midi_device) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", midi_device.GongGetReferenceIdentifier(stage))
	return
}

func (midi_instrument *Midi_instrument) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", midi_instrument.GongGetReferenceIdentifier(stage))
	return
}

func (miscellaneous *Miscellaneous) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", miscellaneous.GongGetReferenceIdentifier(stage))
	return
}

func (miscellaneous_field *Miscellaneous_field) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", miscellaneous_field.GongGetReferenceIdentifier(stage))
	return
}

func (mordent *Mordent) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", mordent.GongGetReferenceIdentifier(stage))
	return
}

func (multiple_rest *Multiple_rest) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", multiple_rest.GongGetReferenceIdentifier(stage))
	return
}

func (name_display *Name_display) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", name_display.GongGetReferenceIdentifier(stage))
	return
}

func (non_arpeggiate *Non_arpeggiate) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", non_arpeggiate.GongGetReferenceIdentifier(stage))
	return
}

func (notations *Notations) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notations.GongGetReferenceIdentifier(stage))
	return
}

func (note *Note) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note.GongGetReferenceIdentifier(stage))
	return
}

func (note_size *Note_size) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note_size.GongGetReferenceIdentifier(stage))
	return
}

func (note_type *Note_type) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", note_type.GongGetReferenceIdentifier(stage))
	return
}

func (notehead *Notehead) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notehead.GongGetReferenceIdentifier(stage))
	return
}

func (notehead_text *Notehead_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", notehead_text.GongGetReferenceIdentifier(stage))
	return
}

func (numeral *Numeral) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral.GongGetReferenceIdentifier(stage))
	return
}

func (numeral_key *Numeral_key) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral_key.GongGetReferenceIdentifier(stage))
	return
}

func (numeral_root *Numeral_root) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", numeral_root.GongGetReferenceIdentifier(stage))
	return
}

func (octave_shift *Octave_shift) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", octave_shift.GongGetReferenceIdentifier(stage))
	return
}

func (offset *Offset) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", offset.GongGetReferenceIdentifier(stage))
	return
}

func (opus *Opus) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", opus.GongGetReferenceIdentifier(stage))
	return
}

func (ornaments *Ornaments) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", ornaments.GongGetReferenceIdentifier(stage))
	return
}

func (other_appearance *Other_appearance) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_appearance.GongGetReferenceIdentifier(stage))
	return
}

func (other_direction *Other_direction) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_direction.GongGetReferenceIdentifier(stage))
	return
}

func (other_listening *Other_listening) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_listening.GongGetReferenceIdentifier(stage))
	return
}

func (other_notation *Other_notation) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_notation.GongGetReferenceIdentifier(stage))
	return
}

func (other_placement_text *Other_placement_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_placement_text.GongGetReferenceIdentifier(stage))
	return
}

func (other_play *Other_play) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_play.GongGetReferenceIdentifier(stage))
	return
}

func (other_text *Other_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", other_text.GongGetReferenceIdentifier(stage))
	return
}

func (page_layout *Page_layout) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", page_layout.GongGetReferenceIdentifier(stage))
	return
}

func (page_margins *Page_margins) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", page_margins.GongGetReferenceIdentifier(stage))
	return
}

func (part_clef *Part_clef) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_clef.GongGetReferenceIdentifier(stage))
	return
}

func (part_group *Part_group) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_group.GongGetReferenceIdentifier(stage))
	return
}

func (part_link *Part_link) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_link.GongGetReferenceIdentifier(stage))
	return
}

func (part_list *Part_list) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_list.GongGetReferenceIdentifier(stage))
	return
}

func (part_name *Part_name) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_name.GongGetReferenceIdentifier(stage))
	return
}

func (part_symbol *Part_symbol) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_symbol.GongGetReferenceIdentifier(stage))
	return
}

func (part_transpose *Part_transpose) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", part_transpose.GongGetReferenceIdentifier(stage))
	return
}

func (pedal *Pedal) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pedal.GongGetReferenceIdentifier(stage))
	return
}

func (pedal_tuning *Pedal_tuning) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pedal_tuning.GongGetReferenceIdentifier(stage))
	return
}

func (per_minute *Per_minute) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", per_minute.GongGetReferenceIdentifier(stage))
	return
}

func (percussion *Percussion) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", percussion.GongGetReferenceIdentifier(stage))
	return
}

func (pitch *Pitch) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pitch.GongGetReferenceIdentifier(stage))
	return
}

func (pitched *Pitched) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", pitched.GongGetReferenceIdentifier(stage))
	return
}

func (placement_text *Placement_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", placement_text.GongGetReferenceIdentifier(stage))
	return
}

func (play *Play) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", play.GongGetReferenceIdentifier(stage))
	return
}

func (player *Player) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", player.GongGetReferenceIdentifier(stage))
	return
}

func (principal_voice *Principal_voice) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", principal_voice.GongGetReferenceIdentifier(stage))
	return
}

func (print *Print) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", print.GongGetReferenceIdentifier(stage))
	return
}

func (release *Release) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", release.GongGetReferenceIdentifier(stage))
	return
}

func (repeat *Repeat) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", repeat.GongGetReferenceIdentifier(stage))
	return
}

func (rest *Rest) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", rest.GongGetReferenceIdentifier(stage))
	return
}

func (root *Root) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", root.GongGetReferenceIdentifier(stage))
	return
}

func (root_step *Root_step) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", root_step.GongGetReferenceIdentifier(stage))
	return
}

func (scaling *Scaling) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", scaling.GongGetReferenceIdentifier(stage))
	return
}

func (scordatura *Scordatura) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", scordatura.GongGetReferenceIdentifier(stage))
	return
}

func (score_instrument *Score_instrument) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_instrument.GongGetReferenceIdentifier(stage))
	return
}

func (score_part *Score_part) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_part.GongGetReferenceIdentifier(stage))
	return
}

func (score_partwise *Score_partwise) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_partwise.GongGetReferenceIdentifier(stage))
	return
}

func (score_timewise *Score_timewise) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", score_timewise.GongGetReferenceIdentifier(stage))
	return
}

func (segno *Segno) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", segno.GongGetReferenceIdentifier(stage))
	return
}

func (slash *Slash) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slash.GongGetReferenceIdentifier(stage))
	return
}

func (slide *Slide) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slide.GongGetReferenceIdentifier(stage))
	return
}

func (slur *Slur) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", slur.GongGetReferenceIdentifier(stage))
	return
}

func (sound *Sound) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", sound.GongGetReferenceIdentifier(stage))
	return
}

func (staff_details *Staff_details) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_details.GongGetReferenceIdentifier(stage))
	return
}

func (staff_divide *Staff_divide) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_divide.GongGetReferenceIdentifier(stage))
	return
}

func (staff_layout *Staff_layout) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_layout.GongGetReferenceIdentifier(stage))
	return
}

func (staff_size *Staff_size) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_size.GongGetReferenceIdentifier(stage))
	return
}

func (staff_tuning *Staff_tuning) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", staff_tuning.GongGetReferenceIdentifier(stage))
	return
}

func (stem *Stem) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", stem.GongGetReferenceIdentifier(stage))
	return
}

func (stick *Stick) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", stick.GongGetReferenceIdentifier(stage))
	return
}

func (string_mute *String_mute) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", string_mute.GongGetReferenceIdentifier(stage))
	return
}

func (string_type *String_type) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", string_type.GongGetReferenceIdentifier(stage))
	return
}

func (strong_accent *Strong_accent) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", strong_accent.GongGetReferenceIdentifier(stage))
	return
}

func (style_text *Style_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", style_text.GongGetReferenceIdentifier(stage))
	return
}

func (supports *Supports) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", supports.GongGetReferenceIdentifier(stage))
	return
}

func (swing *Swing) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", swing.GongGetReferenceIdentifier(stage))
	return
}

func (sync *Sync) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", sync.GongGetReferenceIdentifier(stage))
	return
}

func (system_dividers *System_dividers) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_dividers.GongGetReferenceIdentifier(stage))
	return
}

func (system_layout *System_layout) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_layout.GongGetReferenceIdentifier(stage))
	return
}

func (system_margins *System_margins) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", system_margins.GongGetReferenceIdentifier(stage))
	return
}

func (tap *Tap) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tap.GongGetReferenceIdentifier(stage))
	return
}

func (technical *Technical) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", technical.GongGetReferenceIdentifier(stage))
	return
}

func (text_element_data *Text_element_data) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", text_element_data.GongGetReferenceIdentifier(stage))
	return
}

func (tie *Tie) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tie.GongGetReferenceIdentifier(stage))
	return
}

func (tied *Tied) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tied.GongGetReferenceIdentifier(stage))
	return
}

func (time *Time) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", time.GongGetReferenceIdentifier(stage))
	return
}

func (time_modification *Time_modification) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", time_modification.GongGetReferenceIdentifier(stage))
	return
}

func (timpani *Timpani) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", timpani.GongGetReferenceIdentifier(stage))
	return
}

func (transpose *Transpose) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", transpose.GongGetReferenceIdentifier(stage))
	return
}

func (tremolo *Tremolo) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tremolo.GongGetReferenceIdentifier(stage))
	return
}

func (tuplet *Tuplet) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet.GongGetReferenceIdentifier(stage))
	return
}

func (tuplet_dot *Tuplet_dot) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_dot.GongGetReferenceIdentifier(stage))
	return
}

func (tuplet_number *Tuplet_number) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_number.GongGetReferenceIdentifier(stage))
	return
}

func (tuplet_portion *Tuplet_portion) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_portion.GongGetReferenceIdentifier(stage))
	return
}

func (tuplet_type *Tuplet_type) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", tuplet_type.GongGetReferenceIdentifier(stage))
	return
}

func (typed_text *Typed_text) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", typed_text.GongGetReferenceIdentifier(stage))
	return
}

func (unpitched *Unpitched) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", unpitched.GongGetReferenceIdentifier(stage))
	return
}

func (virtual_instrument *Virtual_instrument) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", virtual_instrument.GongGetReferenceIdentifier(stage))
	return
}

func (wait *Wait) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wait.GongGetReferenceIdentifier(stage))
	return
}

func (wavy_line *Wavy_line) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wavy_line.GongGetReferenceIdentifier(stage))
	return
}

func (wedge *Wedge) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wedge.GongGetReferenceIdentifier(stage))
	return
}

func (wood *Wood) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", wood.GongGetReferenceIdentifier(stage))
	return
}

func (work *Work) GongMarshallUnstaging(stage *Stage) (decl string) {
	decl = GongUnstageStmt
	decl = strings.ReplaceAll(decl, "{{Identifier}}", work.GongGetReferenceIdentifier(stage))
	return
}

// end of template
