// generated by gong
import { Component, OnInit, Inject, Optional } from '@angular/core';
import { TypeofExpr } from '@angular/compiler';
import { CdkDragDrop, moveItemInArray } from '@angular/cdk/drag-drop';

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData } from '../front-repo.service'
import { SelectionModel } from '@angular/cdk/collections';

import { Router, RouterState } from '@angular/router';
import { SliceOfPointerToGongStructFieldDB } from '../sliceofpointertogongstructfield-db'
import { SliceOfPointerToGongStructFieldService } from '../sliceofpointertogongstructfield.service'

import { FrontRepoService, FrontRepo } from '../front-repo.service'
import { NullInt64 } from '../null-int64'

@Component({
  selector: 'lib-sliceofpointertogongstructfield-sorting',
  templateUrl: './sliceofpointertogongstructfield-sorting.component.html',
  styleUrls: ['./sliceofpointertogongstructfield-sorting.component.css']
})
export class SliceOfPointerToGongStructFieldSortingComponent implements OnInit {

  frontRepo: FrontRepo = new (FrontRepo)

  // array of SliceOfPointerToGongStructField instances that are in the association
  associatedSliceOfPointerToGongStructFields = new Array<SliceOfPointerToGongStructFieldDB>();

  constructor(
    private sliceofpointertogongstructfieldService: SliceOfPointerToGongStructFieldService,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of sliceofpointertogongstructfield instances
    public dialogRef: MatDialogRef<SliceOfPointerToGongStructFieldSortingComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
  ) {
    this.router.routeReuseStrategy.shouldReuseRoute = function () {
      return false;
    };
  }

  ngOnInit(): void {
    this.getSliceOfPointerToGongStructFields()
  }

  getSliceOfPointerToGongStructFields(): void {
    this.frontRepoService.pull(this.dialogData.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        let index = 0
        for (let sliceofpointertogongstructfield of this.frontRepo.SliceOfPointerToGongStructFields_array) {
          let ID = this.dialogData.ID
          let revPointerID = sliceofpointertogongstructfield[this.dialogData.ReversePointer as keyof SliceOfPointerToGongStructFieldDB] as unknown as NullInt64
          let revPointerID_Index = sliceofpointertogongstructfield[this.dialogData.ReversePointer + "_Index" as keyof SliceOfPointerToGongStructFieldDB] as unknown as NullInt64
          if (revPointerID.Int64 == ID) {
            if (revPointerID_Index == undefined) {
              revPointerID_Index = new NullInt64
              revPointerID_Index.Valid = true
              revPointerID_Index.Int64 = index++
            }
            this.associatedSliceOfPointerToGongStructFields.push(sliceofpointertogongstructfield)
          }
        }

        // sort associated sliceofpointertogongstructfield according to order
        this.associatedSliceOfPointerToGongStructFields.sort((t1, t2) => {
          let t1_revPointerID_Index = t1[this.dialogData.ReversePointer + "_Index" as keyof typeof t1] as unknown as NullInt64
          let t2_revPointerID_Index = t2[this.dialogData.ReversePointer + "_Index" as keyof typeof t2] as unknown as NullInt64
          if (t1_revPointerID_Index && t2_revPointerID_Index) {
            if (t1_revPointerID_Index.Int64 > t2_revPointerID_Index.Int64) {
              return 1;
            }
            if (t1_revPointerID_Index.Int64 < t2_revPointerID_Index.Int64) {
              return -1;
            }
          }
          return 0;
        });
      }
    )
  }

  drop(event: CdkDragDrop<string[]>) {
    moveItemInArray(this.associatedSliceOfPointerToGongStructFields, event.previousIndex, event.currentIndex);

    // set the order of SliceOfPointerToGongStructField instances
    let index = 0

    for (let sliceofpointertogongstructfield of this.associatedSliceOfPointerToGongStructFields) {
      let revPointerID_Index = sliceofpointertogongstructfield[this.dialogData.ReversePointer + "_Index" as keyof SliceOfPointerToGongStructFieldDB] as unknown as NullInt64
      revPointerID_Index.Valid = true
      revPointerID_Index.Int64 = index++
    }
  }

  save() {

    this.associatedSliceOfPointerToGongStructFields.forEach(
      sliceofpointertogongstructfield => {
        this.sliceofpointertogongstructfieldService.updateSliceOfPointerToGongStructField(sliceofpointertogongstructfield, this.dialogData.GONG__StackPath)
          .subscribe(sliceofpointertogongstructfield => {
            this.sliceofpointertogongstructfieldService.SliceOfPointerToGongStructFieldServiceChanged.next("update")
          });
      }
    )

    this.dialogRef.close('Sorting of ' + this.dialogData.ReversePointer + ' done');
  }
}
