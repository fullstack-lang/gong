// generated by gong
import { Component, OnInit, AfterViewInit, ViewChild, Inject, Optional, Input } from '@angular/core';
import { BehaviorSubject } from 'rxjs'
import { MatSort } from '@angular/material/sort';
import { MatPaginator } from '@angular/material/paginator';
import { MatTableDataSource } from '@angular/material/table';
import { MatButton } from '@angular/material/button'

import { MatDialogRef, MAT_DIALOG_DATA, MatDialog } from '@angular/material/dialog'
import { DialogData, FrontRepoService, FrontRepo, SelectionMode } from '../front-repo.service'
import { NullInt64 } from '../null-int64'
import { SelectionModel } from '@angular/cdk/collections';

const allowMultiSelect = true;

import { ActivatedRoute, Router, RouterState } from '@angular/router';
import { PointerToGongStructFieldDB } from '../pointertogongstructfield-db'
import { PointerToGongStructFieldService } from '../pointertogongstructfield.service'

// insertion point for additional imports

import { RouteService } from '../route-service';

// TableComponent is initilizaed from different routes
// TableComponentMode detail different cases 
enum TableComponentMode {
  DISPLAY_MODE,
  ONE_MANY_ASSOCIATION_MODE,
  MANY_MANY_ASSOCIATION_MODE,
}

// generated table component
@Component({
  selector: 'app-pointertogongstructfieldstable',
  templateUrl: './pointertogongstructfields-table.component.html',
  styleUrls: ['./pointertogongstructfields-table.component.css'],
})
export class PointerToGongStructFieldsTableComponent implements OnInit {

  @Input() GONG__StackPath: string = ""

  // mode at invocation
  mode: TableComponentMode = TableComponentMode.DISPLAY_MODE

  // used if the component is called as a selection component of PointerToGongStructField instances
  selection: SelectionModel<PointerToGongStructFieldDB> = new (SelectionModel)
  initialSelection = new Array<PointerToGongStructFieldDB>()

  // the data source for the table
  pointertogongstructfields: PointerToGongStructFieldDB[] = []
  matTableDataSource: MatTableDataSource<PointerToGongStructFieldDB> = new (MatTableDataSource)

  // front repo, that will be referenced by this.pointertogongstructfields
  frontRepo: FrontRepo = new (FrontRepo)

  // displayedColumns is referenced by the MatTable component for specify what columns
  // have to be displayed and in what order
  displayedColumns: string[];

  // for sorting & pagination
  @ViewChild(MatSort)
  sort: MatSort | undefined
  @ViewChild(MatPaginator)
  paginator: MatPaginator | undefined;

  ngAfterViewInit() {

    // enable sorting on all fields (including pointers and reverse pointer)
    this.matTableDataSource.sortingDataAccessor = (pointertogongstructfieldDB: PointerToGongStructFieldDB, property: string) => {
      switch (property) {
        case 'ID':
          return pointertogongstructfieldDB.ID

        // insertion point for specific sorting accessor
        case 'Name':
          return pointertogongstructfieldDB.Name;

        case 'GongStruct':
          return (pointertogongstructfieldDB.GongStruct ? pointertogongstructfieldDB.GongStruct.Name : '');

        case 'Index':
          return pointertogongstructfieldDB.Index;

        case 'CompositeStructName':
          return pointertogongstructfieldDB.CompositeStructName;

        case 'GongStruct_PointerToGongStructFields':
          if (this.frontRepo.GongStructs.get(pointertogongstructfieldDB.GongStruct_PointerToGongStructFieldsDBID.Int64) != undefined) {
            return this.frontRepo.GongStructs.get(pointertogongstructfieldDB.GongStruct_PointerToGongStructFieldsDBID.Int64)!.Name
          } else {
            return ""
          }

        default:
          console.assert(false, "Unknown field")
          return "";
      }
    };

    // enable filtering on all fields (including pointers and reverse pointer, which is not done by default)
    this.matTableDataSource.filterPredicate = (pointertogongstructfieldDB: PointerToGongStructFieldDB, filter: string) => {

      // filtering is based on finding a lower case filter into a concatenated string
      // the pointertogongstructfieldDB properties
      let mergedContent = ""

      // insertion point for merging of fields
      mergedContent += pointertogongstructfieldDB.Name.toLowerCase()
      if (pointertogongstructfieldDB.GongStruct) {
        mergedContent += pointertogongstructfieldDB.GongStruct.Name.toLowerCase()
      }
      mergedContent += pointertogongstructfieldDB.Index.toString()
      mergedContent += pointertogongstructfieldDB.CompositeStructName.toLowerCase()
      if (pointertogongstructfieldDB.GongStruct_PointerToGongStructFieldsDBID.Int64 != 0) {
        mergedContent += this.frontRepo.GongStructs.get(pointertogongstructfieldDB.GongStruct_PointerToGongStructFieldsDBID.Int64)!.Name.toLowerCase()
      }


      let isSelected = mergedContent.includes(filter.toLowerCase())
      return isSelected
    };

    this.matTableDataSource.sort = this.sort!
    this.matTableDataSource.paginator = this.paginator!
  }

  applyFilter(event: Event) {
    const filterValue = (event.target as HTMLInputElement).value;
    this.matTableDataSource.filter = filterValue.trim().toLowerCase();
  }

  constructor(
    private pointertogongstructfieldService: PointerToGongStructFieldService,
    private frontRepoService: FrontRepoService,

    // not null if the component is called as a selection component of pointertogongstructfield instances
    public dialogRef: MatDialogRef<PointerToGongStructFieldsTableComponent>,
    @Optional() @Inject(MAT_DIALOG_DATA) public dialogData: DialogData,

    private router: Router,
    private activatedRoute: ActivatedRoute,

    private routeService: RouteService,
  ) {

    // compute mode
    if (dialogData == undefined) {
      this.mode = TableComponentMode.DISPLAY_MODE
    } else {
      this.GONG__StackPath = dialogData.GONG__StackPath
      switch (dialogData.SelectionMode) {
        case SelectionMode.ONE_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.ONE_MANY_ASSOCIATION_MODE
          break
        case SelectionMode.MANY_MANY_ASSOCIATION_MODE:
          this.mode = TableComponentMode.MANY_MANY_ASSOCIATION_MODE
          break
        default:
      }
    }

    // observable for changes in structs
    this.pointertogongstructfieldService.PointerToGongStructFieldServiceChanged.subscribe(
      message => {
        if (message == "post" || message == "update" || message == "delete") {
          this.getPointerToGongStructFields()
        }
      }
    )
    if (this.mode == TableComponentMode.DISPLAY_MODE) {
      this.displayedColumns = ['ID', 'Delete', // insertion point for columns to display
        "Name",
        "GongStruct",
        "Index",
        "CompositeStructName",
        "GongStruct_PointerToGongStructFields",
      ]
    } else {
      this.displayedColumns = ['select', 'ID', // insertion point for columns to display
        "Name",
        "GongStruct",
        "Index",
        "CompositeStructName",
        "GongStruct_PointerToGongStructFields",
      ]
      this.selection = new SelectionModel<PointerToGongStructFieldDB>(allowMultiSelect, this.initialSelection);
    }

  }

  ngOnInit(): void {
    let stackPath = this.activatedRoute.snapshot.paramMap.get('GONG__StackPath')
    if (stackPath != undefined) {
      this.GONG__StackPath = stackPath
    }

    this.getPointerToGongStructFields()

    this.matTableDataSource = new MatTableDataSource(this.pointertogongstructfields)
  }

  getPointerToGongStructFields(): void {
    this.frontRepoService.pull(this.GONG__StackPath).subscribe(
      frontRepo => {
        this.frontRepo = frontRepo

        this.pointertogongstructfields = this.frontRepo.PointerToGongStructFields_array;

        // insertion point for time duration Recoveries
        // insertion point for enum int Recoveries

        // in case the component is called as a selection component
        if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {
          for (let pointertogongstructfield of this.pointertogongstructfields) {
            let ID = this.dialogData.ID
            let revPointer = pointertogongstructfield[this.dialogData.ReversePointer as keyof PointerToGongStructFieldDB] as unknown as NullInt64
            if (revPointer.Int64 == ID) {
              this.initialSelection.push(pointertogongstructfield)
            }
            this.selection = new SelectionModel<PointerToGongStructFieldDB>(allowMultiSelect, this.initialSelection);
          }
        }

        if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

          let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, PointerToGongStructFieldDB>
          let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

          // we associates on sourceInstance of type SourceStruct with a MANY MANY associations to PointerToGongStructFieldDB
          // the field name is sourceField
          let sourceFieldArray = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]! as unknown as PointerToGongStructFieldDB[]
          if (sourceFieldArray != null) {
            for (let associationInstance of sourceFieldArray) {
              let pointertogongstructfield = associationInstance[this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as PointerToGongStructFieldDB
              this.initialSelection.push(pointertogongstructfield)
            }
          }

          this.selection = new SelectionModel<PointerToGongStructFieldDB>(allowMultiSelect, this.initialSelection);
        }

        // update the mat table data source
        this.matTableDataSource.data = this.pointertogongstructfields
      }
    )
  }

  // newPointerToGongStructField initiate a new pointertogongstructfield
  // create a new PointerToGongStructField objet
  newPointerToGongStructField() {
  }

  deletePointerToGongStructField(pointertogongstructfieldID: number, pointertogongstructfield: PointerToGongStructFieldDB) {
    // list of pointertogongstructfields is truncated of pointertogongstructfield before the delete
    this.pointertogongstructfields = this.pointertogongstructfields.filter(h => h !== pointertogongstructfield);

    this.pointertogongstructfieldService.deletePointerToGongStructField(pointertogongstructfieldID, this.GONG__StackPath).subscribe(
      pointertogongstructfield => {
        this.pointertogongstructfieldService.PointerToGongStructFieldServiceChanged.next("delete")
      }
    );
  }

  editPointerToGongStructField(pointertogongstructfieldID: number, pointertogongstructfield: PointerToGongStructFieldDB) {

  }

  // set editor outlet
  setEditorRouterOutlet(pointertogongstructfieldID: number) {
    let outletName = this.routeService.getEditorOutlet(this.GONG__StackPath)
    let fullPath = this.routeService.getPathRoot() + "-" + "pointertogongstructfield" + "-detail"

    let outletConf: any = {}
    outletConf[outletName] = [fullPath, pointertogongstructfieldID, this.GONG__StackPath]

    this.router.navigate([{ outlets: outletConf }])
  }

  /** Whether the number of selected elements matches the total number of rows. */
  isAllSelected() {
    const numSelected = this.selection.selected.length;
    const numRows = this.pointertogongstructfields.length;
    return numSelected === numRows;
  }

  /** Selects all rows if they are not all selected; otherwise clear selection. */
  masterToggle() {
    this.isAllSelected() ?
      this.selection.clear() :
      this.pointertogongstructfields.forEach(row => this.selection.select(row));
  }

  save() {

    if (this.mode == TableComponentMode.ONE_MANY_ASSOCIATION_MODE) {

      let toUpdate = new Set<PointerToGongStructFieldDB>()

      // reset all initial selection of pointertogongstructfield that belong to pointertogongstructfield
      for (let pointertogongstructfield of this.initialSelection) {
        let index = pointertogongstructfield[this.dialogData.ReversePointer as keyof PointerToGongStructFieldDB] as unknown as NullInt64
        index.Int64 = 0
        index.Valid = true
        toUpdate.add(pointertogongstructfield)

      }

      // from selection, set pointertogongstructfield that belong to pointertogongstructfield
      for (let pointertogongstructfield of this.selection.selected) {
        let ID = this.dialogData.ID as number
        let reversePointer = pointertogongstructfield[this.dialogData.ReversePointer as keyof PointerToGongStructFieldDB] as unknown as NullInt64
        reversePointer.Int64 = ID
        reversePointer.Valid = true
        toUpdate.add(pointertogongstructfield)
      }


      // update all pointertogongstructfield (only update selection & initial selection)
      for (let pointertogongstructfield of toUpdate) {
        this.pointertogongstructfieldService.updatePointerToGongStructField(pointertogongstructfield, this.GONG__StackPath)
          .subscribe(pointertogongstructfield => {
            this.pointertogongstructfieldService.PointerToGongStructFieldServiceChanged.next("update")
          });
      }
    }

    if (this.mode == TableComponentMode.MANY_MANY_ASSOCIATION_MODE) {

      // get the source instance via the map of instances in the front repo
      let mapOfSourceInstances = this.frontRepo[this.dialogData.SourceStruct + "s" as keyof FrontRepo] as Map<number, PointerToGongStructFieldDB>
      let sourceInstance = mapOfSourceInstances.get(this.dialogData.ID)!

      // First, parse all instance of the association struct and remove the instance
      // that have unselect
      let unselectedPointerToGongStructField = new Set<number>()
      for (let pointertogongstructfield of this.initialSelection) {
        if (this.selection.selected.includes(pointertogongstructfield)) {
          // console.log("pointertogongstructfield " + pointertogongstructfield.Name + " is still selected")
        } else {
          console.log("pointertogongstructfield " + pointertogongstructfield.Name + " has been unselected")
          unselectedPointerToGongStructField.add(pointertogongstructfield.ID)
          console.log("is unselected " + unselectedPointerToGongStructField.has(pointertogongstructfield.ID))
        }
      }

      // delete the association instance
      let associationInstance = sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]
      let pointertogongstructfield = associationInstance![this.dialogData.IntermediateStructField as keyof typeof associationInstance] as unknown as PointerToGongStructFieldDB
      if (unselectedPointerToGongStructField.has(pointertogongstructfield.ID)) {
        this.frontRepoService.deleteService(this.dialogData.IntermediateStruct, associationInstance)


      }

      // is the source array is empty create it
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] == undefined) {
        (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance] as unknown as Array<PointerToGongStructFieldDB>) = new Array<PointerToGongStructFieldDB>()
      }

      // second, parse all instance of the selected
      if (sourceInstance[this.dialogData.SourceField as keyof typeof sourceInstance]) {
        this.selection.selected.forEach(
          pointertogongstructfield => {
            if (!this.initialSelection.includes(pointertogongstructfield)) {
              // console.log("pointertogongstructfield " + pointertogongstructfield.Name + " has been added to the selection")

              let associationInstance = {
                Name: sourceInstance["Name"] + "-" + pointertogongstructfield.Name,
              }

              let index = associationInstance[this.dialogData.IntermediateStructField + "ID" as keyof typeof associationInstance] as unknown as NullInt64
              index.Int64 = pointertogongstructfield.ID
              index.Valid = true

              let indexDB = associationInstance[this.dialogData.IntermediateStructField + "DBID" as keyof typeof associationInstance] as unknown as NullInt64
              indexDB.Int64 = pointertogongstructfield.ID
              index.Valid = true

              this.frontRepoService.postService(this.dialogData.IntermediateStruct, associationInstance)

            } else {
              // console.log("pointertogongstructfield " + pointertogongstructfield.Name + " is still selected")
            }
          }
        )
      }

      // this.selection = new SelectionModel<PointerToGongStructFieldDB>(allowMultiSelect, this.initialSelection);
    }

    // why pizza ?
    this.dialogRef.close('Pizza!');
  }
}
