// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { AclassBclassUseAPI } from './aclassbclassuse-api';
import { AclassBclassUseDB } from './aclassbclassuse-db';

@Injectable({
  providedIn: 'root'
})
export class AclassBclassUseService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  AclassBclassUseServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private aclassbclassusesUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/test/go/v1/aclassbclassuses';

  constructor(
    private http: HttpClient
  ) { }

  /** GET aclassbclassuses from the server */
  getAclassBclassUses(): Observable<AclassBclassUseDB[]> {
    return this.http.get<AclassBclassUseDB[]>(this.aclassbclassusesUrl)
      .pipe(
        tap(_ => this.log('fetched aclassbclassuses')),
        catchError(this.handleError<AclassBclassUseDB[]>('getAclassBclassUses', []))
      );
  }

  /** GET aclassbclassuse by id. Will 404 if id not found */
  getAclassBclassUse(id: number): Observable<AclassBclassUseDB> {
    const url = `${this.aclassbclassusesUrl}/${id}`;
    return this.http.get<AclassBclassUseDB>(url).pipe(
      tap(_ => this.log(`fetched aclassbclassuse id=${id}`)),
      catchError(this.handleError<AclassBclassUseDB>(`getAclassBclassUse id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new aclassbclassuse to the server */
  postAclassBclassUse(aclassbclassusedb: AclassBclassUseDB): Observable<AclassBclassUseDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    aclassbclassusedb.Bclass2 = {}
    let _Aclass_AnarrayofbUse_reverse = aclassbclassusedb.Aclass_AnarrayofbUse_reverse
    aclassbclassusedb.Aclass_AnarrayofbUse_reverse = {}

		return this.http.post<AclassBclassUseDB>(this.aclassbclassusesUrl, aclassbclassusedb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
        aclassbclassusedb.Aclass_AnarrayofbUse_reverse = _Aclass_AnarrayofbUse_reverse
				this.log(`posted aclassbclassusedb id=${aclassbclassusedb.ID}`)
			}),
			catchError(this.handleError<AclassBclassUseDB>('postAclassBclassUse'))
		);
  }

  /** DELETE: delete the aclassbclassusedb from the server */
  deleteAclassBclassUse(aclassbclassusedb: AclassBclassUseDB | number): Observable<AclassBclassUseDB> {
    const id = typeof aclassbclassusedb === 'number' ? aclassbclassusedb : aclassbclassusedb.ID;
    const url = `${this.aclassbclassusesUrl}/${id}`;

    return this.http.delete<AclassBclassUseDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted aclassbclassusedb id=${id}`)),
      catchError(this.handleError<AclassBclassUseDB>('deleteAclassBclassUse'))
    );
  }

  /** PUT: update the aclassbclassusedb on the server */
  updateAclassBclassUse(aclassbclassusedb: AclassBclassUseDB): Observable<AclassBclassUseDB> {
    const id = typeof aclassbclassusedb === 'number' ? aclassbclassusedb : aclassbclassusedb.ID;
    const url = `${this.aclassbclassusesUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    aclassbclassusedb.Bclass2 = {}
    let _Aclass_AnarrayofbUse_reverse = aclassbclassusedb.Aclass_AnarrayofbUse_reverse
    aclassbclassusedb.Aclass_AnarrayofbUse_reverse = {}

    return this.http.put(url, aclassbclassusedb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        aclassbclassusedb.Aclass_AnarrayofbUse_reverse = _Aclass_AnarrayofbUse_reverse
        this.log(`updated aclassbclassusedb id=${aclassbclassusedb.ID}`)
      }),
      catchError(this.handleError<AclassBclassUseDB>('updateAclassBclassUse'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
