// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { CclassDB } from './cclass-db';

@Injectable({
  providedIn: 'root'
})
export class CclassService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  CclassServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private cclasssUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/test/go/v1/cclasss';

  constructor(
    private http: HttpClient
  ) { }

  /** GET cclasss from the server */
  getCclasss(): Observable<CclassDB[]> {
    return this.http.get<CclassDB[]>(this.cclasssUrl)
      .pipe(
        tap(_ => this.log('fetched cclasss')),
        catchError(this.handleError<CclassDB[]>('getCclasss', []))
      );
  }

  /** GET cclass by id. Will 404 if id not found */
  getCclass(id: number): Observable<CclassDB> {
    const url = `${this.cclasssUrl}/${id}`;
    return this.http.get<CclassDB>(url).pipe(
      tap(_ => this.log(`fetched cclass id=${id}`)),
      catchError(this.handleError<CclassDB>(`getCclass id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new cclass to the server */
  postCclass(cclassdb: CclassDB): Observable<CclassDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

		return this.http.post<CclassDB>(this.cclasssUrl, cclassdb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
				this.log(`posted cclassdb id=${cclassdb.ID}`)
			}),
			catchError(this.handleError<CclassDB>('postCclass'))
		);
  }

  /** DELETE: delete the cclassdb from the server */
  deleteCclass(cclassdb: CclassDB | number): Observable<CclassDB> {
    const id = typeof cclassdb === 'number' ? cclassdb : cclassdb.ID;
    const url = `${this.cclasssUrl}/${id}`;

    return this.http.delete<CclassDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted cclassdb id=${id}`)),
      catchError(this.handleError<CclassDB>('deleteCclass'))
    );
  }

  /** PUT: update the cclassdb on the server */
  updateCclass(cclassdb: CclassDB): Observable<CclassDB> {
    const id = typeof cclassdb === 'number' ? cclassdb : cclassdb.ID;
    const url = `${this.cclasssUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put(url, cclassdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated cclassdb id=${cclassdb.ID}`)
      }),
      catchError(this.handleError<CclassDB>('updateCclass'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
