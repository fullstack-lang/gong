// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { DclassDB } from './dclass-db';

@Injectable({
  providedIn: 'root'
})
export class DclassService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  DclassServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private dclasssUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/test/go/v1/dclasss';

  constructor(
    private http: HttpClient
  ) { }

  /** GET dclasss from the server */
  getDclasss(): Observable<DclassDB[]> {
    return this.http.get<DclassDB[]>(this.dclasssUrl)
      .pipe(
        tap(_ => this.log('fetched dclasss')),
        catchError(this.handleError<DclassDB[]>('getDclasss', []))
      );
  }

  /** GET dclass by id. Will 404 if id not found */
  getDclass(id: number): Observable<DclassDB> {
    const url = `${this.dclasssUrl}/${id}`;
    return this.http.get<DclassDB>(url).pipe(
      tap(_ => this.log(`fetched dclass id=${id}`)),
      catchError(this.handleError<DclassDB>(`getDclass id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new dclass to the server */
  postDclass(dclassdb: DclassDB): Observable<DclassDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

		return this.http.post<DclassDB>(this.dclasssUrl, dclassdb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
				this.log(`posted dclassdb id=${dclassdb.ID}`)
			}),
			catchError(this.handleError<DclassDB>('postDclass'))
		);
  }

  /** DELETE: delete the dclassdb from the server */
  deleteDclass(dclassdb: DclassDB | number): Observable<DclassDB> {
    const id = typeof dclassdb === 'number' ? dclassdb : dclassdb.ID;
    const url = `${this.dclasssUrl}/${id}`;

    return this.http.delete<DclassDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted dclassdb id=${id}`)),
      catchError(this.handleError<DclassDB>('deleteDclass'))
    );
  }

  /** PUT: update the dclassdb on the server */
  updateDclass(dclassdb: DclassDB): Observable<DclassDB> {
    const id = typeof dclassdb === 'number' ? dclassdb : dclassdb.ID;
    const url = `${this.dclasssUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put(url, dclassdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated dclassdb id=${dclassdb.ID}`)
      }),
      catchError(this.handleError<DclassDB>('updateDclass'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
