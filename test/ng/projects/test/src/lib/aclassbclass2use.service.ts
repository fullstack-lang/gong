// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { AclassBclass2UseAPI } from './aclassbclass2use-api';
import { AclassBclass2UseDB } from './aclassbclass2use-db';

@Injectable({
  providedIn: 'root'
})
export class AclassBclass2UseService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  AclassBclass2UseServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private aclassbclass2usesUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gong/test/go/v1/aclassbclass2uses';

  constructor(
    private http: HttpClient
  ) { }

  /** GET aclassbclass2uses from the server */
  getAclassBclass2Uses(): Observable<AclassBclass2UseDB[]> {
    return this.http.get<AclassBclass2UseDB[]>(this.aclassbclass2usesUrl)
      .pipe(
        tap(_ => this.log('fetched aclassbclass2uses')),
        catchError(this.handleError<AclassBclass2UseDB[]>('getAclassBclass2Uses', []))
      );
  }

  /** GET aclassbclass2use by id. Will 404 if id not found */
  getAclassBclass2Use(id: number): Observable<AclassBclass2UseDB> {
    const url = `${this.aclassbclass2usesUrl}/${id}`;
    return this.http.get<AclassBclass2UseDB>(url).pipe(
      tap(_ => this.log(`fetched aclassbclass2use id=${id}`)),
      catchError(this.handleError<AclassBclass2UseDB>(`getAclassBclass2Use id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new aclassbclass2use to the server */
  postAclassBclass2Use(aclassbclass2usedb: AclassBclass2UseDB): Observable<AclassBclass2UseDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    aclassbclass2usedb.Bclass2 = {}
    let _Aclass_Anarrayofb2Use_reverse = aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse
    aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse = {}

		return this.http.post<AclassBclass2UseDB>(this.aclassbclass2usesUrl, aclassbclass2usedb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
        aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse = _Aclass_Anarrayofb2Use_reverse
				this.log(`posted aclassbclass2usedb id=${aclassbclass2usedb.ID}`)
			}),
			catchError(this.handleError<AclassBclass2UseDB>('postAclassBclass2Use'))
		);
  }

  /** DELETE: delete the aclassbclass2usedb from the server */
  deleteAclassBclass2Use(aclassbclass2usedb: AclassBclass2UseDB | number): Observable<AclassBclass2UseDB> {
    const id = typeof aclassbclass2usedb === 'number' ? aclassbclass2usedb : aclassbclass2usedb.ID;
    const url = `${this.aclassbclass2usesUrl}/${id}`;

    return this.http.delete<AclassBclass2UseDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted aclassbclass2usedb id=${id}`)),
      catchError(this.handleError<AclassBclass2UseDB>('deleteAclassBclass2Use'))
    );
  }

  /** PUT: update the aclassbclass2usedb on the server */
  updateAclassBclass2Use(aclassbclass2usedb: AclassBclass2UseDB): Observable<AclassBclass2UseDB> {
    const id = typeof aclassbclass2usedb === 'number' ? aclassbclass2usedb : aclassbclass2usedb.ID;
    const url = `${this.aclassbclass2usesUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    aclassbclass2usedb.Bclass2 = {}
    let _Aclass_Anarrayofb2Use_reverse = aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse
    aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse = {}

    return this.http.put(url, aclassbclass2usedb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        aclassbclass2usedb.Aclass_Anarrayofb2Use_reverse = _Aclass_Anarrayofb2Use_reverse
        this.log(`updated aclassbclass2usedb id=${aclassbclass2usedb.ID}`)
      }),
      catchError(this.handleError<AclassBclass2UseDB>('updateAclassBclass2Use'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
